---
phase: 07-column-lineage
plan: 04
type: execute
wave: 3
depends_on:
  - 03
files_modified:
  - binding/schema.mbt
  - binding/json.mbt
  - binding/exports.mbt
  - binding/catalog_json.mbt
  - binding/moon.pkg
  - fathom-sql/args.mbt
  - fathom-sql/run.mbt
  - fathom-sql/main.mbt
  - fathom-sql/cli_test.mbt
autonomous: true
requirements:
  - LINE-01
estimate:
  tokens: 70000
  raw_tokens: 46000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "schema v3 纯增第 8 命名空间（Pitfall V6）：binding/schema.mbt 增 `pub const LINEAGE_SCHEMA_VERSION : String = \"fathom.lineage.v1\"` + validate_schema_version 追加分支；既有 7 命名空间分支一行不动（parity/export_smoke_test.mbt 既有断言不破）"
    - "fathom_lineage_v1(raw, dialect, profile, mode, catalog_json : Bytes) -> Bytes（D-07/D-05）：dialect-first 校验（ParseOptions::new）→ flink 门禁（FATHOM-SCHEMA-003 + message \"lineage is Doris-only\"，D-08，RESEARCH A6）→ catalog_json 解析（空 bytes/\"{}\" = 无 catalog）→ @api.lineage_text → lineage_result_json envelope；catalog_json 非空非法 → 结构化错误（FATHOM-SCHEMA-004，仿 parse_overrides，never silent fallback，T-06-03-01/ASVS V5）"
    - "binding/moon.pkg js+wasm 两处 exports 列表各追加 \"fathom_lineage_v1\"（第 8 个，Pitfall 5：漏注册静默缺符号）；#export_name(\"fathom_lineage_v1\") 在 binding/ 声明（Pitfall 17：export 必须在产出 artifact 的包中声明）"
    - "catalog_json.mbt：JSON 形状 {tables[], db_tables[], functions[]}（RESEARCH A5）→ @api.StaticCatalog::new/with_db/with_function；非法 UTF-8/JSON/字段 → Err(String) → wire 层 FATHOM-SCHEMA-004（仿 exports.mbt parse_overrides:213-277）"
    - "CLI `fathom-sql lineage --dialect doris --profile <id> [--catalog <file>] [file|-]`（D-07/D-39）：0 = envelope 到 stdout（fathom.lineage.v1，edges+gaps）；1 = parse 失败；2 = 用法/配置错误（缺 dialect/profile、坏 --catalog 路径、catalog JSON 非法、flink 选择）；Command 增 catalog : String? 字段"
    - "FLAGGED ASSUMPTION (LINE-01 ordering, UNRESOLVED→RESOLVED): wire envelope 的 edges/gaps 数组顺序与 07-02 派生顺序一致（文档序）——07-05 parity 三目标字节一致依赖此顺序契约"
    - "FLAGGED ASSUMPTION (LINE-01 empty, UNRESOLVED→RESOLVED): catalog_json 空 bytes 与 \"{}\" 等价（无 catalog，D-05）；空文档 → envelope edges/gaps 均空数组"
    - "FLAGGED ASSUMPTION (LINE-01 concurrency/idempotency, UNRESOLVED→RESOLVED): fathom_lineage_v1 是纯函数（raw+dialect+profile+mode+catalog_json → Bytes）；并行调用同输入产出相同字节；CLI 无服务状态"
  artifacts:
    - binding/schema.mbt（LINEAGE_SCHEMA_VERSION 常量 + validate_schema_version 第 8 分支）
    - binding/json.mbt（lineage_result_json：envelope 含 dialect/profile/exact_release 元数据 + edges/gaps 数组）
    - binding/exports.mbt（#export_name(\"fathom_lineage_v1\") + catalog_json 解析接入）
    - binding/catalog_json.mbt（catalog JSON → StaticCatalog，结构化错误）
    - binding/moon.pkg（js+wasm exports 各增 fathom_lineage_v1）
    - fathom-sql/args.mbt（subcommand 白名单 + \"lineage\" + --catalog 标志 + Command.catalog 字段）
    - fathom-sql/run.mbt（run_lineage：D-39 0/1/2，读 --catalog 文件）
    - fathom-sql/main.mbt（\"lineage\" 分发）
    - fathom-sql/cli_test.mbt（lineage 退出码矩阵）
  key_links:
    - "fathom_lineage_v1 → catalog_json.mbt → @api.StaticCatalog → lineage_text：catalog 从 wire 字节到库面的唯一注入链（T-02-42 调用方注入，无 catalog 全 gap）"
    - "validate_schema_version 第 8 分支纯增 → parity/export_smoke_test.mbt（07-05）加法断言：既有 7 命名空间消费者零影响（Pitfall V6）"
    - "CLI 只调 @api 层（run_lineage → @api.lineage_text）——CLI 不含 lineage 逻辑（D-37/D-38）；D-39 0/1/2 映射复用 run_lint/run_fingerprint 模板"
  prohibitions:
    - statement: "永不新造 FATHOM-LINE-* 错误码或 fathom.lineage.flink 命名空间（D-08：复用 FATHOM-SCHEMA 族）——FLAGGED-UNVERIFIED（无 wired check，specless 描述符空）"
    - statement: "flink 选择永不经 fathom_lineage_v1/CLI 返回空 envelope（D-08：结构化 FATHOM-SCHEMA-003 错误，CLI exit 2）——FLAGGED-UNVERIFIED（无 wired check，specless 描述符空）"
    - statement: "catalog_json 非法输入永不静默忽略/回退空 catalog（T-06-03-01/ASVS V5：结构化 FATHOM-SCHEMA-004 错误）——FLAGGED-UNVERIFIED（无 wired check，specless 描述符空）"
    - statement: "wire/CLI 永不注册漏掉 fathom_lineage_v1 到 js/wasm exports（Pitfall 5：漏注册静默缺符号）——FLAGGED-UNVERIFIED（无 wired check，specless 描述符空）"
---

<objective>
交付 **LINE-01** 的宿主消费面：`fathom.lineage.v1` wire 导出（第 8 命名空间，schema v3 纯增）+ `binding/catalog_json.mbt`（catalog JSON → StaticCatalog）+ `fathom-sql lineage --catalog <file>` 子命令（D-39 退出码 0/1/2）。

Purpose: ROADMAP SC1/SC2 的用户承诺需要宿主可消费的 ABI——wire 导出供 Native/JS/Wasm 宿主（07-05 parity 证明跨端一致），CLI 子命令供终端用户直接运行血缘检查（含 `--catalog` 注入，D-05）。完全对齐 Phase 6 模板（07-CONTEXT D-07 明文）。
Output: `fathom.lineage.v1` wire + catalog JSON 解析 + CLI `lineage` 子命令。

Execution note（scope_sanity）：本 plan files_modified=9，3 tasks——Task 1（schema+json+export+moon.pkg tracer）、Task 2（catalog_json）、Task 3（CLI）。**按 task 原子提交**；Task 1 的 tracer 先跑通 `fathom_lineage_v1(b\"SELECT a FROM t\", \"doris\", \"4.x\", \"strict\", b\"\")` → envelope 的最短链路（含 flink 门禁），再补 catalog_json 与 CLI。Task 3 依赖 Task 1/2 的 wire 与 catalog_json（CLI --catalog 复用）。
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-column-lineage/07-CONTEXT.md
@.planning/phases/07-column-lineage/07-RESEARCH.md
@.planning/phases/07-column-lineage/07-PATTERNS.md
@binding/schema.mbt
@binding/json.mbt
@binding/exports.mbt
@binding/moon.pkg
@fathom-sql/args.mbt
@fathom-sql/run.mbt
@fathom-sql/main.mbt
@api/api.mbt
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: fathom.lineage.v1 wire——schema 第 8 分支 + lineage_result_json + fathom_lineage_v1 + moon.pkg 注册</name>
  <files>
    binding/schema.mbt
    binding/json.mbt
    binding/exports.mbt
    binding/moon.pkg
  </files>
  <read_first>
    binding/schema.mbt validate_schema_version（schema.mbt:6-28——第 8 命名空间追加点）+ lint_result_json（schema.mbt:150-205 envelope 模板）+ schema_error_code（FATHOM-SCHEMA 码表）
    binding/exports.mbt fathom_lint_v1（exports.mbt:205-252——wire 导出模板）+ parse_overrides（exports.mbt:213-277——catalog_json 解析模板）+ dialect-first 校验注释（exports.mbt:41-45）
    binding/json.mbt byte_array_json + lint_result_json（json.mbt:150-205——edges/gaps 序列化模板，span 用 Json::number(x.to_double())）
    binding/moon.pkg（js+wasm exports 列表，第 8 个追加点）
    api/api.mbt lineage_text + LineageResult（07-03 产物）
    07-RESEARCH.md Pattern 5（wire 交付面：schema v2 bump/envelope/导出）+ Pitfall 5/6
    07-PATTERNS.md「4. wire 导出 + schema bump：fathom.lineage.v1（第 8 命名空间）」
  </read_first>
  <behavior>
    - Test 1（envelope）：fathom_lineage_v1(b"SELECT a FROM t", "doris", "4.x", "strict", b"") → 字节含 "fathom.lineage.v1"、"\"dialect\":\"doris\""、"\"profile\":\"4.x\""、"edges"、"gaps"
    - Test 2（flink 门禁）：fathom_lineage_v1(b"SELECT 1", "flink", "flink-2.3.0", "strict", b"") → 字节含 "FATHOM-SCHEMA-003" 且含 "lineage is Doris-only"，绝不空 envelope
    - Test 3（schema 加法）：validate_schema_version("fathom.lineage.v1") == Ok(())；既有 7 命名空间仍 Ok（Pitfall V6）
    - Test 4（未知 dialect/profile）：fathom_lineage_v1(..., "mysql", "4.x", "strict", b"") → fathom.error.v1 结构化错误（FATHOM-SCHEMA-007）
  </behavior>
  <action>
    在 `binding/schema.mbt` 增 `pub const LINEAGE_SCHEMA_VERSION : String = "fathom.lineage.v1"`，并把 `validate_schema_version`（schema.mbt:20-28）的 match 追加 `LINEAGE_SCHEMA_VERSION => Ok(())` 分支——**纯加法**（Pitfall V6），既有 7 命名空间分支一行不动。在 `binding/json.mbt` 新增 `pub fn lineage_result_json(source : Bytes, result : @api.LineageResult, dialect : String, profile : String, exact_release : String) -> String`：envelope 含 `"schema_version": Json::string("fathom.lineage.v1")`、`"source_transport": Json::string(SOURCE_TRANSPORT)`、dialect/profile/exact_release 元数据（D-09）、`"source_bytes": byte_array_json(source)`、`"edges"` 数组（每条 `{source_name, source_resolved_to, source_start_byte, source_end_byte, target_name, target_start_byte, target_end_byte}`，span 用 `Json::number(x.to_double())` ≤ 2^53 安全，json.mbt:7-12 先例）、`"gaps"` 数组（每条 `{code, message, start_byte, end_byte}`）——**边/gap 数组顺序即 07-02 派生顺序，公开契约**。在 `binding/exports.mbt` 新增 `#export_name("fathom_lineage_v1") pub fn fathom_lineage_v1(raw : Bytes, dialect : String, profile : String, mode : String, catalog_json : Bytes) -> Bytes`（仿 fathom_lint_v1）：`@api.ParseOptions::new(dialect, profile, mode)`（Err → parse_error_bytes）；**flink 门禁（D-08）**：`if dialect == "flink" { return error_bytes("FATHOM-SCHEMA-003", "lineage is Doris-only") }`（RESEARCH A6，明确 message，绝不静默空结果）；catalog 解析（Task 2 的 catalog_json.mbt，空 bytes/"{ }" → None）；调 `@api.lineage_text(raw, selection, catalog)` → Ok → `json_bytes(lineage_result_json(raw, result, dialect, profile, selection.exact_release()))` / Err → parse_error_bytes。在 `binding/moon.pkg` 的 js 与 wasm 两处 exports 列表各追加 `"fathom_lineage_v1",`（第 8 个，Pitfall 5 漏注册静默缺符号）。在 binding 包内加 test（export 面白盒）：Behavior Test 1-4（envelope 断言、flink 门禁、schema 加法、未知 dialect）。
  </action>
  <verify>
    <automated>moon check --target native && moon build --target js --package binding && moon test --target native --package binding</automated>
  </verify>
  <acceptance_criteria>
    - schema.mbt 含 LINEAGE_SCHEMA_VERSION + validate_schema_version 第 8 分支；既有 7 分支原样（git diff 确认仅追加）
    - fathom_lineage_v1 在 js+wasm 两处 exports 列表注册；binding 产物含该符号（moon build --target js 通过）
    - binding 包内 test 全绿：envelope 含 fathom.lineage.v1/dialect/profile/edges/gaps；flink → FATHOM-SCHEMA-003 + "lineage is Doris-only"；未知 dialect → fathom.error.v1（FATHOM-SCHEMA-007）
    - lineage_result_json 的 edges/gaps span 用 Json::number(to_double())（≤2^53 安全）；顺序与 07-02 派生一致
  </acceptance_criteria>
  <done>fathom.lineage.v1 wire envelope 可被 Native/JS/Wasm 宿主消费；schema 第 8 命名空间纯增且旧消费者零影响；flink 门禁在 wire 边界落地</done>
  <reversibility rating="one-way">D-07 wire 命名空间是公开 ABI；发布后改名需迁移所有宿主——决策已在 CONTEXT.md 锁定，不插 re-ask checkpoint</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: binding/catalog_json.mbt——catalog JSON → StaticCatalog + 结构化错误</name>
  <files>
    binding/catalog_json.mbt
    binding/exports.mbt
  </files>
  <read_first>
    binding/exports.mbt parse_overrides（exports.mbt:213-277——JSON parse → 结构化错误的完整模板：空 bytes 默认 / try-catch @utf8.decode + @json.parse / never silent fallback）
    analyzer/analyzer.mbt StaticCatalog::new/with_db/with_function（analyzer.mbt:58-90——catalog 构造器）
    api/api.mbt StaticCatalog 类型再导出（07-03 产物）
    07-RESEARCH.md Pattern 5（catalog JSON schema：{tables[], db_tables[], functions[]}，A5）+ Open Question 5
    07-PATTERNS.md「binding/catalog_json.mbt」
  </read_first>
  <behavior>
    - Test 1（空 bytes → None）：parse_catalog_json(b"") → Ok(None)（无 catalog，D-05）；parse_catalog_json(b"{}") → Ok(None)
    - Test 2（tables）：parse_catalog_json(b"{\"tables\":[{\"name\":\"t\",\"columns\":[{\"name\":\"a\",\"data_type\":\"INT\"}]}]}") → Ok(Some(StaticCatalog)) 含 t.a
    - Test 3（db_tables + functions）：db_tables 走 with_db、functions 走 with_function → Ok(Some(catalog)) 含 db.t 与 fn
    - Test 4（非法输入）：非 JSON / 非法 UTF-8 / 字段类型错误 → Err(String)（FATHOM-SCHEMA-004 上游），绝不静默忽略
  </behavior>
  <action>
    新建 `binding/catalog_json.mbt`：`pub fn parse_catalog_json(bytes : Bytes) -> Result[@api.StaticCatalog?, String]`——完全镜像 `parse_overrides`（exports.mbt:213-277）形状：(1) 空 bytes 或 `"{}"` → `Ok(None)`（无 catalog，D-05）；(2) `@utf8.decode` try/catch → 非法 UTF-8 → `Err("invalid UTF-8 in catalog JSON")`；(3) `@json.parse` try/catch → 非法 JSON → `Err("malformed JSON in catalog JSON")`；(4) JSON 形状（RESEARCH A5）`{tables: [{name, columns: [{name, data_type}]}], db_tables: [{db, name, table: {...}}], functions: [{name, param_types, return_type, min_arity}]}` → 逐项构造 `@api.StaticCatalog::new(tables)` + `with_db(db, name, table)` + `with_function(name, info)`（analyzer.mbt:58-90 构造器；case-sensitive keys 文档化，D-05/Phase 5 纪律）；字段缺失/类型错误 → `Err("... missing/expected ...")`。在 `binding/exports.mbt` 的 `fathom_lineage_v1`（Task 1）接入：`parse_catalog_json(catalog_json)` 的 Err(message) → `error_bytes("FATHOM-SCHEMA-004", message)`（T-06-03-01 never silent fallback，Open Question 5 推荐）；Ok(catalog?) → 传给 `@api.lineage_text`。在 binding 包内加 test（parse_catalog_json 正反例，Behavior Test 1-4）。
  </action>
  <verify>
    <automated>moon check --target native && moon test --target native --package binding</automated>
  </verify>
  <acceptance_criteria>
    - catalog_json.mbt 含 parse_catalog_json；空 bytes/"{}" → Ok(None)；tables/db_tables/functions 正确构造 StaticCatalog
    - 非法 UTF-8/JSON/字段 → Err(String)（上游 FATHOM-SCHEMA-004），fathom_lineage_v1 对坏 catalog_json 返回结构化错误而非静默空
    - binding 包内 test 全绿（Behavior Test 1-4）；catalog 列顺序 = JSON 数组顺序（LinkedHashMap 确定性，parity 契约）
  </acceptance_criteria>
  <done>catalog JSON 经 wire 面安全注入 StaticCatalog（非法输入结构化错误），无 catalog 缺省语义落地</done>
  <reversibility rating="one-way">D-05 wire 签名含 catalog_json 参数是公开 ABI；catalog JSON schema（A5）是 CLI/wire 的公开输入契约——决策已在 CONTEXT.md 锁定，不插 re-ask checkpoint</reversibility>
</task>

<task type="auto">
  <name>Task 3: fathom-sql lineage 子命令 + --catalog + D-39 退出码</name>
  <files>
    fathom-sql/args.mbt
    fathom-sql/run.mbt
    fathom-sql/main.mbt
    fathom-sql/cli_test.mbt
  </files>
  <read_first>
    fathom-sql/args.mbt parse_args + UsageError + Command（args.mbt:14-135——子命令白名单 + --rule 可重复 flag 解析模板 + pub(all) Command）
    fathom-sql/run.mbt run_lint/run_fingerprint + parse_options + parse_error_outcome + CliOutcome（run.mbt:142-290——D-39 0/1/2 映射模板）+ usage_text/usage_error_message（run.mbt:295-325）
    fathom-sql/main.mbt subcommand match + write_fd（main.mbt:31-63——分发 + 字节精确 stdout）
    fathom-sql/cli_test.mbt（Command 全字段字面量 + run_*_stdin_happy_path 先例）
    binding/exports.mbt fathom_lineage_v1（Task 1 产物）或 @api.lineage_text（07-03）
    api/api.mbt lineage_text（07-03 产物）
    07-RESEARCH.md Pattern 5（CLI：lineage --catalog + D-39 0/1/2）
    07-PATTERNS.md「5. CLI 子命令：fathom-sql lineage --catalog <file>」
  </read_first>
  <behavior>
    - Test 1（exit 0 envelope）：`lineage --dialect doris --profile 4.x` 输入 `SELECT a FROM t` → exit 0、stdout 为 fathom.lineage.v1 envelope（edges+gaps）
    - Test 2（exit 2 flink）：`lineage --dialect flink --profile flink-2.3.0` → exit 2、stderr 含 lineage is Doris-only（D-08）
    - Test 3（exit 2 坏 catalog）：`lineage --dialect doris --profile 4.x --catalog /nonexistent` → exit 2；`--catalog` 指向坏 JSON 文件 → exit 2（FATHOM-SCHEMA-004）
    - Test 4（exit 1 parse 失败）：输入超限/无效树 → exit 1；缺 --dialect/--profile → exit 2
    - Test 5（有 catalog 星号展开）：`--catalog` 提供 {t:[a,b]}，输入 `SELECT * FROM t` → exit 0、envelope 含 2 边（无 requires-catalog gap）
  </behavior>
  <action>
    扩展 `fathom-sql/args.mbt`（D-04/D-11）：subcommand 白名单（args.mbt:62-64）增加 `"lineage"`；`Command` 结构（pub(all)）新增 `pub catalog : String?` 字段；新增 flag `--catalog <file>`（仿 `--rule` 解析块：`--catalog` 后取值，缺值 → `Err(MissingValue(flag="--catalog"))`，文件路径本身不校验存在性——run 层处理坏路径 exit 2）。扩展 `fathom-sql/run.mbt`：新增 `pub fn run_lineage(command : Command, stdin_bytes : Bytes) -> CliOutcome`——读输入（file/stdin，run.mbt:98-125 形状）→ `parse_options(command)`（Err → parse_error_outcome，复用 D-39 0/1/2）→ 处理 `--catalog`：Some(path) → read_file（失败 → usage_error "cannot read file" exit 2），读入后经 `@binding.parse_catalog_json`（或等价库面）解析（Err → usage_error exit 2，FATHOM-SCHEMA-004 message；Ok(None) → 无 catalog）→ **flink 门禁**：selection dialect 为 flink → usage_error exit 2（"lineage is Doris-only"，D-08）→ 调 `@api.lineage_text(input, selection, catalog)`：Ok(result) → `{ exit_code: 0, stdout: @utf8.encode(@binding.lineage_result_json(input, result, command.dialect, command.profile, selection.exact_release()) + "\n"), stderr: b"" }`（字节精确 write_fd，D-39）；Err(error) → parse_error_outcome(command.dialect, error)（exit 1/2）。扩展 `fathom-sql/main.mbt`：subcommand match 加 `"lineage" => { let stdin_bytes = ...; run_lineage(command, stdin_bytes) }`；`usage_text()`/`usage_error_message`（run.mbt:295-325）把 lineage 加入 subcommand 枚举与 --catalog 到 flag 列表。扩展 `fathom-sql/cli_test.mbt`：Behavior Test 1-5 退出码矩阵（Command 全字段字面量 + run_lineage 断言）。
  </action>
  <verify>
    <automated>moon test --target native --package fathom-sql</automated>
  </verify>
  <acceptance_criteria>
    - args.mbt 白名单含 lineage + Command.catalog 字段 + --catalog flag（缺值 → MissingValue exit 2）
    - run.mbt 含 run_lineage（D-39 0/1/2）；main.mbt 分发 lineage；usage_text 含 lineage
    - cli_test.mbt 矩阵全绿：exit 0 envelope（含/不含 catalog 星号展开）、exit 1 parse 失败、exit 2 flink 门禁/坏 --catalog 路径/坏 catalog JSON/缺 dialect-profile
    - CLI 只调 @api（+ @binding 序列化）——不含 lineage 逻辑（D-37/D-38）；flink 门禁在 run_lineage 显式 exit 2（D-08）
  </acceptance_criteria>
  <done>用户可经 `fathom-sql lineage --catalog <file>` 运行列级血缘检查，退出码符合 D-39（0=envelope/1=parse 失败/2=用法配置错误）</done>
  <reversibility rating="costly">D-08 CLI 面与 D-39 退出码是消费契约；改动触及公共边界——决策已在 CONTEXT.md 锁定，不插 re-ask checkpoint</reversibility>
</task>

</tasks>

## Artifacts this phase produces

- **新符号（扩展既有文件）：**
  - `binding/schema.mbt` — `pub const LINEAGE_SCHEMA_VERSION : String = "fathom.lineage.v1"`；`validate_schema_version` 追加第 8 分支（纯增）。
  - `binding/json.mbt` — `pub fn lineage_result_json(source, result : @api.LineageResult, dialect, profile, exact_release) -> String`（edges/gaps 数组，span to_double）。
  - `binding/exports.mbt` — `#export_name("fathom_lineage_v1") pub fn fathom_lineage_v1(raw, dialect, profile, mode, catalog_json : Bytes) -> Bytes`（flink 门禁 FATHOM-SCHEMA-003 + "lineage is Doris-only"）。
  - `binding/catalog_json.mbt`（新增）— `pub fn parse_catalog_json(bytes : Bytes) -> Result[@api.StaticCatalog?, String]`（tables/db_tables/functions；非法输入 Err）。
  - `binding/moon.pkg` — js+wasm exports 各追加 `"fathom_lineage_v1"`（第 8 个）。
  - `fathom-sql/args.mbt` — subcommand 白名单增 `"lineage"`；`Command` 增 `pub catalog : String?`；`--catalog <file>` 标志。
  - `fathom-sql/run.mbt` — `pub fn run_lineage(command, stdin_bytes) -> CliOutcome`（D-39 0/1/2；--catalog 读文件 + catalog JSON 解析 + flink 门禁）。
  - `fathom-sql/main.mbt` — subcommand match 增 `"lineage"`；usage_text 增 lineage。
  - `fathom-sql/cli_test.mbt` — lineage 退出码矩阵（含 --catalog 正反例、flink exit 2、star 展开）。

- **明确不改动：** `lineage/`、`api/`（07-02/07-03 产物，只消费）、`parity/`（07-05）、`parser/`/`analyzer/`。

## threat_model

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 宿主调用 fathom_lineage_v1 → binding | 未信任 raw SQL + catalog_json；dialect-first 校验 + flink 门禁在此边界 |
| CLI stdin/file + --catalog 文件 → run_lineage | 未信任输入；ParseLimits + 文件读取错误 + catalog JSON 解析错误全结构化（D-39 exit 2） |
| binding envelope → JS/Wasm 宿主 | 参数全 primitive（Bytes/String）；无对象句柄跨边界（Pitfall 8/17） |

### STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04-01 | Spoofing | flink 选择静默当作 Doris | high | mitigate | fathom_lineage_v1 + run_lineage 双边界 flink 门禁 → FATHOM-SCHEMA-003 + "lineage is Doris-only" / exit 2（D-08，RESEARCH A6） |
| T-07-04-02 | Tampering | 恶意/畸形 catalog JSON | high | mitigate | parse_catalog_json try/catch + 字段校验 → Err → FATHOM-SCHEMA-004（never silent fallback，T-06-03-01/ASVS V5）；空 bytes 缺省无 catalog |
| T-07-04-03 | Tampering | schema 版本泄漏/既有命名空间破坏 | high | mitigate | validate_schema_version 纯增第 8 分支（Pitfall V6）；js+wasm 两处 exports 注册（Pitfall 5）；07-05 export_smoke 加法断言 |
| T-07-04-04 | Tampering | 边/gap span 序列化精度（to_double 2^53） | medium | mitigate | span Int ≤ 2^53 用 Json::number(to_double()) 安全（json.mbt:7-12 先例）；07-05 parity 三目标字节一致兜底 |
| T-07-04-05 | DoS | --catalog 大文件/超大输入 | medium | mitigate | ParseLimits（api 8 MiB）封顶 raw；catalog 文件大小由 read_file 边界控制（CLI 层）；lineage 线性处理 |
| T-07-04-SC | Tampering | npm/pip/cargo 安装 | low | accept | 零新增外部依赖 |

<verification>
- Task 1 后：`moon check --target native && moon build --target js --package binding && moon test --target native --package binding` 全绿（wire envelope + flink 门禁 + schema 加法）。
- Task 2 后：`moon test --target native --package binding` 全绿（catalog_json 正反例）。
- Task 3 后：`moon test --target native --package fathom-sql` 全绿（lineage 退出码矩阵 + --catalog 正反例）。
</verification>

<success_criteria>
- `fathom.lineage.v1` wire 导出（第 8 命名空间纯增）+ `fathom_lineage_v1` ABI 可被 Native/JS/Wasm 宿主消费，flink 门禁在 wire 边界落地。
- `--catalog <file>` 注入链完整：CLI 读文件 → catalog_json.mbt → StaticCatalog → lineage_text，非法输入全结构化错误。
- `fathom-sql lineage` 子命令 D-39 0/1/2 退出码矩阵落地，用户可运行列级血缘检查。
</success_criteria>

<output>
Create `.planning/phases/07-column-lineage/07-04-SUMMARY.md` when done.
</output>
