---
phase: 07-column-lineage
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - analyzer/select_model.mbt
  - analyzer/select_parser.mbt
  - analyzer/resolve.mbt
  - analyzer/analyzer_wbtest.mbt
  - test/analyzer_public_surface_test.mbt
autonomous: true
requirements:
  - LINE-01
estimate:
  tokens: 62000
  raw_tokens: 41000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Wave 0 在 analyzer 打开 lineage-facing 公开面：SelectModel/SelectCore/SelectItem/FromItem/CteDef/NameRef/TokenSlice/ClauseKind 加 pub(all)（只增不改既有行为，moon 0.1.20260724 跨包读访问的 Binding/TableInfo 先例，analysis.mbt:22-59）；source_tokens/split_select_model/matching_paren/collect_refs/qualified_ref_at/find_word_at_depth0/slice_tokens/has_error_missing 由 fn 转 pub fn"
    - "analyze 公开面（resolve.mbt:1382）与既有 ANAL-01 行为零改动：本 plan 只加 pub 标记与薄公开包装，不触碰 parser（Phase 12 冻结 baseline），不改 analyze_dml_body/analyze_create_view_body 语义"
    - "SelectItem 增 except_cols : Array[TokenSlice]（Pitfall 3 选项 a）：build_select_item 捕获 `* EXCEPT (...)` 排除列 token 而非剥离丢弃；expand_star 应用排除（ASCII case-fold，引号字节复核）——`SELECT * EXCEPT (b) FROM t` 不再为 b 产出伪造边"
    - "D-04 与 analyzer 现实调和（下游消费者明文）：UNION 按 SelectModel.branches 位置映射；EXCEPT 是 Doris 投影修饰符（parser.mbt:1637-1646），经 except_cols 排除语义处理，绝不当作集合运算分支；INTERSECT 不在 Doris 接受集 → 语法无效文档走 requires-complete-parse gap（07-02）"
    - "FLAGGED ASSUMPTION (LINE-01 adjacency, UNRESOLVED→RESOLVED): pub(all) select-model 字段跨包只读即够（Binding/TableInfo 先例证明 pub(all) 结构体字段可跨包读取）；若 moon 0.1.20260724 实测要求字段级 pub，则对 lineage 读取的字段（refs/alias/alias_slice/star/star_qualifier/parts/start_byte/end_byte）补 pub——执行器首任务探针核实"
    - "FLAGGED ASSUMPTION (LINE-01 empty, UNRESOLVED→RESOLVED): 空文档/无语句文档经公开面重解析 → 空 SelectModel（branches=[]），不 panic、不产伪内容——split_select_model 对空 token 流返回 Some 空模型"
  artifacts:
    - analyzer/select_model.mbt（pub(all) SelectModel/SelectCore/SelectItem/FromItem/CteDef/NameRef/TokenSlice/ClauseKind；SelectItem.except_cols 新字段）
    - analyzer/select_parser.mbt（pub fn source_tokens/split_select_model/matching_paren/collect_refs/qualified_ref_at/find_word_at_depth0/slice_tokens；build_select_item 捕获 EXCEPT 排除列）
    - analyzer/resolve.mbt（pub fn has_error_missing；expand_star 应用 except_cols 排除；薄公开包装 view_body_location/insert_body_location）
    - analyzer/analyzer_wbtest.mbt（EXCEPT 排除白盒测试 + 公开面正反例）
    - test/analyzer_public_surface_test.mbt（跨包公开面端到端测试：parse → source_tokens → split_select_model → SelectModel 结构断言）
  key_links:
    - "source_tokens → split_select_model：公开 token 流 → 结构化 SelectModel 的单一入口，lineage/（07-02）与视图/INSERT 体重解析共用"
    - "expand_star × except_cols：星号展开排除列是 analyzer 内部单点（resolve.mbt:388 起），lineage 只消费结果——绝不重复实现排除逻辑"
    - "pub(all) 公开面不改变 analyze 输出字节：ANLY-01 语法 valid 通道与既有快照零漂移（Pitfall V6 纯增哲学）"
  prohibitions:
    - statement: "本 plan 永不触碰 parser/（Phase 12 冻结 baseline；D-21 parser 永不 import analyzer 负门禁维持）——FLAGGED-UNVERIFIED（无 wired check，specless 描述符空）"
    - statement: "公开面绝不改变 analyze 的 Binding/诊断输出（ANLY-01：语法 valid 通道不变，analyzer 诊断独立通道）——FLAGGED-UNVERIFIED（无 wired check，specless 描述符空）"
    - statement: "EXCEPT 处理绝不伪造被排除列的绑定/边（SC2 红线；Pitfall 3 至少 (b) 显式 gap，绝不静默伪造）——FLAGGED-UNVERIFIED（无 wired check，specless 描述符空）"
---

<objective>
交付 **LINE-01** 的 Wave 0 地基：在 `analyzer/` 打开最小 lineage-facing 公开面——`pub(all)` select-model 类型 + 公开 re-parser/体定位入口 + `* EXCEPT` 排除语义——使 `lineage/`（07-02）能以跨包只读方式消费结构化 SELECT 模型，且不触碰 Phase 12 冻结 parser baseline。

Purpose: 研究 RQ1 直接读源证实 `SelectModel`/`SelectItem`/`NameRef`/`TokenSlice` 全部包私有（select_model.mbt:11-107 无 pub），`split_select_model`/`source_tokens` 等全部 `fn`（select_parser.mbt:25,1150）——lineage 若要「零重写解析」地复用 ANAL-01 绑定与结构化模型，必须先在此打开公开面。这是 Phase 7 比 Phase 6 多出的、必须排在第一波的第一批改动（07-PATTERNS 明文）。同时按研究 Pitfall 3 选项 (a) 实现 `* EXCEPT` 诚实展开，杜绝为被排除列伪造边（SC2 红线）。
Output: analyzer 公开面 + EXCEPT 排除语义 + 跨包端到端测试。

Execution note（scope_sanity）：本 plan files_modified=5，全部为 analyzer 内部改动 + test/ 新增一个公开面测试。**按 task 原子提交**（Task 1/2 各一次 commit）；**Task 1 首探针**：`moon check --target native` 核实 pub(all) select-model 字段跨包可读（Binding 先例应成立），探针结论记录后再写 test/analyzer_public_surface_test.mbt。Task 2 依赖 Task 1 的 pub(all)（except_cols 是公开结构体新字段），顺序执行。
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-column-lineage/07-CONTEXT.md
@.planning/phases/07-column-lineage/07-RESEARCH.md
@.planning/phases/07-column-lineage/07-PATTERNS.md
@analyzer/select_model.mbt
@analyzer/select_parser.mbt
@analyzer/resolve.mbt
@analyzer/analysis.mbt
@analyzer/analyzer.mbt
@analyzer/analyzer_wbtest.mbt
@test/analyzer_anal01_test.mbt
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 公开 select-model 面 + re-parser/体定位入口 + 跨包端到端测试</name>
  <files>
    analyzer/select_model.mbt
    analyzer/select_parser.mbt
    analyzer/resolve.mbt
    test/analyzer_public_surface_test.mbt
  </files>
  <read_first>
    analyzer/select_model.mbt（当前包私有类型全貌，select_model.mbt:11-107——需 pub(all) 的 8 个类型）
    analyzer/select_parser.mbt（source_tokens:25 / matching_paren:168 / collect_refs:210 / qualified_ref_at / find_word_at_depth0 / slice_tokens / split_select_model:1150——fn → pub fn）
    analyzer/resolve.mbt has_error_missing（resolve.mbt:69-90）+ analyze 公开入口（resolve.mbt:1382-1406，pub fn 写法先例）+ analyze_create_view_body（resolve.mbt:1295-1314，view 体定位逻辑）+ analyze_dml_body Insert 臂（resolve.mbt:1256-1264，INSERT 尾体缺口）
    analyzer/analysis.mbt（pub(all) Binding 先例，analysis.mbt:22-59——跨包字段只读的证据）
    test/analyzer_anal01_test.mbt（analyze_sql 助手 + 跨包读 pub(all) 字段先例，analyzer_anal01_test.mbt:9-90）
    07-RESEARCH.md Pattern 1（按 span 关联 SelectModel 的前置公开面）+ Wave 0 说明
    07-PATTERNS.md「2. analyzer 包私有模型公开面（Wave 0，本阶段特有）」
  </read_first>
  <behavior>
    - Test 1（公开面端到端）：parse `SELECT a + b AS x FROM t`（doris 4.x strict）→ @analyzer.source_tokens(root, bytes) → @analyzer.split_select_model(tokens) 返回 Some(model)，model.branches.length()==1，branches[0].select_list.length()==1，select_list[0].refs.length()==2，select_list[0].alias == Some("x")
    - Test 2（空文档总函数）：空 token 流 split_select_model → Some 空模型（branches=[]），不 panic
    - Test 3（has_error_missing 公开）：含 error/missing 材料的语句节点 has_error_missing(node)==true；干净 SELECT == false
  </behavior>
  <action>
    在 `analyzer/select_model.mbt` 给 8 个类型加 `pub(all)`（ClauseKind/TokenSlice/NameRef/SelectItem/FromItem/CteDef/SelectCore/SelectModel，select_model.mbt:11-107），保持 derive(Eq, @debug.Debug)。**首探针**：`moon check --target native` 核实 pub(all) 结构体字段跨包可读（Binding/TableInfo 先例应成立，test/ 已跨包读 Binding.kind/start_byte）；若字段级 pub 必需，对 lineage 读取的字段（refs/alias/alias_slice/star/star_qualifier/parts/start_byte/end_byte）补 `pub` 并记录结论。在 `analyzer/select_parser.mbt` 将 `source_tokens`（select_parser.mbt:25）、`split_select_model`（:1150）、`matching_paren`（:168）、`collect_refs`（:210）、`qualified_ref_at`、`find_word_at_depth0`、`slice_tokens` 由 `fn` 转 `pub fn`（签名不变，只加 pub）。在 `analyzer/resolve.mbt` 将 `has_error_missing`（resolve.mbt:69-90）转 `pub fn`（D-33 拒绝扫描，07-02 requires-complete-parse gap 复用）；新增两个**薄公开包装**（行为从既有私有逻辑提取，零语义改动）：`pub fn view_body_location(tokens : Array[(Bytes, Int, Int)]) -> Int`（返回 CREATE VIEW 语句 token 流中 AS 之后 query_start 的 token 索引，逻辑源自 analyze_create_view_body resolve.mbt:1295-1314）与 `pub fn insert_body_location(tokens : Array[(Bytes, Int, Int)]) -> (col_list: (Int, Int)?, body_start: Int)?`（返回 INSERT 语句的括号列清单 token 区间与 VALUES/SELECT/WITH 体起始索引，复用 matching_paren + find_word_at_depth0，逻辑源自 analyze_dml_body Insert 臂 resolve.mbt:1256-1264——**不修改 analyze_dml_body 行为**）。新建 `test/analyzer_public_surface_test.mbt`（test/ 已 import parser/analyzer/source/dialect，test/moon.pkg）：仿 analyzer_anal01_test.mbt 的 analyze_sql 助手——@parser.parse_with_limits 得到 @syntax.SyntaxNode root → @analyzer.source_tokens(root, source.bytes()) → @analyzer.split_select_model(tokens)，断言 Behavior Test 1-3。
  </action>
  <verify>
    <automated>moon check --target native && moon test --target native --package test</automated>
  </verify>
  <acceptance_criteria>
    - `moon check --target native` 通过：pub(all) select-model 字段跨包只读成立（探针结论记录）；test/ 能从 test 包读 SelectModel.branches[0].select_list[0].refs/alias
    - test/analyzer_public_surface_test.mbt 三用例全绿：Test 1（SELECT a + b AS x → 2 refs + alias "x"）、Test 2（空 token 流 → Some 空模型）、Test 3（has_error_missing 正反例）
    - analyzer/select_parser.mbt 与 analyzer/resolve.mbt 的公开函数签名与既有私有逻辑一致（只加 pub/薄包装，无行为改动）
    - analyzer 既有测试（analyzer_wbtest.mbt + test/analyzer_anal01_test.mbt）全绿——公开面零漂移（ANLY-01/Pitfall V6）
  </acceptance_criteria>
  <done>lineage/ 可跨包调用 source_tokens/split_select_model/has_error_missing/view_body_location/insert_body_location 并只读 SelectModel 结构，公开面被端到端测试锁定</done>
  <reversibility rating="costly">pub(all) 公开面与薄包装是 lineage 消费的公共边界；改签名需同步 07-02/07-03——决策已在 CONTEXT.md Claude's Discretion 锁定（--auto），不插 re-ask checkpoint</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SelectItem.except_cols + expand_star EXCEPT 排除（Pitfall 3 选项 a）</name>
  <files>
    analyzer/select_model.mbt
    analyzer/select_parser.mbt
    analyzer/resolve.mbt
    analyzer/analyzer_wbtest.mbt
  </files>
  <read_first>
    analyzer/select_parser.mbt build_select_item（select_parser.mbt:352-434——当前剥离 EXCEPT(...) 后丢弃，需改为捕获进 except_cols）
    analyzer/select_model.mbt SelectItem（select_model.mbt:51-62——增 except_cols 字段）
    analyzer/resolve.mbt resolve_select_item + expand_star（resolve.mbt:388-460, 796-875——星号展开应用排除的注入点）
    analyzer/analyzer_wbtest.mbt（wb build_select_item 系列 + wb_tokens 助手，analyzer_wbtest.mbt:1-60——白盒测试先例）
    07-RESEARCH.md Pitfall 3（`* EXCEPT` 选项 (a)/(b) 与推荐）+ Pattern 4 gap 派生（except 与 requires-catalog 的关系）
  </read_first>
  <behavior>
    - Test 1（build_select_item 捕获）：`SELECT * EXCEPT (b) FROM t` 的 select-item token 流 → SelectItem.star==true、except_cols.length()==1、except_cols[0].bytes==b"b"
    - Test 2（expand_star 排除）：catalog {t: [a,b,c]} 上 `SELECT * EXCEPT (b) FROM t` 展开 → Column 绑定为 a、c（不含 b）；无 EXCEPT 的 `SELECT *` 仍展开 a,b,c
    - Test 3（引号字节复核）：`SELECT * EXCEPT ("B") FROM t` 排除引号 B（字节精确）；`SELECT * EXCEPT (b)` 对 catalog 列 "B" 不排除（ASCII case-fold 只对未引号）
  </behavior>
  <action>
    在 `analyzer/select_model.mbt` 的 `SelectItem` 增字段 `except_cols : Array[TokenSlice]`（pub(all) 结构体新字段；D-03 纪律——token 字节 + 平铺 span）。在 `analyzer/select_parser.mbt` 的 `build_select_item`（select_parser.mbt:352-434）：当前逻辑剥离 `EXCEPT (...)`，改为在检测到星号项紧跟 `EXCEPT (` 时，把括号内每个顶层 token 切片收集进 `SelectItem.except_cols`（复用 matching_paren 定位闭括号；括号内容按顶层逗号/空白切分为列名切片，D-03 大小写语义——未引号 ASCII case-fold、引号字节复核，与 find_column_in 语义一致）。在 `analyzer/resolve.mbt` 的 `expand_star`（resolve.mbt:388-460）增加排除应用：展开出的 Column 绑定若列名命中 except_cols（按 D-03 匹配语义），跳过该列（不产出绑定）；签名按既有 resolve_select_item 调用点调整（expand_star 增 except_cols 参数或新辅助函数），**不触碰 parser/**。在 `analyzer/analyzer_wbtest.mbt` 增白盒测试（wb_tokens + build_select_item/expand_star 直接调用）锁定 Behavior Test 1-3；expand_star 的排除路径若有新诊断需求（如排除列不存在于 catalog）不新增诊断（保持 expand_star 静默，lineage 侧 07-02 自检星号未展开）。
  </action>
  <verify>
    <automated>moon test --target native --package analyzer && moon test --target native --package test</automated>
  </verify>
  <acceptance_criteria>
    - analyzer_wbtest.mbt 新增测试全绿：build_select_item 捕获 EXCEPT 排除列；expand_star 对 `* EXCEPT (b)` 产出 a,c 绑定（无 b）；引号/大小写语义正确
    - analyzer 既有白盒测试与 test/ 集成测试全绿（无 EXCEPT 用例零漂移；ANLY-01 valid 通道不变）
    - SelectItem 结构体含 except_cols 字段；parser/ 无任何改动（git status 确认 parser/ 目录未动）
    - `SELECT * EXCEPT (b) FROM t`（有 catalog）的 Column 绑定数量 = catalog 列数 - 排除列数，且 span 为星号 span（共享，D-03）
  </acceptance_criteria>
  <done>`* EXCEPT (cols)` 在 analyzer 层诚实展开（排除列不产绑定/边），SC2「不伪造边」在星号修饰符场景落地</done>
  <reversibility rating="costly">SelectItem 增字段是公开结构体变更；expand_star 排除改变 ANAL-01 星号展开行为（无 EXCEPT 用例零漂移）——决策按研究 Pitfall 3 推荐 (a) 锁定，不插 re-ask checkpoint</reversibility>
</task>

</tasks>

## Artifacts this phase produces

- **新符号（扩展既有文件）：**
  - `analyzer/select_model.mbt` — `pub(all)` 加于 ClauseKind/TokenSlice/NameRef/SelectItem/FromItem/CteDef/SelectCore/SelectModel；`SelectItem` 新字段 `except_cols : Array[TokenSlice]`。
  - `analyzer/select_parser.mbt` — `pub fn source_tokens(node, source_bytes) -> Array[(Bytes, Int, Int)]`；`pub fn split_select_model(tokens) -> SelectModel?`；`pub fn matching_paren(tokens, open) -> Int`；`pub fn collect_refs(tokens) -> Array[NameRef]`；`pub fn qualified_ref_at(...)`；`pub fn find_word_at_depth0(...)`；`pub fn slice_tokens(...)`；`build_select_item` 捕获 EXCEPT 排除列进 except_cols。
  - `analyzer/resolve.mbt` — `pub fn has_error_missing(node) -> Bool`；`pub fn view_body_location(tokens) -> Int`（薄包装）；`pub fn insert_body_location(tokens) -> (col_list: (Int, Int)?, body_start: Int)?`（薄包装）；`expand_star` 应用 except_cols 排除。
  - `analyzer/analyzer_wbtest.mbt` — EXCEPT 排除 + 公开面白盒测试。
  - `test/analyzer_public_surface_test.mbt`（新增）— 跨包端到端：parse → source_tokens → split_select_model → SelectModel 结构断言 + has_error_missing 正反例。

- **明确不改动：** `parser/`（Phase 12 冻结 baseline，git status 验证）、`analyzer/analysis.mbt`（Binding/AnalysisDiagnostic 已 pub(all)，零改动）、`analyzer/analyzer.mbt`（Catalog trait 零改动）、`analyze_dml_body`/`analyze_create_view_body` 行为（薄包装只提取定位逻辑）。

## threat_model

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test/ → analyzer 公开面 | 跨包只读 SelectModel/Binding 字段；pub(all) 是读访问边界（构造仍限 analyzer 内部） |
| analyzer 公开面 → 既有 ANAL-01 通道 | 公开面零行为改动；ANLY-01 语法 valid 通道不得被触碰 |

### STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01-01 | Tampering | pub(all) 公开面意外改变 analyze 输出 | high | mitigate | Task 1 后跑 analyzer_wbtest + test/analyzer_anal01_test 全量快照——公开面零漂移（Pitfall V6 纯增）；无 EXCEPT 用例断言不变量 |
| T-07-01-02 | Spoofing | `* EXCEPT` 排除列大小写误匹配导致误排除/漏排除 | medium | mitigate | 排除匹配复用 D-03 语义：未引号 ASCII case-fold、引号字节复核（find_column_in 同源，analyzer.mbt:117-128）；wb Test 3 锁定引号/大小写正反例 |
| T-07-01-03 | Tampering | 薄包装 view_body_location/insert_body_location 与既有分析逻辑漂移 | medium | mitigate | 薄包装只提取定位逻辑，不改 analyze_dml_body/analyze_create_view_body 语义；Task 1 后既有集成测试全绿 |
| T-07-01-04 | DoS | 公开 split_select_model 对畸形 token 流 | medium | mitigate | 既有 paren-depth 上限返回 None（T-05-03-01）；空 token 流返回空模型（Test 2） |
| T-07-01-SC | Tampering | npm/pip/cargo 安装 | low | accept | 本阶段零新增外部依赖（D-07）；无安装面 |

<verification>
- Task 1 后：`moon check --target native && moon test --target native --package test` 全绿；公开面端到端三用例通过；analyzer 既有测试零漂移。
- Task 2 后：`moon test --target native --package analyzer && moon test --target native --package test` 全绿；EXCEPT 排除白盒测试通过；`git status` 确认 parser/ 未动。
</verification>

<success_criteria>
- analyzer 打开 lineage-facing 公开面：8 个 select-model 类型 pub(all)，7 个 re-parser/体定位函数 pub，has_error_missing 公开，两个薄体定位包装就绪——lineage/（07-02）可零重写解析地消费。
- `* EXCEPT (cols)` 诚实展开落地，为被排除列伪造边的隐患从源头消除（SC2 红线）。
- 公开面零行为漂移：既有 analyzer 测试与集成快照全绿，parser/ 冻结 baseline 未动。
</success_criteria>

<output>
Create `.planning/phases/07-column-lineage/07-01-SUMMARY.md` when done.
</output>
