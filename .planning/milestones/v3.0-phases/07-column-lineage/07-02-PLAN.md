---
phase: 07-column-lineage
plan: 02
type: execute
wave: 1
depends_on:
  - 01
files_modified:
  - lineage/moon.pkg
  - lineage/model.mbt
  - lineage/edges.mbt
  - lineage/gaps.mbt
  - lineage/views.mbt
  - lineage/insert.mbt
  - lineage/lineage_test.mbt
autonomous: true
requirements:
  - LINE-01
estimate:
  tokens: 76000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "lineage/ 独立库（D-21/D-07）：moon.pkg 只 import analyzer + syntax（公开入口签名需 @syntax.SyntaxNode），永不 import parser/source/dialect——边/gap span 用平铺 Int 字节（Pitfall 8）"
    - "LineageEdge/LineageGap/LineageResult 为 pub(all) + 全 pub 字段 + derive(Eq, @debug.Debug)（跨包字面量构造要求 all-public，lint/rules.mbt:13 教训）——edge 形状 {source_name, source_resolved_to, source_start_byte, source_end_byte, target_name, target_start_byte, target_end_byte}；gap {code, message, start_byte, end_byte}；result {edges, gaps} 分离（D-06）"
    - "D-01 表达式直通：输出列表达式内每个已解析列引用各贡献一条边到该输出列（SELECT a + b AS x → t.a→x + t.b→x；upper(c) AS u → t.c→u）；复用 analyze 的 Binding 按 span 关联 NameRef（D-02），目标 span = alias_slice / 单 ref span / star span（Pattern 1）"
    - "D-06 gap 模型独立列表：requires-catalog（`*`/`table.*` 无 catalog 列元数据、外部视图无元数据、无列清单 INSERT）、unresolved-reference（unknown-table/column/function、ambiguous-reference——Open Question 2 归并）、requires-complete-parse（error/missing 树，复用 pub has_error_missing）；每个 gap 带 span；gaps 与 edges 严格分离"
    - "D-03 视图注册表：同文档 CREATE VIEW 体经 view_body_location + split_select_model 重解析 → view→output_columns；ViewCatalog[T] 泛型包装（pub struct + pub impl[T:Catalog] Catalog for ViewCatalog[T]——table 先查注册表后委托 inner，table_in_db/function 委托 inner，A3）；同名视图 shadow catalog 表（Pitfall 4 约定）"
    - "D-04 调和：UNION 按 SelectModel.branches 位置列映射（输出列名取首分支）；EXCEPT 是投影修饰符（07-01 except_cols 处理）；INTERSECT 不在 Doris 接受集；INSERT 目标列清单与尾 SELECT 按位置对齐，尾体独立空作用域重解析（Open Question 4），无列清单无 catalog → 目标表 span 一个 requires-catalog gap（Open Question 3），VALUES 行无边"
    - "FLAGGED ASSUMPTION (LINE-01 adjacency, UNRESOLVED→RESOLVED): 边永不因 span 相等/相邻而合并——同一星号 span 展开的多个列各产一条边；两个不同 refs 即使 span 重叠也各产一条边（D-01 每 ref 一边）"
    - "FLAGGED ASSUMPTION (LINE-01 empty, UNRESOLVED→RESOLVED): 空文档/无语句文档 → LineageResult{edges: [], gaps: []}；单语句 → 其边/gap；catalog None → 所有星号/外部视图 → requires-catalog gap；无 null 输入面（Bytes 参数）"
    - "FLAGGED ASSUMPTION (LINE-01 ordering, UNRESOLVED→RESOLVED): 边/gap 数组顺序是公共契约——文档语句序 → SelectModel 分支/CTE 序 → SelectItem 序 → refs 序；星号展开按 scope entry 序 × catalog 列序（LinkedHashMap 确定性，STACK.md 核实）；相等元素保持源序稳定"
    - "FLAGGED ASSUMPTION (LINE-01 idempotency, UNRESOLVED→RESOLVED): derive_lineage 是 (source_bytes, catalog) 的纯函数——同输入重跑产出字节一致 edges/gaps；不修改 catalog/CST"
    - "FLAGGED ASSUMPTION (LINE-01 concurrency, UNRESOLVED→RESOLVED): 纯内存派生、无共享可变状态、无 IO（除调用方传入 bytes）——并行调用同输入产出相同结果；中断不产生部分结果（API 原子返回）"
  artifacts:
    - lineage/moon.pkg（library，import analyzer + syntax；永不 parser/source/dialect，D-21 负门禁）
    - lineage/model.mbt（pub(all) LineageEdge/LineageGap/LineageResult + gap 码常量或字符串文档）
    - lineage/edges.mbt（pub fn derive_lineage(root, source_bytes, catalog) -> LineageResult——文档走查 + SELECT/CTE/UNION 边派生）
    - lineage/gaps.mbt（gap 派生：star 未展开自检 + 诊断映射 + has_error_missing）
    - lineage/views.mbt（视图注册表构建 + pub struct ViewCatalog[T] + pub impl[T:Catalog] Catalog for ViewCatalog[T]）
    - lineage/insert.mbt（INSERT 目标列清单 + 尾 SELECT 位置映射；VALUES/无列清单处理）
    - lineage/lineage_test.mbt（白盒正反例：边派生、gap 派生、视图/CTE/UNION/INSERT、确定性向量）
  key_links:
    - "derive_lineage 经 @analyzer.analyze 拿 Binding + 经公开 split_select_model 拿 SelectModel——按 start_byte/end_byte span 关联，是边派生唯一正确路径（Pitfall 1：绝不自行做大小写判定）"
    - "ViewCatalog[T] 先查视图注册表后委托 inner——视图 shadow 注入 catalog 表；无注册表命中且 inner 无元数据 → requires-catalog gap（D-03/SC2）"
    - "star 未展开自检是 SC2 的机制保证：expand_star 静默零列（resolve.mbt:388），lineage 必须自己发现并产 requires-catalog gap（Pitfall 2/7）"
    - "边/gap 顺序确定性 → 07-05 parity 三目标字节一致的输入契约（Pattern 6）"
  prohibitions:
    - statement: "lineage/ 永不 import parser/source/dialect（D-21；Pitfall 8 平铺 Int span）——FLAGGED-UNVERIFIED（无 wired check，specless 描述符空）"
    - statement: "永不伪造血缘边：未解析引用/无 catalog 星号/外部视图无元数据 → 显式 gap，绝不推测列名合成边（SC2 红线）——FLAGGED-UNVERIFIED（无 wired check，specless 描述符空）"
    - statement: "永不静默丢弃未解析引用（必须产 unresolved-reference gap）；永不把 gap 混入 edges 数组（D-06 独立列表）——FLAGGED-UNVERIFIED（无 wired check，specless 描述符空）"
    - statement: "边/gap 顺序永不依赖无序 Map 迭代（LinkedHashMap + 文档序 + catalog 列序是公共契约，跨目标 parity）——FLAGGED-UNVERIFIED（无 wired check，specless 描述符空）"
    - statement: "永不重写解析/作用域引擎（复用 analyze 绑定 + split_select_model，Pitfall V3 漂移风险）——FLAGGED-UNVERIFIED（无 wired check，specless 描述符空）"
---

<objective>
交付 **LINE-01** 的核心：新建 `lineage/` 独立库（D-21：只 import analyzer，永不 parser），实现边派生（D-01 表达式直通）、gap 派生（D-06 诚实报告）、视图注册表 + `ViewCatalog[T]`（D-03）与 INSERT/CTE/UNION 位置映射（D-04）——跨 SELECT/INSERT/CTE/集合运算/视图展开，每边带源列引用 span + 目标输出列 span。

Purpose: ROADMAP SC1/SC2 的核心承诺——「用户可检查列级数据血缘（跨 SELECT/INSERT/CTE/集合运算/视图展开，边带源码位置）」与「无 catalog 时未解析引用/`*` 展开产生显式 requires-catalog gap 而非伪造边」。本 plan 建立血缘引擎本体（库面），api/wire/CLI/parity 在 07-03/07-04/07-05 完成。
Output: `lineage/` 库 + `derive_lineage` 公开入口 + 白盒测试。

Execution note（scope_sanity）：本 plan files_modified=7，3 tasks——Task 1（model+edges tracer）、Task 2（gaps）、Task 3（views+insert）。**按 task 原子提交**；Task 1 的 tracer 先跑通 SELECT 边派生再展开。**首探针**：`moon check --target native` 核实 `ViewCatalog[T]` 泛型 trait 包装在 moon 0.1.20260724 编译（研究 A3/A4）；Task 3 前务必探针。lineage_test.mbt 是 lineage/ 包内白盒（同包可构造 pub(all) 模型），不 import parser——构造 SelectModel/Binding/StaticCatalog 直接测边/gap 派生。
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-column-lineage/07-CONTEXT.md
@.planning/phases/07-column-lineage/07-RESEARCH.md
@.planning/phases/07-column-lineage/07-PATTERNS.md
@analyzer/analysis.mbt
@analyzer/analyzer.mbt
@analyzer/select_model.mbt
@analyzer/resolve.mbt
@lint/rules.mbt
@lint/engine.mbt
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: lineage/ 包 + model.mbt + derive_lineage SELECT/CTE/UNION 边派生 tracer</name>
  <files>
    lineage/moon.pkg
    lineage/model.mbt
    lineage/edges.mbt
    lineage/lineage_test.mbt
  </files>
  <read_first>
    analyzer/select_model.mbt（07-01 产物：pub(all) SelectModel/SelectCore/SelectItem/NameRef/TokenSlice + SelectItem.except_cols）
    analyzer/analysis.mbt（pub(all) Binding/BindingKind/AnalysisResult——start_byte/end_byte 平铺 span，analysis.mbt:22-59）
    analyzer/resolve.mbt analyze（resolve.mbt:1382-1406，泛型 [T:Catalog] 签名先例）+ source_tokens/split_select_model（07-01 公开）
    lint/rules.mbt LintFinding/LintResult（lint/rules.mbt——pub(all) + 全 pub 字段 + derive 的跨包构造模板）
    lint/moon.pkg（library 包依赖面模板）
    07-RESEARCH.md Pattern 1（按 span 关联 SelectModel 的边派生）+ Pattern 6（确定性顺序）
    07-PATTERNS.md「1. 新分析包模式：lineage/ 独立库」+「lineage/model.mbt」「lineage/edges.mbt」
  </read_first>
  <behavior>
    - Test 1（表达式直通 D-01）：SelectModel{1 branch, select_list=[{alias:"x", refs:[a@7-8, b@10-11]}]} × Binding[Table t, Column a@7-8, Column b@10-11] × StaticCatalog{t:[a,b]} → derive_lineage 产 2 边：t.a→x（source 7-8, target alias span）、t.b→x（source 10-11, target alias span）
    - Test 2（单 ref 无边目标）：`SELECT a AS x` → 1 边 t.a→x；`SELECT upper(c) AS u` 函数实参 c → 1 边 t.c→u（is_call ref 的 call_args 内列引用）
    - Test 3（CTE/UNION）：WITH c AS (SELECT a FROM t) SELECT a FROM c → 边 t.a→c.a（CTE 体）+ c.a→输出（CTE 引用侧）；UNION 两分支各自产边（位置映射，首分支列名）
    - Test 4（无 catalog 星号零边）：`SELECT * FROM t`（catalog None）→ edges==[]（gaps 由 Task 2 产）
  </behavior>
  <action>
    新建 `lineage/moon.pkg`：`pkgtype(kind: "library")`，import 仅 `"fathom/sql/analyzer" @analyzer` + `"fathom/sql/syntax" @syntax`（公开入口签名需要 @syntax.SyntaxNode，与 analyzer.analyze 一致；**永不 import @parser/@source/@dialect**，D-21 负门禁）。新建 `lineage/model.mbt`：`pub(all) struct LineageEdge { pub source_name : String, pub source_resolved_to : String, pub source_start_byte : Int, pub source_end_byte : Int, pub target_name : String, pub target_start_byte : Int, pub target_end_byte : Int } derive(Eq, @debug.Debug)`；`pub(all) struct LineageGap { pub code : String, pub message : String, pub start_byte : Int, pub end_byte : Int }`（code ∈ "requires-catalog"|"unresolved-reference"|"requires-complete-parse"）；`pub(all) struct LineageResult { pub edges : Array[LineageEdge], pub gaps : Array[LineageGap] }`（全 pub 字段，binding/json.mbt 07-04 跨包构造）。新建 `lineage/edges.mbt`：`pub fn derive_lineage(root : @syntax.SyntaxNode, source_bytes : Bytes, catalog : @analyzer.Catalog?) -> LineageResult`——**文档走查**仿 analyze（root.children() → Statement → body，@syntax.SyntaxElement 分发）；对每个 SELECT-family 体（顶层 SELECT/CTE 体/子查询体/CREATE VIEW 体/INSERT 尾 SELECT 由 Task 3 接入）：调 @analyzer.analyze(root, source_bytes, catalog) 得 AnalysisResult（catalog 为 None 时用空 StaticCatalog 或 trait 的 None 语义——**T-02-42 调用方注入，无 catalog 不伪造**），再对每个体调公开 split_select_model 得 SelectModel；**按 span 关联**：每个 SelectItem.refs 中的 NameRef 的 start_byte/end_byte 匹配某条 BindingKind::Column 绑定 → 该绑定为源（source_name=绑定.name 源码拼写、source_resolved_to=绑定.resolved_to catalog 规范名、span=绑定 start/end，Pitfall 1 不自做大小写）；目标输出列：有别名 → name=alias、span=alias_slice；无别名单 ref → name=该 ref 的 Column 绑定 name、span=ref span；star → 每个星号展开的 Column 绑定各一条边（共享 star span）；无别名多 ref 表达式 → 输出列取 resolve 侧 resolved_refs[0]（resolve.mbt:834-839）、目标 span=item 表达式 span。CTE 体经 SelectModel.ctes 递归；UNION 按 branches 顺序走查。Catalog? 泛型：`pub fn derive_lineage[T : Catalog](root, source_bytes, catalog : T)` 或 `catalog : @analyzer.Catalog?`（可选 catalog 语义 = ViewCatalog 包装，Task 3；本 Task 用 StaticCatalog 具现）。新建 `lineage/lineage_test.mbt`（lineage/ 包内白盒，不 import parser）：直接构造 SelectModel/Binding/StaticCatalog 输入，断言 Behavior Test 1-4 的边集合与 span。
  </action>
  <verify>
    <automated>moon check --target native && moon test --target native --package lineage</automated>
  </verify>
  <acceptance_criteria>
    - lineage/moon.pkg 只 import analyzer+syntax（D-21 负门禁成立，grep lineage/moon.pkg 无 parser/source/dialect）
    - model.mbt 含 pub(all) LineageEdge/LineageGap/LineageResult 全 pub 字段 + derive(Eq, @debug.Debug)
    - lineage_test.mbt Test 1-4 全绿：`SELECT a + b AS x` → 2 边（t.a→x + t.b→x）；单 ref/函数实参 → 1 边；CTE/UNION 位置映射正确；无 catalog 星号 → 0 边
    - 边 span 为平铺 Int 字节（source 用绑定 start/end，target 用 alias_slice/ref/star span）
    - derive_lineage 可被 test/ 跨包调用（@syntax.SyntaxNode 参数），空文档 → 空结果
  </acceptance_criteria>
  <done>lineage/ 能对 SELECT/CTE/UNION 文档产出按 D-01 表达式直通的列级边（span 正确），零重写解析（复用 analyze + split_select_model）</done>
  <reversibility rating="one-way">D-01 边语义是公开契约（costly→one-way 边界）；derive_lineage 签名与 LineageEdge 形状决定全部下游消费方——决策已在 CONTEXT.md 锁定（--auto），不插 re-ask checkpoint</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: gaps.mbt — star 自检 + 诊断映射 + has_error_missing（D-06）</name>
  <files>
    lineage/gaps.mbt
    lineage/lineage_test.mbt
  </files>
  <read_first>
    analyzer/resolve.mbt expand_star（resolve.mbt:388-460——静默零列，lineage 必须自检星号）+ has_error_missing（resolve.mbt:69-90，07-01 已公开）
    analyzer/analysis.mbt AnalysisDiagnostic（analysis.mbt——unknown-table/column/function、ambiguous-reference、requires-complete-parse 码）
    lineage/edges.mbt + lineage/model.mbt（Task 1 产物——derive_lineage 内接入 gaps）
    07-RESEARCH.md Pattern 4（gap 派生规则表：requires-catalog / unresolved-reference / requires-complete-parse）+ Open Question 1/2
    07-PATTERNS.md「lineage/gaps.mbt」
  </read_first>
  <behavior>
    - Test 1（star 无 catalog → requires-catalog）：`SELECT * FROM t`（catalog None）→ gaps==[requires-catalog @ star span]，edges==[]
    - Test 2（star 有 catalog → 真实边）：`SELECT * FROM t`（catalog {t:[a,b]}）→ 2 边 t.a→a、t.b→b，gaps==[]
    - Test 3（unknown 引用 → unresolved-reference）：`SELECT nope FROM t` → gaps==[unresolved-reference @ ref span]，message 保留 analyzer 原文；unknown-table 无 catalog → requires-catalog（无法区分表 vs 外部视图，A1）
    - Test 4（error/missing → requires-complete-parse）：含 has_error_missing 的体 → gaps==[requires-complete-parse @ 体 span]，复用 analyzer 诊断
    - Test 5（ambiguous-reference 归并）：ambiguous-reference 诊断 → unresolved-reference gap（Open Question 2）
  </behavior>
  <action>
    新建 `lineage/gaps.mbt`，在 `derive_lineage`（Task 1）中接入 gap 派生（与 edges 严格分离，D-06）。派生规则（研究 Pattern 4，[ASSUMED] A1 落地）：(1) **star 自检**——SelectItem.star 为 true 且该 item 未展开出任何 Column 绑定（无 catalog 列元数据；expand_star 静默零列）→ 在 star span（或 star_qualifier span）产 `requires-catalog` gap；(2) **表引用未解析**——FROM/JOIN 的 FromItem 名未解析成 Table 绑定：catalog 未注入（None）→ `requires-catalog`（无法区分表 vs 外部视图，D-03）；catalog 已注入 → `unresolved-reference`（unknown-table）；(3) **列/函数引用未解析**——analyze 的 AnalysisDiagnostic code ∈ {unknown-column, unknown-function, ambiguous-reference} → `unresolved-reference` gap（span=诊断 span，message=诊断 message 原样，Open Question 2 归并）；(4) **error/missing 拒绝**——体含 error/missing（复用公开 has_error_missing 或 analyzer 的 requires-complete-parse 诊断）→ `requires-complete-parse` gap（D-33，span=体 span）；ambiguous-reference 不产独立码（Open Question 2 锁定归并）。**edge/gap 顺序契约（Pattern 6）**：按文档语句序 → 模型序 → refs/诊断序 追加；catalog 列序 = 调用方注入顺序（LinkedHashMap 确定性）。在 `lineage/lineage_test.mbt` 增 Behavior Test 1-5 的白盒断言（构造含 star item 的 SelectModel + 含 unknown 诊断的 AnalysisResult + 含 error/missing 的节点）。`derive_lineage` 返回 LineageResult 时先收 edges 再收 gaps（或按语句交错，**锁定一种顺序并写注释**——parity 契约）。
  </action>
  <verify>
    <automated>moon test --target native --package lineage</automated>
  </verify>
  <acceptance_criteria>
    - gaps.mbt 实现四类 gap 派生（requires-catalog / unresolved-reference / requires-complete-parse），gaps 与 edges 分离
    - lineage_test.mbt Test 1-5 全绿：star 无 catalog → requires-catalog 且 0 边（SC2）；有 catalog star → 真实边；unknown 引用 → unresolved-reference；error/missing → requires-complete-parse；ambiguous-reference 归并 unresolved-reference
    - gap 均携带平铺 Int span；message 对诊断派生 gap 保留 analyzer 原文
    - 边/gap 顺序确定性契约写入代码注释（文档序 → 模型序 → refs 序）
  </acceptance_criteria>
  <done>无 catalog 场景对 `*`/外部视图/未解析引用/error 树全部诚实报告显式 gap，绝不伪造边（SC2 全部落地）</done>
  <reversibility rating="one-way">D-06 gap 码是公共契约（requires-catalog/unresolved-reference/requires-complete-parse）；重命名破坏下游过滤——决策已在 CONTEXT.md 锁定，不插 re-ask checkpoint</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: views.mbt（ViewCatalog[T]）+ insert.mbt（INSERT/CTE/UNION 位置映射）</name>
  <files>
    lineage/views.mbt
    lineage/insert.mbt
    lineage/lineage_test.mbt
  </files>
  <read_first>
    analyzer/analyzer.mbt Catalog trait + StaticCatalog（analyzer.mbt:44-90——pub(open) trait Catalog {table/table_in_db/function}；StaticCatalog 字段私有无枚举访问器，ViewCatalog[T] 是唯一通用包装方案）
    analyzer/resolve.mbt view_body_location / insert_body_location（07-01 公开薄包装）
    lineage/edges.mbt + lineage/gaps.mbt（Task 1/2 产物——view/insert 走查接入 derive_lineage）
    07-RESEARCH.md Pattern 2（视图注册表 + ViewCatalog[T]）+ Pattern 3（INSERT 位置映射 + 解析层缺口）+ Open Question 3/4 + A3/A4
    07-PATTERNS.md「lineage/views.mbt」「lineage/insert.mbt」+「No Analog Found（ViewCatalog[T] 泛型包装）」
  </read_first>
  <behavior>
    - Test 1（视图注册表 + ViewCatalog）：CREATE VIEW v AS SELECT a FROM t; SELECT * FROM v（catalog {t:[a]}）→ 边 t.a→v.a（视图输出列），view 优先于 catalog 表（同名 shadow）
    - Test 2（外部视图 gap）：SELECT * FROM v（v 不在文档、catalog 无 v）→ requires-catalog gap；catalog 提供 v 的 TableInfo → 按表展开
    - Test 3（INSERT 位置映射）：INSERT INTO t(c1,c2) SELECT a,b FROM u → 边 u.a→t.c1、u.b→t.c2（目标列清单与 SELECT 输出按位置对齐，尾体独立空作用域）
    - Test 4（无列清单 INSERT gap）：INSERT INTO t SELECT a FROM u（catalog None）→ 目标表 span 一个 requires-catalog gap（列序未知），SELECT 源侧边照常（Open Question 3）
    - Test 5（VALUES 无边）：INSERT INTO t(c1) VALUES (1) → 无边、无 gap（行字面量无列引用）；有列清单无 catalog 仍可命名目标列
  </behavior>
  <action>
    新建 `lineage/views.mbt`：(1) **视图注册表**——文档走查中收集 CREATE VIEW 语句：用公开 view_body_location(tokens) 定位 AS 后 query_start，对体 token 切片调公开 split_select_model → SelectModel，首个 branch 的 select_list 输出列名（alias 优先，否则单 ref 列名/表达式 span 列名）→ `Map[String, Array[ColumnInfo]]`（LinkedHashMap 确定性；视图名 ASCII case-fold 查找、引号字节复核，Pitfall V1/D-03）；(2) **`pub struct ViewCatalog[T] { pub views : Map[String, Array[ColumnInfo]], pub inner : T }`** + `pub impl[T : Catalog] Catalog for ViewCatalog[T]`：`table(self, name)` 先查注册表（case-fold 命中 → 视图输出列构造 TableInfo），未中则 `self.inner.table(name)`（同名视图 shadow catalog 表，Pitfall 4）；`table_in_db`/`function` 直接委托 inner（视图只在默认库命名空间，A3 保守假设）。**首探针**（Task 3 前）：`moon check --target native` 核实泛型 trait 包装编译（研究 A3/A4）。新建 `lineage/insert.mbt`：(1) INSERT 语句用公开 insert_body_location(tokens) 得 (col_list, body_start)；col_list 存在 → 目标列名列表（token 文本，无需 catalog）；(2) 尾体为 SELECT/WITH → 用**独立空作用域**对体 token 切片重解析（复用 split_select_model，Open Question 4 隔离目标表作用域）→ 源侧边照常产出；目标列 i 与 SELECT 输出列 i 位置对齐（D-04）；(3) 尾体为 VALUES → 行字面量无列 refs → 无源边（目标列名来自显式列清单或 catalog 列序）；(4) 无列清单且无 catalog → 在目标表 span 产**一个** requires-catalog gap（Open Question 3 锁定）；(5) INSERT 目标表名经 analyze 既有 Table 绑定（resolve_table_references 走查，不新写）。在 `lineage/lineage_test.mbt` 增 Behavior Test 1-5（构造视图注册表 + ViewCatalog + INSERT 定位输入的白盒断言）。
  </action>
  <verify>
    <automated>moon check --target native && moon test --target native --package lineage</automated>
  </verify>
  <acceptance_criteria>
    - views.mbt 含视图注册表构建 + pub struct ViewCatalog[T] + pub impl[T:Catalog] Catalog for ViewCatalog[T]（table 先查视图后委托 inner，table_in_db/function 委托 inner）
    - insert.mbt 用 insert_body_location 定位并重解析 INSERT 尾 SELECT/VALUES；目标列清单与 SELECT 输出位置对齐
    - lineage_test.mbt Test 1-5 全绿：视图展开边/外部视图 gap/INSERT 位置映射/无列清单 gap/VALUES 无边
    - ViewCatalog 泛型包装在 moon 0.1.20260724 编译通过（探针结论记录）；derive_lineage 接入视图注册表与 INSERT 走查
    - D-04 调和落地：UNION 位置映射（Task 1）+ EXCEPT 投影修饰符（07-01 except_cols）+ INTERSECT 不接受集（语法无效 → requires-complete-parse）
  </acceptance_criteria>
  <done>lineage/ 跨 SELECT/INSERT/CTE/UNION/视图展开产边，视图注册表 + ViewCatalog 注入生效，INSERT 位置映射与 gap 政策完整</done>
  <reversibility rating="one-way">D-03 视图注册表语义决定 INSERT...FROM v 的边（Phase 5 D-05 顺延兑现）；ViewCatalog[T] 包装是唯一通用方案（StaticCatalog 字段私有）——决策已在 CONTEXT.md 锁定，不插 re-ask checkpoint</reversibility>
</task>

</tasks>

## Artifacts this phase produces

- **新符号（新增包 lineage/）：**
  - `lineage/moon.pkg` — library 包，import 仅 analyzer + syntax（D-21，永不 parser/source/dialect）。
  - `lineage/model.mbt` — `pub(all) struct LineageEdge`（source_name/source_resolved_to/source_start_byte/source_end_byte/target_name/target_start_byte/target_end_byte，全 pub + derive）；`pub(all) struct LineageGap`（code/message/start_byte/end_byte）；`pub(all) struct LineageResult`（edges/gaps，全 pub + derive）。
  - `lineage/edges.mbt` — `pub fn derive_lineage(root : @syntax.SyntaxNode, source_bytes : Bytes, catalog : @analyzer.Catalog?) -> LineageResult`（文档走查 + SELECT/CTE/UNION 边派生，D-01 表达式直通，span 关联）。
  - `lineage/gaps.mbt` — gap 派生（requires-catalog / unresolved-reference / requires-complete-parse；star 自检 + 诊断映射 + has_error_missing）。
  - `lineage/views.mbt` — 视图注册表构建 + `pub struct ViewCatalog[T] { views : Map[String, Array[ColumnInfo]], inner : T }` + `pub impl[T : Catalog] Catalog for ViewCatalog[T]`。
  - `lineage/insert.mbt` — INSERT 目标列清单 + 尾 SELECT/VALUES 重解析与位置映射。
  - `lineage/lineage_test.mbt` — 白盒正反例（边/gap 派生、视图/CTE/UNION/INSERT、确定性）。

- **明确不改动：** `parser/`、`analyzer/`（07-01 已打开公开面，本 plan 只消费）、`api/`/`binding/`/`fathom-sql/`/`parity/`（后续 plan）。

## threat_model

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 调用方 source bytes + catalog → derive_lineage | 未信任 SQL 与 catalog 元数据（T-02-42）；lineage 只消费 analyze 绑定，不解释 catalog 语义 |
| lineage/ → analyzer 公开面 | 跨包只读 SelectModel/Binding；D-21 单向依赖（analyzer 永不反向 import） |
| derive_lineage 输出 → 序列化（07-04） | 边/gap span 是平铺 Int；顺序是公共契约（跨目标 parity 输入） |

### STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02-01 | Spoofing | 无 catalog 星号/外部视图伪造边 | critical | mitigate | star 自检（expand_star 静默零列 → requires-catalog gap，SC2 红线）；gaps 与 edges 严格分离（D-06）；Test 1/4 断言 0 边 |
| T-07-02-02 | Spoofing | catalog 元数据不可信导致误边（T-02-42） | medium | mitigate | lineage 只读 analyze 绑定（resolved_to 是 catalog 规范名）；Pitfall 1：绝不自行大小写判定；catalog 列序 = 调用方注入顺序 |
| T-07-02-03 | Tampering | 边/gap span 越界或源长度不匹配 | medium | mitigate | 复用 analyzer 的 source slice 纪律（start ≤ end ≤ source_bytes.length()，resolve.mbt:27-35）；span 全部来自绑定/模型（已校验） |
| T-07-02-04 | Tampering | error/missing 树被当作干净树分析 | high | mitigate | has_error_missing（D-33）→ requires-complete-parse gap；复用 analyzer 拒绝分支（resolve.mbt:1030/1195/1302） |
| T-07-02-05 | DoS | 超大文档/深层嵌套重解析 | medium | mitigate | 输入经 ParseLimits（api 层 8 MiB）；split_select_model 有 paren-depth 上限返回 None（T-05-03-01）→ requires-complete-parse gap |
| T-07-02-SC | Tampering | npm/pip/cargo 安装 | low | accept | 零新增外部依赖（D-07） |

<verification>
- Task 1 后：`moon check --target native && moon test --target native --package lineage` 全绿；SELECT/CTE/UNION 边派生白盒通过。
- Task 2 后：`moon test --target native --package lineage` 全绿；四类 gap 派生 + SC2 无 catalog 断言通过。
- Task 3 后：`moon check --target native && moon test --target native --package lineage` 全绿；视图注册表 + ViewCatalog + INSERT 位置映射白盒通过。
</verification>

<success_criteria>
- `lineage/` 独立库交付：`derive_lineage` 对 SELECT/INSERT/CTE/UNION/视图展开产列级边（span 正确），D-01 表达式直通语义落地。
- 无 catalog 场景全部诚实 gap（requires-catalog/unresolved-reference/requires-complete-parse），SC2「绝不伪造边」核心机制闭环。
- 视图注册表 + `ViewCatalog[T]` + INSERT 位置映射（D-03/D-04，与 analyzer 现实调和）全部落地，白盒测试锁定。
</success_criteria>

<output>
Create `.planning/phases/07-column-lineage/07-02-SUMMARY.md` when done.
</output>
