---
phase: 08-incremental-parsing-benchmark-gated
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - bench/moon.pkg
  - bench/bench.mbt
  - bench/build_editor_scale.mbt
  - .planning/phases/08-incremental-parsing-benchmark-gated/08-BENCHMARK.md
  - .planning/phases/08-incremental-parsing-benchmark-gated/COVERAGE.md
autonomous: true
requirements:
  - EDIT-01
estimate:
  tokens: 66000
  raw_tokens: 44000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "bench/ 包已建立：bench/moon.pkg 声明 pkgtype(kind: \"library\") 且库 import 面 = @api + @parser + @source（D-01 被测面；绝不 import incremental/）；bench/bench.mbt 含 @bench 函数调用 @api.parse（api.mbt:431）并经 keep() 消费结果防 DCE（STACK.md:231）"
    - "editor-scale 合成输入已构造：bench/build_editor_scale.mbt 以内嵌语句池（authoring-time 从 corpus/doris-{2.1,3.x,4.x}/*.sql 拷贝为 Bytes 字面量，运行时不读磁盘，仿 parity/baseline_test.mbt:7-9）拼接/重复至规模梯度 25/50/100/200KB（≥100KB 覆盖 D-01 目标；含注释/空白/多语句/复杂 SELECT+DDL）"
    - "门禁命令已执行并产出结构化数据：moon bench --target native --output-json 退出 0 且输出 JSON summaries（或 @bench 不可用时降级的 manual-timing harness 产出等价 JSON-shaped 数据）；规模梯度下 median 非零且随规模合理增长（DCE 冒烟，Pitfall 3——全部 ~0ms 或相等即失败）"
    - "08-BENCHMARK.md 五要素齐全：fixture 清单、输入规模（25/50/100/200KB）、median/p95（native；js/wasm 若可行补充）、门禁结论（D-02 阈值判定：≥100KB median > 50ms 或超线性迹象）、工具链记录（moon bench --version 输出 + 若降级的方法）；无数据不宣称已评估（Pitfall 4 / D-17）"
    - "分支决策已记录：08-BENCHMARK.md 含明确 branch 结论（A=descope 或 B=implement），08-01 SUMMARY 记录 fired branch——供 orchestrator 决定路由 08-02（A）或 08-03/08-04（B）"
  artifacts:
    - bench/moon.pkg
    - bench/bench.mbt
    - bench/build_editor_scale.mbt
    - .planning/phases/08-incremental-parsing-benchmark-gated/08-BENCHMARK.md
    - .planning/phases/08-incremental-parsing-benchmark-gated/COVERAGE.md
  key_links:
    - "bench/bench.mbt @bench fn → @api.parse：被测路径（编辑器真实成本，含 envelope 序列化）；@parser.parse_with_limits_context 辅测面分离核心成本（Pattern 2 双测面）"
    - "build_editor_scale.mbt → @api.parse：≥100KB 合成输入经默认 ParseLimits（max_bytes=8MiB，api.mbt:15-19）通过；避免病态深嵌套（max_recursion_depth=128，parser.mbt:38-45）"
    - "08-BENCHMARK.md ← moon bench --output-json：证据落库——fixture/规模/median/p95/结论五要素是后续 08-02（descope）或 08-03/08-04（实现）的决策输入"
  prohibitions:
    - statement: "绝不无测量数据宣称 descope（无数据 → 无 claim；SC1 明文 benchmarks-demonstrate 与 with-the-benchmark-evidence-documented；D-17 诚实 provenance）——FLAGGED-UNVERIFIED（无 wired check，specless 描述符空）"
    - statement: "bench 包绝不触碰 parser/（Phase 12 冻结 baseline；benchmark 只读调用 @api.parse/@parser.parse_with_limits_context）——FLAGGED-UNVERIFIED（无 wired check，specless 描述符空）"
    - statement: "绝不跳过 benchmark 直接假设 descope（08-DISCUSSION-LOG 已记录该选项被用户否决；D-01 门禁先行）——FLAGGED-UNVERIFIED（无 wired check，specless 描述符空）"
  flagged_assumption:
    - "EDIT-01 边界探针 UNRESOLVED（requirement_id: EDIT-01, category: unclassified, probe: unclassified — review manually）：specless 阶段无 SPEC.md，EDIT-01 的精确边界未由确定性探针分类——本 plan 以 ROADMAP SC1/SC2/SC3 + RESEARCH/PATTERNS 的 verified 事实为边界来源；执行期若发现计划边界与实测不符，在 SUMMARY 记录偏差而非静默扩大/缩小范围"
---

<objective>
交付 **EDIT-01 的基准门禁 tracer**（本阶段唯一 Wave 0 plan，gate-first 分解的第一个 plan）：建立 `bench/` 包（`@bench` attribute + `moon bench --target native --output-json`），测量**整文档重解析延迟**（主测 `@api.parse` 全路径、辅测 `@parser.parse_with_limits_context` 纯核心），输入 = 既有 corpus fixture 基线 + 合成 editor-scale 文档（≥100KB，规模梯度 25/50/100/200KB），记录 native median/p95 与输入规模关系；产出 `08-BENCHMARK.md`（fixture/规模/median/p95/结论五要素 + 分支决策 + 工具链记录）与 `COVERAGE.md`（api-coverage 声明）。**SUMMARY 必须记录 fired branch（A=descope / B=implement）**，供 orchestrator 路由后续 plan（D-03）。

Purpose: D-01 门禁先行——任何增量解析实现决策必须以 benchmark 数据为据，绝不先实现后补测量（研究 Pitfall 6 / EDIT-01 gated）；ROADMAP SC1 要求 "moon bench benchmarks demonstrate ... or the requirement is descoped with the benchmark evidence documented"。
Output: bench/ 包（3 文件）+ 08-BENCHMARK.md + COVERAGE.md + 分支决策记录。
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-incremental-parsing-benchmark-gated/08-CONTEXT.md
@.planning/phases/08-incremental-parsing-benchmark-gated/08-RESEARCH.md
@.planning/phases/08-incremental-parsing-benchmark-gated/08-PATTERNS.md
@api/api.mbt
@parser/parser.mbt
@source/source.mbt
@printer/printer.mbt
</context>

<tasks>

<task type="auto">
  <name>Task 1: `@bench` + `moon bench` 在 pinned moon 0.1.20260724 上的可用性探针（Pitfall 1）</name>
  <files>
    bench/moon.pkg
    bench/bench.mbt
  </files>
  <read_first>
    08-RESEARCH.md（Common Pitfalls P1 + Assumptions A1/A2 + Standard Stack 的 @bench 行）
    08-PATTERNS.md（bench/moon.pkg 与 bench/bench.mbt 段；lint/moon.pkg 与 test/moon.pkg 的 import 声明先例）
    moon.mod（工具链 pin：moon 0.1.20260724）
    api/api.mbt（parse 公共入口 api.mbt:431 + ParseOptions::new api.mbt:79 + ParseLimits::default api.mbt:15-19）
    milestones/v1.0-research/STACK.md（@bench + moon bench + keep()，STACK.md:231）
    test/moon.pkg（import 声明与 for "test" 块先例）
  </read_first>
  <action>
    在 `bench/` 新建 `bench/moon.pkg`：`pkgtype(kind: "library")`，库 import = `"fathom/sql/api" @api` + `"fathom/sql/parser" @parser` + `"fathom/sql/source" @source`（D-01 被测面，仿 lint/moon.pkg；绝不 import incremental/）。在 `bench/bench.mbt` 写**最小探针**：一个 `@bench` 函数（无参、返回 Unit），函数体 = 构造输入 → `@api.parse(raw, options)`（`ParseOptions::new("doris", "4.x", "editor")`，api.mbt:79）→ 用 `keep()` 消费结果防 DCE（STACK.md:231 指定；精确语法以 pinned toolchain 实测为准）。执行探针三连：`moon bench --version`（记录工具链输出）；`moon check --target native`（确认探针包编译）；`moon bench --target native --output-json`（确认 @bench attribute 可解析、CLI 可用、JSON summaries 产出）。探针结论三态并记录：(1) @bench 可用 → 按 D-01 命令继续；(2) attribute 可用但 `--output-json`/`keep()` 语义有差异 → 记录差异并适配；(3) @bench/`moon bench` 不可用 → 记录降级路径：以 `moon test --target native --package bench` + 手动计时循环（黑盒经 `@api.parse`，`@mtest` 断言 wall-clock 并手写 JSON-shaped 汇总）作为 manual-timing fallback，保证门禁仍产出数据。探针结论作为 Task 2 执行方式的输入。
  </action>
  <verify>
    <automated>moon check --target native</automated>
  </verify>
  <acceptance_criteria>
    - bench/moon.pkg 存在且库 import 面 = @api/@parser/@source（git diff 可见，无 incremental/）
    - bench/bench.mbt 含最小 @bench 探针函数 + keep() 消费结果
    - `moon bench --version` 与 `moon check --target native` 通过；`moon bench --target native --output-json` 退出 0 且输出 JSON（或降级路径已明确记录，含 fallback 命令）
    - 探针结论（可用/适配/降级三态）明确记录在案，作为 Task 2 执行方式输入
  </acceptance_criteria>
  <done>`@bench`/`moon bench` 在 pinned toolchain 上的可用性已确证（或降级方案已定），Task 2 可按确定路径构建完整 bench 包</done>
  <reversibility rating="reversible">bench/ 是独立测试面（D-01 reversible）；探针产物可增删，不影响任何运行时库路径</reversibility>
</task>

<task type="tracer">
  <name>Task 2 (TRACER): bench 包端到端——合成 editor-scale 输入 + @bench 被测函数 + `moon bench --target native --output-json`</name>
  <files>
    bench/build_editor_scale.mbt
    bench/bench.mbt
  </files>
  <read_first>
    08-PATTERNS.md（build_editor_scale.mbt 段：parity/baseline_test.mbt:7-9 内嵌 fixture 纪律 + corpus/script-multi-statement.sql 多语句形状；bench/bench.mbt 段：test/source_test.mbt:1-5 parse_editor helper + api.mbt:431 调用面）
    parity/baseline_test.mbt（内嵌 raw bytes 纪律，baseline_test.mbt:7-9,38-39）
    corpus/doris-4.x/select-industrial.sql + corpus/doris-4.x/script-multi-statement.sql + corpus/doris-4.x/ddl-create-table.sql（复杂 SELECT/多语句/DDL 代表输入形状）
    api/api.mbt（parse:431 + ParseOptions::for_profile:179 + ParseLimits::default:15-19）
    test/source_test.mbt（parse_editor 黑盒调用形状，source_test.mbt:1-5）
  </read_first>
  <action>
    在 `bench/build_editor_scale.mbt` 内嵌语句池：authoring-time 从 corpus/doris-{2.1,3.x,4.x}/*.sql 拷贝为 Bytes 字面量（运行时不读磁盘，仿 baseline_test.mbt:7-9），覆盖注释头/空白/多语句/复杂 SELECT+DDL 形状；提供 `fn build_editor_scale(size_bytes : Int) -> Bytes`：拼接语句池 + 循环重复至目标规模（25/50/100/200KB），**避免病态深嵌套**（max_recursion_depth=128，parser.mbt:38-45——深嵌套会触发 resource 诊断而非正常路径）。在 `bench/bench.mbt` 按 Task 1 探针结论写 @bench 被测函数集：每个规模梯度一个（命名 `parse_full_editor_scale_25k/50k/100k/200k`）+ 既有 corpus 基线（命名 `parse_full_<fixture>`，如 parse_full_select_industrial）；函数体 = `build_editor_scale(size)` → `@api.parse(raw, options)`（`ParseOptions::new("doris", "4.x", "editor")` 或 `for_profile`，api.mbt:79/179）→ `keep()` 消费结果；另加 1-2 个纯核心辅测函数调 `@parser.parse_with_limits_context`（Pattern 2 双测面——定位瓶颈归属：若全路径超阈值但核心不超，瓶颈在序列化而非解析）。执行门禁命令：`moon bench --target native --output-json`（@bench 不可用时按 Task 1 降级路径用手动计时 harness 产出等价数据）。记录各规模 median/p95 与输入规模关系。
  </action>
  <verify>
    <automated>moon bench --target native --output-json</automated>
  </verify>
  <acceptance_criteria>
    - bench/build_editor_scale.mbt 提供 `build_editor_scale(size_bytes : Int) -> Bytes` 且 100KB/200KB 输入可构造（运行时不读磁盘）
    - bench/bench.mbt 含规模梯度 @bench 函数 + corpus 基线 @bench 函数 + keep() 消费 + 纯核心辅测函数（若 @bench 可用）
    - `moon bench --target native --output-json` 退出 0 且 JSON summaries 覆盖全部 @bench 函数；median 非零且随规模合理增长
    - 各规模（25/50/100/200KB）native median/p95 已采样并记录（manual-timing fallback 时以等价 JSON-shaped 数据落库）
  </acceptance_criteria>
  <done>整文档重解析延迟在 editor-scale 规模梯度下已被测量并产出结构化数据（JSON），输入-延迟关系可审计</done>
  <reversibility rating="reversible">bench/ 独立测试面；@bench 函数与输入构造可增删（D-01）</reversibility>
</task>

<task type="auto">
  <name>Task 3: 门禁判定 + 08-BENCHMARK.md 五要素证据 + COVERAGE.md + 分支决策</name>
  <files>
    .planning/phases/08-incremental-parsing-benchmark-gated/08-BENCHMARK.md
    .planning/phases/08-incremental-parsing-benchmark-gated/COVERAGE.md
  </files>
  <read_first>
    08-RESEARCH.md（D-02 阈值判定 + Common Pitfalls P2/P3/P4 + Pattern 2 双测面）
    08-CONTEXT.md（D-01/D-02/D-03）
    08-PATTERNS.md（08-BENCHMARK.md 段：五要素 + CORPUS-REPORT.md 形状）
    corpus/CORPUS-REPORT.md（结构化证据报告形状）
  </read_first>
  <action>
    按 D-02 阈值判定门禁结果：editor-scale（≥100KB）整文档重解析 median > 50ms **或** 延迟随规模超线性增长（O(n²) 迹象）→ 分支 B（可测瓶颈）；否则 → 分支 A（reparse 足够快，descope）。在 `.planning/phases/08-incremental-parsing-benchmark-gated/08-BENCHMARK.md` 记录五要素：fixture 清单（内嵌语句池来源 + corpus 基线名）、输入规模（25/50/100/200KB 梯度）、median/p95 延迟（native；js/wasm 若可行补充）、门禁结论（明确引用 D-02 阈值与实测数字的 A/B 判定）、工具链记录（`moon bench --version` 输出 + 若降级的替代方法）。在 `.planning/phases/08-incremental-parsing-benchmark-gated/COVERAGE.md` 写入 api-coverage 声明，内容为：
    `# API Coverage`
    `No external API integration: Phase 8 adds an internal benchmark package (bench/) measuring whole-document parse latency, and, only if the benchmark gate fails, an internal incremental/ reparse package. No external API/SDK/service is integrated.`
    分支决策必须可复现：结论行引用实测数字（如 ≥100KB median=Xms ≤/＞ 50ms），绝不无数据宣称已评估（Pitfall 4 / D-17）。在 SUMMARY 记录 fired branch（A/B）供 orchestrator 路由。
  </action>
  <verify>
    <automated>grep -q "median" .planning/phases/08-incremental-parsing-benchmark-gated/08-BENCHMARK.md && grep -q "p95" .planning/phases/08-incremental-parsing-benchmark-gated/08-BENCHMARK.md && grep -q "结论" .planning/phases/08-incremental-parsing-benchmark-gated/08-BENCHMARK.md && grep -q "No external API integration" .planning/phases/08-incremental-parsing-benchmark-gated/COVERAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - 08-BENCHMARK.md 五要素齐全：fixture 清单、规模梯度（25/50/100/200KB）、median/p95、门禁结论（A/B）、工具链记录
    - 门禁结论与实测数字一致（≥100KB median 实测值 vs 50ms 阈值；超线性迹象记录）
    - 分支决策行明确（A=descope / B=implement），SUMMARY 记录 fired branch
    - COVERAGE.md 存在且内容为 api-coverage 声明（含 "No external API integration"）
  </acceptance_criteria>
  <done>基准门禁证据已落库（08-BENCHMARK.md 五要素 + 分支决策 + 工具链），COVERAGE.md 已声明，SUMMARY 记录 fired branch 供 orchestrator 路由 08-02（A）或 08-03/08-04（B）</done>
  <reversibility rating="costly">分支决策记录是 one-way 门（D-02/D-03 锁定阈值与分支语义）；但决策由锁定阈值 + 实测数据机械决定，非人审可选项——gate 数据本身即 checkpoint，不插 blocking-human（REVERSIBILITY_GATES 下 locked 决策不再 re-ask）</reversibility>
</task>

</tasks>

## Artifacts this phase produces

- **新符号（新建 bench/ 包，独立测试面，reversible）：**
  - `bench/moon.pkg` — `pkgtype(kind: "library")`；库 import = `@api` + `@parser` + `@source`（D-01 被测面；不 import incremental/）。
  - `bench/bench.mbt` — `@bench fn parse_full_editor_scale_25k/50k/100k/200k`（规模梯度主测面，调用 `@api.parse`）+ `@bench fn parse_full_<fixture>`（corpus 基线）+ 纯核心辅测函数（调 `@parser.parse_with_limits_context`）；均 `keep()` 消费防 DCE。
  - `bench/build_editor_scale.mbt` — `fn build_editor_scale(size_bytes : Int) -> Bytes` + authoring-time 内嵌 corpus 语句池（运行时不读磁盘）。
- **证据文档（新建）：**
  - `.planning/phases/08-incremental-parsing-benchmark-gated/08-BENCHMARK.md` — fixture/规模/median/p95/结论五要素 + 分支决策 + 工具链记录。
  - `.planning/phases/08-incremental-parsing-benchmark-gated/COVERAGE.md` — api-coverage 声明。

- **明确不改动：** `parser/`（Phase 12 冻结 baseline，git status 验证——benchmark 只读调用）、`source/`、`syntax/`、`api/`、`lexer/`、`printer/`；`incremental/` 不创建（分支 B 才建）。

## threat_model

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bench/ → @api.parse / @parser.parse_with_limits_context | 只读测量冻结 parser 的整文档重解析延迟；无任何写路径 |
| bench 合成输入 → parser | 本地固定 fixture + 拼接（运行时不读磁盘），无外部输入面/注入 |
| 08-BENCHMARK.md ← gate 数据 | 证据完整性——门禁结论必须可复现，无数据不宣称已评估 |

### STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01-01 | Tampering | 08-BENCHMARK.md 门禁证据被夸大（无数据宣称 descope/实现） | high | mitigate | 五要素强制 + 结论行引用实测数字；grep 断言 median/p95/结论存在（Pitfall 4 / D-17） |
| T-08-01-02 | Tampering | DCE 吞掉被测路径（测出 0ms 假象） | high | mitigate | `keep()` 消费结果（STACK.md:231）+ median 非零且随规模增长冒烟（Pitfall 3） |
| T-08-01-03 | DoS | 合成输入病态深嵌套触发 recursion/recovery 预算（A4） | medium | mitigate | 构造避开病态嵌套；记录 resource 诊断行为而非当作正常延迟（parser.mbt:38-45） |
| T-08-01-04 | Tampering | bench 包意外修改冻结 parser/（基线漂移） | high | mitigate | bench/ 只 import api/parser/source 且只读调用；git status 确认 parser/ 未动 |
| T-08-01-SC | Tampering | npm/pip/cargo 安装 | low | accept | 本阶段零新增外部依赖（Package Legitimacy Audit N/A，research 明文）；无安装面 |

<verification>
- Task 1 后：`moon bench --version` + `moon check --target native` + `moon bench --target native --output-json`（或降级路径明确记录）。
- Task 2 后：门禁命令退出 0 且 JSON summaries 覆盖全部 @bench 函数；median 非零且随规模增长。
- Task 3 后：08-BENCHMARK.md 五要素 + 分支决策 + 工具链记录；COVERAGE.md api-coverage 声明；SUMMARY 记录 fired branch。
- 全程 `git status` 确认 parser/ 未动。
</verification>

<success_criteria>
- `moon bench` 在 pinned moon 0.1.20260724 上的可用性已确证（或降级方案已定），整文档重解析延迟在 editor-scale（≥100KB）规模梯度下已被测量并产出结构化数据。
- 08-BENCHMARK.md 含五要素（fixture/规模/median/p95/结论）与明确的分支决策；COVERAGE.md 完成 api-coverage 声明。
- 分支决策基于实测数字（D-02 阈值），SUMMARY 记录 fired branch——为 08-02（分支 A descope）或 08-03/08-04（分支 B 实现）提供决策输入。
</success_criteria>

<output>
Create `.planning/phases/08-incremental-parsing-benchmark-gated/08-01-SUMMARY.md` when done — 必须记录 fired branch（A/B）。
</output>
