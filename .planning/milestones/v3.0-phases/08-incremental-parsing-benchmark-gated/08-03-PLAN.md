---
phase: 08-incremental-parsing-benchmark-gated
plan: 03
type: execute
wave: 1
depends_on:
  - 01
files_modified:
  - incremental/moon.pkg
  - incremental/edit.mbt
  - incremental/incremental.mbt
  - incremental/incremental_test.mbt
autonomous: true
requirements:
  - EDIT-01
estimate:
  tokens: 56000
  raw_tokens: 37000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "incremental/ 包已建立且依赖面合规：incremental/moon.pkg 声明 pkgtype(kind: \"library\")，库 import = @source + @syntax + @parser（D-04/D-21 单向纪律；parser 永不 import incremental）；@printer + @mtest 仅出现在 for \"test\" 块——测试面用 print_lossless 比对 SC2，不进入库依赖面（仿 test/moon.pkg）"
    - "Edit/Revision 层已就位：incremental/edit.mbt 定义 Edit 结构（start_byte/end_byte/replacement: Bytes 或等价）+ EditError enum + Edit::checked 构造——边界校验复用 @source.Span::checked（source.mbt:16-31，含 OutOfBounds/Reversed），不手写越界检查"
    - "SC2 不变量在 tracer 单路径成立：incremental_test.mbt 对 1 个 edit fixture（replace 编辑）断言 @printer.print_lossless(parse_incremental(prev, [edit])) == @printer.print_lossless(parse_full(edited))（printer.mbt:28）——字节相等，绝不近似（Pitfall 5）"
    - "有界重解析 + span-overlap invalidation：对编辑区间 + 必要词法上下文（引号字符串/注释/方言字面量）重 lex/re-parse（复用 lex_with_limit，lexer/lexer.mbt:428）；与编辑区间重叠的节点失效，绝不 span-unchanged⇒valid 假设（D-04 / Pitfall 5）"
  artifacts:
    - incremental/moon.pkg
    - incremental/edit.mbt
    - incremental/incremental.mbt
    - incremental/incremental_test.mbt
  key_links:
    - "Edit::checked → @source.Span::checked：编辑区间边界校验复用既有受测实现（source.mbt:16-31），不新造"
    - "parse_incremental → @parser.parse_with_limits_context / lex_with_limit：bounded reparse 复用 parser 既有入口，绝不触碰 parser/（冻结 baseline）"
    - "incremental_test.mbt → print_lossless：SC2 硬不变量（增量 vs 全量 Bytes 相等）是分支 B 正确性的唯一仲裁"
  prohibitions:
    - statement: "增量输出必须与整文档重解析字节相等（print_lossless 等值硬不变量，绝不近似）——FLAGGED-UNVERIFIED（无 wired check，specless 描述符空）"
    - statement: "绝不触碰 parser/（冻结 baseline；bounded reparse 复用既有 parser，parser/ 目录零改动）——FLAGGED-UNVERIFIED（无 wired check，specless 描述符空）"
  flagged_assumption:
    - "EDIT-01 边界探针 UNRESOLVED（requirement_id: EDIT-01, category: unclassified, probe: unclassified — review manually）：分支 B 实现细节（region re-parse 机制、DialectContext 线程方式）在 08-RESEARCH 为 LOW/[ASSUMED]——本 plan 以 SC2/SC3 不变量为正确性判定，导入面/形状偏差在 SUMMARY 记录而非静默改契约"
    - "D-04 导入面在 parse_with_limits_context 需要 @dialect.DialectContext 类型（parser.mbt:4275 参数签名）时允许最小必要增补 @dialect（仅类型面）——执行期首探针核实，绝不破坏单向方向（parser 永不 import incremental）"
---

<objective>
**Branch B only — do NOT run if the 08-01 gate records branch A.** 交付分支 B 的**增量解析 tracer**：新建 `incremental/` 包（D-04：只 import source+syntax+parser，单向依赖纪律延续 D-21），实现 Edit/Revision 层 + 有界重解析 + span-overlap invalidation，以 `print_lossless(parse_incremental(x)) == print_lossless(parse_full(x))` 为硬不变量（SC2），在 **1 个 edit fixture（replace 编辑）** 上打通端到端路径。

Purpose: 08-01 门禁判定分支 B（≥100KB median > 50ms 或超线性迹象）时的实现起点——tracer 先行证明有界增量解析架构端到端成立，08-04 再横向扩展完整 edit fixture 矩阵与 SC3。
Output: incremental/ 包（moon.pkg + edit.mbt + incremental.mbt + incremental_test.mbt）+ 单路径 SC2 不变量测试。
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-incremental-parsing-benchmark-gated/08-CONTEXT.md
@.planning/phases/08-incremental-parsing-benchmark-gated/08-RESEARCH.md
@.planning/phases/08-incremental-parsing-benchmark-gated/08-PATTERNS.md
@source/source.mbt
@parser/parser.mbt
@printer/printer.mbt
@lexer/lexer.mbt
@lineage/moon.pkg
</context>

<precondition>
08-01 SUMMARY 记录 fired branch = B（实现）。若 08-01 记录 branch A，本 plan 必须跳过（orchestrator 应路由至 08-02 descope 记录）。
</precondition>

<tasks>

<task type="auto">
  <name>Task 1: incremental/ 包基座——moon.pkg 依赖面 + Edit/Revision 类型（checked 构造）+ 编译探针</name>
  <files>
    incremental/moon.pkg
    incremental/edit.mbt
  </files>
  <read_first>
    08-PATTERNS.md（incremental/moon.pkg 与 incremental/edit.mbt 段：lineage/moon.pkg 最小依赖面先例 + source.mbt:8-31 Span/SpanError/checked 三件套）
    source/source.mbt（Span/Span::checked/SourceText/LineIndex，source.mbt:6-53）
    08-RESEARCH.md（Open Question 3——Edit/Revision 层放 source/ 还是 incremental/；D-04/D-21）
    lineage/moon.pkg（最小 import 面先例：只 import 消费面）
  </read_first>
  <action>
    在 `incremental/` 新建 `incremental/moon.pkg`：`pkgtype(kind: "library")`，库 import = `"fathom/sql/source" @source` + `"fathom/sql/syntax" @syntax` + `"fathom/sql/parser" @parser`（D-04/D-21 单向纪律）；`for "test"` 块加 `"fathom/sql/printer" @printer` + `"moonbitlang/core/test" @mtest`（测试面用 print_lossless 比对 SC2，不进入库依赖面，仿 test/moon.pkg）。**首探针**：若编译要求 `@dialect.DialectContext` 类型（parse_with_limits_context 参数签名，parser.mbt:4275），允许最小必要增补 `"fathom/sql/dialect" @dialect`（仅类型面），记录探针结论并确保单向方向不变（parser 永不 import incremental）。在 `incremental/edit.mbt` 定义 `Edit` 结构（`start_byte : Int` / `end_byte : Int` / `replacement : Bytes` 或等价）+ `EditError` enum + `Edit::checked(start_byte, end_byte, replacement, source_length) -> Result[Edit, EditError]`——边界校验复用 `@source.Span::checked`（source.mbt:16-31，含 OutOfBounds/Reversed），不手写越界检查。运行 `moon check --target native` 确认包编译通过。
  </action>
  <verify>
    <automated>moon check --target native --package incremental</automated>
  </verify>
  <acceptance_criteria>
    - incremental/moon.pkg 存在，库 import 面 = @source/@syntax/@parser（+ 如探针确认的 @dialect 类型面），测试 import 面 = @printer/@mtest（for "test" 块）
    - incremental/edit.mbt 含 Edit 结构 + EditError enum + Edit::checked（复用 Span::checked 边界校验）
    - `moon check --target native` 通过；探针结论（dialect 是否必需）记录
    - parser/ 目录零改动（git status 确认）
  </acceptance_criteria>
  <done>incremental/ 包基座就位（依赖面 + Edit 层），编译通过，单向纪律未破坏（parser/ 未动）</done>
  <reversibility rating="costly">incremental/ 是新的公共库边界（D-04 one-way 门）；Edit 类型形状发布后影响调用方——但本 plan 仅当门禁分支 B 触发才执行（D-03 锁定），不插 re-ask checkpoint</reversibility>
</task>

<task type="tracer" tdd="true">
  <name>Task 2 (TRACER): parse_incremental 有界重解析单路径——1 个 replace edit fixture + SC2 不变量</name>
  <files>
    incremental/incremental.mbt
    incremental/incremental_test.mbt
  </files>
  <read_first>
    parser/parser.mbt（ParsedDocument 形状 parser.mbt:97-104 + parse_with_limits_context parser.mbt:4275）
    printer/printer.mbt（print_lossless 签名 printer.mbt:28-31）
    lexer/lexer.mbt（lex_with_limit lexer.mbt:428-430——bounded relex 入口）
    test/source_test.mbt（parse_editor 黑盒 helper source_test.mbt:1-5——full 路径参考）
    08-RESEARCH.md（Pitfall 5 span-overlap；Pattern 3 bounded reparse；SC2）
  </read_first>
  <behavior>
    - Test 1（SC2 不变量，replace 编辑）：original = 一条 SELECT（含标识符），edited = 同文档将某标识符替换为新名字 → full = parse_full(edited)（黑盒经 @api 或白盒经 parser 入口）+ inc = parse_incremental(parse_full(original), [Edit::checked(...)]) → `@printer.print_lossless(inc.root, inc.source) == @printer.print_lossless(full.root, full.source)`（Bytes 相等）
    - Test 2（Edit::checked 边界）：越界/反向编辑区间 → Err(OutOfBounds/Reversed)（复用 Span::checked 语义）
  </behavior>
  <action>
    在 `incremental/incremental.mbt` 实现 `pub fn parse_incremental(prev : @parser.ParsedDocument, edits : Array[Edit]) -> @parser.ParsedDocument`（或等价形状，D-05 "或等价"）：计算覆盖全部编辑区间的 affected byte range + 必要词法上下文（引号字符串/注释/方言字面量边界）；对该区间构造区域 SourceText（`@source.SourceText::new_with_limit` 于切片 bytes）→ 复用 `lex_with_limit` 重 lex（lexer.mbt:428）→ 经 `@parser.parse_with_limits_context`（parser.mbt:4275）有界 re-parse 区域；对 prev 的旧 CST 做 **span-overlap invalidation**——凡与 affected range 重叠的节点失效重建，绝不 "span unchanged ⇒ node valid"（Pitfall 5）；spans 按 `@source.LineIndex`/byte-offset 重映射（source.mbt:33-53）。在 `incremental/incremental_test.mbt` 写 1 个 tracer edit fixture（replace 编辑，如 SELECT 语句内替换一个标识符）：`original`/`edited` bytes 对 → `full = parse_full(edited)` + `inc = parse_incremental(parse_full(original), [Edit::checked(...)])` → 断言 `@printer.print_lossless(inc.root, inc.source) == @printer.print_lossless(full.root, full.source)`（SC2 硬不变量，printer.mbt:28）。运行 `moon test --target native --package incremental` 使该 fixture 通过。
  </action>
  <verify>
    <automated>moon test --target native --package incremental</automated>
  </verify>
  <acceptance_criteria>
    - incremental/incremental.mbt 提供 `parse_incremental(prev, edits)`，返回 ParsedDocument 形状（或等价）
    - incremental_test.mbt 含 ≥1 个 replace edit fixture，断言 `print_lossless(inc) == print_lossless(full)`（Bytes 相等，绝不近似）
    - `moon test --target native --package incremental` 通过
    - parser/ 目录零改动（git status 确认）；incremental/ 库 import 面未超出 D-04（+探针确认的 @dialect 类型面）
  </acceptance_criteria>
  <done>分支 B tracer 路径端到端成立——1 个 replace edit fixture 上增量输出与全量输出字节相等，架构可扩展（08-04）</done>
  <reversibility rating="costly">parse_incremental 库 API 形状发布后影响调用方（D-05 costly）——形状由 D-05 "或等价" 锁定，不插 re-ask checkpoint</reversibility>
</task>

</tasks>

## Artifacts this phase produces

- **新符号（分支 B 触发时新建 incremental/ 包）：**
  - `incremental/moon.pkg` — `pkgtype(kind: "library")`；库 import = @source + @syntax + @parser（D-04/D-21；+ 探针确认的 @dialect 类型面）；`for "test"` 块 = @printer + @mtest。
  - `incremental/edit.mbt` — `Edit` 结构（start_byte/end_byte/replacement: Bytes）+ `EditError` enum + `Edit::checked(...) -> Result[Edit, EditError]`（复用 `@source.Span::checked`）。
  - `incremental/incremental.mbt` — `pub fn parse_incremental(prev : @parser.ParsedDocument, edits : Array[Edit]) -> @parser.ParsedDocument`（或等价）：affected range 计算 + 区域 SourceText + `lex_with_limit` 重 lex + `@parser.parse_with_limits_context` 有界 re-parse + span-overlap invalidation + span 重映射。
  - `incremental/incremental_test.mbt` — 1 个 replace edit fixture：`print_lossless(inc) == print_lossless(full)`（SC2）+ Edit::checked 边界用例。

- **明确不改动：** `parser/`（Phase 12 冻结 baseline，git status 验证——bounded reparse 复用既有入口）、`source/`（Edit/Revision 放 incremental/ 内，source/ 只读复用 Span/LineIndex）、`syntax/`、`api/`、`lexer/`、`printer/`（测试面只读调用）；无 wire/CLI/LSP 新导出（D-05 交付面为库 API）。

## threat_model

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| incremental/ → @parser.parse_with_limits_context / lex_with_limit | 有界重解析复用冻结 parser 的既有入口；单向读路径，parser 永不 import incremental（D-04/D-21） |
| Edit::checked → @source.Span::checked | 编辑区间跨过不可信边界前的校验点；复用既有受测实现 |
| incremental_test.mbt → print_lossless | SC2 字节等值断言是正确性仲裁；测试面 import 不进入库依赖面 |

### STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-03-01 | Tampering | 增量结果 stale spans/越界访问（编辑区间失效不完整） | high | mitigate | span-overlap invalidation（D-04）+ Edit::checked 复用 Span::checked（source.mbt:16-31）+ SC2 不变量逐 fixture 断言（Pitfall 5） |
| T-08-03-02 | DoS | 病态编辑区间/深嵌套触发 recursion/recovery 预算 | medium | mitigate | 复用 ParserLimits 默认上限（parser.mbt:38-45）；tracer fixture 常规规模 |
| T-08-03-03 | Tampering | parser/ 被意外修改（冻结 baseline 漂移） | high | mitigate | D-04 单向纪律（incremental 只 import 不反向）+ git status 确认 parser/ 未动 |
| T-08-03-SC | Tampering | npm/pip/cargo 安装 | low | accept | 零新增外部依赖（Package Legitimacy Audit N/A）；无安装面 |

<verification>
- Task 1 后：`moon check --target native` 通过；incremental/ 依赖面 = @source/@syntax/@parser（+ 探针确认的 @dialect 类型面）；Edit::checked 复用 Span::checked。
- Task 2 后：`moon test --target native --package incremental` 通过——1 个 replace edit fixture 的 SC2 断言（增量 Bytes == 全量 Bytes）。
- 全程 `git status` 确认 parser/ 未动。
</verification>

<success_criteria>
- incremental/ 包基座（依赖面 + Edit 层）就位且编译通过，单向依赖纪律维持（parser 永不 import incremental）。
- 有界重解析 + span-overlap invalidation 在单路径（replace 编辑）端到端成立：`print_lossless(parse_incremental(x)) == print_lossless(parse_full(x))`（SC2）。
- tracer 架构可扩展——08-04 在此基础上扩展完整 edit fixture 矩阵与 SC3。
</success_criteria>

<output>
Create `.planning/phases/08-incremental-parsing-benchmark-gated/08-03-SUMMARY.md` when done.
</output>
