---
phase: 06-lint-and-fingerprint
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fingerprint/moon.pkg
  - fingerprint/normalize.mbt
  - fingerprint/hash.mbt
  - fingerprint/hash_test.mbt
  - api/api.mbt
  - api/moon.pkg
  - binding/schema.mbt
  - binding/exports.mbt
  - binding/json.mbt
  - binding/moon.pkg
  - test/fingerprint_test.mbt
autonomous: true
requirements:
  - FING-01
estimate:
  tokens: 78000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "fingerprint/ 独立库（D-01）：normalize(root, source, context) -> Bytes 只折叠 syntactic trivia（D-06）——空白/换行折叠为单空格（仅在两已发射 token 之间发射一个 0x20，首/尾 trivia 不产生字节）、注释整体剔除、BOM 剔除；关键字经 @dialect.classification_of（D-28 单源）ASCII 小写；标识符拼写/大小写、字面量内容、引号风格原样保留"
    - "归一化不变量快照锁定：SELECT a, b ≡ select\\n a , b ≡ SELECT /*c*/ a, b（同 canonical）；SELECT \"A\" ≠ SELECT 'A'（引号风格）、SELECT a ≠ SELECT A（标识符大小写）、SELECT 'x' ≠ SELECT 'y'（字面量内容）"
    - "fnv1a64(b\"\") == 0xcbf29ce484222325、fnv1a64(b\"a\") == 0xaf63dc4c8601ec8c（A2 核实后锁为断言）；UInt64 字面量后缀与环绕乘法语义经 moon check 探针确认并记录（A1/Open Question 1，[ASSUMED] resolved）"
    - "api.fingerprint_text(raw, parse_options) -> Result[FingerprintResult, ParseError] 复用 format_text 内部 parse：validate_limits → SourceText::new_with_limit → parse_with_limits_context → is_valid 门禁 → normalize + fnv1a64；FingerprintResult{ fingerprint: UInt64, normalized: Bytes }"
    - "binding schema v2 bump 纯加法（Pitfall V6）：validate_schema_version 保留现有 5 命名空间，新增 LINT_SCHEMA_VERSION=\"fathom.lint.v1\" + FINGERPRINT_SCHEMA_VERSION=\"fathom.fingerprint.v1\""
    - "fathom_fingerprint_v1(raw, dialect, profile, mode) 输出 fathom.fingerprint.v1 envelope；fingerprint 字段序列化为十进制 JSON string（Json::string(value.to_string())），绝不 Json::number(to_double())（2^53 精度丢失，JS 宿主读舍入值）"
    - "FLAGGED ASSUMPTION (FING-01 unclassified, RESOLVED): fingerprint 是总函数——空文档 → canonical 空字节 → 指纹 = FNV-1a offset basis；error/skipped 材料原样发射；任何输入（含空/错误树）都产出确定性指纹，仅有效语句上语义有意义（Open Question 3 推荐）"
    - "FLAGGED ASSUMPTION (LINT-01 empty, fingerprint 面, RESOLVED): 空输入 normalize 产出空 canonical bytes，fingerprint = FNV-1a offset basis；无 null 输入面（Bytes 参数）"
    - "FLAGGED ASSUMPTION (LINT-01 adjacency, fingerprint 面, RESOLVED): 相邻 token 之间零宽度（无空白）不插入分隔符；空白 run 折叠为恰一个 0x20"
  artifacts:
    - fingerprint/moon.pkg（import syntax + dialect + source，无 analyzer/parser，D-01/D-21）
    - fingerprint/normalize.mbt（normalize(root, source, context) -> Bytes）
    - fingerprint/hash.mbt（fnv1a64(bytes) -> UInt64，FNV-1a 64-bit 纯函数）
    - fingerprint/hash_test.mbt（FNV 向量 + 归一化不变量 + UInt64 探针）
    - api/api.mbt（fingerprint_text + FingerprintResult 结构）
    - binding/schema.mbt（LINT_SCHEMA_VERSION + FINGERPRINT_SCHEMA_VERSION 常量 + validate_schema_version 加法分支）
    - binding/exports.mbt（fathom_fingerprint_v1 #export_name）
    - binding/json.mbt（fingerprint_result_json，UInt64 十进制 string）
    - binding/moon.pkg（js+wasm exports 增 fathom_fingerprint_v1）
  key_links:
    - "normalize → fnv1a64：canonical bytes 是哈希唯一输入；两者由 api.fingerprint_text 串联（D-08）"
    - "关键字折叠只消费 @dialect.classification_of（D-28）——fingerprint/ 不建第二关键字表（Pitfall 14 / naming gate）"
    - "binding 序列化 UInt64 走十进制 string——JS 宿主读到精确值（非舍入）"
    - "schema v2 bump 是加法不是替换——既有 fathom.parse/format/complete/error/capabilities.v1 消费者不受影响"
  prohibitions:
    - statement: "fingerprint 归一化永不折叠标识符大小写、字面量内容或引号风格（FING-01 保留语义，CONTEXT Deferred 明确反需求）"
    - statement: "fingerprint 永不归一化序列化 JSON（必须归一化 CST——Pitfall V4，schema 版本漂移不得改变指纹值）"
    - statement: "UInt64 指纹 wire 序列化永不使用 Json::number(to_double())（2^53 精度丢失）"
    - statement: "FNV-1a 是公开非加密哈希——只作标识/缓存/差异/CI 用途，永不用于防篡改/认证"
---

<objective>
交付 **FING-01** 的第一层：`fingerprint/` 独立库（CST→canonical→UInt64）、`api.fingerprint_text` 入口、以及 binding schema v2 bump + `fathom_fingerprint_v1` wire 导出。

Purpose: FING-01 的核心承诺（ROADMAP SC3/SC4）是「稳定 SQL 指纹与归一化形式」——跨空白/关键字大小写/注释稳定、保留标识符拼写/字面量内容/引号风格、`UInt64` 哈希跨 Native/JS/linear-Wasm 一致。本 plan 建立 `fingerprint/` 库与 wire 面，使 `api.fingerprint_text` 成为可调用入口、`fathom_fingerprint_v1` 成为宿主可消费的 ABI；CLI/parity 在 06-04 完成。
Output: `fingerprint/` 库 + `api.fingerprint_text` + binding schema v2 bump + `fathom_fingerprint_v1`。

Execution note（scope_sanity WARNING 2）：本 plan files_modified=10（binding 三文件为单函数加法、fingerprint/ 为新建库，3 tasks 尚可控）——执行时**按 task 原子提交**（Task 1/2/3 各一次 commit，勿整 plan 一次提交），隔离 fingerprint 库 / api 入口 / binding wire 三块；**Task 1 的 UInt64 探针务必首跑**（`moon check --target native` 先核实 UInt64 字面量后缀与 `*` 环绕乘法语义，A1/Open Question 1 结论记录后再写 hash.mbt 正文），探针失败即停，避免基于错误语义堆叠实现。
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-lint-and-fingerprint/06-CONTEXT.md
@.planning/phases/06-lint-and-fingerprint/06-RESEARCH.md
@.planning/phases/06-lint-and-fingerprint/06-PATTERNS.md
@formatter/case.mbt
@formatter/layout.mbt
@syntax/syntax.mbt
@source/source.mbt
@dialect/classification.mbt
@api/api.mbt
@binding/schema.mbt
@binding/exports.mbt
@binding/json.mbt
@binding/moon.pkg
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: fingerprint/ 库 — normalize + fnv1a64 + UInt64 探针 + 向量锁定</name>
  <files>
    fingerprint/moon.pkg
    fingerprint/normalize.mbt
    fingerprint/hash.mbt
    fingerprint/hash_test.mbt
  </files>
  <read_first>
    formatter/case.mbt（rewrite_keyword_case + ascii_case_fold，case.mbt:21-50 —— 关键字折叠模板）
    formatter/layout.mbt（is_comment / is_line_comment / layout_document_trivia 的 leaf 分类形状，layout.mbt 注释判定 + format.mbt:118-143）
    syntax/syntax.mbt（SyntaxNode.children / SyntaxLeaf.kind / LeafKind::SourceToken|SourceTrivia|SourceError|SourceSkipped，syntax.mbt:55-58）
    source/source.mbt（SourceText::slice / Span::checked / byte_length，source.mbt:8-14,96-104）
    dialect/classification.mbt（classification_of(context, raw) -> KeywordEntry?，classification.mbt:99-106）
    06-RESEARCH.md Pattern 4（归一化语义 + 不变量）、Pattern 5（FNV-1a 常量与签名）、Open Question 1/2/3
    06-PATTERNS.md「fingerprint/normalize.mbt」「fingerprint/hash.mbt」「fingerprint/hash_test.mbt」
  </read_first>
  <behavior>
    - Test 1（FNV 向量）：fnv1a64(b"") 精确等于 0xcbf29ce484222325（offset basis）；fnv1a64(b"a") 精确等于 0xaf63dc4c8601ec8c
    - Test 2（归一化等价不变量）：normalize(SELECT a, b) == normalize(select\\n a , b) == normalize(SELECT /*c*/ a, b)
    - Test 3（语义保留不变量）：normalize(SELECT "A") != normalize(SELECT 'A')；normalize(SELECT a) != normalize(SELECT A)；normalize(SELECT 'x') != normalize(SELECT 'y')
    - Test 4（总函数）：normalize(空文档) == 空 Bytes，fnv1a64(空) == offset basis；error/skipped 材料原样发射
  </behavior>
  <action>
    创建 `fingerprint/` 独立库（D-01）：`fingerprint/moon.pkg` 用 `pkgtype(kind: "library")`，import 仅 `fathom/sql/syntax` @syntax、`fathom/sql/dialect` @dialect、`fathom/sql/source` @source——绝不 import analyzer/parser（D-21/D-27 单向纪律，A3）。实现 `fingerprint/hash.mbt`：`pub fn fnv1a64(bytes : Bytes) -> UInt64` 纯函数，常量 `FNV1A_OFFSET_BASIS : UInt64` 与 `FNV1A_PRIME : UInt64` 分别取标准 FNV-1a 64-bit offset basis `0xcbf29ce484222325` 与 prime `0x100000001b3`；逐字节 `hash = (hash lxor byte) * prime`。**首任务探针（[ASSUMED] A1/Open Question 1）**：先 `moon check --target native` 核实 MoonBit UInt64 字面量后缀（预期 `UL`）与 `*` 是否为 64-bit 环绕乘法；若 `*` 为 checked（溢出 panic）则改用显式 wrapping 乘法（手工 `+`/移位或 core 提供的 wrapping API），把探针结论写进 hash.mbt 注释与 hash_test.mbt。实现 `fingerprint/normalize.mbt`：`pub fn normalize(root : @syntax.SyntaxNode, source : @source.SourceText, context : @dialect.DialectContext) -> Bytes`，递归走读 root.children()，对每个 leaf 按 LeafKind 分派（D-06）：`SourceToken` → 调 `@dialect.classification_of(context, raw)`（D-28 单源）命中则发射 `entry.word` 的 ASCII 小写（复用 case.mbt:35-50 `ascii_case_fold` 语义，不得复制关键字表），未命中则原样发射；`SourceTrivia` → 用 `is_comment`（layout.mbt 注释判定形状）判定 Comment 整体剔除（不产生字节）、Whitespace/Newline 折叠为单 `0x20`（仅在两已发射 token 之间发射一个；首/尾 trivia 不产生字节）、Bom 剔除；`SourceError`/`SourceSkipped` → 原样发射（总函数，Open Question 3 推荐）。实现 `fingerprint/hash_test.mbt`：锁 Test 1-4 为 `assert_eq` 断言，FNV 向量必须对照独立 FNV 参考核实后再定（Open Question 2，A2）；`moon test --update` 是唯一快照写路径。
  </action>
  <verify>
    <automated>moon check --target native && moon test --target native --package fingerprint</automated>
  </verify>
  <acceptance_criteria>
    - `fnv1a64(b"")` 返回 `0xcbf29ce484222325`、`fnv1a64(b"a")` 返回 `0xaf63dc4c8601ec8c`（断言锁定，A2 核实后不再漂移）
    - normalize 不变量 Test 2/3 全绿：空白/换行/注释等价输入产出相同 canonical；引号风格/标识符大小写/字面量内容不同则 canonical 不同
    - 空文档与 error/skipped 材料产出确定性 fingerprint（总函数，Test 4 绿）
    - fingerprint/moon.pkg 无 analyzer/parser import（D-01/D-21 负门禁成立）
    - UInt64 字面量后缀与环绕乘法结论记录在 hash.mbt 注释（A1 resolved）
  </acceptance_criteria>
  <done>fingerprint/ 库能对任意 parsed CST 产出 canonical bytes 与 UInt64 指纹；FNV 向量与归一化不变量被测试锁定</done>
  <reversibility rating="one-way">D-07 FNV-1a 算法与 D-06 归一化语义决定全部已发布指纹值；换算法/改折叠语义需指纹版本迁移——决策已在 CONTEXT.md 锁定（--auto），不插 re-ask checkpoint（planner-reversibility anti-pattern）</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: api.fingerprint_text + FingerprintResult</name>
  <files>
    api/api.mbt
    api/moon.pkg
    test/fingerprint_test.mbt
  </files>
  <read_first>
    api/api.mbt format_text（api.mbt:566-580 —— validate_limits → SourceText::new_with_limit → parse_with_limits_context → is_valid 门禁，fingerprint_text 复用同款内部 parse）
    api/api.mbt 类型别名块（api.mbt:463-472，D-38 再导出模式）
    api/moon.pkg（import 面）
    fingerprint/normalize.mbt + fingerprint/hash.mbt（Task 1 产物，normalize + fnv1a64 签名）
  </read_first>
  <behavior>
    - Test 1：fingerprint_text(b"SELECT 1", ParseOptions::new("doris","4.x","strict")) -> Ok，fingerprint 为 UInt64、normalized 为稳定 bytes
    - Test 2：同一输入不同空白/关键字大小写/注释 → 相同 fingerprint（跨空白/大小写/注释稳定）
    - Test 3：fingerprint_text(b"SELECT \"A\"", ...) 与 fingerprint_text(b"SELECT 'A'", ...) → 不同 fingerprint（引号风格保留）
  </behavior>
  <action>
    扩展 `api/moon.pkg`：增 `fathom/sql/fingerprint` @fingerprint import。在 `api/api.mbt` 新增 `pub(all) struct FingerprintResult { pub fingerprint : UInt64, pub normalized : Bytes }`（pub(all) 以便 binding 跨包字面量构造，moon 0.1.20260724 约束），并新增 `pub fn fingerprint_text(raw : Bytes, parse_options : ParseOptions) -> Result[FingerprintResult, ParseError]`：完全镜像 `format_text`（api.mbt:566-580）的解析前置——`validate_limits` → `SourceText::new_with_limit` → `parser.parse_with_limits_context(source, parse_options.dialect_context, parser_mode, parser_limits)` → `parsed.root.is_valid(source.byte_length())` 门禁（InvalidSyntaxTree 返回 Err）；通过后调 `@fingerprint.normalize(parsed.root, source, parse_options.dialect_context)` 得 canonical bytes，再 `@fingerprint.fnv1a64(canonical)` 得 UInt64，组装 FingerprintResult。解析职责留在 api（fingerprint/ 不 import parser，D-01）；不新增 api→fingerprint 反向依赖（fingerprint/ 是叶子库）。在 api/api.mbt 或 test 包内加集成断言：fingerprint_text 对等价输入产出相同指纹、对引号风格差异产出不同指纹（Test 1-3）。
  </action>
  <verify>
    <automated>moon check --target native && moon test --target native --package test</automated>
  </verify>
  <acceptance_criteria>
    - api.mbt 导出 fingerprint_text 与 FingerprintResult；fingerprint_text 对有效输入返回 Ok 且 fingerprint 为 UInt64
    - 等价输入（空白/关键字大小写/注释差异）指纹相同；引号风格差异指纹不同（Test 1-3 绿）
    - api/moon.pkg 含 fingerprint import；fingerprint/ 仍不 import parser（D-01）
    - 解析前置与 format_text 一致：超限/无效树返回 ParseError（InputTooLarge/InvalidSyntaxTree），不 panic
  </acceptance_criteria>
  <done>api.fingerprint_text 可对任意输入产出稳定 UInt64 指纹 + normalized bytes，语义保留不变量由测试锁定</done>
  <reversibility rating="one-way">D-06 归一化语义经 api 入口固化；改动改变所有已发布指纹值——决策已在 CONTEXT.md 锁定（--auto），不插 re-ask checkpoint</reversibility>
</task>

<task type="auto">
  <name>Task 3: binding schema v2 bump + fathom_fingerprint_v1 + fingerprint_result_json</name>
  <files>
    binding/schema.mbt
    binding/exports.mbt
    binding/json.mbt
    binding/moon.pkg
  </files>
  <read_first>
    binding/schema.mbt validate_schema_version（schema.mbt:20-28）+ format_result_json（schema.mbt:116-140）
    binding/exports.mbt fathom_parse_v1 / fathom_format_v1（exports.mbt:30-75 —— dialect-first 校验 + json_bytes 组装模板）
    binding/json.mbt byte_array_json + format_diagnostic_json（json.mbt:7-14,41-53）
    binding/moon.pkg（js+wasm exports 列表）
    api/api.mbt fingerprint_text + FingerprintResult（Task 2 产物）
  </read_first>
  <action>
    在 `binding/schema.mbt` 增两个常量 `pub const LINT_SCHEMA_VERSION : String = "fathom.lint.v1"` 与 `pub const FINGERPRINT_SCHEMA_VERSION : String = "fathom.fingerprint.v1"`，并把 `validate_schema_version`（schema.mbt:20-28）的 match 增 `LINT_SCHEMA_VERSION | FINGERPRINT_SCHEMA_VERSION => Ok(())` 分支——**纯加法**（Pitfall V6），现有 5 个命名空间分支原样保留。在 `binding/json.mbt` 新增 `pub fn fingerprint_result_json(source : Bytes, result : @api.FingerprintResult, dialect : String, profile : String, exact_release : String) -> String`：envelope 含 `"schema_version": Json::string("fathom.fingerprint.v1")`、`"source_transport": Json::string(SOURCE_TRANSPORT)`、dialect/profile/exact_release 元数据（D-09：消费者只能在同 selection 内比较指纹）、`"source_bytes": byte_array_json(source)`、`"normalized": byte_array_json(result.normalized)`、**`"fingerprint": Json::string(result.fingerprint.to_string())`——十进制 string，绝不 `Json::number(result.fingerprint.to_double())`**（2^53 精度丢失，schema.mbt:107 反模式勿复制）。在 `binding/exports.mbt` 新增 `#export_name("fathom_fingerprint_v1") pub fn fathom_fingerprint_v1(raw : Bytes, dialect : String, profile : String, mode : String) -> Bytes`：先 `@api.ParseOptions::new(dialect, profile, mode)`（dialect-first，T-09-10）失败返回 `parse_error_bytes`；成功 `@api.fingerprint_text` 得 Ok(result) → `json_bytes(fingerprint_result_json(raw, result, dialect, profile, selection.exact_release()))`，Err(error) → `parse_error_bytes(error)`（exports.mbt:30-34 形状）。在 `binding/moon.pkg` 的 js 与 wasm 两个 `exports` 数组各追加 `"fathom_fingerprint_v1"`（缺注册 = 编译通过但产物缺符号，Pitfall 17；两个数组必须同时）。
  </action>
  <verify>
    <automated>moon check --target native && moon build --target js --package binding && moon test --target native --package binding</automated>
  </verify>
  <acceptance_criteria>
    - validate_schema_version 接受 fathom.lint.v1 + fathom.fingerprint.v1，且原 5 命名空间仍接受（Pitfall V6 加法）
    - fathom_fingerprint_v1 在 js+wasm 两个 exports 列表注册，binding 产物含该符号
    - fingerprint_result_json 的 fingerprint 字段是十进制 JSON string（`"fingerprint":"...数字..."`），绝不出现 number 形式（to_double 反模式）
    - 未知 dialect/profile 经 fathom_fingerprint_v1 返回 fathom.error.v1 结构化错误（FATHOM-SCHEMA-003/007）
  </acceptance_criteria>
  <done>fathom.fingerprint.v1 wire envelope 可被 Native/JS/Wasm 宿主消费；schema v2 bump 为纯加法且旧消费者不受影响</done>
  <reversibility rating="one-way">D-05 wire 命名空间是公开 ABI；发布后改名需迁移所有宿主——决策已在 CONTEXT.md 锁定（--auto），不插 re-ask checkpoint</reversibility>
</task>

</tasks>

## Artifacts this phase produces

- **新符号（新增包）：**
  - `fingerprint/moon.pkg` — library 包，import syntax/dialect/source（无 analyzer/parser）。
  - `fingerprint/hash.mbt` — `pub fn fnv1a64(bytes : Bytes) -> UInt64`（FNV-1a 64-bit 纯函数）；常量 `FNV1A_OFFSET_BASIS`、`FNV1A_PRIME`。
  - `fingerprint/normalize.mbt` — `pub fn normalize(root : @syntax.SyntaxNode, source : @source.SourceText, context : @dialect.DialectContext) -> Bytes`（CST→canonical，D-06 语义）。
  - `fingerprint/hash_test.mbt` — FNV 向量 + 归一化不变量 + UInt64 探针断言。

- **新符号（扩展既有文件）：**
  - `api/api.mbt` — `pub(all) struct FingerprintResult { fingerprint: UInt64, normalized: Bytes }`；`pub fn fingerprint_text(raw : Bytes, parse_options : ParseOptions) -> Result[FingerprintResult, ParseError]`。
  - `binding/schema.mbt` — `pub const LINT_SCHEMA_VERSION : String = "fathom.lint.v1"`；`pub const FINGERPRINT_SCHEMA_VERSION : String = "fathom.fingerprint.v1"`；`validate_schema_version` 增两个加法分支。
  - `binding/exports.mbt` — `#export_name("fathom_fingerprint_v1") pub fn fathom_fingerprint_v1(raw, dialect, profile, mode) -> Bytes`。
  - `binding/json.mbt` — `pub fn fingerprint_result_json(source, result, dialect, profile, exact_release) -> String`（fingerprint 十进制 string）。
  - `binding/moon.pkg` — js+wasm exports 各增 `"fathom_fingerprint_v1"`。

- **明确不改动：** `fingerprint/moon.pkg` 不 import analyzer/parser；`binding/schema.mbt` 原 5 命名空间分支不动；`formatter/`、`syntax/`、`source/`、`dialect/` 不修改。

## threat_model

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 调用方 source bytes → fingerprint normalize | 未信任 SQL；normalize 只按 leaf kind + `classification_of` 分类，不解释语义 |
| fingerprint/ → api/binding wire | UInt64 指纹在此跨边界；必须十进制 string 序列化（2^53 精度） |
| binding export → JS/Wasm 宿主 | 唯一参数为 raw bytes + dialect/profile/mode 字符串；无对象句柄跨边界 |

### STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01-01 | Tampering | `fingerprint_result_json` 的 UInt64 序列化 | high | mitigate | `Json::string(result.fingerprint.to_string())` 十进制 string；`to_double()` 反模式禁用（ASVS V5 数值精确） |
| T-06-01-02 | DoS | normalize/hash 对超大输入 | medium | mitigate | 输入经 `ParseLimits`（max_bytes 8 MiB 等，api.mbt:9-15）前置封顶；normalize/fnv1a64 均为 O(n) 单次线性走查，无嵌套放大 |
| T-06-01-03 | Spoofing | FNV-1a 碰撞 | low | accept | FNV-1a 是公开非加密哈希，可构造碰撞——文档明示只作缓存/差异/CI 标识，不做防篡改/认证（RESEARCH Security Domain） |
| T-06-01-04 | Tampering | 关键字折叠建第二关键字表 | medium | mitigate | 只消费 `@dialect.classification_of`（D-28）；建表会被 Phase 9 naming gate 与 D-28 双拒绝 |
| T-06-01-SC | Tampering | npm/pip/cargo 安装 | low | accept | 本阶段零新增外部依赖（D-07）；无安装面 |

<verification>
- Task 1 后：`moon test --target native --package fingerprint` 全绿（FNV 向量 + normalize 不变量 + 空/错误树总函数）。
- Task 2 后：`moon test --target native --package test` 全绿；api.fingerprint_text 集成断言（等价稳定 + 引号/字面量保留）通过。
- Task 3 后：`moon build --target js --package binding` 通过；validate_schema_version 7 命名空间全接受；fingerprint 字段十进制 string 断言通过。
</verification>

<success_criteria>
- fingerprint/ 库 + api.fingerprint_text + fathom_fingerprint_v1 全部交付，FING-01 的「稳定跨空白/关键字大小写/注释、保留标识符/字面量/引号」不变量由测试锁定。
- schema v2 bump 纯加法完成，既有 5 命名空间消费者零影响；UInt64 十进制 string 序列化落地。
</success_criteria>

<output>
Create `.planning/phases/06-lint-and-fingerprint/06-01-SUMMARY.md` when done.
</output>
