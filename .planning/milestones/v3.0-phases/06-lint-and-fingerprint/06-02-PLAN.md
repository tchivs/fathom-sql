---
phase: 06-lint-and-fingerprint
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lint/moon.pkg
  - lint/rules.mbt
  - lint/registry.mbt
  - lint/engine.mbt
  - lint/fixes.mbt
  - lint/lint_test.mbt
autonomous: true
requirements:
  - LINT-01
estimate:
  tokens: 72000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "lint/ 独立库（D-01）：moon.pkg import syntax/dialect/source/formatter/analyzer，永不 import parser（D-21/D-27 单向纪律）"
    - "SQLFluff 风格注册表（D-02）：默认注册表含稳定码 FATHOM-LINT-001..008，每条规则含 name/category/default_severity/fixable/applies_to/enabled 元数据；LintSeverity::from_id 支持 error/warning/info"
    - "LintOptions per-rule 覆盖（D-04）：enable/disable + severity 覆盖解析，未知规则码返回结构化错误（CLI exit 2 依据）"
    - "FATHOM-LINT-003 select-star-without-limit：顶层 Select 族投影含裸 `*` 且语句无 LIMIT token → warning finding；带 LIMIT 或非顶层不触发"
    - "analyzer 增强规则 FATHOM-LINT-004..007 仅在注入 AnalysisResult 时激活（映射 unknown-table/unknown-column/ambiguous-reference/function-arity）；无 catalog 时静默跳过（ANLY-01，语法 valid 通道永不改变）"
    - "autofix（D-03）：只替换 SourceToken leaf span 的最小 span edits；重叠 edit 跳过并标记不可修复；error/missing/skipped 树 → accepted=false + 空输出 + 恰好一个 FATHOM-LINT-000 拒绝诊断（D-33），绝不部分编辑"
    - "每个 fix 通过 round-trip 断言：应用后 untouched 字节逐字节不变、reparse 干净"
    - "FLAGGED ASSUMPTION (LINT-01 adjacency, RESOLVED): findings 永不合并——每个 violation 独立成 finding；相邻（end_byte == start_byte）或分离的 edits 各自独立应用；仅重叠区间（start < next_start < end）跳过后者"
    - "FLAGGED ASSUMPTION (LINT-01 empty, RESOLVED): 空输入 → 零 findings、accepted=true、空输出；单语句输入 → 按 statement_id 归属各自 findings"
    - "FLAGGED ASSUMPTION (LINT-01 ordering, RESOLVED): findings 按 start_byte 源码序稳定排序，同 start_byte 按规则码字典序排序，跨运行确定"
  artifacts:
    - lint/moon.pkg（import syntax/dialect/source/formatter/analyzer，无 parser）
    - lint/rules.mbt（LintSeverity/LintRule 元数据 + from_id）
    - lint/registry.mbt（default_registry 8 规则 + LintOptions::new 覆盖校验）
    - lint/engine.mbt（run_rules CST 走查 + statement_family 分发 + 8 规则判定）
    - lint/fixes.mbt（apply_fixes 最小 span edits + first_unsafe_element D-33 安全闸 + FATHOM-LINT-000）
    - lint/lint_test.mbt（规则正反例 + ANLY-01 skip + round-trip/refusal/overlap 用例）
  key_links:
    - "autofix 安全闸直接复用 formatter/refuse.mbt 的 first_unsafe_element（D-33，refuse.mbt:6-17）——不重写 unsafe 扫描"
    - "规则判定只走 @dialect.classification_of（D-28 单源）——不建第二关键字表（Pitfall 14）"
    - "statement_family 分发复用 formatter/layout.mbt:329-335 的遍历形状（lint 内重新实现该私有函数）"
    - "FATHOM-LINT-000 保留为引擎级拒绝码（镜像 FATHOM-FORMAT-001，format.mbt:145-154），规则码从 001 起"
  prohibitions:
    - statement: "autofix 永不整文档重排（D-03 只做 violation 局部最小 span edits）"
    - statement: "对含 error/missing/skipped 材料的树永不做部分编辑（D-33 拒绝绝对）"
    - statement: "lint/ 内永不建第二关键字表（D-28 单源纪律）"
    - statement: "analyzer 增强规则无 catalog 时永不产生 findings（ANLY-01 静默跳过，wire/CLI 面不暴露 004-007）"
---

<objective>
交付 **LINT-01** 的核心：`lint/` 独立库——SQLFluff 风格规则注册表（稳定码 FATHOM-LINT-001..008、per-rule enable/disable + severity）+ CST 走查引擎 + 安全无损 autofix（最小 span edits、D-33 拒绝绝对、round-trip 断言）。

Purpose: LINT-01 的用户承诺（ROADMAP SC1/SC2）是「可运行 Doris 专属 Lint 规则集」且「autofix 保留注释/trivia/格式、拒绝 error 树的 unsafe 编辑」。本 plan 交付 lint/ 库本体（规则判定 + 修复引擎），api/binding/CLI 消费面在 06-03、06-04 接入。
Output: lint/ 库（rules/registry/engine/fixes + 白盒测试）。
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-lint-and-fingerprint/06-CONTEXT.md
@.planning/phases/06-lint-and-fingerprint/06-RESEARCH.md
@.planning/phases/06-lint-and-fingerprint/06-PATTERNS.md
@formatter/refuse.mbt
@formatter/options.mbt
@formatter/layout.mbt
@formatter/format.mbt
@analyzer/analysis.mbt
@syntax/syntax.mbt
@dialect/classification.mbt
@source/source.mbt
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: lint 库核心 — 注册表 + engine（FATHOM-LINT-003 端到端）+ moon.pkg</name>
  <files>
    lint/moon.pkg
    lint/rules.mbt
    lint/registry.mbt
    lint/engine.mbt
    lint/lint_test.mbt
  </files>
  <read_first>
    formatter/options.mbt（KeywordCase::from_id 的 string-id→enum 形状，options.mbt:101-110）
    formatter/layout.mbt statement_family（layout.mbt:329-335 —— lint 内重新实现的遍历形状）
    formatter/format.mbt statement 分发循环（format.mbt:45-66）
    syntax/syntax.mbt（SyntaxNode/SyntaxLeaf/LeafKind/SyntaxElement，syntax.mbt:55-58）
    dialect/classification.mbt（classification_of / is_reserved_word，classification.mbt:99-106,148-160）
    06-RESEARCH.md Pattern 1（注册表元数据形状 + LintSeverity::from_id 签名）、Pattern 2（statement_family + 规则 003 判定示例）
    06-PATTERNS.md「lint/rules.mbt」「lint/registry.mbt」「lint/engine.mbt」
  </read_first>
  <behavior>
    - Test 1（003 正例）：SELECT * FROM t → 恰 1 条 FATHOM-LINT-003 finding，severity=warning
    - Test 2（003 LIMIT 反例）：SELECT * FROM t LIMIT 10 → 0 条 003
    - Test 3（003 无星反例）：SELECT a, b FROM t → 0 条 003
    - Test 4（注册表）：default_registry() 含 8 条规则且 code 为 FATHOM-LINT-001..008；LintSeverity::from_id("error"/"warning"/"info") 往返，未知返回 None
    - Test 5（LintOptions）：LintOptions::new 对未知规则码返回 Err（结构化）
  </behavior>
  <action>
    创建 `lint/` 独立库（D-01）。`lint/moon.pkg` 用 `pkgtype(kind: "library")`，import `fathom/sql/syntax` @syntax、`fathom/sql/dialect` @dialect、`fathom/sql/source` @source、`fathom/sql/formatter` @formatter（复用 first_unsafe_element，D-33）、`fathom/sql/analyzer` @analyzer（增强规则，ANLY-01）；**永不 import parser**（D-21/D-27）。实现 `lint/rules.mbt`：`pub(all) enum LintSeverity { Error, Warning, Info }` 与 `pub(all) enum RuleSetting { Disabled, Severity(LintSeverity) }`，`pub fn LintSeverity::from_id(id : String) -> LintSeverity?` 镜像 KeywordCase::from_id（error/warning/info，未知 None）；`pub(all) struct LintRule { pub code : String, pub name : String, pub category : String, pub default_severity : LintSeverity, pub fixable : Bool, pub applies_to : String, pub enabled : Bool }`（pub(all) 因 moon 0.1.20260724 跨包字面量要求）。实现 `lint/registry.mbt`：`pub fn default_registry() -> Array[LintRule]` 返回 8 条规则元数据（FATHOM-LINT-001 unquoted-reserved-word/style/warning/fixable、002 version-gated-syntax/advisory/info/not-fixable、003 select-star-without-limit/safety/warning/not-fixable、004 unknown-table/correctness/error/not-fixable、005 unknown-column/correctness/error/not-fixable、006 ambiguous-reference/correctness/warning/not-fixable、007 function-arity/correctness/error/not-fixable、008 deprecated-syntax/deprecation/info/not-fixable，applies_to 按 RESEARCH 表 2.1+）；`pub(all) struct LintOptions { pub overrides : Array[RuleOverride] }` 与 `pub(all) struct RuleOverride { pub code : String, pub setting : RuleSetting }`，`pub fn LintOptions::new(overrides : Array[RuleOverride]) -> Result[LintOptions, LintError]` 校验 code 存在于注册表（未知 → Err，D-04 exit 2 依据）。实现 `lint/engine.mbt`：`pub fn run_rules(root : @syntax.SyntaxNode, source : @source.SourceText, context : @dialect.DialectContext, analysis : @analyzer.AnalysisResult?, options : LintOptions) -> Array[LintFinding]`——按 Statement 节点遍历、以 statement_family（镜像 layout.mbt:329-335 私有函数形状，lint 内重写）分发；本任务只完整实现 FATHOM-LINT-003：对 `SyntaxKind::Select` 族扫描 source token 序列，投影含裸 `*`（token 原始字节 `*`）且整个 statement 无 LIMIT token（字节级 bytes_equal_ci，镜像 analyzer.mbt:87）→ push `LintFinding{code:"FATHOM-LINT-003", severity:"warning", message:"top-level SELECT * without LIMIT", start_byte/end_byte=该投影 token span, statement_id, fix:None}`；其余规则留桩返回空（Task 2 填充）。`lint/rules.mbt` 中定义 `pub(all) struct LintFinding { pub code : String, pub severity : String, pub message : String, pub start_byte : Int, pub end_byte : Int, pub statement_id : UInt, pub fix : LintEdit? }` 与 `pub(all) struct LintEdit { pub start_byte : Int, pub end_byte : Int, pub new_text : Bytes }`（对齐 api.PrimitiveDiagnostic 扁平形状，api.mbt:305-313）。实现 `lint/lint_test.mbt`：锁 Test 1-5 白盒断言（直接调包内函数，completion_test.mbt:19-46 形状）。
  </action>
  <verify>
    <automated>moon test --target native --package lint</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT * FROM t` 产出恰 1 条 FATHOM-LINT-003（warning，span 指向投影 `*`）；`... LIMIT 10` 与 `SELECT a, b` 产出 0 条
    - default_registry() 恰含 FATHOM-LINT-001..008 八条元数据；LintSeverity::from_id 对 error/warning/info 往返、未知返回 None
    - LintOptions::new 对未知规则码返回 Err（结构化错误）
    - lint/moon.pkg 无 parser import；engine 的 statement_family 遍历与 layout.mbt:329-335 语义一致
    - run_rules 签名含可选 analysis 参数（ANLY-01 接入点），本任务 analysis=None 路径可用
  </acceptance_criteria>
  <done>lint 库可对一条规则（003）端到端从 CST 走查产出 finding；注册表与 LintOptions 覆盖面就绪</done>
  <reversibility rating="costly">D-02 规则码是公共契约（发布后重编号破坏下游配置）；初始注册表即冻结码集——决策已在 CONTEXT.md 锁定（--auto），不插 re-ask checkpoint</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 引擎扩展 — 规则 001/002/008 + analyzer 增强映射 004-007</name>
  <files>
    lint/engine.mbt
    lint/rules.mbt
    lint/registry.mbt
    lint/lint_test.mbt
  </files>
  <read_first>
    dialect/classification.mbt（classification_of / is_reserved_word / is_clause_keyword，classification.mbt:99-160）
    analyzer/analysis.mbt（AnalysisResult/AnalysisDiagnostic/Binding，analysis.mbt:34-59 —— 增强规则消费模型）
    formatter/case.mbt（ascii_case_fold，case.mbt:35-50）
    06-RESEARCH.md Pattern 1 规则表（001-008 判定来源/确定性）、Open Question 6（001 上下文判定）
    06-PATTERNS.md「lint/engine.mbt」
  </read_first>
  <behavior>
    - Test 1（001）：`SELECT order FROM t` → FATHOM-LINT-001（保留字 order 作标识符，warning）；`ORDER BY x` 中 ORDER 不触发；`` SELECT `order` FROM t `` 不触发（quoted 永不判关键字）
    - Test 2（002）：版本门禁构造（如 MERGE 语句）在较新 profile 下 → FATHOM-LINT-002 advisory；所选 profile 已含该构造 → 0 条
    - Test 3（008）：deprecated 语法词表命中 → FATHOM-LINT-008 info
    - Test 4（004-007 映射）：注入含 unknown-table/unknown-column/ambiguous-reference/function-arity 的 AnalysisResult → 分别映射 FATHOM-LINT-004..007 且保留 span
    - Test 5（ANLY-01 skip）：analysis=None → 0 条 004-007 findings
  </behavior>
  <action>
    扩展 `lint/engine.mbt` 实现其余 CST 规则（D-02）：**001 unquoted-reserved-word**——**判定范围 = 投影项首位（Open Question 6 窄版，时间受限降级项）**：仅当保留字 token 出现在 Select 投影项首位时判定；**表名位置（如 `SELECT a FROM order`）不在本阶段 001 判定范围**——Open Question 6 主推荐为「子句边界内标识符位置」检测，需轻量子句边界解析，属 8 条中最高实现成本，本阶段有意采用窄版先锁定正反例（Pitfall 2），表名位置留待后续扩展；判定只走 `@dialect.is_reserved_word(context, raw)`（D-28 单源），quoted token（原始字节以反引号/双引号包裹）永不判定关键字，`fixable=true` 携带 fix（反引号包裹，Task 3 应用）。**002 version-gated-syntax**——构造需较新 profile 的 advisory：以 `context.profile_id` 与规则内建 introduced 表比较（镜像 DorisFeature introduced_profile 语义，规则 002 用 `applies_to` 元数据字符串比较 profile 顺序 2.1 < 3.x < 4.x），命中 → info finding，fix=None。**008 deprecated-syntax**——规则内建版本化废弃词表（corpus 驱动，RESEARCH 表注释），命中 → info finding，fix=None。实现 **analyzer 增强映射 004-007**（D-02 可选 analyzer）：当 `analysis` 为 Some(_) 时，遍历 `analysis.diagnostics`（analysis.mbt:34-59），按 code 映射 unknown-table→004、unknown-column→005、ambiguous-reference→006、function-arity→007（preserve start_byte/end_byte，message 沿用），severity 按 RESEARCH 表（error/error/warning/error）；analysis=None 时全部跳过（ANLY-01，Test 5）。在 `lint/registry.mbt` 确认 8 条规则 `enabled` 默认 true、applies_to 与 002 判定一致。扩展 `lint/lint_test.mbt` 锁 Test 1-5（含构造合成 AnalysisResult 的正反例，独立于 parser）。
  </action>
  <verify>
    <automated>moon test --target native --package lint</automated>
  </verify>
  <acceptance_criteria>
    - 001：`SELECT order FROM t` 触发、`ORDER BY` 不触发、`` SELECT `order` `` 不触发（Pitfall 2 正反例锁定）
    - 001 判定范围**有意采用窄版**（仅投影项首位，Open Question 6 时间受限降级项）；表名位置正例 `SELECT a FROM order` 明确**不在本阶段 001 判定范围**（主推荐为子句边界内位置检测，留待后续扩展）——本 task 不要求、也不测试表名位置判定
    - 002：profile 门禁 advisory 命中/未命中按 applies_to 语义正确；008 词表命中/未命中正确
    - 004-007：注入 AnalysisResult 时按 code 映射且 span 保留；analysis=None 时零 004-007 findings（ANLY-01）
    - lint/engine.mbt 全部 8 规则可判定；无第二关键字表（D-28，grep 确认只走 classification_of）
  </acceptance_criteria>
  <done>lint/ 引擎覆盖全部 8 条规则；analyzer 增强规则有 catalog 激活、无 catalog 静默跳过</done>
  <reversibility rating="costly">001/002/008 判定语义与 004-007 analyzer 映射是公共可观察行为（D-02），所涉决策已在 Task 1 评级（规则码 public contract，--auto 不插 re-ask checkpoint）</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: autofix — fixes.mbt 最小 span edits + D-33 安全闸 + round-trip</name>
  <files>
    lint/fixes.mbt
    lint/lint_test.mbt
  </files>
  <read_first>
    formatter/refuse.mbt（first_unsafe_element，refuse.mbt:6-17 —— autofix 安全闸直接复用）
    formatter/format.mbt（refusal_diagnostic 拒绝诊断形状，format.mbt:145-154）
    source/source.mbt（Span::checked，source.mbt:15-20 —— edit span 越界校验）
    syntax/syntax.mbt（LeafKind::SourceToken 判断，syntax.mbt:55-58）
    06-RESEARCH.md Pattern 3（autofix 最小 span edits + 重叠跳过 + round-trip）
    06-PATTERNS.md「lint/fixes.mbt」
  </read_first>
  <behavior>
    - Test 1（拒绝绝对）：error/missing/skipped 树 → apply_fixes 返回 accepted=false、output 空、恰 1 条 FATHOM-LINT-000，绝不部分编辑
    - Test 2（fix 应用）：`` SELECT order FROM t `` → 001 fix 应用后输出 `` SELECT `order` FROM t ``，untouched 字节逐字节不变
    - Test 3（round-trip）：fix 输出重新 parse 干净（无 error/missing/skipped）
    - Test 4（重叠跳过）：两个 finding 区间重叠 → 后者跳过并标记不可修复；相邻（end==start）各自应用
  </behavior>
  <action>
    实现 `lint/fixes.mbt`（D-03）：`pub fn apply_fixes(root : @syntax.SyntaxNode, source : @source.SourceText, context : @dialect.DialectContext, findings : Array[LintFinding], options : LintOptions) -> LintResult`。**安全闸**：先跑 `@formatter.first_unsafe_element(root)`（直接复用 refuse.mbt:6-17，D-33）——命中 error/missing/skipped → 返回 `{accepted:false, findings:[], output:b"", diagnostics:[FATHOM-LINT-000 拒绝诊断]}`，拒绝诊断镜像 format.mbt:145-154 形状（severity:"error"、code:"FATHOM-LINT-000"、message 说明拒绝 error 树、start_byte/end_byte=unsafe element span、statement_id）。**最小 span edits**：对每个 `finding.fix`（仅 fixable 规则 001 携带），只替换 `LeafKind::SourceToken` 的 leaf span（trivia 是独立 leaf 天然不重叠，Pitfall 1），edit 经 `@source.Span::checked`（source.mbt:15-20，ASVS V5）校验越界；按 start_byte 源码序应用，重叠区间（start < next_start < end）跳过后者并标记不可修复；相邻/分离 edits 各自独立应用（adjacency flagged assumption）。**round-trip 断言**：应用后对 output 重新 parse，reparse 含 error/missing/skipped → 拒绝（防御纵深，D-03）。定义 `pub(all) struct LintResult { pub accepted : Bool, pub findings : Array[LintFinding], pub output : Bytes, pub diagnostics : Array[LintDiagnostic] }`（镜像 FormatResult 拒绝契约，error.mbt:42-49；`pub(all) struct LintDiagnostic` 六字段扁平形状同 PrimitiveDiagnostic）。扩展 `lint/lint_test.mbt` 锁 Test 1-4（含真实 parse 的 error 树夹具 + 重叠 edits 构造）。
  </action>
  <verify>
    <automated>moon test --target native --package lint</automated>
  </verify>
  <acceptance_criteria>
    - error/missing/skipped 树 → accepted=false、output 空、恰 1 条 FATHOM-LINT-000（D-33，Test 1）
    - `` SELECT order FROM t `` → `` SELECT `order` FROM t ``，untouched 字节逐字节不变（Test 2）
    - fix 输出 reparse 干净（Test 3 round-trip）
    - 重叠 edits 跳过并标记、相邻/分离 edits 各自应用（Test 4 adjacency）
    - edits 只替换 SourceToken leaf span + Span::checked 越界校验（ASVS V5）
  </acceptance_criteria>
  <done>autofix 安全闸 + 最小 span edits + round-trip 断言交付；D-33 拒绝绝对落地</done>
  <reversibility rating="one-way">D-03 autofix 编辑语义是安全承诺；改整文档重排需重定义安全边界并迁移断言——决策已在 CONTEXT.md 锁定（--auto），不插 re-ask checkpoint</reversibility>
</task>

</tasks>

## Artifacts this phase produces

- **新符号（新增包）：**
  - `lint/moon.pkg` — library 包，import syntax/dialect/source/formatter/analyzer（无 parser，D-01/D-21）。
  - `lint/rules.mbt` — `pub(all) enum LintSeverity { Error, Warning, Info }`、`pub(all) enum RuleSetting { Disabled, Severity(LintSeverity) }`、`pub(all) struct LintRule`（code/name/category/default_severity/fixable/applies_to/enabled）、`pub fn LintSeverity::from_id`、`pub(all) struct LintFinding`（code/severity/message/start_byte/end_byte/statement_id/fix）、`pub(all) struct LintEdit`（start_byte/end_byte/new_text）。
  - `lint/registry.mbt` — `pub fn default_registry() -> Array[LintRule]`（8 规则）、`pub(all) struct LintOptions { overrides : Array[RuleOverride] }`、`pub(all) struct RuleOverride { code, setting }`、`pub fn LintOptions::new`（校验）。
  - `lint/engine.mbt` — `pub fn run_rules(root, source, context, analysis : @analyzer.AnalysisResult?, options) -> Array[LintFinding]`（8 规则判定 + statement_family 分发）。
  - `lint/fixes.mbt` — `pub fn apply_fixes(root, source, context, findings, options) -> LintResult`（D-33 安全闸 + 最小 span edits + round-trip）；`pub(all) struct LintResult { accepted, findings, output, diagnostics }`、`pub(all) struct LintDiagnostic`。
  - `lint/lint_test.mbt` — 规则正反例 + ANLY-01 skip + round-trip/refusal/overlap 用例。

- **明确不改动：** parser/、formatter/、analyzer/（仅被 lint 消费）、syntax/、source/、dialect/ 不改；api/binding/CLI 消费面在 06-03/06-04。

## threat_model

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 调用方 source bytes → lint 规则引擎 | 未信任 SQL；规则只按 leaf kind + `classification_of` 分类，不解释语义 |
| lint finding/edit → 调用方 | autofix edit span 在此跨边界；必须经 Span::checked + reparse 防御 |
| analyzer AnalysisResult → lint 增强规则 | catalog 注入的元数据不可信（T-02-42），只作 code/span 映射，绝不进入语法 valid 通道 |

### STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02-01 | Tampering | autofix edit 越界/破坏源码 | high | mitigate | edits 只替换 SourceToken leaf span + `@source.Span::checked` 校验（source.mbt:15-20）+ 应用后 reparse 防御（D-03） |
| T-06-02-02 | Tampering | error 树部分编辑 | high | mitigate | `first_unsafe_element` 安全闸（refuse.mbt:6-17）→ accepted=false + 空输出 + 恰一 FATHOM-LINT-000（D-33 拒绝绝对） |
| T-06-02-03 | Tampering | SQL 字符串注入 lint 消息/JSON | medium | mitigate | findings/诊断消息结构化编码 + 长度上限（引用源码截断保留 span），不拼原始字节入消息 |
| T-06-02-04 | DoS | 病态 CST 触发非确定判定 | medium | mitigate | 每条规则单次线性 CST 走查 + ParseLimits（api 层 8 MiB 封顶，本库无解析职责） |
| T-06-02-05 | Tampering | 增强规则泄漏进语法通道 | medium | mitigate | ANLY-01：分析诊断独立通道；analysis=None 时 004-007 静默跳过，语法 valid 通道永不改变 |
| T-06-02-SC | Tampering | npm/pip/cargo 安装 | low | accept | 零新增外部依赖（D-01 复用仓库内 formatter/analyzer） |

<verification>
- Task 1 后：`moon test --target native --package lint` 全绿（003 正反例 + 注册表 8 码 + LintOptions 校验）。
- Task 2 后：001/002/008 正反例 + 004-007 映射 + ANLY-01 skip 用例全绿。
- Task 3 后：refusal/round-trip/overlap 用例全绿；grep 确认 engine 只走 classification_of（无第二关键字表）。
</verification>

<success_criteria>
- lint/ 库交付全部 8 条规则 + autofix（D-03 最小 span edits + D-33 拒绝绝对 + round-trip）。
- LINT-01 的「规则集 + 安全 autofix」库面完成；api/binding/CLI 在 06-03/06-04 接入后满足 SC1/SC2。
</success_criteria>

<output>
Create `.planning/phases/06-lint-and-fingerprint/06-02-SUMMARY.md` when done.
</output>
