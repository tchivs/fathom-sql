---
phase: 13-toolchain-and-editor-packaging
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - analyzer/analyzer.mbt
  - analyzer/moon.pkg
  - test/analyzer_test.mbt
  - docs/API.md
requirements: [TOOL-03]
autonomous: false
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A consumer can run syntax-only Flink analysis through @analyzer.resolve_table_references(parsed.root, source_bytes, catalog) on a Flink document parsed via @api.parse_with_ids(raw, \"flink\", \"flink-2.3.0\", \"strict\") and receive the target table names for the supported Flink statement families (Insert/Update/Delete/CreateTable/CreateView) that exist in the injected catalog (TOOL-03, D-03, D-22)."
    - "The analyzer extension is table-level only and aligned with the Doris minimal scope (D-22/D-24): column/identifier-level reference resolution and type diagnostics are explicitly deferred to v2 — TOOL-03's 'column, and identifier references' is scoped to 'supported references = target table references' and documented as such in docs/API.md (TOOL-03, D-03, D-24)."
    - "Parser validity is independent of the catalog: parsing a Flink document with no catalog yields byte-identical syntax results to parsing with a catalog — the analyzer consumes only @syntax.SyntaxNode + caller-supplied source bytes and never re-enters the parser (ANLY-01, D-21)."
    - "The D-21 read-only syntax-view discipline is preserved: analyzer/moon.pkg imports only fathom/sql/syntax (and core Bytes/String builtins — no parser/token/lexer/api/source import), and parser/moon.pkg contains no reference to the analyzer (negative gate unchanged) (TOOL-03, D-21, Pitfall 7)."
    - "Flink leading-prefix shapes resolve correctly: UPSERT INTO (the parser maps UPSERT -> SyntaxKind::Insert at parser.mbt:4196), INSERT OVERWRITE ... PARTITION (...), and CREATE [TEMPORARY] VIEW are handled by leading_prefix_end so target_table_name returns the right table name for each family (TOOL-03, D-03, RESEARCH Open Question 2)."
    - "A no-catalog Flink analysis returns an empty result array (missing tables are simply absent), never a fabricated resolution, and never a parser-validity change (ANLY-01, D-22, never-fabricate discipline)."
  artifacts:
    - path: "analyzer/analyzer.mbt"
      provides: "leading_prefix_end extended with Flink shapes (UPSERT INTO, INSERT OVERWRITE ... PARTITION skip, CREATE [TEMPORARY] VIEW); resolve_table_references matched-kind set extended to include CreateView (and the Flink target-table families per the confirmed D-03 scope)"
    - path: "test/analyzer_test.mbt"
      provides: "Flink analyzer fixtures: Insert/Upsert, Update, Delete, CreateTable, CreateView against a StaticCatalog; no-catalog case asserting empty result + byte-identical parser validity; leading-prefix shape assertions"
    - path: "docs/API.md"
      provides: "Flink analyzer scope documented: supported Flink statement families + table-level boundary + v2 column/identifier deferral (D-03, D-24)"
  key_links:
    - from: "analyzer/analyzer.mbt leading_prefix_end Flink arms"
      to: "parser/parser.mbt Flink dispatch (UPSERT -> Insert at parser.mbt:4196)"
      via: "the parser maps UPSERT to SyntaxKind::Insert, so the Insert arm must skip UPSERT INTO before the table name; the CreateView arm must skip CREATE [TEMPORARY] VIEW (D-03, RESEARCH Open Question 2)"
    - from: "analyzer/analyzer.mbt resolve_table_references"
      to: "syntax/syntax.mbt SyntaxKind (Insert/Update/Delete/CreateTable/CreateView)"
      via: "the matched-kind match arm extends to CreateView and the Flink target-table families; the walk stays read-only over @syntax.SyntaxNode children (D-21)"
    - from: "analyzer/moon.pkg imports"
      to: "parser/moon.pkg negative gate"
      via: "analyzer imports only fathom/sql/syntax; parser never imports analyzer — the negative gate (grep) stays green (D-21, Pitfall 7)"
  prohibitions:
    - "The analyzer MUST NOT import parser/token/lexer/api/source — D-21 read-only syntax-view discipline; analyzer/moon.pkg imports only fathom/sql/syntax (and core builtins) (D-21, Pitfall 7)."
    - "The parser MUST NOT import the analyzer — parser/moon.pkg negative gate stays green; parser validity is independent of catalog/analyzer (ANLY-01, D-21)."
    - "The analyzer MUST NOT fabricate resolutions — a no-catalog or missing-table case returns an empty/absent result, never an invented table name (D-22, never-fabricate discipline)."
    - "The analyzer MUST NOT do column/identifier-level resolution or type diagnostics in this phase — TOOL-03's scope is target-table references; full ANAL-01 name resolution is v2 (D-24)."
    - "Doris analyzer behavior MUST stay byte-identical (PARITY-01): the Flink arms are additive and the existing Doris leading_prefix_end arms are unchanged."
---

<objective>
把 Phase 11 的 Flink 语句族贯通到 analyzer（TOOL-03 / D-03）：扩展既有 resolve_table_references 走查至 Flink 语句族（Insert/Update/Delete/CreateTable/CreateView 的 Flink leading-prefix 形态，含 UPSERT INTO 与 CREATE VIEW），保持 D-21 只读 syntax-view 纪律（analyzer 只 import fathom/sql/syntax；parser 永不 import analyzer；负门禁维持）与可选 catalog（无 catalog → 空结果、parser validity 字节不变，ANLY-01）。表级解析与 Doris 当前最小范围对齐（D-22/D-24）；column/identifier 级引用解析与类型诊断按 D-24 顺延 v2。本 plan 是 Phase 13 的 wave-1 analyzer 切片：一条端到端路径 = 「flink 文档 → @api.parse_with_ids → resolve_table_references(root, source_bytes, StaticCatalog) → 目标表名列表」；先以 UPSERT INTO + CreateView 两个关键形态证明，再扩展到全部 Flink 目标表族与 no-catalog 案例。

Purpose: TOOL-03 要求消费者能对 Flink 运行语法级 analyzer 与可选 catalog，解析受支持的表/列/标识符引用，且 parser validity 与 catalog 无关；D-03（one-way — resolve_table_references 是公共 API，扩展改变返回集合语义）锁定扩展既有走查。Research 现状（Pattern 3）：leading_prefix_end 已处理 INSERT [OVERWRITE] [TABLE]|INTO、UPDATE、DELETE FROM、CREATE [TEMPORARY] [EXTERNAL] TABLE [IF NOT EXISTS]；缺口是 UPSERT INTO（parser.mbt:4196 把 UPSERT 映射到 Insert）与 CreateView（analyzer.mbt:276-283 未匹配）。D-03 的 one-way 门禁由 Task 1 checkpoint 确认扩展形态。Doris analyzer 行为零漂移。

Output: analyzer/analyzer.mbt（leading_prefix_end Flink 臂 + resolve_table_references 匹配集扩展）、test/analyzer_test.mbt（Flink fixtures + no-catalog）、docs/API.md（Flink analyzer 范围文档）。
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-toolchain-and-editor-packaging/13-CONTEXT.md
@.planning/phases/13-toolchain-and-editor-packaging/13-RESEARCH.md
@.planning/phases/13-toolchain-and-editor-packaging/13-PATTERNS.md
@/opt/source/Fathom/analyzer/analyzer.mbt
@/opt/source/Fathom/analyzer/moon.pkg
@/opt/source/Fathom/test/analyzer_test.mbt
@/opt/source/Fathom/syntax/syntax.mbt
@/opt/source/Fathom/parser/parser.mbt

This is the phase wave-1 analyzer tracer. D-03 (one-way — resolve_table_references is a public API whose extension changes the returned-set semantics) is gated by Task 1 checkpoint; the tracer (Task 2) proves the two critical Flink shapes (UPSERT INTO + CREATE VIEW) end-to-end; Task 3 expands to the full Flink target-table family matrix + no-catalog case. The D-21 boundary is the hard constraint: analyzer/moon.pkg imports ONLY fathom/sql/syntax (verified in RESEARCH — "analyzer/moon.pkg imports only fathom/sql/syntax"), and the negative gate (parser/moon.pkg contains no analyzer reference) must stay green. The existing signature is `pub fn[T : Catalog] resolve_table_references(node : @syntax.SyntaxNode, source_bytes : Bytes, catalog : T) -> Array[String]` (analyzer.mbt:263) — the extension must NOT change this signature (D-03 is about the returned-set semantics, not the ABI). RESEARCH Open Question 2: confirm the parser's Insert kind for UPSERT INTO (parser.mbt:4196) and whether INSERT OVERWRITE ... PARTITION (...) needs a partition-skip arm before the table name — extend leading_prefix_end accordingly.

> **Contribution hooks:** (1) API Coverage checkpoint — the analyzer surface (resolve_table_references) is already a public @api-adjacent library function documented in docs/API.md; no new wire export. (2) Assumption-Delta — the analyzer was Doris-only (matched kinds Insert/Update/Delete/Merge/CreateTable); the plural transition adds CreateView + Flink leading prefixes. (3) Schema Push gate — no ORM/database schema files; skipped silently.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Confirm the Flink analyzer extension scope — D-03 one-way door</name>
  <read_first>
    @.planning/phases/13-toolchain-and-editor-packaging/13-CONTEXT.md
    @.planning/phases/13-toolchain-and-editor-packaging/13-RESEARCH.md
    @/opt/source/Fathom/analyzer/analyzer.mbt
    @/opt/source/Fathom/parser/parser.mbt
  </read_first>
  <acceptance_criteria>
    - The confirm step selects option-a (extend the existing resolve_table_references in place, Flink leading prefixes + CreateView in the matched set, table-level only) or a documented alternative; the resume-signal records the selection so the tracer task proceeds with the confirmed scope.
    - The locked D-03 scope is honored: no separate Flink analyzer entry, no column/identifier-level resolution or type diagnostics in this phase (v2 per D-24).
  </acceptance_criteria>
  <decision>The exact form of the D-03 analyzer extension that becomes the public resolve_table_references contract: (a) extend the EXISTING resolve_table_references in place — add the Flink leading-prefix shapes to leading_prefix_end and add CreateView (plus the Flink target-table families the ROADMAP scopes) to the matched-kind set, keeping the signature `pub fn[T : Catalog] resolve_table_references(node, source_bytes, catalog) -> Array[String]` unchanged; table-level only, column/identifier resolution deferred to v2 (D-24). (b) Do NOT create a separate resolve_flink_table_references entry (would fragment the public API and violate the D-21 read-only walk pattern). (c) Do NOT add column/identifier-level resolution or type diagnostics in this phase.</decision>
  <context>D-03 is one-way because resolve_table_references is a public API whose extension changes the returned-set semantics — once published, callers depend on the extended families (CONTEXT D-03). The RESEARCH Pattern 3 confirms the two gaps: UPSERT INTO (parser maps UPSERT -> SyntaxKind::Insert, parser.mbt:4196) and CreateView (not currently matched at analyzer.mbt:276-283). The alternative forms: a separate Flink entry would fragment the API and duplicate the read-only walk; a column/identifier-level scope would exceed the D-24 v2 deferral and the Doris-aligned minimal scope (D-22). The table-level boundary is locked by TOOL-03's "受支持引用 = 目标表引用" and must be documented in docs/API.md.</context>
  <options>
    <option id="option-a">
      <name>Extend the existing resolve_table_references in place — Flink leading prefixes + CreateView in the matched set, table-level only (recommended — D-03 locked)</name>
      <pros>Preserves the single public API and the read-only syntax-view walk; aligns with the Doris minimal scope (D-22/D-24); the returned-set semantics change is explicit and documented; no API fragmentation.</pros>
      <cons>The matched-kind set grows (CreateView + Flink families), so the returned set for a mixed-dialect document changes — a documented behavioral contract change (one-way).</cons>
    </option>
    <option id="option-b">
      <name>Separate resolve_flink_table_references entry</name>
      <pros>Isolates the Flink behavior from the Doris walk.</pros>
      <cons>Fragments the public API, duplicates the read-only walk, and contradicts D-03's "扩展既有走查" (extend the existing walk) — rejected by the locked decision.</cons>
    </option>
    <option id="option-c">
      <name>Include column/identifier-level resolution in this phase</name>
      <pros>Broader analyzer capability.</pros>
      <cons>Exceeds D-24's explicit v2 deferral and the TOOL-03 table-level boundary; contradicts the locked scope — rejected.</cons>
    </option>
  </options>
  <resume-signal>Select option-a (extend in place, table-level only) or describe a modified analyzer scope; the tracer task is authored only after this confirmation.</resume-signal>
</task>

<task type="tracer">
  <name>Flink analyzer tracer — UPSERT INTO + CREATE VIEW end-to-end with StaticCatalog</name>
  <files>analyzer/analyzer.mbt, test/analyzer_test.mbt</files>
  <read_first>
    @.planning/phases/13-toolchain-and-editor-packaging/13-CONTEXT.md
    @.planning/phases/13-toolchain-and-editor-packaging/13-RESEARCH.md
    @.planning/phases/13-toolchain-and-editor-packaging/13-PATTERNS.md
    @/opt/source/Fathom/analyzer/analyzer.mbt
    @/opt/source/Fathom/test/analyzer_test.mbt
    @/opt/source/Fathom/parser/parser.mbt
  </read_first>
  <action>Build the thinnest end-to-end Flink analyzer slice. (1) In analyzer/analyzer.mbt extend leading_prefix_end (analyzer.mbt:168-220): for SyntaxKind::Insert, after the existing INSERT [OVERWRITE] [TABLE]|INTO skip, add the UPSERT shape — when the first token is UPSERT (bytes_equal_ci(tokens[position], b"UPSERT")) consume UPSERT then INTO then the table name (the parser maps UPSERT INTO to SyntaxKind::Insert at parser.mbt:4196, so the analyzer's Insert arm must recognize it); also add the OVERWRITE ... PARTITION skip — after INSERT OVERWRITE, if the next significant word is PARTITION, skip the PARTITION (...) group before the table name (verify the parser's Flink INSERT OVERWRITE ... PARTITION form; if the parser places the table name BEFORE PARTITION, no skip is needed — confirm against parser.mbt Flink insert dispatch :4190-4210 and implement accordingly). (2) Add a CreateView arm to leading_prefix_end: skip CREATE [TEMPORARY] VIEW [IF NOT EXISTS] before the view-name tokens (mirror the parser's Flink create-view form). (3) In resolve_table_references (analyzer.mbt:276-283) extend the matched-kind match to add @syntax.SyntaxKind::CreateView to the existing Insert|Update|Delete|Merge|CreateTable arm (keep Merge — it exists for Doris 4.x; the Flink parser routes MERGE to the unsupported path per 11-02, so Merge stays Doris-only in the walk but the arm is shared). (4) Add test/analyzer_test.mbt fixtures: (a) UPSERT INTO — `UPSERT INTO t VALUES (1)` parsed via @api.parse_with_ids(raw, "flink", "flink-2.3.0", "strict") then resolve_table_references(parsed.root, source.bytes(), StaticCatalog with t) returns ["t"]; (b) CREATE VIEW — `CREATE VIEW v AS SELECT * FROM t` resolves v's source table t? — NO: CreateView's target_table_name returns the VIEW name (v), so resolve_table_references returns ["v"] when v is in the catalog (the view is the target); assert the walk resolves the CreateView target v (and does NOT resolve t unless t is also a matched family's target — CREATE VIEW's query body is not walked in this minimal scope; document this boundary). (c) a no-catalog case — the same Flink document with StaticCatalog::new([]) returns [] and `@api.parse_with_ids` output is byte-identical with/without catalog resolution (parser validity independent of catalog, ANLY-01).
  <acceptance_criteria>
    - `UPSERT INTO t VALUES (1)` under flink-2.3.0 resolves to ["t"] via resolve_table_references with a StaticCatalog containing t — the UPSERT leading-prefix arm works.
    - `CREATE VIEW v AS SELECT * FROM t` under flink-2.3.0 resolves to ["v"] (the CreateView target) with a catalog containing v — the CreateView matched-kind + leading-prefix arms work; the query body (t) is NOT walked in this minimal scope (documented boundary).
    - A no-catalog Flink analysis returns [] and the parse result is byte-identical to a catalog-present parse (`@api.parse_with_ids(raw, "flink", "flink-2.3.0", "strict")` output unchanged by whether resolution is called) — ANLY-01.
    - Doris analyzer behavior byte-identical: the existing test/analyzer_test.mbt Doris fixtures (analyzer_resolve_table_references_against_injected_catalog, empty/no-statement cases) still pass unchanged — the Flink arms are additive (PARITY-01).
    - analyzer/moon.pkg imports only fathom/sql/syntax: `grep -c "fathom/sql" analyzer/moon.pkg` == 1 (only the syntax import); parser/moon.pkg contains no "analyzer" reference (negative gate green).
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native --package analyzer --package test --package api && test "$(grep -c 'fathom/sql' analyzer/moon.pkg)" -eq 1 && ! grep -q 'analyzer' parser/moon.pkg</automated>
  </verify>
  <done>The Flink analyzer tracer resolves UPSERT INTO and CREATE VIEW targets through the existing resolve_table_references walk, the no-catalog case returns empty with byte-identical parser validity, the D-21 import discipline and negative gate stay green, and Doris analyzer behavior is unchanged.</done>
</task>

<task type="auto">
  <name>Full Flink target-table family matrix + no-catalog/empty cases + docs/API.md scope</name>
  <files>analyzer/analyzer.mbt, test/analyzer_test.mbt, docs/API.md</files>
  <read_first>
    @.planning/phases/13-toolchain-and-editor-packaging/13-RESEARCH.md
    @/opt/source/Fathom/analyzer/analyzer.mbt
    @/opt/source/Fathom/test/analyzer_test.mbt
  </read_first>
  <action>Complete the Flink analyzer matrix and document the scope. (1) Extend test/analyzer_test.mbt with a Flink family matrix fixture covering Insert/Update/Delete/CreateTable/CreateView (and the UPSERT variant) in one mixed script: e.g. `UPSERT INTO t1 VALUES (1); UPDATE t2 SET a=1 WHERE id=1; DELETE FROM t3 WHERE id=1; CREATE TABLE t4 (a INT); CREATE VIEW v5 AS SELECT * FROM t1;` parsed under flink-2.3.0 strict — with a StaticCatalog holding t1/t2/t3/t4/v5, resolve_table_references returns ["t1","t2","t3","t4","v5"] in source order (each matched family's target, absent tables omitted). (2) Verify each family's leading-prefix arm against the parser's Flink dispatch: Insert (INSERT/UPSERT [OVERWRITE] INTO + PARTITION skip), Update (UPDATE ... SET), Delete (DELETE FROM), CreateTable (CREATE [TEMPORARY] [EXTERNAL] TABLE [IF NOT EXISTS]), CreateView (CREATE [TEMPORARY] VIEW [IF NOT EXISTS]) — adjust leading_prefix_end arms if a fixture surfaces a shape gap (do NOT change the parser; the analyzer adapts to the parser's emitted kinds). (3) Add the empty-document case: resolve_table_references on an empty Flink document returns []. (4) Add a case proving the query-body boundary: a Flink INSERT ... SELECT * FROM src resolves ONLY the INSERT target (dst), NOT the SELECT source (src) — the walk is target-table-only in this minimal scope; document this in the test comment. (5) Update docs/API.md analyzer section: document that resolve_table_references now covers the Flink statement families (Insert/Upsert/Update/Delete/CreateTable/CreateView) under a flink selection, is table-level only, and that column/identifier-level resolution + type diagnostics are v2 (D-24); update the existing StaticCatalog case-sensitivity/last-wins notes verbatim (docs/API.md:294-303 and docs/zh-CN/API.md:293-302 in the same change set).
  <acceptance_criteria>
    - The mixed Flink family fixture resolves to ["t1","t2","t3","t4","v5"] in source order with a catalog holding all five targets; absent tables are omitted.
    - The INSERT ... SELECT * FROM src case resolves ONLY the INSERT target (dst), not the SELECT source (src) — the target-table-only boundary is asserted.
    - The empty Flink document case returns [].
    - docs/API.md (and docs/zh-CN/API.md) document the Flink analyzer scope: supported families, table-level boundary, v2 deferral; the existing case-sensitivity/last-wins notes are preserved verbatim.
    - Doris analyzer behavior byte-identical: the existing analyzer_test Doris fixtures pass; `moon test --package parity` (no --update) passes; diff_parity.py --frozen-only exits 0.
    - analyzer/moon.pkg still imports only fathom/sql/syntax; parser/moon.pkg negative gate green.
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native --package analyzer --package test --package api --package parity && python3 scripts/diff_parity.py --frozen-only && test "$(grep -c 'fathom/sql' analyzer/moon.pkg)" -eq 1 && ! grep -q 'analyzer' parser/moon.pkg</automated>
  </verify>
  <done>The Flink analyzer covers the full target-table family matrix (Insert/Upsert/Update/Delete/CreateTable/CreateView) with no-catalog/empty/query-body-boundary cases, the scope is documented in docs/API.md + docs/zh-CN/API.md, the D-21 import discipline and negative gate stay green, and Doris behavior is byte-identical.</done>
</task>

</tasks>

## Source Audit Coverage

| SOURCE | ID | Feature or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | User can run syntax-only Flink analysis with optional catalog without changing parser validity | 13-03 | COVERED | resolve_table_references extended to Flink families; no-catalog case byte-identical. |
| REQ | TOOL-03 | Syntax-only Flink analyzer with optional catalog to resolve supported table, column, and identifier references; parser validity independent of catalog | 13-03 | COVERED | Table-level resolution for Flink families; column/identifier deferred to v2 (D-24) and documented. |
| CONTEXT | D-03 | Extend existing resolve_table_references to Flink families (Insert/Update/Delete/CreateTable/CreateView Flink leading-prefix shapes); D-21 read-only; optional catalog; table-level only (one-way) | 13-03 | COVERED | Task 1 checkpoint (one-way) + Tasks 2-3: UPSERT/CreateView arms + full matrix. |
| CONTEXT | D-21 | Analyzer imports only fathom/sql/syntax; parser never imports analyzer; negative gate | 13-03 | COVERED | analyzer/moon.pkg grep == 1 fathom/sql import; parser/moon.pkg negative gate in every task verify. |
| CONTEXT | D-22 / D-24 | Table-level alignment with Doris minimal scope; column/identifier resolution + type diagnostics v2 | 13-03 | COVERED | Docs/API.md scope note; target-table-only boundary asserted. |
| RESEARCH | Pattern 3 | leading_prefix_end gaps: UPSERT INTO (parser.mbt:4196), CreateView (analyzer.mbt:276-283) | 13-03 | COVERED | Task 2 tracer arms. |
| RESEARCH | Open Question 2 | UPSERT analyzer arm + INSERT OVERWRITE PARTITION skip confirmation | 13-03 | COVERED | Task 2 verifies against parser.mbt dispatch; partition-skip implemented per parser form. |
| RESEARCH | Validation Arch | analyzer_test flink fixtures + no-catalog case + parser validity unchanged | 13-03 | COVERED | Tasks 2-3. |
| REQ | ANLY-01 | Parser validity independent of catalog | 13-03 | COVERED | No-catalog case asserts byte-identical parse. |
| REQ | PARITY-01 | Doris behavior equal to frozen baseline | 13-03 | COVERED | Doris analyzer fixtures unchanged; diff_parity --frozen-only. |
| PROBE | TOOL-03 boundary | supported references = target table references | 13-03 | FLAGGED | must_haves truth + docs note; column/identifier deferral explicit. |

<verification>
Run moon test --target native --package analyzer --package test --package api --package parity after each task; run python3 scripts/diff_parity.py --frozen-only after every change. Inspect: leading_prefix_end Flink arms (UPSERT INTO, INSERT OVERWRITE PARTITION, CREATE VIEW), resolve_table_references matched-kind set (CreateView added), analyzer/moon.pkg import list (syntax only), parser/moon.pkg negative gate, no-catalog byte-identical parse, target-table-only boundary (INSERT ... SELECT resolves only the target), docs/API.md scope update.
</verification>

<success_criteria>
- A consumer resolves Flink target tables for Insert/Update/Delete/CreateTable/CreateView through the existing resolve_table_references with an optional catalog.
- Parser validity is independent of catalog — no-catalog analysis returns empty with byte-identical parse results; column/identifier resolution is documented as v2.
- The D-21 import discipline and parser/analyzer negative gate stay green; Doris analyzer behavior is byte-identical.
</success_criteria>

<output>Create `.planning/phases/13-toolchain-and-editor-packaging/13-03-SUMMARY.md` when this plan executes.</output>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flink CST + caller source bytes -> resolve_table_references | The analyzer reads a caller-supplied parse result; it must not re-enter the parser or alter validity. |
| Catalog metadata -> analyzer | Caller-injected catalog is untrusted (T-02-42); it is consumed only by the analyzer and never reaches the parser/syntax-validity channel. |
| analyzer/moon.pkg imports -> syntax | The D-21 boundary: only fathom/sql/syntax may be imported; a wider import would couple the analyzer to parser internals. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-03-01 | Tampering | Analyzer imports parser/token/lexer/api, breaking the D-21 read-only boundary and coupling analysis to parser internals | high | mitigate | analyzer/moon.pkg imports only fathom/sql/syntax; grep gate == 1 fathom/sql import; parser/moon.pkg negative gate green (D-21, Pitfall 7). |
| T-13-03-02 | Spoofing | Analyzer fabricates a table resolution for a missing table or no catalog | high | mitigate | Missing tables are simply absent from the result; no-catalog returns empty; never-fabricate discipline (D-22, ANLY-01). |
| T-13-03-03 | Tampering | Catalog metadata reaches the parser and changes parser validity | high | mitigate | Analyzer consumes only @syntax.SyntaxNode + source bytes; no-catalog case asserts byte-identical parse output (ANLY-01, T-02-42). |
| T-13-03-04 | Tampering | Wrong leading-prefix skip resolves the wrong table (e.g. UPSERT arm skipping past the name) | medium | mitigate | UPSERT/CreateView/OVERWRITE-PARTITION arms mirror the parser's emitted kinds; per-family fixtures assert the exact target (RESEARCH Open Question 2). |
| T-13-03-05 | Tampering | Analyzer over-reaches into column/identifier resolution, changing the public returned-set semantics beyond the locked scope | high | mitigate | Table-level only; docs/API.md scope note; query-body boundary asserted (INSERT ... SELECT resolves only the target) (D-24). |
| T-13-03-06 | Tampering | Doris analyzer bytes drift when Flink arms are added | high | mitigate | Additive Flink arms; existing analyzer_test Doris fixtures unchanged; diff_parity --frozen-only (PARITY-01). |
| T-13-03-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new runtime dependencies (RESEARCH Package Legitimacy Audit N/A). |
</threat_model>

## Artifacts this plan produces

Files: analyzer/analyzer.mbt (modified — leading_prefix_end Flink arms + resolve_table_references matched-kind extension), analyzer/moon.pkg (unchanged — import discipline verified), test/analyzer_test.mbt (modified — Flink family matrix, UPSERT, CREATE VIEW, no-catalog, empty, query-body-boundary fixtures), docs/API.md + docs/zh-CN/API.md (modified — Flink analyzer scope note, table-level boundary, v2 deferral).

Symbols (analyzer/analyzer.mbt): leading_prefix_end Insert arm extended (UPSERT INTO, INSERT OVERWRITE ... PARTITION skip per parser form); new CreateView arm (CREATE [TEMPORARY] VIEW [IF NOT EXISTS]); resolve_table_references matched-kind set adds @syntax.SyntaxKind::CreateView (arm shared with Insert/Update/Delete/Merge/CreateTable).
Symbols (test/analyzer_test.mbt): flink_analyzer_upsert_into_resolves_target, flink_analyzer_create_view_resolves_target, flink_analyzer_family_matrix_source_order, flink_analyzer_no_catalog_empty_and_byte_identical_parse, flink_analyzer_empty_document_returns_empty, flink_analyzer_insert_select_resolves_only_target, doris_analyzer_behavior_unchanged.
Symbols (docs/API.md + docs/zh-CN/API.md): Flink analyzer scope paragraph (supported Flink families, table-level only, v2 deferral).
