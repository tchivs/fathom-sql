---
phase: 09-dialect-boundary-and-neutral-naming
plan: 06
type: execute
wave: 6
depends_on: [09-03, 09-04, 09-05]
files_modified:
  - lsp/handlers.mbt
  - lsp/documents.mbt
  - lsp/selection_test.mbt
  - lsp/protocol_test.mbt
  - lsp/diagnostics_formatting_test.mbt
  - lsp/completion_test.mbt
  - lsp/lifecycle_test.mbt
  - parity/fixtures/lsp-tracer.json
  - parity/baseline_test.mbt
autonomous: false
requirements: [DIALECT-01, DIALECT-03, DIALECT-04]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 4
  confidence: low
must_haves:
  truths:
    - "The LSP resolves an explicit dialect+profile per document through the D-01 three-level precedence — document-level explicit config > workspace/session explicit default > user-configured languageId mapping — and a missing selection at every level returns a structured config error instead of an implicit languageId fallback (DIALECT-01, D-02)."
    - "Every document carries its own dialect/profile context: Document gains dialect and profile fields, and every parse/format/completion call uses the document's context — no global profile string remains on ServerState (DIALECT-03/04, D-03)."
    - "Switching a document's dialect/profile re-parses the current revision and publishes only the newest version's results: the completion stale-check pattern is extended to diagnostics so a stale async result computed under an old selection/version is dropped (D-03)."
    - "LSP identity is neutral: serverInfo name is fathom-lsp, diagnostics source is fathom, the parse-fallback code is FATHOM-LSP-001, and LSP diagnostics carry FATHOM-PARSE-*/FATHOM-SCHEMA-* codes with fathom.parse.v1 schema references (NAME-03/NAME-02 surface, D-06/D-09/D-10)."
    - "A flink-selected document over LSP produces the explicit FATHOM-PARSE-008 not-implemented diagnostics from the 09-02 api path — never a silent empty result and never Doris acceptance (DIALECT-03, A1)."
    - "[probe DIALECT-01 concurrency] flagged-unverified: rapid didChange + dialect-switch races resolve by document version — only the newest version's diagnostics are published and stale async results under an old selection are dropped; the DocumentStore version guard extended to the publish path is the concurrency contract."
    - "[probe DIALECT-04 precision] flagged-unverified: LSP UTF-16 positions and byte offsets in diagnostics remain exact across a dialect-switch reparse (no span drift from the re-resolved context)."
  artifacts:
    - path: "lsp/handlers.mbt"
      provides: "DialectSelection struct, SelectionError, ServerState selection machinery (default_dialect/default_profile kept from 09-04 + language_mapping), resolve_selection with D-01 precedence, initialize_selection, workspace/didChangeConfiguration + workspace/configuration arms, document-context parse_document/formatting_result/completion_result, serverInfo fathom-lsp, source fathom, FATHOM-LSP-001"
    - path: "lsp/documents.mbt"
      provides: "Document.dialect : String and Document.profile : String fields; DocumentStore::open/change carry the resolved selection"
    - path: "lsp/selection_test.mbt"
      provides: "LSP smoke tests asserting structured config errors for missing, unknown, and conflicting selections plus dialect-switch and stale-response behavior"
    - path: "parity/fixtures/lsp-tracer.json"
      provides: "LSP trace fixture updated to fathom.parse.v1 with the dialect field (owned by this plan per 09-05)"
    - path: "parity/baseline_test.mbt"
      provides: "LSP homomorph callsites with the dialect argument; LSP-output snapshots updated once with pre-registered approved-change entries"
  key_links:
    - from: "lsp/handlers.mbt"
      to: "binding/schema.mbt"
      via: "resolve_selection validates through @binding.validate_dialect_profile(dialect, profile); config errors serialize as FATHOM-SCHEMA-007 (09-05 surface)"
    - from: "lsp/handlers.mbt"
      to: "api/api.mbt"
      via: "parse_document calls @api.parse_with_ids(document.text, document.dialect, document.profile, 'editor') — the 09-02 dialect-first signature with the document's own context"
    - from: "lsp/documents.mbt"
      to: "lsp/handlers.mbt"
      via: "DocumentStore version monotonicity (open/change reject older versions) is the guard the stale-response publication check reuses (D-03)"
    - from: "lsp/handlers.mbt"
      to: "lsp/serve.mbt"
      via: "serve_stdio(initial_dialect, initial_profile) from 09-04 feeds the workspace/session default source; the old ServerState.profile field is removed"
  prohibitions:
    - "No implicit languageId fallback (D-02): a document with no explicit selection anywhere receives a structured config diagnostic, never a guess; languageId participates only through a user-configured mapping."
    - "No global profile string remains: ServerState.profile is removed; every parse/format/completion call uses the document's dialect/profile context."
    - "No stale publications (D-03): diagnostics for a document version older than the current stored version are never sent; a dialect switch re-parses the current revision and drops old-context results."
    - "flink-selected documents never parse as Doris over LSP: the api FATHOM-PARSE-008 not-implemented path is surfaced as-is, unchanged, through publishDiagnostics."
---

<objective>
**As a** SQL editor user, **I want to** have the LSP resolve an explicit dialect and profile for every open document through document-level config, a workspace default, or an explicit languageId mapping — with a structured config error instead of a silent guess — **so that** diagnostics, formatting, and completion always run under the dialect I chose, and switching dialects refreshes results without stale responses.

Purpose: Deliver the LSP boundary of DIALECT-01 (explicit selection with D-01/D-02 precedence and structured config errors) plus the D-03 document-level context and stale-response protection, and complete the neutral LSP identity (fathom-lsp serverInfo/source, FATHOM-* diagnostics) started in 09-02..09-05. This is the last dialect-surface plan before the hosts cutover in 09-07 — the selection contract defined here is what vscode/, web/, and jetbrains/ feed from the client side.
Output: document-level DialectContext in the LSP state machine, three-level selection resolution with structured errors, dialect-switch reparse with version-guarded publication, neutral LSP identity, updated LSP tests/fixtures, and approved baseline snapshot updates.
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-dialect-boundary-and-neutral-naming/09-CONTEXT.md
@.planning/phases/09-dialect-boundary-and-neutral-naming/09-RESEARCH.md
@.planning/phases/09-dialect-boundary-and-neutral-naming/09-PATTERNS.md
@.planning/phases/09-dialect-boundary-and-neutral-naming/09-04-SUMMARY.md
@.planning/phases/09-dialect-boundary-and-neutral-naming/09-05-SUMMARY.md

This plan consumes two genuine interfaces from earlier waves: (1) 09-04's lsp/serve.mbt serve_stdio(initial_dialect : String?, initial_profile : String?) storing ServerState.default_dialect/default_profile fields (the CLI lsp subcommand feeds these as workspace defaults), and (2) 09-05's @binding.validate_dialect_profile + FATHOM-SCHEMA-007 + fathom.parse.v1 (the validation gate and wire identity). 09-02 threaded DialectContext through the api path with FATHOM-PARSE-008 for flink; the LSP layer still calls the old three-arg signatures with state.profile (lsp/handlers.mbt:79,169,269) and still emits doris-lsp/DORIS-LSP-001/source doris (handlers.mbt:45,84,160). Research rows 41-50 (LSP migration) and D-01/D-02/D-03 drive this plan; research A2 defines the transport (LSP 3.17 has no standard per-document dialect field — document-level config rides on didOpen extension fields + workspace/didChangeConfiguration settings, languageId mappings are client-configured). The completion stale-check pattern at handlers.mbt:253-257 is the model for the D-03 version guard on diagnostics.
> **Scope note (checker I-1, accepted):** 4 tasks, 9 files — cohesive document-context/selection work over the LSP state machine plus its parity fixture and callsites; each task is a vertical slice of the same D-01..D-03 selection contract, so no split is required.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the D-01 LSP selection contract and wire transport (one-way door)</name>
  <decision>LSP dialect-selection contract: the three-level precedence semantics, same-source conflict behavior, and the wire transport carrying document-level config — a public LSP contract, one-way after release.</decision>
  <context>D-01 locks the precedence (document-level explicit config > workspace/session explicit default > languageId explicit mapping) and that same-priority-source conflicts return a structured config error — never a silent guess. D-02 locks the no-implicit-fallback rule: no document-level config, no workspace default, and no user-configured languageId mapping means a structured config error, not a languageId-derived guess. Research A2 notes LSP 3.17 has no standard per-document dialect field, so the transport is host-defined: initializationOptions {dialect, profile} (workspace/session default, extended from the current {profile} shape), workspace/didChangeConfiguration settings for the fathom section (workspace default + languageId mapping), didOpen/didChange params extension fields for document-level explicit config, and serve_stdio(initial_dialect, initial_profile) from 09-04 as the CLI-supplied workspace default. Two workspace/session sources (initializationOptions vs serve_stdio args) are the same priority — a conflict between them must error per D-01. 09-06's contract is what 09-07's hosts (VS Code/IntelliJ) feed from the client side, so the transport shape is a one-way door.</decision>
  <options>
    <option id="option-a">
      <name>Locked D-01/D-02 + research A2 transport (recommended)</name>
      <pros>Matches the locked decisions verbatim; document-level config rides didOpen/didChange extension fields, workspace default comes from initializationOptions OR serve_stdio args (conflict between the two -> structured -32602 error), languageId mapping only from user configuration; missing everywhere -> a single FATHOM-SCHEMA-007 config diagnostic on the document; initialize missing/unknown/conflicting -> -32602 structured message.</pros>
      <cons>None beyond the locked one-way commitment; the host clients (09-07) must send the exact extension fields.</cons>
    </option>
    <option id="option-b">
      <name>Implicit languageId fallback when nothing else is set</name>
      <pros>Simpler client UX on first open.</pros>
      <cons>Directly violates D-02 (locked): every host's first-open behavior would silently guess a dialect, making diagnostics and formatting untrustworthy.</cons>
    </option>
    <option id="option-c">
      <name>workspace/configuration pull instead of didChangeConfiguration push</name>
      <pros>More canonical LSP 3.17 (server pulls the settings section).</pros>
      <cons>Requires server-initiated request/response correlation; VS Code's standard path pushes settings via didChangeConfiguration. Both can coexist — push primary, pull optional (the plan implements both arms).</cons>
    </option>
  </options>
  <resume-signal>Select option-a (or describe the exact transport/error-shape modifications); the tracer then implements the confirmed contract.</resume-signal>
</task>

<task type="tracer">
  <name>Task 2 (tracer): End-to-end LSP document dialect selection — initialize {dialect, profile} -> didOpen -> per-document context diagnostics with neutral identity</name>
  <files>lsp/handlers.mbt, lsp/documents.mbt, lsp/protocol_test.mbt, lsp/diagnostics_formatting_test.mbt, parity/fixtures/lsp-tracer.json</files>
  <read_first>
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-CONTEXT.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-RESEARCH.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-PATTERNS.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-04-SUMMARY.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-05-SUMMARY.md
    @/opt/source/Fathom/lsp/handlers.mbt
    @/opt/source/Fathom/lsp/documents.mbt
    @/opt/source/Fathom/lsp/protocol_test.mbt
    @/opt/source/Fathom/lsp/diagnostics_formatting_test.mbt
    @/opt/source/Fathom/parity/fixtures/lsp-tracer.json
  </read_first>
  <action>Wire ONE end-to-end LSP document path with explicit dialect selection and neutral identity, production-quality, on the single happy path.

lsp/documents.mbt: add pub dialect : String and pub profile : String to the Document struct (research row 42) and extend DocumentStore::open and DocumentStore::change to accept and store the resolved dialect/profile alongside uri/version/text; the version monotonicity rules (open rejects version <= stored, change requires version > stored) stay untouched.

lsp/handlers.mbt: replace the initialize profile intake with initialize_selection — read initializationOptions { dialect, profile } (research row 43), validate via @binding.validate_dialect_profile(dialect, profile) (09-05 gate), store the workspace/session default into ServerState.default_dialect/default_profile (fields added by 09-04; on success set initialized as today); the serverInfo name becomes "fathom-lsp" (handlers.mbt:160, research row 46). Add a minimal resolve_selection(state, params) -> Result[DialectSelection, SelectionError] implementing the tracer's slice of the D-01 precedence: document-level explicit config first (didOpen params extension fields "dialect"/"profile" inside the textDocument object per the Task 1 confirmation; if absent, the workspace/session default from default_dialect/default_profile); the languageId mapping and conflict paths are Task 3's expansion. parse_document(state, document) becomes parse_document(state, document) using the document's own context: @api.parse_with_ids(document.text, document.dialect, document.profile, "editor") (handlers.mbt:79, research row 48/49); the fallback diagnostic code "DORIS-LSP-001" becomes "FATHOM-LSP-001" (handlers.mbt:84, row 45) and diagnostic source "doris" becomes "fathom" (handlers.mbt:45,85, row 44). didOpen stores the resolved selection into the Document via the extended DocumentStore::open call.

Tests and fixture: update lsp/protocol_test.mbt initialize payloads (three tests at :10,29,49) to {"initializationOptions":{"dialect":"doris","profile":"4.x"}} (row 50); update lsp/diagnostics_formatting_test.mbt schema assertions from the old parse schema string to fathom.parse.v1 and source/code assertions to fathom / FATHOM-* (row 50); update parity/fixtures/lsp-tracer.json to fathom.parse.v1 with the "dialect":"doris" field (row 80 — owned by this plan per 09-05). After the pre-registered approved-change entries (LSP identity, FATHOM-* codes, fathom.parse.v1 — already in the 09-01 register), update the affected LSP-homomorph baseline snapshots via moon test --update --package parity once; baseline_diff.py must report zero unexpected diffs. Commit as feat(09-06): LSP document dialect selection tracer.</action>
  <acceptance_criteria>
    - End-to-end: initialize with {"dialect":"doris","profile":"4.x"} responds with serverInfo name fathom-lsp; didOpen of a SELECT document publishes diagnostics whose source is fathom, whose codes are FATHOM-PARSE-*, and whose data carries start_byte/end_byte as today.
    - The invalid-input fallback path emits FATHOM-LSP-001 with source fathom (never the old code/source).
    - Document.dialect/profile are set at didOpen time and parse_document calls @api.parse_with_ids with the document's own dialect/profile (no state.profile reference remains on this path).
    - lsp/protocol_test.mbt and lsp/diagnostics_formatting_test.mbt pass with the new initialize shape and fathom.parse.v1 assertions; parity/fixtures/lsp-tracer.json contains fathom.parse.v1 and dialect doris.
    - moon test --target native --package lsp --package parity passes; python3 scripts/baseline_diff.py compares the committed HEAD snapshot tree (git archive HEAD:parity/__snapshot__) to the working tree and reports zero unexpected diffs; moon test --package parity without --update is the genuine drift gate.
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native --package lsp --package parity && ref=$(mktemp -d) && git archive HEAD:parity/__snapshot__ | tar -x -C "$ref" && python3 scripts/baseline_diff.py --left "$ref" --right parity/__snapshot__ --approve .planning/phases/09-dialect-boundary-and-neutral-naming/approved-changes.md</automated>
  </verify>
  <done>The single happy path works end-to-end: a document resolves an explicit dialect+profile, parses under its own context, and publishes neutral FATHOM-* diagnostics through a fathom-lsp identity — committed against the baseline gate.</done>
</task>

<task type="auto">
  <name>Task 3: Selection resolution — languageId explicit mapping, didChangeConfiguration/configuration handling, structured config errors for missing/unknown/conflicting selections</name>
  <files>lsp/handlers.mbt, lsp/documents.mbt, lsp/selection_test.mbt</files>
  <read_first>
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-CONTEXT.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-RESEARCH.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-PATTERNS.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-04-SUMMARY.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-05-SUMMARY.md
    @/opt/source/Fathom/lsp/handlers.mbt
    @/opt/source/Fathom/lsp/documents.mbt
    @/opt/source/Fathom/lsp/protocol_test.mbt
  </read_first>
  <action>Complete the D-01/D-02 selection resolution across all three priority levels with the structured error surface.

lsp/handlers.mbt: add pub struct DialectSelection { pub dialect : String; pub profile : String } and pub enum SelectionError { MissingSelection; UnknownDialect(dialect_id~ : String); UnknownProfile(profile_id~ : String); ConflictingSelection(source_a~ : String, source_b~ : String) } (deriving Eq/Debug per the package's record/enum convention); add pub mut language_mapping : @map.Map[String, DialectSelection]? to ServerState (the user-configured languageId -> selection table; stays None until a client supplies one). Implement resolve_selection(state, params, language_id : String?) -> Result[DialectSelection, SelectionError] with the full D-01 precedence: (1) document-level explicit config from the didOpen/didChange params extension fields per the Task 1 confirmation — two conflicting values at this level -> ConflictingSelection; (2) else the workspace/session default (initializationOptions and/or serve_stdio args — if both present and differing -> ConflictingSelection); (3) else, ONLY if language_mapping is configured, the languageId mapping for the document's languageId — an unconfigured languageId never participates (D-02); (4) nothing resolves -> MissingSelection.

Add the handle_message arms: "workspace/didChangeConfiguration" — parse the settings payload's fathom section { dialect?, profile?, languageMapping? : { <languageId> : { dialect, profile } } }, validate each selection through @binding.validate_dialect_profile, store into default_dialect/default_profile/language_mapping, and re-resolve open documents (re-parse behavior is Task 4; this arm stores and resolves); per the Task 1 confirmation, also support the optional "workspace/configuration" server-to-client request path: after "initialized", the server may send one workspace/configuration request (items [{ section: "fathom" }]) storing the pending request id, and a message with method "workspace/configuration" whose id matches is treated as the settings response applied like didChangeConfiguration.

Structured error surface: initialize — missing dialect, unknown dialect, unknown profile, or same-source conflict returns error_response(id, -32602, <neutral structured message naming the missing/unknown/conflicting item>) (replaces "unsupported Doris profile" at handlers.mbt:309, research row 47); document-level — an unresolvable selection (MissingSelection, UnknownDialect, UnknownProfile, ConflictingSelection) publishes a single diagnostic { code: "FATHOM-SCHEMA-007", source: "fathom", message: <neutral text naming the item and its sources> } over publishDiagnostics for that document and does NOT parse (D-02 no-guess rule). The old ServerState.profile field (handlers.mbt:6) is removed; ServerState::new() initializes the new fields.

lsp/selection_test.mbt (new, white-box in-package mirroring protocol_test.mbt): (a) initialize without dialect -> -32602 response containing "dialect"; (b) initialize with unknown dialect -> -32602 containing "unknown dialect"; (c) initialize with unknown profile for doris -> -32602; (d) initialize with conflicting workspace/session sources (ServerState default_dialect "doris" + initializationOptions {"dialect":"flink",...}) -> -32602 containing "conflicting"; (e) didOpen with no selection anywhere -> exactly one publishDiagnostics with code FATHOM-SCHEMA-007 and a message naming the missing selection — no silent empty diagnostics, no languageId guess; (f) didOpen with a configured languageId mapping (didChangeConfiguration languageMapping {"sql": {"dialect":"doris","profile":"4.x"}}) resolves to that selection and parses normally; (g) didOpen whose document-level config conflicts -> FATHOM-SCHEMA-007. Commit as feat(09-06): LSP selection resolution.</action>
  <acceptance_criteria>
    - resolve_selection implements document > workspace/session > languageId-mapping precedence; an unconfigured languageId yields MissingSelection (D-02), and same-source conflicts yield ConflictingSelection (D-01).
    - initialize missing/unknown/conflicting selection returns -32602 with a neutral structured message; no "unsupported Doris profile" phrasing remains in lsp/ (grep over lsp/ finds zero occurrences).
    - A document with no resolvable selection gets exactly one FATHOM-SCHEMA-007 diagnostic with source fathom and is not parsed; ServerState.profile is gone (grep finds zero references to state.profile in lsp/).
    - workspace/didChangeConfiguration stores the fathom settings section and re-resolves; the optional workspace/configuration pull path correlates by request id.
    - moon test --target native --package lsp passes; the eight selection_test cases (a)-(g) all pass.
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native --package lsp</automated>
  </verify>
  <done>Selection resolution is complete across all three D-01 levels with structured config errors for missing, unknown, and conflicting selections — no implicit fallback anywhere.</done>
</task>

<task type="auto">
  <name>Task 4: D-03 dialect-switch reparse + stale-response guard + full LSP smoke matrix, fixture and snapshot approval</name>
  <files>lsp/handlers.mbt, lsp/selection_test.mbt, lsp/completion_test.mbt, lsp/lifecycle_test.mbt, parity/baseline_test.mbt</files>
  <read_first>
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-CONTEXT.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-RESEARCH.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-PATTERNS.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-04-SUMMARY.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-05-SUMMARY.md
    @/opt/source/Fathom/lsp/handlers.mbt
    @/opt/source/Fathom/lsp/completion_test.mbt
    @/opt/source/Fathom/lsp/lifecycle_test.mbt
    @/opt/source/Fathom/parity/baseline_test.mbt
  </read_first>
  <action>Deliver the D-03 switch/stale contract and the remaining LSP smoke surface, then approve the LSP baseline output changes.

lsp/handlers.mbt: formatting_result (handlers.mbt:169) and completion_result (handlers.mbt:269) switch from state.profile to the document's dialect/profile: @api.format_with_ids(document.text, document.dialect, document.profile, "strict", ...) and @completion.complete(document.text, document.dialect, document.profile, cursor_byte) (research row 48). D-03 reparse-on-switch: when didChangeConfiguration (or a workspace/configuration response) changes a document's resolved selection, re-parse every open document at its CURRENT stored version under the new context and publish — and when didOpen arrives with a document-level config that differs from a previously open document's context, the switch is immediate (reparse current revision, refresh diagnostics, no stale window). Stale-response guard: parse_document publishes only when state.docs.get(uri).version == document.version — the completion stale-check pattern (handlers.mbt:253-257 "completion document version is stale") extended to the diagnostics publish path; results computed under an older selection/version are dropped before publication (D-03). flink-selected documents: parse under a flink document context surfaces the 09-02 api FATHOM-PARSE-008 not-implemented statement diagnostics through publishDiagnostics unchanged (never Doris acceptance, never silent empty).

lsp/selection_test.mbt additions: (h) didChangeConfiguration switching the document dialect from doris to flink re-parses and publishes FATHOM-PARSE-008 diagnostics for a SELECT 1 document; (i) a stale publication attempt (document version older than the stored version) is dropped — the response stream contains no publishDiagnostics for the stale version; (j) formatting and completion under a flink-selected document return the structured rejection (format -> the api unsupported-profile error mapped to an empty edit response per the existing refusal shape; completion -> the api error surfaced as the -32602 bounded error) — no Doris policy leak.

lsp/completion_test.mbt and lsp/lifecycle_test.mbt: initialize payloads gain "dialect":"doris" (row 50) alongside protocol_test.mbt's Task 2 updates. parity/baseline_test.mbt: LSP homomorph callsites gain the dialect argument; record any new approved-change entries (document-level config extension fields in the trace fixture, dialect fields in LSP outputs) in .planning/phases/09-dialect-boundary-and-neutral-naming/approved-changes.md FIRST, then run moon test --update --package parity exactly once and confirm baseline_diff.py reports zero unexpected diffs. Commit as feat(09-06): LSP dialect switch and stale-response guard.</action>
  <acceptance_criteria>
    - formatting_result and completion_result use document.dialect/document.profile; zero state.profile references remain in lsp/.
    - A dialect switch re-parses the current revision and publishes the new diagnostics; a stale version's publication is dropped (D-03); the tests (h)-(j) pass.
    - A flink-selected document's parse publishes FATHOM-PARSE-008 not-implemented diagnostics with source fathom — never Doris acceptance or a silent empty result.
    - lsp/completion_test.mbt and lsp/lifecycle_test.mbt pass with the dialect-bearing initialize payloads.
    - moon test --target native --package lsp --package parity passes; baseline_diff.py (committed HEAD snapshot tree vs working tree) reports zero unexpected diffs after the single approved snapshot update; moon test --package parity without --update is the genuine drift gate.
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native --package lsp --package parity && ref=$(mktemp -d) && git archive HEAD:parity/__snapshot__ | tar -x -C "$ref" && python3 scripts/baseline_diff.py --left "$ref" --right parity/__snapshot__ --approve .planning/phases/09-dialect-boundary-and-neutral-naming/approved-changes.md</automated>
  </verify>
  <done>The LSP delivers dialect-switch reparse with version-guarded stale protection, flink rejection over LSP, and the full structured-config-error smoke matrix — all pinned by the baseline gate.</done>
</task>

</tasks>

## Source Audit Coverage

| SOURCE | ID | Feature or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| REQ | DIALECT-01 | explicit selection through LSP; missing/unknown/conflicting -> structured config error | 09-06 | COVERED (LSP boundary) | Tasks 2-4 (CLI boundary was 09-04); initialize/didOpen/didChangeConfiguration error surface + smoke matrix. |
| REQ | DIALECT-03 | explicit dialect routing; flink rejected | 09-06 | COVERED (LSP surface) | Task 4: flink-selected documents publish FATHOM-PARSE-008, never Doris acceptance. |
| REQ | DIALECT-04 | dialect/profile metadata + FATHOM-* stable across the boundary | 09-06 | COVERED | Tasks 2/4: document context metadata + FATHOM-*/FATHOM-LSP-001 over publishDiagnostics. |
| REQ | NAME-03 | LSP server identity neutral | 09-06 | COVERED (LSP part) | serverInfo fathom-lsp + source fathom; host config keys/package names in 09-07. |
| REQ | NAME-02 | fathom.parse.v1 over LSP | 09-06 | COVERED (LSP surface) | diagnostics_formatting_test + lsp-tracer.json assertions. |
| RESEARCH | rows 41-50 | LSP ServerState/Document/initialize/source/codes/serverInfo/tests | 09-06 | COVERED | Tasks 2-4. |
| RESEARCH | row 80 | lsp-tracer.json owned by this plan | 09-06 | COVERED | Task 2. |
| RESEARCH | A2 / OQ6 | per-document transport; languageId role | 09-06 | COVERED | Task 1 checkpoint + Task 3 (mapping only when configured). |
| RESEARCH | Pitfall 5 | stale-response + global profile state | 09-06 | COVERED | Task 4 version guard + document-bound context. |
| CONTEXT | D-01 | three-level precedence; same-source conflict error | 09-06 | COVERED | Task 1 checkpoint + Task 3. |
| CONTEXT | D-02 | no implicit languageId fallback; missing -> config error | 09-06 | COVERED | Task 3 MissingSelection + FATHOM-SCHEMA-007. |
| CONTEXT | D-03 | switch reparse + stale-response protection | 09-06 | COVERED | Task 4. |
| PROBE | DIALECT-01 concurrency | stale async results under dialect switch | 09-06 | FLAGGED | truth: version-guarded publication is the concurrency contract. |
| PROBE | DIALECT-04 precision | UTF-16/byte offsets across reparse | 09-06 | FLAGGED | truth: spans exact across dialect-switch reparse. |

<verification>
Run moon test --target native --package lsp --package parity + baseline_diff.py after each task. Inspect: ServerState/Document selection fields, resolve_selection precedence and conflict detection, initialize/didChangeConfiguration/workspace-configuration arms, FATHOM-LSP-001/source fathom/serverInfo fathom-lsp, document-context parse/format/completion callsites, D-03 reparse + version-guarded publication, flink FATHOM-PARSE-008 over LSP, updated protocol/diagnostics/completion/lifecycle tests and lsp-tracer.json, zero state.profile references. Do not touch vscode/, web/, or jetbrains/ (09-07 owns the host cutover).
</verification>

<success_criteria>
- The LSP resolves an explicit dialect+profile per document with the D-01 precedence and structured config errors for missing, unknown, and conflicting selections — no implicit fallback.
- Dialect switches re-parse the current revision and stale results are dropped (D-03); flink-selected documents are explicitly rejected with FATHOM-PARSE-008.
- LSP identity is neutral (fathom-lsp, source fathom, FATHOM-* codes) and the baseline gate is green with approved diffs only.
</success_criteria>

<output>Create `.planning/phases/09-dialect-boundary-and-neutral-naming/09-06-SUMMARY.md` when this plan executes.</output>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| editor client -> initialize/didChangeConfiguration/didOpen selection params | Untrusted selection strings and settings cross here; must be closed-enum validated with structured errors, never guessed (D-02). |
| async parse results -> publishDiagnostics | A stale result computed under an old selection/version must never overwrite newer diagnostics (D-03). |
| LSP diagnostics -> editor UI | Diagnostic codes/source must be neutral FATHOM-* / fathom, or clients keyed on old identity misrender. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-19 | Tampering | Dialect-confusion injection via client selection strings (initialize/didChange/settings) | high | mitigate | resolve_selection validates through @binding.validate_dialect_profile; missing/unknown/conflicting -> structured SelectionError -> FATHOM-SCHEMA-007 or -32602; no default path exists (D-02, ASVS V5). |
| T-09-20 | Spoofing | Stale async results overwrite newer diagnostics after a dialect switch | high | mitigate | DocumentStore version monotonicity + publish only when stored version matches (D-03); completion stale-check pattern extended to the diagnostics path; concurrency probe truth. |
| T-09-21 | Tampering | Implicit languageId fallback re-introduced (silent dialect guess) | high | mitigate | language_mapping participates only when user-configured (D-02); MissingSelection test (e) is a permanent negative gate. |
| T-09-22 | Spoofing | Old LSP identity leaks (doris-lsp / DORIS-LSP-001 / source doris) | medium | mitigate | grep gates in acceptance criteria + NAME-04 gate in 09-07 scans lsp/ product files. |
| T-09-23 | Tampering | Flink-selected document parsed under Doris policy over LSP | high | mitigate | Document context drives @api.parse_with_ids; flink -> FATHOM-PARSE-008 surfaced unchanged; tests (h)/(j). |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies (09-RESEARCH Package Legitimacy Audit N/A). |
</threat_model>

## Artifacts this phase produces

New files: lsp/selection_test.mbt (config-error + switch/stale smoke matrix).
Symbols (lsp/handlers.mbt): DialectSelection struct (dialect, profile), SelectionError enum (MissingSelection, UnknownDialect, UnknownProfile, ConflictingSelection), ServerState.language_mapping : Map[String, DialectSelection]? (default_dialect/default_profile kept from 09-04 as the workspace/session default source; old profile field removed), resolve_selection(state, params, language_id?) -> Result[DialectSelection, SelectionError], initialize_selection(params), workspace/didChangeConfiguration + workspace/configuration arms, parse_document with document context, FATHOM-LSP-001 fallback code, source "fathom", serverInfo name "fathom-lsp", version-guarded publish, formatting_result/completion_result document-context callsites.
Symbols (lsp/documents.mbt): Document.dialect : String, Document.profile : String; DocumentStore::open/change carry the resolved selection.
Modified: lsp/protocol_test.mbt + lsp/diagnostics_formatting_test.mbt + lsp/completion_test.mbt + lsp/lifecycle_test.mbt (initialize JSON with dialect, fathom.parse.v1/source/code assertions), parity/fixtures/lsp-tracer.json (fathom.parse.v1 + dialect field), parity/baseline_test.mbt (LSP homomorph dialect args), parity/__snapshot__ (approved update).
</threat_model>
