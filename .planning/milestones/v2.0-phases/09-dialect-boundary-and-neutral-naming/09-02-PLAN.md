---
phase: 09-dialect-boundary-and-neutral-naming
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - dialect/dialect.mbt
  - dialect/classification.mbt
  - dialect/doris.mbt
  - dialect/flink.mbt
  - token/token.mbt
  - lexer/lexer.mbt
  - parser/parser.mbt
  - api/api.mbt
  - binding/exports.mbt
  - binding/schema.mbt
  - completion/completion.mbt
  - formatter/case.mbt
  - formatter/format.mbt
  - lsp/handlers.mbt
  - parity/baseline_test.mbt
  - parity/export_smoke_test.mbt
  - test/parser_test.mbt
  - test/corpus_test.mbt
  - test/ddl_test.mbt
  - test/dml_test.mbt
  - test/keyword_test.mbt
  - test/recovery_test.mbt
  - test/source_test.mbt
autonomous: false
requirements: [DIALECT-02, DIALECT-03, DIALECT-04, NAME-02]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A consumer can parse SQL through an explicit dialect+profile selection end-to-end: dialect/ provides Dialect {Doris; Flink} and DialectContext (dialect, profile_id, exact_release, feature_introduction), ParseOptions::new(dialect_id, profile_id, mode_id) validates dialect BEFORE profile (Pitfall 6), and the serialized parse output carries schema_version fathom.parse.v1, dialect, profile, and exact_release metadata with FATHOM-PARSE-* codes (DIALECT-01/04, D-09/D-10)."
    - "Doris and Flink keyword classification is independent: doris_classification_rows (116 rows, content byte-identical to the v1 table) and flink_classification_rows (empty in Phase 9) live in dialect/, every classification query takes DialectContext, and no parameterless public is_reserved_word/classification_of remains (DIALECT-02, Pitfall 1)."
    - "Statement/clause grammar routes explicitly by dialect through one parse_segment router: Doris paths byte-identical to v1 (baseline gate green with approved changes only), and Flink mode never falls back to Doris — parse_flink_segment returns an explicit FATHOM-PARSE-008 not-implemented statement node for any input (DIALECT-03, A1/OQ1)."
    - "Missing/unknown/conflicting dialect selection returns structured errors: ParseError::UnknownDialect and ConflictingSelection exist in api, validate_dialect_profile rejects all flink profiles in Phase 9 (Phase 10 fills flink-2.3.0/2.1.3/1.20.5), and messages are neutral (no 'unsupported Doris profile' phrasing) with dialect expressed in fields (DIALECT-01, Pitfall 6)."
    - "[probe DIALECT-02 unclassified] resolved: independence is structural, not incidental — the two rows arrays are separate module-level data with no shared mutable state; classification_of(context, raw) selects the array by context.dialect and nothing else (Pitfall 7)."
    - "[probe DIALECT-03 adjacency] flagged-unverified: half-open byte spans and segment-split adjacency (statement boundaries, trailing-token consumption in finish_statement) are preserved exactly as v1 — the baseline snapshots pin span bytes; the Flink not-implemented node is source-backed with valid spans over the whole segment."
    - "[probe DIALECT-03 empty] flagged-unverified: empty input under a Doris context behaves exactly as v1 (frozen); empty input under a Flink context produces the FATHOM-PARSE-008 not-implemented diagnostic — never a silent empty success."
    - "[probe DIALECT-03 ordering] flagged-unverified: diagnostic ordering remains source order with statement_id assignment unchanged; classification lookup remains first-match-wins over the dialect's own rows (the ci-compare loop is preserved verbatim)."
    - "[probe DIALECT-04 precision] flagged-unverified: byte offsets, spans, and statement_id integers serialize without loss across the new dialect metadata fields (Int arithmetic unchanged; JSON emission unchanged)."
  artifacts:
    - path: "dialect/dialect.mbt"
      provides: "Dialect enum, DialectContext struct, FeatureId (closed-enum policy authority; no parser/api dependency)"
    - path: "dialect/classification.mbt"
      provides: "KeywordEntry, ClassificationKind (name preserved per D-05), token_bytes_equal_ci (moved from token), classification_of(context, raw), is_clause_keyword(context, raw), is_reserved_word(context, raw), is_unquoted_identifier(context, raw), classification_entries(context)"
    - path: "dialect/doris.mbt"
      provides: "DorisProfile/DorisFeature/ValidatedProfileContext/FeatureMetadata (names preserved, moved from token per D-05), doris_classification_rows (116 rows verbatim), *_docs_url constants, DorisProfile::metadata/from_id/supports"
    - path: "dialect/flink.mbt"
      provides: "FlinkProfile placeholder + flink_classification_rows = [] (Phase 10 fills)"
    - path: "token/token.mbt"
      provides: "Token.context : DialectContext, TokenStream.context : DialectContext (fields renamed from profile per research rows 8-9)"
    - path: "parser/parser.mbt"
      provides: "parse_segment(context) with match context.dialect router, parse_doris_segment (v1 body verbatim), parse_flink_segment (FATHOM-PARSE-008 not-implemented), RecoveryState.context, parse/parse_with_limits context forms"
    - path: "api/api.mbt"
      provides: "ParseOptions::new(dialect_id, profile_id, mode_id), ParseError::UnknownDialect/ConflictingSelection, ParseResult.dialect, parse_with_ids(raw, dialect_id, profile_id, mode_id), schema_version fathom.parse.v1"
    - path: "binding/exports.mbt + binding/schema.mbt"
      provides: "fathom_parse_v1(raw, dialect, profile, mode) (#export_name + moon.pkg exports synced), validate_dialect_profile(dialect, profile), fathom.error.v1 + FATHOM-* parse/schema codes, neutral messages"
  key_links:
    - from: "dialect/classification.mbt"
      to: "token/token.mbt"
      via: "token imports dialect (context type + rows); classification queries in token delegate to dialect — dependency direction dialect <- token, never reversed"
    - from: "parser/parser.mbt"
      to: "dialect/dialect.mbt"
      via: "parse_segment matches context.dialect; single router — no scattered per-dialect conditionals (Pitfall 2)"
    - from: "api/api.mbt"
      to: "dialect/dialect.mbt"
      via: "ParseOptions holds DialectContext; validation order dialect then profile (Pitfall 6)"
    - from: "binding/exports.mbt"
      to: "parity/baseline_test.mbt"
      via: "fathom_parse_v1 callsites updated in the same commit; parity snapshots updated once with approved-change register entries only"
  prohibitions:
    - "The SDK must never silently select a dialect or fall back when selection is missing, unknown, or conflicting (D-01/D-02); flink mode never accepts Doris-only syntax and never returns silent empty success (DIALECT-03/A1)."
    - "No parameterless public classification query remains: every public classification_of/is_clause_keyword/is_reserved_word/is_unquoted_identifier takes DialectContext (Pitfall 1); Doris-only private helpers are allowed inside dialect/doris.mbt."
    - "No Flink keyword row is added to any table in Phase 9; flink_classification_rows stays empty (Phase 10 owns it)."
    - "No parser code outside parse_segment branches on dialect; the single router is the only dispatch point (Pitfall 2)."
assumption_delta_decision:
  noun: "Doris parser core (single-dialect identity)"
  decision: "add-alongside"
  rationale: "The shared source/token/CST/Pratt/recovery core stays singular; dialect is an added selection dimension (Dialect enum + DialectContext) routed explicitly — not a promotion of Doris to 'the' dialect. Doris remains one dialect value among equals (D-01/D-05); the baseline gate proves Doris behavior is preserved alongside the new dimension."
  no_accepted_debt: "deliberate architecture boundary: the single parse_segment router is the only place dialect branching may live (Pitfall 2); the generalized intent is encoded by the baseline gate plus the dialect-negative tests (Flink mode rejects all input with FATHOM-PARSE-008)."
---

<objective>
**As a** consumer, **I want to** select doris or flink explicitly at the API boundary and receive dialect-tagged serialized results with independent per-dialect keyword policy, **so that** no dialect's syntax can change another dialect's acceptance and the wire output carries neutral naming.

Purpose: Build the dialect layer (DIALECT-02/03) and thread the dialect dimension end-to-end on the parse path with neutral wire identity (DIALECT-04/NAME-02) — the phase's tracer slice. Doris paths must stay byte-identical (proven by the 09-01 baseline gate, approved changes only); Flink mode is an explicit not-implemented rejection (A1/OQ1), never a fallback.
Output: dialect/ package (Dialect, DialectContext, KeywordEntry, two rows arrays), context-parameterized token/lexer/parser/api/binding parse path, fathom_parse_v1 + fathom.parse.v1 + FATHOM-PARSE-* codes, parse_segment dialect router with FATHOM-PARSE-008 Flink path, updated parity/test packages.
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-dialect-boundary-and-neutral-naming/09-CONTEXT.md
@.planning/phases/09-dialect-boundary-and-neutral-naming/09-RESEARCH.md
@.planning/phases/09-dialect-boundary-and-neutral-naming/09-PATTERNS.md
@.planning/phases/09-dialect-boundary-and-neutral-naming/09-01-SUMMARY.md

Wave 2 follows the 09-01 baseline freeze; every diff against parity/__snapshot__ must be explainable by the approved-change register. The dialect layer is the research's primary recommendation: move profile/keyword/dispatch into an explicit dialect policy instead of copying the parser. D-05 preserves Doris type names (DorisProfile/DorisFeature/ValidatedProfileContext/ClassificationKind/ClassificationEntry) — only their FILE location moves to dialect/doris.mbt. The tracer slice (Task 2) is the thin end-to-end path: fathom_parse_v1(raw, "doris", "4.x", "strict") -> dialect context -> lex -> parse_segment -> ParseResult with dialect metadata -> fathom.parse.v1 serialized output. The Flink not-implemented path (FATHOM-PARSE-008) is NEW behavior (not a rename) and must be added to approved-changes.md by this plan. Test package call sites (93 references to ValidatedProfileContext/DorisProfile per research) migrate to DialectContext in this wave.

> **Scope note (checker W-4, accepted):** 23 files modified (Task 3 = 16 incl. lsp/handlers.mbt) — a uniform mechanical context sweep across core, test, and the lsp callsites; each signature change lands with its callsites, so the sweep is atomic. Accepted with rationale; if the executor reports context pressure during the test sweep, split the test callsite sweep into a follow-up task within this plan.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm D-09/D-10 wire identity and ABI surface (one-way doors)</name>
  <decision>Public wire contract shape: schema namespaces, diagnostic codes, and the binding export ABI — all one-way doors published at release.</decision>
  <context>D-09 locks fathom.parse.v1/fathom.format.v1/fathom.error.v1/fathom.capabilities.v1 with dialect/profile/exact-release as metadata fields; D-10 locks FATHOM-PARSE/FORMAT/SCHEMA/LSP code prefixes with dialect expressed in fields, not the prefix; the research recommendations (09-RESEARCH.md Open Questions 2-4, Assumption A4) fix the export surface: fathom_parse_v1(raw, dialect, profile, mode) with dialect right after raw (consistent with CLI --dialect --profile and ParseOptions::new(dialect_id, profile_id, mode_id)); doris_profile_v1 becomes fathom_dialect_v1(dialect); ConflictingSelection maps to a new FATHOM-SCHEMA-007. This plan's tracer implements the parse half of these; 09-03/09-05 implement the format/capabilities halves. After release these shapes require schema migration to change.</decision>
  <options>
    <option id="option-a">
      <name>Adopt all locked decisions + research recommendations (recommended)</name>
      <pros>Matches D-09/D-10 verbatim; one coherent ABI (dialect right after raw everywhere); the approved-change register already lists these exact names.</pros>
      <cons>None beyond the locked one-way commitment.</cons>
    </option>
    <option id="option-b">
      <name>Keep doris.*.v1 names, only add dialect fields</name>
      <pros>No namespace churn.</pros>
      <cons>Violates D-06/D-09 (locked); NAME-02 would fail; the naming gate (09-07) would reject doris.*.v1 in product files.</cons>
    </option>
    <option id="option-c">
      <name>Different export parameter order (raw, profile, dialect, mode)</name>
      <pros>None observed.</pros>
      <cons>Breaks CLI/ParseOptions consistency (A4); ABI order is one-way after release.</cons>
    </option>
  </options>
  <resume-signal>Select option-a, or state the exact namespace/order/code changes; the tracer then implements the approved surface.</resume-signal>
</task>

<task type="tracer">
  <name>Task 2 (tracer): End-to-end explicit dialect selection — API parse -> CST -> diagnostics -> neutral serialized output</name>
  <files>dialect/dialect.mbt, dialect/classification.mbt, dialect/doris.mbt, dialect/flink.mbt, token/token.mbt, lexer/lexer.mbt, parser/parser.mbt, api/api.mbt, binding/exports.mbt, binding/schema.mbt, parity/baseline_test.mbt, parity/export_smoke_test.mbt</files>
  <read_first>
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-CONTEXT.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-RESEARCH.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-PATTERNS.md
    @/opt/source/Fathom/token/token.mbt
    @/opt/source/Fathom/lexer/lexer.mbt
    @/opt/source/Fathom/parser/parser.mbt
    @/opt/source/Fathom/api/api.mbt
    @/opt/source/Fathom/binding/exports.mbt
    @/opt/source/Fathom/binding/schema.mbt
    @/opt/source/Fathom/parity/export_smoke_test.mbt
    @/opt/source/Fathom/parity/baseline_test.mbt
  </read_first>
  <action>Wire ONE end-to-end parse path with explicit dialect selection and neutral wire identity, production-quality, on the single happy path.

Create dialect/ as a new low-level package (no parser/api dependency): dialect/dialect.mbt with pub enum Dialect { Doris; Flink } and pub struct DialectContext { pub dialect : Dialect, pub profile_id : String, pub exact_release : String, pub feature_introduction : String } (field shapes per 09-PATTERNS.md, following the ProfileMetadata pub-field convention). dialect/classification.mbt with pub enum ClassificationKind { Reserved; NonReserved; Contextual } (name preserved, D-05), pub struct KeywordEntry { pub word : Bytes, pub classification : ClassificationKind, pub introduced_profile : String, pub source : String } (ClassificationEntry shape, renamed per research), the ASCII case-insensitive compare moved here from token/token.mbt:221-236 as the shared lookup primitive, pub fn classification_of(context : DialectContext, raw : Bytes) -> KeywordEntry? selecting rows by match context.dialect (first-match loop preserved verbatim), and pub fn is_reserved_word(context, raw) / is_clause_keyword(context, raw) / is_unquoted_identifier(context, raw). dialect/doris.mbt moves DorisProfile, ProfileMetadata, ValidatedProfileContext, DorisFeature, FeatureMetadata, and DorisProfile::metadata/from_id/supports from token/token.mbt with NAMES UNCHANGED (D-05) and carries doris_classification_rows (the v1 classification_rows content, rows byte-identical) plus the *_docs_url constants; DorisFeature::metadata diagnostic_code values become FATHOM-PARSE-006 (approved-change register entry). dialect/flink.mbt defines pub enum FlinkProfile (empty in Phase 9) and let flink_classification_rows : Array[KeywordEntry] = [].

Thread the context through the parse path: token/token.mbt — Token.profile and TokenStream.profile fields become pub context : DialectContext (research migration rows 8-9); the v1 classification_rows table and classification_of/is_clause_keyword/is_reserved_word/is_unquoted_identifier move to dialect (token re-exports nothing — callers import dialect; token/moon.pkg gains the dialect import). lexer/lexer.mbt — lex_with_limit(source, context, max_tokens) and lex(source, context); push_token carries context (rows 12-13). parser/parser.mbt — RecoveryState.profile_context becomes context : DialectContext (row 17); parse_with_limits_context takes DialectContext (row 15); parse_segment gains context and routes match context.dialect { Doris => parse_doris_segment(...) with the v1 Doris starters body moved verbatim; Flink => parse_flink_segment(...) }; parse_flink_segment returns an explicit source-backed statement node carrying diagnostic code FATHOM-PARSE-008 (new code — add to approved-changes.md first) with message "flink grammar is not yet implemented in this release", consuming the whole segment so no Doris fallback and no silent success; parser convenience entries parse/parse_with_limits switch profile to context (row 16).

api/api.mbt — ParseOptions { dialect_context : DialectContext, mode, limits }; ParseOptions::new(dialect_id, profile_id, mode_id) validates dialect FIRST in api directly (api cannot import binding — binding imports api): dialect_id "doris" -> Dialect::Doris, "flink" -> Dialect::Flink, else Err(UnknownDialect(dialect_id~)); then profile_id -> DorisProfile::from_id for doris (Err(UnknownProfile) on unknown) and for flink return Err(UnknownProfile) for EVERY profile id in Phase 9 (Phase 10 fills); mode as today. ParseError gains UnknownDialect(dialect_id~ : String) and ConflictingSelection(selection_a~ : String, selection_b~ : String). ParseResult gains pub dialect : String; schema_version becomes "fathom.parse.v1" (row 23). parse_with_ids(raw, dialect_id, profile_id, mode_id, ...) per A4 order.

binding/exports.mbt — #export_name("fathom_parse_v1") and pub fn fathom_parse_v1(raw : Bytes, dialect : String, profile : String, mode : String) -> Bytes; binding/moon.pkg js/wasm exports lists updated to fathom_parse_v1 in the same commit (Pitfall 8 — #export_name and moon.pkg exports must stay in sync). binding/schema.mbt — PARSE_SCHEMA_VERSION "fathom.parse.v1", validate_dialect_profile(dialect : String, profile : String) (dialect first, then profile; flink branch rejects all; unknown dialect -> SchemaError::UnknownDialect), schema_error_code/parse_error_json emit FATHOM-SCHEMA-*/FATHOM-PARSE-* per the section-5 mapping and fathom.error.v1, and the "unsupported Doris profile" message becomes neutral "unsupported profile for dialect doris" (row 40).

Update parity/baseline_test.mbt and parity/export_smoke_test.mbt callsites to fathom_parse_v1(raw, "doris", profile, mode) in the same commit; extend export_smoke_test with the end-to-end assertions: (a) fathom_parse_v1(b"SELECT 1", "doris", "4.x", "strict") output contains "fathom.parse.v1", dialect doris, and profile 4.x fields; (b) fathom_parse_v1(b"SELEKT 1", "doris", "4.x", "strict") output contains "FATHOM-PARSE-002" and "fathom.error.v1"; (c) fathom_parse_v1(b"SELECT 1", "flink", "flink-2.3.0", "strict") returns the structured error for an unsupported flink profile (FATHOM-SCHEMA-003, neutral message, no Doris fallback); (d) the internal Flink not-implemented route: construct a Flink DialectContext directly and parse through the parser entry — the statement carries FATHOM-PARSE-008. Run the 09-01 baseline gate: the ONLY allowed diffs are the approved-change entries (fathom.parse.v1, FATHOM-* codes, dialect field, export name, neutral message) — then update snapshots ONCE with moon test --update --package parity after recording the entries, and run baseline_diff.py to confirm zero unexpected diffs. Commit as feat(09-02): dialect layer tracer slice.</action>
  <acceptance_criteria>
    - End-to-end: fathom_parse_v1(b"SELECT 1", "doris", "4.x", "strict") serialized output contains fathom.parse.v1, dialect=doris, profile=4.x, valid=true; the invalid-input call yields FATHOM-PARSE-002 with fathom.error.v1; the flink call yields the structured unsupported-profile error with no Doris fallback.
    - Internal Flink route: a directly constructed Flink DialectContext parses to a statement node carrying FATHOM-PARSE-008 with message "flink grammar is not yet implemented in this release"; no Doris parse runs.
    - Baseline gate: moon test --package parity (no --update) is the genuine byte-drift gate and passes; python3 scripts/baseline_diff.py compares the committed HEAD snapshot tree (git archive HEAD:parity/__snapshot__) against the working tree and reports zero unexpected diffs as supplementary evidence (only approved-change entries).
    - DialectContext carries dialect/profile_id/exact_release/feature_introduction; ParseError::UnknownDialect and ConflictingSelection compile and serialize through binding with FATHOM-SCHEMA codes.
    - binding/moon.pkg exports lists and #export_name are both fathom_parse_v1 (Pitfall 8); grep finds no parameterless pub classification query in token/ or dialect/.
  </acceptance_criteria>
  <verify>
    <automated>moon test --package parity && ref=$(mktemp -d) && git archive HEAD:parity/__snapshot__ | tar -x -C "$ref" && python3 scripts/baseline_diff.py --left "$ref" --right parity/__snapshot__ --approve .planning/phases/09-dialect-boundary-and-neutral-naming/approved-changes.md</automated>
  </verify>
  <done>The single happy path works end-to-end with explicit dialect selection and neutral wire identity, Doris bytes preserved, and the Flink path explicitly rejected — all committed against the baseline gate.</done>
</task>

<task type="auto">
  <name>Task 3: Complete the classification split and context sweep across core and test packages</name>
  <files>dialect/doris.mbt, dialect/classification.mbt, token/token.mbt, lexer/lexer.mbt, parser/parser.mbt, completion/completion.mbt, formatter/case.mbt, formatter/format.mbt, lsp/handlers.mbt, test/parser_test.mbt, test/corpus_test.mbt, test/ddl_test.mbt, test/dml_test.mbt, test/keyword_test.mbt, test/recovery_test.mbt, test/source_test.mbt</files>
  <read_first>
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-CONTEXT.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-RESEARCH.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-PATTERNS.md
    @/opt/source/Fathom/token/token.mbt
    @/opt/source/Fathom/completion/completion.mbt
    @/opt/source/Fathom/formatter/case.mbt
    @/opt/source/Fathom/formatter/format.mbt
    @/opt/source/Fathom/test/parser_test.mbt
    @/opt/source/Fathom/test/corpus_test.mbt
    @/opt/source/Fathom/test/ddl_test.mbt
    @/opt/source/Fathom/test/dml_test.mbt
    @/opt/source/Fathom/test/keyword_test.mbt
    @/opt/source/Fathom/test/recovery_test.mbt
    @/opt/source/Fathom/test/source_test.mbt
    @/opt/source/Fathom/lsp/handlers.mbt
  </read_first>
  <action>Complete the dialect dimension across the whole core and migrate every consumer.

dialect/doris.mbt: move the full v1 classification_rows content into doris_classification_rows (all rows verbatim — word, classification, introduced_profile, source; count must equal the v1 table count exactly). dialect/classification.mbt: add pub fn classification_entries(context : DialectContext) -> Array[KeywordEntry] (the context-routed accessor replacing token's classification_entry_count/classification_entry_at used by completion). token/token.mbt: remove the v1 classification_rows table and the old parameterless queries; keep Token.context/TokenStream.context; all internal consumers read context from the stream. lexer/lexer.mbt: confirm every push_token call site carries context (no profile remnants). parser/parser.mbt: replace every DORIS-PARSE-* code string with FATHOM-PARSE-* per the section-5 mapping at the definition points (finish_statement trailing-tokens 001, expected-X sites 002, lexical 003, resource-limit 004, feature gate 006 in dialect/doris.mbt metadata, unsupported-statement 007 in unsupported_statement); DORIS-PARSE-005 stays vacant — do not mint it; let profile = cursor.stream.profile becomes let context = cursor.stream.context at the parse_select_core call sites (row 18); RecoveryState.context consumers updated; parser convenience parse/parse_with_limits take DialectContext (row 16, OQ5: no Doris-only overloads retained).

completion/completion.mbt: signature complete(raw : Bytes, dialect_id : String, profile_id : String, cursor_byte : Int) -> CompletionResult; profile_allows gains the dialect dimension by routing through dialect classification; the classification accessor callsites (classification_entry_count/classification_entry_at) switch to dialect.classification_entries(context) — compile-correct here, dialect-aware behavior (detail string, flink empty results) is 09-03's task. formatter/case.mbt: rewrite_keyword(raw) becomes rewrite_keyword(context : DialectContext, raw : Bytes) routing through dialect.classification_of(context, raw); formatter/format.mbt entry format(root, source, options) gains a context parameter threaded to case rewriting — Doris behavior identical (same rows), code prefix DORIS-FORMAT-001 stays until 09-03. Update all 93 test-package call sites: replace ValidatedProfileContext/DorisProfile construction with DialectContext (a small test-local builder using DorisProfile::from_id + DialectContext fields, or reuse the api ParseOptions entry where the parse entry accepts ids); every DORIS-PARSE-* assertion in test files becomes FATHOM-PARSE-*; every parse call gains the dialect argument.

lsp/handlers.mbt: mechanical callsite sweep for the new signatures — parse_document's @api.parse_with_ids(document.text, state.profile, "editor") (handlers.mbt:79), formatting_result's @api.format_with_ids(document.text, state.profile, "strict", ...) (:169), and completion_result's @completion.complete(document.text, state.profile, cursor_byte) (:269) gain the "doris" dialect_id argument in position 2, e.g. parse_with_ids(document.text, "doris", state.profile, "editor"); compile-correct only — the document-context selection logic replacing state.profile is 09-06's task. The --package lsp leg of the verify below proves lsp/ compiles against the new signatures.

Run the full suite: moon test --target native --package test --package api --package parity --package lsp --package completion --package formatter; run baseline_diff.py — Doris outputs must be byte-identical (zero approved diffs beyond those already recorded in Task 2; any NEW diff is a regression, fix it). Commit as feat(09-02): complete dialect context sweep.</action>
  <acceptance_criteria>
    - doris_classification_rows row count equals the v1 classification_rows count and content is byte-identical (spot-assert 10 representative rows incl. SELECT, WITH, QUALIFY, TABLET, DISTRIBUTED, MERGE, VIEW, DEFAULT, BUCKETS, PROPERTIES); flink_classification_rows is empty.
    - moon test --target native --package test --package api --package parity --package lsp --package completion --package formatter --package token --package lexer --package parser --package binding --package dialect passes (token/lexer/parser/binding/dialect inline tests aligned with the ci.yml matrix); moon check --target native passes.
    - baseline_diff.py compares the committed HEAD snapshot tree (git archive HEAD:parity/__snapshot__) against the working tree and reports zero unexpected diffs after the Task 2 approved update; moon test --package parity (no --update) is the genuine drift gate; no NEW snapshot diffs beyond the register.
    - grep finds zero occurrences of the old parameterless query signatures in token/ and dialect/; zero DORIS-PARSE- strings remain in parser/, token/, dialect/, and test/ (the FORMAT prefix is deferred to 09-03 per plan).
    - No scattered dialect conditionals outside parse_segment (grep for per-dialect branches in parser/ finds only the router).
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native --package test --package api --package parity --package lsp --package completion --package formatter --package token --package lexer --package parser --package binding --package dialect && ref=$(mktemp -d) && git archive HEAD:parity/__snapshot__ | tar -x -C "$ref" && python3 scripts/baseline_diff.py --left "$ref" --right parity/__snapshot__ --approve .planning/phases/09-dialect-boundary-and-neutral-naming/approved-changes.md</automated>
  </verify>
  <done>The dialect dimension is complete across the core: independent rows, context-parameterized queries everywhere, one parse_segment router, Flink explicitly rejected, and the Doris baseline gate green with approved changes only.</done>
</task>

</tasks>

## Source Audit Coverage

| SOURCE | ID | Feature or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| REQ | DIALECT-02 | independent Doris/Flink lexical & keyword policy; no global union | 09-02 | COVERED | Task 3 split + Task 2 context threading; prohibitions. |
| REQ | DIALECT-03 | explicit dialect routing; Doris-only rejected in Flink mode | 09-02 | COVERED | Task 2 router + FATHOM-PARSE-008 not-implemented path. |
| REQ | DIALECT-04 | dialect/profile/exact-release metadata + FATHOM-* stability | 09-02 | COVERED | ParseResult.dialect + metadata fields + code mapping. |
| REQ | NAME-02 | fathom.parse.v1 / fathom.error.v1 + FATHOM-* + dialect fields | 09-02 | COVERED (parse half) | Tracer exports + schema; format/capabilities halves in 09-03/09-05. |
| RESEARCH | §4.1 dialect layer design | rows split, KeywordEntry, context queries | 09-02 | COVERED | Tasks 2-3. |
| RESEARCH | §4.3 parse_segment routing | explicit match, Flink not-implemented | 09-02 | COVERED | Task 2 (OQ1 -> FATHOM-PARSE-008, A1). |
| RESEARCH | §5 code mapping | DORIS-* -> FATHOM-*; PARSE-005 vacant | 09-02 | COVERED | Task 3; PARSE-005 not minted. |
| RESEARCH | Pitfall 1/2/6/7/8 | table pollution, scattered branches, profile phrasing, ci-lookup, export sync | 09-02 | COVERED | Prohibitions + acceptance criteria. |
| RESEARCH | OQ2/OQ3/OQ4 | ABI order, ConflictingSelection, fathom_dialect_v1 | 09-02 | COVERED | Task 1 checkpoint + Task 2 (dialect_v1 lands in 09-05). |
| CONTEXT | D-01 | explicit selection, no silent guess | 09-02 | COVERED | api validation + structured errors (LSP priority semantics in 09-06). |
| CONTEXT | D-05 | preserve Doris type names | 09-02 | COVERED | dialect/doris.mbt names unchanged. |
| CONTEXT | D-09 / D-10 | fathom.*.v1 namespaces; FATHOM-* codes | 09-02 | COVERED | Task 1 checkpoint + tracer implementation. |
| PROBE | DIALECT-02 unclassified | independence structural | 09-02 | RESOLVED | truth + Pitfall 7 prohibition. |
| PROBE | DIALECT-03 adjacency/empty/ordering | spans, empty input, ordering | 09-02 | FLAGGED | truths (span preservation, Flink empty contract, first-match order). |
| PROBE | DIALECT-04 precision | offsets/spans exact | 09-02 | FLAGGED | truth. |

<verification>
Run moon test --package parity + baseline_diff.py after each task; run the full target-native package suite after Task 3. Inspect: dialect/ package structure, doris_classification_rows content equality, FATHOM-PARSE-* coverage, FATHOM-PARSE-008 Flink path behavior, neutral messages (no "unsupported Doris profile" phrasing in binding/lsp), ParseOptions validation order (dialect before profile), export name sync (#export_name + moon.pkg). Do not install packages or touch vscode/web/jetbrains (hosts are 09-07).
</verification>

<success_criteria>
- Explicit dialect+profile selection works end-to-end through the public API with neutral wire identity; Flink is selectable but every Flink profile is rejected with a structured error and any Flink parse is an explicit FATHOM-PARSE-008 not-implemented node.
- Doris keyword policy is fully independent (two rows arrays, context-parameterized queries, one router).
- The 09-01 baseline gate is green with only approved diffs; Doris v1 bytes preserved.
</success_criteria>

<output>Create `.planning/phases/09-dialect-boundary-and-neutral-naming/09-02-SUMMARY.md` when this plan executes.</output>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| dialect/profile strings -> api ParseOptions | Untrusted selection strings cross here; must be closed-enum validated with structured errors, never silently defaulted. |
| Flink context -> parser | Any parse under a Flink context must reject explicitly (FATHOM-PARSE-008), never fall back to Doris grammar. |
| classification lookup -> identifier acceptance | Dialect rows must never leak across dialects (a Flink keyword must not affect Doris acceptance and vice versa). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-05 | Tampering | Dialect confusion injection (attacker-controlled dialect/profile strings) | high | mitigate | Closed Dialect enum + ParseOptions::new validates dialect first then profile (Pitfall 6); unknown/missing/conflict -> structured UnknownDialect/ConflictingSelection; results carry actual selection metadata (DIALECT-04, ASVS V5). |
| T-09-06 | Tampering | Cross-dialect classification leakage (global union effect) | high | mitigate | Two module-level rows arrays selected only by context.dialect; parameterless public queries removed (Pitfall 1); baseline gate pins Doris acceptance (Pitfall 7). |
| T-09-07 | Tampering | Flink mode silently accepts Doris grammar (try-all fallback) | high | mitigate | Single parse_segment router; parse_flink_segment returns source-backed FATHOM-PARSE-008 node for any input; no fallback path exists (Pitfall 2, A1). |
| T-09-08 | Spoofing | Neutral-message regression (Doris phrasing leaks into flink errors) | medium | mitigate | validate_dialect_profile + neutral messages; dialect expressed in fields (D-10); grep gates in acceptance criteria. |
| T-09-09 | Tampering | Export half-migration (#export_name vs moon.pkg exports) | medium | mitigate | Both lists updated in the same commit (Pitfall 8); NAME-04 gate in 09-07 backs this up. |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies (09-RESEARCH Package Legitimacy Audit N/A). |
</threat_model>

## Artifacts this phase produces

New files: dialect/dialect.mbt, dialect/classification.mbt, dialect/doris.mbt, dialect/flink.mbt, dialect/moon.pkg.
Symbols (dialect/dialect.mbt): Dialect enum (Doris|Flink), DialectContext struct (dialect, profile_id, exact_release, feature_introduction).
Symbols (dialect/classification.mbt): KeywordEntry, ClassificationKind (preserved name), token_bytes_equal_ci (moved), classification_of(context, raw), is_clause_keyword(context, raw), is_reserved_word(context, raw), is_unquoted_identifier(context, raw), classification_entries(context).
Symbols (dialect/doris.mbt): DorisProfile, ProfileMetadata, ValidatedProfileContext, DorisFeature, FeatureMetadata (all names preserved, D-05), doris_classification_rows (116 rows), DorisProfile::metadata/from_id/supports, *_docs_url constants.
Symbols (dialect/flink.mbt): FlinkProfile (empty enum), flink_classification_rows = [].
Modified symbols: Token.context, TokenStream.context, lex_with_limit(source, context, max_tokens), lex(source, context), RecoveryState.context, parse_segment(context) router, parse_doris_segment, parse_flink_segment (FATHOM-PARSE-008), parse_with_limits_context(context), parse/parse_with_limits(context), ParseOptions::new(dialect_id, profile_id, mode_id), ParseError::UnknownDialect/ConflictingSelection, ParseResult.dialect, parse_with_ids(raw, dialect_id, profile_id, mode_id), fathom_parse_v1(raw, dialect, profile, mode) export, validate_dialect_profile(dialect, profile), PARSE_SCHEMA_VERSION fathom.parse.v1, FATHOM-PARSE-001..007 (005 vacant) + FATHOM-PARSE-008 (new), complete(raw, dialect_id, profile_id, cursor_byte), rewrite_keyword(context, raw), format(root, source, options, context).
