---
phase: 12-cross-dialect-corpus-and-parity-gates
plan: 03
type: execute
wave: 2
depends_on: [12-01, 12-02]
files_modified:
  - scripts/compare_backends.py
  - .github/workflows/ci.yml
autonomous: false
requirements: [PARITY-02, PARITY-03]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The same fixture set produces byte-identical serialized results, diagnostics, spans, and lossless replay across Native, JavaScript, and linear-Wasm targets: `moon test --target native --package parity`, `moon test --target js --package parity`, and `moon test --target wasm --package parity` each pass, and `python3 scripts/compare_backends.py` exits 0 with a per-target report whose snapshot-tree sha256 digest is identical across all three targets (PARITY-02, D-05)."
    - "CI runs a three-target runtime matrix that includes JavaScript: the linear-wasm-parity job executes the parity suite on js in addition to wasm and native, so a js-only byte regression cannot silently diverge (PARITY-02, D-05, Research §4.5 gap: js had only moon check, no runtime job)."
    - "The offline gate pipeline runs from pinned local artifacts only: the corpus job runs `python3 scripts/verify_corpus.py --check` and `python3 corpus/tools/generate_corpus_report.py --check`, and the parity-gate job runs `python3 scripts/diff_parity.py --frozen-only` — no CI gate step performs network access, Doris FE, a Flink cluster, or a database connection (PARITY-03, D-06, Pitfall 5)."
    - "A maintainer can inspect a three-target aggregate parity report: compare_backends.py reports per-target pass/fail, the failing-fixture list (snapshot filenames from moon output), and the three snapshot-tree digests; it fails closed (exit 1) when any target is skipped, any target fails, or the digests diverge (PARITY-02, Pitfall 8 non-empty guard)."
    - "The coverage report wired into CI enforces the semantic distinction: catalog-prerequisite / planner-prerequisite / known-limitation rows are never counted as engine-supported in either dialect — the extended generate_corpus_report.py --check hard-gates this (PARITY-03, D-01)."
    - "[probe PARITY-02 unclassified] flagged-unverified: the edge-probe left PARITY-02 unclassified — the byte-identity contract above (three-target matrix + digest compare, no normalization) is authored to enforce PARITY-02 in full and remains flagged for manual review."
    - "[probe PARITY-03 concurrency] flagged-unverified backstop: { statement: interrupting or running the offline gates in parallel must never corrupt or half-write the gate state — compare_backends.py and verify_corpus.py are read-only over pinned artifacts and committed snapshots, so an interrupted run leaves no partial state and cannot fabricate a pass; verification: backstop } (authored here to close the PARITY-03 concurrency probe; the 12-01 backstop marker covers the verifier, this covers the aggregate + CI jobs)"
  artifacts:
    - path: "scripts/compare_backends.py"
      provides: "Three-target byte-parity aggregate reporter (stdlib): runs moon test --target {native,js,wasm} --package parity, captures exit code + failing snapshot filenames, computes a deterministic sha256 tree digest over the committed parity/__snapshot__ tree, reports per-target status + divergence list, non-empty guard (any skipped/failed target or empty digest = exit 1)"
    - path: ".github/workflows/ci.yml"
      provides: "Wired offline gates: js runtime parity step in linear-wasm-parity (moon test --target js --package parity), compare_backends.py aggregate step, diff_parity.py --frozen-only step in parity-gate, verify_corpus.py --check + extended generate_corpus_report.py --check in corpus job; NO --update and NO network steps added"
  key_links:
    - from: "scripts/compare_backends.py subprocess runs"
      to: "moon test --target {native,js,wasm} --package parity"
      via: "per-target exit code + failing snapshot filenames captured from moon output; snapshot-tree sha256 digest over parity/__snapshot__ must be identical across targets (D-05, Research §7.2)"
    - from: "parity/__snapshot__ committed tree"
      to: "compare_backends.py digest"
      via: "deterministic sha256 tree digest — the cross-target comparison object; wasm cannot stdout dump (run_wasm.mbt), so exit code + digest is the byte-parity proof (A8)"
    - from: "ci.yml parity-gate job"
      to: "scripts/diff_parity.py --frozen-only"
      via: "regenerated-comparison step after moon test --package parity (no --update) and the baseline_diff self-check — the frozen baseline proof (D-03, 12-02 output)"
    - from: "ci.yml corpus job"
      to: "scripts/verify_corpus.py --check + corpus/tools/generate_corpus_report.py --check"
      via: "offline manifest/hash verifier + semantic-distinction coverage report, both stdlib and read-only (D-06, 12-01 output)"
    - from: "ci.yml linear-wasm-parity job"
      to: "moon test --target js --package parity"
      via: "closing the Research §4.5 js-runtime gap so the three-target matrix includes JavaScript (D-05)"
  prohibitions:
    - "The offline gate pipeline MUST NOT access the network, Doris FE, a Flink cluster, or a database at runtime — verify_corpus.py and compare_backends.py are stdlib read-only over pinned local artifacts, and no CI gate job performs curl/pip/git-ls-remote against release sources (D-06, Pitfall 5)."
    - "Cross-backend parity MUST NOT be reported when any of native/js/wasm was skipped or failed — the three-target matrix and compare_backends.py non-empty guard fail closed (D-05, Pitfall 8)."
    - "Byte-level parity MUST NOT be normalized away (float coercion, encoding/whitespace folding) — serialized results, diagnostics, spans, and lossless replay must be byte-identical, not merely semantically equivalent (D-05, Pitfall 4)."
    - "The --update flag MUST NOT be added to any CI job — the only sanctioned snapshot writer is a moon test --update run whose change is pre-declared in approved-changes.md (Pitfall 1, parity-gate comment)."
---

<objective>
补齐跨后端字节 parity 的三目标矩阵与汇总报告（PARITY-02 / D-05），并把全部离线门禁接线到 CI（PARITY-03 / D-06）：新增 `python3 scripts/compare_backends.py` 三目标运行 + 快照树 digest 比对报告，CI 的 linear-wasm-parity job 增 `moon test --target js --package parity`（Research §4.5 缺口：js 只有 moon check、无运行时 job），parity-gate job 增 `diff_parity.py --frozen-only`（12-02 产物），corpus job 增 `verify_corpus.py --check` + 扩展后 `generate_corpus_report.py --check`（12-01 产物）。本 plan 是 Phase 12 的集成/接线 plan：wave 2，depends_on 12-01（verify_corpus.py + flink-coverage 报告）与 12-02（diff_parity.py）。

Purpose: PARITY-02 要求同一 fixture 在 Native/JS/linear-Wasm 三目标上序列化结果、诊断、span、lossless replay 字节级一致，且 CI 常驻三目标矩阵；PARITY-03 要求 CI/release 门禁纯离线运行（无 Doris FE/Flink cluster/DB/网络）+ 覆盖报告区分 parser 接受 vs 引擎语义前置。Research §7 现状：MoonBit @test.T::snapshot 已是跨目标字节门禁（同一 committed 快照文件被三目标逐一比对，本 session 实测三目标 570/570），缺的是 CI js 运行时 job 与显式 compare_backends.py 报告工具；§8 离线门禁由 12-01 的 verify_corpus.py 承担。本 plan 补全这两个缺口并把 12-01/12-02 的产物接线进 CI，零 MoonBit 核心改动。
Output: scripts/compare_backends.py、.github/workflows/ci.yml（js 运行时 + diff_parity --frozen-only + verify_corpus --check + compare_backends 汇总）。
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-cross-dialect-corpus-and-parity-gates/12-CONTEXT.md
@.planning/phases/12-cross-dialect-corpus-and-parity-gates/12-RESEARCH.md
@.planning/phases/12-cross-dialect-corpus-and-parity-gates/12-PATTERNS.md
@.planning/phases/12-cross-dialect-corpus-and-parity-gates/12-01-SUMMARY.md
@.planning/phases/12-cross-dialect-corpus-and-parity-gates/12-02-SUMMARY.md
@/opt/source/Fathom/.github/workflows/ci.yml
@/opt/source/Fathom/parity/run_wasm.mbt
@/opt/source/Fathom/parity/run_js.mbt
@/opt/source/Fathom/scripts/check_naming.py

This plan wires the outputs of 12-01 (scripts/verify_corpus.py, corpus/flink-coverage.tsv + extended generate_corpus_report.py) and 12-02 (scripts/diff_parity.py) into CI, and adds the missing cross-backend pieces (compare_backends.py + js runtime job). D-05 (three-target byte parity, one-way) is gated by Task 1 checkpoint; the D-06 offline-gate CI form was confirmed by 12-01's Task 1 checkpoint and is wired mechanically here. A8 is carried: wasm cannot stdout dump (parity/run_wasm.mbt comment), so compare_backends.py uses per-target exit code + snapshot-tree sha256 digest as the byte-parity proof. The CI file ownership is exclusive to this plan (12-01/12-02 do not touch ci.yml); waves: 12-01 + 12-02 wave 1, this plan wave 2.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Confirm the cross-backend parity contract and CI matrix form — D-05 one-way door</name>
  <decision>The cross-backend byte-parity contract that becomes an SDK release promise once CI-wired: all three targets (native, js, linear-wasm) run the parity suite and byte-compare; CI completes the three-target matrix by executing `moon test --target js --package parity` inside the existing linear-wasm-parity job (Research A10 recommendation — one checkout/install, matrix semantics), and a new stdlib `python3 scripts/compare_backends.py` produces the aggregate per-target report + snapshot-tree sha256 digest comparison (exit 1 when any target is skipped/failed or digests diverge). Byte-identity is asserted on the committed snapshot files (the MoonBit snapshot mechanism already compares each target against the same committed files), never on a normalized or semantic-equivalence view.</decision>
  <context>D-05 is one-way because cross-backend byte consistency is the SDK core promise (CONTEXT D-05) and the CI matrix shape becomes the release contract. RESEARCH §7.2: the byte-comparison object is the committed parity/__snapshot__ tree — MoonBit @test.T::snapshot writes the same committed files and each target must reproduce them byte-for-byte; the cross-target.4.x-industrial.strict.json file makes one target's output the others' reference. The form decisions: (a) js job merged into linear-wasm-parity vs a separate js-parity job — A10 recommends the merge; (b) compare_backends.py proof = per-target moon test exit code + deterministic snapshot-tree sha256 digest (wasm cannot stdout dump, A8) vs a per-fixture explicit dump (would require a new target-scoped dump entry beyond the snapshot mechanism); (c) the serialized envelope is float-free (fathom.parse.v1 uses integers/strings/byte arrays; JS Uint8Array is normalized to int[] at the binding layer), so digest comparison needs no float/endianness handling (Pitfall 4). The D-06 offline-gate CI form (verify_corpus.py --check in the corpus job, extract_* staying local) was already confirmed in 12-01 Task 1 and is wired mechanically in Task 3.</decision>
  <options>
    <option id="option-a">
      <name>Merge js runtime into linear-wasm-parity + compare_backends.py with rc+digest proof (recommended — RESEARCH recommendation)</name>
      <pros>One job, one checkout/install; three-target matrix complete with minimal CI surface; compare_backends.py gives maintainers a readable per-target report + digest; rc+digest proof is honest about the wasm no-stdout constraint; float-free envelope keeps digest byte-identical.</pros>
      <cons>compare_backends.py re-runs the three targets it aggregates (redundant execution with the job steps), and the digest is over the committed tree rather than per-fixture dumps.</cons>
    </option>
    <option id="option-b">
      <name>Separate js-parity job + per-fixture explicit dump entry for byte comparison</name>
      <pros>Isolated failure domain per target; per-fixture dumps give byte-level per-fixture evidence.</pros>
      <cons>A new target-scoped dump entry exceeds the snapshot mechanism (A8); a second job doubles checkout/install; per-fixture dump comparison requires new runner surface — higher maintenance cost for no additional contract value.</cons>
    </option>
  </options>
  <resume-signal>Select option-a (merged js runtime + rc+digest compare_backends.py) or describe a modified parity contract; compare_backends.py in Task 2 and the CI wiring in Task 3 are authored only after this confirmation.</resume-signal>
</task>

<task type="auto">
  <name>Implement scripts/compare_backends.py — three-target byte-parity aggregate report</name>
  <files>scripts/compare_backends.py</files>
  <read_first>
    @.planning/phases/12-cross-dialect-corpus-and-parity-gates/12-RESEARCH.md
    @.planning/phases/12-cross-dialect-corpus-and-parity-gates/12-PATTERNS.md
    @/opt/source/Fathom/parity/run_wasm.mbt
    @/opt/source/Fathom/parity/run_js.mbt
    @/opt/source/Fathom/scripts/check_naming.py
  </read_first>
  <action>Create scripts/compare_backends.py (Python stdlib, mirroring the gate-script skeleton and the check_naming.py scanned==0 non-empty-guard precedent) implementing the D-05-confirmed contract. CLI: no positional args (like the other gate scripts), default behavior is the three-target aggregate; optional --targets native,js,wasm override. For each target in {native, js, wasm}: (1) run `moon test --target {t} --package parity` via subprocess, capture the exit code and the stdout/stderr text; (2) extract the failing snapshot filenames from the moon output (snapshot name patterns flink-grammar.*, flink-lexical.*, and the doris baseline {profile}.*.{mode}.json groups) into the per-target failure list; (3) compute a deterministic sha256 tree digest over the committed parity/__snapshot__ directory (sorted relative paths + per-file sha256, mirroring a standard directory-digest routine). After all three targets: print the per-target report (target, exit code, pass/fail, failing-fixture list, digest), verify the three digests are identical, and exit 0 only when all three targets passed AND the digests match AND the snapshot tree was non-empty; exit 1 otherwise with the divergence/failure detail. Non-empty guard: a skipped target (moon command not found or run aborted), an empty snapshot tree, or zero scanned targets exits 1, never an ok line (Pitfall 8). The digest routine must be deterministic across runs and platforms (no mtime, no unsorted ordering). Commit the script.</action>
  <acceptance_criteria>
    - CLI: `python3 -m py_compile scripts/compare_backends.py` succeeds; `python3 scripts/compare_backends.py` on the current tree exits 0 with a per-target report showing native/js/wasm all pass and the three snapshot-tree digests identical (the parity suite is 570/570 on each target per RESEARCH §4.1).
    - Failing-fixture capture: a deliberately injected snapshot drift (e.g. temporarily editing one committed snapshot byte) makes `python3 scripts/compare_backends.py` exit 1 with that target's failing fixture named in the report and the digest reflecting the modified tree; after reverting, the script exits 0 again.
    - Non-empty guard: pointing the script at a missing or empty snapshot directory (dry-run via an env override or a temporary empty tree argument) exits 1 with an error line, never ok — proving the blind-spot guard (Pitfall 8).
    - Deterministic digest: running `python3 scripts/compare_backends.py` twice produces the identical digest value for each target on the same committed tree.
    - No working-tree writes: `git status --short parity/__snapshot__` is empty after the run (the script is read-only over the committed tree).
  </acceptance_criteria>
  <verify>
    <automated>python3 -m py_compile scripts/compare_backends.py && python3 scripts/compare_backends.py && test -z "$(git status --short parity/__snapshot__)"</automated>
  </verify>
  <done>compare_backends.py exists and exits 0 with an identical three-target snapshot-tree digest on the current tree, names failing fixtures on injected drift, fails closed on empty/skipped targets, and is read-only over the committed snapshots.</done>
</task>

<task type="auto">
  <name>Wire the offline gates and the three-target matrix into CI</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    @.planning/phases/12-cross-dialect-corpus-and-parity-gates/12-RESEARCH.md
    @/opt/source/Fathom/.github/workflows/ci.yml
  </read_first>
  <action>Modify .github/workflows/ci.yml to wire the D-05/D-06-confirmed gates. (1) linear-wasm-parity job: add a step `moon test --target js --package parity` after the existing wasm and native execution steps (closing the Research §4.5 js-runtime gap so the three-target matrix includes JavaScript), and add a final step `python3 scripts/compare_backends.py` (with the existing Set up Python step added if the job lacks one) producing the three-target aggregate report. (2) parity-gate job: add a step `python3 scripts/diff_parity.py --frozen-only` after the existing baseline_diff self-check and baseline-hashes sha256sum steps — the regenerated-comparison frozenness proof from 12-02. (3) corpus job: add a step `python3 scripts/verify_corpus.py --check` and keep the existing `python3 corpus/tools/generate_corpus_report.py --check` (now the extended semantic-distinction report) — the offline verifier from 12-01. Preserve the existing hard constraints verbatim: parity-gate never adds --update (the job comment already states it); no gate step introduces curl/pip/git-ls-remote network access against release sources (the MoonBit installer curl step is the pre-existing toolchain bootstrap and stays as-is); the js/wasm/native execution steps keep the same packages. Use the existing Set up Python template (actions/setup-python@v6, python-version 3.11) for any job that gains a python step. The check job's moon check already covers all three targets and stays unchanged. Commit the ci.yml change.</action>
  <acceptance_criteria>
    - ci.yml contains a `moon test --target js --package parity` runtime step inside linear-wasm-parity (the Research §4.5 gap is closed), a `python3 scripts/compare_backends.py` aggregate step in the same job, a `python3 scripts/diff_parity.py --frozen-only` step in parity-gate, and a `python3 scripts/verify_corpus.py --check` step in the corpus job.
    - The parity-gate job body contains no update-flag in any run line (the single-use approval path is preserved, Pitfall 1); the update-flag literal appears nowhere in a new step.
    - No new gate step performs network access against release sources: the only network step in the workflow remains the MoonBit installer curl (toolchain bootstrap), and the new python steps call only local stdlib scripts over committed artifacts (D-06).
    - `python3 -m py_compile` succeeds on all gate scripts referenced by the workflow (verify_corpus.py, diff_parity.py, compare_backends.py, generate_corpus_report.py) so the wired steps are runnable.
    - Locally, each wired command passes on the current tree: `python3 scripts/verify_corpus.py --check`, `python3 scripts/diff_parity.py --frozen-only`, `python3 scripts/compare_backends.py`, and `python3 corpus/tools/generate_corpus_report.py --check`.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m py_compile scripts/verify_corpus.py scripts/diff_parity.py scripts/compare_backends.py corpus/tools/generate_corpus_report.py && python3 scripts/verify_corpus.py --check && python3 scripts/diff_parity.py --frozen-only && python3 scripts/compare_backends.py && python3 corpus/tools/generate_corpus_report.py --check && ! grep -E 'run:.*--update' .github/workflows/ci.yml</automated>
  </verify>
  <done>ci.yml wires the js runtime step + compare_backends.py aggregate into linear-wasm-parity, the diff_parity.py --frozen-only step into parity-gate, and the verify_corpus.py --check step into the corpus job — all offline, no update-flag, and every wired command passes locally on the current tree.</done>
</task>

</tasks>

## Source Audit Coverage

| SOURCE | ID | Feature or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Maintainers audit reproducible Doris/Flink coverage; release only within explicit parity contracts | 12-03 | COVERED | Three-target matrix + offline CI gates complete the release-contract surface. |
| REQ | PARITY-02 | Same fixture byte-identical serialized results/diagnostics/spans/lossless replay across Native/JS/linear-Wasm | 12-03 | COVERED | compare_backends.py + js runtime job + three-target digest; snapshot mechanism is the byte comparison object. |
| REQ | PARITY-03 | CI/release checks from pinned offline artifacts; coverage distinguishes parser acceptance from engine semantic support | 12-03 | COVERED (CI wiring) | verify_corpus.py --check + extended report --check + diff_parity --frozen-only wired into CI; no network/FE/cluster/DB steps. |
| CONTEXT | D-05 | Same fixture byte-identical across three targets; CI three-target matrix + byte compare (one-way) | 12-03 | COVERED | Task 1 checkpoint (one-way) + Task 2 compare_backends.py + Task 3 js runtime step. |
| CONTEXT | D-06 | Offline manifest/hash verifier; pinned artifacts only; coverage distinguishes parser vs engine prereq (one-way) | 12-03 | COVERED (CI form wired) | verify_corpus.py + report --check wired; form confirmed in 12-01 Task 1. |
| CONTEXT | D-03 | Diff harness CI form (one-way once wired) | 12-03 | COVERED (wiring) | diff_parity.py --frozen-only wired into parity-gate; form confirmed in 12-02 Task 1. |
| RESEARCH | §7.2 | Three-target matrix + compare_backends.py (rc + snapshot-tree sha256 digest; wasm no stdout dump, A8) | 12-03 | COVERED | Task 2. |
| RESEARCH | §4.5 | CI gap: js has no runtime parity job | 12-03 | COVERED | Task 3 js step in linear-wasm-parity (A10 merge). |
| RESEARCH | §8.2 | CI wiring of verify_corpus.py --check + extended report --check | 12-03 | COVERED | Task 3 corpus job steps. |
| RESEARCH | Pitfall 4 | Float/endianness/encoding divergence | 12-03 | COVERED | float-free envelope + digest over committed bytes + prohibition (no normalization). |
| RESEARCH | Pitfall 5 | CI network access creep | 12-03 | COVERED | prohibition + only-installer-curl constraint + stdlib read-only scripts. |
| RESEARCH | Pitfall 8 | Gate blind spot (0 targets/0 files) | 12-03 | COVERED | compare_backends.py non-empty guard (check_naming.py precedent). |
| RESEARCH | A8/A9/A10 | wasm no-stdout (A8); temp --update safety (A9); js job merge (A10) | 12-03 | COVERED | A8 → rc+digest proof; A9 carried (12-02); A10 → merged job. |
| PROBE | PARITY-02 unclassified | Edge-probe left PARITY-02 unclassified | 12-03 | FLAGGED | must_haves flagged-unverified truth; contract authored to enforce byte identity in full. |
| PROBE | PARITY-03 concurrency | Interrupted/parallel gate runs guarantee | 12-03 | FLAGGED | must_haves backstop marker (aggregate + CI jobs); 12-01 marker covers the verifier. |
| PROBE | CORPUS-01 unclassified | Edge-probe left CORPUS-01 unclassified | 12-03 | NOT IN SCOPE (12-01) | flagged in 12-01. |
| PROBE | PARITY-01 unclassified | Edge-probe left PARITY-01 unclassified | 12-03 | NOT IN SCOPE (12-02) | flagged in 12-02. |

<verification>
Run python3 -m py_compile on all four wired gate scripts (verify_corpus.py, diff_parity.py, compare_backends.py, generate_corpus_report.py), then each wired command locally (verify_corpus.py --check, diff_parity.py --frozen-only, compare_backends.py, generate_corpus_report.py --check). Verify ci.yml: js runtime step + compare_backends step in linear-wasm-parity, diff_parity --frozen-only in parity-gate, verify_corpus --check in corpus; no update-flag in any run line; the only network step remains the MoonBit installer curl. Run moon test --package parity (no --update) after the changes and confirm the three-target parity suite is still green (native js wasm 570/570).
</verification>

<success_criteria>
- The three-target runtime matrix includes JavaScript in CI; compare_backends.py produces a per-target report with an identical snapshot-tree sha256 digest across native/js/wasm and fails closed on any skip/failure/divergence.
- All offline gates are wired into CI (verify_corpus.py --check, extended generate_corpus_report.py --check, diff_parity.py --frozen-only) with no network access and no update-flag in any gate step.
- The coverage report's semantic distinction (parser acceptance vs engine prerequisite) is CI-enforced for both dialects.
</success_criteria>

<output>Create `.planning/phases/12-cross-dialect-corpus-and-parity-gates/12-03-SUMMARY.md` when this plan executes.</output>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parity/__snapshot__ committed tree -> three-target byte compare | The committed snapshot bytes are the cross-target comparison object; any target divergence is a contract break. |
| CI gate steps -> local scripts | The wired steps run local stdlib scripts over committed artifacts; the only network boundary is the pre-existing MoonBit installer bootstrap. |
| coverage report -> CI corpus job | Category semantics cross here; prerequisite rows must never surface as engine-supported. |
| parity-gate job -> approved-changes register | The frozen baseline proof must not be bypassed by an update-flag step. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-12-03-01 | Tampering | js target silently skipped, masking a js-only byte divergence | high | mitigate | js runtime step in linear-wasm-parity + compare_backends.py non-empty guard (a skipped target exits 1) (D-05, Pitfall 8). |
| T-12-03-02 | Tampering | Byte differences normalized away (float/encoding/whitespace) in the parity proof | high | mitigate | byte-identity asserted on committed snapshot files; digest over raw bytes; prohibition against normalization (Pitfall 4). |
| T-12-03-03 | Spoofing | CI gate network creep (curl/pip/ls-remote against release sources) | high | mitigate | verify_corpus.py/compare_backends.py are stdlib read-only; only the pre-existing installer curl remains; prohibition (Pitfall 5). |
| T-12-03-04 | Tampering | --update flag added to a CI job, absorbing snapshot drift | high | mitigate | parity-gate keeps the no-update constraint verbatim; verify gate greps run lines (Pitfall 1). |
| T-12-03-05 | Spoofing | Frozen baseline proof degrades back to a self-comparison after wiring | high | mitigate | diff_parity.py --frozen-only regenerates current output and byte+path compares; wired as the parity-gate frozenness step (D-03). |
| T-12-03-06 | Tampering | Coverage report counts catalog/planner/known-limitation as engine-supported in CI | high | mitigate | extended generate_corpus_report.py --check hard-gates the prerequisite rule in the corpus job (D-01). |
| T-12-03-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new runtime dependencies; compare_backends.py is Python stdlib (RESEARCH Package Legitimacy Audit N/A). |
</threat_model>

## Artifacts this phase produces

Files: scripts/compare_backends.py (new), .github/workflows/ci.yml (modified — linear-wasm-parity gains js runtime + compare_backends steps, parity-gate gains diff_parity.py --frozen-only, corpus gains verify_corpus.py --check).

Symbols (scripts/compare_backends.py): main(argv) with optional --targets; run_target(t) subprocess (moon test --target {t} --package parity, rc + failing snapshot filenames); snapshot_tree_digest(dir) deterministic sha256 directory digest (sorted relative paths + per-file sha256); per-target report printer (target, rc, pass/fail, failing-fixture list, digest); aggregate verdict (all-pass AND digest-match AND non-empty) exit 0 else 1; non-empty guard.
Symbols (.github/workflows/ci.yml): linear-wasm-parity job steps (moon test --target js --package parity; python3 scripts/compare_backends.py), parity-gate job step (python3 scripts/diff_parity.py --frozen-only), corpus job step (python3 scripts/verify_corpus.py --check).
