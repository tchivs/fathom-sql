---
phase: 10-flink-release-profiles-and-lexical-core
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - lexer/lexer.mbt
  - token/token.mbt
  - parity/flink_lexical_test.mbt
  - parity/fixtures/flink-lexical/manifest.tsv
  - parity/__snapshot__/flink-lexical.*
autonomous: true
requirements: [FLINK-01]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Flink input receives release-specific comment, quote, literal, operator, and identifier tokenization with source trivia/spans preserved: `//` and `--` are single-line comments, `/* */`/`/** */`/`/*+` are block/hint comments, `#` is a lexical error, `'...'` is a string literal with `''` doubling and NO backslash escape, backticks are quoted identifiers with double-backtick escaping, `\"...\"` is a DOUBLE_QUOTE symbol (parse error), X/U&/N/E prefixed literals tokenize as single literal tokens (E only under flink-2.3.0/2.1.3, absent under flink-1.20.5), B is NOT a literal prefix, and `||`/`=>`/`..` are single symbol tokens (D-03, SC3)."
    - "Every lexical behavior is grounded in the pinned release grammar, not folklore: the fixtures assert the Calcite token evidence (SINGLE_LINE_COMMENT \"//\"|\"--\" Parser-calcite-1.36.0.jj:8901, DOUBLE_QUOTE :8797, BINARY_STRING_LITERAL :8708, UNICODE_STRING_LITERAL :8719, C_STYLE_ESCAPED_STRING_LITERAL :8721/1.34.0:8469/none in 1.32.0, BTID backtick :8951-8962) (D-03, Pitfall 2)."
    - "The same input may tokenize differently under doris vs flink, but each path is explainable and frozen by a parity snapshot — never a silent borrow of the other dialect's policy (D-06)."
    - "Doris lexical behavior is byte-identical: every lexer branch explicitly matches context.dialect with the Doris arm preserved verbatim; the 213-snapshot baseline shows zero drift (Pitfall 1)."
    - "[probe FLINK-01 adjacency] flagged-unverified: lexical span adjacency — `//` immediately before EOF, `/*` with no terminator, `''` empty string literal, and backtick double-escape touching a quote byte produce half-open spans that are source-backed and bounded exactly like the Doris scanner; the lexer tests pin these edge spans."
    - "[probe FLINK-01 empty] flagged-unverified: empty lexical constructs — empty string literal `''`, empty single-line comment `--\n`, unterminated `/*`, and a lone backtick — tokenize with the shared scanner's unterminated/error discipline (FATHOM-PARSE-003 path), never a silent success; the fixtures record each result."
    - "[probe FLINK-01 ordering] flagged-unverified: the conflict-matrix snapshot order and the token-stream emission order are deterministic (source order, first-match classification unchanged); the snapshot filenames enumerate the matrix so ordering is auditable."
  artifacts:
    - path: "lexer/lexer.mbt"
      provides: "context.dialect branches for `//`, `\"`, backtick escaping, single-quote escaping, X/U&/N/E literal prefixes (E gated by profile), and `||`/`=>`/`..` symbol scanning — Doris arms byte-identical"
    - path: "token/token.mbt"
      provides: "TokenKind extension (only if the existing StringLiteral/Symbol kinds cannot represent the Flink literal tokens — minimal-extension bias, documented in the code)"
    - path: "parity/flink_lexical_test.mbt"
      provides: "one fixture per D-06 lexical conflict entry, snapshotted under each relevant flink profile AND doris (independent flink-lexical namespace)"
    - path: "parity/__snapshot__/flink-lexical.{fixture}.{profile}.{strict,editor}.json"
      provides: "frozen tokenization/parse envelopes for the full conflict matrix (double-quote, hash, //, E-literal version diff, backtick escape)"
    - path: "parity/fixtures/flink-lexical/manifest.tsv"
      provides: "fixture rows appended for each matrix entry (profile, calcite_version, parser_config provenance)"
  key_links:
    - from: "lexer/lexer.mbt dialect branches"
      to: "dialect/flink.mbt (10-01 FlinkProfile metadata)"
      via: "context.dialect routing + the E-literal profile gate reads the selected FlinkProfile (flink-2.3.0/2.1.3 support E, flink-1.20.5 does not)"
    - from: "parity/flink_lexical_test.mbt fixtures"
      to: "parity/__snapshot__/flink-lexical.*"
      via: "the D-08 @test.T::snapshot gate (moon test --package parity no --update)"
    - from: "parity/__snapshot__/flink-lexical.*"
      to: "the Doris 213 baseline"
      via: "independent namespace (Pitfall 7): flink-lexical snapshots never touch doris-named snapshots; `git diff --name-only -- parity/__snapshot__` shows no doris-named snapshot changed"
  prohibitions:
    - "No lexical behavior is borrowed from Doris or Calcite folklore into Flink — every branch and fixture is grounded in the pinned release grammar token evidence (D-03, Pitfall 2)."
    - "The Doris lexical arms stay byte-identical — each lexer branch explicitly matches context.dialect and the Doris path is preserved verbatim (Pitfall 1); any Doris drift requires a registered approval."
    - "moon test --update --package parity runs only after the Phase 10 approved-changes.md register entry is committed; the flink-lexical group is an independent namespace that never rewrites a Doris snapshot (D-04/D-08, Pitfall 7)."
---

<objective>
**As a** consumer, **I want to** receive Flink-specific tokenization — comments, quoting, literals, operators, and identifiers matching the pinned Flink/Calcite release contract — **so that** Flink lexical behavior is correct and independently verifiable before Phase 11 grammar lands.

Purpose: Expand the 10-01 tracer's single `#` branch into the full Flink lexical core (D-03): `//` comments, double-quote-as-symbol, backtick double-escape, single-quote no-backslash, X/U&/N/E literal prefixes (E version-gated), and `||`/`=>`/`..` operators — then freeze the D-06 conflict matrix as explainable flink-lexical parity snapshots while the Doris 213-snapshot baseline stays byte-identical (Pitfall 1/7).
Output: The dialect-aware lexer branches + lexer tests, the conflict-matrix fixture set + flink-lexical snapshots, and the manifest fixture rows.
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-flink-release-profiles-and-lexical-core/10-CONTEXT.md
@.planning/phases/10-flink-release-profiles-and-lexical-core/10-RESEARCH.md
@.planning/phases/10-flink-release-profiles-and-lexical-core/10-PATTERNS.md

Builds on 10-01's FlinkProfile identity. The lexical facts are in RESEARCH §9 (comment/quote/literal/operator/identifier matrices with verbatim Parser.jj line citations) and the keyword/conflict matrix §10. The lexer analog is `lexer/lexer.mbt`'s `lex_with_limit` (`:250-363`) — the scanning branches at `:277-313` (`#`/`--` line comments, `/* */` block, `'` string via `scan_quoted` `:292-306`, `"`/`` ` `` quoted `:300-306`) and `symbol_width` (`:74-89`) for multi-char symbols. `TokenKind` (`token/token.mbt:5-17`) currently has Whitespace/Newline/Comment/Bom/Identifier/Number/Quoted/StringLiteral/Symbol/Unknown/Error — Flink literal prefixes may map to StringLiteral (minimal-extension bias) or need a new kind (schema-level decision, documented). The conflict matrix (D-06) requires one fixture + snapshot per entry: double-quote, `#`, `//`, `E'..'` version diff, backtick escape, unknown-profile. The Doris baseline gate (D-04/D-08) and the approved-changes register from 10-01 govern every snapshot write.

> **Note on the 10-01 hash-comment fixture:** 10-01 already freezes the `#` conflict; 10-02's Task 2 adds the remaining matrix entries without touching the hash-comment snapshots (independent filenames).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Dialect-aware lexer branches for Flink quoting, literals, operators, and backtick escaping</name>
  <files>lexer/lexer.mbt, token/token.mbt</files>
  <read_first>
    @.planning/phases/10-flink-release-profiles-and-lexical-core/10-CONTEXT.md
    @.planning/phases/10-flink-release-profiles-and-lexical-core/10-RESEARCH.md
    @.planning/phases/10-flink-release-profiles-and-lexical-core/10-PATTERNS.md
    @/opt/source/Fathom/lexer/lexer.mbt
    @/opt/source/Fathom/token/token.mbt
    @/opt/source/Fathom/dialect/dialect.mbt
  </read_first>
  <behavior>
    - Test 1: under a Flink context, `b"a // comment"` lexes `// comment` as one Comment token; under a Doris context the same bytes lex `//` as two Symbol tokens (byte-identical to v1).
    - Test 2: under a Flink context, `b"a \\"x\\""` lexes `"` as one Symbol token then `x` as an Identifier then `"` as a Symbol; under Doris `"x"` is one Quoted token (byte-identical).
    - Test 3: under a Flink context, backtick input `` b"`a``b`" `` lexes as ONE Quoted token (double-backtick escape per BTID); a lone unterminated backtick yields the unterminated-literal error path.
    - Test 4: under a Flink context, `b"E'\\\\n'"` lexes as ONE literal token under flink-2.3.0 and flink-2.1.3, and as Identifier `E` + StringLiteral `'\\\\n'` under flink-1.20.5 (C_STYLE_ESCAPED_STRING_LITERAL absent in Calcite 1.32.0).
    - Test 5: under a Flink context, `b"X'AB'", b"U&'d\\\\0061t'", b"N'name'"` each lex as ONE literal token; `b"B'101'"` lexes as Identifier `B` + StringLiteral (no BIT_STRING_LITERAL token).
    - Test 6: under a Flink context, `b"a || b", b"f(x => 1)", b"t..c"` lex `||`/`=>`/`..` as single Symbol tokens; the Doris multi-char ops `<=`/`>=`/`<>`/`!=`/`<=>` remain single symbols under both dialects.
  </behavior>
  <action>In `lexer/lexer.mbt` `lex_with_limit`, add a `match context.dialect` at each scanning branch (the 10-01 `#` branch already establishes the pattern). Branch list and evidence: (1) `//` (bytes 47 47) — under `Dialect::Flink` route to the existing `scan_comment` (block-comment path handles line comments to newline; SINGLE_LINE_COMMENT accepts `"//"` AND `"--"` per Parser-calcite-1.36.0.jj:8901) and emit a Comment token; the Doris arm keeps the current two-SLASH symbol behavior byte-identical. (2) `"` (byte 34) — under `Dialect::Flink` emit a Symbol token carrying the raw `"` (DOUBLE_QUOTE token :8797; no consuming production, so the parse-level error is Phase 11's concern — at the lexical layer it is a symbol); the Doris arm keeps Quoted byte-identical. (3) backtick `` ` `` (byte 96) — under `Dialect::Flink` use a Flink-aware quoted scan that supports double-backtick escaping (BTID state :8951-8962 `BACK_QUOTED_IDENTIFIER: "\`" ( (~["\`","\n","\r"]) | ("\`\`") )+ "\`"`); a lone backtick or an unterminated one goes through the shared unterminated-literal discipline; the Doris arm keeps the current Quoted scan byte-identical. (4) single quote `'` — under `Dialect::Flink` use a Flink-aware string scan that supports only `''` doubling (QUOTED_STRING :8715 — NO backslash escape), while the Doris arm keeps backslash escape via `scan_quoted` byte-identical. (5) literal prefixes — when a Flink-context token beginning with X/x, U&, N, or E is immediately followed by a quote, scan it as ONE literal token (BINARY_STRING_LITERAL :8708 for X, UNICODE_STRING_LITERAL :8719 for U& with UESCAPE, PREFIXED_STRING_LITERAL :8717 for N and `_charset`, C_STYLE_ESCAPED_STRING_LITERAL :8721 for E); gate the E prefix by the selected FlinkProfile — allowed under flink-2.3.0/2.1.3, NOT under flink-1.20.5 (E absent in Calcite 1.32.0; the profile gate reads `context.profile_id` and matches the FlinkProfile enum). B is NOT a literal prefix (no BIT_STRING_LITERAL token in any release — B tokenizes as Identifier + StringLiteral). If the existing `@token.TokenKind::StringLiteral` can carry these literal tokens with their spans, prefer that (minimal-extension bias); if a distinct kind is genuinely required, extend `TokenKind` in `token/token.mbt` per the existing enum shape and document the schema-level rationale in a code comment. (6) operators — extend `symbol_width` (`:74-89`) so under a Flink context `||` (CONCAT :8793), `=>` (NAMED_ARGUMENT_ASSIGNMENT :8794), and `..` (DOUBLE_PERIOD :8795) scan as single symbols; the Doris multi-char operators (`<=`, `>=`, `<>`, `!=`, `<=>`) and all single-byte symbols remain unchanged under both dialects. Add a `test_flink_context(profile_id : String) -> @dialect.DialectContext` helper (analog of the existing `test_doris_context` at `:488-490`) and write the inline lexer tests from the behavior block. Run `moon test --package lexer` after the branch work; the D-06 conflict fixtures' token-level assertions must match the frozen grammar evidence.</action>
  <acceptance_criteria>
    - `moon test --target native --package lexer` passes with the new Flink-branch tests; each behavior-block case (1)-(6) is asserted with the exact token kinds and spans.
    - The Doris arms are byte-identical: `moon test --package parity` (no --update) passes and `git diff --name-only -- parity/__snapshot__` shows no doris-named snapshot changed.
    - `// x` under Flink is one Comment token, under Doris two Symbol tokens; `"x"` under Flink is Symbol+Identifier+Symbol, under Doris one Quoted token; `` `a``b` `` under Flink is one Quoted token; `E'...'` under flink-2.3.0/2.1.3 is one literal token and under flink-1.20.5 is Identifier+String; `X'..'`/`U&'..'`/`N'..'` are single literal tokens; `B'101'` is Identifier+String; `||`/`=>`/`..` are single Symbol tokens.
    - Unterminated constructs (`'`, `` ` ``, `/*`) under Flink produce the shared FATHOM-PARSE-003-adjacent error tokens, not silent success; span/trivia/progress guarantees stay shared (DIALECT-02).
    - No new external dependency was added; token/token.mbt is changed ONLY if a distinct TokenKind is documented as necessary.
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native --package lexer && moon test --package parity</automated>
  </verify>
  <done>The Flink lexer implements the full §9 lexical matrix with release-grounded behavior, the Doris arms are byte-identical, and the parity baseline shows zero drift.</done>
</task>

<task type="auto">
  <name>Conflict-matrix parity fixtures + flink-lexical snapshot group expansion</name>
  <files>parity/flink_lexical_test.mbt, parity/__snapshot__/flink-lexical.*, parity/fixtures/flink-lexical/manifest.tsv</files>
  <read_first>
    @.planning/phases/10-flink-release-profiles-and-lexical-core/10-RESEARCH.md
    @.planning/phases/10-flink-release-profiles-and-lexical-core/10-PATTERNS.md
    @/opt/source/Fathom/parity/flink_lexical_test.mbt
    @/opt/source/Fathom/parity/baseline_test.mbt
    @.planning/phases/10-flink-release-profiles-and-lexical-core/approved-changes.md
  </read_first>
  <action>Extend `parity/flink_lexical_test.mbt` (created in 10-01) with one fixture per D-06 lexical conflict entry, each snapshotted under the relevant flink profile(s) AND the corresponding doris profile so the same input's different tokenization is frozen side by side (D-06: different tokenization allowed, every path explainable). Fixture list (filename form `flink-lexical.{fixture}.{profile}.{strict|editor}.json`, independent namespace): (1) `double-quote` — raw like `SELECT "a" FROM t` under flink-2.3.0 (DOUBLE_QUOTE symbol → no consuming production, lexical symbol) and under doris-4.x (Quoted, unchanged); (2) `slash-comment` — raw `SELECT a // comment` under flink-2.3.0 (Comment) and under doris-4.x (two Symbol tokens); (3) `e-literal` — raw `SELECT E'a\\nb'` under flink-2.3.0 (one C_STYLE literal), flink-2.1.3 (one literal), and flink-1.20.5 (Identifier E + StringLiteral — the version-sensitive regression entry); (4) `backtick-escape` — raw `` SELECT `a``b` FROM t `` under flink-2.3.0 (one Quoted, double-backtick escape) and doris-4.x (Quoted per current Doris scan); (5) `unknown-profile` — raw `SELECT 1` under an unsupported profile id (e.g. `flink-9.9`) asserting the FATHOM-SCHEMA-003 rejection envelope is frozen. Use the `@test.T::snapshot` mechanism (baseline_test.mbt pattern) so `moon test --package parity` (no --update) is the byte gate. Append the corresponding fixture rows to `parity/fixtures/flink-lexical/manifest.tsv` (fixture_id, profile, exact_release, calcite_version, parser_config). Sequence per D-04/D-08: FIRST record a register entry in `.planning/phases/10-flink-release-profiles-and-lexical-core/approved-changes.md` stating the flink-lexical matrix snapshot additions (new files, independent namespace — Doris 213 untouched), commit it, THEN run `moon test --update --package parity` exactly once, THEN verify `moon test --package parity` (no --update) and `git diff --name-only -- parity/__snapshot__` (no doris-named snapshot changed) report zero Doris drift.</action>
  <acceptance_criteria>
    - parity/__snapshot__/flink-lexical.{double-quote,slash-comment,e-literal,backtick-escape,unknown-profile}.{flink-2.3.0|flink-2.1.3|flink-1.20.5|doris-4.x}.{strict,editor}.json exist; the 10-01 hash-comment snapshots are untouched.
    - The double-quote and slash-comment fixtures show different tokenization under doris vs flink (explainable per the frozen grammar evidence); the e-literal fixture shows E supported under flink-2.3.0/2.1.3 and absent under flink-1.20.5; the unknown-profile fixture freezes the FATHOM-SCHEMA-003 rejection envelope.
    - parity/fixtures/flink-lexical/manifest.tsv contains the appended fixture rows with calcite_version/parser_config per profile.
    - The register entry is committed BEFORE the single --update; `moon test --package parity` (no --update) passes; `git diff --name-only -- parity/__snapshot__` shows no doris-named snapshot changed (only flink-lexical.* files are added/modified).
  </acceptance_criteria>
  <verify>
    <automated>moon test --package parity && test -z "$(git diff --name-only -- parity/__snapshot__ | grep -v 'flink-lexical\.')" && grep -l "flink-lexical\." parity/__snapshot__/*.json | wc -l</automated>
  </verify>
  <done>The full D-06 lexical conflict matrix is frozen as explainable flink-lexical snapshots; Doris 213-snapshot baseline is byte-identical; the register + single approved --update discipline is followed.</done>
</task>

</tasks>

## Source Audit Coverage

| SOURCE | ID | Feature or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | lexical behavior independent from Doris, conflict cases explainable | 10-02 | COVERED | Full §9 matrix branches + conflict snapshots. |
| REQ | FLINK-01 | release-specific comment/quote/literal/operator/identifier classification; explainable snapshots | 10-02 | COVERED | Tasks 1-2. |
| CONTEXT | D-03 | lexical behavior from executable release fixtures; `//`/`"`/literals per release; no folklore | 10-02 | COVERED | Task 1 branches + evidence citations. |
| CONTEXT | D-06 | conflict matrix with explainable snapshots; different tokenization allowed, no silent borrow | 10-02 | COVERED | Task 2 fixtures + snapshots. |
| CONTEXT | D-04 | flink-lexical group + conflict matrix; Doris zero-drift | 10-02 | COVERED | Task 2 independent namespace + register. |
| RESEARCH | §9 lexical matrix | SINGLE_LINE_COMMENT // and --; DOUBLE_QUOTE; X/U&/N/E/B literals; BTID backtick; ||/=>/.. operators | 10-02 | COVERED | Task 1 branch list + Task 2 fixtures. |
| RESEARCH | §10 keyword conflict matrix | QUALIFY/VARIANT version sensitivity (classification-level) | 10-02 | PARTIAL | Classification rows/tests in 10-03; lexical matrix in 10-02. |
| RESEARCH | Pitfall 1/2/7 | Doris zero-drift; folklore; snapshot namespace | 10-02 | COVERED | Prohibitions + Task 2 register discipline. |
| PROBE | FLINK-01 adjacency | lexical span adjacency (// at EOF, `''`, backtick touch) | 10-02 | FLAGGED | truth + lexer tests. |
| PROBE | FLINK-01 empty | empty string `''`, empty comment, unterminated `/*`, lone backtick | 10-02 | FLAGGED | truth + tests. |
| PROBE | FLINK-01 ordering | token emission + snapshot order deterministic | 10-02 | FLAGGED | truth + snapshot enumeration. |

<verification>
Run moon test --target native --package lexer after Task 1 and moon test --package parity (no --update) after Task 2. Inspect: the dialect branches (Doris arms byte-identical), the E-literal profile gate, the TokenKind decision (minimal-extension documented), the conflict-matrix snapshot set (double-quote/hash/slash-comment/e-literal/backtick-escape/unknown-profile under the relevant profiles), the manifest fixture rows, and the register-before-update discipline. Do not touch parser/parse_flink_segment (grammar is Phase 11), do not change Doris profile ids, and do not run moon test --update without a committed register entry.
</verification>

<success_criteria>
- Flink input receives the full release-specific lexical behavior (comments, quoting, literals, operators, identifiers) with shared trivia/span guarantees and Doris arms byte-identical.
- The D-06 conflict matrix is frozen as explainable flink-lexical snapshots (different tokenization per dialect, never a silent borrow).
- The Doris 213-snapshot baseline is byte-identical and the register discipline holds.
</success_criteria>

<output>Create `.planning/phases/10-flink-release-profiles-and-lexical-core/10-02-SUMMARY.md` when this plan executes.</output>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flink input bytes -> lexer branches | Untrusted SQL (including malformed/`#`/`"`/literal-prefix material) crosses here; the dialect branch must not silently apply Doris policy. |
| profile_id -> E-literal gate | The selected FlinkProfile gates E'..' availability; a wrong gate accepts E under flink-1.20.5. |
| flink-lexical snapshots -> CI gate | The matrix group must stay byte-identical unless registered; the Doris group must never be touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-07 | Tampering | Flink lexical branch leaks Doris policy (`//` or `"` accepted as Doris does, `#` accepted as comment) | high | mitigate | Explicit `match context.dialect` at every branch; Doris arm byte-identical; conflict fixtures assert the difference (Task 1-2). |
| T-10-08 | Tampering | E-literal profile gate wrong (E accepted under flink-1.20.5) | high | mitigate | Gate reads the selected FlinkProfile (flink-2.3.0/2.1.3 allow, 1.20.5 denies); e-literal fixture under all three profiles (Task 1). |
| T-10-09 | Tampering | Doris 213-snapshot drift during lexer branch work | high | mitigate | parity gate without --update; register committed before any update; git diff --name-only -- parity/__snapshot__ shows no doris-named snapshot changed (Task 1-2, Pitfall 1). |
| T-10-10 | Tampering | flink-lexical matrix merged into Doris namespace (register pollution) | medium | mitigate | Independent flink-lexical naming + register entry documenting the namespace boundary (Pitfall 7); per-group diff. |
| T-10-11 | Denial of Service | Matrix fixture explosion slows the parity suite | low | accept | Bounded matrix (5 conflict entries x relevant profiles x 2 modes); filenames enumerated in the plan and register. |
| T-10-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies; lexer work is MoonBit core only. |
</threat_model>

## Artifacts this phase produces

Files: lexer/lexer.mbt (modified — dialect branches for `//`, `"`, backtick, single-quote, literals, operators), token/token.mbt (modified ONLY if a distinct TokenKind is documented as necessary), parity/flink_lexical_test.mbt (modified — full conflict-matrix fixtures), parity/__snapshot__/flink-lexical.{double-quote,slash-comment,e-literal,backtick-escape,unknown-profile}.{flink-2.3.0|flink-2.1.3|flink-1.20.5|doris-4.x}.{strict,editor}.json (new, generated), parity/fixtures/flink-lexical/manifest.tsv (modified — fixture rows).

Symbols (lexer/lexer.mbt): `lex_with_limit` dialect branches (comment/quote/literal/operator), `symbol_width` Flink extensions, `test_flink_context(profile_id)` helper, inline lexer tests for cases (1)-(6).
Symbols (token/token.mbt, conditional): optional new TokenKind variant(s) for Flink literal tokens.
Symbols (parity/flink_lexical_test.mbt): per-conflict fixture rows + @test.T::snapshot tests; parity/__snapshot__/flink-lexical.*.{strict,editor}.json.
