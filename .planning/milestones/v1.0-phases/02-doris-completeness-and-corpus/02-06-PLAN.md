---
phase: 02-doris-completeness-and-corpus
plan: 06
type: execute
wave: 5
depends_on: [02-04]
files_modified:
  - corpus/tools/sqlglot_diff.py
  - corpus/tools/fe_nereids_diff.sh
  - corpus/tools/README.md
  - corpus/requirements.txt
  - corpus/differential.tsv
autonomous: true
requirements: [CORP-04]
estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "[probe CORP-04 unclassified] resolved: differential checks run locally against feasible references — SQLGlot (pinned 30.14.0, pip-installed dev tooling) parses each manifest fixture with read='doris' and records accepted/rejected observations, while FE/Nereids remains a documented manual script (Java build, offline-unavailable); every differential row records a version-specific resolution and carries advisory_only=true, and neither implementation can widen public acceptance (CORP-04, D-20)."
    - "differential.tsv rows are advisory evidence only: support status is determined solely by the released-docs manifest (D-07/D-20); a SQLGlot or FE acceptance can never promote an unsupported fixture."
    - "The differential tooling is deterministic and offline-runnable: SQLGlot is pinned, the script records sqlglot.__version__ per run, and a missing/uninstallable sqlglot falls back to not-run-offline rows instead of failing the run."
  flagged_assumptions:
    - "FLAGGED-ASSUMPTION [research A8]: PyPI was reachable at research time; if pip install 'sqlglot==30.14.0' fails at execution time (offline), the script records not-run-offline observations and the plan documents the fallback rather than fabricating results."
    - "FLAGGED-ASSUMPTION [D-20 manual scope]: FE/Nereids differential requires a Java build and possibly a cluster; the manual script is written and documented but never executed or CI-wired in this phase."
  artifacts:
    - path: "corpus/tools/sqlglot_diff.py"
      provides: "Local SQLGlot differential runner (read='doris', pinned version, advisory_only rows)"
    - path: "corpus/tools/fe_nereids_diff.sh"
      provides: "Documented manual FE/Nereids differential script (never CI)"
    - path: "corpus/tools/README.md"
      provides: "Run instructions, pins, and disagreement-resolution policy"
    - path: "corpus/requirements.txt"
      provides: "sqlglot==30.14.0 pin for dev tooling"
    - path: "corpus/differential.tsv"
      provides: "Populated disagreement/resolution records with advisory_only=true"
  key_links:
    - from: "corpus/tools/sqlglot_diff.py"
      to: "corpus/manifest.tsv"
      via: "iterates manifest fixtures (fixture_id + profile) and reads corpus/doris-*/<fixture>.sql"
    - from: "corpus/tools/sqlglot_diff.py"
      to: "corpus/differential.tsv"
      via: "writes one advisory row per fixture with sqlglot_version, sqlglot_accepted, resolution, advisory_only=true"
    - from: "corpus/tools/fe_nereids_diff.sh"
      to: "corpus/differential.tsv"
      via: "documented manual appends of fe_nereids_observation rows"
    - from: "corpus/differential.tsv"
      to: "corpus/coverage.tsv"
      via: "differential observations never alter coverage support status (advisory-only rule)"
  prohibitions:
    - "No differential row may carry advisory_only=false; neither SQLGlot nor FE/Nereids becomes the public contract (D-20)."
    - "No script executes SQL against a cluster or database; SQLGlot parsing only (D-20, threat register)."
    - "No unpinned sqlglot usage; the version is recorded in requirements.txt and per-run output."
---

<objective>
**As a** maintainer, **I want to** run local differential checks against SQLGlot and (manually) FE/Nereids, **so that** disagreements with the released-docs authority are recorded with version-specific resolutions without letting either reference become the public contract.

Purpose: Deliver CORP-04 — a locally runnable SQLGlot differential script (pinned 30.14.0), a documented manual FE/Nereids script, and differential.tsv rows recording observations and resolutions with advisory_only=true (D-20). The released-docs manifest remains the sole acceptance authority (D-07).
Output: sqlglot_diff.py, fe_nereids_diff.sh, README, requirements pin, and populated differential.tsv.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-doris-completeness-and-corpus/02-CONTEXT.md
@.planning/phases/02-doris-completeness-and-corpus/02-RESEARCH.md
@.planning/phases/02-doris-completeness-and-corpus/02-PATTERNS.md
@.planning/phases/02-doris-completeness-and-corpus/02-04-PLAN.md
</execution_context>

<context>
02-04 shipped the extended manifest with DML/DDL fixture rows and the coverage report; this plan adds the differential layer. The research provides the SQLGlot script shape verbatim (Code Example 13: csv.DictReader over manifest.tsv, sqlglot.parse(sql, read="doris"), records sqlglot_version/sqlglot_accepted/resolution/advisory_only=true) and the PyPI legitimacy audit (sqlglot 30.14.0 approved, pin recorded). The existing differential.tsv has rows for Phase 1 fixtures with not-run-offline observations; this plan populates SQLGlot observations for all manifest fixtures (Phase 1 + Phase 2) and adds the FE/Nereids manual script. Disagreement policy: when SQLGlot rejects a fixture the SDK supports (or accepts one the SDK rejects), the resolution column records the version-specific outcome (e.g., "docs 4.x document MERGE; sqlglot accepts; SDK follows docs; disagreement recorded"). The script must handle a missing sqlglot import gracefully (not-run-offline rows) so the corpus pipeline stays honest and offline-tolerant (A8).
</context>

<tasks>

<task type="tracer">
  <name>Task 1: SQLGlot differential runner with pinned version</name>
  <files>corpus/tools/sqlglot_diff.py, corpus/requirements.txt, corpus/differential.tsv</files>
  <read_first>
    @.planning/phases/02-doris-completeness-and-corpus/02-RESEARCH.md
    @.planning/phases/02-doris-completeness-and-corpus/02-PATTERNS.md
    @.planning/phases/02-doris-completeness-and-corpus/02-CONTEXT.md
  </read_first>
  <action>Create corpus/requirements.txt with a single pin line "sqlglot==30.14.0" and corpus/tools/sqlglot_diff.py implementing the research Code Example 13 shape with the following contracts: import sqlglot lazily inside the run path so a missing installation yields not-run-offline observations instead of a traceback; read corpus/manifest.tsv with csv.DictReader (delimiter tab); for each row resolve the fixture SQL path as corpus/doris-<profile>/<fixture_id>.sql (match the file naming used by 02-04) and read the file text; attempt sqlglot.parse(sql, read="doris") and record sqlglot_accepted true/false; write one row per fixture to corpus/differential.tsv with columns fixture_id, public_contract, fe_nereids_observation, sqlglot_observation, sqlglot_version, resolution, advisory_only — public_contract always "released-docs", fe_nereids_observation "not-run-offline" (manual script owns it), advisory_only always "true"; the resolution column carries a version-specific statement per row (e.g., for 4.x MERGE fixtures: "docs 4.x document MERGE; sqlglot agrees/disagrees; SDK follows released-docs authority; disagreement recorded"); keep the existing Phase 1 differential rows updated to the same shape. Run pip install "sqlglot==30.14.0" (dev venv or user-level per environment), run the script, and verify differential.tsv gains a sqlglot_observation for every manifest fixture with sqlglot_version equal to the pinned 30.14.0. If the install fails (offline), the script must still produce not-run-offline rows and the plan's flagged A8 fallback applies. Never execute parsed SQL — parse only.</action>
  <acceptance_criteria>
    - sqlglot_diff.py runs locally, parses every manifest fixture with read='doris' under the pinned 30.14.0, and writes advisory rows with sqlglot_version, sqlglot_accepted, and version-specific resolutions (CORP-04, D-20).
    - Every differential row has advisory_only=true; the script never promotes or demotes a fixture's support status (D-07).
    - Missing sqlglot degrades to not-run-offline rows without failure or fabrication (A8 fallback).
  </acceptance_criteria>
  <verify><automated>python3 corpus/tools/sqlglot_diff.py && python3 corpus/tools/generate_corpus_report.py --check</automated></verify>
  <done>The SQLGlot differential runner is committed and has populated differential.tsv with pinned-version advisory observations for all manifest fixtures.</done>
</task>

<task type="auto">
  <name>Task 2: FE/Nereids manual script and differential documentation</name>
  <files>corpus/tools/fe_nereids_diff.sh, corpus/tools/README.md, corpus/differential.tsv</files>
  <read_first>
    @.planning/phases/02-doris-completeness-and-corpus/02-RESEARCH.md
    @.planning/phases/02-doris-completeness-and-corpus/02-PATTERNS.md
  </read_first>
  <action>Create corpus/tools/fe_nereids_diff.sh as a documented MANUAL script (never CI-wired, never executed in this phase per D-20): it must (1) pin a FE version via an env/argument FE_VERSION (default documented as the release family matching the fixture), (2) document the Java build prerequisite and the NereidsParser-based acceptance check against each corpus fixture (reference the official apache/doris parser path from RESEARCH.md), (3) append rows to corpus/differential.tsv with fe_nereids_observation accepted/rejected/not-run-offline and the same advisory_only=true contract. Create corpus/tools/README.md documenting: how to run sqlglot_diff.py (venv + pip install from requirements.txt), how to run the FE script manually (Java build, FE pin, what it records), the disagreement-resolution policy (released-docs authority wins; resolutions are version-specific), and the not-run-offline fallbacks. Review the final corpus/differential.tsv: every row advisory_only=true, no row alters coverage support status, resolutions are version-specific, and the report --check (02-04 generator) still passes with the differential content present.</action>
  <acceptance_criteria>
    - fe_nereids_diff.sh is a complete, documented manual script (FE pin, Java prerequisite, NereidsParser acceptance loop, advisory row appends) that never runs in CI (CORP-04, D-20).
    - README.md documents both differential paths, pins, fallbacks, and the released-docs-authority resolution policy.
    - Final differential.tsv: every row advisory_only=true with version-specific resolutions; the 02-04 report --check passes.
  </acceptance_criteria>
  <verify><automated>bash -n corpus/tools/fe_nereids_diff.sh && python3 corpus/tools/generate_corpus_report.py --check && moon test</automated></verify>
  <done>The FE/Nereids manual script and differential documentation are committed; differential.tsv is complete, advisory-only, and consistent with the coverage report.</done>
</task>

</tasks>

## Source Audit Coverage

| SOURCE | ID | Feature or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Recorded differential disagreements and resolutions | 02-06 | COVERED | sqlglot + FE scripts + differential.tsv. |
| REQ | CORP-04 | Differential vs feasible FE/Nereids and SQLGlot, disagreements recorded, neither the contract | 02-06 | COVERED | Tasks 1-2. |
| RESEARCH | Code Example 13 | SQLGlot script shape | 02-06 | COVERED | Task 1. |
| RESEARCH | Pattern 6 | Differential harness, advisory_only=true, version-specific resolution | 02-06 | COVERED | Tasks 1-2. |
| RESEARCH | Package audit | sqlglot approved (PyPI, pinned 30.14.0) | 02-06 | COVERED | requirements.txt pin; no checkpoint needed (approved, not ASSUMED/SUS). |
| CONTEXT | D-20 | SQLGlot local, FE manual, neither public contract | 02-06 | COVERED | Tasks 1-2. |
| CONTEXT | D-07 | Released-docs authority preserved | 02-06 | COVERED | public_contract column + prohibition. |

<verification>
Run `python3 corpus/tools/sqlglot_diff.py` (installing sqlglot==30.14.0 first per requirements.txt), then `python3 corpus/tools/generate_corpus_report.py --check` and `bash -n corpus/tools/fe_nereids_diff.sh`. Inspect: differential.tsv rows for every manifest fixture (sqlglot_version 30.14.0, advisory_only=true, version-specific resolutions), the FE script's documented manual-only status, and the report --check passing with the differential content. Do not run the FE script, do not execute SQL, and do not wire either differential into CI.
</verification>

<success_criteria>
- CORP-04 is executable: SQLGlot differential is locally runnable with a pinned version; FE/Nereids is a documented manual script; disagreements carry version-specific resolutions.
- No differential row can widen acceptance; advisory_only=true everywhere; the released-docs manifest remains the sole authority.
</success_criteria>

<output>Create `.planning/phases/02-doris-completeness-and-corpus/02-06-SUMMARY.md` when this plan executes.</output>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Differential observations → coverage report | An advisory acceptance could be misread as widening SDK support. |
| SQLGlot package → dev environment | Third-party code is installed as dev tooling only. |
| FE script → cluster | Running FE differential could execute against a cluster if misused. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-51 | Tampering | Differential rows widening acceptance | high | mitigate | advisory_only=true enforced on every row; public_contract always "released-docs"; coverage support status never reads differential observations; report --check keeps them separate (D-07/D-20). |
| T-02-52 | Tampering | Unpinned or drifted sqlglot | medium | mitigate | sqlglot==30.14.0 pinned in requirements.txt; per-run sqlglot_version recorded; recheck pin before each run (research validity note). |
| T-02-53 | Execution | Differential executing against a cluster | medium | mitigate | SQLGlot script parses only (never executes); FE script documented manual-only with Java/cluster prerequisites and never CI-wired (D-20). |
| T-02-54 | Integrity | Fabricated observations on offline failure | medium | mitigate | Missing sqlglot degrades to not-run-offline rows; no synthetic accepted/rejected values (A8 fallback). |
| T-02-SC | Tampering | sqlglot package install | medium | mitigate | Approved by the RESEARCH.md PyPI legitimacy audit (OK verdict, pinned); installed in a dev venv only; never a runtime or parser-core dependency. |

## Artifacts this phase produces

Files: corpus/tools/sqlglot_diff.py (new), corpus/tools/fe_nereids_diff.sh (new), corpus/tools/README.md (new), corpus/requirements.txt (new), corpus/differential.tsv (populated: sqlglot observations for every manifest fixture, advisory_only=true, version-specific resolutions).
</threat_model>
