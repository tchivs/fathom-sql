---
phase: 02-doris-completeness-and-corpus
plan: 04
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - token/token.mbt
  - corpus/manifest.tsv
  - corpus/coverage.tsv
  - corpus/CORPUS-REPORT.md
  - corpus/doris-2.1/dml-insert-values.sql
  - corpus/doris-2.1/dml-insert-select.sql
  - corpus/doris-2.1/dml-update.sql
  - corpus/doris-2.1/dml-delete.sql
  - corpus/doris-2.1/ddl-create-table.sql
  - corpus/doris-2.1/ddl-create-view.sql
  - corpus/doris-2.1/ddl-create-index.sql
  - corpus/doris-3.x/dml-insert-values.sql
  - corpus/doris-3.x/dml-insert-select.sql
  - corpus/doris-3.x/dml-update.sql
  - corpus/doris-3.x/dml-delete.sql
  - corpus/doris-3.x/ddl-create-table.sql
  - corpus/doris-3.x/ddl-create-view.sql
  - corpus/doris-3.x/ddl-create-index.sql
  - corpus/doris-4.x/dml-insert-values.sql
  - corpus/doris-4.x/dml-insert-select.sql
  - corpus/doris-4.x/dml-insert-overwrite.sql
  - corpus/doris-4.x/dml-update.sql
  - corpus/doris-4.x/dml-delete.sql
  - corpus/doris-4.x/dml-merge.sql
  - corpus/doris-4.x/ddl-create-table.sql
  - corpus/doris-4.x/ddl-create-table-ctas.sql
  - corpus/doris-4.x/ddl-create-table-like.sql
  - corpus/doris-4.x/ddl-create-view.sql
  - corpus/doris-4.x/ddl-create-index.sql
  - corpus/doris-4.x/ddl-create-materialized-view.sql
  - corpus/doris-4.x/script-multi-statement.sql
  - corpus/doris-4.x/malformed-recovery.sql
  - corpus/tools/generate_corpus_report.py
  - test/parser_test.mbt
  - test/corpus_test.mbt
autonomous: true
requirements: [CORP-01, CORP-02, CORP-03]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "[probe CORP-01 unclassified] resolved: corpus/manifest.tsv rows record profile, exact_release, feature_introduction, official URL, retrieval_date, pinned_source_revision (unavailable-offline policy, never fabricated), page_heading, code_fence, category, support_status, parse_mode, classification, and provenance_status for every fixture, including new DML/DDL categories (CORP-01, D-17)."
    - "[probe CORP-02 adjacency] resolved: golden coverage is per-fixture-row — strict validity, lossless replay, and recovery companions are separate assertions; multi-statement fixtures stay in their own category rows and files rather than being merged into statement-family files (CORP-02, D-18)."
    - "[probe CORP-02 empty] resolved: empty and malformed fixtures are explicit expected-error rows with editor-mode recovery goldens; nothing is accepted without a golden (CORP-02)."
    - "[probe CORP-02 ordering] resolved: coverage rows are ordered deterministically by profile then category; the report generator is deterministic and its --check mode fails when CORPUS-REPORT.md is stale relative to manifest/coverage (CORP-02, D-19)."
    - "[probe CORP-03 unclassified] resolved: CORPUS-REPORT.md presents a version×category matrix (fixture counts, supported, expected-error, known-gap), a failure list, and a known-gaps section; no unqualified full-compatibility claim appears anywhere; regeneration is deterministic and offline (CORP-03, D-19)."
    - "ProfileMetadata feature_introduction canonical strings are extended to cover DML/DDL and both validation allowlists (for_manifest, validate_metadata) accept exactly the three new canonical strings; every manifest row (Phase 1 and new) carries its profile's canonical string so disk rows are consistent with the validated contract (research Pattern 3)."
    - "Every supported and negative fixture is executable through the embedded replay oracle: parse_with_metadata → print_result == raw, all_spans_in_bounds, expected_valid, DORIS-PARSE- prefix on diagnostics (CORP-02, D-18)."
  flagged_assumptions:
    - "FLAGGED-ASSUMPTION [research A3 verification]: during fixture writing, re-read the reachable 2.1 and 3.x CREATE TABLE pages and confirm BUCKETS AUTO (assumed 3.x) and AUTO PARTITION BY (assumed 2.1) introduction versions; if a gate is wrong, amend the token.mbt DorisFeature row and 02-02 fixtures, and record the correction in the coverage note. If the pages are unreachable, keep the assumed gates and add a known-gap row."
    - "FLAGGED-ASSUMPTION [research A2]: CREATE ASYNC MATERIALIZED VIEW stays an explicit unsupported-statement known-gap (DORIS-PARSE-007) unless its docs page is read; Task 2 of this plan re-reads the async-MV docs page and either adds fixtures+support or records a permanent known-gap row — the decision is closed here, not deferred."
    - "FLAGGED-ASSUMPTION [research A8/D-17]: GitHub commit SHAs remain unavailable-offline; retrieval_date and official_url are recorded from the docs site, which was reachable at research time but may not be at execution time — if unreachable, fixture SQL is written from the verbatim grammars/examples in 02-RESEARCH.md and provenance records unavailable-offline plus a known-gap note."
    - "FLAGGED-ASSUMPTION [research A5]: if an Oracle-style multi-table INSERT (INSERT ALL/FIRST) form is found in released docs while writing fixtures, record it as a known-gap row rather than silently accepting it (parser support would be a follow-up)."
  artifacts:
    - path: "token/token.mbt"
      provides: "Extended canonical ProfileMetadata feature_introduction strings and updated for_manifest/validate_metadata allowlists"
    - path: "corpus/manifest.tsv"
      provides: "Migrated Phase 1 rows plus DML/DDL fixture rows with full provenance"
    - path: "corpus/doris-{2.1,3.x,4.x}/*.sql"
      provides: "Versioned released-doc fixture SQL per statement family/variant"
    - path: "corpus/coverage.tsv"
      provides: "Profile × category coverage rows incl. the all/known-gaps row"
    - path: "corpus/tools/generate_corpus_report.py"
      provides: "Deterministic stdlib report generator with --check stale mode"
    - path: "corpus/CORPUS-REPORT.md"
      provides: "Generated version×category matrix, failure list, known-gaps (checked in)"
    - path: "test/corpus_test.mbt"
      provides: "Embedded replay oracle entries for every new fixture row"
    - path: "test/parser_test.mbt"
      provides: "Updated EmbeddedManifestFixture and metadata-mismatch assertions for the new canonical strings"
  key_links:
    - from: "token/token.mbt"
      to: "corpus/manifest.tsv"
      via: "for_manifest accepts exactly the canonical feature_introduction strings the rows carry"
    - from: "corpus/manifest.tsv"
      to: "test/corpus_test.mbt"
      via: "embedded replay oracle entries mirror manifest rows (fixture_id, profile, exact_release, feature_introduction, raw, mode, expected_valid)"
    - from: "corpus/manifest.tsv"
      to: "corpus/coverage.tsv"
      via: "every manifest fixture appears in exactly one coverage row (generator invariant)"
    - from: "corpus/tools/generate_corpus_report.py"
      to: "corpus/CORPUS-REPORT.md"
      via: "--check mode fails on stale or inconsistent report output"
    - from: "test/corpus_test.mbt"
      to: "printer/printer.mbt"
      via: "every oracle entry asserts byte-exact replay"
  prohibitions:
    - "No fabricated pinned_source_revision or SHA; unavailable-offline + known-gap only (D-17)."
    - "No fixture SQL containing shell prompts, mysql> output blocks, Query OK lines, or placeholder variables (research Pitfall 6)."
    - "No unqualified full-compatibility claim in any report artifact (D-19)."
    - "No current/dev documentation as fixture authority (D-07)."
---

<objective>
**As a** maintainer and consumer, **I want to** inspect reproducible official-Doris-document coverage — a versioned fixture manifest, layered goldens, and a version×category failure report — **so that** support claims are auditable and honest about known gaps.

Purpose: Deliver CORP-01..CORP-03 by extending the Phase 1 corpus pipeline to DML/DDL: profile-metadata validation grows to accept DML/DDL canonical strings (research Pattern 3 warning), manifest.tsv gains DML/DDL rows with full provenance (D-17/D-18), the embedded replay oracle covers every row (CORP-02), and coverage.tsv plus a generated CORPUS-REPORT.md publish the matrix with failures and known gaps instead of a compatibility badge (D-19). The report generator's --check mode keeps the report honest (research Pattern 5).
Output: Extended metadata contract, migrated + expanded manifest, versioned fixture SQL files, executable replay goldens, extended coverage.tsv, report generator, and the generated CORPUS-REPORT.md.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-doris-completeness-and-corpus/02-CONTEXT.md
@.planning/phases/02-doris-completeness-and-corpus/02-RESEARCH.md
@.planning/phases/02-doris-completeness-and-corpus/02-PATTERNS.md
@.planning/phases/01-core-kernel/01-04-SUMMARY.md
</execution_context>

<context>
Phase 1 shipped corpus/manifest.tsv (15 SELECT-only rows), coverage.tsv, differential.tsv, and per-version select-industrial.sql fixtures as static contract inputs; runtime tests do not load disk files — test/parser_test.mbt embeds its own fixture copies (EmbeddedManifestFixture, metadata_fixture_replay_ok, released_manifest_metadata_drives_deterministic_parse_replay) and the disk rows are the audit record. token.mbt ProfileMetadata::for_manifest and DorisProfile::validate_metadata currently allowlist exactly three SELECT feature_introduction strings; any DML/DDL row is rejected today (research Pattern 3 warning). This plan performs the deliberate contract extension: new canonical strings per profile that include DML/DDL (V2_1 "2.1 baseline SELECT; DML/DDL released", V3_X "2.1 baseline SELECT; DML/DDL released; 3.x window and QUALIFY", V4_X "2.1 baseline SELECT; DML/DDL released; 4.x released SELECT") with both allowlists updated to exactly those three, then migrates every Phase 1 manifest row and every embedded fixture/assertion that pins the old strings. ValidatedProfileContext::supports keeps its canonical-equality check (rows now carry canonical strings, so feature gates still work). This is a flagged costly change: the ParseResult.feature_introduction field value changes and several existing test assertions must be updated in the same commit as the token.mbt change (they are enumerated in Task 1). Fixture SQL is written from the verbatim grammars/examples in 02-RESEARCH.md; docs reachability and revision provenance follow the unavailable-offline policy (D-17, A8).
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Extend profile metadata to DML/DDL and land the first manifest-driven fixture family</name>
  <files>token/token.mbt, corpus/manifest.tsv, test/parser_test.mbt, test/corpus_test.mbt, corpus/doris-2.1/dml-insert-values.sql, corpus/doris-3.x/dml-insert-values.sql, corpus/doris-4.x/dml-insert-values.sql</files>
  <read_first>
    @.planning/phases/02-doris-completeness-and-corpus/02-RESEARCH.md
    @.planning/phases/02-doris-completeness-and-corpus/02-PATTERNS.md
    @.planning/phases/02-doris-completeness-and-corpus/02-CONTEXT.md
    @.planning/phases/01-core-kernel/01-04-SUMMARY.md
    token/token.mbt
  </read_first>
  <action>Extend the profile-metadata contract: change the three canonical ProfileMetadata feature_introduction values in DorisProfile::metadata to the DML/DDL-inclusive strings given in the plan context (V2_1, V3_X, V4_X), and update the allowlists in ProfileMetadata::for_manifest and DorisProfile::validate_metadata to accept exactly those three new strings. Do not change id/release_family/exact_release/introduced_features. Keep ValidatedProfileContext::supports logic unchanged (canonical-equality now holds for migrated rows). Atomically update every consumer of the old strings in the same change: (1) corpus/manifest.tsv — every Phase 1 row's feature_introduction becomes its profile's new canonical string (2.1 rows → V2_1 string, 3.x rows → V3_X string, 4.x rows → V4_X string); leave the intentional negative row 3.x-unsupported-profile (feature_introduction "3.x QUALIFY", still rejected as UnsupportedFeatureIntroduction); (2) test/parser_test.mbt — the five EmbeddedManifestFixture entries get the new canonical strings per profile; the metadata_mismatch test updates its third case to expect ProfileMetadataMismatch with the NEW V3_X canonical string supplied under profile 2.1 (the old V3 string is no longer allowlisted and would now yield UnsupportedFeatureIntroduction, so the test must use the new string to keep exercising the mismatch branch); the profile_metadata assertion expects the new V3_X canonical string. Create the first DML fixture family end-to-end: corpus/doris-{2.1,3.x,4.x}/dml-insert-values.sql files (released-doc INSERT examples: multi-row VALUES with DEFAULT cells, no mysql> prompts, provenance comment header with official URL and retrieval date), manifest rows for the three new fixtures (category dml-insert, support_status supported, parse_mode strict, classification parse-only, provenance unavailable-offline + known-gap note), and a new test/corpus_test.mbt embedding an oracle entry per new row (reuse the EmbeddedManifestFixture shape with a local copy or metadata_fixture_replay_ok-style logic) asserting parse_with_metadata → print_result == raw, all_spans_in_bounds, expected_valid true, DORIS-PARSE- prefix on any diagnostic. Add the dml-insert coverage rows to corpus/coverage.tsv (profile × category counts). Verify the full suite is green after the migration.</action>
  <acceptance_criteria>
    - ProfileMetadata canonical strings include DML/DDL; for_manifest and validate_metadata accept exactly the three new canonical strings and reject everything else (CORP-01 foundation, research Pattern 3).
    - All Phase 1 manifest rows and all embedded fixture/assertion strings are migrated atomically; moon test passes with the new strings (the old strings appear nowhere in code/tests/manifest).
    - corpus/doris-{2.1,3.x,4.x}/dml-insert-values.sql exist with provenance headers; manifest rows and oracle entries for all three parse and replay byte-exact (CORP-01, CORP-02).
    - coverage.tsv gains dml-insert rows consistent with the manifest (fixture-count invariant).
  </acceptance_criteria>
  <verify><automated>moon test</automated></verify>
  <done>The metadata contract accepts DML/DDL canonical strings, all Phase 1 strings are migrated, and the first fixture family is executable end-to-end (manifest → parse → replay → coverage).</done>
</task>

<task type="auto">
  <name>Task 2: Expand fixtures to all DML/DDL families with layered goldens and version negatives</name>
  <files>corpus/doris-2.1/dml-insert-select.sql, corpus/doris-2.1/dml-update.sql, corpus/doris-2.1/dml-delete.sql, corpus/doris-2.1/ddl-create-table.sql, corpus/doris-2.1/ddl-create-view.sql, corpus/doris-2.1/ddl-create-index.sql, corpus/doris-3.x/dml-insert-select.sql, corpus/doris-3.x/dml-update.sql, corpus/doris-3.x/dml-delete.sql, corpus/doris-3.x/ddl-create-table.sql, corpus/doris-3.x/ddl-create-view.sql, corpus/doris-3.x/ddl-create-index.sql, corpus/doris-4.x/dml-insert-overwrite.sql, corpus/doris-4.x/dml-update.sql, corpus/doris-4.x/dml-delete.sql, corpus/doris-4.x/dml-merge.sql, corpus/doris-4.x/ddl-create-table.sql, corpus/doris-4.x/ddl-create-table-ctas.sql, corpus/doris-4.x/ddl-create-table-like.sql, corpus/doris-4.x/ddl-create-view.sql, corpus/doris-4.x/ddl-create-index.sql, corpus/doris-4.x/ddl-create-materialized-view.sql, corpus/doris-4.x/script-multi-statement.sql, corpus/doris-4.x/malformed-recovery.sql, corpus/manifest.tsv, corpus/coverage.tsv, test/corpus_test.mbt</files>
  <read_first>
    @.planning/phases/02-doris-completeness-and-corpus/02-RESEARCH.md
    @.planning/phases/02-doris-completeness-and-corpus/02-PATTERNS.md
    @.planning/phases/02-doris-completeness-and-corpus/02-01-PLAN.md
    @.planning/phases/02-doris-completeness-and-corpus/02-02-PLAN.md
  </read_first>
  <action>Write per-version fixture SQL files for every statement family implemented in 02-01/02-02, using the verbatim released-doc grammars and examples in 02-RESEARCH.md as the source (official_url and retrieval_date recorded in each file header comment; no mysql> prompts, output blocks, or placeholder variables — research Pitfall 6). Family files: 2.1 and 3.x get dml-insert-select, dml-update, dml-delete, ddl-create-table (clauses documented in those releases only — no ORDER BY, no BUCKETS AUTO, no AUTO PARTITION BY unless the 2.1/3.x pages confirm them, see flagged A3), ddl-create-view, ddl-create-index; 4.x additionally gets dml-insert-overwrite (incl. PARTITION (*) and the legacy literal table form), dml-merge (MERGE INTO per the 4.x-only grammar), ddl-create-table-ctas, ddl-create-table-like, ddl-create-materialized-view (sync form), script-multi-statement (a DML + invalid statement + SELECT script per DORIS-03), and malformed-recovery (expected-error rows, editor mode). Register every fixture in corpus/manifest.tsv with full provenance and the profile's canonical feature_introduction string; add a 4.x-negative MERGE row under profile 2.1 (expected-error, strict, classification expected-error, mirroring the existing 3.x-unsupported-profile precedent) proving version-negative coverage. Extend test/corpus_test.mbt with an oracle entry per new row (raw text embedded from the fixture files), asserting expected_valid per support_status, print_result == raw, all_spans_in_bounds, DORIS-PARSE- prefix, and statement_count for script-multi-statement. Update coverage.tsv rows (one per profile × category, plus the all/known-gaps row) so every manifest fixture appears in exactly one coverage row. If the 2.1/3.x CREATE TABLE pages are reachable, verify flagged A3 gates (BUCKETS AUTO / AUTO PARTITION BY) and amend token.mbt DorisFeature rows or fixtures accordingly; if unreachable, add a known-gap row noting the assumption. Close the deferred A2 decision here (no further deferral): re-read the async CREATE MATERIALIZED VIEW docs page (reachable at research time); if the async form (BUILD/REFRESH clauses) is documented in released docs, add a ddl-create-materialized-view-async fixture family with gates and register its manifest/oracle/coverage rows; if the page is unreachable or the async form remains undocumented, record a permanent known-gap row (async CREATE MATERIALIZED VIEW — DORIS-PARSE-007 unsupported) in manifest.tsv/coverage.tsv so it surfaces in the CORPUS-REPORT.md known-gaps section (Task 3).</action>
  <acceptance_criteria>
    - Every DML/DDL family has versioned fixture files and manifest rows with full provenance; no fixture relies on current/dev docs or contains shell/output contamination (CORP-01, D-17, Pitfall 6).
    - Every row has an executable oracle entry: strict/editor per parse_mode, expected_valid per support_status, byte-exact replay, span bounds, diagnostic prefix (CORP-02, D-18).
    - Version negatives exist: MERGE under 2.1 is an expected-error row (4.x-gate honored); ORDER BY / BUCKETS AUTO / AUTO PARTITION BY clauses absent from 2.1/3.x fixture files unless confirmed (A3).
    - script-multi-statement fixture asserts later statements survive with distinct statement ids (DORIS-03 shape); coverage rows satisfy the one-fixture-one-row invariant including the all/known-gaps row.
    - The deferred A2 decision is closed in this task: async CREATE MATERIALIZED VIEW is either fixture-covered with gates or recorded as a permanent known-gap row feeding the CORPUS-REPORT.md known-gaps section; no follow-up deferral remains.
  </acceptance_criteria>
  <verify><automated>moon test</automated></verify>
  <done>The full DML/DDL fixture corpus is registered, executable, and version-gated; coverage.tsv accounts for every row with known gaps recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Coverage report generator and CORPUS-REPORT.md</name>
  <files>corpus/tools/generate_corpus_report.py, corpus/coverage.tsv, corpus/CORPUS-REPORT.md</files>
  <read_first>
    @.planning/phases/02-doris-completeness-and-corpus/02-RESEARCH.md
    @.planning/phases/02-doris-completeness-and-corpus/02-PATTERNS.md
    @.planning/phases/02-doris-completeness-and-corpus/02-CONTEXT.md
  </read_first>
  <action>Create corpus/tools/generate_corpus_report.py (Python stdlib csv/pathlib only, deterministic, offline) that reads corpus/manifest.tsv and corpus/coverage.tsv and renders corpus/CORPUS-REPORT.md with: a version × category matrix (fixture count, supported count, expected-error count, known-gap column), a failure list (expected-error rows and any supported row whose oracle failed), and a known-gaps section (rows carrying known-gap provenance plus the flagged A2/A3/A5 items). The script must include a --check mode that exits non-zero when CORPUS-REPORT.md is stale relative to manifest/coverage (content hash comparison) or when the one-fixture-one-coverage-row invariant is violated or when any "full compatibility"/"100%" claim string appears in the report — the report must never contain an unqualified compatibility claim (D-19, research Pattern 5, Pitfall 7). The script must also embed the keywords.tsv inventory summary (word counts by classification and introduced_profile) so the D-16 deliverable is visible in the published report. Run the generator, commit the generated CORPUS-REPORT.md, and extend coverage.tsv if the generator surfaces an inconsistency (e.g., a missing row). Verify the script's --check mode passes after generation and fails on a deliberately stale copy (temporarily, then restore).</action>
  <acceptance_criteria>
    - generate_corpus_report.py renders the matrix, failure list, and known-gaps deterministically and offline; --check passes on the committed report and fails on stale/inconsistent state (CORP-03, D-19).
    - CORPUS-REPORT.md contains no unqualified compatibility claim; known gaps are explicit rows/sections (CORP-03).
    - keywords.tsv classification summary appears in the report (D-16 visibility); coverage.tsv satisfies the one-fixture-one-row invariant.
  </acceptance_criteria>
  <verify><automated>python3 corpus/tools/generate_corpus_report.py --check && moon test</automated></verify>
  <done>CORPUS-REPORT.md is generated, checked in, honest about gaps, and machine-verified against manifest/coverage drift.</done>
</task>

</tasks>

## Source Audit Coverage

| SOURCE | ID | Feature or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Reproducible coverage and failure reporting | 02-04 | COVERED | Manifest + goldens + CORPUS-REPORT.md. |
| REQ | CORP-01 | Reproducible manifest with release family, URL, revision, category, support status | 02-04 | COVERED | Tasks 1-2 rows + provenance policy. |
| REQ | CORP-02 | Golden coverage: strict, replay, malformed/recovery, no current/dev syntax | 02-04 | COVERED | Oracle entries per row; version negatives. |
| REQ | CORP-03 | Coverage/failure reports by version and category incl. known gaps | 02-04 | COVERED | Task 3 generator + report. |
| RESEARCH | Pattern 3 | Extend ProfileMetadata for_manifest/validate_metadata for DML/DDL | 02-04 | COVERED | Task 1 canonical-string extension + migration. |
| RESEARCH | Pattern 4 | Manifest row shape, category values, provenance discipline | 02-04 | COVERED | Tasks 1-2. |
| RESEARCH | Pattern 5 | CORPUS-REPORT.md generator + --check stale mode | 02-04 | COVERED | Task 3. |
| RESEARCH | Pitfall 6 | Corpus pollution (prompts, output blocks, placeholders) | 02-04 | COVERED | Prohibited + Task 2 fixture discipline. |
| RESEARCH | Open Q2 / A3 | Verify 2.1/3.x CREATE TABLE gates during corpus wave | 02-04 | COVERED | Task 2 verification step + known-gap fallback. |
| CONTEXT | D-17 | Offline hand-collected corpus, unavailable-offline provenance | 02-04 | COVERED | Tasks 1-2. |
| CONTEXT | D-18 | manifest.tsv + version dirs + goldens; recovery per Phase 1 | 02-04 | COVERED | Tasks 1-2. |
| CONTEXT | D-19 | coverage.tsv + CORPUS-REPORT.md matrix, failures, known gaps | 02-04 | COVERED | Task 3. |
| CONTEXT | D-07 | Released-docs authority, no current/dev acceptance | 02-04 | COVERED | Prohibited + fixture sources. |
| CONTEXT | D-16 | keywords.tsv in corpus report deliverables | 02-04 | COVERED | Task 3 summary + 02-03 deliverable. |

<verification>
Run `moon test` after Tasks 1-2 and `python3 corpus/tools/generate_corpus_report.py --check` after Task 3. Inspect: new canonical strings in token.mbt and their exact-match enforcement, migrated manifest rows, fixture provenance headers, oracle replay coverage for every row, version-negative rows (MERGE under 2.1), the script-multi-statement statement-count assertion, coverage rows satisfying the one-fixture-one-row invariant, the generated report's matrix/failures/known-gaps, and the --check failure on a deliberately stale report. Execution guidance for the fixture-heavy surface: the 36 modified files are mostly small mechanical fixture SQL files — author them mechanically per statement family and commit each family atomically (dml-insert-values, dml-insert-select, dml-update, dml-delete, ddl-create-table, ddl-create-view, ddl-create-index, then the 4.x-only families, then script/malformed), with that family's manifest rows, oracle entries, and coverage rows in the same commit; a mid-plan failure then localizes to one family and does not block the rest of the wave. Do not install packages (stdlib only) or invoke FE/database/network services.
</verification>

<success_criteria>
- CORP-01..CORP-03 are executable: manifest, goldens, and report are reproducible, honest, and machine-verified.
- The profile-metadata contract accepts DML/DDL canonical strings with all Phase 1 consumers migrated atomically.
- No fabricated revisions, no full-compatibility claim, no current/dev acceptance; known gaps are explicit.
</success_criteria>

<output>Create `.planning/phases/02-doris-completeness-and-corpus/02-04-SUMMARY.md` when this plan executes.</output>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Docs corpus → fixture SQL | Shell prompts, output blocks, and placeholder variables from docs pages must not enter fixtures. |
| Manifest/coverage/report → published claims | Stale or inconsistent rows would publish dishonest coverage; fabricated revisions would break provenance trust. |
| Feature-introduction metadata → acceptance | Wrong canonical strings or allowlist gaps would reject valid fixtures or admit invalid rows. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-31 | Tampering | Fabricated revisions / provenance | high | mitigate | unavailable-offline + known-gap policy only (D-17); provenance fields audited per row; no SHA synthesis (research anti-pattern). |
| T-02-32 | Tampering | Manifest↔coverage↔report drift | high | mitigate | Generator --check mode fails on stale report and one-fixture-one-row violations; committed report is machine-verified (Pattern 5). |
| T-02-33 | Integrity | Unqualified full-compatibility claims | medium | mitigate | Report structure enforced by the generator; no "full compatibility"/"100%" strings allowed; known-gaps section mandatory (D-19). |
| T-02-34 | Integrity | Corpus pollution from docs pages | medium | mitigate | Fixture extraction discipline (standalone sql code-fence statements only); classification parse-only vs requires-session vs not-sql (Pitfall 6). |
| T-02-35 | Tampering | Metadata allowlist bypass | medium | mitigate | for_manifest/validate_metadata exact-match to the three canonical strings; mismatch/unsupported tests cover negative paths. |
| T-02-SC | Tampering | Package installation | low | accept | Report generator is Python stdlib only; no pip installs in this plan (SQLGlot install is 02-06's dev-only step, audited). |

## Artifacts this phase produces

Contract: canonical ProfileMetadata feature_introduction strings V2_1 "2.1 baseline SELECT; DML/DDL released", V3_X "2.1 baseline SELECT; DML/DDL released; 3.x window and QUALIFY", V4_X "2.1 baseline SELECT; DML/DDL released; 4.x released SELECT"; updated allowlists in ProfileMetadata::for_manifest and DorisProfile::validate_metadata (token/token.mbt).
Files: corpus/doris-{2.1,3.x,4.x}/dml-insert-values.sql (3), dml-insert-select.sql (3), dml-update.sql (3), dml-delete.sql (3), ddl-create-table.sql (3), ddl-create-view.sql (3), ddl-create-index.sql (3), 4.x-only dml-insert-overwrite.sql, dml-merge.sql, ddl-create-table-ctas.sql, ddl-create-table-like.sql, ddl-create-materialized-view.sql, script-multi-statement.sql, malformed-recovery.sql; corpus/tools/generate_corpus_report.py (new); corpus/CORPUS-REPORT.md (generated); corpus/manifest.tsv and corpus/coverage.tsv (extended); test/corpus_test.mbt (new); test/parser_test.mbt (migrated strings).
</threat_model>

