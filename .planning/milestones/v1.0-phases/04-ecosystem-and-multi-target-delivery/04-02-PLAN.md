---
phase: 04-ecosystem-and-multi-target-delivery
plan: 02
type: execute
wave: 2
depends_on: [04-00, 04-01]
files_modified: ["completion/completion.mbt", "completion/moon.pkg", "lsp/handlers.mbt", "lsp/protocol.mbt", "lsp/documents.mbt", "lsp/completion_test.mbt", "lsp/protocol_test.mbt"]
autonomous: true
requirements: [ECO-01, ECO-03]
estimate:
  tokens: 26000
  raw_tokens: 13000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Incomplete SQL receives bounded syntax-aware keyword/clause completion from parser-owned profile/token context, without catalog semantics. D-09"
    - "Completion and protocol requests use the current document version and malformed requests produce bounded JSON-RPC errors rather than process crashes. D-08"
  artifacts: ["completion/completion.mbt", "lsp/completion_test.mbt", "lsp/protocol_test.mbt"]
  key_links: ["editor snapshot/cursor -> completion facade -> LSP CompletionList", "token classification/profile -> candidate filtering"]
<objective>Expand the proven LSP slice with syntax-only completion and protocol/version hardening for incomplete editor documents.</objective>
<execution_context>
@.planning/phases/04-ecosystem-and-multi-target-delivery/04-CONTEXT.md
@.planning/phases/04-ecosystem-and-multi-target-delivery/04-RESEARCH.md
@.planning/phases/04-ecosystem-and-multi-target-delivery/04-01-PLAN.md
</execution_context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Add parser-owned syntax completion facade</name>
<files>completion/completion.mbt, completion/moon.pkg, lsp/completion_test.mbt</files>
<behavior>
- Statement start and incomplete/error regions return bounded profile-valid Doris keyword candidates.
- After SELECT, FROM, WHERE, GROUP BY, ORDER BY, and JOIN ... ON, candidates are clause/context appropriate.
- Catalog/table/column metadata is never consulted and multibyte cursor ranges map through the shared coordinate adapter.
</behavior>
<action>Create a backend-neutral completion package that parses editor-mode snapshots, identifies the cursor token/statement context, filters candidates from token classification and parser-known clauses, and returns primitive labels/ranges/text edits. Keep candidate count bounded and preserve explicit profile selection; do not add semantic completion or a second keyword authority (D-09).</action>
<verify><automated>moon test --target native --filter completion</automated></verify>
<done>Completion tests cover all selected contexts, incomplete SQL, profile filtering, stable ordering, bounded output, and UTF-16 replacement ranges.</done>
</task>
<task type="auto">
<name>Task 2: Harden LSP dispatch, versions, and completion mapping</name>
<files>lsp/handlers.mbt, lsp/protocol.mbt, lsp/documents.mbt, lsp/protocol_test.mbt</files>
<action>Dispatch completion requests only against synchronized current snapshots, reject stale/out-of-order changes and unsupported methods with protocol-safe errors, validate request IDs/params/profile/cursor bounds, and serialize completion items through the frozen schema/protocol types. Ensure shutdown/exit behavior and malformed JSON remain bounded and diagnostics never originate from an older version (D-08/D-13).</action>
<verify><automated>moon test --target native --filter lsp</automated></verify>
<done>Protocol tests prove stale-version rejection, invalid params, unknown methods, current-snapshot completion, clean shutdown, and no crash on malformed messages.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| client request -> completion dispatcher | untrusted cursor, URI, version, and params |
| parser token data -> suggestions | profile-controlled candidate source |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-05 | Elevation of privilege | completion facade | low | accept | syntax-only data exposes no catalog or execution capability |
| T-04-06 | Denial of service | completion parser path | medium | mitigate | input/candidate bounds and current-version checks |
| T-04-07 | Tampering | LSP request dispatch | high | mitigate | strict params, request IDs, method validation, protocol-safe errors |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | no package installs in this plan |
</threat_model>
<verification>Run completion and LSP tests plus a Native framed smoke request for each minimum completion context and an incomplete document.</verification>
<success_criteria>Native LSP provides stable syntax completion and rejects unsafe/stale protocol input without semantic scope creep.</success_criteria>
<output>Create `.planning/phases/04-ecosystem-and-multi-target-delivery/04-02-SUMMARY.md` when done.</output>
