---
phase: 13-toolchain-and-editor-packaging
plan: 07
type: execute
wave: 4
depends_on: [13-06]
files_modified:
  - vscode/scripts/host-verify.mjs
  - vscode/src/host-test.ts
  - jetbrains/src/main/kotlin/fathom/jetbrains/sql/FathomLanguageServerFactory.kt
  - web/scripts/offline-smoke.mjs
  - .github/workflows/ci.yml
  - docs/CONFIGURATION.md
requirements: [TOOL-05]
autonomous: true
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A maintainer can run the three-host final packaging smoke entirely offline (no network, Doris FE, Flink cluster, or database) and each host passes: VS Code real extension-host verification (vscode/scripts/host-verify.mjs) with a flink mode, IntelliJ Gradle build + LSP launch smoke with flink configuration, and Web Chromium smoke with flink selection (TOOL-05, D-08, PARITY-03)."
    - "Each host's flink acceptance is: open a flink file -> select flink dialect/profile -> receive diagnostics (with format/completion verified at supported points) — the same per-file selection behavior across hosts (TOOL-05, D-08, D-05)."
    - "CI has a three-host packaging smoke job: .github/workflows/ci.yml gains a job that runs the web offline/Chromium smoke, the VS Code host-verify (Xvfb :99 per ECO-07), and the IntelliJ gradlew test/verifyPlugin/buildPlugin + LSP launch smoke — all offline, no --update anywhere, and the job fails closed if any host is skipped (D-08, Pitfall 5, Pitfall 8)."
    - "The naming gate stays green: the new flink host values, the CI job, and any doc changes pass scripts/check_naming.py — no product-level Doris remnant is reintroduced (NAME-04, D-08)."
    - "The frozen Doris baseline and cross-backend parity stay green after the packaging work: moon test --package parity (no --update), diff_parity.py --frozen-only, and compare_backends.py all pass — the packaging changes touch hosts/CI only, not the MoonBit core (PARITY-01/02/03, D-08)."
    - "[probe TOOL-05 cross-host parity] flagged-unverified: the three hosts produce the same flink per-file selection behavior (open flink file -> select flink -> diagnostics/format/completion) — the real-extension-host (VS Code), LSP launch (IntelliJ), and Chromium (Web) smoke each assert the flink flow, but the cross-host behavioral identity is asserted per-host, not as a single diff (documented boundary)."
  artifacts:
    - path: "vscode/scripts/host-verify.mjs + vscode/src/host-test.ts"
      provides: "a new flink mode in the ECO-07 real extension-host verification (FATHOM_DIALECT=flink, FATHOM_PROFILE=flink-2.3.0) asserting diagnostics + format/completion at supported points"
    - path: "jetbrains/src/main/kotlin/fathom/jetbrains/sql/FathomLanguageServerFactory.kt"
      provides: "flink configuration passed through initializationOptions (the factory already builds {dialect, profile} — verify a flink selection reaches the LSP) + a Gradle LSP launch smoke"
    - path: "web/scripts/offline-smoke.mjs"
      provides: "Web Chromium smoke with a flink selection (parse/format/complete through the adapter)"
    - path: ".github/workflows/ci.yml"
      provides: "three-host packaging smoke job (web + VS Code + IntelliJ) — offline, no --update, fail-closed on skip"
    - path: "docs/CONFIGURATION.md"
      provides: "documented flink dialect/profile selection per host (VS Code fathom.dialect, IntelliJ FathomSettings, Web demo) per D-05"
  key_links:
    - from: "vscode/scripts/host-verify.mjs flink mode"
      to: "vscode/src/host-test.ts extension-host assertions"
      via: "the ECO-07 mode table gains a flink row (FATHOM_DIALECT='flink', FATHOM_PROFILE='flink-2.3.0'); the extension host opens a flink file, selects flink, and asserts diagnostics + format/completion (D-08, 13-06 output)"
    - from: "jetbrains FathomLanguageServerFactory"
      to: "the LSP initializationOptions {dialect, profile}"
      via: "a flink selection reaches the server (FathomSettings -> initializationOptions -> serve_stdio); the Gradle LSP launch smoke starts fathom-lsp with flink and asserts the initialize handshake (D-08)"
    - from: ".github/workflows/ci.yml packaging job"
      to: "the three host harnesses"
      via: "web offline-smoke/Chromium, VS Code host-verify (Xvfb :99), IntelliJ gradlew + LSP launch — all offline, fail-closed on any skip (D-08, Pitfall 8)"
  prohibitions:
    - "The packaging smoke MUST NOT access the network, Doris FE, a Flink cluster, or a database — fully offline (PARITY-03, D-08, A5); the only network step in CI remains the MoonBit installer bootstrap."
    - "NO --update in any CI run line — the packaging job is a smoke/verification job, never a snapshot writer (Pitfall 1, D-08)."
    - "The packaging job MUST fail closed when a host is skipped or fails — a silent skip (e.g. VS Code unavailable) must red the job, not print ok (Pitfall 8, ECO-07 precedent)."
    - "The three-host smoke MUST NOT assert the old flat profile list — the harnesses assert the per-dialect pairs from 13-06 (Pitfall 5)."
    - "No host maintains a second parser — the packaging smoke verifies hosts call the shared wire/LSP surface only (TOOL-05, D-04)."
---

<objective>
把 Phase 13 的三宿主能力收口为最终打包 smoke 与 CI 门禁（TOOL-05 / D-08）：复用既有 harness——VS Code 真 extension-host 验证（vscode/scripts/host-verify.mjs，ECO-07 模式）、IntelliJ Gradle 构建 + LSP 启动 smoke（gradlew + 配置 flink 后启动 fathom-lsp）、Web Chromium smoke（monaco-adapter 单测 + 浏览器断言）——每个宿主验收：打开 flink 文件 → 选择 flink dialect/profile → 收到诊断（支持处验证 format/completion）。CI 增加三宿主最终打包 smoke job；全程离线（无网络/FE/cluster/DB，PARITY-03 纪律）。本 plan 是 Phase 13 的 wave-4 打包切片：一条端到端路径 = 「CI 打包 job → 三宿主 smoke → 每个宿主 flink 选择 → 诊断/format/completion」；先以 VS Code 真 extension-host flink 模式证明，再扩展到 IntelliJ 与 Web Chromium，最后接线进 ci.yml。

Purpose: TOOL-05 要求四宿主用同一套 dialect-aware API/schema/LSP，按文件/会话选择 Doris 或 Flink；D-08（reversible）锁定复用既有 harness 做最终打包 smoke 并加 CI job。Research（No Analog Found）已实证：ci.yml 尚无同时链 web + VS Code + IntelliJ smoke 的 job——本 plan 从三个既有 harness 入口组合。打包 smoke 全程离线，任何宿主 skip 即失败（Pitfall 8）。命名门禁持续适用（NAME-04）。

Output: vscode/scripts/host-verify.mjs + host-test.ts（flink 模式）、jetbrains FathomLanguageServerFactory.kt（flink 配置验证）+ Gradle LSP launch smoke、web/scripts/offline-smoke.mjs（Chromium flink 断言）、.github/workflows/ci.yml（三宿主打包 job）、docs/CONFIGURATION.md（每宿主 flink 选择文档）。
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-toolchain-and-editor-packaging/13-CONTEXT.md
@.planning/phases/13-toolchain-and-editor-packaging/13-RESEARCH.md
@.planning/phases/13-toolchain-and-editor-packaging/13-PATTERNS.md
@/opt/source/Fathom/vscode/scripts/host-verify.mjs
@/opt/source/Fathom/vscode/src/host-test.ts
@/opt/source/Fathom/jetbrains/src/main/kotlin/fathom/jetbrains/sql/FathomLanguageServerFactory.kt
@/opt/source/Fathom/web/scripts/offline-smoke.mjs
@/opt/source/Fathom/.github/workflows/ci.yml
@/opt/source/Fathom/docs/CONFIGURATION.md

This is the phase wave-4 packaging plan, depending on 13-06 (hosts — the (dialect, profile) pairs + flink values are in place). D-08 (reversible) drives the smoke depth + CI job. RESEARCH Environment: VS Code + @vscode/test-electron verified (STATE.md ECO-07), Xvfb :99 for headless extension-host, JDK 21 + Gradle wrapper for IntelliJ, Node for web/Chromium. The ci.yml analog is the linear-wasm-parity/parity-gate jobs (install MoonBit -> moon build/test -> python gate scripts); the packaging job composes the three host harnesses instead. host-verify.mjs currently runs three modes (functional doris 4.x, profile doris 2.1, fallback) — add a flink mode (FATHOM_DIALECT='flink', FATHOM_PROFILE='flink-2.3.0'). FathomLanguageServerFactory.getInitializationOptions builds {dialect, profile} (FathomLanguageServerFactory.kt:22-43) — verify a flink selection reaches the server. The CI job must be fully offline (only the MoonBit installer curl remains), no --update anywhere, and fail closed if any host is skipped (Pitfall 8). docs/CONFIGURATION.md documents the per-host flink selection.

> **Contribution hooks:** (1) API Coverage checkpoint — the packaging smoke verifies every host capability is actually reachable: Web (parse/format/complete via fathom_*_v1), VS Code (LSP initialize/formatting/completion with per-file dialect), IntelliJ (LSP launch with flink configuration); the full wire surface from 13-04 (five exports) and the LSP methods from 13-05 are exercised end-to-end. (2) Assumption-Delta — the toolchain is now multi-dialect; the packaging smoke confirms each host selects Doris or Flink per file/session and the shared API/schema is used. (3) Schema Push gate — no ORM/database schema files; skipped silently.
</context>

<tasks>

<task type="tracer">
  <name>VS Code real extension-host flink mode end-to-end (ECO-07 pattern)</name>
  <files>vscode/scripts/host-verify.mjs, vscode/src/host-test.ts</files>
  <read_first>
    @.planning/phases/13-toolchain-and-editor-packaging/13-CONTEXT.md
    @.planning/phases/13-toolchain-and-editor-packaging/13-RESEARCH.md
    @.planning/phases/13-toolchain-and-editor-packaging/13-PATTERNS.md
    @/opt/source/Fathom/vscode/scripts/host-verify.mjs
    @/opt/source/Fathom/vscode/src/host-test.ts
  </read_first>
  <action>Build the thinnest end-to-end VS Code flink smoke. (1) vscode/scripts/host-verify.mjs: add a flink mode to the modes array (host-verify.mjs:27-40) — `{ mode: 'flink', env: { VSCODE_HOST_MODE: 'flink', FATHOM_LSP_PATH: lspPath, FATHOM_DIALECT: 'flink', FATHOM_PROFILE: 'flink-2.3.0' } }` — mirroring the existing functional/profile/fallback rows; the runMode loop stays unchanged (each mode launches a real extension host via @vscode/test-electron with Xvfb :99). (2) vscode/src/host-test.ts: add the flink-mode assertions — when VSCODE_HOST_MODE === 'flink', the extension host opens a .sql file, resolves the fathom.dialect/flink-2.3.0 selection, waits for the LSP to publish diagnostics, and asserts: (a) the document receives flink diagnostics (a valid flink SELECT yields zero diagnostics; a deliberately-invalid flink statement yields a FATHOM-PARSE-* diagnostic); (b) textDocument/formatting returns a real edit (or an empty array for an unchanged tree) — never a -32603; (c) textDocument/completion returns real items — never a -32602. Follow the existing functional-mode assertion structure (the ECO-07 host-test already drives the client for diagnostics/format/completion). (3) Run the smoke locally: `FATHOM_LSP_PATH=<path> node vscode/scripts/host-verify.mjs` with Xvfb :99 available — all four modes (functional/profile/fallback/flink) must pass; the flink mode proves the 13-05 LSP real paths through a real extension host.
  <acceptance_criteria>
    - host-verify.mjs has a flink mode (VSCODE_HOST_MODE='flink', FATHOM_DIALECT='flink', FATHOM_PROFILE='flink-2.3.0') in the modes array.
    - host-test.ts flink-mode assertions pass: flink diagnostics (valid -> zero, invalid -> FATHOM-PARSE-*), formatting returns a real edit/empty array (never -32603), completion returns real items (never -32602).
    - `node vscode/scripts/host-verify.mjs` (with Xvfb :99 and FATHOM_LSP_PATH) passes all four modes — the real extension host exercises the flink LSP surface end-to-end.
    - `cd vscode && npm run build` (tsc, compiles host-test.ts) passes.
    - No core change: `moon test --package parity` (no --update) still passes (packaging-only change).
  </acceptance_criteria>
  <verify>
    <automated>cd vscode && npm run build && node scripts/host-verify.mjs && moon test --package parity</automated>
  </verify>
  <done>The VS Code real extension host verifies the flink flow (diagnostics + format + completion) end-to-end through the actual LSP, and the Doris parity baseline stays green.</done>
</task>

<task type="auto">
  <name>IntelliJ Gradle + LSP launch smoke with flink + Web Chromium flink smoke</name>
  <files>jetbrains/src/main/kotlin/fathom/jetbrains/sql/FathomLanguageServerFactory.kt, web/scripts/offline-smoke.mjs</files>
  <read_first>
    @.planning/phases/13-toolchain-and-editor-packaging/13-RESEARCH.md
    @/opt/source/Fathom/jetbrains/src/main/kotlin/fathom/jetbrains/sql/FathomLanguageServerFactory.kt
    @/opt/source/Fathom/web/scripts/offline-smoke.mjs
  </read_first>
  <action>Complete the IntelliJ and Web host smokes. (1) jetbrains/FathomLanguageServerFactory.kt: verify the getInitializationOptions (FathomLanguageServerFactory.kt:22-23) already builds `initializationOptions(configuration.dialect, configuration.profile)` — confirm a flink selection (dialect 'flink', profile 'flink-2.3.0') flows from FathomSettings through this factory to the LSP serve_stdio; add a small Gradle-level LSP launch smoke: a JUnit4 test (or a verifyPlugin-scoped check) that constructs the initializationOptions map with a flink pair and asserts the map carries {dialect: "flink", profile: "flink-2.3.0"} — proving the IntelliJ host sends the flink selection over the LSP wire (D-08). Do NOT add a network dependency — the smoke is in-process/offline. (2) web/scripts/offline-smoke.mjs: extend the Chromium-style smoke to assert the flink selection flow — after the existing adapter/parse assertions, drive a flink selection through the monaco-adapter (the adapter now validates PROFILES_BY_DIALECT.flink and calls fathom_parse_v1/fathom_format_v1/fathom_complete_v1 with 'flink'/'flink-2.3.0') and assert: the profile selector contains the flink values when flink is selected, a flink parse returns the fathom.parse.v1 envelope with dialect 'flink', and a flink completion call returns the fathom.complete.v1 envelope (the built binding.js must export fathom_complete_v1 from 13-04). Run the offline smoke: `cd web && npm run build` then `node scripts/offline-smoke.mjs` (offline — no network). (3) Run the IntelliJ Gradle build + verify: `cd jetbrains && ./gradlew test verifyPlugin buildPlugin` (offline from the Gradle cache).
  <acceptance_criteria>
    - FathomLanguageServerFactory passes a flink {dialect, profile} pair into initializationOptions; the new JUnit4/Gradle LSP-launch smoke asserts the map carries {dialect: "flink", profile: "flink-2.3.0"}.
    - web/scripts/offline-smoke.mjs asserts the flink selection flow: flink profile values in the selector when flink is selected, flink parse -> fathom.parse.v1 with dialect 'flink', flink completion -> fathom.complete.v1 (built binding.js exports fathom_complete_v1).
    - `cd web && npm run build && node scripts/offline-smoke.mjs` passes (offline, no network).
    - `cd jetbrains && ./gradlew test verifyPlugin buildPlugin` passes (offline from cache).
    - `python3 scripts/check_naming.py` passes (no product-level Doris remnant in the new host values/CI).
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm run build && node scripts/offline-smoke.mjs && cd ../jetbrains && ./gradlew test verifyPlugin buildPlugin && python3 scripts/check_naming.py</automated>
  </verify>
  <done>The IntelliJ host sends the flink selection over the LSP wire (verified by the Gradle LSP-launch smoke), the Web Chromium smoke drives the flink selection through parse/format/complete, and both pass offline with the naming gate green.</done>
</task>

<task type="auto">
  <name>CI three-host packaging smoke job + docs/CONFIGURATION.md flink selection</name>
  <files>.github/workflows/ci.yml, docs/CONFIGURATION.md</files>
  <read_first>
    @.planning/phases/13-toolchain-and-editor-packaging/13-RESEARCH.md
    @/opt/source/Fathom/.github/workflows/ci.yml
    @/opt/source/Fathom/docs/CONFIGURATION.md
  </read_first>
  <action>Wire the three-host packaging smoke into CI and document the flink selection. (1) .github/workflows/ci.yml: add a `host-packaging-smoke` job mirroring the existing job structure (checkout -> Set up Node -> Set up JDK 21 -> Set up Python -> Xvfb) that runs: (a) Web — `cd web && npm ci && npm run build && node scripts/offline-smoke.mjs` (offline; npm ci from the lockfile, no network beyond registry if cache-miss — follow the existing offline discipline, prefer the pinned npm cache); (b) VS Code — `cd vscode && npm ci && npm run build && xvfb-run -a node scripts/host-verify.mjs` with FATHOM_LSP_PATH pointing at the built fathom-lsp (build via `moon build --target native fathom-lsp` first, or use the committed _build path per the existing harness); (c) IntelliJ — `cd jetbrains && ./gradlew test verifyPlugin buildPlugin` (Gradle offline/cache). The job must fail closed: each step uses a non-`continue-on-error` shell step so a skipped/failed host reds the job (Pitfall 8); NO --update in any run line (Pitfall 1); the only network step remains the MoonBit installer curl (already present in the other jobs — the packaging job needs the MoonBit build for fathom-lsp if it isn't committed). (2) docs/CONFIGURATION.md: document the per-host flink selection — VS Code (fathom.dialect: "flink", fathom.profile: "flink-2.3.0"), IntelliJ (FathomSettings dialog: dialect flink, profile flink-2.3.0), Web demo (dialect selector flink + profile flink-2.3.0), CLI (`--dialect flink --profile flink-2.3.0`), and the per-file LSP override — with the (dialect, profile) pair validity table (doris 2.1/3.x/4.x; flink flink-2.3.0/2.1.3/1.20.5) per D-05. (3) Verify the full gate stack locally: `moon test --package parity` (no --update), `python3 scripts/diff_parity.py --frozen-only`, `python3 scripts/compare_backends.py`, `python3 scripts/check_naming.py` — all green after the CI/doc changes (the changes touch hosts/CI/docs only, not the MoonBit core).
  <acceptance_criteria>
    - ci.yml has a host-packaging-smoke job with web offline-smoke, VS Code host-verify (xvfb-run), and IntelliJ gradlew test/verifyPlugin/buildPlugin steps — each a fail-closed (non-continue-on-error) step; no --update in any run line; no network beyond the MoonBit installer bootstrap.
    - The job composes the three host harnesses from the existing entry points (D-08): the web Chromium smoke, the VS Code ECO-07 real extension-host, and the IntelliJ Gradle LSP launch smoke.
    - docs/CONFIGURATION.md documents the per-host flink selection (VS Code/IntelliJ/Web/CLI) with the (dialect, profile) pair validity table and the per-file LSP override.
    - Local gates pass after the change: `moon test --package parity` (no --update), `python3 scripts/diff_parity.py --frozen-only`, `python3 scripts/compare_backends.py`, `python3 scripts/check_naming.py` all exit 0.
    - `grep -nE 'run:.*--update' .github/workflows/ci.yml` returns nothing (no update-flag in any CI job).
  </acceptance_criteria>
  <verify>
    <automated>moon test --package parity && python3 scripts/diff_parity.py --frozen-only && python3 scripts/compare_backends.py && python3 scripts/check_naming.py && ! grep -E 'run:.*--update' .github/workflows/ci.yml && grep -q 'host-packaging-smoke' .github/workflows/ci.yml</automated>
  </verify>
  <done>The CI three-host packaging smoke job is wired (fail-closed, offline, no --update), docs/CONFIGURATION.md documents the per-host flink selection, and the full parity/naming gate stack stays green.</done>
</task>

</tasks>

## Source Audit Coverage

| SOURCE | ID | Feature or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | User can use the same dialect-aware API/schema/LSP through JS/linear-Wasm, Web/Monaco, VS Code, IntelliJ; each host selects Doris or Flink per file/session | 13-07 | COVERED (packaging/CI) | Three-host final packaging smoke + CI job verify the per-file/session flink selection end to end. |
| REQ | TOOL-05 | Same dialect-aware API/schema/LSP from JS/linear-Wasm, Web/Monaco, VS Code, IntelliJ; hosts select per file/session, no second parser | 13-07 | COVERED | Packaging smoke drives each host's flink flow through the shared wire/LSP surface (D-08). |
| CONTEXT | D-08 | Reuse existing harnesses — VS Code host-verify, IntelliJ Gradle + LSP launch, Web Chromium smoke; each host: open flink file -> select flink -> diagnostics (format/completion at supported points); CI final packaging job; fully offline (reversible) | 13-07 | COVERED | Tasks 1-3: flink host mode + IntelliJ/Web smokes + CI job + docs. |
| RESEARCH | No Analog Found | ci.yml has no job chaining web + VS Code + IntelliJ smokes | 13-07 | COVERED | Task 3 composes the three existing harness entry points into a host-packaging-smoke job. |
| RESEARCH | §7.2 / Pitfall 8 | fail-closed on skip; no silent empty pass | 13-07 | COVERED | Non-continue-on-error steps; a skipped host reds the job. |
| RESEARCH | Pitfall 1 / D-08 | no --update in CI | 13-07 | COVERED | grep gate + prohibition. |
| RESEARCH | Pitfall 5 | harness asserts the per-dialect pairs (13-06) not the old flat list | 13-07 | COVERED | Smokes use the PROFILES_BY_DIALECT flink values. |
| RESEARCH | A5 | no network/FE/cluster/DB in any smoke | 13-07 | COVERED | Offline discipline; only the MoonBit installer curl remains. |
| REQ | PARITY-03 | offline CI/release checks | 13-07 | COVERED | Three-host smokes fully offline. |
| REQ | NAME-04 | naming gate rejects product-level remnants | 13-07 | COVERED | check_naming.py green after host/CI/doc changes. |
| PROBE | TOOL-05 cross-host parity | per-host flink flow asserted, not a single cross-host diff | 13-07 | FLAGGED | must_haves flagged-unverified truth; documented boundary. |

<verification>
Run the three host smokes after each task: (1) `cd vscode && npm run build && node scripts/host-verify.mjs` (Xvfb :99, FATHOM_LSP_PATH) — all four modes incl. flink; (2) `cd jetbrains && ./gradlew test verifyPlugin buildPlugin` (offline) + the LSP-launch smoke; (3) `cd web && npm run build && node scripts/offline-smoke.mjs` (offline). After Task 3, run the full gate stack: moon test --package parity (no --update), python3 scripts/diff_parity.py --frozen-only, python3 scripts/compare_backends.py, python3 scripts/check_naming.py, and the ci.yml greps (host-packaging-smoke present, no --update). Inspect: host-verify flink mode row, host-test flink assertions, FathomLanguageServerFactory flink initializationOptions, offline-smoke flink assertions, ci.yml job structure, docs/CONFIGURATION.md per-host flink table.
</verification>

<success_criteria>
- A maintainer runs the three-host final packaging smoke offline and each host passes the flink flow (open flink file -> select flink -> diagnostics, format/completion at supported points).
- CI has a fail-closed three-host packaging smoke job (no --update, offline beyond the MoonBit installer bootstrap).
- The naming gate, Doris frozen baseline, and cross-backend parity stay green after the packaging work.
</success_criteria>

<output>Create `.planning/phases/13-toolchain-and-editor-packaging/13-07-SUMMARY.md` when this plan executes.</output>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI host-packaging-smoke job -> host harnesses | The job runs untrusted build/smoke commands; a silent skip or network creep must fail the job. |
| Host smoke -> real extension host / LSP | The VS Code real extension host drives the LSP; a flink document must receive real diagnostics/format/completion, never a Doris-policy leak or a rejection sentinel. |
| Web Chromium smoke -> built binding.js | The built artifact must export fathom_complete_v1 (13-04); a stale artifact silently misses the completion surface. |
| docs/CONFIGURATION.md -> per-host selection | Documented (dialect, profile) pairs must match the host constants (13-06) or the docs mislead users. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-07-01 | Spoofing | A host smoke silently skips (VS Code unavailable / network miss) and the job prints ok | high | mitigate | Non-continue-on-error fail-closed steps; a skipped host reds the job (Pitfall 8, ECO-07 precedent). |
| T-13-07-02 | Tampering | CI smoke network creep (npm registry pull / FE/cluster/DB access) | high | mitigate | Offline discipline; pinned npm cache; only the MoonBit installer curl remains (PARITY-03, A5). |
| T-13-07-03 | Tampering | --update flag added to a CI job, absorbing snapshot drift | high | mitigate | No --update in any run line (grep gate); the packaging job is a smoke, never a snapshot writer (Pitfall 1). |
| T-13-07-04 | Spoofing | A flink document over a real host receives a Doris-policy result or a -32603/-32602 sentinel | high | mitigate | host-test flink-mode asserts real diagnostics/format/completion (never the sentinels) — the 13-05 real paths through a real extension host (D-07, D-08). |
| T-13-07-05 | Tampering | Web smoke uses a stale binding.js lacking fathom_complete_v1 | medium | mitigate | offline-smoke asserts the built artifact exports fathom_complete_v1 (13-04 output); npm run build before the smoke (Pitfall 3). |
| T-13-07-06 | Tampering | docs/CONFIGURATION.md flink pairs drift from the host constants | medium | mitigate | Same-commit doc update with the (dialect, profile) validity table matching PROFILES_BY_DIALECT (13-06) (D-05). |
| T-13-07-07 | Tampering | Host smoke asserts the old flat profile list, masking a 13-06 drift | high | mitigate | Harnesses assert the per-dialect pairs from 13-06 (Pitfall 5). |
| T-13-07-08 | Spoofing | Naming gate misses a product-level Doris remnant in the new CI/doc content | medium | mitigate | check_naming.py runs in the gate stack after the changes (NAME-04). |
| T-13-07-SC | Tampering | npm/pip/cargo installs | low | accept | All host deps already pinned and approved (RESEARCH Package Legitimacy Audit); no new package introduced. |
</threat_model>

## Artifacts this plan produces

Files: vscode/scripts/host-verify.mjs (modified — flink mode row), vscode/src/host-test.ts (modified — flink-mode assertions), jetbrains/.../FathomLanguageServerFactory.kt (modified or verified — flink initializationOptions + LSP-launch smoke), web/scripts/offline-smoke.mjs (modified — flink selection flow assertions), .github/workflows/ci.yml (modified — host-packaging-smoke job), docs/CONFIGURATION.md (modified — per-host flink selection + (dialect, profile) validity table).

Symbols (vscode/scripts/host-verify.mjs): modes array gains `{ mode: 'flink', env: { VSCODE_HOST_MODE: 'flink', FATHOM_DIALECT: 'flink', FATHOM_PROFILE: 'flink-2.3.0' } }`.
Symbols (vscode/src/host-test.ts): flink-mode assertions — flink diagnostics (valid -> zero, invalid -> FATHOM-PARSE-*), formatting real edit/empty (never -32603), completion real items (never -32602).
Symbols (jetbrains/FathomLanguageServerFactory.kt): flink initializationOptions {dialect: "flink", profile: "flink-2.3.0"} + Gradle LSP-launch smoke assertion.
Symbols (web/scripts/offline-smoke.mjs): flink profile-value assertion, flink parse -> fathom.parse.v1 (dialect flink), flink completion -> fathom.complete.v1 (built binding.js exports fathom_complete_v1).
Symbols (.github/workflows/ci.yml): host-packaging-smoke job — web offline-smoke step, VS Code host-verify (xvfb-run) step, IntelliJ gradlew test/verifyPlugin/buildPlugin step (all fail-closed, offline, no --update).
Symbols (docs/CONFIGURATION.md): per-host flink selection (VS Code fathom.dialect/fathom.profile, IntelliJ FathomSettings, Web demo, CLI --dialect --profile) + (dialect, profile) pair validity table + per-file LSP override.

