---
phase: 13-toolchain-and-editor-packaging
plan: 05
type: execute
wave: 2
depends_on: [13-01, 13-02]
files_modified:
  - lsp/handlers.mbt
  - lsp/completion_test.mbt
  - lsp/selection_test.mbt
  - fathom-sql/cli_test.mbt
  - fathom-sql/run.mbt
  - lsp/coordinates.mbt
requirements: [TOOL-04]
autonomous: true
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A consumer can run `fathom-sql parse|format|lsp --dialect flink --profile flink-2.3.0` end to end: parse and format route through the real Flink path (D-07), the LSP serves flink documents over stdio, and the CLI exit codes follow D-39 (0 accepted / 1 refusal or parse failure / 2 usage) (TOOL-04, D-07, D-39)."
    - "LSP flink formatting is real: a textDocument/formatting request on a flink-selected document returns a real edit array (UTF-16 textEdit range + newText via @binding.span_to_range), and an unsafe/uncovered Flink tree publishes diagnostics + an empty edit array — the -32603 not-implemented sentinel is REMOVED (D-07, TOOL-04)."
    - "LSP flink completion is real: a textDocument/completion request on a flink-selected document returns a real CompletionItem array (label/kind/detail/textEdit with UTF-16 range + newText via completion_item_json), the -32602 rejection is removed once complete() returns Ok for flink (D-02), and the result is the standard LSP completion response (D-07, TOOL-04)."
    - "UTF-16 positions are correct: LSP completion/formatting ranges use @binding.span_to_range (UTF-16, CRLF-as-one-break, 4-byte->2-units, conservative invalid-UTF-8) — the same coordinate policy as Doris, never a second converter (D-07, RESEARCH Don't Hand-Roll)."
    - "Document-level dialect selection works for flink files: didOpen/didChange extension fields override the workspace default, and a flink-selected document's format/completion/parse all use the document's own dialect/profile — the D-01/D-02/D-03 selection precedence is unchanged (D-06, TOOL-04)."
    - "Doris LSP/CLI behavior is byte-identical (zero-drift): the flink paths are additive; existing lsp tests (selection_test, completion_test, diagnostics_formatting_test, protocol_test, lifecycle_test) and cli_test Doris cases pass unchanged (D-07, PARITY-01)."
  artifacts:
    - path: "lsp/handlers.mbt"
      provides: "formatting_result flink sentinel (-32603) removed -> real @api.format_with_ids path; completion_result flink rejection removed -> real @completion.complete results (the -32602 mapping stays only for genuine request errors, not flink policy)"
    - path: "lsp/completion_test.mbt"
      provides: "flink completion LSP tests: real textEdit range (UTF-16) + newText; refusal/empty-array vs real-result response shapes"
    - path: "lsp/selection_test.mbt"
      provides: "flink document-level selection tests: didOpen/didChange flink override, workspace-default flink, stale-version guard, format/completion on flink documents"
    - path: "fathom-sql/cli_test.mbt + fathom-sql/run.mbt"
      provides: "flink format exit-code matrix (0 accepted / 1 refusal with FATHOM-FORMAT-001 on stderr / 2 unknown profile); run_format already calls @api.format_with_ids (no guard) — tests only"
    - path: "lsp/coordinates.mbt"
      provides: "UTF-16 coordinate conversion reused (unchanged unless a flink-specific gap surfaces — do NOT fork)"
  key_links:
    - from: "lsp/handlers.mbt formatting_result"
      to: "@api.format_with_ids(document.text, document.dialect, document.profile, \"strict\", FormatOptions::default())"
      via: "the flink sentinel is deleted; the remaining body already calls format_with_ids and returns edits/diagnostics — refusal => diagnostics published + empty edit array (D-07, handlers.mbt:432-458)"
    - from: "lsp/handlers.mbt completion_result"
      to: "@completion.complete(document.text, document.dialect, document.profile, cursor_byte)"
      via: "once complete() returns Ok for flink (13-02), the existing completion_item_json emits the textEdit (UTF-16 range via diagnostic_range -> @binding.span_to_range) — the -32602 rejection for flink is removed (D-07, handlers.mbt:505-545)"
    - from: "lsp/completion_test.mbt + selection_test.mbt flink cases"
      to: "the three response shapes (error vs empty array vs real result)"
      via: "a host must distinguish 'nothing to format' from 'formatting not supported' from 'real edits' — the tests pin the exact LSP response JSON for each (D-07, Pitfall 4)"
    - from: "fathom-sql/cli_test.mbt flink matrix"
      to: "@api.format_with_ids + D-39 exit mapping"
      via: "run_format maps accepted -> 0, refusal -> 1 (FATHOM-FORMAT-001 on stderr), usage/unknown-profile -> 2 (D-07, D-39, run.mbt:28-92)"
  prohibitions:
    - "The LSP flink format/completion sentinels (-32603/-32602 for flink policy) MUST be removed — real @api.format_with_ids / @completion.complete results are the contract; a host must not receive a not-implemented/rejection error for a supported flink document (D-07, Pitfall 4)."
    - "A second UTF-16 coordinate converter MUST NOT be built — lsp/handlers.mbt reuses diagnostic_range -> @binding.span_to_range for both dialects (D-07, Don't Hand-Roll)."
    - "Doris LSP/CLI behavior MUST stay byte-identical — the flink paths are additive; existing lsp/cli tests pass unchanged (D-07, PARITY-01)."
    - "The CLI MUST NOT gain a flink guard or a new exit code — D-39 exit mapping (0/1/2) is unchanged; flink format already routes through @api.format_with_ids (D-07, D-39)."
    - "Document-level dialect selection MUST NOT be auto-detected or extension-guessed — flink values flow through the existing didOpen/didChange extension-field + workspace-default transport only (D-06, D-01)."
---

<objective>
把 Flink 贯通到 CLI 与 Native LSP（TOOL-04 / D-07）：LSP 的 flink format 从 -32603 not-implemented 换成真实 @api.format_with_ids 路径；flink completion 从 -32602 拒绝换成真实 @completion.complete 结果（CompletionItem → LSP textEdit UTF-16 range + newText，复用 completion_item_json 与 binding coordinates）；CLI `fathom-sql format --dialect flink` 走 @api.format_with_ids，退出码沿用 D-39（0 accepted / 1 refusal / 2 usage）；UTF-16 转换沿用 binding.coordinates。Doris 既有 LSP/CLI 行为零漂移。本 plan 是 Phase 13 的 wave-2 LSP/CLI 切片：一条端到端路径 = 「flink 文档 → LSP didOpen（dialect 覆盖）→ textDocument/formatting + textDocument/completion → @api.format_with_ids / @completion.complete → UTF-16 textEdit」；先以 LSP format + completion 两个真实路径证明，再扩展到 CLI 退出码矩阵与 selection/per-file 覆盖测试。

Purpose: TOOL-04 要求消费者能用中立 CLI 与 Native LSP 端到端使用 Flink（parse/format/lsp、诊断、格式化、补全、UTF-16、文档级方言选择）；D-07（costly — LSP 行为契约是宿主依赖面）锁定把 flink 从拒绝换成真实路径。Research（Pitfall 4）已实证：LSP 表面是宿主-facing 契约，错误 vs 空数组 vs 真实结果三种形状必须精确测试；selection_test/completion_test 现有 flink 拒绝断言必须随真实路径更新。本 plan 兑现 D-07 并保持 Doris 零漂移。D-06（选择传输契约 one-way）由 13-06 的 checkpoint 确认；本 plan 只让 flink 值通过既有 didOpen/didChange 传输并在 flink 文件上验证 per-file 覆盖。

Output: lsp/handlers.mbt（flink format/completion 真实路径）、lsp/completion_test.mbt + selection_test.mbt（flink LSP 行为测试）、fathom-sql/cli_test.mbt（flink 退出码矩阵）、fathom-sql/run.mbt（如测试暴露缺口则微调，否则仅测试）。
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-toolchain-and-editor-packaging/13-CONTEXT.md
@.planning/phases/13-toolchain-and-editor-packaging/13-RESEARCH.md
@.planning/phases/13-toolchain-and-editor-packaging/13-PATTERNS.md
@/opt/source/Fathom/lsp/handlers.mbt
@/opt/source/Fathom/lsp/completion_test.mbt
@/opt/source/Fathom/lsp/selection_test.mbt
@/opt/source/Fathom/lsp/coordinates.mbt
@/opt/source/Fathom/fathom-sql/run.mbt
@/opt/source/Fathom/fathom-sql/cli_test.mbt

This is the phase wave-2 LSP/CLI plan, depending on 13-01 (formatter gate — a flink format call now returns a real result or a FATHOM-FORMAT-001 refusal, never a Doris-layout single-line) and 13-02 (completion Flink branch — complete() returns Ok for flink). D-07 (costly — LSP behavior contract) drives the sentinel removal. The flink format sentinel is at handlers.mbt:429-431 (`if document.dialect == "flink" { return [ error_response(id, -32603, ...)] }`) — DELETE it; the remaining formatting_result body (436-458) already calls @api.format_with_ids and returns edits/diagnostics (refusal => diagnostics published + empty edit array). The completion_result (505-545) maps any Err to -32602; once complete() returns Ok for flink, the existing completion_item_json (495-503) emits the textEdit — the flink rejection is gone automatically (verify the -32602 path is only genuine request errors, e.g. stale version / bad position). D-06 (selection transport, one-way) is confirmed in 13-06's checkpoint; this plan only verifies flink values flow through the existing didOpen/didChange extension-field transport. CLI: run_format (run.mbt:28-92) already calls @api.format_with_ids with NO flink guard and maps D-39 exit codes — add cli_test.mbt matrix cases only (flink-2.3.0 accepted => 0; a refused Flink tree => 1 with FATHOM-FORMAT-001 on stderr; unknown flink profile => 2).

> **Contribution hooks:** (1) API Coverage checkpoint — the LSP surface for flink is: initialize (selection), textDocument/didOpen + didChange (document-level dialect), textDocument/formatting (real edits/refusal), textDocument/completion (real items); the 13-04 wire enumeration covers the JS/Wasm exports; this plan covers the LSP methods. (2) Assumption-Delta — the LSP was Doris-only in behavior (flink rejected); the plural transition makes flink format/completion real. (3) Schema Push gate — no ORM/database schema files; skipped silently.
</context>

<tasks>

<task type="tracer">
  <name>LSP flink format + completion real paths end-to-end (sentinel removal + response shapes)</name>
  <files>lsp/handlers.mbt, lsp/completion_test.mbt, lsp/selection_test.mbt</files>
  <read_first>
    @.planning/phases/13-toolchain-and-editor-packaging/13-CONTEXT.md
    @.planning/phases/13-toolchain-and-editor-packaging/13-RESEARCH.md
    @.planning/phases/13-toolchain-and-editor-packaging/13-PATTERNS.md
    @/opt/source/Fathom/lsp/handlers.mbt
    @/opt/source/Fathom/lsp/completion_test.mbt
  </read_first>
  <action>Build the thinnest end-to-end LSP flink slice. (1) In lsp/handlers.mbt formatting_result DELETE the flink sentinel block (handlers.mbt:429-431: `if document.dialect == "flink" { return [ error_response(id, -32603, "flink grammar is not yet implemented")] }`) — the remaining body (436-458) already calls @api.format_with_ids(document.text, document.dialect, document.profile, "strict", @api.FormatOptions::default()) and produces: diagnostics publish + empty edit array on refusal, empty array when output==text, or a real edit array (range via full_document_range -> @binding.span_to_range, newText via @utf8.decode_lossy). (2) In completion_result (handlers.mbt:505-545) verify the flink path: once @completion.complete returns Ok for flink, the existing completion_item_json (495-503) emits `{ "label", "kind": 14.0, "detail", "textEdit": { "range": range_json(diagnostic_range(raw, start_byte, end_byte)), "newText" } }` — the -32602 rejection for flink policy is gone; keep -32602 only for genuine request errors (missing params, stale version, bad position, source invalid). Remove any residual flink-specific rejection if the code still special-cases flink (grep handlers.mbt for `"flink"` special-casing on the format/completion paths). (3) Add lsp/completion_test.mbt flink cases: a flink-selected document (didOpen with dialect "flink", profile "flink-2.3.0") sends textDocument/completion at a statement-start position and receives a real result array containing items with textEdit.range (UTF-16) + newText — assert the response body contains "\"textEdit\"" and "\"items\"" and NOT "-32602". (4) Add lsp/selection_test.mbt flink cases: a flink document's textDocument/formatting returns either a real edit array (for a valid covered tree) or diagnostics + empty array (for an unsafe/uncovered tree) — assert the response contains "\"result\"" (real path) and does NOT contain "-32603" / "flink grammar is not yet implemented".
  <acceptance_criteria>
    - handlers.mbt contains NO flink format -32603 sentinel and NO flink completion -32602 policy rejection: `grep -n "flink grammar is not yet implemented" lsp/handlers.mbt` returns nothing; the formatting/completion paths treat flink like any dialect.
    - A flink-selected document's textDocument/formatting on a valid covered tree (e.g. SELECT 1) returns a real edit array: the response body contains "\"result\":" and an edit with "newText"; an unsafe/uncovered tree returns diagnostics published + an empty edit array.
    - A flink-selected document's textDocument/completion at statement-start returns a real result array: the body contains "\"items\":" and at least one item with "\"textEdit\""; the -32602 rejection is absent.
    - The three response shapes are distinguishable in the tests: error (genuine request error) vs empty array (nothing to format) vs real result (edits/items) — the flink cases pin the exact JSON shape (Pitfall 4).
    - Doris LSP behavior byte-identical: the existing lsp tests (completion_test Doris cases, selection_test Doris cases, diagnostics_formatting_test, protocol_test, lifecycle_test) pass unchanged (PARITY-01).
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native --package lsp && test -z "$(grep -n 'flink grammar is not yet implemented' lsp/handlers.mbt)"</automated>
  </verify>
  <done>The LSP flink format/completion sentinels are removed and replaced by real @api.format_with_ids / @completion.complete results with the three response shapes pinned in tests; Doris LSP behavior is byte-identical.</done>
</task>

<task type="auto">
  <name>Flink LSP completion/formatting behavior tests + UTF-16 textEdit + per-file override verification</name>
  <files>lsp/completion_test.mbt, lsp/selection_test.mbt, lsp/coordinates.mbt</files>
  <read_first>
    @.planning/phases/13-toolchain-and-editor-packaging/13-RESEARCH.md
    @/opt/source/Fathom/lsp/completion_test.mbt
    @/opt/source/Fathom/lsp/selection_test.mbt
    @/opt/source/Fathom/lsp/coordinates.mbt
  </read_first>
  <action>Complete the LSP flink behavior matrix. (1) In lsp/completion_test.mbt add UTF-16 textEdit assertions: a flink document containing a multibyte prefix (e.g. `SELECT 名称 FR` with the cursor after FR) returns a completion item whose textEdit.range is in UTF-16 (line/character, 4-byte char counts as 2 units) and whose newText replaces the FR prefix — assert the range via @binding.byte_to_position semantics (reuse the coordinates_test.mbt oracle shape). (2) In lsp/selection_test.mbt add the per-file override verification: a workspace default of doris-4.x with a flink-selected document (didOpen extension fields dialect "flink" profile "flink-2.3.0") publishes flink diagnostics and its format/completion use flink — the document-level override beats the workspace default (D-06); a didChange with a different flink profile re-parses under the new profile (stale guard: an older version's completion is rejected). (3) In lsp/coordinates.mbt verify the UTF-16 conversion is unchanged and reused by the flink path — do NOT add a second converter; if a flink-specific coordinate gap surfaces (e.g. a range spanning a multibyte boundary), fix it in @binding.span_to_range once (shared), not in the LSP. (4) Assert the refusal response shape for flink formatting: an unsafe flink tree (e.g. `CREATE CATALOG c WITH (` unclosed) returns published diagnostics (FATHOM-PARSE-* / FATHOM-FORMAT-001) + an empty edit array — never a partial edit.
  <acceptance_criteria>
    - A flink document completion with a multibyte prefix returns a UTF-16 textEdit range (line/character) whose start/end map to the correct byte offsets via @binding.byte_to_position; the newText replaces the prefix.
    - A flink-selected document overrides a doris workspace default: its diagnostics/format/completion use flink-2.3.0 (D-06); a didChange profile switch re-parses under the new flink profile.
    - An unsafe flink tree's formatting returns diagnostics + an empty edit array (never partial output, never -32603).
    - lsp/coordinates.mbt has exactly ONE UTF-16 conversion path (no second converter): `grep -c "span_to_range" lsp/coordinates.mbt` == 1 and the flink path reuses it.
    - All existing lsp tests pass unchanged (Doris zero-drift); `moon test --target native --package lsp --package binding --package api` green.
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native --package lsp --package binding --package api && test "$(grep -c 'span_to_range' lsp/coordinates.mbt)" -eq 1</automated>
  </verify>
  <done>The flink LSP behavior matrix is complete: UTF-16 textEdit ranges on multibyte prefixes, per-file flink override + profile-switch reparse, refusal response shape, and a single shared UTF-16 coordinate path — Doris LSP behavior byte-identical.</done>
</task>

<task type="auto">
  <name>CLI flink format exit-code matrix + Doris zero-drift gate</name>
  <files>fathom-sql/cli_test.mbt, fathom-sql/run.mbt</files>
  <read_first>
    @.planning/phases/13-toolchain-and-editor-packaging/13-RESEARCH.md
    @/opt/source/Fathom/fathom-sql/run.mbt
    @/opt/source/Fathom/fathom-sql/cli_test.mbt
  </read_first>
  <action>Add the CLI flink format exit-code matrix and prove Doris zero-drift. (1) In fathom-sql/cli_test.mbt add the D-39 flink matrix cases: (a) `run_format(command with subcommand "format", dialect "flink", profile "flink-2.3.0", stdin b"select 1")` returns exit_code 0 with stdout containing the canonical formatted SQL (e.g. "SELECT 1\n") and empty stderr (accepted); (b) a refused Flink tree (e.g. `CREATE CATALOG c WITH (` unclosed, or an uncovered family if one remains) returns exit_code 1 with empty stdout and stderr containing FATHOM-FORMAT-001 (refusal, parse diagnostics prepended never masked); (c) `run_format(command with dialect "flink", profile "4.x")` — a Doris-shaped profile under flink — returns exit_code 2 (unknown/unsupported profile, args.mbt is_valid_dialect_profile returns false before run_format; if run_format receives an already-validated Command the parse_error_outcome defense-in-depth maps UnknownProfile -> exit 2). (d) `fathom-sql parse --dialect flink --profile flink-2.3.0` on "SELECT 1" exits 0 with the fathom.parse.v1 envelope. (2) In fathom-sql/run.mbt verify no flink guard exists (run_format already calls @api.format_with_ids with no flink special-case, run.mbt:28-92); change run.mbt ONLY if a test surfaces a real gap (do not add a guard). (3) Prove Doris zero-drift: `moon test --package parity` (no --update) passes and diff_parity.py --frozen-only exits 0 — the CLI homomorph snapshots (parity baseline) are unchanged. (4) Run the LSP/CLI smoke: `fathom-sql lsp --dialect flink --profile flink-2.3.0` starts the stdio server (initialize handshake responds with serverInfo fathom-lsp).
  <acceptance_criteria>
    - cli_test.mbt flink matrix passes: accepted flink format -> exit 0 + formatted stdout + empty stderr; refused flink tree -> exit 1 + empty stdout + FATHOM-FORMAT-001 on stderr (parse diagnostics not masked); doris-shaped profile under flink -> exit 2; flink parse -> exit 0 + fathom.parse.v1 envelope.
    - run.mbt contains NO flink guard (the format path is dialect-agnostic via @api.format_with_ids); run.mbt changed only if a test surfaces a real gap.
    - `fathom-sql lsp --dialect flink --profile flink-2.3.0` starts the server and the initialize handshake responds (serverInfo fathom-lsp).
    - Doris CLI zero-drift: `moon test --package parity` (no --update) green; diff_parity.py --frozen-only exits 0; the CLI homomorph snapshots unchanged.
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native --package fathom-sql --package lsp --package parity && python3 scripts/diff_parity.py --frozen-only</automated>
  </verify>
  <done>The CLI flink format exit-code matrix (0/1/2) is tested, run.mbt needs no flink guard, the flink LSP server starts over stdio, and the Doris CLI/LSP parity baseline is byte-identical.</done>
</task>

</tasks>

## Source Audit Coverage

| SOURCE | ID | Feature or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | User can run fathom-sql parse/format/lsp --dialect flink + fathom-lsp end to end with diagnostics, formatting, completion, UTF-16, document-level dialect | 13-05 | COVERED | Real LSP format/completion paths + CLI exit-code matrix + per-file override verification. |
| REQ | TOOL-04 | Neutral CLI + Native LSP end to end for Flink (parse/format/lsp, diagnostics, formatting, completion, UTF-16, per-file dialect) | 13-05 | COVERED | Tasks 1-3. |
| CONTEXT | D-07 | LSP flink format from -32603 -> real @api.format_with_ids; flink completion from -32602 -> real @completion.complete; CLI format honors D-39 exit codes; UTF-16 via binding.coordinates; Doris zero-drift (costly) | 13-05 | COVERED | Sentinel removal + real paths + exit-code matrix + shared coordinates. |
| CONTEXT | D-06 | Keep locked selection model; per-file didOpen/didChange override; no auto-detection (one-way — confirmed in 13-06 checkpoint) | 13-05 | COVERED (verification) | Per-file flink override + profile-switch reparse tests; transport unchanged. |
| RESEARCH | §3.4 / Pitfall 4 | LSP behavioral contract change: error vs empty array vs real result shapes must be tested | 13-05 | COVERED | Three response shapes pinned in tests. |
| RESEARCH | Common Operation 3 | completion_item_json already emits LSP textEdit (UTF-16 via diagnostic_range) | 13-05 | COVERED | Reused unchanged; only the flink rejection removed. |
| RESEARCH | Don't Hand-Roll | UTF-16 via binding.coordinates, no second converter | 13-05 | COVERED | span_to_range single-use grep gate. |
| RESEARCH | Validation Arch | cli_test flink exit-code matrix; selection_test + completion_test flink document flows with UTF-16 | 13-05 | COVERED | Tasks 2-3. |
| REQ | D-39 | CLI exit codes 0/1/2 | 13-05 | COVERED | flink matrix cases. |
| REQ | PARITY-01 | Doris LSP/CLI behavior equal to frozen baseline | 13-05 | COVERED | Existing lsp/cli tests unchanged + diff_parity --frozen-only. |
| PROBE | DIALECT-04 precision | LSP UTF-16 positions exact across a dialect-switch reparse | 13-05 | FLAGGED | must_haves UTF-16 truth + multibyte textEdit test (flagged-unverified backstop carried from Phase 9). |

<verification>
Run moon test --target native --package lsp --package binding --package api --package fathom-sql --package parity after each task; run python3 scripts/diff_parity.py --frozen-only after every change. Inspect: handlers.mbt flink sentinel removal (no "flink grammar is not yet implemented"), completion_result flink path (real items), UTF-16 textEdit ranges on multibyte prefixes, per-file flink override + profile-switch reparse, refusal response shape (diagnostics + empty array), CLI flink exit-code matrix, run.mbt no-guard state, lsp coordinates single span_to_range.
</verification>

<success_criteria>
- A consumer runs fathom-sql parse/format/lsp --dialect flink and fathom-lsp end to end with real diagnostics, formatting, completion, UTF-16 positions, and document-level dialect selection.
- LSP flink format/completion return real results (never -32603/-32602 policy rejections); the three response shapes are distinguishable and tested.
- The CLI flink format honors D-39 exit codes; Doris LSP/CLI behavior is byte-identical.
</success_criteria>

<output>Create `.planning/phases/13-toolchain-and-editor-packaging/13-05-SUMMARY.md` when this plan executes.</output>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LSP client -> handlers.mbt format/completion | Untrusted document text + positions cross here; the flink path must return real results or structured errors, never a misleading rejection. |
| LSP completion items -> editor | textEdit ranges are UTF-16 (line/character); a wrong range corrupts the editor buffer. |
| CLI stdin -> run_format | Untrusted SQL + dialect/profile cross here; D-39 exit codes must be exact. |
| document-level selection -> LSP state | didOpen/didChange extension fields are untrusted; a flink override must not coerce a Doris policy and must be stale-guarded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-05-01 | Tampering | A host that depended on -32603/-32602 to disable format/completion buttons breaks when real results arrive | high | mitigate | Three response shapes (error vs empty vs real) pinned in tests; hosts updated in 13-06 to handle all three (D-07, Pitfall 4, A4). |
| T-13-05-02 | Tampering | A flink document silently formatted under Doris policy (sentinel removed but no real path) | high | mitigate | formatting_result calls @api.format_with_ids with the document's own dialect/profile; refusal (FATHOM-FORMAT-001) publishes diagnostics + empty array — never Doris layout (D-07, D-01). |
| T-13-05-03 | Tampering | Wrong UTF-16 textEdit range on a multibyte prefix corrupts the editor buffer | medium | mitigate | @binding.span_to_range shared single path (CRLF-as-one-break, 4-byte->2-units); multibyte prefix textEdit test (D-07, Don't Hand-Roll). |
| T-13-05-04 | Spoofing | A stale completion under an old dialect/profile version is published | high | mitigate | Existing version + selection stale guard (D-03 pub guard); profile-switch reparse test verifies only the newest selection's results (D-06). |
| T-13-05-05 | Tampering | CLI flink format returns the wrong exit code (accepted/refusal/usage) | medium | mitigate | cli_test flink matrix (0/1/2); D-39 mapping unchanged; parse diagnostics never masked on refusal (T-03-01). |
| T-13-05-06 | Tampering | A second UTF-16 converter drifts from binding.coordinates | high | mitigate | lsp/coordinates.mbt single span_to_range; grep gate (== 1); flink path reuses diagnostic_range (D-07). |
| T-13-05-07 | Tampering | Doris LSP/CLI bytes drift when flink paths are added | high | mitigate | Additive flink handling; existing lsp/cli tests unchanged; diff_parity --frozen-only (D-07, PARITY-01). |
| T-13-05-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new runtime dependencies (RESEARCH Package Legitimacy Audit N/A). |
</threat_model>

## Artifacts this plan produces

Files: lsp/handlers.mbt (modified — flink format -32603 sentinel removed, flink completion -32602 policy rejection removed), lsp/completion_test.mbt (modified — flink completion LSP tests + UTF-16 textEdit), lsp/selection_test.mbt (modified — flink document-level selection/per-file override tests), fathom-sql/cli_test.mbt (modified — flink format exit-code matrix), fathom-sql/run.mbt (unchanged unless a test surfaces a gap), lsp/coordinates.mbt (unchanged — single span_to_range verified).

Symbols (lsp/handlers.mbt): formatting_result flink sentinel deleted (real @api.format_with_ids path for flink); completion_result flink path via @completion.complete -> completion_item_json (real textEdit).
Symbols (lsp/completion_test.mbt): flink_completion_returns_real_textedit, flink_completion_multibyte_utf16_range, flink_completion_no_32602.
Symbols (lsp/selection_test.mbt): flink_document_override_beats_workspace_default, flink_profile_switch_reparses, flink_format_refusal_publishes_diagnostics_empty_array, flink_format_returns_real_edit.
Symbols (fathom-sql/cli_test.mbt): cli_flink_format_accepted_exit_0, cli_flink_format_refusal_exit_1, cli_flink_doris_shaped_profile_exit_2, cli_flink_parse_exit_0_fathom_parse_v1.
