---
phase: 13-toolchain-and-editor-packaging
plan: 04
type: execute
wave: 2
depends_on: [13-02]
files_modified:
  - binding/exports.mbt
  - binding/schema.mbt
  - binding/moon.pkg
  - parity/export_smoke_test.mbt
  - parity/run_js.mbt
  - parity/run_wasm.mbt
  - docs/API.md
requirements: [TOOL-05]
autonomous: false
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A consumer can request completion over the JS/linear-Wasm wire through the new primitive export #export_name(\"fathom_complete_v1\") with signature fathom_complete_v1(raw : Bytes, dialect : String, profile : String, cursor_byte : Int) -> Bytes (A4 export order: raw first, dialect second, profile third, cursor fourth) and receive UTF-8 JSON Bytes whose envelope carries schema_version \"fathom.complete.v1\" plus the completion items (label/detail/start_byte/end_byte/new_text) — mirroring fathom_parse_v1/fathom_format_v1 (TOOL-05, D-04, NAME-02)."
    - "The fathom.complete.v1 envelope is a NEW stable wire contract beyond NAME-02's four namespaces (parse/format/error/capabilities): it is registered in binding/schema.mbt validate_schema_version, binding/exports.mbt #export_name, binding/moon.pkg js+wasm exports lists, docs/API.md, and passes scripts/check_naming.py neutrality — a five-file change set shipped in one commit (D-04, Pitfall 3)."
    - "fathom_complete_v1 routes dialect-first: an unknown dialect returns the fathom.error.v1 envelope with FATHOM-SCHEMA-007, an unknown/unsupported flink profile returns fathom.error.v1 with FATHOM-SCHEMA-003, and a valid flink-2.3.0 completion returns fathom.complete.v1 with items — never a Doris fallback and never an empty-silent success (D-04, D-01/D-02, Pitfall 6)."
    - "The cursor_byte parameter is bounds-checked: an out-of-range cursor returns the structured error (InvalidCursor -> fathom.error.v1) rather than panicking or producing a malformed range (D-04, completion.mbt InvalidCursor)."
    - "The export surface is registered on BOTH JS and linear-Wasm: binding/moon.pkg js AND wasm exports lists both contain fathom_complete_v1 — a missing entry compiles but the built artifact lacks the symbol (Pitfall 3, Pitfall 8)."
    - "[probe TOOL-05 wire ABI] flagged-unverified: MoonBit #export_name primitive ABI for the new cursor_byte : Int parameter on linear-Wasm (A6) — the existing four exports prove the pattern for Bytes/String/Int/Bool, so confidence is high, but the first `moon build --target wasm binding` + a wasm export smoke must confirm the Int parameter maps correctly."
  artifacts:
    - path: "binding/exports.mbt"
      provides: "#export_name(\"fathom_complete_v1\") pub fn fathom_complete_v1(raw : Bytes, dialect : String, profile : String, cursor_byte : Int) -> Bytes; Ok -> json_bytes(completion_result_json(result)); Err -> json_bytes(completion_error_json(error))"
    - path: "binding/schema.mbt"
      provides: "completion_result_json(result) envelope (schema_version fathom.complete.v1 + items label/detail/start_byte/end_byte/new_text); completion_error_json(error) (fathom.error.v1 + FATHOM-SCHEMA-003/007); validate_schema_version registers \"fathom.complete.v1\""
    - path: "binding/moon.pkg"
      provides: "fathom_complete_v1 appended to the js AND wasm exports arrays"
    - path: "parity/export_smoke_test.mbt"
      provides: "wire smoke: fathom_complete_v1(b\"\", \"flink\", \"flink-2.3.0\", 0) output contains fathom.complete.v1 + items; unknown profile -> fathom.error.v1 + FATHOM-SCHEMA-003; unknown dialect -> FATHOM-SCHEMA-007; cursor out-of-range -> error envelope"
    - path: "docs/API.md"
      provides: "fathom_complete_v1 + fathom.complete.v1 registered in the wire/export documentation (D-04 registration)"
  key_links:
    - from: "binding/exports.mbt fathom_complete_v1"
      to: "completion/completion.mbt complete(raw, dialect, profile, cursor_byte)"
      via: "Ok -> completion_result_json (fathom.complete.v1); Err -> completion_error_json (fathom.error.v1 + FATHOM-SCHEMA codes) — the wire wraps the 13-02 core behavior (D-04, Common Operation 1)"
    - from: "binding/schema.mbt validate_schema_version"
      to: "the five fathom.*.v1 namespaces"
      via: "\"fathom.complete.v1\" added alongside parse/format/error/capabilities — the new stable contract is explicitly registered (D-04, NAME-02)"
    - from: "binding/moon.pkg js+wasm exports lists"
      to: "binding/exports.mbt #export_name"
      via: "both lists contain fathom_complete_v1 — #export_name and moon.pkg exports must stay in sync or the built JS/Wasm artifact silently lacks the symbol (Pitfall 3, Pitfall 8)"
  prohibitions:
    - "fathom_complete_v1 MUST NOT return a Doris fallback or an empty-silent success for an unknown dialect/profile — the error envelope (fathom.error.v1 + FATHOM-SCHEMA-003/007) is the contract (D-04, D-01/D-02)."
    - "fathom_complete_v1 MUST be registered in ALL five registries (exports.mbt, schema.mbt, moon.pkg js+wasm, docs/API.md, check_naming.py neutrality) in ONE commit — a partial registration leaves the JS/Wasm artifact silently missing the symbol or the schema validator rejecting the envelope (Pitfall 3)."
    - "The completion wire envelope MUST NOT leak a dialect name into item text — items carry the neutral detail \"SQL syntax keyword\" and the dialect/profile ride in the request/response metadata, never in item content (D-10, D-28)."
    - "No --update in CI: the new export smoke is additive and the Doris/flink frozen snapshots must stay byte-identical (Pitfall 1, D-08)."
---

<objective>
把 13-02 的 Flink 补全行为暴露为新的稳定 wire 契约（TOOL-05 / D-04）：新增 #export_name("fathom_complete_v1") 原始类型导出（raw, dialect, profile, cursor_byte → UTF-8 JSON Bytes）+ fathom.complete.v1 信封，镜像 fathom_parse_v1/fathom_format_v1（A4：dialect 紧跟 raw）。这是 NAME-02 锁定的四个命名空间（parse/format/error/capabilities）之外的新增稳定 wire 契约，须在 schema、文档、命名门禁（check_naming.py 中立性）显式登记。本 plan 是 Phase 13 的 wave-2 wire 切片：一条端到端路径 = 「JS/linear-Wasm 宿主 → fathom_complete_v1 → @completion.complete（13-02 核心）→ completion_result_json（fathom.complete.v1 信封）→ UTF-8 Bytes」；先以最小导出 + 信封证明，再扩展到完整错误矩阵与跨目标注册。

Purpose: TOOL-05 要求 JS/linear-Wasm、Web/Monaco、VS Code、IntelliJ 四宿主使用同一套 dialect-aware API/schema；D-04（one-way — wire schema 发布后变更需 schema 迁移）锁定新增 fathom_complete_v1 导出与 fathom.complete.v1 信封，使 Web/Monaco 与 JS/linear-Wasm 宿主获得与 LSP 相同的补全面。Research（Pitfall 3）已实证：新导出触及 4 个独立注册表（#export_name、moon.pkg js+wasm exports、validate_schema_version、docs + 命名 inventory）——本 plan 以五文件变更集一次性落地。D-04 的 one-way 门禁由 Task 1 checkpoint 确认导出签名与信封形状。

Output: binding/exports.mbt（fathom_complete_v1 导出）、binding/schema.mbt（completion_result_json/completion_error_json + validate_schema_version 注册）、binding/moon.pkg（js+wasm exports 追加）、parity/export_smoke_test.mbt（wire smoke）、docs/API.md（wire 契约文档）。
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-toolchain-and-editor-packaging/13-CONTEXT.md
@.planning/phases/13-toolchain-and-editor-packaging/13-RESEARCH.md
@.planning/phases/13-toolchain-and-editor-packaging/13-PATTERNS.md
@/opt/source/Fathom/binding/exports.mbt
@/opt/source/Fathom/binding/schema.mbt
@/opt/source/Fathom/binding/moon.pkg
@/opt/source/Fathom/parity/export_smoke_test.mbt
@/opt/source/Fathom/completion/completion.mbt
@/opt/source/Fathom/docs/API.md
@/opt/source/Fathom/scripts/check_naming.py

This is the phase wave-2 wire plan, depending on 13-02 (the complete() Flink branch returns real results). D-04 (one-way — wire schema published, migration required after release) is gated by Task 1 checkpoint. The blueprint is fathom_format_v1 (binding/exports.mbt:38-72): #export_name + pub fn returning json_bytes(...); json_bytes = @utf8.encode (exports.mbt:22-24). completion_result_json is new, mirroring format_result_json (schema.mbt:113-133) with schema_version "fathom.complete.v1" and item fields label/detail/start_byte/end_byte/new_text (the CompletionItem struct, completion.mbt:14-20). validate_schema_version (schema.mbt:18-25) currently accepts parse/format/error/capabilities — add "fathom.complete.v1". The five-file change set (exports.mbt, schema.mbt, moon.pkg js+wasm, docs/API.md, check_naming.py neutrality) must land in ONE commit (Pitfall 3). The parity package CAN import binding (export_smoke_test.mbt already calls @binding.fathom_parse_v1) — follow that existing pattern exactly. check_naming.py: the new symbol fathom_complete_v1 is neutral (no doris remnant), so the gate passes — run it to confirm no new forbidden-pattern hit.

> **Contribution hooks:** (1) API Coverage checkpoint — this plan completes the JS/Wasm wire surface: the full export set is fathom_parse_v1, fathom_format_v1, fathom_complete_v1 (NEW here), fathom_dialect_v1, fathom_capabilities_v1; enumerate this in docs/API.md so no host capability is silently un-built (the LSP surface — initialize/didOpen/didChange/formatting/completion — is 13-05). (2) Assumption-Delta — the wire surface was Doris-only in NAME-02's four namespaces; the plural transition adds a fifth namespace (fathom.complete.v1) that names the right thing (completion is dialect-aware via the request args). (3) Schema Push gate — no ORM/database schema files; skipped silently.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Confirm the fathom_complete_v1 wire contract form — D-04 one-way door</name>
  <read_first>
    @.planning/phases/13-toolchain-and-editor-packaging/13-CONTEXT.md
    @.planning/phases/13-toolchain-and-editor-packaging/13-RESEARCH.md
    @/opt/source/Fathom/binding/exports.mbt
    @/opt/source/Fathom/binding/schema.mbt
    @/opt/source/Fathom/binding/moon.pkg
  </read_first>
  <acceptance_criteria>
    - The confirm step selects option-a (fathom_complete_v1 A4 signature + fathom.complete.v1 envelope + five-file registration in one commit) or a documented alternative; the resume-signal records the selection so the export task proceeds with the confirmed form.
    - The locked D-04 contract is honored: A4 arg order (raw, dialect, profile, cursor_byte), result envelope fathom.complete.v1, error envelope fathom.error.v1 + FATHOM-SCHEMA-003/007, five-file registration.
  </acceptance_criteria>
  <decision>The exact form of the new stable completion wire contract: (a) export signature fathom_complete_v1(raw : Bytes, dialect : String, profile : String, cursor_byte : Int) -> Bytes (A4 export order — raw first, dialect second, mirroring fathom_parse_v1/fathom_format_v1); (b) result envelope schema_version "fathom.complete.v1" carrying items label/detail/start_byte/end_byte/new_text (the CompletionItem fields); (c) error envelope fathom.error.v1 with FATHOM-SCHEMA-003 (unknown/unsupported profile) and FATHOM-SCHEMA-007 (unknown dialect); (d) a five-file registration (exports.mbt, schema.mbt, moon.pkg js+wasm, docs/API.md, check_naming.py neutrality) shipped in one commit.</decision>
  <context>D-04 is one-way because the wire schema is published — changing it after release requires a schema migration (CONTEXT D-04). RESEARCH Common Operation 1 + Pitfall 3 fix the form: mirror fathom_format_v1's envelope (schema.mbt:113-133), use json_bytes/@utf8.encode, register in validate_schema_version alongside the four existing namespaces. The alternative forms: (a) a different arg order (e.g. cursor before dialect) would break the A4 dialect-after-raw convention and the CLI/LSP symmetry; (b) reusing fathom.parse.v1 for completion would overload a parse envelope and violate NAME-02; (c) a partial five-file registration leaves the JS/Wasm artifact silently missing the symbol (Pitfall 3). The cursor_byte : Int ABI on linear-Wasm is [ASSUMED] A6 (high confidence from the existing four exports) — the first wasm build confirms it.</context>
  <options>
    <option id="option-a">
      <name>fathom_complete_v1(raw, dialect, profile, cursor_byte) + fathom.complete.v1 envelope + five-file registration in one commit (recommended — D-04 locked + RESEARCH recommendation)</name>
      <pros>Mirrors fathom_parse_v1/fathom_format_v1 exactly (A4); the envelope carries the neutral completion items; the five-file change set is atomic so no host artifact is silently missing the symbol; matches the RESEARCH Common Operation 1 blueprint.</pros>
      <cons>Introduces a fifth namespace beyond NAME-02's four — a deliberate, documented expansion of the stable wire surface (one-way).</cons>
    </option>
    <option id="option-b">
      <name>Different argument order or a completion-inside-parse envelope</name>
      <pros>No new namespace (if completion rides fathom.parse.v1).</pros>
      <cons>Breaks the A4 dialect-after-raw convention and the CLI/LSP symmetry, or overloads a parse envelope — both contradict the locked D-04 and RESEARCH; rejected.</cons>
    </option>
  </options>
  <resume-signal>Select option-a (fathom_complete_v1 A4 signature + fathom.complete.v1 envelope + five-file registration) or describe a modified wire form; the export task is authored only after this confirmation.</resume-signal>
</task>

<task type="tracer">
  <name>fathom_complete_v1 export + fathom.complete.v1 envelope + five-file registration end-to-end</name>
  <files>binding/exports.mbt, binding/schema.mbt, binding/moon.pkg, parity/export_smoke_test.mbt, docs/API.md</files>
  <read_first>
    @.planning/phases/13-toolchain-and-editor-packaging/13-CONTEXT.md
    @.planning/phases/13-toolchain-and-editor-packaging/13-RESEARCH.md
    @.planning/phases/13-toolchain-and-editor-packaging/13-PATTERNS.md
    @/opt/source/Fathom/binding/exports.mbt
    @/opt/source/Fathom/binding/schema.mbt
    @/opt/source/Fathom/binding/moon.pkg
    @/opt/source/Fathom/parity/export_smoke_test.mbt
  </read_first>
  <action>Build the thinnest end-to-end completion wire slice — the five-file change set in ONE commit. (1) binding/exports.mbt: add #export_name("fathom_complete_v1") and `pub fn fathom_complete_v1(raw : Bytes, dialect : String, profile : String, cursor_byte : Int) -> Bytes` that matches @completion.complete(raw, dialect, profile, cursor_byte): Ok(result) => json_bytes(completion_result_json(result)); Err(error) => json_bytes(completion_error_json(error)) — mirror fathom_format_v1's structure (exports.mbt:38-72), using the existing json_bytes helper (@utf8.encode). (2) binding/schema.mbt: add `pub const COMPLETE_SCHEMA_VERSION : String = "fathom.complete.v1"`; add completion_result_json(result : @completion.CompletionList, dialect : String, profile : String) -> String emitting `{ "schema_version": "fathom.complete.v1", "dialect": ..., "profile": ..., "is_incomplete": ..., "items": [ { "label", "detail", "start_byte", "end_byte", "new_text" } ] }` (mirror format_result_json's envelope shape, schema.mbt:113-133); add completion_error_json(error : @completion.CompletionError) -> String emitting the fathom.error.v1 envelope with the FATHOM-SCHEMA code (UnknownDialect -> FATHOM-SCHEMA-007, UnknownProfile -> FATHOM-SCHEMA-003, InvalidCursor/InvalidSource/InputTooLarge -> a schema error code — reuse the existing schema_error_code/error envelope pattern); extend validate_schema_version (schema.mbt:18-25) to accept COMPLETE_SCHEMA_VERSION. (3) binding/moon.pkg: append "fathom_complete_v1" to the js AND wasm exports arrays (moon.pkg options(link)). (4) parity/export_smoke_test.mbt: add a test `completion_export_is_dialect_aware_with_neutral_wire_identity` mirroring format_export_is_dialect_aware_with_neutral_wire_identity: `let ok = @utf8.decode_lossy(@binding.fathom_complete_v1(b"", "flink", "flink-2.3.0", 0)[:])` asserts ok.contains("fathom.complete.v1"), ok.contains("\"dialect\":\"flink\""), ok.contains("\"profile\":\"flink-2.3.0\""), and the items array contains at least one item with a "label" and "start_byte" field; the unknown-profile case `fathom_complete_v1(b"", "flink", "2.1", 0)` asserts fathom.error.v1 + FATHOM-SCHEMA-003; the unknown-dialect case `fathom_complete_v1(b"", "mysql", "4.x", 0)` asserts FATHOM-SCHEMA-007. (5) docs/API.md: register fathom_complete_v1 + fathom.complete.v1 in the wire/export documentation — add it to the export list and document the signature, the envelope fields, and the error codes (FATHOM-SCHEMA-003/007); enumerate the FULL wire surface (fathom_parse_v1, fathom_format_v1, fathom_complete_v1, fathom_dialect_v1, fathom_capabilities_v1) so no host capability is silently un-built.
  <acceptance_criteria>
    - #export_name("fathom_complete_v1") exists with the A4 signature (raw, dialect, profile, cursor_byte); `moon build --target js binding` and `moon build --target wasm binding` both succeed and the built artifact exports fathom_complete_v1 (grep the built binding.js / wasm exports for the symbol).
    - binding/moon.pkg js AND wasm exports lists both contain fathom_complete_v1 (Pitfall 3, Pitfall 8).
    - validate_schema_version accepts "fathom.complete.v1"; completion_result_json emits the envelope with schema_version fathom.complete.v1 + dialect + profile + items (label/detail/start_byte/end_byte/new_text); completion_error_json emits fathom.error.v1 + FATHOM-SCHEMA-003/007.
    - export_smoke_test.mbt passes: flink-2.3.0 empty-input completion returns fathom.complete.v1 with items; unknown flink profile -> fathom.error.v1 + FATHOM-SCHEMA-003; unknown dialect -> FATHOM-SCHEMA-007.
    - docs/API.md enumerates the full five-export wire surface including fathom_complete_v1 + fathom.complete.v1 envelope + error codes.
    - `python3 scripts/check_naming.py` exits 0 (fathom_complete_v1 is neutral — no forbidden remnant).
    - `moon test --package parity` (no --update) passes — no frozen snapshot moved.
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native --package parity --package completion && moon build --target js binding && moon build --target wasm binding && python3 scripts/check_naming.py && test "$(grep -c 'fathom_complete_v1' binding/moon.pkg)" -ge 2</automated>
  </verify>
  <done>The fathom_complete_v1 export exists with the A4 signature, the fathom.complete.v1 envelope + error envelope are registered in all five places in one commit, the wire smoke passes for flink/unknown-profile/unknown-dialect, and both JS and Wasm artifacts export the symbol.</done>
</task>

<task type="auto">
  <name>Wire error matrix + cross-target registration + completion core boundary</name>
  <files>binding/schema.mbt, parity/export_smoke_test.mbt, completion/completion.mbt</files>
  <read_first>
    @.planning/phases/13-toolchain-and-editor-packaging/13-RESEARCH.md
    @/opt/source/Fathom/binding/schema.mbt
    @/opt/source/Fathom/parity/export_smoke_test.mbt
    @/opt/source/Fathom/completion/completion.mbt
  </read_first>
  <action>Complete the wire contract hardening. (1) Extend export_smoke_test.mbt with the full error matrix: cursor out-of-range — `fathom_complete_v1(b"SELECT 1", "flink", "flink-2.3.0", 9999)` returns fathom.error.v1 (InvalidCursor); invalid source / InputTooLarge — `fathom_complete_v1(b"x", "flink", "flink-2.3.0", 0)` with an over-limit input returns the structured error envelope; a valid flink completion with a real cursor `fathom_complete_v1(b"SELECT FRO", "flink", "flink-2.3.0", 9)` returns fathom.complete.v1 with a FROM item whose start_byte/end_byte cover the "FRO" prefix (source-range edit over the wire). (2) Add a cross-target assertion pattern (native parity suite already runs on js/wasm via run_js.mbt/run_wasm.mbt — verify those runners exercise the new export OR add fathom_complete_v1 to parity/run_js.mbt + parity/run_wasm.mbt so the same completion bytes are produced on js and linear-Wasm, matching the PARITY-02 three-target byte-identity contract; if the runners only call parse/format, extend them to call fathom_complete_v1(b"", "flink", "flink-2.3.0", 0) and assert the output contains fathom.complete.v1 — this proves the new Int cursor_byte ABI maps on linear-Wasm (A6 probe). (3) In completion/completion.mbt, verify the core already bounds-checks cursor_byte (InvalidCursor at completion.mbt:165-166) — do NOT duplicate the check in the binding; if a wire-specific guard is needed for the ABI boundary, add it in the export (fathom_complete_v1) only, not in the core. (4) Ensure the completion envelope never leaks a dialect name into item text: assert in export_smoke_test.mbt that the item labels/details contain no "doris"/"flink" string (the neutral detail "SQL syntax keyword" and the dialect ride in the envelope metadata, D-10).
  <acceptance_criteria>
    - export_smoke_test.mbt error matrix passes: cursor out-of-range -> fathom.error.v1 (InvalidCursor); over-limit input -> structured error envelope; `fathom_complete_v1(b"SELECT FRO", "flink", "flink-2.3.0", 9)` -> fathom.complete.v1 with a FROM item whose start_byte/end_byte cover the "FRO" prefix.
    - parity/run_js.mbt + parity/run_wasm.mbt exercise fathom_complete_v1 and the js + linear-Wasm builds produce byte-identical completion output for the same input (three-target byte-identity, PARITY-02) — the A6 Int ABI is confirmed on linear-Wasm.
    - The completion item labels/details contain no "doris"/"flink" string (neutral detail; dialect in envelope metadata only, D-10).
    - completion/completion.mbt cursor bounds-check is single-sourced (no duplicated check in the binding export).
    - `moon test --target native --package parity` AND `moon test --target js --package parity` AND `moon test --target wasm --package parity` all pass; `python3 scripts/compare_backends.py` exits 0 (three-target digest identical).
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native --package parity && moon test --target js --package parity && moon test --target wasm --package parity && python3 scripts/compare_backends.py && python3 scripts/check_naming.py</automated>
  </verify>
  <done>The completion wire surface is fully hardened: the error matrix (invalid cursor/oversize), the source-range edit over the wire, the three-target byte-identity (native/js/wasm exercising fathom_complete_v1, confirming the Int ABI on linear-Wasm), and the neutral item text all pass.</done>
</task>

</tasks>

## Source Audit Coverage

| SOURCE | ID | Feature or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | User can use the same dialect-aware API/schema through JS/linear-Wasm; each host selects Doris or Flink per file/session, no second parser | 13-04 | COVERED (wire half) | fathom_complete_v1 + fathom.complete.v1 give JS/Wasm hosts the completion surface; host selection wiring in 13-06. |
| REQ | TOOL-05 | Same dialect-aware API/schema/LSP from JS and linear Wasm; hosts select Doris or Flink | 13-04 | COVERED (wire) | New export + envelope + three-target byte-identity; host constants in 13-06. |
| CONTEXT | D-04 | New fathom_complete_v1 wire export + fathom.complete.v1 envelope; dialect after raw (A4); register in schema/docs/naming gate (one-way) | 13-04 | COVERED | Task 1 checkpoint + Task 2 five-file change set + Task 3 error matrix/cross-target. |
| RESEARCH | §3.5 / Pitfall 3 | 5-file change set: exports.mbt, moon.pkg both targets, schema.mbt, docs, naming gate | 13-04 | COVERED | Task 2 single commit. |
| RESEARCH | Common Operation 1 | fathom_complete_v1 blueprint = fathom_format_v1 | 13-04 | COVERED | Task 2 export mirrors exports.mbt:38-72. |
| RESEARCH | A6 | cursor_byte Int ABI on linear-Wasm [ASSUMED] | 13-04 | COVERED + FLAGGED | three-target run_js/run_wasm exercise the export; must_haves flagged probe. |
| RESEARCH | Validation Arch | export_smoke_test.mbt fathom_complete_v1 round-trip + envelope | 13-04 | COVERED | Tasks 2-3. |
| RESEARCH | Pitfall 8 | #export_name and moon.pkg exports must stay in sync | 13-04 | COVERED | moon.pkg js+wasm both contain the symbol; grep gate. |
| REQ | NAME-02 | four fathom.*.v1 namespaces + FATHOM-* codes + dialect fields | 13-04 | COVERED (extension) | fathom.complete.v1 added as a documented fifth namespace. |
| REQ | PARITY-02 | byte-identical serialized results across Native/JS/linear-Wasm | 13-04 | COVERED | run_js/run_wasm exercise fathom_complete_v1; compare_backends.py digest. |
| REQ | PARITY-01 | Doris behavior equal to frozen baseline | 13-04 | COVERED | No --update; moon test --package parity green. |
| PROBE | TOOL-05 wire ABI | cursor_byte Int on linear-Wasm | 13-04 | FLAGGED | must_haves flagged-unverified truth; wasm export smoke confirms. |

<verification>
Run moon test --target native --package parity --package completion after each task; run moon test --target js --package parity + moon test --target wasm --package parity + python3 scripts/compare_backends.py in Task 3; run python3 scripts/check_naming.py after the export lands. Inspect: #export_name signature (A4 order), completion_result_json envelope (fathom.complete.v1 + items fields), completion_error_json (fathom.error.v1 + FATHOM-SCHEMA-003/007), validate_schema_version registration, moon.pkg js+wasm exports lists, export_smoke_test assertions, docs/API.md five-export enumeration, run_js/run_wasm completion callsites.
</verification>

<success_criteria>
- A consumer requests completion over JS/linear-Wasm via fathom_complete_v1 and receives the fathom.complete.v1 envelope with neutral items and source-range edits.
- The new stable wire contract is registered in all five registries in one commit; the JS and Wasm artifacts both export the symbol.
- Error handling is dialect-first (FATHOM-SCHEMA-003/007), bounded (cursor checked), and three-target byte-identical; check_naming.py stays green.
</success_criteria>

<output>Create `.planning/phases/13-toolchain-and-editor-packaging/13-04-SUMMARY.md` when this plan executes.</output>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS/linear-Wasm host -> fathom_complete_v1 | Untrusted cursor + raw bytes cross the ABI; cursor must be bounds-checked and the output envelope stable. |
| binding/moon.pkg exports -> built artifact | The registered export list determines what the JS/Wasm artifact exposes; a missing entry silently removes the symbol. |
| completion wire envelope -> host | Item text must stay neutral (no dialect name) and source-range edits must be byte-accurate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-04-01 | Tampering | fathom_complete_v1 missing from the built JS/Wasm artifact (partial registration) | high | mitigate | Five-file change set in one commit; moon.pkg js+wasm both contain the symbol; grep gate + moon build --target js/wasm verify (Pitfall 3, Pitfall 8). |
| T-13-04-02 | Spoofing | Unknown dialect/profile silently returns a Doris-shaped or empty completion | high | mitigate | Dialect-first: UnknownDialect -> FATHOM-SCHEMA-007, UnknownProfile -> FATHOM-SCHEMA-003; export smoke asserts the error envelopes (D-04, Pitfall 6). |
| T-13-04-03 | Tampering | Out-of-range cursor panics or emits a malformed range | medium | mitigate | Cursor bounds-checked in the core (InvalidCursor, completion.mbt:165-166); wire error matrix asserts the error envelope (D-04). |
| T-13-04-04 | Tampering | Int ABI mismatch on linear-Wasm (cursor_byte not mapped) | medium | mitigate | run_wasm.mbt exercises fathom_complete_v1; compare_backends.py proves three-target byte-identity (A6, PARITY-02). |
| T-13-04-05 | Tampering | Envelope leaks a dialect name into item text | medium | mitigate | Neutral detail "SQL syntax keyword"; export smoke asserts no doris/flink string in items (D-10, D-28). |
| T-13-04-06 | Tampering | Schema validator rejects fathom.complete.v1 (registration gap) | high | mitigate | validate_schema_version registers COMPLETE_SCHEMA_VERSION; export smoke asserts the envelope is accepted (Pitfall 3). |
| T-13-04-07 | Tampering | Frozen snapshot drift from the additive export | high | mitigate | No --update in CI; moon test --package parity green; diff_parity --frozen-only (D-08, Pitfall 1). |
| T-13-04-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new runtime dependencies (RESEARCH Package Legitimacy Audit N/A). |
</threat_model>

## Artifacts this plan produces

Files: binding/exports.mbt (modified — #export_name("fathom_complete_v1") + pub fn fathom_complete_v1), binding/schema.mbt (modified — COMPLETE_SCHEMA_VERSION "fathom.complete.v1", completion_result_json, completion_error_json, validate_schema_version registration), binding/moon.pkg (modified — fathom_complete_v1 in js+wasm exports arrays), parity/export_smoke_test.mbt (modified — completion_export_is_dialect_aware_with_neutral_wire_identity + error matrix), parity/run_js.mbt + parity/run_wasm.mbt (modified — fathom_complete_v1 callsites), docs/API.md (modified — five-export wire surface enumeration + fathom.complete.v1 envelope/error documentation).

Symbols (binding/exports.mbt): fathom_complete_v1(raw : Bytes, dialect : String, profile : String, cursor_byte : Int) -> Bytes (#export_name, A4 order, json_bytes/completion_error_json wrappers).
Symbols (binding/schema.mbt): COMPLETE_SCHEMA_VERSION = "fathom.complete.v1"; completion_result_json(result : @completion.CompletionList, dialect : String, profile : String) -> String; completion_error_json(error : @completion.CompletionError) -> String; validate_schema_version extended arm.
Symbols (parity/export_smoke_test.mbt): completion_export_is_dialect_aware_with_neutral_wire_identity; wire error matrix (InvalidCursor/oversize); neutral item text assertion.
Symbols (parity/run_js.mbt + run_wasm.mbt): fathom_complete_v1 callsite producing three-target byte-identical output.
Symbols (docs/API.md): full five-export wire surface table + fathom.complete.v1 envelope + FATHOM-SCHEMA-003/007 error codes.
