---
phase: 13-toolchain-and-editor-packaging
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - completion/completion.mbt
  - completion/completion_test.mbt
  - dialect/flink.mbt
  - dialect/classification.mbt
requirements: [TOOL-02]
autonomous: true
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A consumer can request bounded Flink syntax completion through @completion.complete(raw, \"flink\", \"flink-2.3.0\", cursor_byte) and receive a CompletionList with is_incomplete=false and at most MAX_CANDIDATES=32 items, each carrying label, detail (\"SQL syntax keyword\"), start_byte, end_byte, and new_text — the same CompletionItem source-range edit contract as Doris, no catalog consulted (TOOL-02, D-02)."
    - "Flink completion candidates come ONLY from the dialect/flink.mbt classification table (D-28 no-second-list discipline): no completion-specific keyword table exists; the candidate pool is @dialect.classification_entries(flink_context) after the D-02 row extension, filtered per profile by introduced_profile in release order flink-1.20.5 < flink-2.1.3 < flink-2.3.0 (TOOL-02, D-02, D-28)."
    - "Flink completion_context arms produce the required contexts: statement-start, ddl-header (CREATE/DROP/ALTER), watermark (WATERMARK), partitioned-by (PARTITIONED BY), window-tvf (TUMBLE/HOP/CUMULATE/SESSION), and match-recognize (PATTERN/DEFINE/MEASURES); each arm flows through the existing context_accepts/context_preferred/prefix_matches two-pass priority loop (TOOL-02, D-02, SC2)."
    - "The 142-row flink_classification_rows table is extended with provenance-annotated NonReserved rows for the D-02 context words the completion contexts require: statement verbs (ALTER/DROP/SHOW/DESCRIBE/EXPLAIN/ANALYZE/USE/RESET), DDL nouns (CATALOG/DATABASE/FUNCTION), WATERMARK, PARTITIONED, TVF names (TUMBLE/HOP/CUMULATE/SESSION), PRIMARY/COMPUTED/METADATA — each row carries source = pinned release grammar path and introduced_profile; NonReserved additions are parse-neutral and the frozen flink-grammar/flink-lexical snapshots stay byte-identical (D-02, Pitfall 2, A2)."
    - "Flink completion is bounded and safe: MAX_CANDIDATES=32 enforced, cursor_byte bounds-checked (InvalidCursor on out-of-range), source-range edits unchanged, and an unknown/unsupported flink profile returns the structured UnknownProfile error — never a Doris fallback and never an empty-silent success (TOOL-02, D-02, D-01/D-02 explicit-selection)."
    - "[probe TOOL-02 context depth] flagged-unverified: MATCH_RECOGNIZE sub-clause completion beyond PATTERN/DEFINE/MEASURES (pattern variables, quantifiers) is catalog/scope-bound and explicitly deferred to TOOL-FUTURE-01 — the match-recognize arm is scoped to the four reserved clause words already in the table (RESEARCH Open Question 3 recommendation)."
  artifacts:
    - path: "completion/completion.mbt"
      provides: "complete() Flink branch constructing DialectContext via @dialect.FlinkProfile::from_id + metadata(); profile_allows Flink arm returning true for rows visible under the selected profile (classification already filters by introduced_profile); completion_context Flink arms (statement-start/ddl-header/watermark/partitioned-by/window-tvf/match-recognize)"
    - path: "dialect/flink.mbt"
      provides: "flink_classification_rows extended with NonReserved D-02 context rows (provenance-annotated, introduced_profile-tagged); provenance audit test extended to cover the new rows"
    - path: "completion/completion_test.mbt"
      provides: "Flink completion unit tests: boundedness (<=32), profile gating per introduced_profile, context arms, source-range edits, unknown-profile rejection, no-Doris-fallback"
    - path: "parity/export_smoke_test.mbt"
      provides: "wire-level assertion that @binding.fathom_parse_v1/flink-grammar path remains green (export surface untouched by completion internals); completion wire test lands in 13-04 with fathom_complete_v1"
  key_links:
    - from: "completion/completion.mbt complete() Flink branch"
      to: "dialect/flink.mbt flink_classification_rows + classification.mbt flink_row_visible"
      via: "classification_entries(flink_context) filters rows by introduced_profile <= selected profile (flink_row_visible, classification.mbt:78-89) so the candidate pool is profile-correct with no second list (D-02, D-28)"
    - from: "completion/completion.mbt completion_context Flink arms"
      to: "completion.mbt context_accepts/context_preferred/prefix_matches pipeline"
      via: "the new context strings flow through the UNCHANGED two-pass priority loop and MAX_CANDIDATES=32 bound (completion.mbt:182-205)"
    - from: "dialect/flink.mbt extended rows"
      to: "completion/completion_test.mbt per-profile gating tests"
      via: "a row introduced at flink-2.1.3 is ABSENT under flink-1.20.5 and PRESENT under 2.3.0/2.1.3 — asserted via classification_entries(flink_context) in the completion tests (T-10-13)"
  prohibitions:
    - "Flink completion MUST NOT build a second keyword list — candidates come only from the extended flink_classification_rows table via classification_entries (D-28 single-table discipline); any completion-specific table is a violation (D-02, Pitfall 2)."
    - "The flink_classification_rows extension MUST NOT change parse behavior — new rows are NonReserved (parse-neutral); a Reserved addition that moves the frozen flink-grammar/flink-lexical snapshots is a violation unless pre-declared in approved-changes.md (Pitfall 2, A2)."
    - "Flink completion MUST NOT exceed MAX_CANDIDATES=32 and MUST NOT return a silent empty success for an unknown/unsupported profile — unknown profile returns UnknownProfile, never a Doris fallback and never an empty-silent list that hides the selection error (D-01/D-02)."
    - "Doris completion output MUST stay byte-identical (PARITY-01): the Flink branch and context arms are additive; doris context strings and candidate ordering are unchanged."
---

<objective>
把 Phase 11 的 Flink 语法与 dialect/flink.mbt 分类表贯通到 completion（TOOL-02 / D-02）：complete() 增加 Flink 分支（用 @dialect.FlinkProfile::from_id + metadata 构造 DialectContext，替代 Phase 9 的全量 UnknownProfile 拒绝），profile_allows 增加 Flink 分支，completion_context 扩展 Flink 上下文（statement-start/ddl-header/watermark/partitioned-by/window-tvf/match-recognize），并把 flink_classification_rows 扩展为补全所需的候选池（D-28 单表纪律：不建补全专用表）。本 plan 是 Phase 13 的 wave-1 completion 切片：一条端到端路径 = 「flink 语句 + cursor → @completion.complete → Flink DialectContext → classification_entries（per-profile 过滤）→ context 臂 → 两遍优先级循环（MAX_CANDIDATES=32）→ CompletionList（source-range edit）」；先以 statement-start 上下文 + 最小行扩展证明，再扩展到全部上下文臂与完整行扩展。

Purpose: TOOL-02 要求消费者能获得有界、dialect/profile 感知的 Flink 语法补全（关键字、DDL、WATERMARK、Window TVF、MATCH_RECOGNIZE 上下文）并收到安全的 source-range edit；D-02 要求候选池复用 dialect/flink.mbt 分类表作为唯一候选池，per-profile gating 用 introduced_profile 镜像 Doris profile_allows。Research 现状（Pitfall 2）已实证：142 行分类表缺少语句动词（ALTER/DROP/SHOW/DESCRIBE/EXPLAIN/ANALYZE/USE/RESET）、DDL 名词（CATALOG/DATABASE/FUNCTION）、WATERMARK/PARTITIONED、TVF 名、PRIMARY/COMPUTED/METADATA——必须扩展（NonReserved 行 parse-neutral；Reserved 行会破坏冻结快照，倾向 NonReserved）。本 plan 兑现该扩展并以 provenance audit 测试保护。Doris 补全输出零漂移。

Output: completion/completion.mbt（Flink 分支 + profile_allows Flink 臂 + Flink completion_context 臂）、dialect/flink.mbt（扩展行 + provenance audit）、completion/completion_test.mbt（Flink 补全单测）。
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-toolchain-and-editor-packaging/13-CONTEXT.md
@.planning/phases/13-toolchain-and-editor-packaging/13-RESEARCH.md
@.planning/phases/13-toolchain-and-editor-packaging/13-PATTERNS.md
@/opt/source/Fathom/completion/completion.mbt
@/opt/source/Fathom/dialect/flink.mbt
@/opt/source/Fathom/dialect/classification.mbt
@/opt/source/Fathom/api/api.mbt
@/opt/source/Fathom/lsp/completion_test.mbt

This is the phase wave-1 completion tracer. D-02 (reversible) drives the Flink branch + context extension + candidate-pool extension; the single-table discipline (D-28) is the hard constraint — the candidate pool is `@dialect.classification_entries(flink_context)`, which ALREADY filters Flink rows by introduced_profile through flink_row_visible (classification.mbt:78-89), so the profile gating is "free" once the Flink DialectContext is constructed. The complete() Flink branch must mirror api.ParseOptions::new's Flink arm (api.mbt:79-103): @dialect.FlinkProfile::from_id(profile_id) -> metadata -> DialectContext, None -> Err(UnknownProfile). profile_allows Flink arm: return true for rows the classification pipeline already filtered (flink_row_visible) — do NOT re-implement per-profile gating (the entries are already profile-filtered). The candidate-pool extension (Pitfall 2) is NonReserved-only to stay parse-neutral; frozen flink-grammar/flink-lexical snapshots must stay byte-identical (proven by moon test --package parity no --update). The wire-level completion test (fathom_complete_v1) belongs to 13-04, NOT this plan; export_smoke_test.mbt is touched here only if a parse-path assertion needs a flink fixture (prefer completion/completion_test.mbt for the unit surface).

> **Contribution hooks:** (1) API Coverage checkpoint — the completion surface is exposed to JS/Wasm hosts via the NEW fathom_complete_v1 wire export (D-04, 13-04); this plan builds the core completion behavior the wire wraps. (2) Assumption-Delta — completion becomes dialect-aware in the plural sense (doris + flink both return real candidates); the abstraction names the right thing (complete(raw, dialect_id, profile_id, cursor_byte) already carries the dialect dimension, 09-02). (3) Schema Push gate — no ORM/database schema files; skipped silently.
</context>

<tasks>

<task type="tracer">
  <name>Flink branch in complete() + statement-start context + minimal candidate-pool extension end-to-end</name>
  <files>completion/completion.mbt, dialect/flink.mbt, completion/completion_test.mbt</files>
  <read_first>
    @.planning/phases/13-toolchain-and-editor-packaging/13-CONTEXT.md
    @.planning/phases/13-toolchain-and-editor-packaging/13-RESEARCH.md
    @.planning/phases/13-toolchain-and-editor-packaging/13-PATTERNS.md
    @/opt/source/Fathom/completion/completion.mbt
    @/opt/source/Fathom/dialect/flink.mbt
    @/opt/source/Fathom/dialect/classification.mbt
  </read_first>
  <action>Build the thinnest end-to-end Flink completion slice. (1) In completion/completion.mbt complete() replace the `@dialect.Dialect::Flink => return Err(UnknownProfile(profile_id~))` arm (completion.mbt:163) with a real Flink branch mirroring the api.ParseOptions::new Flink arm (api.mbt:79-103): `@dialect.Dialect::Flink => { match @dialect.FlinkProfile::from_id(profile_id) { Some(profile) => { let metadata = profile.metadata(); { dialect: dialect, profile_id: profile_id, exact_release: metadata.exact_release, feature_introduction: metadata.feature_introduction } } None => return Err(UnknownProfile(profile_id~)) } }`. (2) In profile_allows (completion.mbt:57-65) add the Flink arm returning `true` — the candidate rows are already filtered by introduced_profile via classification_entries -> flink_row_visible, so a second per-profile check would be redundant (do NOT re-implement the Doris-style match on entry.introduced_profile; the classification pipeline owns that for Flink). (3) Extend dialect/flink.mbt flink_classification_rows with the statement-start context words as NonReserved rows: ALTER, DROP, SHOW, DESCRIBE, EXPLAIN, ANALYZE, USE, RESET — each with introduced_profile "flink-1.20.5" (baseline statement verbs, present in all three pinned releases) and source = the pinned release grammar path (e.g. "flink-sql-parser Parser-calcite-1.32.0.jj:NNNN (ALTER)" — the exact line refs come from the pinned /tmp/flink-research/ archives per the extract_flink_grammar.py provenance discipline; do NOT fabricate a line number — use the release archive lookup the 10-03/11-01 tasks established). (4) Add completion/completion_test.mbt (new test module in the completion package) with the tracer assertions: `@completion.complete(b"", "flink", "flink-2.3.0", 0)` returns Ok with items whose labels include the statement verbs (CREATE/INSERT/UPDATE/DELETE/SELECT/WITH already in the table + the new ALTER/DROP/SHOW/DESCRIBE/EXPLAIN/ANALYZE/USE/RESET), items.length() <= 32, each item's detail is "SQL syntax keyword", and start_byte/end_byte/new_text are consistent with the empty cursor; `@completion.complete(b"SEL", "flink", "flink-2.3.0", 3)` returns Ok with at least SELECT (prefix match); `@completion.complete(b"", "flink", "flink-9.9.9", 0)` returns Err(UnknownProfile).
  <acceptance_criteria>
    - `@completion.complete(b"", "flink", "flink-2.3.0", 0)` returns Ok(CompletionList) with is_incomplete=false, items.length() <= 32, and labels covering the statement-start set (SELECT/WITH/INSERT/UPDATE/DELETE/CREATE/SET/VALUES + ALTER/DROP/SHOW/DESCRIBE/EXPLAIN/ANALYZE/USE/RESET).
    - `@completion.complete(b"SEL", "flink", "flink-2.3.0", 3)` returns Ok with an item whose label is "SELECT" (case-insensitive prefix match through the existing prefix_matches pipeline).
    - `@completion.complete(b"", "flink", "flink-9.9.9", 0)` returns Err(UnknownProfile(profile_id="flink-9.9.9")) — no Doris fallback, no empty-silent success.
    - `@completion.complete(b"", "doris", "4.x", 0)` output is byte-identical to the pre-change Doris output (PARITY-01): the Flink branch is additive and the Doris arm is unchanged — assert the Doris statement-start items are unchanged in completion_test.mbt.
    - The new flink_classification_rows rows are NonReserved and carry provenance source paths matching the pinned release grammar (the provenance audit test `flink_classification_rows_source_references_release_grammar` in dialect/flink.mbt:283-299 passes for the new rows).
    - Frozen flink-grammar/flink-lexical snapshots stay byte-identical: `moon test --package parity` (no --update) passes — the NonReserved additions are parse-neutral (A2).
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native --package completion --package dialect --package parity && python3 scripts/diff_parity.py --frozen-only</automated>
  </verify>
  <done>The complete() Flink branch constructs a real DialectContext, profile_allows admits the classification-filtered rows, the statement-start context returns the extended verb set under MAX_CANDIDATES, unknown profiles reject with UnknownProfile, and the Doris + frozen flink snapshots stay byte-identical.</done>
</task>

<task type="auto">
  <name>Flink completion_context arms (ddl-header/watermark/partitioned-by/window-tvf/match-recognize) + full candidate-pool extension</name>
  <files>completion/completion.mbt, dialect/flink.mbt, completion/completion_test.mbt</files>
  <read_first>
    @.planning/phases/13-toolchain-and-editor-packaging/13-RESEARCH.md
    @/opt/source/Fathom/completion/completion.mbt
    @/opt/source/Fathom/dialect/flink.mbt
  </read_first>
  <action>Extend completion_context (completion.mbt:75-107) with the Flink context arms and finish the candidate-pool extension. (1) completion_context currently returns a single string from a chain of word_is(last/previous, ...) checks; the chain is dialect-agnostic but the Flink contexts need last/previous token shapes. Add arms that fire for BOTH dialects only where safe, but gate the Flink-specific ones so Doris context strings are unchanged: the current chain returns "statement-start" when last.length()==0 (already correct for Flink statement-start), "select"/"from"/"where"/"group-by"/"order-by"/"join-on"/"general". Add AFTER the existing arms (so Doris ordering is preserved): `else if word_is(last, b"WATERMARK") { "watermark" }`, `else if word_is(last, b"PARTITIONED") || (word_is(last, b"BY") && word_is(previous, b"PARTITIONED")) { "partitioned-by" }`, `else if word_is(last, b"CREATE") || word_is(last, b"DROP") || word_is(last, b"ALTER") { "ddl-header" }`, `else if word_is(last, b"FROM") || word_is(last, b"JOIN") { "window-tvf" }` (table position — TUMBLE/HOP/CUMULATE/SESSION are table-function names), `else if word_is(last, b"MATCH_RECOGNIZE") || word_is(last, b"PATTERN") || word_is(last, b"DEFINE") || word_is(last, b"MEASURES") { "match-recognize" }`. NOTE: the existing "from"/"join-on" arms already capture FROM/JOIN — to avoid changing Doris behavior, the "window-tvf" arm must fire only for the Flink dialect; pass the dialect into completion_context (extend its signature to take `dialect_context : @dialect.DialectContext` or a `is_flink : Bool` flag) and gate the new arms on Flink where they overlap existing Doris arms (window-tvf for FROM/JOIN, ddl-header for CREATE — CREATE under Doris currently falls to "general", which is fine; gate ddl-header to Flink too so Doris stays byte-identical). The non-overlapping arms (watermark, partitioned-by, match-recognize) are Flink-only words and can fire unconditionally, but gating all new arms on Flink is the safe choice. (2) Add the context_accepts arms for the new contexts in the SAME function (completion.mbt:109-124): "watermark" accepts WATERMARK-adjacent words (the clause words already in the table: PRIMARY/COMPUTED/METADATA/COLUMN/COLUMNS etc. — map to @dialect.is_clause_keyword OR the new DDL nouns), "partitioned-by" accepts the distribution words (PARTITIONED/BY already reserved or the table rows), "ddl-header" accepts the DDL nouns (CATALOG/DATABASE/FUNCTION/TABLE/VIEW), "window-tvf" accepts TUMBLE/HOP/CUMULATE/SESSION, "match-recognize" accepts PATTERN/DEFINE/MEASURES/MATCH_NUMBER (already Reserved rows in the table). (3) Complete the flink_classification_rows extension (NonReserved, provenance-annotated): the DDL nouns CATALOG/DATABASE/FUNCTION, WATERMARK, PARTITIONED, the TVF names TUMBLE/HOP/CUMULATE/SESSION, and the CREATE TABLE body words PRIMARY/COMPUTED/METADATA/VIRTUAL/CONNECTOR/ENFORCED — each with introduced_profile "flink-1.20.5" (baseline words present across releases) and release-grammar source paths from the pinned archives. (4) Extend completion/completion_test.mbt with per-context assertions: `@completion.complete(b"CREATE ", "flink", "flink-2.3.0", 7)` returns items including CATALOG/DATABASE/FUNCTION/TABLE/VIEW (ddl-header context); `@completion.complete(b"CREATE TABLE t (", "flink", "flink-2.3.0", 15)` returns items including WATERMARK/PRIMARY/COMPUTED/METADATA; `@completion.complete(b"SELECT * FROM ", "flink", "flink-2.3.0", 14)` returns items including TUMBLE/HOP/CUMULATE/SESSION (window-tvf); a MATCH_RECOGNIZE context test asserts PATTERN/DEFINE/MEASURES appear.
  <acceptance_criteria>
    - completion_context returns "ddl-header" after CREATE/DROP/ALTER, "watermark" after WATERMARK, "partitioned-by" after PARTITIONED/BY, "window-tvf" after FROM/JOIN (Flink only), "match-recognize" after MATCH_RECOGNIZE/PATTERN/DEFINE/MEASURES — and Doris context strings are unchanged (doris "select"/"from"/"where"/"group-by"/"order-by"/"join-on"/"general" byte-identical).
    - `@completion.complete(b"CREATE ", "flink", "flink-2.3.0", 7)` returns items including CATALOG, DATABASE, FUNCTION, TABLE, VIEW (ddl-header context_accepts arm).
    - `@completion.complete(b"SELECT * FROM ", "flink", "flink-2.3.0", 14)` returns items including TUMBLE, HOP, CUMULATE, SESSION (window-tvf arm; non-reserved TVF names now in the table).
    - A WATERMARK/PARTITIONED/MATCH_RECOGNIZE context assertion returns the expected clause words (WATERMARK/PRIMARY/COMPUTED/METADATA for the column-body context; PATTERN/DEFINE/MEASURES for match-recognize).
    - All new flink_classification_rows rows are NonReserved with provenance source paths; the provenance audit test passes; `moon test --package parity` (no --update) passes (frozen flink snapshots byte-identical).
    - Doris completion output byte-identical: the existing lsp/completion_test.mbt Doris assertions still pass unchanged (PARITY-01).
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native --package completion --package dialect --package parity --package lsp && python3 scripts/diff_parity.py --frozen-only</automated>
  </verify>
  <done>The Flink completion_context arms (ddl-header/watermark/partitioned-by/window-tvf/match-recognize) produce the required contexts, the candidate pool covers the D-02 context words via NonReserved classification rows, and Doris + frozen flink snapshots stay byte-identical.</done>
</task>

<task type="auto">
  <name>Boundedness, profile gating, source-range edit, and no-Doris-fallback tests</name>
  <files>completion/completion_test.mbt, dialect/flink.mbt, dialect/classification.mbt</files>
  <read_first>
    @.planning/phases/13-toolchain-and-editor-packaging/13-RESEARCH.md
    @/opt/source/Fathom/completion/completion.mbt
    @/opt/source/Fathom/dialect/classification.mbt
  </read_first>
  <action>Harden the Flink completion contract with the full test matrix. (1) Boundedness: a broad-context completion (e.g. statement-start on empty input) returns items.length() <= 32 for every flink profile — assert the MAX_CANDIDATES bound is never exceeded even when the table would yield more (statement-start on flink-2.3.0 has many rows). (2) Profile gating per introduced_profile: a row introduced at flink-2.1.3 (e.g. VARIANT/QUALIFY if present) is ABSENT under flink-1.20.5 and PRESENT under flink-2.1.3/flink-2.3.0; a 2.3.0-introduced row (e.g. SAFE_CAST) is ABSENT under 2.1.3/1.20.5 — assert via classification_entries(flink_context) AND via the completion output where the context admits the word (mirror classification_is_dialect_independent_and_release_aware in classification.mbt). (3) Source-range edits: `@completion.complete(b"SELECT 名称", "flink", "flink-2.3.0", ...)` with a multibyte prefix returns start_byte/end_byte covering the identifier prefix and new_text replacing it (mirror completion_filters_profile_and_replaces_multibyte_prefix in lsp/completion_test.mbt:60-75); assert start_byte <= cursor_byte <= end_byte and new_text is the label. (4) No-Doris-fallback + explicit selection: unknown dialect returns UnknownDialect; unknown flink profile returns UnknownProfile; a doris profile id under flink (e.g. "4.x") returns UnknownProfile (no Doris-profile borrowing, T-10-01 exact-match only). (5) If dialect/classification.mbt needs no change, leave it untouched — only touch it if the profile gating test surfaces a filter gap; do NOT add a completion-specific filter (D-28). (6) Add a regression guard: `git grep` finds no completion-specific keyword list (no new Array[KeywordEntry] outside flink_classification_rows / doris_classification_rows).
  <acceptance_criteria>
    - MAX_CANDIDATES bound: every flink completion call returns items.length() <= 32, including statement-start on flink-2.3.0 (the widest context).
    - Profile gating: a 2.1.3-introduced word is absent under flink-1.20.5 and present under 2.1.3/2.3.0; a 2.3.0-introduced word is absent under 1.20.5/2.1.3 — asserted through both classification_entries and the completion output.
    - Multibyte source-range edit: start_byte/end_byte cover the identifier prefix (start_byte <= cursor_byte <= end_byte) and new_text equals the label; UTF-8 byte offsets are authoritative (no char-index confusion).
    - Unknown dialect -> UnknownDialect; unknown flink profile -> UnknownProfile; a doris-shaped profile under flink (e.g. "4.x") -> UnknownProfile — no fallback, no silent empty list.
    - No completion-specific keyword list exists: `grep -rn "KeywordEntry" completion/` returns only references to @dialect.KeywordEntry (the shared row type), no new table declaration.
    - Doris completion byte-identical: `moon test --package parity` (no --update) and the lsp completion_test Doris assertions pass.
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native --package completion --package dialect --package parity --package lsp && test "$(grep -rc 'let .*: Array\[@dialect.KeywordEntry\] = \[' completion/ | grep -v ':0' | wc -l)" -eq 0 && python3 scripts/diff_parity.py --frozen-only</automated>
  </verify>
  <done>Flink completion is proven bounded (<=32), profile-gated per introduced_profile, source-range-safe on multibyte prefixes, and strictly explicit-selection (no Doris fallback, no completion-specific keyword table), with Doris + frozen flink snapshots byte-identical.</done>
</task>

</tasks>

## Source Audit Coverage

| SOURCE | ID | Feature or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | User receives bounded, dialect/profile-aware syntax completion for Flink keywords, DDL, WATERMARK, Window TVF, MATCH_RECOGNIZE contexts | 13-02 | COVERED | Flink branch + context arms + candidate-pool extension + bounded MAX_CANDIDATES=32. |
| REQ | TOOL-02 | Bounded Flink syntax completion (keywords, DDL, WATERMARK, Window TVF, MATCH_RECOGNIZE) with safe source-range edits per dialect/profile | 13-02 | COVERED | Tasks 1-3: Flink branch, context arms, candidate pool, boundedness, profile gating, source-range edits. |
| CONTEXT | D-02 | Flink completion reuses dialect/flink.mbt classification table as ONLY candidate pool; per-profile gating via introduced_profile (flink-1.20.5 < 2.1.3 < 2.3.0); completion_context extended; MAX_CANDIDATES=32; source-range edits unchanged (reversible) | 13-02 | COVERED | profile_allows Flink arm admits classification-filtered rows; context arms; NonReserved row extension; boundedness tests. |
| CONTEXT | D-28 | No second keyword list | 13-02 | COVERED | Candidate pool is classification_entries only; regression guard greps for no completion-specific table. |
| RESEARCH | §3.2 / Pitfall 2 | 142-row table missing statement verbs/DDL nouns/WATERMARK/TVF names; must extend (NonReserved parse-neutral, Reserved breaks frozen snapshots) | 13-02 | COVERED | NonReserved rows with provenance; frozen flink snapshots proven byte-identical. |
| RESEARCH | Open Question 1 | (a) extend flink_classification_rows vs (b) weaker completion — recommend (a) | 13-02 | COVERED | Task 2 full extension + provenance audit. |
| RESEARCH | Open Question 3 | MATCH_RECOGNIZE completion scoped to the four reserved clause words; sub-clause deferred | 13-02 | COVERED | match-recognize arm limited to PATTERN/DEFINE/MEASURES/MATCH_NUMBER; must_haves flagged-unverified probe. |
| RESEARCH | Common Operation 2 | Flink branch blueprint = api.ParseOptions::new Flink arm | 13-02 | COVERED | Task 1 mirrors api.mbt:79-103. |
| RESEARCH | Validation Arch | completion unit tests (boundedness, profile gating, context arms, source-range edits) | 13-02 | COVERED | completion/completion_test.mbt. |
| RESEARCH | A2 | NonReserved additions parse-neutral | 13-02 | COVERED | Moon test --package parity no --update proves it. |
| REQ | PARITY-01 | Doris behavior equal to frozen baseline | 13-02 | COVERED | Doris completion output byte-identical; lsp completion_test Doris assertions unchanged. |
| PROBE | TOOL-02 context depth | MATCH_RECOGNIZE sub-clause completion deferred | 13-02 | FLAGGED | must_haves flagged-unverified truth; scoped to four reserved words. |

<verification>
Run moon test --target native --package completion --package dialect --package parity --package lsp after each task; run python3 scripts/diff_parity.py --frozen-only after every change. Inspect: complete() Flink branch (FlinkProfile::from_id + metadata), profile_allows Flink arm (true — classification already filters), completion_context new arms (gated on Flink to preserve Doris), context_accepts arms per context, flink_classification_rows new NonReserved rows with provenance, MAX_CANDIDATES=32 bound, per-profile gating (introduced_profile), multibyte source-range edits, unknown-profile rejection.
</verification>

<success_criteria>
- A consumer receives bounded (<=32) Flink syntax completion for keywords, DDL, WATERMARK, Window TVF, and MATCH_RECOGNIZE contexts with safe source-range edits.
- Candidates come only from the extended dialect/flink.mbt classification table, gated per profile by introduced_profile — no second keyword list.
- Unknown/unsupported profiles reject with UnknownProfile; no Doris fallback; Doris + frozen flink snapshots stay byte-identical.
</success_criteria>

<output>Create `.planning/phases/13-toolchain-and-editor-packaging/13-02-SUMMARY.md` when this plan executes.</output>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flink SQL + cursor -> completion.complete() | Untrusted editor snapshot crosses here; cursor must be bounds-checked and the result bounded. |
| completion candidate pool -> dialect/flink.mbt classification table | The candidate pool is the classification table (D-28); a second list would drift from parser acceptance. |
| flink_classification_rows extension -> parser/lexer classification | NonReserved additions are parse-neutral; a Reserved addition would change identifier acceptance and move frozen snapshots. |
| completion output -> editor textEdit | Source-range edits (start_byte/end_byte/new_text) are byte offsets; malformed ranges corrupt the editor buffer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-02-01 | Tampering | A completion-specific keyword list drifts from the parser's classification table | high | mitigate | D-28 single-table discipline: candidate pool is classification_entries only; regression guard greps for no new KeywordEntry table (Pitfall 2). |
| T-13-02-02 | Tampering | A Reserved classification addition changes parse behavior and moves frozen flink-grammar/lexical snapshots | high | mitigate | Extension is NonReserved-only (parse-neutral); moon test --package parity no --update + diff_parity --frozen-only prove zero drift (A2, Pitfall 2). |
| T-13-02-03 | Denial of Service | Unbounded completion on a broad context | medium | mitigate | MAX_CANDIDATES=32 enforced in the two-pass loop; boundedness test asserts <=32 for the widest context (D-02). |
| T-13-02-04 | Tampering | Profile gating bypass — a 2.3.0-introduced word offered under flink-1.20.5 | high | mitigate | classification_entries -> flink_row_visible filters by introduced_profile <= selected; per-profile gating tests assert absence under older profiles (T-10-13). |
| T-13-02-05 | Tampering | Silent fallback — unknown profile returns a Doris-shaped candidate list or an empty-silent success | high | mitigate | UnknownProfile on unknown/unsupported profile; no Doris-profile borrowing (exact-match from_id, T-10-01); no-Doris-fallback tests. |
| T-13-02-06 | Tampering | Source-range edit with wrong byte offsets corrupts the editor buffer | medium | mitigate | start_byte/end_byte from cursor_replacement; multibyte prefix test asserts start_byte <= cursor_byte <= end_byte and new_text == label (authoritative UTF-8 bytes). |
| T-13-02-07 | Tampering | Doris completion bytes drift when Flink arms are added | high | mitigate | New context arms gated on Flink; Doris context strings/ordering unchanged; PARITY-01 + lsp completion_test Doris assertions (Pitfall 4). |
| T-13-02-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new runtime dependencies (RESEARCH Package Legitimacy Audit N/A). |
</threat_model>

## Artifacts this plan produces

Files: completion/completion.mbt (modified — complete() Flink branch, profile_allows Flink arm, completion_context Flink arms, context_accepts arms), completion/completion_test.mbt (new — Flink completion unit tests: boundedness, profile gating, context arms, source-range edits, unknown-profile), dialect/flink.mbt (modified — flink_classification_rows extended with NonReserved D-02 context rows + provenance audit), dialect/classification.mbt (modified only if a filter gap surfaces; otherwise untouched).

Symbols (completion/completion.mbt): complete() Flink arm (FlinkProfile::from_id -> metadata -> DialectContext, None -> Err(UnknownProfile)); profile_allows Flink arm returning true (classification-filtered rows); completion_context extended signature with dialect flag + new arms (ddl-header/watermark/partitioned-by/window-tvf/match-recognize); context_accepts arms for the new contexts.
Symbols (dialect/flink.mbt): flink_classification_rows += NonReserved rows for ALTER/DROP/SHOW/DESCRIBE/EXPLAIN/ANALYZE/USE/RESET, CATALOG/DATABASE/FUNCTION, WATERMARK, PARTITIONED, TUMBLE/HOP/CUMULATE/SESSION, PRIMARY/COMPUTED/METADATA/VIRTUAL/CONNECTOR/ENFORCED (provenance-annotated, introduced_profile "flink-1.20.5"); provenance audit test extended.
Symbols (completion/completion_test.mbt): flink_statement_start_returns_verb_set, flink_prefix_matches_select, flink_unknown_profile_rejects, flink_ddl_header_context, flink_watermark_partitioned_context, flink_window_tvf_context, flink_match_recognize_context, flink_boundedness_max_32, flink_profile_gating_per_introduced_profile, flink_multibyte_source_range_edit, flink_no_doris_fallback, doris_output_byte_identical.
