---
phase: 13-toolchain-and-editor-packaging
plan: 06
type: execute
wave: 3
depends_on: [13-04, 13-05]
files_modified:
  - web/src/monaco-adapter.ts
  - web/src/main.ts
  - web/src/main.test.ts
  - web/scripts/offline-smoke.mjs
  - vscode/src/extension-contract.ts
  - vscode/src/extension.ts
  - vscode/scripts/launch-smoke.mjs
  - jetbrains/src/main/kotlin/fathom/jetbrains/sql/FathomSettings.kt
  - jetbrains/src/main/kotlin/fathom/jetbrains/sql/FathomSettingsConfigurable.kt
  - jetbrains/scripts/source-smoke.py
requirements: [TOOL-05]
autonomous: false
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Web/Monaco, VS Code, and IntelliJ hosts validate the (dialect, profile) pair instead of a flat profile list: doris -> ['2.1','3.x','4.x']; flink -> ['flink-2.3.0','flink-2.1.3','flink-1.20.5']; the profile dropdown/validation switches with the selected dialect — flink values appear only when flink is selected (TOOL-05, D-05)."
    - "The server remains authoritative: binding.validate_dialect_profile / LSP validate_selection is the final gate — a host-side validation failure surfaces an explicit error (MISSING_SELECTION or a config error), never a coerced/fallback profile (TOOL-05, D-05, D-02 no-default)."
    - "Hosts keep static constants (no dynamic pull, no shared cross-host JSON): each host's (dialect, profile) table is a local static constant per D-05 — offline-first, no network dependency (D-05, PARITY-03)."
    - "The locked selection model is unchanged (D-06): workspace/session default (LSP initializationOptions / CLI --dialect --profile / VS Code fathom.dialect / IntelliJ FathomSettings) + per-file LSP didOpen/didChange override; NO auto-detection, NO extension-based guessing — flink values now pass each host's validation and per-file override works on flink files (TOOL-05, D-06, D-01)."
    - "The harness assertions are updated in the SAME change set as the host constants: web/scripts/offline-smoke.mjs, vscode/scripts/launch-smoke.mjs, and jetbrains/scripts/source-smoke.py stop pinning the old flat list ['2.1','3.x','4.x'] and assert the per-dialect pairs + flink values (D-05, Pitfall 5, same-commit rule)."
    - "[probe TOOL-05 host parity] flagged-unverified: the Web host additionally calls fathom_complete_v1 (13-04) for the completion surface, and the three hosts must produce the same per-file selection behavior for a flink file (open flink file -> select flink dialect/profile -> receive diagnostics, and format/completion at supported points) — the cross-host behavioral parity is verified in the 13-07 packaging smoke."
  artifacts:
    - path: "web/src/monaco-adapter.ts + web/src/main.ts + web/src/main.test.ts"
      provides: "PROFILES_BY_DIALECT map (doris/flink pairs); validateSelection(dialect, profile) per-dialect; profile dropdown repopulated on dialect change; fathom_complete_v1 call for the completion surface; main.test.ts pair-validation + complete assertions"
    - path: "vscode/src/extension-contract.ts + vscode/src/extension.ts"
      provides: "SUPPORTED_PROFILES replaced by a PROFILES_BY_DIALECT map; resolveFathomConfiguration validates the (dialect, profile) pair and surfaces an explicit error; initializationOptions {dialect, profile} unchanged"
    - path: "jetbrains/.../FathomSettings.kt + FathomSettingsConfigurable.kt"
      provides: "ALLOWED_PROFILES replaced by a per-dialect map; profile ComboBox repopulates when dialectCombo changes; normalizeProfile validates against the selected dialect's list"
    - path: "web/scripts/offline-smoke.mjs + vscode/scripts/launch-smoke.mjs + jetbrains/scripts/source-smoke.py"
      provides: "harness assertions updated to the per-dialect pairs + flink values (same-commit rule)"
  key_links:
    - from: "host PROFILES_BY_DIALECT maps"
      to: "binding.validate_dialect_profile / LSP validate_selection"
      via: "hosts validate statically (defense in depth); the server re-validates every selection — a host accepting a value the server rejects is a server-side error, never a silent fallback (D-05, D-06)"
    - from: "web monaco-adapter fathom_complete_v1"
      to: "binding/exports.mbt fathom_complete_v1 (13-04)"
      via: "the Web host calls the same wire export for completion — same dialect-aware API/schema, no second parser (D-04, TOOL-05)"
    - from: "host harness assertions"
      to: "host constants"
      via: "the same-commit rule: offline-smoke.mjs / launch-smoke.mjs / source-smoke.py assert the per-dialect pairs in the SAME change set as the constants — a stale flat-list assertion fails fast (Pitfall 5)"
    - from: "IntelliJ FathomSettingsConfigurable profileCombo"
      to: "FathomSettings.ALLOWED_PROFILES per-dialect map"
      via: "the profile ComboBox repopulates when dialectCombo changes so flink values appear only for flink (D-05)"
  prohibitions:
    - "Hosts MUST NOT dynamically pull the profile list at runtime (fathom_capabilities_v1) or share a cross-host JSON definition — static per-host constants only (D-05, offline-first, PARITY-03)."
    - "Hosts MUST NOT coerce or fall back a profile on validation failure — an explicit error (MISSING_SELECTION / config error) is the contract; a default-dialect or normalizeProfile fallback is forbidden (D-02, D-05)."
    - "No auto-detection and no extension-based dialect guessing — the locked selection model (workspace default + per-file override) is unchanged; flink values only flow through explicit selection (D-06, D-01)."
    - "The harness assertions MUST change in the same commit as the constants — a stale flat-list ['2.1','3.x','4.x'] assertion is a violation (Pitfall 5)."
    - "No host maintains a second parser — all hosts call the shared wire/LSP surface (fathom_*_v1 / fathom-lsp), never a local parse implementation (TOOL-05, D-04)."
---

<objective>
把 Flink 贯通到 Web/Monaco、VS Code、IntelliJ 三宿主（TOOL-05 / D-05）：将三宿主的扁平 profile 列表改为 (dialect, profile) 二元组校验——doris → 2.1/3.x/4.x；flink → flink-2.3.0/flink-2.1.3/flink-1.20.5——profile 下拉/校验随所选 dialect 切换（选 flink 才出现 flink 值），并保持静态常量模式（不动态拉取、不共享跨宿主 JSON）。服务端（binding.validate_dialect_profile / LSP validate_selection）仍权威校验（纵深防御，宿主侧失败显式报错而非回退）。Web 宿主额外调用 fathom_complete_v1（13-04）获得补全面。本 plan 是 Phase 13 的 wave-3 宿主切片：一条端到端路径 = 「宿主选择 flink dialect/profile → 静态 (dialect, profile) 校验 → 调用共享 wire/LSP 面 → 收到 flink 结果」；先以 Web 宿主证明（含 fathom_complete_v1），再扩展到 VS Code 与 IntelliJ，三宿主 harness 断言同 commit 更新（Pitfall 5）。

Purpose: TOOL-05 要求四宿主（JS/linear-Wasm + Web/Monaco + VS Code + IntelliJ）使用同一套 dialect-aware API/schema/LSP，按文件/会话选择 Doris 或 Flink，且不维护第二个 parser；D-05（reversible）锁定宿主静态 (dialect, profile) 二元组校验；D-06（one-way — 选择传输契约是 LSP 公共契约）锁定保持现有选择模型，只让 flink 值通过各宿主校验并让 per-file 覆盖在 flink 文件上生效。Research（Pitfall 5）已实证：4 个宿主文件 + 3 个 harness 断言全钉住旧扁平列表——必须同 commit 变更。本 plan 兑现 D-05/D-06 宿主侧，JS/linear-Wasm 面已在 13-04 完成，打包 smoke 在 13-07。

Output: web/src/monaco-adapter.ts + main.ts + main.test.ts（PROFILES_BY_DIALECT + fathom_complete_v1）、vscode/src/extension-contract.ts + extension.ts（PROFILES_BY_DIALECT + pair 校验）、jetbrains/FathomSettings.kt + FathomSettingsConfigurable.kt（per-dialect map + ComboBox 切换）、三个 harness 同 commit 更新。
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-toolchain-and-editor-packaging/13-CONTEXT.md
@.planning/phases/13-toolchain-and-editor-packaging/13-RESEARCH.md
@.planning/phases/13-toolchain-and-editor-packaging/13-PATTERNS.md
@/opt/source/Fathom/web/src/monaco-adapter.ts
@/opt/source/Fathom/web/src/main.ts
@/opt/source/Fathom/web/src/main.test.ts
@/opt/source/Fathom/web/scripts/offline-smoke.mjs
@/opt/source/Fathom/vscode/src/extension-contract.ts
@/opt/source/Fathom/vscode/src/extension.ts
@/opt/source/Fathom/vscode/scripts/launch-smoke.mjs
@/opt/source/Fathom/jetbrains/src/main/kotlin/fathom/jetbrains/sql/FathomSettings.kt
@/opt/source/Fathom/jetbrains/src/main/kotlin/fathom/jetbrains/sql/FathomSettingsConfigurable.kt
@/opt/source/Fathom/jetbrains/scripts/source-smoke.py

This is the phase wave-3 hosts plan, depending on 13-04 (fathom_complete_v1 wire — the Web host calls it for completion) and 13-05 (LSP flink real paths — VS Code/IntelliJ hosts exercise real flink format/completion over LSP). D-05 (reversible) drives the host (dialect, profile) pair validation; D-06 (one-way — the didOpen/initializationOptions selection transport is a public LSP contract) is gated by Task 1 checkpoint. RESEARCH Common Operation 4 pins the blueprint: `PROFILES_BY_DIALECT = Object.freeze({ doris: ['2.1','3.x','4.x'], flink: ['flink-2.3.0','flink-2.1.3','flink-1.20.5'] })` + validateSelection branching. The same-commit rule (Pitfall 5): every host constant change updates its harness assertion in the SAME commit — offline-smoke.mjs (web, asserts flat list at :25), launch-smoke.mjs (vscode, initializationOptions assertion at :29), source-smoke.py (jetbrains, requires `listOf("2.1","3.x","4.x")` at :76). The server stays authoritative — hosts are defense in depth, and a host-side failure surfaces an explicit error, never a coercion. The per-file override (D-06) is verified at the LSP layer (13-05); this plan lets flink values pass each host's static validation and wires the profile dropdowns.

> **Scope note (4 tasks):** Task 1 is a `checkpoint:decision` gate (D-06 one-way), not an execution task — the executable surface is Tasks 2-4 (Web tracer + VS Code + IntelliJ), which are one cohesive D-05 host-pair-validation concern sharing the same-commit rule (each host constant change updates its harness in the same commit, Pitfall 5). The three hosts are NOT parallel plans because they must land as one atomic D-05 change set (the 4 host constants + 3 harness assertions pin the same contract); splitting them would break the same-commit rule and risk a partial (doris-only) host surface.

> **Contribution hooks:** (1) API Coverage checkpoint — the host wire surface is: Web calls fathom_parse_v1/fathom_format_v1/fathom_complete_v1 (the full five-export set from 13-04); VS Code/IntelliJ call the LSP methods (initialize/formatting/completion with the document-level dialect). No host capability is silently un-built — the complete surface is wired in the Web adapter, and the LSP methods are exercised by the VS Code/IntelliJ hosts. (2) Assumption-Delta — the toolchain was Doris-only (flat profile lists) and becomes multi-dialect (per-dialect pairs); the abstraction names the right thing (PROFILES_BY_DIALECT + validateSelection), verified by the same-commit harness updates. (3) Schema Push gate — no ORM/database schema files; skipped silently.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Confirm the selection transport stays unchanged — D-06 one-way door</name>
  <read_first>
    @.planning/phases/13-toolchain-and-editor-packaging/13-CONTEXT.md
    @.planning/phases/13-toolchain-and-editor-packaging/13-RESEARCH.md
    @/opt/source/Fathom/lsp/handlers.mbt
    @/opt/source/Fathom/web/src/monaco-adapter.ts
  </read_first>
  <acceptance_criteria>
    - The confirm step selects option-a (keep the locked selection transport unchanged; let flink values pass host validation) or a documented alternative; the resume-signal records the selection so the host tasks proceed with the confirmed model.
    - The locked D-06 contract is honored: workspace/session default + per-file LSP didOpen/didChange extension-field override; no auto-detection, no extension-based guessing.
  </acceptance_criteria>
  <decision>The selection transport contract that remains the public LSP/CLI/host surface: (a) keep the locked model — workspace/session default (LSP initializationOptions / CLI --dialect --profile / VS Code fathom.dialect / IntelliJ FathomSettings) + per-file LSP didOpen/didChange dialect/profile extension-field override; (b) do NOT add auto-detection or extension-based guessing (D-01 forbids); (c) Phase 13 only lets flink values pass each host's static validation and verifies per-file override on flink files — the didOpen/initializationOptions JSON shape is unchanged (document-level config rides didOpen/didChange extension fields, workspace default from initializationOptions/serve_stdio, languageId mapping only when user-configured).</decision>
  <context>D-06 is one-way because the selection transport contract (didOpen extension fields / initializationOptions) is an LSP public contract — changing it after release requires client migration (CONTEXT D-06). The current transport was locked in Phase 9 (D-01 selection transport: document-level config rides didOpen/didChange extension fields, workspace default from initializationOptions/serve_stdio, languageId mapping only when user-configured). Phase 13's job is only to let the flink profile values pass host validation (D-05) and to verify per-file override works on flink files — the transport JSON shape is frozen. The alternative forms: auto-detection or extension-based guessing is explicitly forbidden by D-01; a new transport channel (e.g. a per-file config file or URI mapping) would add a second selection mechanism and break the locked contract — both rejected.</context>
  <options>
    <option id="option-a">
      <name>Keep the locked selection transport unchanged; only let flink values pass host validation (recommended — D-06 locked)</name>
      <pros>Preserves the Phase 9 D-01 transport contract byte-for-byte (didOpen/didChange extension fields + initializationOptions/serve_stdio workspace default); no new selection mechanism; per-file override verified on flink files; minimal host surface change.</pros>
      <cons>No auto-detection ergonomics — the user must explicitly select flink per file/session (the locked product decision).</cons>
    </option>
    <option id="option-b">
      <name>Add extension-based guessing or a new transport channel</name>
      <pros>More convenient selection.</pros>
      <cons>Violates D-01's explicit-selection prohibition and adds a second selection mechanism to the locked LSP contract — rejected.</cons>
    </option>
  </options>
  <resume-signal>Select option-a (keep the locked transport; let flink values pass) or describe a modified selection model; the host tasks are authored only after this confirmation.</resume-signal>
</task>

<task type="tracer">
  <name>Web/Monaco host — (dialect, profile) pairs + fathom_complete_v1 + profile dropdown end-to-end</name>
  <files>web/src/monaco-adapter.ts, web/src/main.ts, web/src/main.test.ts, web/scripts/offline-smoke.mjs</files>
  <read_first>
    @.planning/phases/13-toolchain-and-editor-packaging/13-CONTEXT.md
    @.planning/phases/13-toolchain-and-editor-packaging/13-RESEARCH.md
    @.planning/phases/13-toolchain-and-editor-packaging/13-PATTERNS.md
    @/opt/source/Fathom/web/src/monaco-adapter.ts
    @/opt/source/Fathom/web/src/main.ts
    @/opt/source/Fathom/web/src/main.test.ts
    @/opt/source/Fathom/web/scripts/offline-smoke.mjs
  </read_first>
  <action>Build the thinnest end-to-end Web host slice. (1) web/src/monaco-adapter.ts: replace the flat `export const PROFILES = Object.freeze(['2.1','3.x','4.x'])` (monaco-adapter.ts:4) with `export const PROFILES_BY_DIALECT = Object.freeze({ doris: ['2.1','3.x','4.x'], flink: ['flink-2.3.0','flink-2.1.3','flink-1.20.5'] })`; update validateSelection (monaco-adapter.ts:92-96) to `const allowed = PROFILES_BY_DIALECT[dialect]; if (!DIALECTS.includes(dialect) || !allowed || !allowed.includes(profile)) throw new Error(MISSING_SELECTION)` (RESEARCH Common Operation 4 blueprint). (2) Add a `complete(source, dialect, profile, cursorByte)` method to ParserAdapter calling `module.fathom_complete_v1(utf8Bytes(source), dialect, profile, cursorByte)` through the same decodeResult envelope — the Web host gains the completion surface from 13-04. (3) web/src/main.ts: replace the profile dropdown population loop (`for (const profile of PROFILES)`, main.ts:36-41) with a function that repopulates the profile `<select>` from PROFILES_BY_DIALECT[dialect] when the dialect `<select>` changes — flink profile values appear only when flink is selected; wire it into the existing selectionChanged handler so a dialect change clears/repopulates the profile list and a missing profile surfaces the MISSING_SELECTION error (D-02 no-default). (4) web/src/main.test.ts: update the fakeArtifact to include fathom_complete_v1; add assertions: validateSelection rejects a doris-shaped profile under flink (e.g. ('flink','2.1') -> MISSING_SELECTION) and accepts ('flink','flink-2.3.0'); the complete() method calls module.fathom_complete_v1(raw, 'flink', 'flink-2.3.0', cursorByte) and returns the decoded envelope. (5) web/scripts/offline-smoke.mjs: replace the flat-profile assertion at :25 (`['2.1','3.x','4.x'].filter(...)`) with per-dialect assertions: PROFILES_BY_DIALECT.doris deep-equals ['2.1','3.x','4.x'] and PROFILES_BY_DIALECT.flink deep-equals ['flink-2.3.0','flink-2.1.3','flink-1.20.5'] — same commit as the adapter change (Pitfall 5).
  <acceptance_criteria>
    - web/src/monaco-adapter.ts exports PROFILES_BY_DIALECT with exactly { doris: ['2.1','3.x','4.x'], flink: ['flink-2.3.0','flink-2.1.3','flink-1.20.5'] }; validateSelection accepts only valid (dialect, profile) pairs and throws MISSING_SELECTION otherwise (including ('flink','2.1') and ('doris','flink-2.3.0')).
    - ParserAdapter.complete(source, dialect, profile, cursorByte) calls module.fathom_complete_v1(utf8Bytes(source), dialect, profile, cursorByte) and returns the decoded fathom.complete.v1 envelope.
    - web/src/main.ts profile dropdown repopulates from PROFILES_BY_DIALECT[dialect] on dialect change — flink profile values appear only when flink is selected; a missing/unsupported profile surfaces MISSING_SELECTION (D-02).
    - web/src/main.test.ts passes: pair validation rejects cross-dialect profiles, complete() calls the export with the A4 args.
    - web/scripts/offline-smoke.mjs passes with the per-dialect assertions (no stale flat list).
    - `cd web && npm test && npm run build` (offline smoke) passes.
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm test && npm run build</automated>
  </verify>
  <done>The Web/Monaco host validates (dialect, profile) pairs per dialect, gains the fathom_complete_v1 completion surface, repopulates the profile dropdown on dialect change, and its harness asserts the per-dialect pairs in the same commit.</done>
</task>

<task type="auto">
  <name>VS Code host — PROFILES_BY_DIALECT + pair validation + launch-smoke same-commit</name>
  <files>vscode/src/extension-contract.ts, vscode/src/extension.ts, vscode/scripts/launch-smoke.mjs</files>
  <read_first>
    @.planning/phases/13-toolchain-and-editor-packaging/13-RESEARCH.md
    @/opt/source/Fathom/vscode/src/extension-contract.ts
    @/opt/source/Fathom/vscode/src/extension.ts
    @/opt/source/Fathom/vscode/scripts/launch-smoke.mjs
  </read_first>
  <action>Update the VS Code host to the (dialect, profile) pair validation. (1) vscode/src/extension-contract.ts: replace `SUPPORTED_PROFILES = Object.freeze(['2.1','3.x','4.x'])` (extension-contract.ts:3) with `export const PROFILES_BY_DIALECT = Object.freeze({ doris: ['2.1','3.x','4.x'], flink: ['flink-2.3.0','flink-2.1.3','flink-1.20.5'] })`; update resolveFathomConfiguration (extension-contract.ts:8-15) so the returned profile is valid for the selected dialect: `const allowed = PROFILES_BY_DIALECT[dialect]; const profile = allowed && allowed.includes(configuration.get('profile')) ? configuration.get('profile') : undefined` — a missing/unsupported pair yields undefined (the existing explicit-config-error surface, D-02), never a coerced default. (2) vscode/src/extension.ts: verify the initializationOptions {dialect, profile} shape is unchanged (it sends configuration.dialect + configuration.profile); if a profile-undefined case needs an explicit error message (e.g. "choose a valid profile for the selected dialect"), surface it through the existing explicit missing-selection error path — never start with a guessed dialect (D-02). (3) vscode/scripts/launch-smoke.mjs: update the assertions to the per-dialect contract — assert PROFILES_BY_DIALECT contains the flink values and that initializationOptions still carries {dialect, profile} (the assertion at :29 `initializationOptions: { dialect: configuration.dialect, profile: configuration.profile }` stays; add a flink profile value assertion) — same commit as the constants (Pitfall 5).
  <acceptance_criteria>
    - vscode/src/extension-contract.ts exports PROFILES_BY_DIALECT with the per-dialect pairs; resolveFathomConfiguration returns undefined for a cross-dialect profile (e.g. dialect flink + profile 2.1) and the configured profile for a valid pair (e.g. flink + flink-2.3.0).
    - vscode/src/extension.ts sends initializationOptions {dialect, profile} unchanged; a missing/unsupported pair surfaces an explicit error, never a guessed dialect (D-02).
    - vscode/scripts/launch-smoke.mjs asserts the per-dialect pairs + flink values and passes.
    - `cd vscode && npm run build` (tsc) passes; `node scripts/launch-smoke.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd vscode && npm run build && node scripts/launch-smoke.mjs</automated>
  </verify>
  <done>The VS Code host validates (dialect, profile) pairs per dialect with the explicit missing-selection error, keeps initializationOptions unchanged, and its launch-smoke asserts the per-dialect pairs in the same commit.</done>
</task>

<task type="auto">
  <name>IntelliJ host — per-dialect ALLOWED_PROFILES + ComboBox switch + source-smoke same-commit</name>
  <files>jetbrains/src/main/kotlin/fathom/jetbrains/sql/FathomSettings.kt, jetbrains/src/main/kotlin/fathom/jetbrains/sql/FathomSettingsConfigurable.kt, jetbrains/scripts/source-smoke.py</files>
  <read_first>
    @.planning/phases/13-toolchain-and-editor-packaging/13-RESEARCH.md
    @/opt/source/Fathom/jetbrains/src/main/kotlin/fathom/jetbrains/sql/FathomSettings.kt
    @/opt/source/Fathom/jetbrains/src/main/kotlin/fathom/jetbrains/sql/FathomSettingsConfigurable.kt
    @/opt/source/Fathom/jetbrains/scripts/source-smoke.py
  </read_first>
  <action>Update the IntelliJ host to the (dialect, profile) pair validation. (1) jetbrains/FathomSettings.kt: replace `ALLOWED_PROFILES: List<String> = listOf("2.1","3.x","4.x")` (FathomSettings.kt:59) with a per-dialect map: `val PROFILES_BY_DIALECT: Map<String, List<String>> = mapOf("doris" to listOf("2.1","3.x","4.x"), "flink" to listOf("flink-2.3.0","flink-2.1.3","flink-1.20.5"))`; update normalizeProfile (FathomSettings.kt:68) to take the dialect into account — `fun normalizeProfile(dialect: String, value: String?): String? = value?.trim()?.takeIf { PROFILES_BY_DIALECT[dialect]?.contains(it) == true }`; update the FathomSettings.update/normalize call sites to pass the dialect to normalizeProfile (the existing IllegalArgumentException on an invalid profile stays, but the message lists the selected dialect's values). (2) jetbrains/FathomSettingsConfigurable.kt: the profileCombo (FathomSettingsConfigurable.kt:15) currently uses `FathomSettings.ALLOWED_PROFILES.toTypedArray()` — change it to repopulate from PROFILES_BY_DIALECT when dialectCombo changes: add a dialectCombo action listener that rebuilds the profileCombo's model from `FathomSettings.PROFILES_BY_DIALECT[dialect]` and clears/reselects appropriately; the apply()/reset() paths call normalizeDialect + normalizeProfile(dialect, ...) with the dialect (FathomSettingsConfigurable.kt:41-69). (3) jetbrains/scripts/source-smoke.py: update the required pattern at :76 — the `listOf("2.1","3.x","4.x")` requirement becomes a per-dialect map requirement: require `PROFILES_BY_DIALECT` with `listOf("2.1","3.x","4.x")` and `listOf("flink-2.3.0","flink-2.1.3","flink-1.20.5")` — same commit as FathomSettings.kt (Pitfall 5).
  <acceptance_criteria>
    - FathomSettings.kt exports PROFILES_BY_DIALECT: Map<String, List<String>> with doris -> ['2.1','3.x','4.x'] and flink -> ['flink-2.3.0','flink-2.1.3','flink-1.20.5']; normalizeProfile(dialect, value) validates against the selected dialect's list (flink + '2.1' -> null; flink + 'flink-2.3.0' -> 'flink-2.3.0').
    - FathomSettingsConfigurable.kt profileCombo repopulates when dialectCombo changes — flink profile values appear only for flink; apply()/reset() use normalizeProfile with the dialect.
    - jetbrains/scripts/source-smoke.py passes with the per-dialect map assertions (no stale flat list).
    - `cd jetbrains && ./gradlew test` passes (or the Gradle test task for the settings/Configurable unit behavior); `python3 scripts/source-smoke.py` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd jetbrains && ./gradlew compileKotlin test && python3 scripts/source-smoke.py</automated>
  </verify>
  <done>The IntelliJ host validates (dialect, profile) pairs per dialect with the profile ComboBox repopulating on dialect change, and its source-smoke asserts the per-dialect pairs in the same commit.</done>
</task>

</tasks>

## Source Audit Coverage

| SOURCE | ID | Feature or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | User can use the same dialect-aware API/schema through Web/Monaco, VS Code, IntelliJ; each host selects Doris or Flink per file/session | 13-06 | COVERED | (dialect, profile) pair validation in all three hosts + per-file override verification. |
| REQ | TOOL-05 | Same dialect-aware API/schema/LSP from Web/Monaco, VS Code, IntelliJ; hosts pick Doris or Flink per file/session, no second parser | 13-06 | COVERED | PROFILES_BY_DIALECT + fathom_complete_v1 (Web) + shared LSP/wire calls; harness same-commit. |
| CONTEXT | D-05 | Web/VS Code/IntelliJ keep static constants but validate (dialect, profile) pairs; profile dropdown switches with dialect; server authoritative (reversible) | 13-06 | COVERED | Tasks 2-4: per-dialect maps + dropdown repopulation + server-authoritative note. |
| CONTEXT | D-06 | Keep locked selection model; per-file didOpen/didChange override; no auto-detection (one-way) | 13-06 | COVERED | Task 1 checkpoint (one-way) + per-file override verification in 13-05 + flink values pass host validation here. |
| RESEARCH | Common Operation 4 | PROFILES_BY_DIALECT blueprint + validateSelection | 13-06 | COVERED | Task 2 Web tracer. |
| RESEARCH | Pitfall 5 | harness assertions pin the OLD flat list; same-commit update | 13-06 | COVERED | offline-smoke/launch-smoke/source-smoke updated in the same tasks. |
| RESEARCH | §3.5 | 4 host files + 3 harness assertions change in the same commit | 13-06 | COVERED | Tasks 2-4 each pair a host constant with its harness. |
| RESEARCH | A4 | LSP hosts tolerate the flink format/completion behavior change | 13-06 | COVERED | Hosts handle the three response shapes (13-05 tests) — host-side tolerance verified in 13-07 smoke. |
| REQ | PARITY-03 | offline discipline — no network in host validation | 13-06 | COVERED | Static constants only, no dynamic pull; harnesses are offline. |
| PROBE | TOOL-05 host parity | cross-host behavioral parity for flink files | 13-06 | FLAGGED | must_haves flagged-unverified truth; verified in 13-07 packaging smoke. |

<verification>
Run the web test/build (cd web && npm test && npm run build), the VS Code build + launch-smoke (cd vscode && npm run build && node scripts/launch-smoke.mjs), and the IntelliJ Gradle + source-smoke (cd jetbrains && ./gradlew compileKotlin test && python3 scripts/source-smoke.py) after each host task. Inspect: PROFILES_BY_DIALECT per-dialect maps, validateSelection/normalizeProfile pair validation, profile dropdown/ComboBox repopulation on dialect change, fathom_complete_v1 call in the Web adapter, initializationOptions shape unchanged, harness assertions updated in the same commit.
</verification>

<success_criteria>
- Web/Monaco, VS Code, and IntelliJ validate (dialect, profile) pairs per dialect with flink values appearing only when flink is selected.
- The server remains authoritative; host-side failures surface explicit errors, never coercion or fallback.
- Hosts keep static constants (offline, no dynamic pull); harness assertions are updated in the same commit; no host maintains a second parser.
</success_criteria>

<output>Create `.planning/phases/13-toolchain-and-editor-packaging/13-06-SUMMARY.md` when this plan executes.</output>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host UI selection -> static PROFILES_BY_DIALECT | The user's (dialect, profile) choice crosses here; a stale flat list would reject or accept the wrong values. |
| Host -> shared wire/LSP surface | Hosts call fathom_*_v1 / fathom-lsp; a host-side validation gap could pass an invalid pair to the server. |
| Host profile dropdown -> user | The dropdown must show the selected dialect's profiles; a cross-dialect value misleads the user. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-06-01 | Tampering | Host accepts a cross-dialect profile (flink + '2.1') and sends it to the server | high | mitigate | Per-dialect PROFILES_BY_DIALECT + pair validation in validateSelection/normalizeProfile; server re-validates (defense in depth, D-05); tests reject cross-dialect pairs. |
| T-13-06-02 | Tampering | Host coerces or defaults a profile on validation failure (silent fallback) | high | mitigate | Explicit MISSING_SELECTION / config error on any invalid pair; no normalizeProfile-style fallback (D-02, D-05); tests assert the explicit error. |
| T-13-06-03 | Tampering | Host dynamically pulls the profile list (fathom_capabilities_v1) or shares cross-host JSON, coupling hosts or adding network | medium | mitigate | Static constants only per D-05; offline-first (PARITY-03); no network dependency in host validation. |
| T-13-06-04 | Tampering | Harness assertion pins the old flat list, masking a D-05 drift or failing after the legit change | high | mitigate | Same-commit rule: each host constant change updates its harness assertion (offline-smoke/launch-smoke/source-smoke) (Pitfall 5). |
| T-13-06-05 | Tampering | Host auto-detects dialect or guesses by extension, violating the locked selection model | high | mitigate | D-06 checkpoint confirms the transport stays unchanged; no auto-detection/extension guessing; flink values only via explicit selection (D-01). |
| T-13-06-06 | Tampering | A host maintains a second parser, diverging from the shared wire/LSP surface | high | mitigate | All hosts call fathom_*_v1 / fathom-lsp only; no local parse implementation (TOOL-05, D-04). |
| T-13-06-07 | Spoofing | Host-side validation gap lets a flink file be analyzed under Doris policy | high | mitigate | Server-authoritative validate_selection/validate_dialect_profile; per-file override verified on flink files (13-05); host pair validation is defense in depth (D-05, D-06). |
| T-13-06-SC | Tampering | npm/pip/cargo installs | low | accept | All host deps already pinned (monaco-editor, vscode-languageclient, LSP4IJ — RESEARCH Package Legitimacy Audit approved). |
</threat_model>

## Artifacts this plan produces

Files: web/src/monaco-adapter.ts (modified — PROFILES_BY_DIALECT + validateSelection pair check + complete() calling fathom_complete_v1), web/src/main.ts (modified — profile dropdown repopulation on dialect change), web/src/main.test.ts (modified — pair-validation + complete assertions), web/scripts/offline-smoke.mjs (modified — per-dialect assertions), vscode/src/extension-contract.ts (modified — PROFILES_BY_DIALECT + pair validation in resolveFathomConfiguration), vscode/src/extension.ts (modified only if the undefined-profile case needs an explicit error message), vscode/scripts/launch-smoke.mjs (modified — per-dialect assertions), jetbrains/FathomSettings.kt (modified — PROFILES_BY_DIALECT + normalizeProfile(dialect, value)), jetbrains/FathomSettingsConfigurable.kt (modified — profileCombo repopulation on dialectCombo change), jetbrains/scripts/source-smoke.py (modified — per-dialect map assertions).

Symbols (web/src/monaco-adapter.ts): PROFILES_BY_DIALECT (frozen per-dialect map); validateSelection(dialect, profile) per-dialect; ParserAdapter.complete(source, dialect, profile, cursorByte) -> fathom_complete_v1 call.
Symbols (web/src/main.ts): repopulateProfileOptions(dialect) reading PROFILES_BY_DIALECT; dialect change listener.
Symbols (vscode/src/extension-contract.ts): PROFILES_BY_DIALECT; resolveFathomConfiguration returns profile only when valid for the dialect.
Symbols (jetbrains/FathomSettings.kt): PROFILES_BY_DIALECT: Map<String, List<String>>; normalizeProfile(dialect: String, value: String?): String?.
Symbols (jetbrains/FathomSettingsConfigurable.kt): dialectCombo action listener repopulating profileCombo.
Symbols (harnesses): offline-smoke.mjs PROFILES_BY_DIALECT.doris/flink deep-equal assertions; launch-smoke.mjs per-dialect + flink value assertions; source-smoke.py PROFILES_BY_DIALECT map requirements.
