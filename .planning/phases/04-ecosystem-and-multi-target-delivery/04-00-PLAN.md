---
phase: 04-ecosystem-and-multi-target-delivery
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: ["binding/schema_test.mbt", "binding/coordinates_test.mbt", "lsp/lifecycle_test.mbt", "lsp/framing_test.mbt", "lsp/diagnostics_formatting_test.mbt", "binding/export_smoke_test.mbt", "parity/parity_test.mbt", "parity/run_native.mbt", "parity/run_js.mbt", "parity/run_wasm.mbt", "web/src/main.test.ts", "web/scripts/offline-smoke.mjs", "vscode/src/extension.test.ts", "vscode/scripts/launch-smoke.mjs"]
autonomous: true
requirements: [ECO-01, ECO-02, ECO-03, ECO-04, ECO-05, ECO-06, ECO-07]
estimate:
  tokens: 24000
  raw_tokens: 12000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "Every later plan has a runnable focused harness rather than a placeholder verification."
    - "The harnesses decode and compare the shared doris.parse.v1/doris.format.v1 contract, including source bytes, trivia, error leaves, and coordinates."
  artifacts: ["binding/schema_test.mbt", "parity/parity_test.mbt", "web/scripts/offline-smoke.mjs", "vscode/scripts/launch-smoke.mjs"]
  key_links: ["focused harnesses -> later implementation commands", "decoded parity fixtures -> all target runners"]
<objective>Establish the focused schema, LSP, parity, Web, and VS Code smoke harnesses required by all Phase 4 implementation plans.</objective>
<execution_context>
@.planning/phases/04-ecosystem-and-multi-target-delivery/04-RESEARCH.md
@.planning/phases/04-ecosystem-and-multi-target-delivery/04-CONTEXT.md
</execution_context>
<tasks>
<task type="auto"><name>Task 1: Create MoonBit schema, LSP, export, and parity harnesses</name><files>binding/schema_test.mbt, binding/coordinates_test.mbt, lsp/lifecycle_test.mbt, lsp/framing_test.mbt, lsp/diagnostics_formatting_test.mbt, binding/export_smoke_test.mbt, parity/parity_test.mbt, parity/run_native.mbt, parity/run_js.mbt, parity/run_wasm.mbt</files><action>Create deterministic focused tests and runners for the frozen primitive schemas, UTF-16/CRLF/multibyte coordinates, framed lifecycle, diagnostics/formatting, exports, and decoded cross-target parity. Fixtures must assert source transport, trivia leaves, error/missing leaves, spans, diagnostics, profile metadata, and doris.parse.v1/doris.format.v1 fields. Keep all checks offline and use no external packages.</action><verify><automated>moon test --target native --filter 'schema|coordinates|lifecycle|framing|diagnostics|formatting|export|parity'</automated></verify><done>All referenced MoonBit test paths exist and fail on schema, transport, coordinate, lifecycle, or parity regressions.</done></task>
<task type="auto"><name>Task 2: Create Web and VS Code host smoke harnesses</name><files>web/src/main.test.ts, web/scripts/offline-smoke.mjs, vscode/src/extension.test.ts, vscode/scripts/launch-smoke.mjs</files><action>Create host-level deterministic harnesses that exercise relative offline artifact loading/reload failure, profile propagation, diagnostics, formatting refusal, accessibility state hooks, and VS Code client lifecycle/profile propagation/unavailable-server behavior. The VS Code harness must be protocol/host-contract based and must not claim a local VS Code executable exists.</action><verify><automated>node --test web/src/main.test.ts vscode/src/extension.test.ts && node web/scripts/offline-smoke.mjs --offline && node vscode/scripts/launch-smoke.mjs --protocol</automated></verify><done>Web and VS Code later tasks have runnable commands plus an explicit human-hosted VS Code checkpoint for final lifecycle validation.</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| test fixtures -> adapters | fixture bytes and malformed protocol inputs |
| host harness -> local artifacts | generated outputs and executable paths |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-00 | Tampering | harness inputs | medium | mitigate | deterministic bounded fixtures and decoded field assertions |
| T-04-00-SC | Tampering | npm/pip/cargo installs | high | mitigate | no installs in Wave 0; external packages remain human-gated |
</threat_model>
<verification>Run `moon test --target native --package binding --package lsp --package parity`, then run the Web/VS Code host smoke commands from Task 2. No project-wide test suite or external package installation is required.</verification>
<success_criteria>Every subsequent Phase 4 verification references an executable harness created here.</success_criteria>
<output>Create .planning/phases/04-ecosystem-and-multi-target-delivery/04-00-SUMMARY.md when done.</output>
