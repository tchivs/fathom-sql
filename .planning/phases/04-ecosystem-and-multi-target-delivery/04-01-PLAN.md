---
phase: 04-ecosystem-and-multi-target-delivery
plan: 01
type: execute
wave: 1
depends_on: [04-00]
files_modified: ["binding/schema.mbt", "binding/json.mbt", "binding/coordinates.mbt", "lsp/moon.pkg", "lsp/framing.mbt", "lsp/protocol.mbt", "lsp/documents.mbt", "lsp/coordinates.mbt", "lsp/handlers.mbt", "lsp/main.mbt", "parity/fixtures/lsp-tracer.json"]
autonomous: true
requirements: [ECO-01, ECO-02, ECO-05]
estimate:
  tokens: 36000
  raw_tokens: 18000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A Native stdio server completes initialize, full-content open/change/close, diagnostics, formatting, shutdown, and exit without FE/database/network. D-03 D-08"
    - "The public envelope explicitly versions profile, source transport, CST/trivia/spans, diagnostics, and format results; unsupported schema versions are reported. D-04 D-06"
    - "UTF-8 byte spans remain authoritative and one converter produces correct UTF-16 LSP ranges for CRLF and multibyte source. D-05"
  artifacts: ["binding/schema.mbt", "lsp/main.mbt", "lsp/framing.mbt", "lsp/coordinates.mbt", "parity/fixtures/lsp-tracer.json"]
  key_links: ["api.parse_with_ids -> schema serializer -> publishDiagnostics", "api.format_with_ids -> full-document TextEdit", "source byte spans -> centralized UTF-16 Position/Range"]
<objective>
Freeze the cross-target serialized/coordinate contract and deliver the first production vertical slice from Native JSON-RPC stdio through versioned document state, parser diagnostics, and safe full-document formatting edits.

Purpose: Establish the boundary all later wrappers and hosts consume, honoring D-01/D-02/D-03/D-04/D-05/D-06/D-08/D-10/D-13.
Output: Native `doris-lsp` executable, schema/coordinate adapters, and tracer fixtures.
</objective>
<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-ecosystem-and-multi-target-delivery/04-CONTEXT.md
@.planning/phases/04-ecosystem-and-multi-target-delivery/04-RESEARCH.md
@.planning/phases/03-formatting-and-safe-edits/03-01-SUMMARY.md
@.planning/phases/03-formatting-and-safe-edits/03-04-SUMMARY.md
</execution_context>
<tasks>
<task type="tracer">
<name>Task 1: Freeze serialized envelope and coordinate policy</name>
<files>binding/schema.mbt, binding/json.mbt, binding/coordinates.mbt, parity/fixtures/lsp-tracer.json</files>
<action>Define the documented `doris.parse.v1` and `doris.format.v1` primitive envelopes from ParseResult/PrimitiveNode/PrimitiveDiagnostic/FormatResult, including profile validation, source byte transport, half-open byte spans, trivia/error leaves, diagnostics, formatting offsets, and explicit unsupported-version errors (D-04/D-06). Implement one byte-to-line/UTF-16 converter over SourceText/LineIndex with CRLF and surrogate-pair cases; do not expose MoonBit ADTs or treat statement_offsets as source ranges (D-05/D-10). Add representative valid, recovered, comment, non-ASCII, and refusal fixtures.
</action>
<verify><automated>moon test --target native --filter schema && moon test --target native --filter coordinates</automated></verify>
<done>Schema fields and transport rules are documented and tested; byte/UTF-16 fixtures agree exactly with expected ranges and reject unknown schema/profile IDs.</done>
</task>
<task type="auto">
<name>Task 2: Implement Native JSON-RPC LSP lifecycle and document synchronization</name>
<files>lsp/moon.pkg, lsp/framing.mbt, lsp/protocol.mbt, lsp/documents.mbt, lsp/main.mbt</files>
<action>Build a Native executable adapter using Content-Length CRLF framing, bounded byte reads, core JSON decoding, and protocol-safe malformed-message responses. Implement initialize/initialized/shutdown/exit, full-content didOpen/didChange/didClose, URI-to-raw-Bytes snapshots with strictly advancing versions, and explicit profile selection; confine libc/stdin/stdout/process IO to the executable boundary following doris-sql patterns (D-03/D-08/D-11). Never connect to FE, a database, HTTP, or a second parser.</action>
<verify><automated>moon test --target native --filter lifecycle && moon test --target native --filter framing</automated></verify>
<done>A real framed stdio session reaches initialized state, stores only the newest document version, handles malformed frames without crashing, and exits cleanly.</done>
</task>
<task type="auto">
<name>Task 3: Wire diagnostics and full-document formatting TextEdit</name>
<files>lsp/handlers.mbt, lsp/coordinates.mbt, parity/fixtures/lsp-tracer.json</files>
<action>On open/change, call api.parse_with_ids in editor mode and publish serialized diagnostics mapped through the shared UTF-16 converter. On document formatting, call api.format_with_ids with existing formatter options, return no edit plus diagnostics on refusal, and otherwise return one full-document TextEdit from byte 0 to source length; preserve comments/hints and do not fabricate granular ranges from statement_offsets (D-02/D-05/D-10).</action>
<verify><automated>moon test --target native --filter diagnostics_formatting</automated></verify>
<done>The tracer session publishes stable diagnostics and applies an accepted comment-preserving full-document edit while malformed/incomplete SQL remains unchanged and safely reported.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| editor process -> LSP stdio | hostile framed bytes, JSON, URI, and SQL input |
| LSP adapter -> api | untrusted document snapshots and profile IDs |
| serialized result -> client | public compatibility boundary |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-01 | Tampering | JSON-RPC framing | high | mitigate | strict Content-Length, UTF-8 validation, frame/input caps, malformed-message error responses |
| T-04-02 | Denial of service | document store/parser | high | mitigate | bounded frame and parser input sizes; newest-version-only processing |
| T-04-03 | Tampering | schema/profile envelope | high | mitigate | explicit schema/profile validation and no silent fallback |
| T-04-04 | Information disclosure | process adapter | medium | mitigate | no FE/database/network access; diagnostics limited to requested document |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | no installs in this plan; retain slopcheck policy |
</threat_model>
<verification>Run Wave 0 focused harnesses, targeted MoonBit suites for schema, coordinates, framing, lifecycle, diagnostics, and formatting, then build the Native executable and exercise a framed initialize/open/change/format/shutdown session with malformed input.</verification>
<success_criteria>Native LSP tracer is independently smoke-testable and its schema/coordinate contract is frozen for JS/Wasm and editor plans.</success_criteria>
<output>Create `.planning/phases/04-ecosystem-and-multi-target-delivery/04-01-SUMMARY.md` when done.</output>
