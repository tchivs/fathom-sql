---
phase: 04-ecosystem-and-multi-target-delivery
plan: 04
type: execute
wave: 4
depends_on: [04-00, 04-03]
files_modified: ["web/package.json", "web/index.html", "web/src/main.ts", "web/src/styles.css", "web/src/monaco-adapter.ts", "vscode/package.json", "vscode/src/extension.ts", "vscode/tsconfig.json", "vscode/README.md"]
autonomous: false
requirements: [ECO-06, ECO-07]
user_setup: []
estimate:
  tokens: 38000
  raw_tokens: 19000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Offline Web/Monaco demo loads relative JS/Wasm assets, requires visible 2.1/3.x/4.x profile, shows diagnostics and comment-preserving formatting without network/database. D-11 D-12"
    - "Web UI follows approved ECO-06 contract for loading/error/partial/refusal/responsive/accessibility states and never exposes raw protocol/internal types."
    - "VS Code extension launches local Native LSP over standard stdio client lifecycle, exposes diagnostics/formatting/completion, and leaves documents usable when server startup fails. D-12"
  artifacts: ["web/index.html", "web/src/main.ts", "web/src/styles.css", "vscode/package.json", "vscode/src/extension.ts"]
  key_links: ["relative generated artifact -> JS facade -> Monaco diagnostics/format", "VS Code activation -> local doris-lsp stdio -> standard LSP client"]
<objective>Deliver the approved offline Web/Monaco demonstration and VS Code client as thin hosts over the proven JS/Wasm and Native LSP contracts.</objective>
<execution_context>
@.planning/phases/04-ecosystem-and-multi-target-delivery/04-CONTEXT.md
@.planning/phases/04-ecosystem-and-multi-target-delivery/04-RESEARCH.md
@.planning/phases/04-ecosystem-and-multi-target-delivery/04-UI-SPEC.md
@.planning/phases/04-ecosystem-and-multi-target-delivery/04-03-PLAN.md
</execution_context>
<tasks>
<task type="checkpoint:human-verify" gate="blocking-human">
<name>Task 1: Approve external editor dependencies before installation</name>
<what-built>Dependency decision point for `monaco-editor`, `vscode-languageclient`, and optional `@vscode/vsce`; no package is installed before this gate.</what-built>
<how-to-verify>Review each package on its official registry/source, confirm legitimacy and permitted versions, or select a dependency-free/static alternative for the Web host and an approved standard client strategy for VS Code. Record the decision in the plan summary before proceeding.</how-to-verify>
<resume-signal>Type approved with package names/versions, or specify the dependency-free alternative.</resume-signal>
</task>
<task type="auto">
<name>Task 2: Implement offline Web/Monaco host and approved UI contract</name>
<files>web/package.json, web/index.html, web/src/main.ts, web/src/styles.css, web/src/monaco-adapter.ts</files>
<action>Build a static relative-asset Web host around the generated JS/Wasm facade. Implement explicit Doris profile select (2.1/3.x/4.x, no Auto), 150ms parse debounce, local loading/error/reload states, Monaco diagnostics and full-document formatting, recoverable/incomplete SQL, exact fixed copy, and accessible textual diagnostic list. Follow 04-UI-SPEC: neutral light tooling surface, 44px controls, visible labels/focus rings, non-color severity cues, responsive 320/768/1280 layouts, keyboard order, live-region status, reduced-motion, and no service/network/database/auth (D-12).</action>
<verify><automated>node --test web/src/main.test.ts && node web/scripts/offline-smoke.mjs --offline</automated></verify>
<done>With network unavailable the demo reaches Parser ready from relative assets, each profile changes serialized metadata, diagnostics navigate to UTF-16 positions, accepted formatting preserves comments/hints, refusal leaves source untouched, artifact failure offers Reload demo, and approved checkpoints are exercised at widths 320/768/1280 with keyboard-only flow, live-region announcements, non-color severity cues, long UTF-16/multibyte coordinates, and local artifact failure/reload.</done>
</task>
<task type="auto">
<name>Task 3: Implement VS Code stdio client lifecycle and UX fallback</name>
<files>vscode/package.json, vscode/src/extension.ts, vscode/tsconfig.json, vscode/README.md</files>
<action>Implement a standard LanguageClient extension that launches the local doris-lsp executable over stdio, passes the configured explicit profile, registers Doris document selector, starts/disposes in activate/deactivate, and delegates diagnostics, formatting, and native completion. Use standard Problems/suggestion/status surfaces; on missing/exiting executable show the fixed actionable failure message while leaving documents as plain text. Keep VS Code packaging separate from MoonBit and Web runtime assumptions (D-11/D-12).</action>
<verify><automated>node --test vscode/src/extension.test.ts && node vscode/scripts/launch-smoke.mjs --protocol</automated></verify>
<done>Extension lifecycle, profile propagation, diagnostics, Format Document, completion, disposal, and unavailable-server fallback are covered by tests and a local VS Code smoke/manual check.</done>
<task type="checkpoint:human-verify" gate="blocking-human"><name>Task 4: Verify ECO-07 in a human-hosted VS Code</name><what-built>VS Code standard LanguageClient extension and local Native stdio server integration; this environment has no local VS Code executable.</what-built><how-to-verify>On a host with VS Code installed, launch the extension against the configured local doris-lsp executable. Verify activate/initialized, didOpen/didChange/didClose lifecycle, diagnostics in Problems with stable severity/code/UTF-16 ranges, Format Document preserving comments and hints, parser-known completion, unavailable/exiting server actionable message while the document remains editable, and explicit profile propagation from extension configuration to initialize/server requests.</how-to-verify><resume-signal>Type approved with the host/version and observed checks, or describe failures.</resume-signal></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser/VS Code host -> local artifact/server | host controls paths, profiles, and user text |
| npm dependencies -> build/runtime | third-party package supply chain |
| editor UI -> serialized/LSP data | untrusted diagnostics and messages displayed/applied |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-11 | Tampering | npm dependencies | high | mitigate | blocking human legitimacy checkpoint before every external package install; pin versions and review source |
| T-04-12 | Spoofing | local LSP executable path | high | mitigate | explicit configured path, stdio-only launch, actionable failure state, no remote fallback |
| T-04-13 | Tampering | formatting edits/UI | medium | mitigate | apply only accepted serialized full-document edits; refusal preserves source bytes |
| T-04-14 | Information disclosure | web host | medium | mitigate | relative local assets only; no network/database/FE/auth code |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | checkpoint is blocking and verifies npmjs/source legitimacy before install |
</threat_model>
<verification>After the dependency checkpoint, run host tests, build Web/VS Code packages, open the static demo with network blocked, exercise the UI checkpoint matrix from 04-UI-SPEC, and launch VS Code against local doris-lsp through stdio including unavailable-server fallback.</verification>
<success_criteria>Offline Web/Monaco and VS Code integrations satisfy ECO-06/ECO-07 and the approved accessibility/responsive contract without adding scope or runtime services.</success_criteria>
<output>Create `.planning/phases/04-ecosystem-and-multi-target-delivery/04-04-SUMMARY.md` when done.</output>
