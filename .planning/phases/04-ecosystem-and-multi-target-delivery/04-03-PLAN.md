---
phase: 04-ecosystem-and-multi-target-delivery
plan: 03
type: execute
wave: 3
depends_on: [04-00, 04-01, 04-02]
files_modified: ["binding/moon.pkg", "binding/exports.mbt", "binding/schema.mbt", "parity/fixtures/", "parity/parity_test.mbt", "parity/run_native.mbt", "parity/run_js.mbt", "parity/run_wasm.mbt"]
autonomous: true
requirements: [ECO-04, ECO-05]
estimate:
  tokens: 34000
  raw_tokens: 17000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "JS ESM and linear-Wasm wrappers accept explicit profile/text/options and return the frozen serialized results without MoonBit ADTs. D-04 D-07 D-11"
    - "Native, JS, and linear-Wasm outputs match shared parity fixtures for source bytes, schema/profile metadata, CST/trivia/spans, diagnostics, and formatting results. D-07"
    - "The byte transport is explicit and preserves supported raw source bytes at each host boundary. D-06"
  artifacts: ["binding/moon.pkg", "binding/exports.mbt", "parity/fixtures/", "parity/parity_test.mbt"]
  key_links: ["primitive api facade -> serialized envelope -> JS export", "primitive api facade -> serialized envelope -> linear-Wasm export", "same fixture inputs -> byte-for-byte permitted parity outputs"]
<objective>Expose the frozen schema through stable JavaScript ESM and linear-Wasm primitive wrappers, then prove cross-target parity with shared fixtures.</objective>
<execution_context>
@.planning/phases/04-ecosystem-and-multi-target-delivery/04-CONTEXT.md
@.planning/phases/04-ecosystem-and-multi-target-delivery/04-RESEARCH.md
@.planning/phases/03-formatting-and-safe-edits/03-01-SUMMARY.md
@.planning/phases/04-ecosystem-and-multi-target-delivery/04-01-PLAN.md
</execution_context>
<tasks>
<task type="tracer">
<name>Task 1: Build explicit JS ESM and linear-Wasm exports</name>
<files>binding/moon.pkg, binding/exports.mbt, binding/schema.mbt</files>
<action>Declare a dedicated foreign_library package with explicit JS ESM and linear-Wasm exports. Import the single authoritative shared schema producer from `binding/schema.mbt` used by Native LSP, Native wrapper, JS, and linear-Wasm; export only stable non-generic primitive functions for parse, format, profile/capability metadata, and serialized result bytes. Require decoded parity over `doris.parse.v1` and `doris.format.v1`, including source transport, source bytes, trivia/error leaves, spans, diagnostics, and formatting fields; never return internal ADTs (D-01/D-04/D-06/D-07/D-11).</action>
<verify><automated>moon test --target native --filter export_smoke</automated></verify>
<done>Native/JS/Wasm builds expose documented names, accept all three profiles, produce consumable serialized bytes, and have no parser/formatter fork or host IO dependency.</done>
</task>
<task type="auto">
<name>Task 2: Create shared parity fixture corpus and runners</name>
<files>parity/fixtures/, parity/run_native.mbt, parity/run_js.mbt, parity/run_wasm.mbt</files>
<action>Define fixture metadata for profiles 2.1/3.x/4.x, valid/recovered/error SQL, comments/hints, CRLF, non-ASCII, formatting acceptance/refusal, and explicit raw-byte transport cases. Run each through Native, JS, and linear-Wasm wrappers and normalize only target-permitted transport framing; compare schema version, profile metadata, source bytes/encoding, recursive nodes, diagnostics, spans, and formatting output byte-for-byte (D-07).</action>
<verify><automated>moon test --target native --filter parity</automated></verify>
<done>Fixture runner reports deterministic parity failures with fixture IDs and all supported cases pass across the three required targets.</done>
</task>
<task type="auto">
<name>Task 3: Add target build and ABI regression gates</name>
<files>parity/parity_test.mbt, binding/moon.pkg, parity/run_native.mbt, parity/run_js.mbt, parity/run_wasm.mbt</files>
<action>Add repeatable target checks for native, js, and wasm artifacts, schema-version/profile rejection, malformed input handling, empty input, and no accidental internal-type exposure. Record toolchain/target metadata in parity output; do not add Wasm GC compatibility promise unless a separate exact runtime smoke is present (D-07).</action>
<verify><automated>moon check --target native && moon build --target js && moon build --target wasm</automated></verify>
<done>All three required target builds and parity gates pass, with linear-Wasm explicitly documented and Wasm GC absent from compatibility claims.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| host JS/Wasm caller -> foreign exports | arbitrary text/profile/options and byte buffers |
| serialized envelope -> consumer | public ABI and compatibility boundary |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-08 | Tampering | foreign export ABI | high | mitigate | explicit primitive exports, schema validation, no internal object exposure |
| T-04-09 | Denial of service | JS/Wasm parse wrapper | high | mitigate | enforce parser/input bounds and deterministic error envelope |
| T-04-10 | Information disclosure | source transport | medium | mitigate | explicit byte encoding and no hidden host memory handles |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | no package-manager install; wrapper uses MoonBit local tooling |
</threat_model>
<verification>Build native/js/wasm artifacts, execute all parity fixtures through each wrapper, inspect exported symbol surface, and run malformed/profile/version/byte-transport smoke cases.</verification>
<success_criteria>Stable cross-target schema and parity are proven before any browser or VS Code host consumes them.</success_criteria>
<output>Create `.planning/phases/04-ecosystem-and-multi-target-delivery/04-03-SUMMARY.md` when done.</output>
