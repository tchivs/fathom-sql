---
phase: 02-doris-completeness-and-corpus
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - parser/parser.mbt
  - token/token.mbt
  - syntax/syntax.mbt
  - api/api.mbt
  - test/ddl_test.mbt
autonomous: true
requirements: [DORIS-02]
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "[probe DORIS-02 unclassified] resolved: CREATE TABLE (full body: columns, keys, aggregation semantics, distribution, buckets, partitions incl. dynamic/AUTO partitions, properties) and CREATE VIEW / CTAS / CREATE TABLE LIKE / CREATE INDEX / sync CREATE MATERIALIZED VIEW parse per the released 4.x docs grammar with byte-exact lossless replay; per-clause version gates (ORDER BY since 4.1.0 verified; BUCKETS AUTO and AUTO PARTITION BY introduction ASSUMED per research A3) emit DORIS-PARSE-006 under older profiles (DORIS-02, D-10)."
    - "DDL clause recognition uses per-family sync predicates and consume_word; grammar words that are absent from the official reserved list (BUCKETS, PROPERTIES, COMMENT, AGGREGATE, ENGINE, ROLLUP) remain usable as unquoted identifiers outside their clause (DORIS-04 groundwork, research Pitfall 2)."
    - "A malformed DDL statement (unclosed paren, truncated partition clause) recovers at statement boundaries without swallowing later statements in the same document (DORIS-03 shape, research Pitfall 5)."
  flagged_assumptions:
    - "FLAGGED-ASSUMPTION [research A2]: CREATE ASYNC MATERIALIZED VIEW (with BUILD/REFRESH clauses, documented since ~2.1) is outside this plan's verified scope — only the sync form was read from released docs. Async MV remains an explicit DORIS-PARSE-007 unsupported statement until its docs page is read in the corpus wave (02-04) and a follow-up decision is made."
    - "FLAGGED-ASSUMPTION [research A3]: BUCKETS AUTO introduced in 3.x and AUTO PARTITION BY introduced in 2.1 are ASSUMED from doc-tree presence, not verified per-clause this session; gates use these assumptions and the corpus wave (02-04) MUST verify against the reachable 2.1/3.x CREATE TABLE pages and amend gates or fixtures."
    - "FLAGGED-ASSUMPTION [research A4]: column-type introduction versions are assumed — VARBINARY 'since 4.0, not for table storage' is cited in docs; TIMESTAMPTZ introduction is assumed; per-type introduction rows are recorded in the classification table (02-03) and verified during the corpus wave."
    - "FLAGGED-ASSUMPTION [research A8]: docs-site reachability at execution time is not guaranteed; DDL fixtures are written from the verbatim grammars in 02-RESEARCH.md with official_url and retrieval_date recorded and unavailable-offline revision when the site cannot be re-read."
  artifacts:
    - path: "parser/parser.mbt"
      provides: "CREATE dispatch (TEMPORARY/EXTERNAL TABLE, VIEW, INDEX, MATERIALIZED VIEW), parse_create_table/parse_create_view/parse_create_index/parse_create_materialized_view, per-family DDL sync predicates"
    - path: "token/token.mbt"
      provides: "DorisFeature::OrderByClause (4.x, docs note since 4.1.0), DorisFeature::BucketsAuto (assumed 3.x, FLAGGED), DorisFeature::AutoPartitionBy (assumed 2.1, FLAGGED)"
    - path: "syntax/syntax.mbt"
      provides: "CreateTable/CreateView/CreateIndex/CreateMaterializedView/ColumnDefinition/KeyClause/DistributionClause/PartitionClause/PropertyList SyntaxKind variants"
    - path: "api/api.mbt"
      provides: "kind_id arms for the new DDL kinds"
    - path: "test/ddl_test.mbt"
      provides: "DDL statement, version-gate, recovery, replay, and script tests"
  key_links:
    - from: "parser/parser.mbt"
      to: "token/token.mbt"
      via: "ORDER BY / BUCKETS AUTO / AUTO PARTITION BY clauses consult DorisFeature gates and emit DORIS-PARSE-006 with version_invalid_node leaves"
    - from: "parser/parser.mbt"
      to: "syntax/syntax.mbt"
      via: "DDL nodes built through SyntaxNode::new with touching in-bounds spans; lossless replay depends on every consumed token becoming a leaf"
    - from: "api/api.mbt"
      to: "syntax/syntax.mbt"
      via: "exhaustive kind_id match forces arms for every new DDL kind by compilation"
    - from: "test/ddl_test.mbt"
      to: "printer/printer.mbt"
      via: "every fixture asserts print_result == raw and all_spans_in_bounds"
  prohibitions:
    - "No DDL grammar word (BUCKETS/PROPERTIES/COMMENT/AGGREGATE/ENGINE/ROLLUP) is added to is_reserved_word — non-reserved words must stay usable as unquoted identifiers (research Pitfall 2)."
    - "No name resolution or catalog access inside parse_create (D-05/D-21): DDL parsing is purely syntactic."
    - "No silent acceptance of version-invalid DDL clauses (D-12/D-15)."
---

<objective>
**As a** Doris SQL consumer, **I want to** parse warehouse DDL — full CREATE TABLE (keys, aggregation, distribution, buckets, partitions, dynamic/AUTO partitions, properties), CREATE VIEW, CTAS, CREATE TABLE LIKE, CREATE INDEX, and sync CREATE MATERIALIZED VIEW — under an explicit profile, **so that** warehouse schemas round-trip losslessly with version-aware diagnostics instead of silent acceptance.

Purpose: Deliver the DORIS-02 surface in the locked priority order (D-10): full CREATE TABLE first, then VIEW/CTAS/LIKE, then INDEX/sync-MV, all under introduced_profile-style gates (D-15) reusing the dispatch table from 02-01. Every new SyntaxKind obeys the Phase 1 span invariants so print_lossless(parse(x)) == x holds for DDL (research Pitfall 4).
Output: CREATE dispatch arms, four DDL statement parsers, reusable clause kinds, per-family sync predicates, gate rows, kind_id arms, and executable DDL tests.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-doris-completeness-and-corpus/02-CONTEXT.md
@.planning/phases/02-doris-completeness-and-corpus/02-RESEARCH.md
@.planning/phases/02-doris-completeness-and-corpus/02-PATTERNS.md
@.planning/phases/02-doris-completeness-and-corpus/02-01-PLAN.md
</execution_context>

<context>
02-01 delivered the keyword-first dispatch table and DML parsers; this plan adds the CREATE arm and DDL families. Grammar authority is the verbatim 4.x CREATE TABLE / CREATE VIEW / CREATE INDEX / CREATE SYNC MATERIALIZED VIEW grammars in 02-RESEARCH.md (including the CTAS and LIKE variants, column types, and the 'ORDER BY supported since 4.1.0' note). Version gates: ORDER BY in CREATE TABLE is documented since 4.1.0 (within the 4.x family → introduced_profile "4.x"); BUCKETS AUTO and AUTO PARTITION BY introduction versions are ASSUMED (research A3, FLAGGED) pending 02-04 verification. Column-type recognition covers the documented families (BOOLEAN; TINYINT/SMALLINT/INT/BIGINT/LARGEINT; FLOAT/DOUBLE; DECIMAL with precision/scale; DATE/TIME/DATETIME/TIMESTAMPTZ; CHAR(M)/VARCHAR(M)/STRING; VARBINARY since 4.0 flagged; ARRAY/MAP/STRUCT/VARIANT/JSON; BITMAP/HLL/QUANTILE_STATE/AGG_STATE; IPv4/IPv6) with length/precision parameters. All DDL parsing stays purely syntactic — no catalog, no name resolution (D-05/D-21). The shared token is_clause_keyword and is_reserved_word stay untouched by DDL grammar words; clause recognition is per-family consume_word, and non-reserved words remain unquoted identifiers (research Pitfall 2). CREATE ASYNC MATERIALIZED VIEW remains an unsupported statement (DORIS-PARSE-007 from 02-01) until its docs page is read (FLAGGED A2).
</context>

<tasks>

<task type="tracer">
  <name>Task 1: CREATE TABLE full-body vertical slice</name>
  <files>parser/parser.mbt, token/token.mbt, syntax/syntax.mbt, api/api.mbt, test/ddl_test.mbt</files>
  <read_first>
    @.planning/phases/02-doris-completeness-and-corpus/02-RESEARCH.md
    @.planning/phases/02-doris-completeness-and-corpus/02-PATTERNS.md
    @.planning/phases/02-doris-completeness-and-corpus/02-01-PLAN.md
  </read_first>
  <action>Add the CREATE arm to the dispatch table (route CREATE to parse_create; do not let CREATE fall through to the unsupported path introduced in 02-01). Implement parse_create consuming the optional TEMPORARY / EXTERNAL modifiers, then TABLE, optional IF NOT EXISTS, table name, and the documented 4.x body: '(' columns_definition [ indexes_definition ] ')' — columns_definition is col_name col_type [ KEY ] [ aggregate_type for AGGREGATE models ] [ [ GENERATED ALWAYS ] AS ( expr ) ] [ [NOT] NULL ] [ AUTO_INCREMENT ( start ) ] [ DEFAULT value ] [ ON UPDATE CURRENT_TIMESTAMP [ ( precision ) ] ] [ COMMENT '...' ], comma-separated; indexes_definition is INDEX [ IF NOT EXISTS ] name ( col list ) [ USING type ] [ PROPERTIES ( ... ) ] [ COMMENT '...' ]. Column types recognized per the documented family list in the plan context (with CHAR(M)/VARCHAR(M)/DECIMAL(p,s)/ARRAY<...> parameter forms). Then optional clauses in documented order: ENGINE = type, key_type KEY ( cols ) where key_type is DUPLICATE | UNIQUE | AGGREGATE, optional ORDER BY ( cols ) gated by DorisFeature::OrderByClause (introduced_profile "4.x", message "feature CREATE TABLE ORDER BY is unavailable in the selected released profile (docs note: since 4.1.0)"), COMMENT '...', partitions_definition (AUTO PARTITION BY RANGE/LIST gated by DorisFeature::AutoPartitionBy assumed 2.1, or PARTITION BY type ( cols ), with one_partition_definition variants: VALUES LESS THAN list, VALUES [ lower, upper ), FROM lower TO upper INTERVAL n [ unit ], VALUES IN { ( list ) | value }), DISTRIBUTED BY HASH ( cols ) | RANDOM [ BUCKETS count | AUTO (gated by DorisFeature::BucketsAuto assumed 3.x) ], ROLLUP ( name ( cols ) [ DUPLICATE KEY ( cols ) ] , ... ), PROPERTIES ( key = value , ... ). Add SyntaxKind variants CreateTable, ColumnDefinition, KeyClause, DistributionClause, PartitionClause, PropertyList with span invariants, and kind_id arms "create_table", "column_definition", "key_clause", "distribution_clause", "partition_clause", "property_list". Per-family sync predicate is_create_table_clause_boundary covering KEY/ORDER/BY/DISTRIBUTED/BUCKETS/PARTITION/PROPERTIES/AS/LIKE/COMMENT/ENGINE — never the shared set. Create test/ddl_test.mbt with local strict/editor helpers using profile "4.x" and tests: one complete CREATE TABLE exercising every clause (columns with KEY/aggregate/DEFAULT/COMMENT, ENGINE, UNIQUE KEY + ORDER BY, partitions with VALUES LESS THAN, DISTRIBUTED BY HASH BUCKETS, ROLLUP, PROPERTIES) asserting strict valid, print_result == raw, all_spans_in_bounds; ORDER BY under 2.1/3.x → DORIS-PARSE-006 with version_invalid_node in editor; a column literally named bucket / comment / value / order parses as an unquoted identifier (Pitfall 2); malformed body with an unclosed '(' in a script (b"CREATE TABLE t (a INT; INSERT INTO t VALUES (1); SELECT * FROM t") asserts the later statements survive as separate Statement nodes (Pitfall 5).</action>
  <acceptance_criteria>
    - A full CREATE TABLE body (columns, indexes, ENGINE, keys + ORDER BY, partitions incl. AUTO PARTITION BY and one-partition variants, distribution + buckets, ROLLUP, properties) parses with byte-exact replay and span-valid CST (DORIS-02, D-10 priority 1).
    - ORDER BY under 2.1/3.x emits DORIS-PARSE-006 with a source-backed version_invalid_node; BUCKETS AUTO and AUTO PARTITION BY gates are wired with their FLAGGED assumed introduction versions (D-15, A3).
    - Grammar words absent from the reserved list (bucket, comment, value, order) work as unquoted column identifiers (research Pitfall 2).
    - An unclosed '(' in CREATE TABLE does not swallow later statements: the script test yields 3 Statement nodes with byte-exact replay (Pitfall 5, DORIS-03 shape).
    - The shared token clause/reserved sets are unchanged; Phase 1 and 02-01 tests stay green.
  </acceptance_criteria>
  <verify><automated>moon test</automated></verify>
  <done>The CREATE TABLE tracer is committed: full-body table DDL parses end-to-end under 4.x with gates, per-family sync, identifier-safe non-reserved words, and script-safe recovery.</done>
</task>

<task type="auto">
  <name>Task 2: CREATE VIEW, CTAS, and CREATE TABLE LIKE</name>
  <files>parser/parser.mbt, syntax/syntax.mbt, api/api.mbt, test/ddl_test.mbt</files>
  <read_first>
    @.planning/phases/02-doris-completeness-and-corpus/02-RESEARCH.md
    @.planning/phases/02-doris-completeness-and-corpus/02-PATTERNS.md
  </read_first>
  <action>Extend parse_create with the VIEW arm and the CTAS/LIKE TABLE variants per the documented 4.x grammars. CREATE VIEW [ IF NOT EXISTS ] [ db. ] view_name [ ( column_definition list: column_name [ COMMENT '...' ] ) ] [ AS ] query_stmt — reuse the existing parse_query for the body (a CTE-bodied view is a query and must parse; see flagged assumption on WITH view attributes). CTAS: after the optional table body clauses, an optional AS marker followed by a query (reuse parse_query), so CREATE [ EXTERNAL ] TABLE [ IF NOT EXISTS ] name [ ( column_defs ) ] [ index_defs ] [ ENGINE = ... ] [ key_type KEY ... ] [ COMMENT ] [ partitions ] [ DISTRIBUTED BY ... ] [ ROLLUP ... ] [ PROPERTIES ] [ AS ] query all compose. LIKE: CREATE TABLE new_table_name LIKE existing_table_name [ WITH ROLLUP ( rollup_list ) ]. Add SyntaxKind variants CreateView and kind_id arms "create_view"; reuse CreateTable for CTAS/LIKE (the AS-query and LIKE children are leaves under CreateTable). Extend test/ddl_test.mbt: CREATE VIEW with column comments, plain and CTE-bodied view bodies; CTAS with and without column definitions and with DISTRIBUTED/PROPERTIES prefix; CREATE TABLE LIKE with and without WITH ROLLUP; each asserts strict validity, print_result == raw, all_spans_in_bounds; malformed variants (view without body, LIKE with trailing garbage) recover with localized diagnostics and later statements survive.</action>
  <acceptance_criteria>
    - CREATE VIEW (with optional column definitions, AS keyword, plain and CTE-bodied query bodies) parses with lossless replay (DORIS-02, D-10 priority 2).
    - CTAS composes the full table-body clause stack with an optional AS query; CREATE TABLE LIKE parses with optional WITH ROLLUP (DORIS-02).
    - Every variant asserts byte-exact replay and span bounds; malformed DDL recovers at statement boundaries without discarding later statements.
  </acceptance_criteria>
  <verify><automated>moon test</automated></verify>
  <done>CREATE VIEW, CTAS, and CREATE TABLE LIKE parse with lossless replay and statement-safe recovery; no catalog or name resolution entered the parser.</done>
</task>

<task type="auto">
  <name>Task 3: CREATE INDEX and sync CREATE MATERIALIZED VIEW</name>
  <files>parser/parser.mbt, syntax/syntax.mbt, api/api.mbt, test/ddl_test.mbt</files>
  <read_first>
    @.planning/phases/02-doris-completeness-and-corpus/02-RESEARCH.md
    @.planning/phases/02-doris-completeness-and-corpus/02-PATTERNS.md
  </read_first>
  <action>Extend parse_create with the INDEX and MATERIALIZED VIEW arms per the documented 4.x grammars. CREATE INDEX [ IF NOT EXISTS ] index_name ON table_name ( column list ) [ USING INVERTED | NGRAM_BF | ANN ] [ PROPERTIES ( "key" = "value" , ... ) ] [ COMMENT '...' ]. CREATE MATERIALIZED VIEW mv_name [ AS ] query where query is the documented restricted sync-MV shape: SELECT expr list FROM base_table [ WHERE condition ] [ GROUP BY col list ] [ ORDER BY col list ] — per the docs constraint notes, reject JOIN, HAVING, LIMIT, and LATERAL VIEW in the MV body with a localized DORIS-PARSE-002-class diagnostic (expected_class "sync materialized view body") while keeping the rejected bytes source-backed in the CST; aggregate functions remain root expressions. Add SyntaxKind variants CreateIndex, CreateMaterializedView and kind_id arms "create_index", "create_materialized_view". Extend test/ddl_test.mbt: CREATE INDEX with USING INVERTED / NGRAM_BF with PROPERTIES / ANN with the documented hnsw example; sync CREATE MATERIALIZED VIEW with WHERE/GROUP BY/ORDER BY; negative MV bodies (JOIN, HAVING, LIMIT) assert the diagnostic and byte-exact replay; a script with CREATE INDEX followed by a SELECT asserts both statements survive; every input asserts print_result == raw and all_spans_in_bounds.</action>
  <acceptance_criteria>
    - CREATE INDEX parses the documented forms (INVERTED, NGRAM_BF with PROPERTIES, ANN) with lossless replay (DORIS-02, D-10 priority 3).
    - Sync CREATE MATERIALIZED VIEW parses the restricted single-table SELECT body; JOIN/HAVING/LIMIT bodies are rejected with a localized diagnostic while preserving bytes (DORIS-02, docs constraint notes).
    - Multi-statement documents mixing DDL and DML/SELECT keep all statements with distinct statement_ids and byte-exact replay (DORIS-03 shape).
  </acceptance_criteria>
  <verify><automated>moon test</automated></verify>
  <done>CREATE INDEX and sync CREATE MATERIALIZED VIEW parse with documented constraints; DORIS-02 is fully covered across the three DDL tasks.</done>
</task>

</tasks>

## Source Audit Coverage

| SOURCE | ID | Feature or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Warehouse-specific DML/DDL with localized errors | 02-02 | COVERED | Full DDL surface per D-10 order. |
| REQ | DORIS-02 | Tables, views, CTAS/LIKE, keys, aggregation, distribution, buckets, partitions, dynamic partitions, properties, indexes, MVs | 02-02 | COVERED | Task 1 (table body incl. partitions/AUTO), Task 2 (view/CTAS/LIKE), Task 3 (index/sync-MV). |
| RESEARCH | Pattern 1 | CREATE arm in keyword dispatch | 02-02 | COVERED | Task 1. |
| RESEARCH | Pattern 2 | Per-family DDL sync predicates, shared set untouched | 02-02 | COVERED | Tasks 1-3; prohibited in must_haves. |
| RESEARCH | Pattern 3 | ORDER BY since 4.1.0 gate; BUCKETS AUTO / AUTO PARTITION BY gates | 02-02 | COVERED | Task 1 DorisFeature rows; A3 flagged for 02-04 verification. |
| RESEARCH | Pitfall 2 | Keyword-as-identifier in DDL contexts | 02-02 | COVERED | Task 1 bucket/comment/value/order identifier tests. |
| RESEARCH | Pitfall 4 | Lossless replay for new statements | 02-02 | COVERED | print_result == raw on every fixture. |
| RESEARCH | Pitfall 5 | Recovery cascades across statements | 02-02 | COVERED | Task 1 unclosed-paren script test. |
| CONTEXT | D-10 | DDL order: CREATE TABLE → VIEW/CTAS/LIKE → INDEX/MV | 02-02 | COVERED | Task order. |
| CONTEXT | D-15 | introduced_profile gates | 02-02 | COVERED | ORDER BY / BUCKETS AUTO / AUTO PARTITION BY. |
| CONTEXT | D-21 | Parser stays catalog-free | 02-02 | COVERED | Prohibited name resolution; syntax-only. |
| CONTEXT | D-12 | Explicit unsupported nodes | 02-02 | COVERED | Async MV stays DORIS-PARSE-007 (flagged A2); JOIN/HAVING/LIMIT MV bodies get localized diagnostics. |

<verification>
Run `moon test` after each task. Inspect: CREATE dispatch, per-family DDL sync predicates, gate negatives (ORDER BY under 2.1/3.x, BUCKETS AUTO wiring), non-reserved identifier acceptance, unclosed-paren script recovery, kind_id arms, byte-exact replay on every DDL fixture, and unchanged Phase 1 / 02-01 tests. Do not install packages, run formatters/linters, or invoke FE/database/network services.
</verification>

<success_criteria>
- DORIS-02 is executable across the three DDL tasks in the locked D-10 order with byte-exact replay everywhere.
- D-15 gates fire for version-invalid DDL clauses; FLAGGED assumptions A2/A3/A4 are recorded for the corpus wave to verify.
- Non-reserved DDL grammar words remain usable as identifiers; the shared keyword sets are unchanged.
- The parser remains purely syntactic — no catalog, name resolution, or analyzer reference (D-05/D-21).
</success_criteria>

<output>Create `.planning/phases/02-doris-completeness-and-corpus/02-02-SUMMARY.md` when this plan executes.</output>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Untrusted DDL script → CREATE parser | Deep paren nesting, huge property lists, many partition clauses, and malformed table bodies are untrusted input. |
| Selected profile → DDL version gates | ORDER BY (4.1.0), BUCKETS AUTO (assumed 3.x), AUTO PARTITION BY (assumed 2.1) must not be silently accepted under older profiles. |
| MV body constraints | JOIN/HAVING/LIMIT in sync MV bodies must be rejected per docs, not accepted silently. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-11 | Denial of Service | CREATE TABLE body / nested parens / property floods | high | mitigate | Reuse ParseLimits (max_bytes, max_tokens, max_recursion_depth 128, max_recovery_steps, max_diagnostics); add adversarial DDL fixtures (deep parens, large PROPERTIES list) with bounded recovery. |
| T-02-12 | Tampering | DDL version-gate bypass | high | mitigate | DorisFeature::OrderByClause / BucketsAuto / AutoPartitionBy introduced_profile gates + DORIS-PARSE-006 + version_invalid_node + negative fixtures per profile (D-15). |
| T-02-13 | Integrity | Lossless replay broken by new DDL kinds | high | mitigate | Every DDL fixture asserts print_result == raw and all_spans_in_bounds; SyntaxNode::new span invariants; zero-width Missing only (research Pitfall 4). |
| T-02-14 | Denial of Service | CTAS/MV query-body recursion | medium | mitigate | Reuse the existing parse_query depth limits and expression recovery; MV-body constraint diagnostics are localized and bounded. |
| T-02-15 | Tampering | MV body constraints silently accepted | medium | mitigate | JOIN/HAVING/LIMIT rejection with localized DORIS-PARSE-002-class diagnostics; negative fixtures assert the code and replay. |
| T-02-SC | Tampering | Package installation | low | accept | No package installation task; pinned MoonBit toolchain only. |

## Artifacts this phase produces

Symbols (parser/parser.mbt): parse_create dispatch (TABLE/VIEW/INDEX/MATERIALIZED VIEW arms), parse_create_table (full body incl. CTAS/LIKE), parse_create_view, parse_create_index, parse_create_materialized_view (restricted sync-MV body), is_create_table_clause_boundary and per-family DDL sync predicates.
Symbols (token/token.mbt): DorisFeature::OrderByClause (4.x), DorisFeature::BucketsAuto (assumed 3.x, FLAGGED), DorisFeature::AutoPartitionBy (assumed 2.1, FLAGGED) with FeatureMetadata rows.
Symbols (syntax/syntax.mbt): SyntaxKind::CreateTable, CreateView, CreateIndex, CreateMaterializedView, ColumnDefinition, KeyClause, DistributionClause, PartitionClause, PropertyList.
Symbols (api/api.mbt): kind_id arms "create_table", "create_view", "create_index", "create_materialized_view", "column_definition", "key_clause", "distribution_clause", "partition_clause", "property_list".
Files: test/ddl_test.mbt (new).
</threat_model>
