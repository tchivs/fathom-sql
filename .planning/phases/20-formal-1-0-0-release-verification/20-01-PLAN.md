---
phase: 20-formal-1-0-0-release-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/vsce-publish.yml
autonomous: true
requirements:
  - VER-04
estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "VER-04 / D-01：`v1.0.0` annotated tag 推送触发 fathom-native-release（三平台资产 + 双 manifest + GitHub Release，notes=RELEASE-NOTES.md）。"
    - "D-03：vsce-publish 发布前画廊检查同版本已存在则跳过（fathom-sql.sql 1.0.0 已发布）。"
    - "D-04/D-05：npm 注册表 `@fathom/sql@1.0.0` 可见；发布后冒烟下载 linux 资产校验 SHA-256 并运行 `fathom-lsp --version` 断言 `fathom-lsp 1.0.0`。"
  artifacts:
    - path: ".github/workflows/vsce-publish.yml"
      provides: "幂等发布（版本已存在则跳过）"
  key_links:
    - from: "v1.0.0 tag"
      to: "fathom-native-release.yml / npm-publish.yml / vsce-publish.yml"
      via: "push tags v* 触发"
      pattern: "v1.0.0"
---

<objective>
正式打 `v1.0.0` tag 并推送，触发三条发布管线（原生 Release、npm、VS Code 市场），随后验证 Release 资产/双 manifest/notes 引用披露，并执行发布后冒烟（SHA-256 校验 + `fathom-lsp --version` 1.0.0）（VER-04，D-01..D-05）。
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-formal-1-0-0-release-verification/20-CONTEXT.md
@.github/workflows/fathom-native-release.yml
@.github/workflows/npm-publish.yml
@.github/workflows/vsce-publish.yml
@RELEASE-NOTES.md
</context>

<dependency_graph>
- Task 1 preps idempotency and creates the tag; Task 2 verifies the triggered pipelines and runs the smoke.
</dependency_graph>

<flagged_assumptions>
- **[ASSUMED]** tag push 会触发全部三条 `v*` 工作流（GitHub 行为）；npm/vsce 凭据已在 secrets。
- **[PROHIBITIONS RECALLED]** 无后续阶段；本阶段即里程碑收尾。
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: vsce idempotency and v1.0.0 tag creation</name>
  <reversibility rating="one-way">Per D-01, the formal release is irreversible once tagged and published.</reversibility>
  <files>.github/workflows/vsce-publish.yml</files>
  <read_first>
    - .github/workflows/vsce-publish.yml current publish step
  </read_first>
  <action>
In `.github/workflows/vsce-publish.yml`, before the Publish step, add a gallery pre-check: query the VS Code gallery API for `fathom-sql.sql`; if the requested version (manifest version) already exists among the extension's versions, set a step output/`SKIP_PUBLISH=true` and skip the publish (and the marketplace-listing verify can still run). This makes the tag-triggered run idempotent since `fathom-sql.sql` 1.0.0 is already live.

Then create the annotated tag `v1.0.0` on master HEAD and push it (`git tag -a v1.0.0 -m ...`; `git push origin v1.0.0`). This triggers `fathom-native-release.yml`, `npm-publish.yml`, and `vsce-publish.yml` on the tag.
  </action>
  <verify>
    <automated>git ls-remote origin refs/tags/v1.0.0 | grep -q "$(git rev-parse HEAD)" && echo "tag v1.0.0 pushed at HEAD"</automated>
  </verify>
  <acceptance_criteria>
    - vsce workflow 幂等（同版本跳过发布）。
    - tag v1.0.0 已推送且指向 master HEAD。
  </acceptance_criteria>
  <done>tag 就位，三条管线触发。</done>
</task>

<task type="auto">
  <name>Task 2: Release verification and post-release smoke</name>
  <reversibility rating="one-way">Per D-04/D-05, registry publication and release assets are the public contract.</reversibility>
  <files></files>
  <read_first>
    - 三条 workflow 的触发约定（fathom-native-release/npm-publish/vsce-publish）
  </read_first>
  <action>
After the tag push, identify and watch the three runs:
1. `fathom-native-release` (tag push): expect 5/5 jobs success (3 builds + release-gates + release); verify `gh release view v1.0.0` shows the three platform assets + `fathom-lsp-manifest.json` + `moon-toolchain-manifest.json`; verify the release body references the disclosure (RELEASE-NOTES content present); verify no `phase14-dry-run` tag artifacts.
2. `npm-publish` (tag push): expect success; verify `npm view @fathom/sql@1.0.0` shows version 1.0.0 (and `npm view @fathom/sql dist-tags.latest == 1.0.0`).
3. `vsce-publish` (tag push): expect the idempotency skip (or successful re-publish); gallery API shows `fathom-sql.sql` 1.0.0.

Post-release smoke (local Linux): download `fathom-lsp-linux-x86_64` and `fathom-lsp-manifest.json` from the v1.0.0 release, verify the asset's SHA-256 matches the manifest entry, `chmod +x`, run `./fathom-lsp-linux-x86_64 --version` and assert the output is `fathom-lsp 1.0.0`.
  </action>
  <verify>
    <automated>gh release view v1.0.0 -R tchivs/fathom-sql --json tagName,assets --jq '.tagName + " " + ([.assets[].name] | join(","))' && npm view @fathom/sql@1.0.0 version && python3 - <<'PYEOF'
import hashlib, json, subprocess
m = json.load(open("/tmp/phase20/fathom-lsp-manifest.json"))
a = m["assets"]["linux-x86_64"]
h = hashlib.sha256(open("/tmp/phase20/fathom-lsp-linux-x86_64","rb").read()).hexdigest()
assert h == a["sha256"], f"MISMATCH {h} != {a['sha256']}"
out = subprocess.run(["/tmp/phase20/fathom-lsp-linux-x86_64", "--version"], capture_output=True, text=True)
assert out.stdout.strip() == "fathom-lsp 1.0.0" and out.returncode == 0
print("SMOKE PASS: sha256 OK, fathom-lsp --version =", out.stdout.strip())
PYEOF
'</automated>
  </verify>
  <acceptance_criteria>
    - Release v1.0.0 含三平台资产 + 双 manifest，notes 引用披露。
    - npm @fathom/sql@1.0.0 在注册表可见。
    - vsce fathom-sql.sql 1.0.0 已存在（幂等跳过或更新）。
    - 冒烟：SHA-256 校验通过 + `fathom-lsp --version` → `fathom-lsp 1.0.0`。
  </acceptance_criteria>
  <done>三条管线验证 + 发布后冒烟全部通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| tag -> pipelines | tag 内容决定发布产物；HEAD 已含全部验证工作。 |
| release assets -> consumers | SHA-256 manifest 是下载校验基准。 |
| registry/marketplace | 不可逆；凭据已验证。 |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-20-01 | Tampering | assets | high | mitigate | manifest SHA-256 + 冒烟比对。 |
| T-20-02 | Spoofing | release notes | medium | mitigate | notes=RELEASE-NOTES.md 披露。 |
| T-20-03 | Repudiation | publish | high | mitigate | 三条管线运行记录 + npm view/gallery 复核。 |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | 正式 1.0.0 发布与验证 | 20-01 | COVERED | Tasks 1/2。
REQ | VER-04 | v1.0.0 tag 触发管线 + 资产 + manifest | 20-01 | COVERED | Task 1 tag + Task 2 验证。
CONTEXT | D-01..D-05 | tag/三平台/幂等/npm 验证/冒烟 | 20-01 | COVERED | Tasks 1/2。
</source_audit>

## Artifacts this phase produces
- `v1.0.0` tag + GitHub Release（三平台资产 + 双 manifest + 披露 notes）。
- `@fathom/sql@1.0.0`（npm registry）。
- `fathom-sql.sql` 1.0.0（VS Code Marketplace，幂等确认）。
- 发布后冒烟证据（SHA-256 + --version）。

<verification>
- gh release view / npm view / 画廊 API / 本地 SHA-256 + --version 冒烟。
</verification>

<success_criteria>
1. v1.0.0 Release 资产与双 manifest 就位，notes 引用披露。
2. npm 与市场产物可见。
3. 冒烟通过（SHA-256 一致 + fathom-lsp --version 1.0.0）。
</success_criteria>

<output>
Create `.planning/phases/20-formal-1-0-0-release-verification/20-01-SUMMARY.md` when done.
</output>
