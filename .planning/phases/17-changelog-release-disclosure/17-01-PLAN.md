---
phase: 17-changelog-release-disclosure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
  - RELEASE-NOTES.md
  - .github/workflows/fathom-native-release.yml
autonomous: true
requirements:
  - VER-03
  - DIS-01
  - DIS-02
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "VER-03 / D-01：CHANGELOG.md 含 `## [1.0.0]` 条目，覆盖自 v0.1.0 以来的用户可见变更（无损 CST/Doris profiles/双模式/格式化/CLI 子命令/方言抽象+Flink 语法级/catalog 分析/fathom.*.v1/三平台资产/工具链锁定/--version）。"
    - "DIS-01 / D-02：RELEASE-NOTES.md 固定五段披露：Flink 语法级、Wasm GC 非一等公民、corpus unavailable-offline 缺口、5 项验证覆盖、工具链版本策略（含 macOS Intel 非发布目标）。"
    - "DIS-02 / D-03：fathom-native-release.yml 用 `--notes-file RELEASE-NOTES.md` 替代 `--generate-notes`，Release notes 直接呈现披露。"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "1.0.0 用户可见变更记录"
    - path: "RELEASE-NOTES.md"
      provides: "五段边界披露文档"
  key_links:
    - from: ".github/workflows/fathom-native-release.yml release job"
      to: "RELEASE-NOTES.md"
      via: "--notes-file"
      pattern: "--notes-file RELEASE-NOTES.md"
---

<objective>
为 1.0.0 发布交付 CHANGELOG 条目与边界披露文档，并把 Release notes 接线为直接引用披露文档（VER-03、DIS-01、DIS-02，D-01..D-04）。
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-changelog-release-disclosure/17-CONTEXT.md
@.github/workflows/fathom-native-release.yml
@docs/VERSIONING.md
</context>

<dependency_graph>
- Task 1 writes both documents; Task 2 wires the workflow and verifies.
</dependency_graph>

<flagged_assumptions>
- **[ASSUMED]** v0.1.0 是上一公开基线（git tag 存在）；v1.0/v2.0/v3.0 里程碑能力来自 ROADMAP 概述与阶段总结。
- **[PROHIBITIONS RECALLED]** 不打 v1.0.0 tag/Release（Phase 20）；不改产品代码；不发布市场包（Phases 18–19）。
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: CHANGELOG 1.0.0 entry and release disclosure document</name>
  <reversibility rating="one-way">Per D-01/D-02, these documents are consumer-facing and audit history.</reversibility>
  <files>CHANGELOG.md, RELEASE-NOTES.md</files>
  <read_first>
    - .planning/ROADMAP.md overview lines (v1.0/v2.0/v3.0/v4.0 milestone capabilities)
    - .planning/STATE.md Blockers/Concerns + Deferred Items (corpus gaps, 5 overrides)
    - docs/VERSIONING.md (toolchain/version policy)
  </read_first>
  <action>
Create `CHANGELOG.md` (Keep-a-Changelog style) with `## [1.0.0] - 2026-08-17` covering user-visible changes since the v0.1.0 baseline: Added — lossless CST core with spans/trivia replay; Doris 2.1/3.x/4.x profile-aware parsing with strict/editor modes; structured FATHOM-* diagnostics; CST formatting with configurable options and refusal on error trees; native CLI subcommands (parse/format/lsp/lint/fingerprint/lineage) with explicit dialect/profile selection and exit-code contract; multi-dialect abstraction with syntax-level Flink SQL support; catalog-injected analysis (lineage); stable `fathom.*.v1` wire contracts; JavaScript/linear-Wasm facades and editor integrations; three-platform native release assets with content-locked toolchain and SHA-256 manifests; `--version` product reporting. Note at the top that v0.1.0 was the prior public baseline.

Create `RELEASE-NOTES.md` with the fixed five boundary disclosures plus a lead line telling consumers to read it before downloading assets: (1) Flink coverage is syntax-level — no planner/catalog/type/execution equivalence; (2) Wasm GC is not first-class — only linear Wasm is advertised; (3) corpus provenance has `unavailable-offline` gaps — no hashes are fabricated; (4) 5 documented verification overrides — see `.planning/STATE.md` Deferred Items; (5) toolchain version policy — three-platform content lock (`moon 0.1.20260807`, official SHA-256 sidecars; core digests recorded because no official checksum exists; macOS Intel is not a release target per the D-01/D-03 revision) and product version 1.0.0 decoupled from the `moon.mod` 0.1.0 module version.
  </action>
  <verify>
    <automated>python3 -c 'from pathlib import Path; c=Path("CHANGELOG.md").read_text(); assert "## [1.0.0]" in c; r=Path("RELEASE-NOTES.md").read_text(); for k in ["Flink", "Wasm GC", "unavailable-offline", "5", "0.1.20260807", "Intel"]: assert k in r'</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG 1.0.0 条目覆盖关键用户可见能力（Doris/Flink/CLI/--version/三平台等）。
    - RELEASE-NOTES.md 五段边界全部齐备且诚实。
  </acceptance_criteria>
  <done>CHANGELOG 与披露文档就绪。</done>
</task>

<task type="auto">
  <name>Task 2: Release notes wiring and executable verification</name>
  <reversibility rating="one-way">Per D-03, release notes content is the publication contract.</reversibility>
  <files>.github/workflows/fathom-native-release.yml</files>
  <read_first>
    - .github/workflows/fathom-native-release.yml release job create step
  </read_first>
  <action>
In `.github/workflows/fathom-native-release.yml`, replace `--generate-notes` in the release job's `gh release create` with `--notes-file RELEASE-NOTES.md` (the checkout in the release job provides the file at repo root). Keep the rest of the step unchanged. This makes the published Release notes the disclosure document itself, so consumers see the boundaries before download (DIS-02).
  </action>
  <verify>
    <automated>python3 -c 'from pathlib import Path; s=Path(".github/workflows/fathom-native-release.yml").read_text(); assert "--notes-file RELEASE-NOTES.md" in s; assert "--generate-notes" not in s'</automated>
  </verify>
  <acceptance_criteria>
    - release job 用 --notes-file RELEASE-NOTES.md，无 --generate-notes。
  </acceptance_criteria>
  <done>Release notes 直接引用披露文档。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Disclosure doc -> consumers | 边界声明不实会误导下载者；必须诚实。 |
| Release notes -> assets | notes 在资产下载前呈现披露。 |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-17-01 | Spoofing | 披露声明 | high | mitigate | 全部要点源自 STATE/计划决策；断言覆盖。 |
| T-17-02 | Tampering | notes 接线 | medium | mitigate | workflow 断言 --notes-file 且无 --generate-notes。 |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | CHANGELOG + 披露 + notes 引用 | 17-01 | COVERED | Tasks 1/2。
REQ | VER-03 | CHANGELOG 1.0.0 条目 | 17-01 | COVERED | Task 1。
REQ | DIS-01 | 披露文档五段边界 | 17-01 | COVERED | Task 1。
REQ | DIS-02 | notes 引用披露 | 17-01 | COVERED | Task 2。
CONTEXT | D-01 | CHANGELOG 格式/范围 | 17-01 | COVERED | Task 1。
CONTEXT | D-02 | 披露内容 | 17-01 | COVERED | Task 1。
CONTEXT | D-03 | workflow 接线 | 17-01 | COVERED | Task 2。
CONTEXT | D-04 | 可执行核对 | 17-01 | COVERED | verify 断言。
</source_audit>

## Artifacts this phase produces
- `CHANGELOG.md` — 1.0.0 用户可见变更记录。
- `RELEASE-NOTES.md` — 五段边界披露。
- `.github/workflows/fathom-native-release.yml` — --notes-file 接线。

<verification>
- 可执行 python 断言：CHANGELOG [1.0.0] 条目、RELEASE-NOTES 五段要点、workflow --notes-file 且无 --generate-notes。
</verification>

<success_criteria>
1. CHANGELOG 1.0.0 条目覆盖自 v0.1.0 的用户可见变更。
2. 披露文档诚实声明全部五段边界。
3. Release notes 接线为引用披露文档。
</success_criteria>

<output>
Create `.planning/phases/17-changelog-release-disclosure/17-01-SUMMARY.md` when done.
</output>
