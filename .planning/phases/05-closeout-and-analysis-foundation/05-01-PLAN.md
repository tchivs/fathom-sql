---
phase: 05-closeout-and-analysis-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - CLOSE-01
  - CLOSE-02
estimate:
  tokens: 18000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "CLOSE-01 证据已正式核实并记录：.planning/REQUIREMENTS.md 与 .planning/STATE.md 的 traceability 条目引用 vscode/scripts/host-verify.mjs（真 extension-host 4 隔离模式）与 2026-08-06 核实记录（D-07）"
    - "CLOSE-02 证据已正式核实并记录：.planning/REQUIREMENTS.md 与 .planning/STATE.md 引用 .github/workflows/ci.yml 的 linear-wasm-parity job 与 scripts/compare_backends.py（moon test --target wasm --package parity）"
    - "未重跑 VS Code host 验证、未重跑 wasm parity、未新实现任何代码（D-07）——仅文档核实与 traceability 更新"
  artifacts:
    - .planning/REQUIREMENTS.md（CLOSE-01/CLOSE-02 行从 Complete 升级为正式核实条目）
    - .planning/STATE.md（Deferred Items 两行状态升级为 closed / formalized）
  key_links:
    - "traceability 条目与既有证据文件路径、job 名、2026-08-06 记录行精确对应，无虚构证据"
---

<objective>
正式核实并记录 v1.0 遗留验证项 CLOSE-01（VS Code 真 extension-host 验证）与 CLOSE-02（linear-Wasm CI 运行时执行）的证据，并把 traceability 升级为正式条目（D-07）。

Purpose: 两项验证已在 2026-08-06 核实完毕且证据均在仓库内（`vscode/scripts/host-verify.mjs` 与 `.github/workflows/ci.yml` 的 `linear-wasm-parity` job），本 plan 只做**正式记录核实**——不重跑、不新实现，兑现 v1.0 收尾。
Output: `.planning/REQUIREMENTS.md` 与 `.planning/STATE.md` 的 traceability 条目更新。
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 核实并记录 CLOSE-01（VS Code 真 extension-host 验证）证据</name>
  <read_first>
    vscode/scripts/host-verify.mjs
    .planning/STATE.md（Deferred Items 表 Phase 04 ECO-07 verification_override 行）
    .planning/REQUIREMENTS.md（CLOSE-01 行 + Traceability 表）
  </read_first>
  <action>
    阅读 vscode/scripts/host-verify.mjs 确认证据：以 @vscode/test-electron 启动真实 VS Code extension host，含 4 个隔离模式（functional doris-4.x / profile doris-2.1 / flink flink-2.3.0 / fallback），每个模式断言 host 干净退出。核对 .planning/STATE.md Deferred Items 中 Phase 04 ECO-07 行的 2026-08-06 记录（VS Code 1.132.0、3 模式通过、LogOutputChannel {log:true} bug 修复）与 .planning/REQUIREMENTS.md CLOSE-01 行。

    在 .planning/REQUIREMENTS.md 做两处更新：
    1) CLOSE-01 需求行（保持 VERIFIED 2026-08-06 文本）追加 Phase 5 正式核实标记：`[Phase 5 formalized]`，引用 `vscode/scripts/host-verify.mjs` 与 4 模式名。
    2) Traceability 表 CLOSE-01 行状态从 `Complete (verified 2026-08-06)` 升级为 `Complete — Phase 5 formalized (host-verify.mjs, 4 modes, record in STATE.md Deferred Items)`。

    在 .planning/STATE.md Deferred Items 表中，将 Phase 04 ECO-07 verification_override 行的状态从 `verified 2026-08-06` 升级为 `closed — Phase 5 CLOSE-01 formally verified`，保留原 2026-08-06 记录文本与引用路径不变。

    约束：不运行 host-verify.mjs、不修改 vscode/ 下任何文件、不新增任何测试（D-07 明文不重跑）。
  </action>
  <verify>
    grep -n "host-verify.mjs" .planning/REQUIREMENTS.md（≥1 处，且 CLOSE-01 行含 "formalized"）
    grep -n "CLOSE-01" .planning/STATE.md（ECO-07 行状态含 "closed"）
    git status --porcelain（仅 .planning/ 下改动，无 vscode/ 改动）
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md 的 CLOSE-01 行包含 "host-verify.mjs" 与 "formalized"
    - .planning/STATE.md 的 ECO-07 verification_override 行状态包含 "closed"
    - git status 显示本次改动仅限 .planning/ 下的 REQUIREMENTS.md 与 STATE.md
    - 未运行 host-verify.mjs（D-07 不重跑）
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 核实并记录 CLOSE-02（linear-Wasm CI 运行时执行）证据</name>
  <read_first>
    .github/workflows/ci.yml（linear-wasm-parity job）
    scripts/compare_backends.py
    .planning/STATE.md（Deferred Items 表 ci_recommendation 行）
    .planning/REQUIREMENTS.md（CLOSE-02 行 + Traceability 表）
  </read_first>
  <action>
    阅读 .github/workflows/ci.yml 的 linear-wasm-parity job 确认证据链：`moon build --target wasm binding` + `moon build --target wasm parity` + `moon test --target wasm --package parity`（线性 Wasm 运行时执行）→ native/js 交叉核对 → `python3 scripts/compare_backends.py`（三目标字节 digest，只读不写 snapshot，CI 无 --update）。核对 .planning/STATE.md Deferred Items 中 ci_recommendation 行的 2026-08-06 记录与 .planning/REQUIREMENTS.md CLOSE-02 行。

    在 .planning/REQUIREMENTS.md 做两处更新：
    1) CLOSE-02 需求行追加 Phase 5 正式核实标记：`[Phase 5 formalized]`，引用 `linear-wasm-parity` job 名与 `scripts/compare_backends.py`。
    2) Traceability 表 CLOSE-02 行状态升级为 `Complete — Phase 5 formalized (ci.yml linear-wasm-parity, record in STATE.md Deferred Items)`。

    在 .planning/STATE.md Deferred Items 表中，将 ci_recommendation 行状态从 `addressed 2026-08-06` 升级为 `closed — Phase 5 CLOSE-02 formally verified`，保留原 2026-08-06 记录文本。

    约束：不运行 `moon test --target wasm --package parity`、不运行 compare_backends.py（D-07 不重跑，CI 持续执行）。
  </action>
  <verify>
    grep -n "linear-wasm-parity" .planning/REQUIREMENTS.md（≥1 处，且 CLOSE-02 行含 "formalized"）
    grep -n "CLOSE-02" .planning/STATE.md（ci_recommendation 行状态含 "closed"）
    git status --porcelain（仅 .planning/ 下改动）
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md 的 CLOSE-02 行包含 "linear-wasm-parity" 与 "formalized"
    - .planning/STATE.md 的 ci_recommendation 行状态包含 "closed"
    - 未运行 wasm parity 测试或 compare_backends.py（D-07 不重跑）
    - git status 显示本次改动仅限 .planning/ 下文件
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- **修改文件（无新增符号）：**
  - `.planning/REQUIREMENTS.md` — CLOSE-01/CLOSE-02 需求行追加 `[Phase 5 formalized]` 标记；Traceability 表两行状态升级为正式核实条目（引用 host-verify.mjs / linear-wasm-parity job / compare_backends.py / 2026-08-06 记录）。
  - `.planning/STATE.md` — Deferred Items 表 ECO-07（CLOSE-01）与 ci_recommendation（CLOSE-02）两行状态升级为 `closed — Phase 5 formally verified`。

- **本 plan 不新增任何源代码、测试、脚本或构建产物。**

## threat_model

本 plan 为纯文档/traceability 更新，无代码、无未信任输入面；唯一完整性风险是 traceability 记录与既有证据不符（虚构证据）。

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文档记录 → 仓库证据 | traceability 条目必须引用仓库内真实存在的证据文件（host-verify.mjs、ci.yml job、compare_backends.py、STATE.md 2026-08-06 记录行），不允许虚构路径或状态 |

### STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01-01 | Tampering | .planning/REQUIREMENTS.md / STATE.md traceability 条目 | low | mitigate | 每个 acceptance_criteria 用 grep 断言引用路径真实存在于仓库；改动用 git status 收窄到 .planning/ 文件 |
| T-05-01-02 | Spoofing | 证据状态夸大（"verified" 而无 in-repo 依据） | low | accept | D-07 明文不重跑；状态升级仅建立在 2026-08-06 既有记录之上，plan 不新增任何运行声明 |
| T-05-01-SC | Tampering | 依赖安装 | low | accept | 本阶段零新增依赖（D-21）；无 npm/pip/cargo 安装面 |

<verification>
- 两项 grep 断言分别锁定 CLOSE-01 与 CLOSE-02 的正式核实标记与引用路径。
- git status 收窄改动范围，确保未触碰 vscode/、.github/、scripts/ 或任何源码。
- 全程不运行任何验证/测试命令（D-07 不重跑）。
</verification>

<success_criteria>
- REQUIREMENTS.md 与 STATE.md 的 CLOSE-01/02 traceability 条目已升级为正式核实状态，引用路径与既有证据一致。
- 未产生任何代码、测试或运行证据变更。
</success_criteria>

<output>
Create `.planning/phases/05-closeout-and-analysis-foundation/05-01-SUMMARY.md` when done.
</output>
