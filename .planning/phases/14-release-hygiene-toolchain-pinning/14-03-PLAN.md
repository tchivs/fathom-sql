---
phase: 14-release-hygiene-toolchain-pinning
plan: 03
type: execute
wave: 3
depends_on:
  - 14-02
files_modified:
  - .github/workflows/fathom-native-release.yml
  - scripts/run_phase14_release_dry_run.py
  - scripts/validate_toolchain_evidence.py
  - scripts/tests/test_validate_toolchain_evidence.py
  - scripts/tests/fixtures/toolchain-evidence/
autonomous: true
requirements:
  - TC-01
  - TC-02
estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "TC-01 / D-07（2026-08-14 修订）：三个平台 build 在构建前使用同一 verified lock，并把结构相同的 `moon-toolchain.json` 与对应 Native binary 放入同一 artifact。"
    - "TC-02 / D-04/D-05/D-06：tag 与 workflow_dispatch 共用一个独立、无 bypass 的 release-gates job，九个真实命令各运行一次并且任一失败阻断。"
    - "D-08/D-09：publish 聚合器只接受精确三平台证据集；缺失、重复、未知、字段空值、requested/reported/lock 不一致均阻断，成功后才生成聚合清单。"
    - "最终 publication job 显式依赖 `build` 与 `release-gates`，没有 `always()` 或 warning-only 路径，写权限仍只在 publication job。"
    - "Pre-merge dry-run uses workflow database ID 328270211 only after proving it is active and registered to the legacy default-branch path, then executes the exact proposed Fathom workflow bytes from that same legacy path on a validation-only temporary branch; the persistent implementation retains only the clean new Fathom path."
  artifacts:
    - path: ".github/workflows/fathom-native-release.yml"
      provides: "三平台 evidence build、独立 release-gates、双依赖 publication barrier 和 aggregate upload"
    - path: "scripts/validate_toolchain_evidence.py"
      provides: "精确平台集合与 evidence/lock identity 的 fail-closed validator/aggregate writer"
      exports: ["main"]
    - path: "scripts/tests/test_validate_toolchain_evidence.py"
      provides: "aggregate success plus missing/duplicate/unknown/malformed/identity-mismatch no-output executable tests"
    - path: "scripts/tests/fixtures/toolchain-evidence/"
      provides: "deterministic three-record evidence sets and defective variants"
    - path: "scripts/run_phase14_release_dry_run.py"
      provides: "same-path temporary-ref construction, registration preflight, numeric-ID dispatch, exact-run selection, artifact validation and branch cleanup"
  key_links:
    - from: ".github/workflows/fathom-native-release.yml"
      to: ".github/scripts/install-moonbit.sh / .github/scripts/install-moonbit.ps1"
      via: "matrix 每行在 build 前生成 dist/moon-toolchain.json"
      pattern: "moon-toolchain\\.json"
    - from: ".github/workflows/fathom-native-release.yml release-gates"
      to: ".github/moonbit-toolchain.json"
      via: "single Ubuntu gate 使用与 build/ordinary CI 相同 exact lock"
      pattern: "release-gates"
    - from: ".github/workflows/fathom-native-release.yml release"
      to: "build + release-gates"
      via: "explicit needs publication barrier"
      pattern: "needs: \\[build, release-gates\\]"
    - from: "scripts/validate_toolchain_evidence.py"
      to: "three downloaded moon-toolchain.json records + committed lock"
      via: "exact-set/schema/identity validation before aggregate write"
      pattern: "linux-x86_64|macos-aarch64|windows-x86_64"
    - from: "scripts/run_phase14_release_dry_run.py"
      to: "GitHub workflow 328270211 + exact run artifacts"
      via: "legacy default registration preflight, numeric-ID dispatch, workflow/path/branch/SHA/event selection and explicit artifact download"
      pattern: "328270211|gh run download|phase14-dry-run"
---

<objective>
把冻结并共享的工具链扩展到 release workflow：每平台携带可审计证据，单一 release-gates 运行完整资格矩阵，而 publication 只有在 build 和 gates 都成功且三平台证据聚合通过后才可继续（TC-01、TC-02，D-04..D-09）。

Purpose: 让发布资格与工件工具链身份成为同一 fail-closed DAG 的真实依赖，而不是日志、另一个 workflow 的历史状态或各平台重复门禁。
Output: 完整 release DAG 和可复用的 evidence aggregate validator。
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-release-hygiene-toolchain-pinning/14-CONTEXT.md
@.planning/phases/14-release-hygiene-toolchain-pinning/14-RESEARCH.md
@.planning/phases/14-release-hygiene-toolchain-pinning/14-PATTERNS.md
@.planning/phases/14-release-hygiene-toolchain-pinning/14-01-SUMMARY.md
@.planning/phases/14-release-hygiene-toolchain-pinning/14-02-SUMMARY.md
@.github/moonbit-toolchain.json
@.github/scripts/install-moonbit.sh
@.github/scripts/install-moonbit.ps1
@.github/workflows/ci.yml
@.github/workflows/fathom-native-release.yml
@scripts/compare_backends.py
@scripts/diff_parity.py
@scripts/check_naming.py
@scripts/verify_corpus.py
@corpus/tools/generate_corpus_report.py
@corpus/tools/check_keywords.py
</context>

<dependency_graph>
- Task 1 needs Plan 14-02's shared installers; it creates per-platform evidence, the single release-gates job, and the executable pre-merge dry-run driver that maps the proposed workflow bytes to the currently registered legacy path on an ephemeral validation ref.
- Task 2 needs Task 1's artifact shape/job name; it creates the aggregate validator and makes publication depend on both build and gates.
- Both tasks share the release workflow and therefore remain in one sequential plan; Plan 14-04 is file-disjoint but waits for the full release readiness matrix.
</dependency_graph>

<flagged_assumptions>
- **[FLAGGED ASSUMPTION — descriptor-less edge probe]** TC-01 and TC-02 were `unclassified/unresolved`; exact evidence fields, nine commands and DAG prohibitions below are concrete requirement-derived must-haves, not optional interpretations.
- **[PROHIBITIONS RECALLED]** No API coverage document, schema push, UI, package installation, snapshot update, skip/bypass input, external-workflow status dependency, formal `v1.0.0` tag/release action, product semver/`--version`, docs or marketplace work. The existing generic release job remains a pipeline surface; Phase 20 owns the formal release event.
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Wire verified three-platform evidence and one complete release-gates job</name>
  <precondition>`gh auth status` and `gh workflow view 328270211 --json id,path,name,state` (or the equivalent Actions workflow API) succeed and report ID 328270211, state `active`, path `.github/workflows/doris-native-release.yml`, and the expected current legacy name; the implementation commit exists locally and contains `.github/workflows/fathom-native-release.yml` but not the legacy path. Otherwise halt before creating or pushing a validation ref.</precondition>
  <reversibility rating="one-way">Per D-05, the nine-command release qualification set is a locked 1.0 eligibility contract; removing or bypassing it would weaken promised guarantees, and the prior user decision is not re-asked.</reversibility>
  <files>.github/workflows/fathom-native-release.yml, scripts/run_phase14_release_dry_run.py</files>
  <read_first>
    - .github/workflows/fathom-native-release.yml current build matrix, install blocks, artifact upload, release job
    - .github/workflows/ci.yml exact authoritative gate commands and Python 3.11 setup
    - .github/scripts/install-moonbit.sh and .github/scripts/install-moonbit.ps1 invocation/output contract
    - .github/moonbit-toolchain.json
    - .planning/phases/14-release-hygiene-toolchain-pinning/14-CONTEXT.md D-04, D-05, D-06, D-07
    - Live default-branch workflow registration via `gh workflow view 328270211 --json id,path,name,state` or the equivalent API (ID 328270211, active, legacy path `.github/workflows/doris-native-release.yml` before cutover)
  </read_first>
  <action>
Per D-07 and TC-01, replace both release build bootstrap branches with Plan 14-02's shared helpers and committed lock. Map exactly `linux-x86_64`, `macos-aarch64`, `windows-x86_64`; require the helper to write `dist/moon-toolchain.json` before build. Preserve the existing x86_64 clang wrapper and output-binary `lipo` assertion, while also relying on the installer’s downloaded-compiler architecture proof. Require evidence fields `schemaVersion`, `requestedVersion`, complete `reportedVersion`, `runnerOS`, observed `runnerArch`, `targetPlatform`, `binaryArchiveSha256`, `coreArchiveSha256` plus source URLs; upload the evidence beside the binary through the existing `dist/*` artifact and retain `if-no-files-found: error`.

Per D-04/D-05/D-06 and TC-02, replace the partial linear-Wasm release job with one independent Ubuntu `release-gates` job that checks out the current implementation commit, installs from the same lock through the Unix helper, pins Python 3.11, and executes exactly once and in this order with exact step names `Native parity`, `JavaScript parity`, `linear-Wasm parity`, `compare_backends`, `diff_parity`, `check_naming`, `verify_corpus`, `corpus report`, `keywords`: `moon test --target native --package parity`; `moon test --target js --package parity`; `moon test --target wasm --package parity`; `python3 scripts/compare_backends.py`; `python3 scripts/diff_parity.py --frozen-only`; `python3 scripts/check_naming.py`; `python3 scripts/verify_corpus.py --check`; `python3 corpus/tools/generate_corpus_report.py --check`; `python3 corpus/tools/check_keywords.py corpus/keywords.tsv`. Preserve `workflow_dispatch.inputs.tag.required: true` and add an explicit boolean `dry_run` input defaulting false. Tag and ordinary dispatch paths share build/gates/aggregation with no gate skip/bypass; publication steps alone use the positive condition `dry_run != true` (and the existing tag/release condition), so the exactly named `Create GitHub Release` and `Upload GitHub Release assets` steps are skipped during the dry run while build, all nine gates, artifact download, `Validate toolchain evidence`, `Generate SHA-256 manifest`, and `Upload release manifests` still execute. Upload `fathom-release-manifests` with `moon-toolchain-manifest.json` and `fathom-lsp-manifest.json` even in dry-run mode; this is Actions evidence, not a GitHub Release.

Create `scripts/run_phase14_release_dry_run.py` as the single stdlib + `git`/`gh` orchestration command with CLI `--implementation-sha SHA --output-dir PATH --workflow-id 328270211`. It creates the explicit output path and refuses a pre-existing nonempty directory. Resolve and verify `IMPLEMENTATION_SHA`, require its persistent tree to contain `.github/workflows/fathom-native-release.yml` and not `.github/workflows/doris-native-release.yml`, and capture the exact proposed Fathom workflow bytes from that commit. Before creating or pushing any ref, run `gh workflow view 328270211 --json id,path,name,state` (or a supported equivalent/API) and require ID 328270211, state `active`, path `.github/workflows/doris-native-release.yml`, and the expected current legacy registration name. A mismatch is fatal before dispatch.

Create a new local branch `phase14-release-dry-run-&lt;UTC&gt;-&lt;nonce&gt;` from `IMPLEMENTATION_SHA`, copy those exact captured Fathom workflow bytes to `.github/workflows/doris-native-release.yml`, remove `.github/workflows/fathom-native-release.yml` from this temporary branch, assert exactly one triggerable release-workflow copy remains and its bytes equal the proposed implementation bytes, then commit this ephemeral same-path mapping and push the branch. Record the resulting temporary head SHA separately from `IMPLEMENTATION_SHA`. This mapping commit is validation-only and must never be merged: the actual Phase 14 implementation commit and all persistent plan artifacts retain only `.github/workflows/fathom-native-release.yml`. Dispatch exactly `gh workflow run 328270211 --ref "$TEMP_BRANCH" -f tag=phase14-dry-run -f dry_run=true`; do not perform any filename lookup or assume cross-path workflow-ID resolution.

Poll `gh api --method GET repos/$REPO/actions/workflows/328270211/runs -f branch="$TEMP_BRANCH" -f event=workflow_dispatch` and require exactly one candidate. It must report `workflow_id` 328270211, path `.github/workflows/doris-native-release.yml`, the expected Fathom workflow name/content identity, exact `head_branch == TEMP_BRANCH`, exact `head_sha == TEMP_HEAD_SHA`, and `event == workflow_dispatch`; zero, multiple, new-path, wrong-name/content, wrong-branch, wrong-SHA, or wrong-event candidates fail. Await with `gh run watch "$RUN_ID" --exit-status`. After success, execute supported `gh run view "$RUN_ID" --json event,headBranch,headSha,conclusion,jobs,workflowName` into `$OUTPUT_DIR/run.json` and enforce the same branch/temp-head/event/Fathom-name identity plus success. Persist registration preflight data, implementation SHA, temporary mapping commit SHA, and a digest of the exact proposed/mapped workflow bytes as evidence. Do not request an `artifacts` field from `gh run view`.

Then execute exactly `gh run download "$RUN_ID" --dir "$OUTPUT_DIR/artifacts"`. Require the downloaded directory set to contain exactly the three platform artifact directories `fathom-lsp-linux-x86_64`, `fathom-lsp-macos-aarch64`, `fathom-lsp-windows-x86_64` plus `fathom-release-manifests`, with no missing/duplicate/unknown platform artifact. Each platform directory must contain its expected Native binary and exactly one `moon-toolchain.json`; the manifest artifact must contain `moon-toolchain-manifest.json` and `fathom-lsp-manifest.json`. The driver's live artifact validator parses all four records and the committed lock, enforces the same exact-set/schema/identity/digest contract as `scripts/validate_toolchain_evidence.py`, requires the downloaded aggregate toolchain manifest to match the validated records, and validates the product manifest's `tag == "phase14-dry-run"`, exact four binary names, and SHA-256 values against the downloaded binaries. Require four successful `Build &lt;platform&gt;` jobs, one successful `release-gates` job with all nine exact successful steps, and successful aggregate validation/product-manifest/manifest-upload steps; require both publication steps skipped or absent and `gh release view phase14-dry-run --json tagName` to fail.

Only after run identity, jobs, evidence, artifacts, manifests, and non-publication all validate, delete both the temporary remote branch and temporary local branch and verify each is absent; cleanup failure makes the driver fail. On any earlier validation failure retain the branch and print the exact recovery command. The same-path copy and mapping commit are never merged. After the Phase 14 implementation commit is merged/pushed to the default branch, require a post-cutover check that `gh workflow view fathom-native-release.yml --json id,path,name,state` succeeds with active new-path registration and that lookup/API enumeration no longer registers `.github/workflows/doris-native-release.yml`. That post-cutover requirement does not alter or become a precondition of this pre-merge dry-run.
  </action>
  <verify>
    <automated>python3 -c 'from pathlib import Path; s=Path(".github/workflows/fathom-native-release.yml").read_text(); required=["moon test --target native --package parity","moon test --target js --package parity","moon test --target wasm --package parity","python3 scripts/compare_backends.py","python3 scripts/diff_parity.py --frozen-only","python3 scripts/check_naming.py","python3 scripts/verify_corpus.py --check","python3 corpus/tools/generate_corpus_report.py --check","python3 corpus/tools/check_keywords.py corpus/keywords.tsv"]; assert "release-gates:" in s and all(s.count(x)==1 for x in required); assert "tag:" in s and "required: true" in s and "dry_run" in s; assert s.count("moon-toolchain.json") &gt;= 2; assert not Path(".github/workflows/doris-native-release.yml").exists()' &amp;&amp; TMP_DIR=$(mktemp -d) &amp;&amp; trap 'rm -rf "$TMP_DIR"' EXIT &amp;&amp; IMPLEMENTATION_SHA=$(git rev-parse HEAD) &amp;&amp; gh workflow view 328270211 --json id,path,name,state &gt; "$TMP_DIR/registration.json" &amp;&amp; python3 scripts/run_phase14_release_dry_run.py --implementation-sha "$IMPLEMENTATION_SHA" --output-dir "$TMP_DIR/release-dry-run" --workflow-id 328270211</automated>
  </verify>
  <acceptance_criteria>
    - D-07：exact three build rows each generate a schema-identical runtime evidence record beside the binary before artifact upload.
    - D-04/D-05：single Ubuntu `release-gates` contains all nine exact commands once; it is not a build-matrix child and has no dependence on ordinary CI history.
    - D-06：tag and workflow_dispatch share jobs and gate body with no skip/bypass input; Phase 20 formal release action is absent。
    - TC-01/TC-02：all build and gate MoonBit calls are downstream of the same verified lock helper and fail naturally on helper/command nonzero exit。
    - Existing build matrix, asset names, macOS x86_64 output assertion and `if-no-files-found: error` remain intact。
    - Pre-merge dispatch first proves ID 328270211 is active at the registered legacy path, then runs the exact proposed Fathom workflow bytes from that same legacy path on the temporary mapping commit; selected-run identity is uniquely bound to the Fathom name/content, temp branch, temp head SHA, event, ID and ephemeral legacy path, with no cross-path resolution assumption.
    - The same automated unit downloads real artifacts, validates exact three binary+toolchain payloads plus aggregate/product manifests, proves nine gates and aggregation succeeded and GitHub Release publication was skipped/absent, then deletes and verifies absence of both temporary remote/local branches; the persistent implementation remains new-path-only and post-default-branch cutover verifies new registration plus legacy deregistration.
  </acceptance_criteria>
  <done>Release workflow has three verified evidence-producing builds and one complete, non-bypassable qualification job; its pre-merge live proof executes by same-path temporary mapping without polluting the persistent implementation or assuming cross-path workflow-ID resolution。</done>
</task>

<task type="auto">
  <name>Task 2: Behavior-test aggregate evidence and harden publication prerequisites</name>
  <reversibility rating="one-way">Per D-09, downstream consumers will rely on the evidence schema and fail-closed aggregate; breaking it requires a versioned migration.</reversibility>
  <files>scripts/validate_toolchain_evidence.py, scripts/tests/test_validate_toolchain_evidence.py, scripts/tests/fixtures/toolchain-evidence/, .github/workflows/fathom-native-release.yml</files>
  <behavior>
    - Success: exactly one valid record for each allowed platform, all identity fields matching lock, produces deterministic aggregate retaining all three records.
    - Failure: missing, duplicate, unknown or malformed evidence exits nonzero and leaves aggregate output absent.
    - Failure: requested/reported/lock/platform/OS/architecture/digest mismatch exits nonzero and leaves aggregate output absent.
  </behavior>
  <read_first>
    - .github/workflows/fathom-native-release.yml after Task 1
    - .github/moonbit-toolchain.json
    - .planning/phases/14-release-hygiene-toolchain-pinning/14-PATTERNS.md §§Manifest Aggregation, validate_toolchain_evidence analog
    - scripts/check_naming.py fail-closed scanner shape
    - .planning/phases/14-release-hygiene-toolchain-pinning/14-CONTEXT.md D-08, D-09
  </read_first>
  <action>
Create the stdlib validator to deterministically enumerate records, require exactly one of `linux-x86_64`, `macos-aarch64`, `windows-x86_64`, reject unknown/duplicate/missing records, validate schema/types/nonempty raw version/64-hex archive digests, require target/OS/observed architecture consistency, and require one requested/reported identity equal to the committed lock. Accumulate actionable errors and exit nonzero without creating or retaining the requested output on every defect; on success write sorted/indented UTF-8 JSON with terminal newline atomically.

Create exact fixtures under `scripts/tests/fixtures/toolchain-evidence/` for aggregate success, missing, duplicate, unknown, malformed JSON/schema/type, requested identity mismatch, reported raw-version mismatch, target/OS/architecture mismatch and digest mismatch. `scripts/tests/test_validate_toolchain_evidence.py` invokes the validator as a subprocess for every fixture; it must assert success output content for the valid set and nonzero plus absent aggregate output for each defect. Tests are mandatory release-control proof despite Nyquist being disabled.

Per D-08/D-09, publication uses exact `needs: [build, release-gates]`, downloads three artifacts without merge semantics that hide duplicates, invokes validator before product SHA manifest or upload, and includes aggregate toolchain manifest in final assets. Missing/malformed/mismatched evidence or upstream failure blocks publication; no `always()` or warning-only condition. Commit only named paths per D-13.
  </action>
  <verify>
    <automated>python3 -m unittest scripts.tests.test_validate_toolchain_evidence -v &amp;&amp; python3 -c 'from pathlib import Path; s=Path(".github/workflows/fathom-native-release.yml").read_text(); assert "needs: [build, release-gates]" in s; assert "scripts/validate_toolchain_evidence.py" in s; assert "moon-toolchain-manifest.json" in s; assert "contents: write" in s; assert s.index("scripts/validate_toolchain_evidence.py") &lt; s.index("gh release upload")'</automated>
  </verify>
  <acceptance_criteria>
    - D-08/D-09 focused subprocess tests execute aggregate success and every named defect, proving failure leaves no output。
    - publication validates exact three records before every manifest/upload and depends on both upstream jobs。
    - aggregate retains all source records/lock identity；per-platform evidence remains beside binaries。
    - D-13 path-specific commit excludes unrelated user/runtime paths。
  </acceptance_criteria>
  <done>Executable fixtures prove the aggregate validator fails closed, and publication is unreachable unless three builds, the real nine-command gate job and exact evidence consistency all succeed。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Matrix runner → uploaded artifact | 每个平台把 binary 和 runtime-observed evidence 交给 artifact storage。 |
| Release checkout → qualification commands | 当前 tag/dispatch checkout 在 exact toolchain 下接受九项发布资格检查。 |
| Artifact storage → publication job | 下载内容可能缺失、重复、被替换或包含未知平台记录。 |
| Default-branch workflow registration → temporary-ref dispatch | 预合并 ID 328270211 注册 legacy path；driver 必须在临时 ref 的同一路径承载待验证 Fathom bytes，并移除 new-path duplicate，禁止跨路径身份假设。 |
| Publication job → GitHub Release | 只有通过 dependencies 和 aggregate validation 的内容获得 contents write/upload。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-13 | Spoofing | Per-platform evidence | high | mitigate | helper 生成 observed OS/arch/digests/raw version；validator 严格 target/arch/lock 一致性和 exact set。 |
| T-14-14 | Tampering | Downloaded artifacts | high | mitigate | 不信任合并 glob；拒绝 missing/duplicate/unknown/schema/digest/identity mismatch，成功前不写 aggregate。 |
| T-14-15 | Elevation of Privilege | Publication permission/DAG | high | mitigate | contents write 仅 publication job；exact `needs` 绑定 build+release-gates，validator 在 upload 前。 |
| T-14-16 | Repudiation | Toolchain provenance | medium | mitigate | 每平台记录 + aggregate 保留 raw version、runner facts、archive digests and lock identity。 |
| T-14-17 | Denial of Service | Gate or platform failure | medium | accept | 任何失败应阻断 publication；没有 bypass 是 TC-02 的预期可用性取舍。 |
| T-14-18 | Tampering | Gate topology/commands | high | mitigate | 九个权威命令 exact-once，no matrix duplication/历史状态/skip/tolerance；结构 verify 锁定命令与 needs。 |
| T-14-18A | Spoofing / Tampering | Workflow registration, ephemeral mapping and dry-run selection | high | mitigate | dispatch 前通过 `gh workflow view 328270211 --json id,path,name,state` 验证 active legacy registration；临时 mapping commit 只保留 legacy path 且 bytes digest 等于 proposed Fathom workflow；按 ID dispatch 并 exact-match ID/legacy path/Fathom name+content/temp branch/temp head SHA/event；取证成功后删除并验证 remote/local ref，persistent commit 始终仅保留 new path。 |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | release pin + 完整发布门禁矩阵 | 14-03 | COVERED | evidence、gates、publication barrier 端到端。
REQ | TC-01 | pinned builds and recorded exact toolchain evidence | 14-03 | COVERED | per-platform + aggregate assets。
REQ | TC-02 | full release gate matrix blocks publishing | 14-03 | COVERED | nine commands + exact needs。
RESEARCH | Evidence contract | three records + aggregate exact-set validator | 14-03 | COVERED | Tasks 1/2。
RESEARCH | Dedicated topology | gates run once; publication needs build+gates | 14-03 | COVERED | Tasks 1/2。
RESEARCH | Registered workflow cutover | pre-merge numeric-ID validation must execute proposed bytes at the currently registered legacy path; persistent implementation stays new-path-only and default-branch cutover later registers new path/removes legacy | 14-03 | COVERED | Task 1 preflight, ephemeral same-path mapping, exact run identity, cleanup and post-cutover checks; no cross-path resolution。
CONTEXT | D-04 | standalone fail-closed release-gates + needs | 14-03 | COVERED | both tasks。
CONTEXT | D-05 | exact nine gate commands | 14-03 | COVERED | Task 1。
CONTEXT | D-06 | tag/dispatch same DAG, required tag retained, explicit non-publishing dry run | 14-03 | COVERED | Task 1 verifies active legacy registration, maps exact proposed bytes to that same path on a temp ref, dispatches ID 328270211 with `tag=phase14-dry-run`, `dry_run=true`, validates evidence/gates/non-publication, then removes both temporary refs。
CONTEXT | D-07 | per-platform evidence beside binary | 14-03 | COVERED | Task 1。
CONTEXT | D-08 | three-record validation + aggregate asset | 14-03 | COVERED | Task 2。
CONTEXT | D-09 | every mismatch blocks | 14-03 | COVERED | Task 2。
CONTEXT | D-13 | path-specific commit/preserve runtime state | 14-03 | COVERED | Task 2。
</source_audit>

## Artifacts this phase produces

- `.github/workflows/fathom-native-release.yml` — exact toolchain matrix, evidence, release-gates and publication DAG。
- `scripts/validate_toolchain_evidence.py` — fail-closed aggregate evidence validator/writer。
- `scripts/tests/test_validate_toolchain_evidence.py` + `scripts/tests/fixtures/toolchain-evidence/` — success and all fail-closed aggregate behaviors。
- `scripts/run_phase14_release_dry_run.py` — active-registration preflight, ephemeral same-path workflow mapping, numeric-ID dispatch, exact identity/artifact validation, and verified remote/local branch cleanup。
- Runtime artifacts specified by the workflow: exact three platform artifact directories each containing binary + `moon-toolchain.json`, and `fathom-release-manifests` containing `moon-toolchain-manifest.json` + `fathom-lsp-manifest.json`。

<verification>
- Execute `python3 -m unittest scripts.tests.test_validate_toolchain_evidence -v` and invoke `scripts/run_phase14_release_dry_run.py` for the implementation SHA. The driver proves ID 328270211 is active at the legacy path, creates a temporary mapping commit with the exact proposed Fathom bytes only at that path, dispatches the ID, uniquely selects the legacy-path/Fathom-name/temp-branch/temp-head/event run, downloads and validates all evidence, proves gates/aggregation success and publication absence, then deletes and verifies absence of both temporary refs.
- Keep YAML exact-command/order/needs assertions as supplemental topology proof. After the implementation reaches the default branch, verify `gh workflow view fathom-native-release.yml` succeeds and the legacy path is deregistered; this post-cutover check is separate from pre-merge same-path validation. Do not run unrelated formatter/linter/project-wide suites locally.
</verification>

<success_criteria>
1. TC-01 has per-platform and aggregate machine-readable evidence tied exactly to the lock。
2. TC-02 nine-command qualification runs once for both trigger paths and blocks publication on any failure。
3. Pre-merge validation dispatches active ID 328270211 against exact proposed bytes at its registered legacy path, uniquely validates the temporary run identity, and removes the validation-only refs after evidence succeeds。
4. Persistent implementation retains only `.github/workflows/fathom-native-release.yml`; after default-branch cutover the new filename is registered and the legacy path is absent。
5. Publication requires build, gates and strict evidence aggregation, with no permissive branch。
</success_criteria>

<output>
Create `.planning/phases/14-release-hygiene-toolchain-pinning/14-03-SUMMARY.md` when done.
</output>
