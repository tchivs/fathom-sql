---
phase: 14-release-hygiene-toolchain-pinning
plan: 05
type: execute
wave: 5
depends_on:
  - 14-04
files_modified:
  - .planning/quick/260805-df9-add-a-kotlin-gradle-jetbrains-intellij-p/PLAN.md
  - .planning/quick/260805-e28-align-the-jetbrains-plugin-wrapper-and-d/PLAN.md
  - .planning/milestones/v1.0-research/STACK.md
  - .planning/milestones/v1.0-research/FEATURES.md
  - .planning/milestones/v1.0-research/ARCHITECTURE.md
  - .planning/milestones/v1.0-research/PITFALLS.md
  - .planning/milestones/v1.0-research/SUMMARY.md
  - scripts/classify_release_status.py
  - scripts/tests/test_classify_release_status.py
  - .planning/phases/14-release-hygiene-toolchain-pinning/14-RELEASE-READINESS.md
autonomous: true
requirements:
  - HYG-01
  - HYG-02
  - HYG-03
  - TC-01
  - TC-02
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "HYG-03 / D-11：两个已分类 untracked duplicate PLAN 被删除；两个 canonical SUMMARY 的 pre/post SHA-256 完全相同，删除仅作为 hygiene evidence。"
    - "HYG-03 / D-12：正式 archive commit 恰含五个命名且非空文件，所有现有 canonical references 解析到这些 tracked committed paths。"
    - "D-13：一个 executable porcelain-v1/-z classifier 在 matrix commit 前只允许命名的 pre-existing `.omp-*` runtime 路径和 matrix transient，拒绝其余 modified product 与所有 untracked generated/cache/duplicate；commit 后再运行时不允许 matrix transient。"
    - "唯一 readiness matrix 汇总 HYG-01/02/03、TC-01/02 和 D-01..D-14；matrix 内容不能替代实际 status classifier 或 earlier behavior/gate run evidence。"
  artifacts:
    - path: ".planning/milestones/v1.0-research/SUMMARY.md"
      provides: "one member of exact tracked five-file canonical archive"
    - path: "scripts/classify_release_status.py"
      provides: "porcelain-v1/-z parser and fail-closed pre/post commit status classifier"
      exports: ["main"]
    - path: "scripts/tests/test_classify_release_status.py"
      provides: "allowlist and reject-every-other-path executable cases"
    - path: ".planning/phases/14-release-hygiene-toolchain-pinning/14-RELEASE-READINESS.md"
      provides: "single final five-requirement evidence/status matrix"
  key_links:
    - from: "scripts/classify_release_status.py"
      to: "git status --porcelain=v1 -z --untracked-files=all"
      via: "NUL-safe rename/status parsing with phase-mode exact allowlists"
      pattern: "--porcelain=v1"
    - from: ".planning/phases/14-release-hygiene-toolchain-pinning/14-RELEASE-READINESS.md"
      to: "Plans 14-01..14-04 execution evidence"
      via: "records exact run/test/commit/status commands and observed results"
      pattern: "TC-01|TC-02|HYG-01|HYG-02|HYG-03"
---

<objective>
独占完成 quick duplicate deletion、五文件 v1.0 archive commit 和唯一 final readiness/status classification（HYG-03、D-11/D-12/D-13），并汇总整个 Phase 14 的可执行证据。

Purpose: 保留 canonical history、拒绝伪造 clean-tree 自述，并使 14-04/14-05 文件与任务完全 MECE。
Output: quick hygiene evidence、exact tracked archive、porcelain classifier/test、single final matrix。
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-release-hygiene-toolchain-pinning/14-CONTEXT.md
@.planning/phases/14-release-hygiene-toolchain-pinning/14-RESEARCH.md
@.planning/phases/14-release-hygiene-toolchain-pinning/14-PATTERNS.md
@.planning/phases/14-release-hygiene-toolchain-pinning/14-01-SUMMARY.md
@.planning/phases/14-release-hygiene-toolchain-pinning/14-02-SUMMARY.md
@.planning/phases/14-release-hygiene-toolchain-pinning/14-03-SUMMARY.md
@.planning/phases/14-release-hygiene-toolchain-pinning/14-04-SUMMARY.md
@.planning/quick/260805-df9-add-a-kotlin-gradle-jetbrains-intellij-p/SUMMARY.md
@.planning/quick/260805-e28-align-the-jetbrains-plugin-wrapper-and-d/SUMMARY.md
</context>

<dependency_graph>
- Task 1 deletes only quick duplicates and emits digest evidence; it creates no Git deletion commit when inputs are untracked.
- Task 2 commits exactly the five archive paths and validates references; it is file-disjoint from Task 1.
- Task 3 needs all Phase 14 summaries plus Tasks 1/2 settled status, creates classifier/test and the only readiness matrix, commits them, then reruns strict post-commit classification.
</dependency_graph>

<tasks>

<task type="tracer">
  <name>Task 1: Delete only duplicate quick plans with digest-preserved summaries</name>
  <files>.planning/quick/260805-df9-add-a-kotlin-gradle-jetbrains-intellij-p/PLAN.md, .planning/quick/260805-e28-align-the-jetbrains-plugin-wrapper-and-d/PLAN.md</files>
  <read_first>
    - both named canonical quick SUMMARY.md files
    - both named duplicate quick PLAN.md files if present
    - .planning/phases/14-release-hygiene-toolchain-pinning/14-CONTEXT.md D-11, D-13
  </read_first>
  <action>
Per D-11/HYG-03, calculate and save SHA-256 for both tracked canonical SUMMARY files, delete only the two named untracked duplicate PLAN paths, then calculate again and require exact digest equality. Require the summaries remain tracked and their working-tree bytes equal `HEAD`. Record pre/post digests and absence commands in 14-05-SUMMARY evidence. Because the duplicates are untracked, do not invent a deletion commit or stage parent directories; if either path is tracked, halt for classification rather than changing ownership semantics. Preserve all runtime/user status per D-13.
  </action>
  <verify>
    <automated>python3 -c 'import hashlib,os,subprocess; from pathlib import Path; ps=[Path(".planning/quick/260805-df9-add-a-kotlin-gradle-jetbrains-intellij-p/SUMMARY.md"),Path(".planning/quick/260805-e28-align-the-jetbrains-plugin-wrapper-and-d/SUMMARY.md")]; recorded=dict(line.split("  ",1) for line in Path(os.environ["RUNNER_TEMP"],"phase14-quick-summary-pre.sha256").read_text().splitlines()); assert all(hashlib.sha256(p.read_bytes()).hexdigest()==recorded[str(p)] for p in ps); assert all(subprocess.run(["git","diff","--quiet","HEAD","--",str(p)]).returncode==0 for p in ps); assert not Path(".planning/quick/260805-df9-add-a-kotlin-gradle-jetbrains-intellij-p/PLAN.md").exists(); assert not Path(".planning/quick/260805-e28-align-the-jetbrains-plugin-wrapper-and-d/PLAN.md").exists()'</automated>
  </verify>
  <done>Exact pre/post digests prove canonical quick summaries are untouched while only the two untracked duplicate plans disappear as local hygiene evidence。</done>
</task>

<task type="auto">
  <name>Task 2: Commit and reference-check the exact five-file research archive</name>
  <reversibility rating="costly">Per D-12, removing this archive later breaks canonical references and audit history.</reversibility>
  <files>.planning/milestones/v1.0-research/STACK.md, .planning/milestones/v1.0-research/FEATURES.md, .planning/milestones/v1.0-research/ARCHITECTURE.md, .planning/milestones/v1.0-research/PITFALLS.md, .planning/milestones/v1.0-research/SUMMARY.md</files>
  <read_first>
    - exact five `.planning/milestones/v1.0-research/*.md` files
    - .planning/STATE.md and all tracked planning files containing `v1.0-research/`
    - .planning/phases/14-release-hygiene-toolchain-pinning/14-CONTEXT.md D-12, D-13
  </read_first>
  <action>
Per D-12/HYG-03, assert directory membership is exactly `ARCHITECTURE.md`, `FEATURES.md`, `PITFALLS.md`, `STACK.md`, `SUMMARY.md` and each file is nonempty. Parse each tracked textual reference containing `v1.0-research/`, resolve relative to the referencing file or planning root according to its syntax, and require the target exists within this five-file set. Stage and commit exactly these paths. Verify the resulting commit has exactly five changed members, `git ls-files --error-unmatch` accepts every member, and every member blob in the commit is nonempty. Stop on a sixth member, unresolved reference or unrelated staged path per D-13.
  </action>
  <verify>
    <automated>python3 -c 'import re,subprocess; from pathlib import Path; root=Path(".planning/milestones/v1.0-research"); expected={"ARCHITECTURE.md","FEATURES.md","PITFALLS.md","STACK.md","SUMMARY.md"}; assert {p.name for p in root.iterdir() if p.is_file()}==expected; assert all((root/n).stat().st_size&gt;0 for n in expected); changed=set(subprocess.check_output(["git","diff-tree","--no-commit-id","--name-only","-r","HEAD"]).decode().splitlines()); wanted={str(root/n) for n in expected}; assert changed==wanted; assert set(subprocess.check_output(["git","ls-files",str(root)]).decode().splitlines())==wanted; refs=[]; tracked=subprocess.check_output(["git","ls-files",".planning"]).decode().splitlines(); [refs.extend(re.findall(r"(?:\.planning/milestones/)?v1\.0-research/([A-Z-]+\.md)",Path(p).read_text(errors="strict"))) for p in tracked if Path(p).is_file()]; assert refs and all(r in expected and (root/r).is_file() for r in refs)'</automated>
  </verify>
  <done>The archive commit is exactly five nonempty tracked files and every canonical reference resolves to that set。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Execute the final porcelain classifier and commit the sole readiness matrix</name>
  <files>scripts/classify_release_status.py, scripts/tests/test_classify_release_status.py, .planning/phases/14-release-hygiene-toolchain-pinning/14-RELEASE-READINESS.md</files>
  <behavior>
    - Pre-commit: allow only explicitly configured pre-existing `.planning/.omp-next-action.json`, `.planning/.omp-task-results.json`, `.planning/.omp-checkpoint.json` plus the named readiness-matrix transient.
    - Post-commit: allow only the same explicitly present pre-existing runtime paths; matrix transient is forbidden.
    - Any other tracked modification/deletion/rename or any untracked generated/cache/quick-duplicate/product path fails, including paths containing spaces/newlines parsed through NUL records.
  </behavior>
  <read_first>
    - Plans 14-01..14-04 SUMMARYs and all exact commands/run IDs/commit IDs recorded there
    - .planning/REQUIREMENTS.md Phase 14 requirements
    - .planning/phases/14-release-hygiene-toolchain-pinning/14-CONTEXT.md D-01..D-14
    - raw `git status --porcelain=v1 -z --untracked-files=all`
  </read_first>
  <action>
Create `scripts/classify_release_status.py` with a NUL-safe porcelain-v1 parser including rename two-path records. `snapshot` records the exact pre-existing runtime allowlist only for the three named `.omp-*` paths that are currently present/modified; `classify --mode pre-matrix-commit` additionally permits only `14-RELEASE-READINESS.md` and the classifier/test paths being committed by this task; `classify --mode post-matrix-commit` permits no task transient. In both modes fail on every other modified/deleted/renamed product/planning path and every untracked generated/cache/duplicate path; an allowlisted runtime path must keep its recorded status class and may not broaden to another path. Print a deterministic JSON verdict only on success and nonzero with diagnostics on failure. Add subprocess/temp-repo tests for clean status, each allowed runtime path, matrix transient only in pre-mode, forbidden product edit, generated/cache/duplicate untracked paths, unknown `.omp-*`, rename, spaces/newlines and post-commit matrix drift.

Run the classifier pre-commit against real `git status --porcelain=v1 -z --untracked-files=all`, then create the only `14-RELEASE-READINESS.md`. For each HYG-01/02/03 and TC-01/02 record authoritative text, D-ID coverage, committed paths/commit IDs, actual focused command/run ID, observed exit/result and failure contract. Include freeze four-runner/attestation/verifier negative proof, installer behavior suite, aggregate behavior suite, actual nine-command release-gates run, exact JetBrains diff, ignore provenance, summary digests, exact archive/reference proof and pre-commit classifier verdict. State that matrix content is descriptive evidence, not the status check. Commit exactly classifier, test and matrix, then run post-commit classifier and record its verdict in 14-05-SUMMARY; if post-commit fails, Phase 14 is not ready.
  </action>
  <verify>
    <automated>python3 -m unittest scripts.tests.test_classify_release_status -v &amp;&amp; python3 scripts/classify_release_status.py classify --mode post-matrix-commit --snapshot "$RUNNER_TEMP/phase14-runtime-status.json" --porcelain-command 'git status --porcelain=v1 -z --untracked-files=all' &amp;&amp; python3 -c 'from pathlib import Path; s=Path(".planning/phases/14-release-hygiene-toolchain-pinning/14-RELEASE-READINESS.md").read_text(); assert all(x in s for x in ["HYG-01","HYG-02","HYG-03","TC-01","TC-02"]+[f"D-{i:02d}" for i in range(1,15)]); assert "porcelain=v1 -z" in s'</automated>
  </verify>
  <done>The executable post-commit classifier, not matrix prose, proves only named pre-existing runtime state remains；the sole matrix accurately links all five requirements to executed evidence。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Quick/archive paths → canonical history | Untracked duplicates sit beside tracked summaries and archive references. |
| Porcelain byte stream → readiness verdict | Filenames/status/renames are attacker-controlled and require NUL-safe parsing. |
| Matrix prose → phase verdict | Self-report must not replace executed tests/jobs/status. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-14-25 | Tampering/Information loss | quick summaries | high | mitigate | exact pre/post digests + HEAD byte equality；untracked deletion only evidence。 |
| T-14-26 | Tampering/Information loss | research archive | high | mitigate | exact five-member commit/tracking/nonempty/reference checks。 |
| T-14-27 | Spoofing | status parser | high | mitigate | porcelain-v1/-z parser tests for rename and unusual names；exact pre/post modes。 |
| T-14-28 | Repudiation | readiness matrix | medium | mitigate | matrix cites observed commands/run/commit IDs while classifier/test independently execute。 |
| T-14-29 | Tampering | runtime allowlist | high | mitigate | only three explicitly named pre-existing paths/status classes；unknown `.omp-*` fails。 |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | clean tree + pinned toolchain/gates | 14-01..14-05 | COVERED | Task 3 sole final executed audit。
REQ | HYG-01 | exact JetBrains commit/clean release tree | 14-04,14-05 | COVERED | final classifier audits settled tree。
REQ | HYG-02 | generated interface policy | 14-04,14-05 | COVERED | exact ignore proof + untracked rejection。
REQ | HYG-03 | cache/quick/archive classification | 14-04,14-05 | COVERED | 14-05 exclusively quick/archive/final audit。
REQ | TC-01 | exact pin + artifact evidence | 14-01..14-03,14-05 | COVERED | matrix cites executed signed/behavior evidence。
REQ | TC-02 | nine gates block publish | 14-03,14-05 | COVERED | matrix cites actual successful job run。
RESEARCH | Hygiene classification | digest quick + exact archive + runtime exclusion | 14-05 | COVERED | Tasks 1..3。
CONTEXT | D-01..D-09 | toolchain/gate/evidence chain | 14-01..14-03,14-05 | COVERED | final evidence mapping。
CONTEXT | D-10 | generated basename rule | 14-04,14-05 | COVERED | final status rejects strays。
CONTEXT | D-11 | cache/duplicate deletion + summary retention | 14-04,14-05 | COVERED | Task 1 owns duplicate/digest。
CONTEXT | D-12 | exact formal archive | 14-05 | COVERED | Task 2。
CONTEXT | D-13 | preserve named runtime/user state | 14-05 | COVERED | Task 3 executable classifier。
CONTEXT | D-14 | action-only JetBrains | 14-04,14-05 | COVERED | matrix cites exact diff proof。
</source_audit>

## Artifacts this phase produces
- Local deletion evidence for two untracked duplicate quick PLANs；canonical SUMMARYs unchanged。
- Exact tracked five-file `.planning/milestones/v1.0-research/` archive commit。
- `scripts/classify_release_status.py`, `scripts/tests/test_classify_release_status.py` and sole `14-RELEASE-READINESS.md`。

<verification>
- Execute quick digest comparison, exact archive member/tracking/reference checks, classifier unit tests, real pre-commit classification, and real strict post-commit classification。
- Matrix checks supplement but never replace executed freeze/installer/aggregate/release-gates/status evidence。
</verification>

<success_criteria>
1. 14-04 and 14-05 own no common files/tasks/artifacts。
2. Quick summaries and archive references are cryptographically/content-preserved and exact。
3. Post-commit porcelain classifier rejects all non-permitted drift and the sole matrix maps all requirements/decisions to observed evidence。
</success_criteria>

<output>
Create `.planning/phases/14-release-hygiene-toolchain-pinning/14-05-SUMMARY.md` with pre/post classifier JSON, summary digests, archive commit/members/references and final readiness verdict。
</output>
