---
phase: 14-release-hygiene-toolchain-pinning
plan: 04
type: execute
wave: 4
depends_on:
  - 14-03
files_modified:
  - .github/workflows/jetbrains-plugin.yml
  - .gitignore
  - fathom-sql/pkg.generated.mbti
  - .planning/research/.cache/
autonomous: true
requirements:
  - HYG-01
  - HYG-02
  - HYG-03
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "HYG-01 / D-14：JetBrains commit 的唯一 workflow delta 是 checkout v4→v7、setup-java v4→v5、upload-artifact v4→v7；路径和其他 YAML bytes 均不变。"
    - "HYG-02 / D-10：`.gitignore` 的仓库 basename rule 精确覆盖任意 `pkg.generated.mbti`，cache rule 精确覆盖 `.planning/research/.cache/`；手写 `.mbti` 和 `.omp-*` 可见。"
    - "HYG-03 / D-11：research cache 与 observed generated interface 被删除/忽略；quick duplicate、五文件 archive 和 readiness matrix 完全留给 14-05。"
    - "D-13：两个 path-specific commits 不暂存、不回退、不隐藏未分类用户/runtime 状态。"
  artifacts:
    - path: ".github/workflows/jetbrains-plugin.yml"
      provides: "exact action-only v7/v5/v7 committed delta"
    - path: ".gitignore"
      provides: "exact repository basename and research-cache rules"
    - path: "fathom-sql/pkg.generated.mbti"
      provides: "classified generated output removed/untracked if present"
    - path: ".planning/research/.cache/"
      provides: "classified regenerable cache removed and ignored"
  key_links:
    - from: ".gitignore"
      to: "all MoonBit package generated interfaces"
      via: "git check-ignore -v reports the exact basename rule"
      pattern: "pkg\.generated\.mbti"
---

<objective>
仅收口 JetBrains action delta、generated-interface ignore 和 research-cache hygiene（HYG-01/02 与 HYG-03 cache 部分，D-10/D-11/D-13/D-14）；不触碰 quick/archive/final audit。

Purpose: 使 action-only commit 与 ignore/cache policy 各自可执行证明，且与 14-05 的 history/archive/readiness 职责互斥。
Output: exact JetBrains workflow commit，以及 `.gitignore` + generated/cache hygiene commit。
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-release-hygiene-toolchain-pinning/14-CONTEXT.md
@.planning/phases/14-release-hygiene-toolchain-pinning/14-RESEARCH.md
@.planning/phases/14-release-hygiene-toolchain-pinning/14-PATTERNS.md
@.planning/phases/14-release-hygiene-toolchain-pinning/14-03-SUMMARY.md
@.github/workflows/jetbrains-plugin.yml
@.gitignore
</context>

<dependency_graph>
- Task 1 creates one exact path commit and no artifact consumed by Task 2.
- Task 2 creates a disjoint path-specific ignore/cache commit.
- 14-05 follows this plan only so its final classifier and matrix see settled product/hygiene commits; no files overlap.
</dependency_graph>

<tasks>

<task type="tracer">
  <name>Task 1: Prove and commit the exact JetBrains action-only delta</name>
  <files>.github/workflows/jetbrains-plugin.yml</files>
  <read_first>
    - .github/workflows/jetbrains-plugin.yml
    - .planning/phases/14-release-hygiene-toolchain-pinning/14-CONTEXT.md D-13, D-14
    - .planning/REQUIREMENTS.md HYG-01
  </read_first>
  <action>
Per D-14/HYG-01, derive the parent baseline from `git show HEAD:.github/workflows/jetbrains-plugin.yml`, construct an expected file by replacing exactly three scalar action refs—checkout v4 with v7, setup-java v4 with v5, upload-artifact v4 with v7—and byte-compare expected to the working file. Require `git diff --name-only -- .github/workflows/jetbrains-plugin.yml` to name only that path and `git diff --word-diff=porcelain --` to contain only those three old/new action values; any trigger, permission, Gradle, Kotlin, IDE compatibility, publication, artifact name/path or whitespace delta fails before staging. Stage and commit only this path, then assert the created commit's changed-path set is exactly `.github/workflows/jetbrains-plugin.yml` and its parent→commit byte transform is exactly the same three replacements. Preserve all other status per D-13.
  </action>
  <verify>
    <automated>python3 -c 'import subprocess; from pathlib import Path; parent=subprocess.check_output(["git","show","HEAD^:.github/workflows/jetbrains-plugin.yml"]); current=subprocess.check_output(["git","show","HEAD:.github/workflows/jetbrains-plugin.yml"]); expected=parent.replace(b"actions/checkout@v4",b"actions/checkout@v7").replace(b"actions/setup-java@v4",b"actions/setup-java@v5").replace(b"actions/upload-artifact@v4",b"actions/upload-artifact@v7"); assert current==expected; assert subprocess.check_output(["git","diff-tree","--no-commit-id","--name-only","-r","HEAD"]).splitlines()==[b".github/workflows/jetbrains-plugin.yml"]'</automated>
  </verify>
  <done>The committed JetBrains path is byte-identical to its parent except the exact three D-14 action-version replacements。</done>
</task>

<task type="auto">
  <name>Task 2: Enforce exact generated-interface and research-cache hygiene</name>
  <files>.gitignore, fathom-sql/pkg.generated.mbti, .planning/research/.cache/</files>
  <read_first>
    - .gitignore
    - .planning/phases/14-release-hygiene-toolchain-pinning/14-RESEARCH.md §Hygiene Artifact Classification
    - .planning/phases/14-release-hygiene-toolchain-pinning/14-CONTEXT.md D-10, D-11, D-13
  </read_first>
  <action>
Per D-10/HYG-02, add exactly one repository basename pattern `pkg.generated.mbti` and exact cache pattern `.planning/research/.cache/`. Delete the observed generated `fathom-sql/pkg.generated.mbti`; if `git ls-files` finds any tracked generated basename, remove only those exact generated paths from tracking while retaining handwritten/source `.mbti`. Per D-11/HYG-03, delete only `.planning/research/.cache/`; do not touch either quick directory or milestone archive.

Prove ignore provenance with `git check-ignore -v --no-index` and a small Python parser that splits each output record once at TAB. For `probe/pkg.generated.mbti`, require the post-tab pathname exactly equals that probe and parse the pre-tab value with `rsplit(":", 2)` into source `.gitignore`, a positive decimal line number, and exact pattern `pkg.generated.mbti`. Independently do the same for `.planning/research/.cache/probe.json`, requiring exact pathname and pattern `.planning/research/.cache/`. Representative `probe/handwritten.mbti`, `.planning/.omp-next-action.json`, `.planning/.omp-task-results.json`, and `.planning/.omp-checkpoint.json` must all return the unignored status. Commit only `.gitignore` plus exact tracked generated removals; cache/generated untracked deletion is hygiene evidence, not a fabricated Git deletion. D-13 rejects unexpected paths without cleaning them.
  </action>
  <verify>
    <automated>python3 -c 'import subprocess; cases={"probe/pkg.generated.mbti":"pkg.generated.mbti",".planning/research/.cache/probe.json":".planning/research/.cache/"};
for path,expected in cases.items():
 out=subprocess.check_output(["git","check-ignore","-v","--no-index",path],text=True).rstrip("\n"); pre,actual=out.split("\t",1); source,line,pattern=pre.rsplit(":",2); assert source==".gitignore" and line.isdecimal() and int(line)&gt;0 and pattern==expected and actual==path' &amp;&amp; ! git check-ignore -q --no-index probe/handwritten.mbti &amp;&amp; ! git check-ignore -q --no-index .planning/.omp-next-action.json &amp;&amp; ! git check-ignore -q --no-index .planning/.omp-task-results.json &amp;&amp; ! git check-ignore -q --no-index .planning/.omp-checkpoint.json &amp;&amp; test ! -e fathom-sql/pkg.generated.mbti &amp;&amp; test ! -e .planning/research/.cache/</automated>
  </verify>
  <done>Exact-rule provenance and negative examples prove generated/cache output is hidden without hiding handwritten interfaces, runtime state, quick history or milestone archives。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Existing workflow delta → commit | Intended action refs are adjacent to immutable plugin behavior. |
| Ignore rules → repository visibility | Broad rules could hide source/history/runtime drift. |
| Untracked generated/cache → hygiene evidence | Absence is local hygiene, not necessarily a Git deletion delta. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-14-19 | Tampering | JetBrains workflow | high | mitigate | parent→expected byte transform and exact one-path commit proof。 |
| T-14-20 | Tampering | `.gitignore` | high | mitigate | exact `check-ignore -v` source/rule and negative handwritten/runtime cases。 |
| T-14-21 | Repudiation | untracked deletion | medium | mitigate | record as verified local hygiene, never claim nonexistent Git delta。 |
| T-14-22 | Information loss | user/runtime paths | high | mitigate | exact path operations and fail on unexpected status per D-13。 |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | clean working tree component | 14-04 | COVERED | action/generated/cache only。
REQ | HYG-01 | exact JetBrains action upgrades committed | 14-04 | COVERED | Task 1 byte proof。
REQ | HYG-02 | generated-interface policy | 14-04 | COVERED | Task 2 provenance/negative proof。
REQ | HYG-03 | research cache classified/removed | 14-04 | COVERED | Task 2 cache slice；quick/archive in 14-05。
RESEARCH | Hygiene classification | exact JetBrains/generated/cache handling | 14-04 | COVERED | Tasks 1/2。
CONTEXT | D-10 | basename generated rule | 14-04 | COVERED | Task 2。
CONTEXT | D-11 | cache deletion | 14-04 | COVERED | Task 2；quick half in 14-05。
CONTEXT | D-13 | explicit paths, preserve runtime/user work | 14-04 | COVERED | both tasks。
CONTEXT | D-14 | exact action-only state | 14-04 | COVERED | Task 1。
</source_audit>

## Artifacts this phase produces
- `.github/workflows/jetbrains-plugin.yml` exact three-action commit。
- `.gitignore` exact generated-interface/cache policy。
- Local removal of classified `fathom-sql/pkg.generated.mbti` and `.planning/research/.cache/`。

<verification>
- Execute parent→commit byte-transform/path checks and exact `git check-ignore -v` positive/negative cases。
- Do not touch or verify quick/archive/matrix here；14-05 owns those exclusively。
</verification>

<success_criteria>
1. JetBrains commit is exactly one path and three replacements。
2. Exact ignore rules hide generated/cache but not handwritten `.mbti` or `.omp-*`。
3. No 14-05-owned path appears in files/tasks/artifacts。
</success_criteria>

<output>
Create `.planning/phases/14-release-hygiene-toolchain-pinning/14-04-SUMMARY.md` with exact commit IDs, byte proof and ignore provenance output。
</output>
