---
phase: 14-release-hygiene-toolchain-pinning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/probe_moonbit_freeze.py
  - scripts/verify_moonbit_freeze.py
  - scripts/tests/test_verify_moonbit_freeze.py
  - .planning/phases/14-release-hygiene-toolchain-pinning/14-FREEZE-EVIDENCE.json
  - .planning/phases/14-release-hygiene-toolchain-pinning/14-FREEZE-ATTESTATIONS.jsonl
  - .github/moonbit-toolchain.json
autonomous: false
requirements:
  - TC-01
user_setup:
  - service: github
    why: "D-01 requires genuine hosted Linux, Intel macOS, Apple-silicon macOS, and Windows execution plus GitHub artifact attestations before the lock exists"
    env_vars: []
    dashboard_config:
      - task: "Authenticate gh with a caller credential limited to pushing/deleting the temporary branch and reading Actions runs/artifacts; repository Actions must allow OIDC attestations"
        location: "GitHub CLI gh auth login / repository Actions settings; caller PAT/CLI scope is separate from job GITHUB_TOKEN permissions"
estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "TC-01 / D-01（2026-08-14 修订）：一个官方内容锁定身份先通过五 archive 静态验证（三 binary 官方 sidecar + 二 core 记录式摘要），再在 Linux x86_64、Darwin arm64、Windows x86_64 三个原生 GitHub runner 上执行。"
    - "D-03（2026-08-14 修订）：结构化证据精确包含五个 archive 记录（三 binary 官方 sidecar 摘要 + 二 core 记录式摘要与官方 URL/审计声明）、三个 runner 记录及其 GitHub attestation；最终 URL、sidecar 文件名/摘要、布局安全、host/executable 架构与完整 raw version 全部由 verifier fail closed 校验。"
    - "lock 仅由 verifier 在全部记录成功后原子 rename 产生；缺失、重复、未知、畸形、身份不符或任一 runner 非零时没有 lock。"
    - "临时 probe workflow/script 只存在于认证的临时远端分支；证据收集后删除该分支，当前实现分支的 workflow、helper 与 product 文件在 freeze 成功前不变。"
  artifacts:
    - path: "scripts/probe_moonbit_freeze.py"
      provides: "静态官方 GET/sidecar/archive 检查、临时 workflow 生成和跨平台 runner probe"
    - path: "scripts/verify_moonbit_freeze.py"
      provides: "精确集合、attestation、身份和原子 lock 的 fail-closed verifier"
      exports: ["main"]
    - path: "scripts/tests/test_verify_moonbit_freeze.py"
      provides: "成功与缺失/畸形/篡改证据的 executable negative proof"
    - path: ".planning/phases/14-release-hygiene-toolchain-pinning/14-FREEZE-EVIDENCE.json"
      provides: "exact five archives (three binary + two core) plus exact three native runner records"
    - path: ".planning/phases/14-release-hygiene-toolchain-pinning/14-FREEZE-ATTESTATIONS.jsonl"
      provides: "downloaded GitHub Sigstore attestation bundles bound to probe run and record digests"
    - path: ".github/moonbit-toolchain.json"
      provides: "only-on-success immutable six-record toolchain lock"
  key_links:
    - from: "temporary phase14-freeze-probe workflow"
      to: ".planning/phases/14-release-hygiene-toolchain-pinning/14-FREEZE-EVIDENCE.json"
      via: "gh workflow run/watch/download and gh attestation verify bound to probe commit/run"
      pattern: "ubuntu-24.04|macos-14|windows-2025"
    - from: "scripts/verify_moonbit_freeze.py"
      to: ".github/moonbit-toolchain.json"
      via: "validate exact archive/runner/attestation sets, then fsync temporary file and os.replace"
      pattern: "os\.replace"
---

<objective>
在任何持久 CI workflow/helper 变更前，以可执行的官方静态探针和三种真实原生 GitHub runner（Linux x86_64 / macOS arm64 / Windows x86_64）冻结唯一 MoonBit 内容锁定身份；只有结构化证据与签名验证全部通过才原子生成 lock（TC-01，D-01、D-03、D-13）。

Purpose: 消除 Linux executor 伪称能证明 Darwin/Windows、以及 Markdown/自写 JSON 可伪造的缺口；`latest`、mirror、emulation、平台删除均无降级路径。
Output: probe/verifier/test、三 runner + 五 archive 结构化证据、attestation bundle，以及成功路径唯一 lock。
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-release-hygiene-toolchain-pinning/14-CONTEXT.md
@.planning/phases/14-release-hygiene-toolchain-pinning/14-RESEARCH.md
@.planning/phases/14-release-hygiene-toolchain-pinning/14-PATTERNS.md
@.github/workflows/ci.yml
@.github/workflows/fathom-native-release.yml
@moon.mod
</context>

<dependency_graph>
- Task 1 first performs read-only static acquisition; only a successful static candidate may enter the authenticated temporary-branch runner probe.
- Task 1 creates all structured evidence and invokes the verifier; lock creation is the final atomic operation, never an input fabrication step.
- Task 2 is the explicit blocking evidence checkpoint because GitHub authentication/permissions and four hosted runner classes are external prerequisites a local Linux agent cannot guarantee.
- Plans 14-02..14-05 depend transitively on this plan; no lock/checkpoint approval means they are unreachable.
</dependency_graph>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Run the official static probe and authenticated three-native-runner freeze</name>
  <precondition>`gh auth status` succeeds for `tchivs/fathom-sql`; the caller credential can push/delete only the temporary branch and read Actions runs/artifacts, while repository Actions permits a job `GITHUB_TOKEN` with `contents: read`, `id-token: write`, and `attestations: write`; otherwise stop at the blocking evidence checkpoint with no lock.</precondition>
  <reversibility rating="costly">Per D-01, changing this compiler identity later forces every platform build and parity artifact to be requalified.</reversibility>
  <files>scripts/probe_moonbit_freeze.py, scripts/verify_moonbit_freeze.py, scripts/tests/test_verify_moonbit_freeze.py, .planning/phases/14-release-hygiene-toolchain-pinning/14-FREEZE-EVIDENCE.json, .planning/phases/14-release-hygiene-toolchain-pinning/14-FREEZE-ATTESTATIONS.jsonl, .github/moonbit-toolchain.json</files>
  <behavior>
    - Static success: exactly three binary and two core archives GET successfully, end at official URLs; the three binary `.sha256` sidecars parse the matching filename/digest and hash-match; core digests are computed and recorded with official-URL provenance (official core sidecars do not exist per D-03 revision); all pass traversal/link/duplicate/root-layout checks.
    - Runner success: exact targets `linux-x86_64`, `macos-aarch64`, `windows-x86_64` report expected native host/executable architectures and byte-identical complete `moon version` bytes.
    - Failure: missing/duplicate/unknown/malformed records, bad attestation/run identity, redirect drift, unsafe layout, wrong architecture/version, or nonzero probe exits nonzero and leaves `.github/moonbit-toolchain.json` absent.
  </behavior>
  <read_first>
    - .planning/phases/14-release-hygiene-toolchain-pinning/14-RESEARCH.md §§Official Toolchain Acquisition Findings, Mandatory Task 1, Environment Availability
    - .planning/phases/14-release-hygiene-toolchain-pinning/14-CONTEXT.md D-01, D-03, D-13
    - official Unix/PowerShell installers and checksum documentation cited by 14-RESEARCH.md
    - .github/workflows/ci.yml and .github/workflows/fathom-native-release.yml read-only baseline digests
  </read_first>
  <action>
Per D-01/D-03, implement `probe_moonbit_freeze.py` and `verify_moonbit_freeze.py` with Python stdlib only. Before any branch/workflow mutation, `probe_moonbit_freeze.py static` obtains an official static channel key from an official response, then GETs with redirect capture exactly `moonbit-linux-x86_64.tar.gz`, `moonbit-darwin-aarch64.tar.gz`, `moonbit-windows-x86_64.zip`, `core-&lt;key&gt;.tar.gz`, `core-&lt;key&gt;.zip` and the three binary `.sha256` sidecars (official core sidecars do not exist; their computed digests are recorded with official-URL provenance per the D-03 revision). Require HTTP 200/nonempty, official final URLs, matching sidecar filename and lowercase SHA-256 for binaries, byte match, no absolute/parent/escaping-link/duplicate entries, and exact binary `bin/lib/include/share` or core `core/` roots. Write only a temporary candidate JSON outside the repository; failure removes candidate/lock and returns nonzero.

After static success, capture `SOURCE_COMMIT=$(git rev-parse HEAD)`, `REPO=$(gh repo view --json nameWithOwner -q .nameWithOwner)`, candidate SHA-256, and baseline digests of both persistent workflows. In a fresh temporary clone create the exact branch `TEMP_BRANCH=phase14-freeze-&lt;UTC&gt;-&lt;nonce&gt;` from `SOURCE_COMMIT`; add only `.github/workflows/phase14-freeze-probe.yml`, `.github/scripts/probe_moonbit_freeze.py`, and the candidate JSON, commit locally, and record `PROBE_COMMIT=$(git rev-parse HEAD)`. The temporary workflow trigger is only `push.branches: ["$TEMP_BRANCH"]` with the generated literal exact branch (not `workflow_dispatch`); pushing `HEAD:refs/heads/$TEMP_BRANCH` starts the run. It declares exactly `permissions: { contents: read, id-token: write, attestations: write }`; add `artifact-metadata: write` only when the selected official action mode demonstrably requires artifact metadata and document that requirement. Caller PAT/CLI permissions never substitute for this job `GITHUB_TOKEN` block. The workflow has one four-row matrix exactly: `(ubuntu-24.04, linux-x86_64)`, `(macos-15-intel, macos-x86_64)`, `(macos-14, macos-aarch64)`, `(windows-2025, windows-x86_64)`; checkout uses `${{ github.sha }}` and the probe asserts checked-out SHA equals `GITHUB_SHA`, `github.event_name == 'push'`, `github.ref == 'refs/heads/$TEMP_BRANCH'`, and candidate digest equals the committed candidate digest. Unix steps use `set -euo pipefail`; PowerShell uses `$ErrorActionPreference='Stop'` and `$PSNativeCommandUseErrorActionPreference=$true`; no `continue-on-error` or fallback exists.

Each row re-downloads and verifies its binary plus correct core/sidecars before extraction, observes host architecture (`uname -m` or .NET OSArchitecture), inspects executable architecture (`file`/`lipo -archs` or PE machine), executes natively, and writes one canonical `records/&lt;targetPlatform&gt;/moon-toolchain-runner.json` with schemaVersion, sourceCommit, probeCommit, candidateSha256, repository, runId, runAttempt, job/runner labels, targetPlatform, requested/final archive+sidecar URLs, sidecar filename/digest, computed digests, archive safety/layout verdicts, observed host/executable architectures, exit code, and base64 of exact raw stdout bytes. For each exact record path invoke `actions/attest-build-provenance@v3` with `subject-path: records/&lt;targetPlatform&gt;/moon-toolchain-runner.json`; capture its `attestation-id`, `attestation-url`, and `bundle-path` outputs (or the action's documented equivalent names) into a target-specific identity JSON, preserve the returned bundle, then upload record, identity, and bundle under one target-unique artifact.

Bind orchestration to workflow path/name, exact branch, and pushed SHA: after the push, query Actions runs for `.github/workflows/phase14-freeze-probe.yml`, branch `$TEMP_BRANCH`, and event `push`; filter JSON to `headSha == "$PROBE_COMMIT"`, require exactly one match (zero or multiple is fatal), and require its `event`, `headBranch`, `headSha`, and workflow path/name all match before assigning `RUN_ID`. Run `gh run watch "$RUN_ID" --exit-status`, then `gh run download "$RUN_ID" --dir "$EVIDENCE_DIR"`; download must finish and all three record/identity/bundle triples must exist before cleanup is enabled. For each record run `gh attestation verify "$record" --repo "$REPO" --signer-workflow "$REPO/.github/workflows/phase14-freeze-probe.yml" --source-ref "refs/heads/$TEMP_BRANCH" --format json`; preserve the JSON and bundle in `14-FREEZE-ATTESTATIONS.jsonl`, and require verified repository, subject digest, workflow path, source ref, and source/head SHA equal `$REPO`, the record SHA-256, the temporary workflow, the exact branch, and `$PROBE_COMMIT`. Only after evidence has been copied into the persistent evidence paths may cleanup delete `refs/heads/$TEMP_BRANCH` and the temporary clone; cleanup must never delete `$EVIDENCE_DIR`, `14-FREEZE-EVIDENCE.json`, or `14-FREEZE-ATTESTATIONS.jsonl`. Verify the branch no longer resolves and persistent workflow digests remain unchanged.

Normalize neither raw version nor architectures. `verify_moonbit_freeze.py` must require exactly five archive records (three binary with official sidecar identities/digests, two core with recorded digests and official-URL provenance) and exactly three runner records, unique roles/targets, exact source/probe/candidate/run identity, valid attestation subject digests, official final URLs, sidecar identities/digests, safe layouts, target-specific host/executable architectures, zero exits, and byte-identical decoded raw version. Only then write a same-directory temporary lock containing schemaVersion, staticChannelKey, exact expectedMoonVersion bytes encoding, and five explicit archive records (three binary with official sidecar digests + two core with recorded digests); flush/fsync and `os.replace` it atomically. Add `test_verify_moonbit_freeze.py` that synthesizes deterministic signed-metadata-shaped fixtures and proves valid output plus missing/malformed/tampered evidence nonzero with no lock. Never accept `latest`, mirror, emulation, relabeling or platform deletion.
  </action>
  <verify>
    <automated>python3 -m unittest scripts.tests.test_verify_moonbit_freeze -v &amp;&amp; python3 scripts/verify_moonbit_freeze.py --evidence .planning/phases/14-release-hygiene-toolchain-pinning/14-FREEZE-EVIDENCE.json --attestations .planning/phases/14-release-hygiene-toolchain-pinning/14-FREEZE-ATTESTATIONS.jsonl --output .github/moonbit-toolchain.json &amp;&amp; python3 scripts/probe_moonbit_freeze.py verify-cleanup --evidence .planning/phases/14-release-hygiene-toolchain-pinning/14-FREEZE-EVIDENCE.json --workflows .github/workflows/ci.yml .github/workflows/fathom-native-release.yml</automated>
  </verify>
  <done>真实三 runner、五 archive（三 binary 官方 sidecar + 二 core 记录式摘要）、attestation 和 raw-version verifier 全部成功并原子产生 lock；测试证明缺失/畸形/篡改证据非零且无输出。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Approve externally executed native evidence barrier</name>
  <what-built>TC-01 的官方静态 acquisition、GitHub 四原生 runner run、签名证据、fail-closed verifier 与原子 lock；D-01/D-03 禁止的降级均未使用。</what-built>
  <how-to-verify>
1. 打开 Task 1 记录的 GitHub Actions RUN_ID，确认三行 runner 均成功且 head SHA 等于记录的 PROBE_COMMIT。
2. 核对 `14-FREEZE-EVIDENCE.json` 恰有三 runner/五 archive，`14-FREEZE-ATTESTATIONS.jsonl` 的三个 subject digest 均由 `gh attestation verify` 通过。
3. 确认 Apple-silicon 行 host 与 executable 均为 arm64；Windows 行为 x86_64；Linux 行为 x86_64。
4. 确认远端临时分支已删除，持久 workflow baseline digest 未变；随后确认 verifier negative suite 非零路径均未留下 lock。
  </how-to-verify>
  <resume-signal>输入 `approved` 并附 RUN_ID，或指出证据/权限问题；未批准不得执行 14-02。</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Official distribution → static probe | 网络响应、重定向、sidecar 与 archive 均不可信。 |
| Temporary GitHub branch → four hosted runners | 临时 workflow 具有运行身份但不得落入实现分支。 |
| Runner artifacts/attestations → verifier | JSON label 不可信，须绑定 run/probe commit/subject digest 与实际架构。 |
| Verifier → committed lock | 仅 exact-set 全成功可原子发布 lock。 |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-14-01 | Spoofing/Tampering | official URLs/sidecars | high | mitigate | GET final immutable URL、filename/vendor/computed digest 三方一致。 |
| T-14-02 | Tampering/Elevation | archives | high | mitigate | 解包前 traversal/link/duplicate/root 检查及隔离目录。 |
| T-14-03 | Spoofing | native architecture | high | mitigate | 三个明确 runner label + observed host/executable checks；attestation 绑定。 |
| T-14-04 | Repudiation | evidence JSON | high | mitigate | GitHub provenance attestation、run/probe/source/candidate identity 和 exact-set verifier。 |
| T-14-05 | Tampering | lock publication | high | mitigate | fail-closed tests、fsync + same-directory atomic replace；失败无 lock。 |
| T-14-06 | Elevation | temporary workflow permissions | high | mitigate | exact branch-only push trigger；job token only contents read + OIDC/attestation writes（artifact metadata only if action requires）；caller credential separately limited to branch mutation/Actions read；evidence persists before cleanup；blocking human review。 |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | 可重复钉版发布工具链 | 14-01 | COVERED | concrete static + native runner freeze barrier。
REQ | TC-01 | exact pin and recorded toolchain identity | 14-01 | COVERED | signed structured proof precedes lock。
RESEARCH | Freeze | five archives + three native executions | 14-01 | COVERED | Task 1 exact orchestration。
CONTEXT | D-01 | same officially available exact version on three platforms | 14-01 | COVERED | exact runner matrix, no fallback。
CONTEXT | D-03 | official checksum + exact raw version fail closed | 14-01 | COVERED | verifier/test/atomic output。
CONTEXT | D-13 | preserve workflows/user/runtime state | 14-01 | COVERED | temporary clone/branch and baseline digest proof。
</source_audit>

## Artifacts this phase produces
- `scripts/probe_moonbit_freeze.py`, `scripts/verify_moonbit_freeze.py`, `scripts/tests/test_verify_moonbit_freeze.py`。
- `14-FREEZE-EVIDENCE.json` exact five archive/three runner records and `14-FREEZE-ATTESTATIONS.jsonl` verified bundles。
- `.github/moonbit-toolchain.json` only after total success。

<verification>
- Execute focused verifier tests, the real `gh workflow run/watch/download` path, four `gh attestation verify` calls, final exact-set verification, negative no-lock proof, and temporary branch/workflow cleanup proof.
- Do not run project build/format/lint suites in this plan；the downloaded compiler runs only for freeze identity proof。
</verification>

<success_criteria>
1. A concrete signed three-native-runner run proves all target architectures and identical raw version。
2. Structured verifier proves exact three/five sets, official URLs, sidecars, safety/layout and identity；failure cannot leave a lock。
3. Persistent workflows/product files remain unchanged until approved freeze evidence exists。
</success_criteria>

<output>
Create `.planning/phases/14-release-hygiene-toolchain-pinning/14-01-SUMMARY.md` after checkpoint approval；include RUN_ID, source/probe commits and verifier/test commands。
</output>
