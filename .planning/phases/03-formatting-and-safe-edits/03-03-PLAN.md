---
phase: 03-formatting-and-safe-edits
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - formatter/layout.mbt
  - formatter/case.mbt
  - formatter/format.mbt
  - test/formatter_test.mbt
autonomous: true
requirements: [FMT-02, FMT-03]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "User can configure all six format dimensions and each is functional end-to-end: keyword case (Upper/Lower), indentation width, line width, comma style (Trailing/Leading), newline style (FollowInput/Lf/Crlf), trailing-newline on/off; invalid values are rejected at FormatOptions::new with FormatError (FMT-02, D-26, ASVS V5)."
    - "The corpus contract holds: every fixture from the 44-row manifest is mirrored as an embedded FormatterFixture (runtime never reads disk); expected_valid fixtures are byte-exact idempotent and reparse with zero diagnostics; expected_error and version-negative fixtures refuse with DORIS-FORMAT-001 — the malformed/error half of D-34 (D-34/D-35, FMT-03)."
    - "Idempotence holds under every supported option combination: format(format(x)) == format(x) byte-exact for at least one representative fixture per option combination (D-34, FMT-03)."
    - "The formatter never panics: boundary inputs (empty, whitespace-only, max_bytes boundary, slice failures) produce refusal diagnostics or ParseError, never a crash (threat T-03-02, research Pitfall 4)."
    - "[probe FMT-03 unclassified] resolved: refusal is the mode chosen for unsafe transformations — D-33 is absolute (no partial output, no best-effort); the 'refuses or reports' wording of FMT-03 is satisfied by refuse-with-structured-diagnostic, which is strictly stronger than report."
  flagged_assumptions:
    - "FLAGGED-ASSUMPTION [research A3, resolved]: trailing-newline policy implemented as normalize-to-exactly-one — asserted idempotent for both on and off values (already locked in 03-01; this plan adds the off-value end-to-end fixtures)."
    - "FLAGGED-ASSUMPTION [research A7, resolved]: keyword case rewriting is profile-independent — classification_of has no profile parameter; the corpus harness asserts identical formatting across 2.1/3.x/4.x fixtures where the same statement is valid in multiple profiles."
    - "FLAGGED-ASSUMPTION [corpus authoring]: embedded fixtures are authored from corpus/manifest.tsv and corpus/doris-{2.1,3.x,4.x}/*.sql at implementation time (authoring reads disk); runtime tests never load the files (STATE.md convention)."
  artifacts:
    - path: "formatter/case.mbt"
      provides: "rewrite_keyword_case(raw, KeywordCase) with ASCII case-folding for Lower"
    - path: "formatter/layout.mbt"
      provides: "line_width wired into every measure-then-break decision; indent wired into break_line; comma_style and newline_style and trailing_newline honored everywhere"
    - path: "formatter/format.mbt"
      provides: "boundary-safe entry (empty/whitespace-only documents, slice-failure refusal)"
    - path: "test/formatter_test.mbt"
      provides: "full 44-row FormatterFixture mirror, option-combination idempotence matrix, boundary/never-panic tests, golden review records"
  key_links:
    - from: "formatter/case.mbt"
      to: "formatter/options.mbt"
      via: "rewrite_keyword_case branches on KeywordCase (Upper = entry.word, Lower = ASCII case-fold of entry.word)"
    - from: "formatter/layout.mbt"
      to: "formatter/options.mbt"
      via: "every Layout decision reads options.line_width()/indent()/comma_style()/newline_style()/trailing_newline() — no hard-coded defaults left in layout"
    - from: "test/formatter_test.mbt"
      to: "corpus/manifest.tsv"
      via: "the embedded FormatterFixture array mirrors the 44 manifest rows (fixture_id and expected_valid copied; raw bytes embedded at authoring time)"
    - from: "test/formatter_test.mbt"
      to: "api/api.mbt"
      via: "format_with_ids/format_with_metadata drive every corpus fixture; parse_with_ids re-parses output for the D-35 gate"
  prohibitions:
    - "No hard-coded layout constants remain in layout.mbt — every policy value flows from FormatOptions (D-26 'no extra config items' also means no hidden constants)."
    - "No fixture is skipped: every manifest row is mirrored; malformed rows assert refusal rather than being omitted (research Pitfall 6 — the malformed half of D-34 is a refusal assertion, not a skipped test)."
    - "Goldens are reviewed before commit, not blind-updated via moon test --update (research Pitfall 7)."
    - "No new diagnostic codes beyond DORIS-FORMAT-001 this plan — refusal is the only format diagnostic the phase defines."
assumption_delta_decision:
  noun: "exact lossless replay (print_lossless baseline)"
  decision: "add-alongside"
  rationale: "D-27 keeps formatter/ separate from printer/; print_lossless (lossless contract) and canonical format (policy rendering) are distinct consumer operations; promote would break the lossless round-trip contract"
  no_accepted_debt: "deliberate architecture boundary"
---

<objective>
**As a** Doris SQL consumer, **I want to** configure all six format dimensions and trust corpus-wide idempotence and reparse guarantees, **so that** my chosen style is honored everywhere while correctness is a CI contract.

Purpose: Make every FormatOptions field functional end-to-end (FMT-02) and lock the corpus-wide safety contract (FMT-03): all six dimensions drive real layout behavior, the 44-row manifest is mirrored into an embedded fixture harness proving byte-exact idempotence and zero-diagnostic reparse for accepted inputs and DORIS-FORMAT-001 refusal for error/version-negative inputs, option-combination idempotence is asserted, and the formatter is proven never to panic on boundary inputs.
Output: Lower-case keyword rewriting, all policy values wired into layout, the full embedded corpus harness, the option-matrix idempotence suite, and boundary/never-panic tests.
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-formatting-and-safe-edits/03-RESEARCH.md
@.planning/phases/03-formatting-and-safe-edits/03-PATTERNS.md
@.planning/phases/03-formatting-and-safe-edits/03-CONTEXT.md
@.planning/phases/03-formatting-and-safe-edits/03-01-SUMMARY.md
@.planning/phases/03-formatting-and-safe-edits/03-02-SUMMARY.md

Plans 03-01/03-02 delivered the formatter core, full per-family layout, list measure-then-break, and script/comment handling. This plan completes the FMT-02 dimension functionality and the FMT-03 corpus contract. The D-35 reparse gate and D-34 byte-exact idempotence oracle are the project's formatting safety contract (research Pattern 6 mirrors the Phase 2 embedded fixture convention, test/parser_test.mbt:463-505 — runtime tests never read disk, STATE.md). The manifest has 44 rows; 31 disk fixture files exist under corpus/doris-{2.1,3.x,4.x}/ (03-RESEARCH, verified). Every manifest row must be mirrored — none skipped, malformed rows assert refusal (research Pitfall 6).

The classification table (token.mbt:450-460) is the single keyword authority: every row's canonical word is UPPERCASE; Lower-case output is the ASCII case-fold of that canonical word (SQL keywords are ASCII). All six dimensions stay exactly the six D-26 fields — no extra config items.
</context>

<tasks>

<task type="auto">
  <name>Task 1: All six dimensions functional with no hard-coded layout constants</name>
  <files>formatter/case.mbt, formatter/layout.mbt, test/formatter_test.mbt</files>
  <read_first>
    @.planning/phases/03-formatting-and-safe-edits/03-RESEARCH.md
    @.planning/phases/03-formatting-and-safe-edits/03-PATTERNS.md
    @.planning/phases/03-formatting-and-safe-edits/03-CONTEXT.md
    @/opt/source/Fathom/formatter/case.mbt
    @/opt/source/Fathom/formatter/layout.mbt
    @/opt/source/Fathom/formatter/options.mbt
    @/opt/source/Fathom/test/formatter_test.mbt
  </read_first>
  <action>Make every FormatOptions field drive real behavior and remove any remaining hard-coded layout constants.

formatter/case.mbt: add pub fn rewrite_keyword_case(raw : Bytes, case : KeywordCase) -> Bytes — Upper -> @token.classification_of(raw) entry.word (existing rewrite_keyword), Lower -> ASCII case-fold of entry.word (A-Z -> a-z byte-wise; every classification word is ASCII), None -> raw unchanged. Wire layout's SourceToken emission through rewrite_keyword_case with options.keyword_case(). Trivia still never reaches it (D-28/D-36).

formatter/layout.mbt: audit every layout decision so all policy flows from FormatOptions — options.line_width() feeds every measure-then-break whole-list check (replace any fixed 100 used in 03-02 fixtures), options.indent() feeds every break_line indent (replace any fixed 2), options.comma_style() decides Trailing vs Leading placement, options.newline_style() feeds detect_newline (Lf -> b"\n", Crlf -> b"\r\n", FollowInput -> first-\r\n-scan), options.trailing_newline() feeds finalize_output. No literal 2 / 100 / b"\n" policy constants remain in layout code paths; the only literal newlines are the Lf/Crlf definitions.

test/formatter_test.mbt: (a) Lower-case fixture: FormatOptions::new(KeywordCase::Lower, 2, 100, Trailing, FollowInput, true) formats a SELECT to lowercase keywords with identifiers/strings/comments untouched (b"view" used as an identifier is NOT lowercased further — it already is; assert a quoted "SELECT" identifier stays quoted and unchanged); (b) indent 4 fixture asserting 4-space indents in a broken list; (c) line_width 20 fixture asserting list breaks where width 100 does not (dimension observable); (d) comma_style Leading fixture (already in 03-02, re-assert under default width); (e) newline_style Lf override on a CRLF input -> b"\n" output (override beats FollowInput); (f) newline_style Crlf override on an LF input -> b"\r\n" output; (g) trailing_newline false fixture asserting no trailing newline in output and that format(format(x)) == format(x) still holds for both policy values (A3 backstop). Every fixture asserts byte-exact idempotence and zero-diagnostic reparse. Commit as feat(03-03): full option dimensions.
  </action>
  <acceptance_criteria>
    - Lower-case output renders keywords in lowercase and leaves identifiers, quoted names, strings, comments, and hints byte-identical; quoted "SELECT" never rewritten.
    - indent 4 produces 4-space indents in broken lists; line_width 20 forces breaks width 100 does not; both are observable in goldens.
    - Lf override on CRLF input emits \n only; Crlf override on LF input emits \r\n only; FollowInput still detects from source.
    - trailing_newline false emits no trailing newline and stays idempotent; trailing true normalizes to exactly one and stays idempotent.
    - No hard-coded policy constants (2, 100, fixed newline bytes) remain in layout code paths; grep over formatter/layout.mbt finds only the Lf/Crlf byte definitions and options accessors.
    - moon test and moon check --target native pass; printer/ untouched.
  </acceptance_criteria>
  <verify><automated>moon test && moon check --target native</automated></verify>
  <done>All six dimensions are functional and observable in goldens; Lower case, indent, line width, comma style, newline-style overrides, and trailing-newline off all work and stay idempotent.</done>
</task>

<task type="auto">
  <name>Task 2: Full 44-row embedded corpus harness (D-34/D-35 contract)</name>
  <files>test/formatter_test.mbt</files>
  <read_first>
    @.planning/phases/03-formatting-and-safe-edits/03-RESEARCH.md
    @.planning/phases/03-formatting-and-safe-edits/03-PATTERNS.md
    @.planning/phases/03-formatting-and-safe-edits/03-CONTEXT.md
    @/opt/source/Fathom/test/formatter_test.mbt
    @/opt/source/Fathom/test/parser_test.mbt
    @/opt/source/Fathom/test/corpus_test.mbt
    @/opt/source/Fathom/corpus/manifest.tsv
  </read_first>
  <action>Build the full embedded corpus harness mirroring all 44 manifest rows.

test/formatter_test.mbt: extend the FormatterFixture struct usage into a committed array `formatter_corpus_fixtures : Array[FormatterFixture]` with ONE row per manifest row: fixture_id and profile copied from corpus/manifest.tsv, mode "strict", raw embedded from the matching corpus/doris-{2.1,3.x,4.x}/*.sql file (authoring-time disk read; runtime never loads disk — STATE.md), expected_valid copied from the manifest's support status, expected_golden set by the first authoring run of the formatter and REVIEWED before commit (research Pitfall 7 — never blind-update via moon test --update). For manifest rows without a disk file (malformed/recovery rows recorded inline in the TSV), embed the raw bytes exactly as recorded.

Oracle assertions per fixture (mirroring metadata_fixture_replay_ok, test/parser_test.mbt:463-505): for expected_valid — format_with_ids(raw, profile, "strict", FormatOptions::default()) accepted=true, output == expected_golden, format(format(x)) == format(x) byte-exact, parse_with_ids(output, profile, "strict") valid with zero diagnostics (D-35), all statement_offsets index into output; for expected_error / version-negative — accepted=false, output empty, diagnostics contains DORIS-FORMAT-001 (the malformed half of D-34 is the refusal assertion, research Pitfall 6). CRLF fixtures additionally assert \r\n byte preservation. A dedicated test iterates the whole array and reports fixture_id on failure so a broken golden is locatable.

Commit the generated goldens as part of the fixture array; do not leave any fixture with expected_golden unset or placeholder bytes. Commit as feat(03-03): corpus formatting contract.
  </action>
  <acceptance_criteria>
    - formatter_corpus_fixtures has exactly 44 rows with fixture_ids matching corpus/manifest.tsv (no skipped rows, no invented rows).
    - Every expected_valid fixture passes byte-exact idempotence and zero-diagnostic reparse; every expected_error/version-negative fixture refuses with DORIS-FORMAT-001 and empty output.
    - Goldens are reviewed-and-committed inline (not moon test --update blind refresh); no fixture has placeholder goldens.
    - CRLF fixtures assert \r\n preservation; statement_offsets index into output for every multi-statement fixture.
    - moon test and moon check --target native pass; runtime tests load no disk files.
  </acceptance_criteria>
  <verify><automated>moon test && moon check --target native</automated></verify>
  <done>The full 44-row manifest is mirrored: accepted fixtures are byte-exact idempotent and reparse clean, error/version-negative fixtures refuse with DORIS-FORMAT-001, and the whole array passes in one committed test.</done>
</task>

<task type="auto">
  <name>Task 3: Option-matrix idempotence, determinism, and never-panic boundary suite</name>
  <files>test/formatter_test.mbt, formatter/format.mbt</files>
  <read_first>
    @.planning/phases/03-formatting-and-safe-edits/03-RESEARCH.md
    @.planning/phases/03-formatting-and-safe-edits/03-PATTERNS.md
    @/opt/source/Fathom/test/formatter_test.mbt
    @/opt/source/Fathom/formatter/format.mbt
    @/opt/source/Fathom/api/api.mbt
  </read_first>
  <action>Close the FMT-03 contract with matrix, determinism, and never-panic coverage.

test/formatter_test.mbt: (a) option-matrix idempotence — for a representative SELECT fixture and one DDL fixture, iterate the cartesian product of the supported values of each dimension (keyword_case x {Upper, Lower}, comma_style x {Trailing, Leading}, newline_style x {FollowInput, Lf, Crlf}, trailing_newline x {true, false}, indent x {2, 4}, line_width x {100, 40}) and assert format(format(x)) == format(x) byte-exact under every combination (D-34 across configurations; build the option sets explicitly, do not loop over invalid values); (b) determinism — format each combination twice and assert identical output (probe FMT-01 backstop already in 03-01; keep one matrix-level determinism assertion); (c) boundary suite — empty input b"", whitespace-only b"   \n", a doc with only comments, a doc at the max_bytes boundary (construct a raw input of ParseLimits max_bytes length via a loop; expect InputTooLarge ParseError or refusal — never a panic), and a slice-failure scenario if one can be constructed with public API (otherwise assert the refusal path handles all valid-but-error trees from the 03-01/03-02 fixtures); (d) refusal diagnostics shape — every DORIS-FORMAT-001 diagnostic has code prefix "DORIS-FORMAT-", expected_class "format", start_byte <= end_byte, and a statement_id.

formatter/format.mbt: make the entry robust to empty and trivia-only documents — an empty/trivia-only document returns accepted=true with empty (or comment-only) output, no diagnostics, and empty statement_offsets; the trailing-newline policy applies to comment-only output. No panic path exists: every source.slice failure inside layout already returns a refusal diagnostic (03-01); add a test asserting format never panics on any fixture in formatter_corpus_fixtures nor on the boundary inputs. Commit as feat(03-03): idempotence matrix and boundary suite.
  </action>
  <acceptance_criteria>
    - Every option combination in the matrix is byte-exact idempotent; the matrix runs in one test and fails with the offending combination named.
    - Empty, whitespace-only, and comment-only documents format without panic and without fabricated statements; max_bytes boundary input yields ParseError (InputTooLarge) or refusal, never a crash.
    - Every refusal diagnostic has prefix DORIS-FORMAT-, expected_class "format", valid span ordering, and a statement_id.
    - The full corpus array plus boundary inputs run without panic; moon test and moon check --target native pass.
  </acceptance_criteria>
  <verify><automated>moon test && moon check --target native</automated></verify>
  <done>FMT-03 is closed: idempotence holds across the option matrix, determinism is asserted, boundary inputs never panic, and every refusal diagnostic is well-formed.</done>
</task>

</tasks>

## Source Audit Coverage

| SOURCE | ID | Feature or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| REQ | FMT-02 | All 6 configurable dimensions functional | 03-03 | COVERED | Task 1 makes every dimension observable; invalid values rejected at construction (ASVS V5). |
| REQ | FMT-03 | Idempotent, reparses, refuses/reports unsafe | 03-03 | COVERED | Task 2 corpus contract + Task 3 matrix/boundary; refusal is the mode (D-33). |
| REQ | FMT-01 | (covered 03-01/03-02) | 03-03 | — | Corpus goldens also document behavior per fixture. |
| REQ | FMT-04 | (covered 03-04) | 03-03 | — | CLI plan consumes this plan's option surface. |
| RESEARCH | Pattern 6 | Corpus-driven idempotence + reparse harness | 03-03 | COVERED | Task 2 full 44-row mirror. |
| RESEARCH | Pitfall 6 | No trimmed comparisons; malformed = refusal assertion | 03-03 | COVERED | Byte-exact Bytes equality; error rows refuse. |
| RESEARCH | Pitfall 7 | Goldens reviewed, not blind-updated | 03-03 | COVERED | Task 2 review-before-commit rule. |
| RESEARCH | Assumption A3/A7 | Trailing-newline normalization; profile-independent case | 03-03 | COVERED | Matrix + cross-profile fixtures (flagged). |
| CONTEXT | D-26 | Exactly six config fields, no extras | 03-03 | COVERED | No hidden constants; every policy flows from FormatOptions. |
| CONTEXT | D-34/D-35 | CI idempotence + reparse contracts | 03-03 | COVERED | Full corpus harness + option matrix. |
| PROBE | FMT-03 unclassified | Refuse vs report | 03-03 | COVERED | Resolved truth: absolute refusal with structured diagnostic. |
| PROBE | FMT-02 unclassified | (covered 03-02) | 03-03 | — | Dimension functionality is the config half. |
| PROBE | FMT-01 concurrency | (covered 03-01) | 03-03 | — | Matrix-level determinism re-asserted. |

<verification>
Run `moon test` after each task and `moon check --target native` after Task 1. Inspect: Lower-case rewriting, all policy values flowing from FormatOptions (no hard-coded constants), the 44-row embedded corpus array matching manifest.tsv, byte-exact idempotence + zero-diagnostic reparse oracles, DORIS-FORMAT-001 refusal on error/version-negative rows, option-matrix idempotence, boundary/never-panic behavior, and untouched printer/ (D-27). Do not install packages, run formatters/linters, or invoke network services.
</verification>

<success_criteria>
- FMT-02 is executable end-to-end: all six dimensions are functional, observable in goldens, and invalid values are rejected.
- FMT-03 is executable as a CI contract: the full 44-row corpus is byte-exact idempotent and reparses clean; error trees refuse with DORIS-FORMAT-001; the option matrix is idempotent; the formatter never panics.
- Goldens are reviewed, committed inline, and runtime tests load no disk files (STATE.md convention).
- The D-27 boundary and printer/ remain untouched; the formatter stays pure and backend-neutral.
</success_criteria>

<output>Create `.planning/phases/03-formatting-and-safe-edits/03-03-SUMMARY.md` when this plan executes.</output>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Config values → layout | Every policy value crosses from FormatOptions into layout; invalid values must fail construction, not silently degrade. |
| Corpus fixtures → formatter | Untrusted SQL from the corpus (including malformed and version-negative rows) must never panic or hang the formatter. |
| Boundary inputs → format entry | Empty, whitespace-only, comment-only, and max_bytes inputs must yield structured results, never crashes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-14 | Tampering | Option-matrix idempotence subversion | high | mitigate | Byte-exact idempotence asserted across the full cartesian option matrix in one committed test (D-34); goldens reviewed (Pitfall 7). |
| T-03-15 | Tampering | Corpus contract drift (fixtures skipped or trimmed) | high | mitigate | 44-row mirror with manifest fixture_id match; byte-exact Bytes equality (no trim, Pitfall 6); malformed rows assert refusal, never omission. |
| T-03-16 | Denial of Service | max_bytes boundary input | medium | mitigate | format_text propagates InputTooLarge from SourceText::new_with_limit (mirrors api parse); boundary test asserts ParseError or refusal, never a crash (parser.mbt:38-44). |
| T-03-17 | Tampering | Invalid config silently ignored | high | mitigate | FormatOptions::new rejects indent < 0 and line_width <= 0 with FormatError; the api entry never receives an invalid options struct (ASVS V5, research Security Domain). |
| T-03-18 | Tampering | Panic on malformed/version-negative corpus rows | high | mitigate | refusal-first entry (03-01) + boundary suite covering the full corpus array and empty/whitespace/comment-only inputs (research Pattern 5 / Pitfall 4). |
| T-03-SC | Tampering | Package installation | low | accept | No new dependency; core-only pinned packages (03-RESEARCH Package Legitimacy Audit). |
</threat_model>

## Artifacts this phase produces

Symbols (formatter/case.mbt): rewrite_keyword_case(raw, KeywordCase) with ASCII case-fold for Lower.
Symbols (formatter/layout.mbt): all policy values wired through options accessors (line_width in measure-then-break, indent in break_line, comma_style, newline_style, trailing_newline); no hard-coded layout constants.
Symbols (formatter/format.mbt): robust entry for empty/trivia-only documents (accepted=true, empty/comment-only output, empty statement_offsets).
Files: formatter/case.mbt (extended), formatter/layout.mbt (extended), formatter/format.mbt (extended), test/formatter_test.mbt (extended).
Test data: formatter_corpus_fixtures (44 embedded rows mirroring corpus/manifest.tsv), option-matrix idempotence test, determinism assertion, boundary suite (empty/whitespace/comment-only/max_bytes), refusal-diagnostic shape test.
