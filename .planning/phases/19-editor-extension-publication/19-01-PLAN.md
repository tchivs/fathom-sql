---
phase: 19-editor-extension-publication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vscode/package.json
  - vscode/README.md
autonomous: true
requirements:
  - VSC-01
  - VSC-02
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "VSC-02 / D-01/D-02：vscode/package.json version 1.0.0 且无 `private: true`；README 含市场安装 + fathom-lsp 获取（GitHub Release + SHA-256 + --version 验证）+ fathom.serverPath 配置指引。"
    - "VSC-01 / D-03：`npm run package` 产出有效 .vsix，vsce ls 校验 dist/extension.js + manifest + README。"
    - "D-04：无凭据时发布以认证门记录（OVSX_TOKEN / Azure DevOps PAT + publisher fathom），不伪造。"
  artifacts:
    - path: "vscode/package.json"
      provides: "1.0.0 发布化 manifest"
    - path: "vscode/README.md"
      provides: "市场安装 + fathom-lsp 获取指引"
  key_links:
    - from: "vscode/README.md"
      to: "Phase 16 install guide + fathom.serverPath"
      via: "release 资产获取与配置"
      pattern: "fathom.serverPath"
---

<objective>
把 VS Code 扩展推向发布形态：manifest 发布化（1.0.0、去 private）、README 补市场安装与 fathom-lsp 获取指引，vsce 打包验证，市场发布以认证门交付（VSC-01、VSC-02，D-01..D-04）。
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-editor-extension-publication/19-CONTEXT.md
@vscode/package.json
@vscode/README.md
@vscode/scripts/host-verify.mjs
</context>

<dependency_graph>
- Task 1 updates manifest + README; Task 2 packages and verifies.
</dependency_graph>

<flagged_assumptions>
- **[ASSUMED]** vsce 3.9.2 离线可用（npm cache，Phase 13 决策）；publisher `fathom` 需在目标市场注册（认证门前置，无法本地验证）。
- **[PROHIBITIONS RECALLED]** 不打 v1.0.0 tag/Release（Phase 20）；不改扩展功能代码。
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Release-version manifest and install guide</name>
  <reversibility rating="one-way">Per D-01/D-02, the manifest identity and install guide are marketplace contracts.</reversibility>
  <files>vscode/package.json, vscode/README.md</files>
  <read_first>
    - vscode/package.json (version, private, publisher, scripts)
    - vscode/README.md (existing Configuration/Build sections)
  </read_first>
  <action>
In `vscode/package.json`: set `version` to `1.0.0` and remove the `"private": true` line (publication prerequisite). Keep publisher `fathom`, engines, activation events, contributes, and scripts unchanged; sync the lockfile if `package-lock.json` pins the old version.

In `vscode/README.md`: add an `## Install` section before `## Configuration` covering: (1) install the extension from the VS Code Marketplace or Open VSX; (2) acquire the `fathom-lsp` server from a GitHub Release (per the repository README's "Install `fathom-lsp` from GitHub Release" — asset table, `fathom-lsp-manifest.json` SHA-256 verification, recommended `~/.fathom/bin`, `chmod +x`); (3) verify with `fathom-lsp --version` → `fathom-lsp 1.0.0`; (4) set `fathom.serverPath` to the installed executable (or leave default `fathom-lsp` if on PATH) plus the explicit `fathom.dialect`/`fathom.profile` requirements. Keep all existing sections.
  </action>
  <verify>
    <automated>python3 -c 'import json; m=json.load(open("vscode/package.json")); assert m["version"]=="1.0.0"; assert "private" not in m; t=open("vscode/README.md").read(); assert "## Install" in t and "fathom.serverPath" in t and "fathom-lsp --version" in t and "GitHub Release" in t and "1.0.0" in t'</automated>
  </verify>
  <acceptance_criteria>
    - manifest 1.0.0 且非 private；README 安装指引四要素齐全（市场安装/获取/SHA-256/--version 验证 + serverPath）。
  </acceptance_criteria>
  <done>扩展 manifest 发布化，README 指引就绪。</done>
</task>

<task type="auto">
  <name>Task 2: Package verification and publication auth gates</name>
  <reversibility rating="one-way">Per D-03/D-04, the vsix is the publication carrier; registry pushes are irreversible.</reversibility>
  <files>vscode/package.json (lock sync if needed)</files>
  <read_first>
    - Task 1 outputs
  </read_first>
  <action>
Run `npm run package` in `vscode/` (compile + `vsce package`) to produce the `.vsix`; run `vsce ls` to verify the package contains `dist/extension.js`, `package.json`, `README.md`, and `language-configuration.json`. If the package succeeds, record the vsix path and size. Do NOT run `vsce publish` or `ovsx publish`: with no credentials (no `OVSX_TOKEN`, no Azure DevOps PAT, publisher `fathom` registration unverifiable) the publication steps are recorded as auth gates — Open VSX requires `OVSX_TOKEN` (and a registered `fathom` namespace), VS Code Marketplace requires an Azure DevOps PAT with a registered publisher. Do not fabricate a publish.
  </action>
  <verify>
    <automated>cd vscode && npm run package >/dev/null 2>&1 && ls *.vsix && npx vsce ls 2>/dev/null | grep -qE 'dist/extension\.js|package\.json|README\.md|language-configuration\.json'</automated>
  </verify>
  <acceptance_criteria>
    - vsce package 产出 .vsix；vsce ls 包含四要素。
    - 发布动作以认证门记录（无凭据不推送）。
  </acceptance_criteria>
  <done>vsix 打包验证通过；市场发布以认证门交付。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| vsix contents -> consumers | 包内容必须与源码同源；manifest 元数据准确。 |
| marketplace push | 不可逆；无凭据必须显式门控。 |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-19-01 | Spoofing | manifest | high | mitigate | 版本/private/publisher 断言。 |
| T-19-02 | Tampering | vsix contents | high | mitigate | vsce package + ls 校验。 |
| T-19-03 | Repudiation | publish | high | mitigate | 认证门记录；无凭据不假装发布。 |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | 扩展发布准备 | 19-01 | COVERED | Tasks 1/2。
REQ | VSC-01 | 市场发布 | 19-01 | COVERED | Task 2 打包 + 认证门。
REQ | VSC-02 | README 安装指引 | 19-01 | COVERED | Task 1。
CONTEXT | D-01..D-04 | manifest/指引/打包/认证门 | 19-01 | COVERED | Tasks 1/2。
</source_audit>

## Artifacts this phase produces
- `vscode/package.json` — 1.0.0 发布化 manifest。
- `vscode/README.md` — 市场安装 + fathom-lsp 获取指引。
- `.vsix` 构建产物（本地验证）。

<verification>
- python 断言 manifest 与 README；`npm run package` + `vsce ls` 校验 vsix 内容；发布认证门记录。
</verification>

<success_criteria>
1. 扩展 manifest 发布化（1.0.0、非 private），README 指引完整。
2. vsix 打包验证通过。
3. 市场发布以认证门呈现，不伪造。
</success_criteria>

<output>
Create `.planning/phases/19-editor-extension-publication/19-01-SUMMARY.md` when done.
</output>
