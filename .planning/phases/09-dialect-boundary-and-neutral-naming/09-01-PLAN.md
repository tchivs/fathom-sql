---
phase: 09-dialect-boundary-and-neutral-naming
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - parity/baseline_test.mbt
  - parity/baseline-hashes.txt
  - scripts/baseline_diff.py
  - .github/workflows/ci.yml
  - .planning/phases/09-dialect-boundary-and-neutral-naming/approved-changes.md
autonomous: false
requirements: [DIALECT-04]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every v1 Doris public behavior category is frozen to bytes BEFORE any dialect/naming refactor: CST shape/span, diagnostics code/span/statement_id, strict/editor modes, formatter output, completion, CLI exit code/stdout/stderr, LSP protocol output, and wire schema output — via MoonBit @test.T::snapshot under parity/__snapshot__ (D-07/D-08)."
    - "The frozen baseline is enforceable: moon test --package parity fails on ANY byte difference, and a committed sha256 record (parity/baseline-hashes.txt) pins corpus/manifest.tsv, corpus/keywords.tsv, and corpus/doris-*/ fixtures (D-08)."
    - "Approved vs regression is mechanically distinguishable: a committed approved-change register (approved-changes.md) pre-declares the D-09/D-10 schema-namespace, code-prefix, and new-field changes, and scripts/baseline_diff.py groups diffs so any diff NOT in the register is an unexpected regression."
    - "[probe DIALECT-04 boundary] flagged-unverified: the freeze captures exactly the profile boundary set {2.1, 3.x, 4.x} and strict/editor thresholds as they exist in v1 — one step outside the set is an UnknownProfile error; the snapshots record that boundary so later waves cannot silently widen it."
    - "[probe DIALECT-03 empty] flagged-unverified: empty, whitespace-only, and single-statement inputs are snapshotted as v1 behaves today (Doris mode); the Flink-mode empty-input contract is decided in 09-02 (explicit not-implemented diagnostic, never silent empty success)."
    - "[probe DIALECT-03 ordering] flagged-unverified: diagnostic ordering (source order, statement_diagnostics semantics) and first-match classification lookup order are frozen exactly as v1 emits them; any reordering in later waves is a baseline diff and must be approved."
    - "[probe DIALECT-04 precision] flagged-unverified: byte offsets/spans and statement_id integers serialize exactly as v1 (half-open spans, Int arithmetic, JSON number emission); the snapshots pin the precision contract through every rename."
  artifacts:
    - path: "parity/baseline_test.mbt"
      provides: "snapshot tests over every corpus fixture x profile x mode through the existing serialized entries (parse/format/completion/CLI homomorph/LSP homomorph), cross-target byte equality via parity run_native/run_js/run_wasm"
    - path: "parity/baseline-hashes.txt"
      provides: "sha256 of corpus/manifest.tsv, corpus/keywords.tsv, corpus/doris-{2.1,3.x,4.x}/*.sql (provenance record, manifest.tsv shape)"
    - path: "scripts/baseline_diff.py"
      provides: "stdlib shape-diff reporter: groups approved diffs (schema namespace, code prefix, new fields) separately from unexpected diffs; non-zero exit on unexpected"
    - path: ".github/workflows/ci.yml"
      provides: "parity gate job: moon test --package parity WITHOUT --update (byte-level fail) + baseline_diff.py self-diff"
    - path: ".planning/phases/09-dialect-boundary-and-neutral-naming/approved-changes.md"
      provides: "pre-declared D-09/D-10 approved byte changes (fathom.*.v1 names, FATHOM-* codes, dialect metadata fields, export names, CLI usage, LSP identity) — the only allowed snapshot-update reasons"
  key_links:
    - from: "parity/baseline_test.mbt"
      to: "corpus/manifest.tsv"
      via: "fixture_id/profile copied at authoring time; raw bytes embedded (runtime never reads disk — STATE.md convention)"
    - from: "parity/__snapshot__"
      to: ".github/workflows/ci.yml"
      via: "parity job runs moon test --package parity (no --update) on every push — byte-level gate"
    - from: "scripts/baseline_diff.py"
      to: "approved-changes.md"
      via: "the register is the whitelist; any diff outside it exits non-zero (D-08 approved-change discipline)"
  prohibitions:
    - "moon test --update is NEVER run without a matching approved-changes.md entry already committed (D-08 single-use approval path; research Pitfall 3/7)."
    - "No refactor lands before the baseline snapshots are committed (order: freeze -> dialect -> naming -> hosts; research §9.2)."
    - "No snapshot is trimmed to a subset: every fixture x profile x mode combination present in the corpus harness is frozen."
---

<objective>
**As a** maintainer, **I want to** freeze the shipped v1 Doris behavior to bytes before any dialect or naming refactor, **so that** every later Phase 9 step proves it preserved Doris behavior instead of silently changing it.

Purpose: Establish the D-07/D-08 baseline gate — the byte-level parity contract that makes the whole refactor phase safe and becomes Phase 12 PARITY-01's comparison basis. The gate must exist BEFORE the first refactor commit; any code-first ordering would freeze post-refactor behavior (research Pitfall 3).
Output: parity/baseline_test.mbt + parity/__snapshot__ (generated), parity/baseline-hashes.txt, scripts/baseline_diff.py, the approved-change register, and the CI parity gate job.
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-dialect-boundary-and-neutral-naming/09-CONTEXT.md
@.planning/phases/09-dialect-boundary-and-neutral-naming/09-RESEARCH.md
@.planning/phases/09-dialect-boundary-and-neutral-naming/09-PATTERNS.md

This is the mandatory first wave: freeze before any refactor. D-07 (one-way: the frozen baseline is Phase 12 PARITY-01's comparison basis) and D-08 (snapshot-diff gate, reversible) drive this plan. The mechanism is MoonBit's official `@test.T::snapshot` (`__snapshot__/` + `moon test --update` generate, `moon test` fails byte-level) — RESEARCH §9.2 steps 1-5. The corpus has 44 manifest rows; the executable harness mirrors them embedded (runtime never reads disk — STATE.md convention, test/corpus_test.mbt pattern). Cross-target byte equality uses the existing parity run_native/run_js/run_wasm skeleton (parity/moon.pkg targets mapping).
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm D-07 baseline freeze scope (one-way door)</name>
  <decision>Doris v1 baseline freeze scope and gate mechanism — the frozen baseline becomes the Phase 12 PARITY-01 comparison basis; its scope is a one-way door.</decision>
  <context>The research inventory (09-RESEARCH.md §9.1) enumerates nine output categories covering the full public behavior D-07 locks: corpus fixtures, CST shape/span, diagnostics (code/span/statement_id), strict/editor modes, formatter output, completion, CLI exit code/stdout/stderr, LSP protocol output, wire schema output. The approved-change register pre-declares the later intentional changes (D-09/D-10 schema namespace, FATHOM-* code prefixes, new dialect metadata fields, export renames, CLI usage text, LSP identity) so the gate can mechanically distinguish "approved change" from "regression". If the scope were narrowed, the unprotected categories could regress invisibly during the rename waves.</decision>
  <options>
    <option id="option-a">
      <name>Full freeze per D-07 (recommended — locked decision)</name>
      <pros>All nine categories frozen byte-level; every later wave's diff is mechanically checkable; matches the locked D-07 wording; PARITY-01 gets a complete basis.</pros>
      <cons>Larger snapshot corpus; the first parity test run takes longer; more approved-change entries to maintain.</cons>
    </option>
    <option id="option-b">
      <name>Narrow freeze (parse + format only)</name>
      <pros>Faster to generate and maintain.</pros>
      <cons>CLI exit codes, LSP protocol output, completion, and wire schema can regress invisibly during rename waves — contradicts D-07's byte-level comparison requirement.</cons>
    </option>
  </options>
  <resume-signal>Select option-a (full freeze) or describe a modified scope; the baseline snapshots are generated only after this confirmation.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Freeze the v1 baseline — snapshot every fixture x profile x mode to parity/__snapshot__</name>
  <files>parity/baseline_test.mbt, parity/baseline-hashes.txt</files>
  <read_first>
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-CONTEXT.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-RESEARCH.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-PATTERNS.md
    @/opt/source/Fathom/parity/export_smoke_test.mbt
    @/opt/source/Fathom/parity/schema_test.mbt
    @/opt/source/Fathom/parity/parity_test.mbt
    @/opt/source/Fathom/parity/moon.pkg
    @/opt/source/Fathom/parity/run_native.mbt
    @/opt/source/Fathom/parity/run_js.mbt
    @/opt/source/Fathom/parity/run_wasm.mbt
    @/opt/source/Fathom/corpus/manifest.tsv
    @/opt/source/Fathom/test/corpus_test.mbt
  </read_first>
  <action>Create parity/baseline_test.mbt using the CURRENT (pre-rename) shared entries — this wave runs before 09-02, so the export names are still @binding.doris_parse_v1(raw, profile, mode), @binding.doris_format_v1(...), @binding.doris_capabilities_v1(). Mirror the test/corpus_test.mbt embedded-oracle pattern: an array `baseline_fixtures : Array[BaselineFixture]` with fixture_id and profile copied from corpus/manifest.tsv and raw bytes embedded at authoring time from corpus/doris-{2.1,3.x,4.x}/*.sql plus the manifest's inline rows (runtime never reads disk — STATE.md). One @test.T::snapshot test per fixture x profile x mode combination, filename "<fixture_id>.<profile>.<mode>.json", writing the full serialized output of: @binding.doris_parse_v1(raw, profile, mode) for both "strict" and "editor" modes; @binding.doris_format_v1(raw, profile, "strict", default options) for expected_valid fixtures; the completion items JSON via @completion.complete(raw, profile, cursor_at_end) for a representative subset; the CLI homomorph via @api.format_with_ids + @binding.format_result_json; and the LSP protocol output via the existing parity/fixtures/lsp-tracer.json invocation pattern for the 4.x representative fixture. Add one test asserting cross-target byte equality: run the same representative serialized output through parity/run_native.mbt, run_js.mbt, run_wasm.mbt and assert identical bytes (PARITY-02 foundation, research §9.2 step 5). Compute and commit parity/baseline-hashes.txt: sha256 of corpus/manifest.tsv, corpus/keywords.tsv, and every corpus/doris-{2.1,3.x,4.x}/*.sql, one "<hash>  <path>" line each (compute on the host with sha256sum; record like manifest.tsv provenance). Generate snapshots with moon test --update --package parity, then verify WITHOUT --update. Commit snapshots + hashes as feat(09-01): freeze doris v1 baseline.</action>
  <acceptance_criteria>
    - parity/__snapshot__ exists with one file per fixture x profile x mode combination; parity/baseline-hashes.txt is committed with one sha256 line per corpus file.
    - moon test --package parity passes WITHOUT --update after generation (any later byte difference fails).
    - baseline_test.mbt contains no filesystem read at runtime (embedded bytes only); the cross-target equality test passes on native/js/wasm targets.
    - The v1 names (doris.parse.v1 schema strings, DORIS-PARSE-* codes, doris_parse_v1 export) are frozen inside the snapshots as they exist today — no renames in this wave.
  </acceptance_criteria>
  <verify>
    <automated>moon test --package parity && test -s parity/baseline-hashes.txt && test -d parity/__snapshot__</automated>
  </verify>
  <done>The v1 Doris behavior is frozen byte-level in parity/__snapshot__ with committed provenance hashes; the parity package gate fails on any later drift.</done>
</task>

<task type="auto">
  <name>Task 3: Approved-change register + baseline diff tooling + CI parity gate</name>
  <files>.planning/phases/09-dialect-boundary-and-neutral-naming/approved-changes.md, scripts/baseline_diff.py, .github/workflows/ci.yml</files>
  <read_first>
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-CONTEXT.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-RESEARCH.md
    @/opt/source/Fathom/corpus/tools/check_keywords.py
    @/opt/source/Fathom/.github/workflows/ci.yml
  </read_first>
  <action>Create .planning/phases/09-dialect-boundary-and-neutral-naming/approved-changes.md as the pre-declared register of intentional byte changes for the whole phase, copied from 09-CONTEXT.md D-09/D-10 and 09-RESEARCH.md sections 5-6: (1) wire schema namespace doris.parse.v1 to fathom.parse.v1, doris.format.v1 to fathom.format.v1, doris.error.v1 to fathom.error.v1, doris.profile.v1 to fathom.dialect.v1, doris.capabilities.v1 to fathom.capabilities.v1; (2) diagnostic codes DORIS-PARSE-001/002/003/004/006/007 to FATHOM-PARSE-*, DORIS-FORMAT-001..007 to FATHOM-FORMAT-*, DORIS-SCHEMA-001..006 to FATHOM-SCHEMA-*, DORIS-LSP-001 to FATHOM-LSP-001, with DORIS-PARSE-005 kept vacant and new FATHOM-PARSE-008 minted for the Flink not-implemented path; (3) new fields: ParseResult.dialect, dialect/profile/exact_release metadata on results and diagnostics; (4) export names doris_parse_v1 to fathom_parse_v1, doris_format_v1 to fathom_format_v1, doris_profile_v1 to fathom_dialect_v1, doris_capabilities_v1 to fathom_capabilities_v1 (signature adds dialect parameter); (5) CLI usage text and LSP serverInfo/source strings. Create scripts/baseline_diff.py (Python stdlib only, mirror the check_keywords.py loop shape: problems list, per-line reporting, non-zero exit, print("ok: ...") success line) with CLI args --left DIR --right DIR and --approve FILE: walk both snapshot dirs, parse each *.json, group diffs into (a) approved — matches the --approve register patterns (schema_version values, code prefix values, presence of new dialect/profile/exact_release fields, export/source/serverInfo strings); (b) unexpected — everything else; print grouped reports and exit 1 if any unexpected diff exists, else print "ok: N snapshots, M approved diffs, K unexpected". Add a parity gate job to .github/workflows/ci.yml mirroring the existing corpus job shape: checkout, run moon test --package parity (no --update anywhere in the job), run python3 scripts/baseline_diff.py --left parity/__snapshot__ --right parity/__snapshot__ --approve .planning/phases/09-dialect-boundary-and-neutral-naming/approved-changes.md (self-diff must report zero unexpected). Commit as feat(09-01): baseline diff gate and approved-change register.</action>
  <acceptance_criteria>
    - approved-changes.md lists all four schema namespaces, all four diagnostic code families (PARSE/FORMAT/SCHEMA/LSP) with the PARSE-005 vacancy note, the FATHOM-PARSE-008 mint, the new dialect/profile/exact_release fields, the four export renames, and the CLI/LSP identity strings.
    - python3 scripts/baseline_diff.py --left parity/__snapshot__ --right parity/__snapshot__ --approve <register> exits 0 and prints a line containing "0 unexpected".
    - .github/workflows/ci.yml contains a parity job that runs moon test --package parity without --update and the baseline_diff.py self-diff; the job contains no --update flag.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/baseline_diff.py --left parity/__snapshot__ --right parity/__snapshot__ --approve .planning/phases/09-dialect-boundary-and-neutral-naming/approved-changes.md && python3 -m py_compile scripts/baseline_diff.py</automated>
  </verify>
  <done>The diff gate and approved-change register are in place; every later wave's diff check is mechanical and distinguishes approved changes from regressions.</done>
</task>

</tasks>

## Source Audit Coverage

| SOURCE | ID | Feature or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | "preserves the shipped Doris behavior" while refactoring | 09-01 | COVERED | Byte-level freeze is the proof mechanism for the whole phase. |
| REQ | DIALECT-04 | strict/editor mode, byte spans, statement identity, diagnostics remain stable | 09-01 | COVERED | Snapshots pin exactly these across all categories. |
| REQ | PARITY-01 (Phase 12) | Doris behavior equals a frozen baseline | 09-01 | COVERED (prep) | This plan creates the baseline Phase 12 consumes; PARITY-01 itself is Phase 12. |
| RESEARCH | §9.1 nine output categories | D-07 freeze inventory | 09-01 | COVERED | Task 2 covers all nine. |
| RESEARCH | §9.2 snapshot mechanism | @test.T::snapshot + --update workflow + order | 09-01 | COVERED | Task 2 generation; order enforced by wave 1. |
| RESEARCH | §9.3 approval path | approved-change register; single --update use | 09-01 | COVERED | Task 3 register + prohibition. |
| RESEARCH | Pitfall 3 / 7 | no code-before-baseline; no blind --update | 09-01 | COVERED | Prohibitions + Task 3. |
| CONTEXT | D-07 | full public behavior freeze, one-way | 09-01 | COVERED | Task 1 checkpoint + Task 2. |
| CONTEXT | D-08 | snapshot-diff gate, approved-or-identical | 09-01 | COVERED | Tasks 2-3. |
| PROBE | DIALECT-04 boundary / precision | min/max thresholds, byte offsets | 09-01 | FLAGGED | truths: profile boundary set + precision frozen. |
| PROBE | DIALECT-03 empty / ordering | empty input; diagnostic ordering | 09-01 | FLAGGED | truths: v1 empty-input + ordering frozen; Flink empty contract in 09-02. |

<verification>
Run moon test --package parity after each task (no --update after generation). Inspect: parity/__snapshot__ completeness (every fixture x profile x mode), parity/baseline-hashes.txt committed, baseline_diff.py self-diff reporting 0 unexpected, ci.yml parity job present and --update-free. Do not rename anything, do not install packages, do not touch token/lexer/parser/api/binding/lsp/doris-sql source in this wave.
</verification>

<success_criteria>
- The v1 baseline is frozen byte-level before any refactor; the parity package gate fails on any later drift.
- The approved-change register and baseline_diff.py mechanically separate approved changes (D-09/D-10) from regressions.
- CI enforces the gate without --update; corpus provenance hashes are committed.
</success_criteria>

<output>Create `.planning/phases/09-dialect-boundary-and-neutral-naming/09-01-SUMMARY.md` when this plan executes.</output>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| corpus fixtures -> snapshot tests | Untrusted SQL (including malformed/recovery rows) flows into frozen snapshots; the gate must fail, never silently update. |
| executor -> moon test --update | The only sanctioned snapshot writer; misuse masks regressions. |
| CI checkout -> parity gate | The gate must run on a clean checkout with no --update path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-01 | Tampering | parity/__snapshot__ silent update | high | mitigate | moon test --package parity without --update in CI; approved-changes.md + baseline_diff.py make any unregistered diff exit non-zero (D-08). |
| T-09-02 | Tampering | Refactor lands before baseline (records post-refactor bytes) | high | mitigate | Wave 1 ordering enforced by depends_on []; baseline_test.mbt committed before 09-02 starts (research Pitfall 3). |
| T-09-03 | Tampering | baseline-hashes.txt fabricated/missing | medium | mitigate | sha256 computed on host from committed corpus files; file committed to git; self-diff job re-verifies snapshots. |
| T-09-04 | Denial of Service | Snapshot explosion slows CI | low | accept | 44 fixtures x 2 modes bounded; representative subset for completion/LSP homomorphs (research §9.1). |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new external dependencies this phase (09-RESEARCH Package Legitimacy Audit N/A); scripts are Python stdlib. |
</threat_model>

## Artifacts this phase produces

Files: parity/baseline_test.mbt (new), parity/baseline-hashes.txt (new), parity/__snapshot__/*.json (generated, committed), scripts/baseline_diff.py (new), .github/workflows/ci.yml (extended: parity gate job), .planning/phases/09-dialect-boundary-and-neutral-naming/approved-changes.md (new).
Symbols (parity/baseline_test.mbt): baseline_fixtures : Array[BaselineFixture] (fixture_id, profile, raw embedded), one @test.T::snapshot test per fixture x profile x mode, cross-target equality test reusing parity run_native/run_js/run_wasm.
Symbols (scripts/baseline_diff.py): CLI --left/--right/--approve, approved-vs-unexpected diff grouping, exit 0/1 semantics.
