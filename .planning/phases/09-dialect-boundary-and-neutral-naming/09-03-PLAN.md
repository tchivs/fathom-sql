---
phase: 09-dialect-boundary-and-neutral-naming
plan: 03
type: execute
wave: 3
depends_on: [09-02]
files_modified:
  - api/api.mbt
  - binding/exports.mbt
  - binding/schema.mbt
  - formatter/case.mbt
  - formatter/format.mbt
  - completion/completion.mbt
  - test/formatter_test.mbt
  - parity/baseline_test.mbt
autonomous: true
requirements: [DIALECT-02, DIALECT-03, NAME-02]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The formatter is dialect-aware end-to-end: format_with_ids(raw, dialect_id, profile_id, mode_id, ...) and the formatter entry carry DialectContext; keyword case rewriting routes through dialect.classification_of(context, raw); Doris formatting output is byte-identical to v1 (baseline gate), and Flink-mode formatting returns the structured unsupported-profile error (no silent format of a Flink request under Doris policy) (DIALECT-02/03, D-10)."
    - "Completion is dialect-aware: complete(raw, dialect_id, profile_id, cursor_byte) returns Doris keyword items only in doris mode (detail string neutralized to 'SQL syntax keyword'), empty items in flink mode (flink rows are empty in Phase 9), and a structured error for unknown dialect/profile (DIALECT-02)."
    - "Wire identity is complete: fathom_format_v1(raw, dialect, profile, mode, ...) export exists with #export_name and moon.pkg exports lists in sync, FORMAT_SCHEMA_VERSION is fathom.format.v1, and format error codes are FATHOM-FORMAT-001..007 in both the formatter emission and the binding mapping (NAME-02, D-10)."
    - "[probe DIALECT-03 ordering] flagged-unverified: completion item order and format statement_offsets remain exactly as v1 for Doris (baseline snapshots pin them); the dialect dimension adds no reordering."
  artifacts:
    - path: "api/api.mbt"
      provides: "format_with_ids(raw, dialect_id, profile_id, mode_id, ...) and format_with_metadata with dialect dimension; FormatResult/FormatError paths carry dialect metadata"
    - path: "formatter/case.mbt + format.mbt"
      provides: "rewrite_keyword(context, raw) routing through dialect classification (Doris behavior identical); FATHOM-FORMAT-001 refusal code"
    - path: "completion/completion.mbt"
      provides: "complete(raw, dialect_id, profile_id, cursor_byte); profile_allows by context; neutral detail string"
    - path: "binding/exports.mbt + binding/schema.mbt"
      provides: "fathom_format_v1 export (+ moon.pkg exports sync), fathom.format.v1, FATHOM-FORMAT-002..007 mapping in format_error_json"
  key_links:
    - from: "formatter/case.mbt"
      to: "dialect/classification.mbt"
      via: "rewrite_keyword(context, raw) -> dialect.classification_of(context, raw) — the single keyword authority is now dialect-routed"
    - from: "completion/completion.mbt"
      to: "dialect/classification.mbt"
      via: "profile_allows + item generation read dialect.classification_entries(context)"
    - from: "binding/exports.mbt"
      to: "api/api.mbt"
      via: "fathom_format_v1 -> api.format_with_ids(dialect_id, profile_id, ...) — same validation order as parse"
  prohibitions:
    - "Flink requests must never be formatted or completed under Doris policy — the dialect dimension is validated first and flink returns the structured error (Pitfall 6)."
    - "No partial wire migration: every format-path schema string and code emitted by formatter or binding is fathom.format.v1 / FATHOM-FORMAT-*; no old-format-schema or DORIS-FORMAT- string may remain in product files after this plan."
    - "Doris formatter/completion output must stay byte-identical to v1 (baseline gate); any diff not in the approved-change register is a regression."
---

<objective>
**As a** consumer, **I want to** format and complete SQL under the same explicit dialect selection as parsing, **so that** keyword case rewriting and completion candidates never leak Doris policy into a Flink session and the wire identity is uniformly fathom.format.v1 with FATHOM-FORMAT-* codes.

Purpose: Close the dialect dimension on the format and completion paths (DIALECT-02/03) and finish the format half of the wire identity (NAME-02, D-09/D-10). Doris behavior stays byte-identical (baseline gate); the FATHOM-FORMAT-* code family completes the D-10 mapping started in 09-02.
Output: dialect-aware format_with_ids/fathom_format_v1/fathom.format.v1, FATHOM-FORMAT-001..007, dialect-aware completion with neutral detail, updated formatter/completion/parity tests.
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-dialect-boundary-and-neutral-naming/09-CONTEXT.md
@.planning/phases/09-dialect-boundary-and-neutral-naming/09-RESEARCH.md
@.planning/phases/09-dialect-boundary-and-neutral-naming/09-PATTERNS.md
@.planning/phases/09-dialect-boundary-and-neutral-naming/09-02-SUMMARY.md

09-02 threaded DialectContext through the parse path and left formatter/completion signature-threaded but behaviorally Doris-only; this plan makes them dialect-aware (research migration rows 26-29): complete(raw, dialect_id, profile_id, cursor_byte), rewrite_keyword(context, raw), format entry context, FATHOM-FORMAT-001 at formatter/format.mbt:131, and the format half of NAME-02 (rows 32, 36, 39). Doris outputs must be byte-identical — the 09-01/09-02 baseline gate enforces this. The refusal-first contract (D-33) and FormatOptions (six dimensions, D-26) are untouched by this plan.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dialect-aware format path — api format_with_ids, formatter routing, FATHOM-FORMAT codes, fathom_format_v1</name>
  <files>api/api.mbt, formatter/case.mbt, formatter/format.mbt, binding/exports.mbt, binding/schema.mbt, test/formatter_test.mbt, parity/baseline_test.mbt</files>
  <read_first>
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-CONTEXT.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-RESEARCH.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-PATTERNS.md
    @/opt/source/Fathom/api/api.mbt
    @/opt/source/Fathom/formatter/case.mbt
    @/opt/source/Fathom/formatter/format.mbt
    @/opt/source/Fathom/binding/exports.mbt
    @/opt/source/Fathom/binding/schema.mbt
    @/opt/source/Fathom/test/formatter_test.mbt
  </read_first>
  <action>Make the format path fully dialect-aware with neutral wire identity.

api/api.mbt: format_with_ids(raw, dialect_id, profile_id, mode_id, ...) and format_with_metadata gain the dialect_id argument (research row 24, A4 order); the dialect/profile validation is shared with the parse path (dialect first, then profile — flink rejected in Phase 9); FormatResult/FormatError paths thread the DialectContext into the formatter entry; format result metadata carries dialect/profile/exact_release like the parse result (DIALECT-04).

formatter/case.mbt: rewrite_keyword(context : DialectContext, raw : Bytes) routes through dialect.classification_of(context, raw) — for a Doris context this is the same table as v1, so output is byte-identical; the canonical word remains the UPPERCASE row word (D-28 case-fold contract unchanged). formatter/format.mbt: the entry format(root, source, options, context) threads context to every case-rewriting call; the refusal diagnostic code at the refusal_diagnostic helper becomes FATHOM-FORMAT-001 (approved-change register entry; update snapshots once after the register entry is recorded).

binding/exports.mbt: #export_name("fathom_format_v1") with pub fn fathom_format_v1(raw : Bytes, dialect : String, profile : String, mode : String, keyword_case : String, indent : Int, line_width : Int, comma_style : String, newline_style : String, trailing_newline : Bool) -> Bytes (row 32); binding/moon.pkg js/wasm exports lists add fathom_format_v1 in the same commit (Pitfall 8); the format option error codes DORIS-FORMAT-002/003/004 emitted at the export boundary become FATHOM-FORMAT-002/003/004 (row 39). binding/schema.mbt: FORMAT_SCHEMA_VERSION becomes fathom.format.v1 (row 36); format_error_json emits fathom.error.v1 and FATHOM-FORMAT-005/006/007 for invalid indent/line-width/invalid-tree (rows 36, 39); any remaining DORIS-FORMAT-* strings in binding are converted.

test/formatter_test.mbt: update every format_with_ids call to the (raw, dialect_id, profile_id, ...) signature with "doris"; add assertions: (a) fathom_format_v1(b"select 1", "doris", "4.x", "strict", ...) output contains fathom.format.v1; (b) a refusal case (malformed input) yields FATHOM-FORMAT-001 with fathom.error.v1; (c) fathom_format_v1(..., "flink", "flink-2.3.0", ...) returns the structured unsupported-profile error (FATHOM-SCHEMA-003, no formatting under Doris policy). Update parity/baseline_test.mbt format callsites to the new signature. After recording the approved-change entries (schema namespace, FATHOM-FORMAT-* codes), run moon test --update --package parity once, then baseline_diff.py must report zero unexpected diffs. Commit as feat(09-03): dialect-aware format path.</action>
  <acceptance_criteria>
    - format_with_ids(raw, "doris", profile, mode, ...) produces byte-identical Doris output to v1 (baseline gate green, approved diffs only); a refusal yields FATHOM-FORMAT-001 with fathom.error.v1.
    - fathom_format_v1 exists with #export_name and in binding/moon.pkg exports lists; FORMAT_SCHEMA_VERSION is fathom.format.v1; no old-format-schema or DORIS-FORMAT- string remains in formatter/, binding/, api/, or parity/.
    - fathom_format_v1(..., "flink", ...) returns the structured unsupported-profile error; no format output is produced under Doris policy for a flink request.
    - moon test --target native --package test --package parity --package formatter --package binding --package api passes; baseline_diff.py compares the committed HEAD snapshot tree (git archive HEAD:parity/__snapshot__) to the working tree and reports zero unexpected diffs; moon test --package parity without --update is the genuine drift gate.
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native --package test --package parity --package formatter --package api && ref=$(mktemp -d) && git archive HEAD:parity/__snapshot__ | tar -x -C "$ref" && python3 scripts/baseline_diff.py --left "$ref" --right parity/__snapshot__ --approve .planning/phases/09-dialect-boundary-and-neutral-naming/approved-changes.md</automated>
  </verify>
  <done>The format path is dialect-aware with complete fathom.format.v1 / FATHOM-FORMAT-* wire identity; Doris formatting is byte-identical; flink formatting is explicitly rejected.</done>
</task>

<task type="auto">
  <name>Task 2: Dialect-aware completion + completion tests</name>
  <files>completion/completion.mbt, lsp/completion_test.mbt</files>
  <read_first>
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-CONTEXT.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-RESEARCH.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-PATTERNS.md
    @/opt/source/Fathom/completion/completion.mbt
    @/opt/source/Fathom/lsp/completion_test.mbt
  </read_first>
  <action>Make completion dialect-aware (research rows 26-27, Pitfall 1).

completion/completion.mbt: complete(raw : Bytes, dialect_id : String, profile_id : String, cursor_byte : Int) -> CompletionResult validates dialect/profile FIRST (dialect then profile, flink rejected with the structured error in Phase 9 — a flink completion request must never enumerate Doris keywords); item generation reads dialect.classification_entries(context) (the accessor from 09-02) instead of the old token classification accessors; profile_allows(profile, feature) becomes context-routed (a Doris feature gate only applies in doris mode); the item detail string "Doris syntax keyword" becomes the neutral "SQL syntax keyword" (row 27). Doris completion output (item labels, order, ranges, is_incomplete) must be byte-identical to v1 — the baseline snapshots pin completion output for the representative subset (09-01 Task 2).

lsp/completion_test.mbt (the completion test suite lives in the lsp package — test/completion_test.mbt does not exist): update callsites to the (raw, dialect_id, profile_id, cursor_byte) signature with "doris"; add assertions: (a) doris-mode completion returns the same items as v1 for a representative prefix (e.g., after "SEL" the item set includes SELECT with the neutral detail); (b) flink-mode completion returns the structured unsupported-profile error, never Doris items; (c) unknown dialect returns the structured error. Commit as feat(09-03): dialect-aware completion.</action>
  <acceptance_criteria>
    - complete(raw, "doris", profile, cursor) returns byte-identical items to v1 for the representative prefix; item detail reads "SQL syntax keyword".
    - complete(raw, "flink", profile, cursor) returns the structured unsupported-profile error (FATHOM-SCHEMA-003) — no Doris items leak; complete(raw, "unknown", ...) returns UnknownDialect.
    - moon test --target native --package completion --package lsp --package test --package parity passes; baseline_diff.py (committed HEAD snapshot tree vs working tree) reports zero unexpected diffs; moon test --package parity without --update is the genuine drift gate.
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native --package completion --package lsp --package test --package parity && ref=$(mktemp -d) && git archive HEAD:parity/__snapshot__ | tar -x -C "$ref" && python3 scripts/baseline_diff.py --left "$ref" --right parity/__snapshot__ --approve .planning/phases/09-dialect-boundary-and-neutral-naming/approved-changes.md</automated>
  </verify>
  <done>Completion is dialect-aware: Doris items unchanged, flink explicitly rejected, neutral detail string, structured errors for unknown selection.</done>
</task>

</tasks>

## Source Audit Coverage

| SOURCE | ID | Feature or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| REQ | DIALECT-02 | keyword policy independent; completion/formatter consume context | 09-03 | COVERED | Tasks 1-2 route both consumers through dialect classification. |
| REQ | DIALECT-03 | explicit routing; flink rejected | 09-03 | COVERED | Format/completion validate dialect first; flink -> structured error. |
| REQ | NAME-02 | fathom.format.v1 + FATHOM-FORMAT-* | 09-03 | COVERED (format half) | Task 1 exports/schema/codes; capabilities half in 09-05. |
| REQ | DIALECT-04 | metadata on format/completion results | 09-03 | COVERED | Format result metadata carries dialect/profile/exact_release (Task 1). |
| RESEARCH | rows 24/26-29/32/36/39 | format/completion dialect params, export rename, schema, codes | 09-03 | COVERED | Tasks 1-2. |
| RESEARCH | Pitfall 1 / 6 / 8 | no parameterless queries; dialect-first validation; export sync | 09-03 | COVERED | Prohibitions + acceptance criteria. |
| CONTEXT | D-09 / D-10 | fathom.format.v1; FATHOM-FORMAT-* codes | 09-03 | COVERED | Task 1 (format half of the mapping). |
| CONTEXT | D-28 / D-33 | case-fold authority; refusal-first | 09-03 | COVERED | rewrite_keyword context-routing keeps canonical-word authority; refusal code renamed only. |
| PROBE | DIALECT-03 ordering | completion item order stable | 09-03 | FLAGGED | truth: no reordering for Doris. |

<verification>
Run the target-native package suite + baseline_diff.py after each task. Inspect: format_with_ids/fathom_format_v1 signature and validation order, FATHOM-FORMAT-001..007 coverage (formatter emission + binding mapping), completion neutral detail + flink rejection, no old-format-schema/DORIS-FORMAT- strings in product files, baseline gate green.
</verification>

<success_criteria>
- Format and completion are dialect-aware with neutral wire identity (fathom.format.v1, FATHOM-FORMAT-*); Doris behavior byte-identical.
- Flink format/completion requests are explicitly rejected with structured errors; no Doris-policy leak.
- The approved-change register covers every diff; the baseline gate stays green.
</success_criteria>

<output>Create `.planning/phases/09-dialect-boundary-and-neutral-naming/09-03-SUMMARY.md` when this plan executes.</output>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| dialect/profile strings -> format/completion entries | Selection strings must be validated before any formatting/completion work (dialect first, Pitfall 6). |
| formatter output -> consumers | Keyword case rewriting must follow the SELECTED dialect's classification, never a global table. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-10 | Tampering | Flink request formatted/completed under Doris keyword policy | high | mitigate | dialect-first validation in complete/format_with_ids; flink -> FATHOM-SCHEMA-003 before any keyword enumeration (Pitfall 6). |
| T-09-11 | Tampering | Case rewriting reads a stale global classification table | medium | mitigate | rewrite_keyword(context, raw) routes through dialect.classification_of; parameterless queries already removed in 09-02 (Pitfall 1). |
| T-09-12 | Tampering | Partial FATHOM-FORMAT migration (formatter emits new, binding maps old) | medium | mitigate | Both emission (formatter) and mapping (binding) updated in the same task; grep gates in acceptance criteria; NAME-04 gate in 09-07. |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies (09-RESEARCH Package Legitimacy Audit N/A). |
</threat_model>

## Artifacts this phase produces

Symbols (api/api.mbt): format_with_ids(raw, dialect_id, profile_id, mode_id, ...), format_with_metadata with dialect dimension, format-result dialect/profile/exact_release metadata.
Symbols (formatter/case.mbt): rewrite_keyword(context, raw) routed through dialect.classification_of.
Symbols (formatter/format.mbt): format(root, source, options, context), FATHOM-FORMAT-001 refusal code.
Symbols (completion/completion.mbt): complete(raw, dialect_id, profile_id, cursor_byte), context-routed profile_allows, neutral detail "SQL syntax keyword".
Symbols (binding/exports.mbt): fathom_format_v1(raw, dialect, profile, mode, ...) export; FATHOM-FORMAT-002/003/004 at the export boundary.
Symbols (binding/schema.mbt): FORMAT_SCHEMA_VERSION fathom.format.v1; FATHOM-FORMAT-005/006/007 in format_error_json.
