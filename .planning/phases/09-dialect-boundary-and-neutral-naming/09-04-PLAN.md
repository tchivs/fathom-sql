---
phase: 09-dialect-boundary-and-neutral-naming
plan: 04
type: execute
wave: 4
depends_on: [09-03]
files_modified:
  - moon.mod
  - moon.pkg
  - api/moon.pkg
  - token/moon.pkg
  - lexer/moon.pkg
  - parser/moon.pkg
  - syntax/moon.pkg
  - source/moon.pkg
  - printer/moon.pkg
  - formatter/moon.pkg
  - completion/moon.pkg
  - analyzer/moon.pkg
  - binding/moon.pkg
  - lsp/moon.pkg
  - parity/moon.pkg
  - test/moon.pkg
  - doris-sql/moon.pkg
  - doris-sql/args.mbt
  - doris-sql/run.mbt
  - doris-sql/main.mbt
  - doris-sql/cli_test.mbt
  - lsp/serve.mbt
  - lsp/main.mbt
  - fathom-lsp/moon.pkg
  - fathom-lsp/main.mbt
  - .github/workflows/doris-native-release.yml
  - .github/workflows/jetbrains-plugin.yml
  - README.md
  - README.zh-CN.md
  - docs/API.md
  - docs/GETTING-STARTED.md
autonomous: false
requirements: [NAME-01, DIALECT-01]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The module identity is neutral: moon.mod name is fathom/sql, all 16 moon.pkg import prefixes are fathom/sql/<pkg> with aliases unchanged, and the CLI package directory is fathom-sql/ producing the fathom-sql.exe binary — no old module/package name remains in any product file (NAME-01, D-06)."
    - "The CLI implements the D-11 contract: fathom-sql parse|format|lsp subcommands with --dialect <doris|flink> and --profile <id> both required; missing or unknown dialect/profile returns exit 2 with a structured usage error; no default dialect exists (DIALECT-01, D-11)."
    - "The lsp subcommand and the standalone fathom-lsp binary run the SAME server loop through a shared library entry: lsp/ becomes a library exposing serve_stdio(initial_dialect : String?, initial_profile : String?), the new fathom-lsp/ executable package calls it with None defaults, and the CLI lsp subcommand passes the parsed --dialect/--profile as workspace defaults (NAME-01, D-01 seam)."
    - "Release packaging uses neutral asset names: the native release workflow (renamed to fathom-native-release.yml) produces fathom-lsp-{platform} assets and fathom-lsp-manifest.json, and the JetBrains workflow artifact is fathom-sql-intellij (NAME-03)."
    - "[probe NAME-01 unclassified] resolved: 'no old aliases' is enforced as a zero-tolerance grep gate over product files — no re-export shim, no compat alias, no legacy import path remains (D-06)."
  artifacts:
    - path: "moon.mod"
      provides: "name = fathom/sql (version stays 0.1.0 — release-bump decision deferred to release planning, research OQ7)"
    - path: "fathom-sql/args.mbt"
      provides: "UsageError + MissingDialect/UnknownDialect, parse_args with parse|format|lsp and --dialect/--profile required, is_valid_dialect_profile(dialect, id)"
    - path: "fathom-sql/run.mbt"
      provides: "usage text fathom-sql parse|format|lsp --dialect <doris|flink> --profile <id>, run_parse/run_format/run_lsp dispatch, exit 0/1/2 semantics"
    - path: "lsp/serve.mbt"
      provides: "pub fn serve_stdio(initial_dialect : String?, initial_profile : String?) — the stdio server loop extracted from lsp/main.mbt"
    - path: "fathom-lsp/moon.pkg + main.mbt"
      provides: "standalone fathom-lsp executable calling serve_stdio(None, None)"
  key_links:
    - from: "moon.mod"
      to: "16 moon.pkg import blocks"
      via: "module name change is atomic — all import prefixes update in one commit or nothing compiles (Pitfall 8)"
    - from: "doris-sql/" (renamed "fathom-sql/")
      to: "lsp/serve.mbt"
      via: "run_lsp -> @lsp.serve_stdio(command.dialect, command.profile); the CLI and fathom-lsp binary share one server loop"
    - from: "fathom-sql/args.mbt"
      to: "binding/schema.mbt"
      via: "is_valid_dialect_profile mirrors validate_dialect_profile (doris: 2.1/3.x/4.x; flink: all rejected in Phase 9)"
  prohibitions:
    - "No backward-compatibility aliases: the clean cutover (D-06) forbids re-export shims, legacy import paths, or compat binary names — old names appear nowhere in product files."
    - "The CLI never defaults a dialect: --dialect and --profile are required for every subcommand; a missing flag is exit 2, never a guess (D-02/D-11)."
    - "lsp/ and fathom-sql/ must not duplicate the server loop — one serve_stdio implementation is the only stdio entry (no second server main)."
---

<objective>
**As a** consumer, **I want to** install and invoke the SDK under one neutral product identity — fathom/sql imports, fathom-sql CLI, fathom-lsp server — **so that** no old Doris product name leaks into modules, binaries, or scripts, while every invocation still requires an explicit dialect and profile.

Purpose: Execute the D-06 clean cutover (one-way) for the module/package/CLI core and the D-11 CLI contract (costly): moon.mod rename, all import prefixes, git mv of the CLI package, the --dialect/--profile CLI surface with parse|format|lsp subcommands, the shared LSP serve seam, and neutral release asset names. Host config keys and docs prose beyond import examples land in 09-06/09-07.
Output: fathom/sql module identity, fathom-sql CLI with the full D-11 argument contract, lsp library + serve_stdio seam + fathom-lsp executable, neutral release workflow assets, updated docs import examples.
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-dialect-boundary-and-neutral-naming/09-CONTEXT.md
@.planning/phases/09-dialect-boundary-and-neutral-naming/09-RESEARCH.md
@.planning/phases/09-dialect-boundary-and-neutral-naming/09-PATTERNS.md
@.planning/phases/09-dialect-boundary-and-neutral-naming/09-03-SUMMARY.md

D-06 is the one-way product cutover: module/import rename to fathom/sql, CLI to fathom-sql, server identity to fathom-lsp; no aliases. Research rows 1-7 (module/imports), 51-57 (CLI), 77-78 (release assets) are this plan's checklist. The research verified the MoonBit facts: module names may contain /, package names = directory names (renaming doris-sql/ renames the binary), #export_name + moon.pkg exports must stay in sync (Pitfall 8). D-11 requires --dialect and --profile on parse|format|lsp; the lsp subcommand's dialect/profile become workspace defaults consumed by 09-06's ServerState selection resolution. The existing lsp/ package is executable — to let fathom-sql run the server in-process it becomes a library with a serve_stdio entry, and the standalone binary moves to a new fathom-lsp/ executable package (release workflow path _build/native/release/build/fathom-lsp/fathom-lsp.exe).
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm D-06 clean cutover scope (one-way door)</name>
  <decision>Product naming cutover: module/import/CLI/binary renames with no backward-compatibility aliases — a one-way door (D-06).</decision>
  <context>D-06 locks the full cutover surface: fathom/sql module and imports, fathom-sql CLI, fathom-lsp server identity, fathom_*_v1 exports (done in 09-02/09-03), fathom.*.v1 schemas (done in 09-02/09-03), FATHOM-* codes (done), LSP serverInfo/source (09-06), host config keys and package names (09-06/09-07), doc titles (09-07). No old public alias is retained — the product is not formally released, so this milestone intentionally performs the clean cutover (REQUIREMENTS.md Out of Scope). The module version stays 0.1.0 (research OQ7 — bump is a release-planning decision, recorded for the release manifest). The GitHub release repository name tchivs/doris-sql-parser-sdk is unchanged this phase (research OQ8 — repository renaming is a user-run GitHub operation; the JetBrains downloader keeps it and NAME-04 allowlists it as release-repository provenance).</decision>
  <options>
    <option id="option-a">
      <name>Full clean cutover per D-06 (recommended — locked decision)</name>
      <pros>Single atomic rename wave; zero compat surface to maintain; NAME-04 gate (09-07) passes on the finished state.</pros>
      <cons>All import-bearing files must change in one commit; docs/examples must move with the module name or they rot.</cons>
    </option>
    <option id="option-b">
      <name>Keep the old module name, add a fathom alias</name>
      <pros>Smaller diff per commit.</pros>
      <cons>Violates D-06 and REQUIREMENTS.md Out of Scope (no backward-compat aliases); NAME-04 gate would fail on the alias surface.</cons>
    </option>
  </options>
  <resume-signal>Select option-a (full clean cutover) or describe a modified scope; the module rename and git mv execute only after this confirmation.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Module identity rename — moon.mod, 16 moon.pkg import prefixes, docs import examples</name>
  <files>moon.mod, moon.pkg, api/moon.pkg, token/moon.pkg, lexer/moon.pkg, parser/moon.pkg, syntax/moon.pkg, source/moon.pkg, printer/moon.pkg, formatter/moon.pkg, completion/moon.pkg, analyzer/moon.pkg, binding/moon.pkg, lsp/moon.pkg, parity/moon.pkg, test/moon.pkg, doris-sql/moon.pkg, README.md, README.zh-CN.md, docs/API.md, docs/GETTING-STARTED.md</files>
  <read_first>
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-CONTEXT.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-RESEARCH.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-PATTERNS.md
    @/opt/source/Fathom/moon.mod
    @/opt/source/Fathom/moon.pkg
    @/opt/source/Fathom/api/moon.pkg
    @/opt/source/Fathom/test/moon.pkg
    @/opt/source/Fathom/analyzer/moon.pkg
  </read_first>
  <action>Rename the module and every import prefix atomically (Pitfall 8: one commit or nothing compiles).

moon.mod: the name line becomes "fathom/sql" (research row 1); keep version = "0.1.0" and the pinned-toolchain header comments unchanged. Every moon.pkg import block (all 16, including the root moon.pkg, api, token, lexer, parser, syntax, source, printer, formatter, completion, analyzer, binding, lsp, parity, test, doris-sql): replace the old "fathom/doris-sql/<pkg>" prefix with "fathom/sql/<pkg>" — aliases unchanged, pkgtype/options blocks unchanged. analyzer/moon.pkg keeps its single import to fathom/sql/syntax (the D-21 negative gate: analyzer imports ONLY syntax — verify the import list is still exactly that after the prefix change).

Docs: update the import examples in README.md, README.zh-CN.md, docs/API.md, and docs/GETTING-STARTED.md from "fathom/doris-sql/api" to "fathom/sql/api" and CLI references from the old CLI package path to fathom-sql (research rows 7, 86 — full doc prose/titles are 09-07's task; this task only fixes the module/import/CLI-path strings so docs compile-check examples).

Verify: moon check --target native passes (module rename + all imports atomic); a grep over product files (excluding .planning/, corpus/, milestones/, _build/, node_modules/) finds zero occurrences of the old module prefix in any moon.pkg or .mbt file. Commit as feat(09-04): rename module to fathom/sql.</action>
  <acceptance_criteria>
    - moon.mod name line reads fathom/sql; every moon.pkg import prefix reads fathom/sql/<pkg> with aliases identical to v1.
    - moon check --target native and moon build --target native pass (the atomic rename compiles); analyzer/moon.pkg imports exactly fathom/sql/syntax and nothing else.
    - A grep over product files (excluding .planning/, corpus/, milestones/, _build/, node_modules/) finds zero occurrences of the old module path prefix in moon.pkg and .mbt files; README/docs import examples read fathom/sql/api.
    - The parity baseline gate still passes (moon test --package parity) — a module rename changes no behavior bytes.
  </acceptance_criteria>
  <verify>
    <automated>moon check --target native && moon test --package parity</automated>
  </verify>
  <done>The module identity is fathom/sql across the whole repo; imports and docs examples are atomic; behavior bytes unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: CLI cutover — git mv, --dialect/--profile contract, parse|format|lsp subcommands, serve seam, release assets</name>
  <files>doris-sql/moon.pkg, doris-sql/args.mbt, doris-sql/run.mbt, doris-sql/main.mbt, doris-sql/cli_test.mbt, lsp/moon.pkg, lsp/serve.mbt, lsp/main.mbt, fathom-lsp/moon.pkg, fathom-lsp/main.mbt, .github/workflows/doris-native-release.yml, .github/workflows/jetbrains-plugin.yml</files>
  <read_first>
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-CONTEXT.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-RESEARCH.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-PATTERNS.md
    @/opt/source/Fathom/doris-sql/args.mbt
    @/opt/source/Fathom/doris-sql/run.mbt
    @/opt/source/Fathom/doris-sql/main.mbt
    @/opt/source/Fathom/doris-sql/cli_test.mbt
    @/opt/source/Fathom/lsp/main.mbt
    @/opt/source/Fathom/lsp/handlers.mbt
    @/opt/source/Fathom/.github/workflows/doris-native-release.yml
    @/opt/source/Fathom/.github/workflows/jetbrains-plugin.yml
  </read_first>
  <action>Execute the CLI and server binary cutover (research rows 51-57, 77-78).

git mv doris-sql fathom-sql (directory + all files in one move — the binary name follows the directory name per the verified MoonBit rule, Pitfall 8). fathom-sql/args.mbt: UsageError gains MissingDialect and UnknownDialect(dialect_id~ : String) variants; parse_args accepts exactly "parse", "format", "lsp" subcommands; --dialect <doris|flink> and --profile <id> are REQUIRED flags for every subcommand (D-11); is_valid_profile becomes is_valid_dialect_profile(dialect : String, id : String) -> Bool (doris: "2.1"/"3.x"/"4.x"; flink: false for every id in Phase 9); Command gains pub dialect : String. fathom-sql/run.mbt: usage_text becomes "usage: fathom-sql parse|format|lsp --dialect <doris|flink> --profile <id> ..."; usage_error_message covers MissingDialect/UnknownDialect (missing flag -> "missing required flag: --dialect <doris|flink>", unknown value -> "unknown dialect: <id>") with exit 2 semantics unchanged (D-39); run_parse executes @api.parse_with_ids(input, command.dialect, command.profile, "strict") and writes @binding.parse_result_json bytes to stdout; run_format passes command.dialect into @api.format_with_ids (row 56); run_lsp calls @lsp.serve_stdio(command.dialect, command.profile). main.mbt keeps the --help-first and stdin-read-once IO skeleton, dispatching on the subcommand.

lsp/ becomes a library: lsp/moon.pkg pkgtype changes from executable to library (imports unchanged); extract the stdio loop from lsp/main.mbt into pub fn serve_stdio(initial_dialect : String?, initial_profile : String?) in a new lsp/serve.mbt — it constructs ServerState::new() and stores initial_dialect/initial_profile into two new optional String fields on ServerState (added in lsp/handlers.mbt; the existing profile field and handler logic stay untouched — 09-06 consumes the new fields and removes profile); delete lsp/main.mbt (the executable main moves to the new package). Create fathom-lsp/ as a new executable package: fathom-lsp/moon.pkg with pkgtype(kind: "executable") importing the lsp library, and fathom-lsp/main.mbt calling @lsp.serve_stdio(None, None).

Release workflows: git mv .github/workflows/doris-native-release.yml .github/workflows/fathom-native-release.yml; rename the workflow display name to "Fathom Native Release"; asset names become dist/fathom-lsp-${{ matrix.platform }} (copy path _build/native/release/build/fathom-lsp/fathom-lsp.exe) and the manifest fathom-lsp-manifest.json (rows 77). .github/workflows/jetbrains-plugin.yml artifact name becomes fathom-sql-intellij (row 78).

fathom-sql/cli_test.mbt: command_stdin gains the dialect field; update every Command construction to dialect "doris"; extend the exit-code matrix: missing --dialect -> exit 2 with a message containing "missing required flag: --dialect"; --dialect flink --profile 4.x -> exit 2 (unknown/unsupported profile for flink in Phase 9); --dialect unknown -> exit 2 with "unknown dialect"; a full parse invocation (fathom-sql parse --dialect doris --profile 4.x on stdin "SELECT 1") exits 0 and prints output containing fathom.parse.v1. Commit as feat(09-04): CLI cutover with explicit dialect contract.</action>
  <acceptance_criteria>
    - fathom-sql binary exists after moon build --target native (directory renamed); the old CLI directory name appears nowhere in product files.
    - Missing --dialect or --profile exits 2 with the missing-flag message; --dialect unknown exits 2 with the unknown-dialect message; --dialect flink --profile 4.x exits 2 (flink profiles all rejected in Phase 9); no invocation defaults a dialect.
    - fathom-sql parse --dialect doris --profile 4.x on "SELECT 1" exits 0 and prints serialized output containing fathom.parse.v1 and dialect doris.
    - fathom-sql lsp --dialect doris --profile 4.x starts the stdio server (smoke: initialize handshake responds); fathom-lsp binary builds and starts the same loop with None defaults.
    - The release workflow is fathom-native-release.yml producing fathom-lsp-{platform} assets + fathom-lsp-manifest.json; jetbrains-plugin.yml artifact is fathom-sql-intellij.
    - moon check --target native passes; moon test --package parity and the full native suite pass (no behavior bytes changed).
  </acceptance_criteria>
  <verify>
    <automated>moon build --target native && moon test --target native --package test --package parity --package lsp --package api</automated>
  </verify>
  <done>The CLI and server carry neutral names with the full D-11 explicit-selection contract; release assets are fathom-lsp-*; behavior bytes unchanged.</done>
</task>

</tasks>

## Source Audit Coverage

| SOURCE | ID | Feature or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| REQ | NAME-01 | fathom/sql imports, fathom-sql/fathom-lsp binaries, no aliases | 09-04 | COVERED | Tasks 2-3 + prohibition. |
| REQ | DIALECT-01 | explicit selection through the CLI; missing/unknown -> structured error | 09-04 | COVERED | Task 3 --dialect/--profile contract (CLI boundary). |
| REQ | NAME-03 | release assets neutral | 09-04 | COVERED (release part) | fathom-native-release.yml + fathom-lsp-* + fathom-sql-intellij; host surfaces in 09-07. |
| RESEARCH | rows 1-7 | module/import/CLI renames | 09-04 | COVERED | Task 2. |
| RESEARCH | rows 51-57 | CLI args/run/usage/test | 09-04 | COVERED | Task 3. |
| RESEARCH | rows 77-78 | release asset names | 09-04 | COVERED | Task 3. |
| RESEARCH | Pitfall 8 / OQ7 / OQ8 | dir name = binary; version bump deferred; repo name kept | 09-04 | COVERED | Task 1 checkpoint + Task 3. |
| CONTEXT | D-06 | clean cutover, no aliases, one-way | 09-04 | COVERED | Task 1 checkpoint + Tasks 2-3. |
| CONTEXT | D-11 | CLI dialect+profile required, exit 2 | 09-04 | COVERED | Task 3. |
| PROBE | NAME-01 unclassified | no old aliases | 09-04 | RESOLVED | truth + zero-tolerance grep gate. |

<verification>
Run moon check/build after Task 2; run the full native suite + parity after Task 3. Inspect: module name + all import prefixes, fathom-sql directory/binary, exit-code matrix (0/1/2), lsp library + serve_stdio seam + fathom-lsp executable, release workflow asset names, docs import examples. Do not rename vscode/web/jetbrains config keys (09-07) or the ServerState selection logic (09-06).
</verification>

<success_criteria>
- fathom/sql module identity and fathom-sql/fathom-lsp binaries are in place with no aliases; imports and docs compile-check.
- The CLI enforces explicit dialect+profile on parse|format|lsp with exit 2 structured errors and no defaults.
- The server loop is single-sourced (serve_stdio); release assets are neutral; Doris behavior bytes unchanged (baseline green).
</success_criteria>

<output>Create `.planning/phases/09-dialect-boundary-and-neutral-naming/09-04-SUMMARY.md` when this plan executes.</output>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| CLI argv -> parse_args | Untrusted subcommand/dialect/profile strings cross here; missing/unknown must exit 2, never default. |
| stdout/stderr bytes | Exit codes 0/1/2 and byte-exact output are a scripted contract (D-39) — regressions break automation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-13 | Tampering | CLI silently defaults a dialect (no --dialect) | high | mitigate | --dialect/--profile required for every subcommand; MissingDialect/MissingProfile -> exit 2 (D-02/D-11). |
| T-09-14 | Tampering | Old binary/asset names still referenced by scripts or workflow | medium | mitigate | git mv in one commit + release workflow renamed; NAME-04 gate (09-07) scans workflows; cli_test exit-code matrix covers the new surface. |
| T-09-15 | Spoofing | Duplicated server loop diverges (CLI lsp vs fathom-lsp) | medium | mitigate | Single serve_stdio implementation; fathom-lsp main and run_lsp are both thin callers (prohibition). |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies (09-RESEARCH Package Legitimacy Audit N/A). |
</threat_model>

## Artifacts this phase produces

Renamed: doris-sql/ -> fathom-sql/ (binary fathom-sql.exe); .github/workflows/doris-native-release.yml -> fathom-native-release.yml.
New files: lsp/serve.mbt, fathom-lsp/moon.pkg, fathom-lsp/main.mbt.
Symbols (fathom-sql/args.mbt): UsageError::MissingDialect, UsageError::UnknownDialect(dialect_id~), parse_args (parse|format|lsp), is_valid_dialect_profile(dialect, id), Command.dialect.
Symbols (fathom-sql/run.mbt): run_parse, run_format (dialect-aware), run_lsp, usage_text "fathom-sql parse|format|lsp --dialect <doris|flink> --profile <id>".
Symbols (lsp/serve.mbt): serve_stdio(initial_dialect : String?, initial_profile : String?).
Symbols (lsp/handlers.mbt): ServerState.default_dialect : String?, ServerState.default_profile : String? (fields added; selection logic consumed in 09-06).
Modified: moon.mod (name fathom/sql), 16 moon.pkg import prefixes, lsp/moon.pkg (executable -> library), README/README.zh-CN/docs/API.md/docs/GETTING-STARTED.md import examples, fathom-native-release.yml asset names (fathom-lsp-{platform}, fathom-lsp-manifest.json), jetbrains-plugin.yml artifact fathom-sql-intellij.
