---
phase: 09-dialect-boundary-and-neutral-naming
plan: 05
type: execute
wave: 5
depends_on: [09-03, 09-04]
files_modified:
  - binding/exports.mbt
  - binding/schema.mbt
  - binding/moon.pkg
  - parity/schema_test.mbt
  - parity/export_smoke_test.mbt
  - parity/parity_test.mbt
  - parity/coordinates_test.mbt
  - parity/fixtures/target-matrix.json
  - parity/baseline_test.mbt
autonomous: true
requirements: [NAME-02, DIALECT-04]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The wire contract is fully neutral and complete: PARSE_SCHEMA_VERSION fathom.parse.v1, FORMAT_SCHEMA_VERSION fathom.format.v1, error schema fathom.error.v1, dialect schema fathom.dialect.v1, capabilities schema fathom.capabilities.v1; exports fathom_parse_v1/fathom_format_v1/fathom_dialect_v1/fathom_capabilities_v1 all exist with #export_name AND in binding/moon.pkg js/wasm exports lists (NAME-02, D-09, Pitfall 8)."
    - "validate_dialect_profile(dialect, profile) is the single validation gate: doris accepts exactly 2.1/3.x/4.x, flink rejects every profile in Phase 9, unknown dialect -> SchemaError::UnknownDialect, and FATHOM-SCHEMA-007 covers the ConflictingSelection variant (DIALECT-01, research OQ3)."
    - "fathom_dialect_v1(dialect) returns the dialect's available profiles + version metadata (doris: 2.1/3.x/4.x with exact_release; flink: empty set in Phase 9) under fathom.dialect.v1, and fathom_capabilities_v1() returns the global dialect list under fathom.capabilities.v1 (research OQ4)."
    - "Every serialized result carries dialect/profile/exact_release metadata and FATHOM-* codes; no doris.*.v1 schema string, doris_*_v1 export symbol, or DORIS-* code remains in any product file (NAME-02, D-09/D-10)."
    - "[probe NAME-02 unclassified] resolved: completeness is enforced by grep — the four schema namespaces, four export symbols, and code families are the exact checklist; parity tests assert each export's schema_version string."
  artifacts:
    - path: "binding/exports.mbt"
      provides: "fathom_parse_v1 (exists from 09-02), fathom_format_v1 (from 09-03), fathom_dialect_v1(dialect), fathom_capabilities_v1()"
    - path: "binding/schema.mbt"
      provides: "validate_dialect_profile, SchemaError::UnknownDialect, FATHOM-SCHEMA-001..007, FATHOM-FORMAT-002..007, dialect_json (fathom.dialect.v1), capabilities_json (fathom.capabilities.v1), neutral messages"
    - path: "parity/schema_test.mbt + export_smoke_test.mbt + parity_test.mbt + coordinates_test.mbt"
      provides: "schema/export assertions migrated to the fathom.*.v1 names and fathom_*_v1 exports with dialect arguments"
    - path: "parity/fixtures/target-matrix.json"
      provides: "cross-target fixture assertions updated to fathom.*.v1 + dialect field"
  key_links:
    - from: "binding/exports.mbt"
      to: "binding/moon.pkg"
      via: "#export_name symbols and the js/wasm exports lists must stay identical — updated in the same commit (Pitfall 8)"
    - from: "binding/schema.mbt"
      to: "api/api.mbt"
      via: "validate_dialect_profile and SchemaError mirror api ParseError::UnknownDialect/ConflictingSelection — one error surface"
    - from: "binding/schema.mbt"
      to: "parity/schema_test.mbt"
      via: "validate_schema_version accepts exactly the four fathom.*.v1 strings; tests assert each schema_version field"
  prohibitions:
    - "No partial wire migration: after this plan, no product file may contain any doris.*.v1 schema string, doris_*_v1 export symbol, or DORIS-* diagnostic code (NAME-02 gate; enforced by grep + 09-07's naming gate)."
    - "No fabricated metadata: exact_release/feature_introduction values in fathom_dialect_v1 come from the existing DorisProfile metadata — nothing invented, nothing inferred (provenance discipline)."
---

<objective>
**As a** machine consumer, **I want to** read one uniform fathom.*.v1 wire contract with fathom_*_v1 exports and FATHOM-* codes from every target, **so that** no consumer ever parses the old doris.*.v1 schema and the dialect/profile/exact-release metadata is always present.

Purpose: Complete the NAME-02 wire-contract cutover (the capabilities/dialect exports, remaining schema namespaces, the full code mapping, and the shared validation gate) and migrate the parity fixtures/tests that pin the wire surface. 09-02/09-03 covered the parse and format halves; this plan closes the dialect/capabilities/error surface and the parity package.
Output: fathom_dialect_v1/fathom_capabilities_v1 exports, fathom.dialect.v1/fathom.capabilities.v1 schemas, validate_dialect_profile full gate, FATHOM-SCHEMA-001..007 + FATHOM-FORMAT-002..007 mapping, migrated parity tests/fixtures, snapshot update (approved).
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-dialect-boundary-and-neutral-naming/09-CONTEXT.md
@.planning/phases/09-dialect-boundary-and-neutral-naming/09-RESEARCH.md
@.planning/phases/09-dialect-boundary-and-neutral-naming/09-PATTERNS.md
@.planning/phases/09-dialect-boundary-and-neutral-naming/09-04-SUMMARY.md

Research rows 31-40 (binding exports/schema), 82-83 (parity tests/fixtures) are this plan's checklist, plus OQ3 (ConflictingSelection -> FATHOM-SCHEMA-007) and OQ4 (doris_profile_v1 -> fathom_dialect_v1). The parity fixture lsp-tracer.json is owned by 09-06 (LSP output); this plan handles target-matrix.json and the parity test files. binding/moon.pkg was renamed by 09-04's import sweep — its js/wasm exports lists still carry the old export symbols and are updated here with the same-commit rule (Pitfall 8). Approved-change register entries: schema namespace strings, export names, FATHOM-SCHEMA-007 mint, capabilities/dialect schema fields. Snapshot update happens once with approval after the register entries are recorded.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete the binding wire contract — fathom_dialect_v1/fathom_capabilities_v1, validate_dialect_profile, FATHOM-SCHEMA/FORMAT mapping</name>
  <files>binding/exports.mbt, binding/schema.mbt, binding/moon.pkg</files>
  <read_first>
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-CONTEXT.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-RESEARCH.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-PATTERNS.md
    @/opt/source/Fathom/binding/exports.mbt
    @/opt/source/Fathom/binding/schema.mbt
    @/opt/source/Fathom/binding/moon.pkg
  </read_first>
  <action>Close the wire contract (research rows 31-40, OQ3/OQ4).

binding/exports.mbt: replace doris_profile_v1 with #export_name("fathom_dialect_v1") and pub fn fathom_dialect_v1(dialect : String) -> Bytes — returns the dialect's available profiles with version metadata under the fathom.dialect.v1 schema (doris: profiles "2.1"/"3.x"/"4.x" with exact_release/feature_introduction from the existing DorisProfile metadata — nothing fabricated; flink: empty profile set in Phase 9); replace doris_capabilities_v1 with #export_name("fathom_capabilities_v1") and pub fn fathom_capabilities_v1() -> Bytes — returns the global dialects list (doris, flink) with per-dialect profile availability under fathom.capabilities.v1 (row 34, OQ4). binding/moon.pkg: the js and wasm exports lists must contain exactly fathom_parse_v1, fathom_format_v1, fathom_dialect_v1, fathom_capabilities_v1 — updated in the SAME commit as #export_name (Pitfall 8).

binding/schema.mbt: validate_dialect_profile(dialect : String, profile : String) -> Result[Unit, SchemaError] is the single gate (doris: 2.1/3.x/4.x; flink: every profile rejected in Phase 9; unknown dialect -> Err(UnknownDialect)); SchemaError gains UnknownDialect; schema_error_code maps UnknownDialect + ConflictingSelection to FATHOM-SCHEMA-007 (new code — add to approved-changes.md first) and keeps FATHOM-SCHEMA-001..006 per the section-5 table; profile_json becomes dialect_json emitting schema_version fathom.dialect.v1 with dialect/profile/exact_release fields; capabilities_json emits schema_version fathom.capabilities.v1 with dialects + per-dialect profiles; any remaining old-schema strings in error_json/parse_error_json/format_error_json (fathom.error.v1 already in place from 09-02/09-03) are confirmed; format_error_json FATHOM-FORMAT-002..007 mapping confirmed complete (row 39). Neutral messages confirmed across schema_error_message/parse_error_json (no "unsupported Doris profile" phrasing — dialect in fields).

Add or extend binding-level tests (in the parity or binding package per existing convention): validate_dialect_profile("doris", "2.1"/"3.x"/"4.x") Ok; ("doris", "5.x") Err(UnsupportedProfile); ("flink", "flink-2.3.0") Err(UnsupportedProfile); ("mysql", "4.x") Err(UnknownDialect); fathom_dialect_v1("doris") output contains fathom.dialect.v1 and "4.x"; fathom_capabilities_v1() output contains fathom.capabilities.v1 and both "doris" and "flink". Commit as feat(09-05): complete fathom wire contract.</action>
  <acceptance_criteria>
    - binding/moon.pkg js+wasm exports lists equal the four #export_name symbols exactly (fathom_parse_v1, fathom_format_v1, fathom_dialect_v1, fathom_capabilities_v1) — no stale symbols remain.
    - validate_dialect_profile passes exactly {doris x 2.1/3.x/4.x}, rejects every flink profile, and returns UnknownDialect for unknown dialects; ConflictingSelection and UnknownDialect serialize to FATHOM-SCHEMA-007.
    - fathom_dialect_v1("doris") output contains fathom.dialect.v1 and the three doris profiles; fathom_capabilities_v1() output contains fathom.capabilities.v1 and both dialect names.
    - grep over product files finds no old-schema string (doris.parse.v1 / doris.format.v1 / doris.error.v1 / doris.profile.v1 / doris.capabilities.v1) and no old export symbol (doris_parse_v1 / doris_format_v1 / doris_profile_v1 / doris_capabilities_v1) and no DORIS- code prefix anywhere outside corpus/, .planning/, milestones/, _build/, node_modules/.
    - moon test --target native --package parity --package api passes.
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native --package parity --package api</automated>
  </verify>
  <done>The wire contract is complete and neutral: four fathom.*.v1 schemas, four fathom_*_v1 exports (sync with moon.pkg), the shared validation gate, and the full FATHOM-* code mapping.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate parity fixtures/tests to the neutral wire surface and update baseline snapshots (approved)</name>
  <files>parity/schema_test.mbt, parity/export_smoke_test.mbt, parity/parity_test.mbt, parity/coordinates_test.mbt, parity/fixtures/target-matrix.json, parity/baseline_test.mbt</files>
  <read_first>
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-CONTEXT.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-RESEARCH.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-PATTERNS.md
    @/opt/source/Fathom/parity/schema_test.mbt
    @/opt/source/Fathom/parity/export_smoke_test.mbt
    @/opt/source/Fathom/parity/parity_test.mbt
    @/opt/source/Fathom/parity/coordinates_test.mbt
    @/opt/source/Fathom/parity/fixtures/target-matrix.json
    @/opt/source/Fathom/parity/baseline_test.mbt
  </read_first>
  <action>Migrate the parity surface (research rows 82-83) and re-freeze snapshots once with approval.

parity/schema_test.mbt: validate_schema_version assertions use the four fathom.*.v1 strings; validate_profile calls become validate_dialect_profile(dialect, profile) pairs incl. the unknown-dialect negative. parity/export_smoke_test.mbt: every export callsite uses fathom_*_v1 with the dialect argument ("doris"); schema-version assertions use fathom.*.v1; add the dialect field assertions on parse/format results. parity/parity_test.mbt and coordinates_test.mbt: parse_with_ids/format_with_ids callsites gain "doris" and any schema string assertions move to fathom.*.v1. parity/fixtures/target-matrix.json: schema assertions updated to fathom.*.v1 and the dialect field added (research row 81). parity/baseline_test.mbt: export callsites use fathom_*_v1(raw, "doris", profile, mode) (already migrated in 09-02 for parse — complete for format/dialect/capabilities as needed).

After recording the approved-change entries for this plan (any remaining schema-string diffs, dialect fields in fixtures), run moon test --update --package parity once, then run baseline_diff.py and confirm zero unexpected diffs. Commit as feat(09-05): migrate parity to fathom wire contract.</action>
  <acceptance_criteria>
    - No parity test or fixture references any old schema string or old export symbol; every callsite passes the dialect argument.
    - moon test --package parity passes on native, js, and wasm targets; the cross-target byte-equality assertion still holds.
    - baseline_diff.py reports zero unexpected diffs (only approved entries); parity/__snapshot__ updated exactly once with the register recorded.
  </acceptance_criteria>
  <verify>
    <automated>moon test --package parity && python3 scripts/baseline_diff.py --left parity/__snapshot__ --right parity/__snapshot__ --approve .planning/phases/09-dialect-boundary-and-neutral-naming/approved-changes.md</automated>
  </verify>
  <done>The parity package pins the neutral wire contract across native/js/wasm with zero unexpected baseline diffs.</done>
</task>

</tasks>

## Source Audit Coverage

| SOURCE | ID | Feature or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| REQ | NAME-02 | fathom.parse/format/error/capabilities.v1 + FATHOM-* + dialect/profile fields | 09-05 | COVERED | Task 1 completes the four namespaces + four exports + code mapping. |
| REQ | DIALECT-04 | serialized results carry dialect/profile/exact-release metadata | 09-05 | COVERED | dialect_json/capabilities_json carry the metadata; parity pins it. |
| RESEARCH | rows 33-34, 37 | doris_profile_v1 -> fathom_dialect_v1; capabilities | 09-05 | COVERED | Task 1 (OQ4). |
| RESEARCH | rows 35, 38-40 | exports sync; validate_dialect_profile; neutral messages | 09-05 | COVERED | Task 1. |
| RESEARCH | rows 80-83 | parity fixtures/tests migration | 09-05 | COVERED | Task 2 (lsp-tracer.json owned by 09-06). |
| RESEARCH | OQ3 | ConflictingSelection -> FATHOM-SCHEMA-007 | 09-05 | COVERED | Task 1. |
| RESEARCH | Pitfall 4 / 8 | half-migration; export sync | 09-05 | COVERED | Prohibition + same-commit rule + grep gates. |
| CONTEXT | D-09 | fathom.*.v1 namespaces | 09-05 | COVERED | Task 1. |
| CONTEXT | D-10 | FATHOM-* codes; dialect in fields | 09-05 | COVERED | Task 1. |
| PROBE | NAME-02 unclassified | wire completeness | 09-05 | RESOLVED | truth + grep checklist. |

<verification>
Run moon test --package parity after each task (native+js+wasm); run baseline_diff.py after the snapshot update. Inspect: four exports + moon.pkg sync, validate_dialect_profile matrix, FATHOM-SCHEMA-007, fathom.dialect.v1/fathom.capabilities.v1 content, parity tests/fixtures migration, zero unexpected baseline diffs.
</verification>

<success_criteria>
- The wire contract is complete: four fathom.*.v1 schemas, four fathom_*_v1 exports in sync, FATHOM-* codes everywhere, no old schema/export/code strings in product files.
- validate_dialect_profile is the single validation gate across all boundaries; flink rejected in Phase 9.
- Parity pins the neutral contract across native/js/wasm; baseline gate green with approved diffs only.
</success_criteria>

<output>Create `.planning/phases/09-dialect-boundary-and-neutral-naming/09-05-SUMMARY.md` when this plan executes.</output>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| JS/Wasm host -> binding exports | The export ABI is the public JS/linear-Wasm contract; half-migration breaks consumers silently. |
| schema strings -> consumers | A consumer keyed on the old doris.*.v1 namespace must never be served by the new namespace (clean cutover). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-16 | Tampering | Export half-migration (#export_name vs moon.pkg exports vs parity callsites) | high | mitigate | Same-commit rule for all three surfaces; grep gates + parity export_smoke_test per target (Pitfall 4/8). |
| T-09-17 | Tampering | Old-schema consumption confusion during cutover | medium | mitigate | Clean cutover with zero aliases (D-06); NAME-04 gate (09-07) rejects any doris.*.v1 in product files; no transitional dual-serving. |
| T-09-18 | Spoofing | Fabricated profile/exact_release metadata in fathom_dialect_v1 | low | mitigate | Values sourced only from DorisProfile::metadata; prohibition (provenance discipline). |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies (09-RESEARCH Package Legitimacy Audit N/A). |
</threat_model>

## Artifacts this phase produces

Symbols (binding/exports.mbt): fathom_dialect_v1(dialect) export, fathom_capabilities_v1() export (#export_name + moon.pkg exports synced).
Symbols (binding/schema.mbt): validate_dialect_profile(dialect, profile), SchemaError::UnknownDialect, FATHOM-SCHEMA-007 (UnknownDialect/ConflictingSelection), dialect_json (fathom.dialect.v1), capabilities_json (fathom.capabilities.v1), FATHOM-FORMAT-002..007 complete mapping.
Modified: binding/moon.pkg (exports lists = four fathom_*_v1), parity/schema_test.mbt, parity/export_smoke_test.mbt, parity/parity_test.mbt, parity/coordinates_test.mbt, parity/fixtures/target-matrix.json, parity/baseline_test.mbt, parity/__snapshot__ (approved update).
