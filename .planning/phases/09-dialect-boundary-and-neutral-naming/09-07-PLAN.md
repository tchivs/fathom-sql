---
phase: 09-dialect-boundary-and-neutral-naming
plan: 07
type: execute
wave: 7
depends_on: [09-06]
files_modified:
  - scripts/check_naming.py
  - .github/workflows/ci.yml
  - vscode/package.json
  - vscode/src/extension.ts
  - vscode/src/extension-contract.ts
  - vscode/README.md
  - web/package.json
  - web/index.html
  - web/src/monaco-adapter.ts
  - web/src/main.ts
  - web/src/main.test.ts
  - jetbrains/src/main/kotlin/fathom/jetbrains/doris/DorisLanguageServerFactory.kt
  - jetbrains/src/main/kotlin/fathom/jetbrains/doris/DorisSettings.kt
  - jetbrains/src/main/kotlin/fathom/jetbrains/doris/DorisSettingsConfigurable.kt
  - jetbrains/src/main/kotlin/fathom/jetbrains/doris/DorisNativeDownloader.kt
  - jetbrains/build.gradle.kts
  - jetbrains/settings.gradle.kts
  - jetbrains/src/main/resources/META-INF/plugin.xml
  - jetbrains/scripts/source-smoke.py
  - README.md
  - README.zh-CN.md
  - docs/API.md
  - docs/CONFIGURATION.md
  - docs/GETTING-STARTED.md
  - docs/TESTING.md
  - docs/DEVELOPMENT.md
  - docs/ARCHITECTURE.md
  - docs/zh-CN/API.md
  - docs/zh-CN/CONFIGURATION.md
  - docs/zh-CN/GETTING-STARTED.md
  - docs/zh-CN/TESTING.md
  - docs/zh-CN/DEVELOPMENT.md
  - docs/zh-CN/ARCHITECTURE.md
autonomous: false
requirements: [NAME-03, NAME-04, NAME-01]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 4
  confidence: low
must_haves:
  truths:
    - "VS Code exposes the neutral identity and dialect-aware settings: extension name fathom-sql-language-client, displayName 'Fathom SQL Language Client', language id 'sql' with .sql extensions, config keys fathom.dialect (enum doris/flink) + fathom.profile + fathom.serverPath (default fathom-lsp), command fathom.restartLanguageServer; no old config key and no default dialect remains (NAME-03, D-02/D-06)."
    - "Web/npm identity is neutral: package @fathom/sql-web-demo with a neutral description, neutral index.html titles with a dialect selector, and monaco-adapter calls fathom_parse_v1(raw, dialect, profile, mode) matching fathom.error.v1; the offline smoke builds and runs (NAME-03, research rows 65-68)."
    - "IntelliJ identity is neutral: package fathom.jetbrains.sql with Fathom* classes, FathomSettings/fathom.xml state, DEFAULT_EXECUTABLE fathom-lsp, initializationOptions {dialect, profile}, plugin id fathom.sql, artifact fathom-sql-intellij, downloader assets fathom-lsp-{platform} + fathom-lsp-manifest.json; jetbrains/scripts/source-smoke.py contracts updated (NAME-03, rows 69-76)."
    - "README and docs carry neutral product titles with dialect+profile tables; Doris appears only as a dialect/profile/corpus/provenance semantic identifier (NAME-03, D-05)."
    - "CI enforces neutral naming: scripts/check_naming.py implements the forbidden/allowlist inventory over product files with D-04 exemptions and runs as a naming-gate job in .github/workflows/ci.yml; the final repository-wide sweep passes with zero product-file hits (NAME-04, research section 7)."
    - "[probe NAME-03 unclassified] flagged-unverified: host-surface completeness is verified by the repository-wide naming sweep plus the web offline smoke and the jetbrains source-smoke; real VS Code/IntelliJ host verification of the dialect-selection UX and config precedence in a live editor is Phase 13 (research OQ6/A2/A3)."
    - "[probe NAME-01 completeness] flagged-unverified: no old alias remains anywhere in product files — the 09-04 module/import/binary names and this plan's host identities are jointly enforced by the same forbidden patterns in check_naming.py."
  artifacts:
    - path: "scripts/check_naming.py"
      provides: "stdlib-only forbidden/allowlist inventory gate (mirror of corpus/tools/check_keywords.py): FORBIDDEN regex list, ALLOWLIST_PATHS (D-04 exemptions), ALLOWLIST_PATTERNS (D-05 dialect semantics), product-file scan scope, problems loop with nonzero exit"
    - path: ".github/workflows/ci.yml"
      provides: "naming-gate job running python3 scripts/check_naming.py on every push/PR"
    - path: "vscode/package.json + src/extension.ts + src/extension-contract.ts"
      provides: "neutral identity, fathom.dialect/fathom.profile/fathom.serverPath config keys, SUPPORTED_DIALECTS, no normalizeProfile '4.x' fallback, initializationOptions {dialect, profile} matching the 09-06 LSP contract"
    - path: "web/package.json + index.html + src/*"
      provides: "@fathom/sql-web-demo, neutral titles with dialect selector, fathom_parse_v1(raw, dialect, profile, mode) + fathom.error.v1 matching (09-05 exports)"
    - path: "jetbrains/** (4 Kotlin sources, gradle files, plugin.xml, source-smoke.py)"
      provides: "Fathom* classes in fathom.jetbrains.sql, fathom.xml, fathom-lsp defaults, fathom.sql plugin id, fathom-sql-intellij artifact, updated contract checker"
    - path: "README.md + README.zh-CN.md + docs/** + docs/zh-CN/**"
      provides: "neutral product titles and dialect+profile tables; Doris only as dialect/profile/corpus/provenance"
  key_links:
    - from: "scripts/check_naming.py"
      to: ".github/workflows/ci.yml"
      via: "the naming-gate job runs the inventory on every push — the enforcement point for NAME-04 (research section 7.1)"
    - from: "vscode/package.json"
      to: "vscode/src/extension.ts"
      via: "getConfiguration('fathom') reads fathom.dialect/fathom.profile/fathom.serverPath and forwards initializationOptions {dialect, profile} to the 09-06 LSP contract"
    - from: "jetbrains/scripts/source-smoke.py"
      to: "jetbrains/build.gradle.kts + settings.gradle.kts + plugin.xml + the four Kotlin sources"
      via: "require() contract regexes assert the neutral names exactly as the gate does — the JetBrains-side naming gate (research row 74)"
    - from: "web/src/monaco-adapter.ts"
      to: "binding/exports.mbt"
      via: "module.fathom_parse_v1(utf8Bytes(source), dialect, profile, mode) — the 09-05 export with the A4 parameter order"
  prohibitions:
    - "No old host identity remains in product files: no old config keys, old language id, old JetBrains class/state names, old npm package name, old plugin id, or old asset names — zero occurrences (D-06, enforced by the gate)."
    - "No default dialect anywhere: VS Code, IntelliJ, and Web never default a dialect; a missing selection is an explicit error surfaced to the user (D-02) — normalizeProfile-style fallbacks are deleted, not adjusted."
    - "The gate is mode+file-scope dual-dimension (Pitfall 4): only the FORBIDDEN patterns fail; a mechanical global replacement of the word 'doris' would destroy required Dialect::Doris / DorisProfile / corpus provenance — the allowlist protects dialect semantics."
    - "Historical archives stay untouched (D-04): milestones/v1.0-*, milestones/v1.0-research/, and .planning/** are exempt from the gate and are not edited."
---

<objective>
**As a** consumer, **I want to** install, configure, and read about the SDK under one neutral product identity across VS Code, IntelliJ, Web/npm, CI/release assets, and documentation — **so that** no old Doris product name leaks into editor settings, package names, plugin identity, or docs, while Doris remains only as an explicit dialect/profile/corpus value, and CI mechanically rejects any future naming regression.

Purpose: Complete the NAME-03 host cutover (the D-06 host surface deferred from 09-04: config keys, package names, plugin id, state names, display titles) and deliver the NAME-04 naming inventory/allowlist gate with the final repository-wide sweep. The 09-06 LSP selection contract is the client-side target every host feeds; release-workflow asset names were already renamed in 09-04 (fathom-native-release.yml, fathom-lsp-*), and this plan's gate enforces them forward.
Output: neutral VS Code/Web/IntelliJ identities, neutral README/docs titles, scripts/check_naming.py + the ci.yml naming-gate job, and a clean repository-wide inventory sweep with zero product-file hits.
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-dialect-boundary-and-neutral-naming/09-CONTEXT.md
@.planning/phases/09-dialect-boundary-and-neutral-naming/09-RESEARCH.md
@.planning/phases/09-dialect-boundary-and-neutral-naming/09-PATTERNS.md
@.planning/phases/09-dialect-boundary-and-neutral-naming/09-06-SUMMARY.md

Research rows 58-76 (VS Code/Web/IntelliJ), 86 (docs), and section 7 (naming gate design) are this plan's checklist, plus assumptions A3 (config keys), A5 (historical release assets keep old names, new releases use fathom-lsp-*), and Open Questions 6 (language id — recommendation: single neutral 'sql' id with a user-configured mapping table) and 8 (release repository name tchivs/doris-sql-parser-sdk kept unchanged this phase — the gate allowlists it as a repository identifier). The pattern analog for scripts/check_naming.py is corpus/tools/check_keywords.py (stdlib problems loop, nonzero exit, ok line); the file-scope/exemption lists come from research section 7.2 and D-04. The 09-06 LSP contract defines the client-side transport the hosts must emit: initializationOptions {dialect, profile} and didChangeConfiguration fathom settings — 09-07 wires the host config keys to exactly those fields.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the D-06 host naming cutover surface (one-way door)</name>
  <decision>Host naming cutover: configuration keys, language id, package names, plugin identity, JetBrains state names, and the no-default-dialect rule — the last one-way D-06 surface (the module/CLI/binary half was confirmed in 09-04).</decision>
  <context>D-06 locks clean cutover with no aliases for the whole product layer, and 09-04 deferred the host surface to this plan: VS Code config keys (A3 recommends fathom.dialect / fathom.profile / fathom.serverPath with default fathom-lsp), language id (OQ6 recommends one neutral 'sql' id — languageId participates in dialect selection only through a user-configured mapping per D-02), package names (fathom-sql-language-client, @fathom/sql-web-demo, fathom-sql-intellij), JetBrains plugin id (fathom.sql) and state (FathomSettings/fathom.xml) and package (fathom.jetbrains.sql), and the no-default-dialect rule (D-02 — the current normalizeProfile '4.x' fallback and DEFAULT_PROFILE = "4.x" are deleted, not adjusted). The 09-06 LSP contract fixes the transport these keys feed (initializationOptions {dialect, profile}); the names here are what users and scripts type, so they are one-way after release.</decision>
  <options>
    <option id="option-a">
      <name>Full host cutover per D-06 + research recommendations (recommended — locked)</name>
      <pros>Matches D-06 verbatim and research A3/OQ6; one coherent naming scheme across hosts; the NAME-04 gate passes on the finished state.</pros>
      <cons>All host files must change in one plan; the JetBrains package/plugin-id rename touches the gradle build and source-smoke contracts together.</cons>
    </option>
    <option id="option-b">
      <name>Keep the old 'doris' language id, rename only package/config surfaces</name>
      <pros>Smaller diff in vscode/web language registrations.</pros>
      <cons>Violates NAME-03 (language id is a public product identity) and forces implicit languageId semantics back into the selection flow — contradicts D-02.</cons>
    </option>
    <option id="option-c">
      <name>Keep a default dialect in hosts (e.g., default 'doris')</name>
      <pros>Simpler first-open UX.</pros>
      <cons>Directly violates D-02 (no implicit fallback): every first open would silently pick a dialect, making diagnostics untrustworthy.</cons>
    </option>
  </options>
  <resume-signal>Select option-a (or describe the exact names/keys to use); the host cutover then executes on the confirmed surface.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: VS Code + Web host cutover — neutral identity, fathom.* config keys, explicit dialect selection</name>
  <files>vscode/package.json, vscode/src/extension.ts, vscode/src/extension-contract.ts, vscode/README.md, web/package.json, web/index.html, web/src/monaco-adapter.ts, web/src/main.ts, web/src/main.test.ts</files>
  <read_first>
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-CONTEXT.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-RESEARCH.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-PATTERNS.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-06-SUMMARY.md
    @/opt/source/Fathom/vscode/package.json
    @/opt/source/Fathom/vscode/src/extension.ts
    @/opt/source/Fathom/vscode/src/extension-contract.ts
    @/opt/source/Fathom/web/package.json
    @/opt/source/Fathom/web/index.html
    @/opt/source/Fathom/web/src/monaco-adapter.ts
    @/opt/source/Fathom/web/src/main.ts
    @/opt/source/Fathom/web/src/main.test.ts
  </read_first>
  <action>Execute the VS Code and Web host cutover per the Task 1 confirmation (research rows 58-68).

vscode/package.json (rows 58-62): name fathom-sql-language-client, displayName "Fathom SQL Language Client", description neutral; activationEvents onLanguage:sql and onCommand:fathom.restartLanguageServer (row 59); the languages section becomes id "sql" with neutral aliases and extensions [".sql"] (row 60); the configuration section title "Fathom SQL" with properties fathom.dialect (type string, enum ["doris","flink"], NO default — D-02), fathom.profile (type string, enum ["2.1","3.x","4.x"], NO default), and fathom.serverPath (type string, default "fathom-lsp") (row 61); the command becomes fathom.restartLanguageServer with title "Fathom SQL: Restart Language Server" (row 62).

vscode/src/extension.ts (row 63): getConfiguration('fathom') reads fathom.dialect/fathom.profile/fathom.serverPath; the LanguageClient is named with the neutral server title; documentSelector [{ language: 'sql' }]; initializationOptions becomes { dialect, profile } exactly matching the 09-06 LSP contract; the restart command id becomes fathom.restartLanguageServer; when fathom.dialect is unset, the extension surfaces an explicit configuration error to the user (window.showErrorMessage) and does not start the client with a guessed dialect (D-02). vscode/src/extension-contract.ts (row 64): add SUPPORTED_DIALECTS = ['doris','flink']; delete the normalizeProfile '4.x' fallback so a missing dialect is an error, never a default; the profile validator keeps SUPPORTED_PROFILES. vscode/README.md: neutral title and a dialect+profile table with Doris listed as a dialect value.

web/package.json (row 65): name @fathom/sql-web-demo with a neutral description; the offline-smoke build script stays. web/index.html (row 66): neutral <title> and <h1> with a dialect selector (doris/flink) plus a profile selector, neither preselected (D-02 — the demo surfaces the missing-selection error until the user chooses). web/src/monaco-adapter.ts (row 67): DIALECTS and PROFILES constants; the schema-version check becomes 'fathom.error.v1'; calls become module.fathom_parse_v1(utf8Bytes(source), dialect, profile, 'editor') and module.fathom_format_v1(...) with the A4 parameter order (09-05 exports); the 'Choose a supported Doris profile.' copy becomes neutral missing-selection copy. web/src/main.ts (row 68): monaco.languages.register({ id: 'sql' }); the dialect/profile dual selectors with no default and the structured error display wired. web/src/main.test.ts: mock calls updated to fathom_parse_v1(raw, dialect, profile, mode) and the neutral error assertions.

Verify: cd web && npm run build (offline smoke) passes; cd ../vscode && npm run build (tsc) passes; a grep over vscode/ and web/ product files (excluding node_modules and build output) finds zero occurrences of the old extension name, old package name, old config keys, old command, and old language id. Commit as feat(09-07): VS Code and Web host cutover.</action>
  <acceptance_criteria>
    - vscode/package.json name is fathom-sql-language-client with displayName "Fathom SQL Language Client"; language id is sql; config keys are fathom.dialect/fathom.profile/fathom.serverPath (serverPath default fathom-lsp, dialect/profile with no default); command is fathom.restartLanguageServer.
    - extension.ts sends initializationOptions {dialect, profile} exactly as the 09-06 contract; a missing fathom.dialect surfaces an explicit error and never starts with a guessed dialect; extension-contract.ts exports SUPPORTED_DIALECTS and has no '4.x' fallback.
    - web/package.json name is @fathom/sql-web-demo; monaco-adapter calls fathom_parse_v1(raw, dialect, profile, mode) and matches fathom.error.v1; index.html has neutral titles and an unpreselected dialect selector.
    - cd web && npm run build and cd ../vscode && npm run build both pass; the grep gate over vscode/ and web/ product files finds zero old-identity occurrences.
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm run build && cd ../vscode && npm run build</automated>
  </verify>
  <done>The VS Code and Web hosts carry the neutral identity with fathom.* config keys, explicit dialect selection, and no defaults — builds green.</done>
</task>

<task type="auto">
  <name>Task 3: JetBrains host cutover — Fathom* classes, fathom.xml state, fathom.sql plugin id, fathom-sql-intellij artifact, updated source-smoke contracts</name>
  <files>jetbrains/src/main/kotlin/fathom/jetbrains/doris/DorisLanguageServerFactory.kt, jetbrains/src/main/kotlin/fathom/jetbrains/doris/DorisSettings.kt, jetbrains/src/main/kotlin/fathom/jetbrains/doris/DorisSettingsConfigurable.kt, jetbrains/src/main/kotlin/fathom/jetbrains/doris/DorisNativeDownloader.kt, jetbrains/build.gradle.kts, jetbrains/settings.gradle.kts, jetbrains/src/main/resources/META-INF/plugin.xml, jetbrains/scripts/source-smoke.py</files>
  <read_first>
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-CONTEXT.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-RESEARCH.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-PATTERNS.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-06-SUMMARY.md
    @/opt/source/Fathom/jetbrains/src/main/kotlin/fathom/jetbrains/doris/DorisLanguageServerFactory.kt
    @/opt/source/Fathom/jetbrains/src/main/kotlin/fathom/jetbrains/doris/DorisSettings.kt
    @/opt/source/Fathom/jetbrains/src/main/kotlin/fathom/jetbrains/doris/DorisSettingsConfigurable.kt
    @/opt/source/Fathom/jetbrains/src/main/kotlin/fathom/jetbrains/doris/DorisNativeDownloader.kt
    @/opt/source/Fathom/jetbrains/build.gradle.kts
    @/opt/source/Fathom/jetbrains/settings.gradle.kts
    @/opt/source/Fathom/jetbrains/src/main/resources/META-INF/plugin.xml
    @/opt/source/Fathom/jetbrains/scripts/source-smoke.py
  </read_first>
  <action>Execute the JetBrains host cutover per the Task 1 confirmation (research rows 69-76, 78).

Package/class renames: git mv the kotlin directory jetbrains/src/main/kotlin/fathom/jetbrains/doris to jetbrains/src/main/kotlin/fathom/jetbrains/sql (row 69); rename the four classes with their files — FathomLanguageServerFactory.kt, FathomSettings.kt, FathomSettingsConfigurable.kt, FathomNativeDownloader.kt — with package fathom.jetbrains.sql in every file (the two Kotlin tests under src/test/kotlin follow the same package/class rename).

FathomLanguageServerFactory.kt: initializationOptions becomes mapOf("dialect" to dialect, "profile" to profile) (row 70) matching the 09-06 LSP contract; the server id registered in plugin.xml becomes fathom-sql (replacing the old server id). FathomSettings.kt: @State(name = "FathomSettings", storages = [Storage("fathom.xml")]); DEFAULT_EXECUTABLE = "fathom-lsp"; the old DEFAULT_PROFILE constant is deleted (D-02 — no default profile); the settings model gains the dialect dimension alongside ALLOWED_PROFILES (row 71). FathomSettingsConfigurable.kt: labels become "fathom-lsp executable:" with a dialect selector; getDisplayName() = "Fathom SQL" (row 72). FathomNativeDownloader.kt: FathomNativePlatform with assetName fathom-lsp-{platform}; MANIFEST_ASSET_NAME = "fathom-lsp-manifest.json"; cache directory Fathom/fathom-sql; DEFAULT_REPOSITORY stays tchivs/doris-sql-parser-sdk per research OQ8 (a repository identifier, allowlisted by the gate) (row 73).

jetbrains/settings.gradle.kts: rootProject.name = "fathom-sql-intellij" (row 76); jetbrains/build.gradle.kts: neutral changeNotes text (row 75, group fathom.jetbrains unchanged); jetbrains/src/main/resources/META-INF/plugin.xml: plugin id fathom.sql, server id fathom-sql, neutral display name.

jetbrains/scripts/source-smoke.py (row 74): every require() contract updated to the neutral names — plugin id, server id, FathomSettings/FathomSettingsConfigurable/FathomNativeDownloader source paths, DEFAULT_EXECUTABLE = "fathom-lsp", fathom-lsp-manifest.json — this script is the JetBrains-side naming gate and must match the finished state.

Verify: python3 jetbrains/scripts/source-smoke.py exits 0 (offline stdlib contract checker); a grep over jetbrains/ product files (excluding build/ and .gradle/) finds zero occurrences of the old package path, old class names, old state name, old executable default, and old asset/manifest names. The gradle build itself is CI-verified (jetbrains-plugin.yml uses temurin 21; the local JDK 1.8 cannot build the plugin — do not attempt a local gradle build, the source-smoke + grep gates are the local proof). Commit as feat(09-07): JetBrains host cutover.</action>
  <acceptance_criteria>
    - The kotlin package is fathom.jetbrains.sql with Fathom* class names in the four sources and the two tests; initializationOptions is mapOf("dialect" to dialect, "profile" to profile).
    - FathomSettings uses @State(name = "FathomSettings", storages = [Storage("fathom.xml")]) with DEFAULT_EXECUTABLE = "fathom-lsp" and no default profile/dialect; the configurable UI labels and getDisplayName() are neutral.
    - FathomNativeDownloader uses fathom-lsp-{platform} assets and fathom-lsp-manifest.json; plugin.xml declares plugin id fathom.sql and server id fathom-sql; settings.gradle.kts rootProject.name is fathom-sql-intellij.
    - python3 jetbrains/scripts/source-smoke.py exits 0; the grep gate over jetbrains/ product files finds zero old-identity occurrences.
  </acceptance_criteria>
  <verify>
    <automated>python3 jetbrains/scripts/source-smoke.py</automated>
  </verify>
  <done>The JetBrains host carries the neutral identity end to end (package, classes, state, plugin id, artifact, assets, contract checker) with explicit dialect selection and no defaults.</done>
</task>

<task type="auto">
  <name>Task 4: README/docs neutral titles + scripts/check_naming.py + ci.yml naming-gate job + final repository-wide inventory sweep</name>
  <files>scripts/check_naming.py, .github/workflows/ci.yml, README.md, README.zh-CN.md, docs/API.md, docs/CONFIGURATION.md, docs/GETTING-STARTED.md, docs/TESTING.md, docs/DEVELOPMENT.md, docs/ARCHITECTURE.md, docs/zh-CN/API.md, docs/zh-CN/CONFIGURATION.md, docs/zh-CN/GETTING-STARTED.md, docs/zh-CN/TESTING.md, docs/zh-CN/DEVELOPMENT.md, docs/zh-CN/ARCHITECTURE.md</files>
  <read_first>
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-CONTEXT.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-RESEARCH.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/09-PATTERNS.md
    @/opt/source/Fathom/corpus/tools/check_keywords.py
    @/opt/source/Fathom/.github/workflows/ci.yml
    @/opt/source/Fathom/README.md
    @/opt/source/Fathom/docs/CONFIGURATION.md
  </read_first>
  <action>Deliver the naming gate (NAME-04), neutral docs titles, and the final repository-wide sweep.

README.md, README.zh-CN.md, and docs/*.md + docs/zh-CN/*.md (rows 7, 86): product titles/headings become neutral ("Fathom SQL Parser SDK", "Fathom SQL Language Server"); the module/CLI references already fixed in 09-04 stay; Doris-profile tables become dialect+profile tables where Doris appears only as a dialect/profile value and Apache Doris only as the engine/provenance name (D-05); any remaining old product phrasing in prose is removed. The import examples keep the 09-04 fathom/sql/api form.

scripts/check_naming.py (new; research section 7 + Common Operation 5; analog corpus/tools/check_keywords.py): Python stdlib only. FORBIDDEN regex list covering the product-level remnants from section 7.3 — the CLI/module names, the LSP binary names, the binding export symbols, the wire schema strings, the DORIS- code prefix, the module import prefix, the old host package names, the old plugin id, the old language id and activation/server-id forms, the old config keys, and the old JetBrains state/class names — plus the section 7.3 display-title patterns. ALLOWLIST_PATHS (D-04 exemptions and build output, section 7.2): corpus/, .planning/, milestones/v1.0-*, milestones/v1.0-research/, jetbrains/build/, .gradle/, _build/, node_modules/, package-lock.json. ALLOWLIST_PATTERNS (D-05/DIALECT-01 semantics, section 7.4): Dialect::Doris, DorisProfile, DorisFeature, ValidatedProfileContext, ProfileMetadata, doris.apache.org, --dialect doris, the "dialect":"doris" metadata value, the 2.1/3.x/4.x profile values, corpus/doris-2.1|3.x|4.x path segments, and the tchivs/doris-sql-parser-sdk repository identifier. Scan scope (section 7.2): product files matching **/*.mbt, **/*.ts, **/*.mjs, **/*.js, **/*.kt, **/*.kts, **/*.gradle*, **/*.json, **/*.yml, **/*.yaml, **/*.mod, **/*.pkg, **/*.py, README*.md, docs/**, web/**, vscode/**, jetbrains/** — minus the ALLOWLIST_PATHS. main() mirrors check_keywords.py: walk the product files, line-scan against FORBIDDEN, skip allowlisted paths and lines matching ALLOWLIST_PATTERNS, collect problems, print "error: <file>:<line>: <pattern>" per hit, exit 1 on any problem, print "ok: N product files scanned, 0 naming violations" on success.

.github/workflows/ci.yml: add the naming-gate job per research section 7.1 — actions/checkout@v7, actions/setup-python@v6, run: python3 scripts/check_naming.py (mirroring the existing corpus job shape).

Final repository-wide naming inventory sweep (the last task of this plan): run python3 scripts/check_naming.py over the whole repo and fix EVERY remaining product-file hit the sweep reports — including stragglers outside the explicit file lists above (for example the vscode host-verify script or jetbrains README if the sweep flags them) — until the gate exits 0 with zero product-file hits. Commit as feat(09-07): neutral naming gate and docs; then chore(09-07): naming sweep clean.</action>
  <acceptance_criteria>
    - README and docs carry neutral product titles with dialect+profile tables; the docs grep (same FORBIDDEN patterns, excluding the allowlisted paths) finds zero product-name remnants in docs/.
    - python3 scripts/check_naming.py exits 0 on the finished repository and prints the ok line; python3 -m py_compile scripts/check_naming.py passes.
    - The script fails (exit 1) on a controlled probe: a temporary product file containing a forbidden pattern in a non-exempt path is reported; the probe is removed after verification.
    - .github/workflows/ci.yml contains the naming-gate job running python3 scripts/check_naming.py with no --update-like bypass.
    - The final repository-wide sweep reports zero product-file hits (only exempt paths may contain the old names, per D-04).
    - The plan's host tasks remain green: cd web && npm run build, cd ../vscode && npm run build, python3 jetbrains/scripts/source-smoke.py, and moon test --target native --package test --package parity --package lsp all pass after the sweep.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/check_naming.py && python3 -m py_compile scripts/check_naming.py</automated>
  </verify>
  <done>The repository carries neutral naming across docs and all hosts, CI enforces it mechanically, and the final sweep is clean with zero product-file hits.</done>
</task>

</tasks>

## Source Audit Coverage

| SOURCE | ID | Feature or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| REQ | NAME-03 | VS Code/IntelliJ/Web/npm/CI/config keys/LSP identity/README/docs neutral | 09-07 | COVERED | Tasks 2-4 (LSP server identity half landed in 09-06); rows 58-76, 86. |
| REQ | NAME-04 | CI naming inventory/allowlist gate; allowlist = dialect semantics only | 09-07 | COVERED | Task 4 (check_naming.py + ci.yml job + final sweep); research section 7. |
| REQ | NAME-01 | no old public aliases remain | 09-07 | COVERED (enforcement) | Gate FORBIDDEN patterns jointly enforce 09-04 module/binary names + this plan's host identities. |
| RESEARCH | rows 58-68 | VS Code/Web host fields | 09-07 | COVERED | Task 2. |
| RESEARCH | rows 69-76, 78 | JetBrains package/classes/state/assets | 09-07 | COVERED | Task 3. |
| RESEARCH | row 86 + rows 7 | docs titles/prose | 09-07 | COVERED | Task 4. |
| RESEARCH | section 7.1-7.4 | gate design: forbidden/allowlist/scopes/exemptions | 09-07 | COVERED | Task 4. |
| RESEARCH | Pitfall 4 / 8 | half-migration; mode+file-scope dual dimension | 09-07 | COVERED | Prohibitions + gate design. |
| RESEARCH | A3 / A5 / OQ6 / OQ8 | config keys; historical assets; language id; repo name | 09-07 | COVERED | Task 1 checkpoint + Tasks 2-4. |
| CONTEXT | D-02 | no default dialect in hosts | 09-07 | COVERED | Tasks 2-3 delete fallbacks; prohibitions. |
| CONTEXT | D-04 | exemptions for historical archives | 09-07 | COVERED | ALLOWLIST_PATHS + prohibition. |
| CONTEXT | D-05 | Doris dialect type names preserved | 09-07 | COVERED | ALLOWLIST_PATTERNS. |
| CONTEXT | D-06 | clean cutover, no aliases, one-way | 09-07 | COVERED (host half) | Task 1 checkpoint + Tasks 2-4. |
| PROBE | NAME-03 unclassified | host-surface completeness | 09-07 | FLAGGED | truth: sweep + smokes are the local proof; live-host UX is Phase 13. |
| PROBE | NAME-01 completeness | no aliases repo-wide | 09-07 | FLAGGED | truth: gate patterns cover both waves. |

<verification>
Run the naming gate after each task (python3 scripts/check_naming.py — it is the enforcement point), plus the host builds/smokes: cd web && npm run build, cd ../vscode && npm run build, python3 jetbrains/scripts/source-smoke.py, and the full moon native suite. Inspect: vscode package identity/config keys/command, web package/adapter/main, JetBrains package/classes/state/plugin/artifacts, source-smoke contracts, README/docs titles, ci.yml naming-gate job, final sweep output. Do not touch token/lexer/parser/api/binding core (09-02/09-03/09-05) or the LSP selection logic (09-06).
</verification>

<success_criteria>
- VS Code, Web/npm, IntelliJ, CI/release assets, README, and docs all carry the neutral fathom identity; Doris remains only as a dialect/profile/corpus/provenance value.
- No host defaults a dialect; missing selection is an explicit error (D-02).
- scripts/check_naming.py + the ci.yml naming-gate job enforce NAME-04 on every push, and the final repository-wide sweep is clean.
</success_criteria>

<output>Create `.planning/phases/09-dialect-boundary-and-neutral-naming/09-07-SUMMARY.md` when this plan executes.</output>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| host config keys -> LSP selection | The 09-06 contract consumes what these keys emit; a key named with the old identity or a defaulted dialect breaks the explicit-selection guarantee. |
| release assets -> consumers | Asset/manifest names must match the neutral binary identity or downloaders/scripts break. |
| repository content -> naming gate | The gate's pattern+scope matrix decides what is a violation vs allowed dialect semantics; a wrong matrix either misses remnants or deletes required provenance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-24 | Tampering | Host half-migration (one host renamed, another stale) | high | mitigate | Same-plan commit rule; check_naming.py scans all host trees; source-smoke + web/vscode builds verify per host (Pitfall 4). |
| T-09-25 | Tampering | Gate bypass via scan-scope gaps (unlisted file types/paths) | medium | mitigate | Explicit product-file scope list (section 7.2) + the final repository-wide sweep runs the gate across everything; probe test proves exit 1 on a violation. |
| T-09-26 | Tampering | False-positive gate deleting Doris dialect semantics (Dialect::Doris, corpus provenance) | medium | mitigate | Mode+file-scope dual dimension: FORBIDDEN patterns are precise (never bare 'doris'), ALLOWLIST_PATTERNS protect D-05/D-04 surfaces (Pitfall 4). |
| T-09-27 | Tampering | Default-dialect reintroduction in hosts (first-open guess) | high | mitigate | D-02 prohibitions: normalizeProfile/'4.x' fallbacks deleted; gate forbids the old default shapes; host tests assert no default. |
| T-09-28 | Tampering | Release asset mismatch (workflow names vs binary identity) | medium | mitigate | fathom-native-release.yml + fathom-lsp-* landed in 09-04; the gate scans workflows so any reintroduction fails CI. |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies (09-RESEARCH Package Legitimacy Audit N/A); check_naming.py is Python stdlib. |
</threat_model>

## Artifacts this phase produces

New files: scripts/check_naming.py (FORBIDDEN regex list, ALLOWLIST_PATHS, ALLOWLIST_PATTERNS, product-file scan scope, main() problems loop with ok/error lines).
Modified: .github/workflows/ci.yml (naming-gate job), vscode/package.json (fathom-sql-language-client / Fathom SQL Language Client / id sql / fathom.dialect + fathom.profile + fathom.serverPath / fathom.restartLanguageServer), vscode/src/extension.ts (getConfiguration('fathom'), initializationOptions {dialect, profile}, explicit missing-selection error), vscode/src/extension-contract.ts (SUPPORTED_DIALECTS, fallback removed), web/package.json (@fathom/sql-web-demo), web/index.html (neutral titles + dialect selector), web/src/monaco-adapter.ts (DIALECTS/PROFILES, fathom_parse_v1(raw, dialect, profile, mode), fathom.error.v1), web/src/main.ts + main.test.ts (id sql, dual selectors), jetbrains four Kotlin sources (fathom.jetbrains.sql, Fathom* classes, fathom.xml, DEFAULT_EXECUTABLE fathom-lsp, fathom-lsp-{platform} + fathom-lsp-manifest.json, cache Fathom/fathom-sql), jetbrains/settings.gradle.kts (fathom-sql-intellij), jetbrains/build.gradle.kts (neutral changeNotes), jetbrains/plugin.xml (plugin id fathom.sql, server id fathom-sql), jetbrains/scripts/source-smoke.py (neutral require() contracts), README/README.zh-CN/docs/**/docs/zh-CN/** (neutral titles + dialect+profile tables).
Renamed package identities: fathom-sql-language-client, @fathom/sql-web-demo, fathom-sql-intellij, fathom.sql plugin id, fathom.xml state, fathom-lsp executable default.
Gate symbols: FORBIDDEN (CLI/module/binary/export/schema/code/host-key/plugin/language-id patterns), ALLOWLIST_PATHS (corpus/, .planning/, milestones/v1.0-*, milestones/v1.0-research/, build outputs), ALLOWLIST_PATTERNS (Dialect::Doris, DorisProfile, DorisFeature, doris.apache.org, --dialect doris, "dialect":"doris", 2.1/3.x/4.x, corpus dirs, repository identifier), exit 0/1 semantics.