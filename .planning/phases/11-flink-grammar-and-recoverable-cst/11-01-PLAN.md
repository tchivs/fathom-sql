---
phase: 11-flink-grammar-and-recoverable-cst
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - parser/parser.mbt
  - parser/flink_grammar.mbt
  - syntax/syntax.mbt
  - api/api.mbt
  - dialect/flink.mbt
  - dialect/classification.mbt
  - parity/flink_lexical_test.mbt
  - parity/flink_grammar_test.mbt
  - parity/fixtures/flink-grammar/manifest.tsv
  - parity/__snapshot__/flink-grammar.*.json
  - parity/__snapshot__/flink-lexical.*.json
  - scripts/extract_flink_grammar.py
  - .planning/phases/11-flink-grammar-and-recoverable-cst/approved-changes.md
autonomous: false
requirements: [FLINK-02, CST-01]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 4
  confidence: low
must_haves:
  truths:
    - "The Flink grammar scope is anchored by D-01: it covers the full FLINK-02..06 enumeration — core queries (SELECT/CTE/JOIN/aggregation/set operations/expressions/types), DML (INSERT/UPDATE/DELETE), EXPLAIN/SHOW/DESCRIBE/ANALYZE, Catalog/DATABASE/TABLE/VIEW/FUNCTION DDL, the CREATE TABLE complex forms (physical/metadata/computed-column/WATERMARK/PRIMARY KEY NOT ENFORCED/PARTITIONED BY/distribution/WITH options/LIKE/AS), Window TVF (TUMBLE/HOP/CUMULATE/SESSION with TABLE/DESCRIPTOR/interval literal/named args/window output columns), and MATCH_RECOGNIZE (PATTERN/DEFINE/MEASURES/skip policy/pattern variables/quantifiers) — with the Window TVF and MATCH_RECOGNIZE supported/known-limitation subsets frozen from the pinned release grammar (syntax-level only, no planner/execution-equivalence claim, D-01)."
    - "A consumer can parse a Flink SELECT with CTE+JOIN+aggregation via `fathom-sql parse --dialect flink --profile flink-2.3.0` into a real lossless CST (valid=true, no FATHOM-PARSE-008) whose fathom.parse.v1 envelope carries dialect=flink and the profile metadata; the identical SQL under doris-4.x parses byte-identically to the frozen baseline (FLINK-02 core-query slice, D-06, CST-01)."
    - "Flink SELECT accepts the Flink expression surface (`||` CONCAT via the Flink precedence arm, INTERVAL literal, profile-gated X'/U&'/N'/E' literals) while Doris expression acceptance stays byte-identical — precedence(context, cursor) only extends the Flink arm, never the shared table (DIALECT-02, Pitfall 7)."
    - "Flink-only and Doris-only constructs surfacing inside a Flink SELECT (INTO OUTFILE, QUALIFY, TABLET, SAMPLE/TABLESAMPLE/REPEATABLE) are rejected with the construct-level dialect-gate diagnostic FATHOM-PARSE-009 (locked by the D-04 decision task) — never silently accepted (DIALECT-03, D-04)."
    - "MATCH_RECOGNIZE and the other missing Calcite-base reserved tokens classify as Reserved under every Flink profile (identifier only when backtick-quoted) — classification_of(flink, \"MATCH_RECOGNIZE\") returns Reserved per the pinned Calcite token manager (Pitfall 9, DIALECT-02)."
    - "FATHOM-PARSE-008 is retired: no valid Flink statement produces it; the code stays vacant and is never reused; genuinely-unsupported whole statements route through FATHOM-PARSE-007; the Phase 10 flink-lexical tests and snapshots that asserted FATHOM-PARSE-008 are updated to the real-grammar expectations with a registered approved-changes entry (D-06)."
    - "Strict and editor mode both produce a bounded, source-backed recoverable CST for Flink input — error/missing/skipped nodes under the shared recovery budget (consume_recovery_step / depth_allowed) — and print_lossless(parse(x)) == x holds for every positive and recovery fixture (CST-01, D-03)."
    - "Doris baseline zero-drift: moon test --package parity (no --update) passes and git diff --name-only -- parity/__snapshot__ shows no doris-named snapshot changed — only the new flink-grammar group and the registered flink-lexical re-generation appear (D-05/D-08, Pitfall 1)."
    - "[probe FLINK-02 adjacency] flagged-unverified: exactly-equal / just-touching constructs (CTE alias vs AS boundary, JOIN ON adjacency, byte-adjacent span end/start) must neither merge nor drop spans — the span-contiguity invariant (node_invariants_hold) is the mechanism and boundary fixtures pin the edges."
    - "[probe FLINK-02 empty] flagged-unverified: empty/single-token Flink input (bare SELECT with no projection, WITH with no body, whitespace-only segment) yields a bounded error/missing node with a localized diagnostic — never an infinite loop, never a fabricated subtree; incomplete fixtures cover these."
    - "[probe FLINK-02 ordering] flagged-unverified: for equal-precedence operators and set operations the parse order is left-associative and stable — the shared Pratt engine binding rules are frozen by expression fixtures, never re-ordered per parse."
    - "[probe CST-01 boundary] flagged-unverified: recovery at segment/statement boundaries (EOF, `;`, next statement) stops exactly at the boundary token without consuming it into the previous statement's skipped material — finish_statement plus recover_to_clause_boundary behavior pinned by recovery fixtures."
    - "[probe CST-01 precision] flagged-unverified: byte spans are exact half-open [start, end) offsets; fixture assertions freeze the exact byte ranges of error/missing/skipped nodes so no off-by-one can silently appear."
  artifacts:
    - path: "parser/parser.mbt"
      provides: "parse_flink_segment real keyword dispatch (SELECT/WITH arms), precedence(context, cursor) Flink arm, add_dialect_gate_diagnostic (FATHOM-PARSE-009), FATHOM-PARSE-008 path removal, shared query gates under dialect.is Flink"
    - path: "parser/flink_grammar.mbt"
      provides: "parse_flink_query + parse_flink_select_core (Flink-safe SELECT/CTE/JOIN/aggregation), first Flink productions in the new file"
    - path: "syntax/syntax.mbt + api/api.mbt"
      provides: "D-02-locked SyntaxKind variants appended to the enum end + kind_id wire strings ()"
    - path: "dialect/flink.mbt + dialect/classification.mbt"
      provides: "MATCH_RECOGNIZE + missing Calcite-base reserved rows (Reserved, per-release source line), reserved-word tests"
    - path: "parity/flink_grammar_test.mbt + parity/fixtures/flink-grammar/manifest.tsv + parity/__snapshot__/flink-grammar.*.json"
      provides: "flink-grammar snapshot namespace (fixture struct + snapshot harness + first SELECT positive/recovery fixtures), provenance manifest rows, generated snapshots"
    - path: "scripts/extract_flink_grammar.py"
      provides: "Python-stdlib extractor/validator (check_keywords.py shape) reading the pinned release grammars for production line refs and fixture/manifest validation"
    - path: ".planning/phases/11-flink-grammar-and-recoverable-cst/approved-changes.md"
      provides: "D-08 register: FATHOM-PARSE-008 retirement (vacant, never reused), FATHOM-PARSE-009 minting, flink-grammar namespace, flink-lexical snapshot re-generation"
  key_links:
    - from: "parser/parser.mbt parse_flink_segment dispatch"
      to: "parser/flink_grammar.mbt parse_flink_query"
      via: "keyword-first dispatch arm (SELECT/WITH through with_prefix_verb) calling the Flink-safe query production — the D-06 replacement of the 008 body"
    - from: "parser/parser.mbt precedence(context, cursor)"
      to: "the two shared Pratt call sites (wildcard_has_invalid_continuation and the parse_expression_context binary loop)"
      via: "context parameter threaded so the Doris arm returns the existing table byte-identically and the Flink arm adds || / =>"
    - from: "dialect/flink.mbt MATCH_RECOGNIZE row"
      to: "dialect/classification.mbt classification_of"
      via: "flink_classification_rows lookup so the reserved word is never accepted as an unquoted identifier"
    - from: "parity/flink_grammar_test.mbt + parity/flink_lexical_test.mbt"
      to: "moon test --package parity"
      via: "the D-08 byte gate (no --update in CI); Doris 213 snapshots stay byte-identical; flink-lexical re-generation is registered"
    - from: "parser/parser.mbt add_dialect_gate_diagnostic"
      to: "the fathom.parse.v1 diagnostics array"
      via: "add_dialect_gate_diagnostic emitting FATHOM-PARSE-009 with dialect carried in envelope metadata (D-10: dialect never in the code prefix)"
  prohibitions:
    - "Flink-only syntax (WATERMARK, computed/metadata columns, PRIMARY KEY NOT ENFORCED, DISTRIBUTED ... INTO n BUCKETS, PARTITIONED BY, WITH connector options, LIKE/AS, INSERT OVERWRITE/UPSERT/ON CONFLICT, Window TVF, MATCH_RECOGNIZE, CREATE CATALOG/DATABASE/FUNCTION) MUST NOT be accepted as valid in Doris mode, and Doris-only syntax (DUPLICATE/UNIQUE/AGGREGATE KEY, ENGINE=, AUTO_INCREMENT, ROLLUP, PROPERTIES, TABLET, SAMPLE/TABLESAMPLE, INTO OUTFILE, WITH LABEL, LATERAL VIEW, DISTRIBUTED BY ... BUCKETS n|AUTO, AUTO PARTITION BY, QUALIFY) MUST NOT be accepted as valid in Flink mode — the bidirectional negative gate is a product contract, not a suggestion (DIALECT-03, D-04)."
    - "The Flink parser MUST NOT claim planner, catalog, connector, type-inference, or execution equivalence for Window TVF or MATCH_RECOGNIZE — syntax-level CST only (FLINK-05/06); MATCH_RECOGNIZE pattern-variable scope/type checks must not reject syntactically valid input."
    - "Flink release provenance (grammar production line numbers, fixture sources, keyword classification rows) MUST NOT be fabricated or taken from moving docs/folklore — every claim traces to the pinned release archives with a line reference (D-05, Pitfall 3/5)."
    - "Editor-mode recoverable CST (error/missing/skipped nodes) MUST NOT be reported as strict-mode validity — valid=true only when strict acceptance holds; recovery never silently upgrades to acceptance (D-03, CST-01)."
    - "The lossless CST MUST NOT silently alter or drop source bytes, trivia, or spans — print_lossless(parse(x)) == x is byte-identical for every positive and recovery fixture (CST-01)."
---

<objective>
**As a** consumer, **I want to** parse an everyday Flink SELECT with CTE+JOIN+aggregation through the real Flink statement entry into a lossless, recoverable, bounded CST (not the FATHOM-PARSE-008 not-implemented rejection), **so that** the FLINK-02 core-query slice lands with strict/editor dual-mode recovery and the frozen Doris baseline stays byte-identical.

Purpose: Deliver the Phase 11 tracer slice — the thinnest end-to-end path that proves the whole Flink grammar stack works: `fathom-sql parse --dialect flink --profile flink-2.3.0` on a CTE+JOIN+aggregation SELECT → `parse_flink_segment` real dispatch (D-06 008 retirement) → shared query skeleton in its Flink-safe subset → real lossless CST with error/missing/skipped recovery → `fathom.parse.v1` with dialect=flink metadata. The tracer also locks the two one-way phase contracts the remaining four plans build on: D-02 (CST statement-family shape / SyntaxKind + kind_id wire strings) and D-04 (FATHOM-PARSE-009 construct-level dialect-gate diagnostic), plus the shared infra (precedence(context, cursor) Flink arm, missing Calcite-base reserved rows, bidirectional-negative-gate mechanism) and the flink-grammar snapshot namespace.
Output: real `parse_flink_segment` dispatch + `parse_flink_query` SELECT path, `precedence(context, cursor)`, MATCH_RECOGNIZE reserved rows, 008 retirement + flink-lexical test/snapshot update, D-02 SyntaxKind/kind_id surface, FATHOM-PARSE-009 gate helper, `scripts/extract_flink_grammar.py`, `parity/fixtures/flink-grammar/manifest.tsv`, `parity/flink_grammar_test.mbt` + first flink-grammar SELECT snapshots, and the Phase 11 approved-changes register.
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-flink-grammar-and-recoverable-cst/11-CONTEXT.md
@.planning/phases/11-flink-grammar-and-recoverable-cst/11-RESEARCH.md
@.planning/phases/11-flink-grammar-and-recoverable-cst/11-PATTERNS.md
@/opt/source/Fathom/parser/parser.mbt
@/opt/source/Fathom/syntax/syntax.mbt
@/opt/source/Fathom/api/api.mbt
@/opt/source/Fathom/dialect/flink.mbt
@/opt/source/Fathom/parity/flink_lexical_test.mbt

This is the phase's wave-1 tracer and carries the phase's two one-way gates. D-06 (FATHOM-PARSE-008 retirement, costly) and D-04 (diagnostic-code family, one-way) drive the statement-entry change; D-02 (CST shape, one-way) drives the SyntaxKind/kind_id surface. The Doris analogs are `parse_doris_segment` (parser.mbt:3354-3403), `precedence` (:263-274), and `SyntaxKind` (syntax.mbt:2-26). The 008-retirement surface is: `parse_flink_segment` (:3405-3429) plus the 008 test (`parser_flink_context_rejects_every_input_as_not_implemented`, :3608-3628) plus the Phase 10 flink-lexical assertions (parity/flink_lexical_test.mbt:187, 227, 234, 267, 274, 367, 369) and their snapshots — all must be updated together in the same commit (D-06 clean cutover).

> **Contribution hooks:** (1) API Coverage checkpoint — NOT detected: Phase 11 integrates NO external service API; the pinned Flink release archives under /tmp/flink-research/ are research-time fixtures (never a runtime dependency), so no COVERAGE.md is produced. (2) Assumption-Delta scan — `detected: false` (no pluralization/optional/chosen signals beyond the locked D-07 slice ordering): no promote-vs-add-alongside decision is required; Flink grammar is the locked new-dialect addition over the Phase 9/10 generalization, not an assumption-driven promotion. (3) Schema Push gate — no ORM schema files in this MoonBit parser project; skipped silently.

> **Scope note (breadth):** 4 tasks and 13 plan-level files exceeds the ideal 2-3 task / 10-file band, but the breadth is inherent to this plan's role — it is the phase's wave-1 tracer (one end-to-end path through every layer the phase touches) AND it carries both one-way phase gates (D-02 CST statement-family shape, D-04 FATHOM-PARSE-009 diagnostic family) plus the flink-grammar snapshot namespace. The calibrated 52k-token estimate is ≈52% of the 100k phase budget, leaving per-task headroom; a split was considered and rejected to keep the one-way gates and their consuming surfaces in one auditable commit.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Confirm D-04 dialect-gate diagnostic family: mint FATHOM-PARSE-009 (one-way door)</name>
  <decision>The bidirectional dialect-negative-gate diagnostic: mint a new stable code FATHOM-PARSE-009 with message "syntax is not supported in the selected dialect" for construct-level rejection, keeping FATHOM-PARSE-007 ("unsupported statement in the selected profile") for whole-statement unsupported. Dialect is never encoded in the code prefix (D-10); the dialect rides in the parse envelope metadata.</decision>
  <context>RESEARCH §9 Open Question 1: 007's semantics are "the whole statement is unsupported in this profile", but the negative gate rejects a clause/construct (WATERMARK FOR ..., MATCH_RECOGNIZE, DISTRIBUTED BY HASH(...) BUCKETS n, computed/metadata columns) that is simply valid only in the other dialect. Reusing 007 conflates two distinct contract states; minting 009 is a one-way door because diagnostic codes are a stable public contract (D-04). The Phase 10 register already minted FATHOM-PARSE-008 and FATHOM-SCHEMA-007; 009 would be the second parser diagnostic mint in this milestone and must be registered in approved-changes.md.</decision>
  <options>
    <option id="option-a">
      <name>Mint FATHOM-PARSE-009 (recommended — RESEARCH recommendation)</name>
      <pros>Precise semantics for construct-level dialect rejection; 007 stays reserved for whole-statement unsupported; consumers can distinguish "statement not supported" from "construct only valid in the other dialect".</pros>
      <cons>Adds a new stable diagnostic code to the public contract, requiring a register entry and future-doc documentation.</cons>
    </option>
    <option id="option-b">
      <name>Reuse FATHOM-PARSE-007 for construct-level rejection</name>
      <pros>Zero new codes; smaller contract surface.</pros>
      <cons>Conflates "whole statement unsupported" with "construct only valid in the other dialect", weakening the negative-gate contract the bidirectional fixtures assert (RESEARCH §9 warns 007's semantics are too broad).</cons>
    </option>
  </options>
  <resume-signal>Select option-a (mint FATHOM-PARSE-009) or describe a modified diagnostic surface; the tracer's negative-gate helper is authored only after this confirmation.</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Confirm D-02 CST statement-family shape: SyntaxKind names + kind_id wire strings (one-way door)</name>
  <decision>The new SyntaxKind variants and their kind_id wire strings for the Flink statement families: coarse-grained kinds per RESEARCH §6.1 — ShowStatement, DescribeStatement, ExplainStatement, AnalyzeStatement, CreateCatalog, CreateDatabase, CreateFunction, DropCatalog, DropDatabase, DropTable, DropView, DropFunction, AlterTable, WatermarkClause, ComputedColumn, MetadataColumn, PrimaryKeyClause, TableLikeClause, WindowTvf, MatchRecognize, SetOption, UseStatement — appended to the SyntaxKind enum end with snake_case kind_id strings, sub-type detail kept in metadata/span rather than extra kinds.</decision>
  <context>D-02 is one-way: SyntaxKind (syntax.mbt:2-26) and kind_id (api/api.mbt:331-357) are the wire CST contract. Appending new variants to the enum end preserves the ordinal positions of existing kinds, so the Doris wire output (kind_id strings) never shifts (Pitfall 1). Reordering existing variants would change the frozen Doris serialized output. RESEARCH Open Question 2 recommends coarse per-statement-family kinds; statement sub-types (SHOW TABLES vs SHOW CATALOGS) ride in node metadata/spans rather than minting a kind per sub-statement. The kind names are consumed by plans 02-05 as their statement families land.</decision>
  <options>
    <option id="option-a">
      <name>Coarse kinds per statement family (recommended — RESEARCH recommendation)</name>
      <pros>Small, stable enum surface; sub-type rides in metadata/span; minimal kind_id surface to document; matches the §6.1 mapping.</pros>
      <cons>Consumers inspecting SHOW TABLES vs SHOW CATALOGS read the statement keyword from node children/span rather than the kind id.</cons>
    </option>
    <option id="option-b">
      <name>Fine-grained per sub-statement kinds (ShowTables, ShowCatalogs, ...)</name>
      <pros>Kind id directly names the sub-statement.</pros>
      <cons>Doubles the enum/wire surface for every DDL/SHOW/DESCRIBE variant; higher migration cost if the set grows (one-way door); no consumer contract requires it.</cons>
    </option>
  </options>
  <resume-signal>Select option-a (coarse kinds) or describe a modified CST naming surface; the syntax.mbt/api.mbt edits in Task 4 use the confirmed names.</resume-signal>
</task>

<task type="tracer">
  <name>End-to-end Flink SELECT with CTE+JOIN+aggregation through real parse_flink_segment dispatch</name>
  <files>parser/parser.mbt, parser/flink_grammar.mbt, dialect/flink.mbt, dialect/classification.mbt, parity/flink_lexical_test.mbt, parity/__snapshot__/flink-lexical.*.json</files>
  <read_first>
    @.planning/phases/11-flink-grammar-and-recoverable-cst/11-CONTEXT.md
    @.planning/phases/11-flink-grammar-and-recoverable-cst/11-RESEARCH.md
    @.planning/phases/11-flink-grammar-and-recoverable-cst/11-PATTERNS.md
    @/opt/source/Fathom/parser/parser.mbt
    @/opt/source/Fathom/dialect/flink.mbt
    @/opt/source/Fathom/dialect/classification.mbt
    @/opt/source/Fathom/parity/flink_lexical_test.mbt
  </read_first>
  <action>Replace the FATHOM-PARSE-008 body of parse_flink_segment (parser.mbt:3405-3429) with a real keyword-first dispatch copying the parse_doris_segment skeleton (:3354-3403): arms for SELECT (finish_statement with SyntaxKind::Select) and WITH (via the existing with_prefix_verb, :1381-1441, dispatching SELECT), falling through to unsupported_statement (FATHOM-PARSE-007, :3163-3175) for unknown starters. Create parser/flink_grammar.mbt (same parser package, auto-compiled by the package-level moon.pkg) with parse_flink_query(cursor, state, source, statement_id) calling the shared query skeleton (parse_query :1363-1382 / parse_select_core :1154-1228 / parse_cte_prefix :1325-1361 / parse_from+parse_table_ref :909-1047) under Flink-safe gates: gate the Doris-only branches of the shared functions with `cursor.stream.context.dialect is @dialect.Dialect::Flink` so they are skipped under Flink — parse_select_core EXCEPT projection modifier (:1174-1183), QUALIFY feature gate (:1212-1219), INTO OUTFILE (:1218-1226); parse_table_ref PARTITION/TABLET/SAMPLE/TABLESAMPLE/REPEATABLE (:917-975); parse_cte_prefix WITH RECURSIVE FATHOM-PARSE-006 diagnostic (:1334-1337) must not fire under Flink (Calcite base WithList accepts it). Parameterize precedence(:263-274) to precedence(context, cursor): the Doris arm returns the existing table verbatim (byte-identical), the Flink arm adds `||` as CONCAT with a precedence between comparison and arithmetic (RESEARCH A3) and leaves `=>` to the function-call argument layer (not a binary operator); update both call sites (:686-691 and :770-778). Add add_dialect_gate_diagnostic(state, span, statement_id) in parser.mbt emitting the D-04-confirmed FATHOM-PARSE-009 with message "syntax is not supported in the selected dialect", modeled on add_feature_diagnostic (:362-380), and call it at the Flink SELECT path's Doris-only construct points (INTO OUTFILE / QUALIFY / TABLET / SAMPLE / TABLESAMPLE when encountered under Flink). Add the MATCH_RECOGNIZE reserved row to dialect/flink.mbt flink_classification_rows (Reserved, introduced_profile flink-1.20.5, source "flink-sql-parser Parser-calcite-1.36.0.jj:8214 (MATCH_RECOGNIZE)" and the 1.34/1.32 equivalents), plus any other missing Calcite-base reserved tokens found by diffing flink-2.3.0-reserved.txt against the current 142-row table; add classification tests in dialect/classification.mbt asserting MATCH_RECOGNIZE is Reserved under all three profiles. Retire FATHOM-PARSE-008: delete the 008 production path, rewrite parser_flink_context_rejects_every_input_as_not_implemented (:3608-3628) to assert a valid Flink SELECT parses valid=true with no FATHOM-PARSE-008 diagnostic, and update the parity/flink_lexical_test.mbt assertions that grep FATHOM-PARSE-008 (:187, :227, :234, :267, :274, :367, :369) to the real-grammar expectations (e.g. a bare `a # comment` now routes to FATHOM-PARSE-007 unsupported-statement; `SELECT "a" FROM t` now parses as a real SELECT under Flink) and re-generate the flink-lexical snapshots with a pre-committed approved-changes register entry — the code value FATHOM-PARSE-008 stays vacant, never reused. Verify the tracer SQL end-to-end (see acceptance criteria) and run the frozen Doris baseline.</action>
  <acceptance_criteria>
    - CLI: `printf 'WITH o AS (SELECT user_id, amount FROM t1) SELECT u.name, SUM(o.amount) AS total FROM o JOIN users u ON o.user_id = u.id GROUP BY u.name' | _build/native/release/build/fathom-sql/fathom-sql.exe parse --dialect flink --profile flink-2.3.0` exits 0, the fathom.parse.v1 envelope carries dialect=flink / profile=flink-2.3.0 / valid=true, the CST root contains a Select statement node, and no FATHOM-PARSE-008 diagnostic appears anywhere in the output.
    - Lossless replay: a test in the parser or parity package asserts print_lossless(parse(input)) == input for the tracer SQL and for an incomplete SELECT (`SELECT a, b FROM t WHERE`) in both strict and editor mode — the incomplete input yields a bounded Missing/Error node (shared recovery budget) and still round-trips.
    - Set operations (FLINK-02): `SELECT a FROM t1 UNION ALL SELECT a FROM t2`, `SELECT a FROM t1 INTERSECT SELECT a FROM t2`, and `SELECT a FROM t1 EXCEPT SELECT a FROM t2` (plus INTERSECT ALL / EXCEPT ALL where the pinned grammar supports them) parse valid=true under flink-2.3.0 through the shared CompoundQuery path of parse_query and each asserts print_lossless(parse(x)) == x in strict and editor mode — the set-op mechanism is asserted end-to-end, not just wired.
    - Negative gate: `SELECT * FROM t INTO OUTFILE 'x'` under flink-2.3.0 strict produces FATHOM-PARSE-009 with valid=false; `SELECT * FROM t TABLESAMPLE(10)` under flink produces FATHOM-PARSE-009 — while the same SQL under doris-4.x still parses exactly as the frozen baseline.
    - Reserved words: dialect/classification.mbt tests assert classification_of(flink-2.3.0, "MATCH_RECOGNIZE") is Reserved and the same holds for flink-2.1.3 and flink-1.20.5; a backtick-quoted `SELECT \`MATCH_RECOGNIZE\`` parses as an identifier reference under Flink.
    - 008 retirement: `moon test --target native --package parser` passes after the 008 test rewrite; a valid Flink SELECT produces no FATHOM-PARSE-008; the code string appears nowhere in parser.mbt's dispatch or diagnostics (the register entry documents the vacancy).
    - Doris baseline: `moon test --package parity` (no --update) passes; `git diff --name-only -- parity/__snapshot__` shows no doris-named snapshot changed and the only non-flink-grammar changes are the registered flink-lexical re-generation files.
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native --package parser --package dialect --package api && moon build --target native --release && printf 'WITH o AS (SELECT user_id, amount FROM t1) SELECT u.name, SUM(o.amount) AS total FROM o JOIN users u ON o.user_id = u.id GROUP BY u.name' | _build/native/release/build/fathom-sql/fathom-sql.exe parse --dialect flink --profile flink-2.3.0 >/dev/null 2>&1 && (printf '%s' "SELECT * FROM t INTO OUTFILE 'x'" | _build/native/release/build/fathom-sql/fathom-sql.exe parse --dialect flink --profile flink-2.3.0 2>&1 | grep -q 'FATHOM-PARSE-009') && (printf '%s' "SELECT a FROM t1 UNION ALL SELECT a FROM t2" | _build/native/release/build/fathom-sql/fathom-sql.exe parse --dialect flink --profile flink-2.3.0 2>&1 | grep -q '"valid":true') && moon test --package parity</automated>
  </verify>
  <done>The Flink SELECT/CTE/JOIN/aggregation path parses end-to-end into a real lossless recoverable CST with dialect=flink envelope metadata, Doris-only SELECT constructs reject with FATHOM-PARSE-009, MATCH_RECOGNIZE and the missing Calcite-base reserved rows classify as Reserved, FATHOM-PARSE-008 is retired (code vacant, flink-lexical tests/snapshots updated with a registered change), and the Doris 213-snapshot baseline is byte-identical.</done>
</task>

<task type="auto">
  <name>D-02 CST contract surface + flink-grammar snapshot namespace + register</name>
  <files>syntax/syntax.mbt, api/api.mbt, scripts/extract_flink_grammar.py, parity/fixtures/flink-grammar/manifest.tsv, parity/flink_grammar_test.mbt, parity/__snapshot__/flink-grammar.*.json, .planning/phases/11-flink-grammar-and-recoverable-cst/approved-changes.md</files>
  <read_first>
    @.planning/phases/11-flink-grammar-and-recoverable-cst/11-RESEARCH.md
    @.planning/phases/11-flink-grammar-and-recoverable-cst/11-PATTERNS.md
    @/opt/source/Fathom/syntax/syntax.mbt
    @/opt/source/Fathom/api/api.mbt
    @/opt/source/Fathom/parity/flink_lexical_test.mbt
    @/opt/source/Fathom/parity/baseline_test.mbt
    @/opt/source/Fathom/corpus/tools/check_keywords.py
  </read_first>
  <action>Apply the D-02-confirmed contract surface: append the locked SyntaxKind variants to syntax/syntax.mbt (:2-26) in the confirmed order (ShowStatement, DescribeStatement, ExplainStatement, AnalyzeStatement, CreateCatalog, CreateDatabase, CreateFunction, DropCatalog, DropDatabase, DropTable, DropView, DropFunction, AlterTable, WatermarkClause, ComputedColumn, MetadataColumn, PrimaryKeyClause, TableLikeClause, WindowTvf, MatchRecognize, SetOption, UseStatement) — appended at the enum end, never reordering existing variants — and add the matching snake_case kind_id arms in api/api.mbt kind_id (:331-357) in the same commit (the match is exhaustive; a variant without its wire string breaks compilation). node_invariants_hold and is_valid need no changes (kind does not participate in those checks). Create scripts/extract_flink_grammar.py as a Python-stdlib-only extractor/validator in the check_keywords.py shape (problems list + per-line report + non-zero exit + trailing ok line) that reads the pinned release grammars under /tmp/flink-research/src/flink-{2.3.0,2.1.3,1.20.5}/flink-table/flink-sql-parser/src/main/codegen/ and validates the production line references used by the fixtures against RESEARCH §5 (reporting and exiting 1 on mismatch). Create parity/fixtures/flink-grammar/manifest.tsv with the D-05 provenance columns (fixture_id, profile, exact_release, calcite_version, parser_config, grammar_path, line_range, source_archive_url, sha512, git_tag, git_commit) — the first rows cover the SELECT tracer fixtures. Create parity/flink_grammar_test.mbt modeled on parity/flink_lexical_test.mbt (:44-101): a FlinkGrammarFixture struct { fixture_id, profile, raw }, a flink_grammar_parse_json(id, profile, mode) helper over @binding.fathom_parse_v1, the flink_snapshot_test snapshot helper, per-fixture snapshot tests writing parity/__snapshot__/flink-grammar.{fixture}.{profile}.{strict,editor}.json, and lossless-replay assertions (print_lossless(parse(x)) == x) on each positive/recovery fixture. Seed the first fixture set from the pinned-release grammar positives (FlinkSqlParserImplTest-style SELECT examples from RESEARCH §5.1 and §Code Examples 6) plus the incomplete SELECT recovery fixture. Commit the approved-changes register entry (008 retirement, 009 minting, flink-grammar namespace, flink-lexical re-generation) BEFORE the single `moon test --update --package parity` run, then generate the flink-grammar snapshots and the re-generated flink-lexical snapshots, and confirm the Doris 213-snapshot baseline is untouched.</action>
  <acceptance_criteria>
    - syntax.mbt contains all confirmed SyntaxKind variants appended after Missing (existing variants unchanged, ordinal positions preserved); api.mbt kind_id maps each new variant to a snake_case string; `moon test --target native --package syntax --package api` passes (exhaustive match proves the surface is complete).
    - python3 scripts/extract_flink_grammar.py exits 0 with an ok line validating the pinned grammar line references used by the manifest/fixtures; a deliberately wrong line reference makes it exit 1.
    - parity/fixtures/flink-grammar/manifest.tsv exists with the SELECT-fixture provenance rows (sha512/tag/commit from RESEARCH §10); the Doris corpus manifest is untouched.
    - parity/flink_grammar_test.mbt snapshot tests pass and parity/__snapshot__/flink-grammar.*.json files exist for the SELECT positive + incomplete-recovery fixtures and the set-operation positive fixtures (UNION [ALL], INTERSECT, EXCEPT) in strict and editor mode; every positive/recovery fixture asserts print_lossless(parse(x)) == x.
    - The set-operation positive fixture set — `SELECT a FROM t1 UNION ALL SELECT a FROM t2`, `SELECT a FROM t1 INTERSECT SELECT a FROM t2`, `SELECT a FROM t1 EXCEPT SELECT a FROM t2` (plus INTERSECT ALL / EXCEPT ALL where the pinned grammar supports them) — parses valid=true under flink-2.3.0 with byte-identical lossless replay in strict and editor mode; the manifest rows carry the CompoundQuery provenance (Parser-calcite-1.36.0.jj:3395).
    - The approved-changes register entry is committed before the single --update; after generation, `git diff --name-only -- parity/__snapshot__` shows no doris-named snapshot changed (only flink-grammar.* and the registered flink-lexical.* re-generation appear).
    - moon test --package parity passes without --update after generation.
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native --package syntax --package api && python3 scripts/extract_flink_grammar.py && python3 -m py_compile scripts/extract_flink_grammar.py && test -s parity/fixtures/flink-grammar/manifest.tsv && moon test --package parity && test -z "$(git diff --name-only -- parity/__snapshot__ | grep -vE 'flink-(grammar|lexical)\.')"</automated>
  </verify>
  <done>The D-02 CST contract surface (SyntaxKind variants + kind_id wire strings) is locked and applied, the flink-grammar snapshot namespace exists with the first SELECT positive/recovery fixtures and the set-operation positive fixtures (UNION [ALL]/INTERSECT/EXCEPT) plus provenance manifest, the approved-changes register documents 008 retirement / 009 minting / snapshot namespaces, and the Doris baseline is byte-identical.</done>
</task>

</tasks>

## Source Audit Coverage

| SOURCE | ID | Feature or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Flink SELECT/CTE/JOIN/aggregation into a lossless bounded recoverable CST without changing Doris acceptance | 11-01 | COVERED | Tracer: SELECT path end-to-end + CST-01 recovery discipline. |
| REQ | FLINK-02 | core queries: SELECT, CTE, JOIN, aggregation, set operations, expressions, types | 11-01 | PARTIAL (queries) | Tracer covers SELECT/CTE/JOIN/aggregation/expressions; set operations asserted in Task 3 acceptance + Task 4 fixtures (UNION/INTERSECT/EXCEPT); DML/aux in 11-02. |
| REQ | CST-01 | strict/editor recoverable lossless CST; error/missing/skipped; lossless replay | 11-01 | COVERED | Tracer recovery fixtures + print_lossless assertions; re-asserted every plan. |
| CONTEXT | D-01 | grammar scope = full FLINK-02..06 enumeration; TVF/MR supported/known-limitation subsets frozen from pinned release; syntax-level only | 11-01 | COVERED (scope anchor) | D-01 scope-anchor truth in must_haves; enumeration slices distributed across 11-01..05. |
| CONTEXT | D-02 | reuse lossless CST node system; new Flink statement-family nodes; one-way | 11-01 | COVERED | Task 2 checkpoint (one-way) + Task 4 SyntaxKind/kind_id surface. |
| CONTEXT | D-04 | single parse_segment route; Flink branch real grammar; bidirectional negative gate; stable codes; one-way | 11-01 | PARTIAL (mechanism + SELECT) | Task 1 checkpoint (one-way) + Task 3 add_dialect_gate_diagnostic + SELECT-path gates; full matrix fixtures in 11-02..05. |
| CONTEXT | D-05 | grammar source = pinned release; fixtures positive/negative/incomplete/recovery; Doris baseline | 11-01 | PARTIAL (namespace + first fixtures) | Task 4; full four-category fixture set built across 11-02..05. |
| CONTEXT | D-06 | FATHOM-PARSE-008 retirement; code vacant | 11-01 | COVERED | Task 3. |
| CONTEXT | D-07 | vertical-slice order FLINK-02 → 03 → 04 → 05 → 06 | 11-01 | COVERED (slice 1 start) | 11-01 is slice FLINK-02 (core queries); DML/aux in 11-02. |
| RESEARCH | §5.1 | core-query production mapping (SqlSelect/WithList/OrderedQueryOrExpr line refs) | 11-01 | COVERED | Task 3 query path. |
| RESEARCH | §6.1 | SyntaxKind additions (coarse kind names) | 11-01 | COVERED | Task 4. |
| RESEARCH | §6.3/6.4 | recovery + lossless replay | 11-01 | COVERED | Task 3 fixtures + invariant. |
| RESEARCH | §7.1/7.3 | expression surface: `||`, `=>`, INTERVAL, literals | 11-01 | PARTIAL (precedence + literal) | precedence(context,cursor) in Task 3; named-arg `=>` consumption + type breadth in 11-02. |
| RESEARCH | §9/§10 | negative-gate matrix + fixture plan | 11-01 | PARTIAL (SELECT rows) | SELECT-only rows in Task 3; full matrix per statement family in 11-02..05. |
| RESEARCH | Pitfall 9 | MATCH_RECOGNIZE missing reserved row | 11-01 | COVERED | Task 3 dialect row + classification test. |
| PROBE | FLINK-02 adjacency | touching/equal constructs must not merge or drop spans | 11-01 | FLAGGED | truth + span-contiguity fixtures. |
| PROBE | FLINK-02 empty | empty/single-token Flink input behavior | 11-01 | FLAGGED | truth + incomplete fixtures. |
| PROBE | FLINK-02 ordering | equal-precedence / set-op parse order stable | 11-01 | FLAGGED | truth + Pratt binding + set-op fixtures (Task 4). |
| PROBE | CST-01 boundary | recovery stops exactly at segment/statement boundary | 11-01 | FLAGGED | truth + recovery fixtures. |
| PROBE | CST-01 precision | exact half-open byte spans on error/missing/skipped | 11-01 | FLAGGED | truth + span assertions. |

<verification>
Run moon test --target native --package parser --package dialect --package syntax --package api after the tracer and the contract surface, and moon test --package parity (no --update) after every snapshot-affecting step. Inspect: the real parse_flink_segment dispatch (SELECT/WITH arms), the Flink-safe query path (Doris-only branches gated, byte-identical under Doris), precedence(context, cursor) Flink arm + both call sites, add_dialect_gate_diagnostic emitting FATHOM-PARSE-009, the MATCH_RECOGNIZE + missing reserved rows and classification tests, the 008-retirement test rewrite and flink-lexical assertion/snapshot update, the D-02 SyntaxKind/kind_id surface, the extract script ok-line, the manifest rows, the flink-grammar SELECT snapshots (strict/editor), the lossless-replay assertions, and the approved-changes register committed before the single --update. Do not install packages; do not touch lexer.mbt (the Flink lexical token surface is Phase 10-complete); do not re-run the update without a pre-committed register entry.
</verification>

<success_criteria>
- A consumer can parse a Flink SELECT with CTE+JOIN+aggregation end-to-end into a real lossless recoverable CST (dialect=flink, no FATHOM-PARSE-008), with strict/editor dual-mode recovery and byte-identical lossless replay.
- The D-02 CST contract surface and D-04 FATHOM-PARSE-009 dialect-gate diagnostic are locked (checkpoints) and applied; MATCH_RECOGNIZE and missing Calcite-base reserved rows classify as Reserved.
- The flink-grammar snapshot namespace exists with the first SELECT fixtures and provenance manifest; the flink-lexical group is re-generated under a registered 008-retirement change; the Doris 213-snapshot baseline is byte-identical.
</success_criteria>

<output>Create `.planning/phases/11-flink-grammar-and-recoverable-cst/11-01-SUMMARY.md` when this plan executes.</output>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flink input text -> parse_flink_segment dispatch | Untrusted SQL crosses here; the real grammar must accept Flink statements and never fall back to Doris (DIALECT-03). |
| dialect/profile strings -> ParseOptions | Closed-enum validated (Phase 10); a flink selection must not silently route to Doris grammar. |
| shared query/expression functions -> dialect gates | Doris-only branches must stay byte-identical under Doris while being skipped/rejected under Flink (Pitfall 1/7). |
| classification rows -> identifier acceptance | Missing reserved rows (MATCH_RECOGNIZE) would accept a reserved word as an identifier (Pitfall 9). |
| parity snapshots -> CI gate | flink-grammar group + re-generated flink-lexical group must never touch the Doris 213 baseline (D-08). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-11-01 | Tampering | Flink SELECT path accepting Doris-only constructs (INTO OUTFILE/QUALIFY/TABLET/SAMPLE) | high | mitigate | add_dialect_gate_diagnostic (FATHOM-PARSE-009) at every Doris-only construct point in the Flink SELECT path + negative fixtures (Task 3). |
| T-11-02 | Tampering | Shared precedence/query changes altering Doris expression acceptance (`||` handled as one CONCAT token in Doris) | high | mitigate | precedence(context, cursor) keeps the Doris arm byte-identical; Doris 213-snapshot gate after every shared change (Pitfall 7). |
| T-11-03 | Tampering | MATCH_RECOGNIZE (and other missing reserved words) accepted as an unquoted identifier under Flink | high | mitigate | flink_classification_rows row per pinned Calcite token line; classification tests under all three profiles (Pitfall 9). |
| T-11-04 | Tampering | FATHOM-PARSE-008 reuse after retirement confusing consumers who saw it | medium | mitigate | code stays vacant (never reused); register entry documents the vacancy; whole-statement unsupported routes to 007 only (D-06). |
| T-11-05 | Denial of Service | Incomplete SELECT / empty input causing unbounded recovery or infinite loop | high | mitigate | shared consume_recovery_step / depth_allowed budget + finish_statement trailing handling; incomplete fixtures assert bounded steps (CST-01, D-03). |
| T-11-06 | Tampering | flink-grammar or re-generated flink-lexical snapshots leaking into the Doris namespace | medium | mitigate | independent flink-grammar./flink-lexical. naming + register entry; git diff --name-only -- parity/__snapshot__ per-group check (Pitfall 7). |
| T-11-07 | Tampering | Fabricated production line refs / fixture provenance (folklore vs pinned release) | high | mitigate | extract_flink_grammar.py validates line refs against the pinned archives; manifest records sha512/tag/commit (D-05, Pitfall 5). |
| T-11-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new runtime dependencies (RESEARCH Package Legitimacy Audit N/A); extract script is Python stdlib. |
</threat_model>

## Artifacts this phase produces

Files: parser/parser.mbt (modified — parse_flink_segment real dispatch, precedence(context, cursor), add_dialect_gate_diagnostic, 008 removal, shared query gates, 008 test rewrite), parser/flink_grammar.mbt (new — parse_flink_query + parse_flink_select_core SELECT path), syntax/syntax.mbt (modified — 22 new SyntaxKind variants appended), api/api.mbt (modified — 22 new kind_id arms), dialect/flink.mbt (modified — MATCH_RECOGNIZE + missing Calcite-base reserved rows), dialect/classification.mbt (modified — MATCH_RECOGNIZE reserved tests), parity/flink_lexical_test.mbt (modified — 008 assertions to real-grammar expectations), parity/flink_grammar_test.mbt (new — FlinkGrammarFixture + snapshot harness + SELECT fixtures), parity/fixtures/flink-grammar/manifest.tsv (new — provenance rows), parity/__snapshot__/flink-grammar.*.json (new, generated — SELECT positive + recovery snapshots), parity/__snapshot__/flink-lexical.*.json (re-generated, registered — 008→real-grammar), scripts/extract_flink_grammar.py (new — stdlib extractor/validator), .planning/phases/11-flink-grammar-and-recoverable-cst/approved-changes.md (new register).

Symbols (parser/parser.mbt): parse_flink_segment (real dispatch), precedence(context, cursor) with Flink arm, add_dialect_gate_diagnostic(state, span, statement_id) -> FATHOM-PARSE-009, removed 008 path + rewritten parser_flink_context test.
Symbols (parser/flink_grammar.mbt): parse_flink_query(cursor, state, source, statement_id), parse_flink_select_core (Flink-safe wrapper).
Symbols (syntax/syntax.mbt): ShowStatement, DescribeStatement, ExplainStatement, AnalyzeStatement, CreateCatalog, CreateDatabase, CreateFunction, DropCatalog, DropDatabase, DropTable, DropView, DropFunction, AlterTable, WatermarkClause, ComputedColumn, MetadataColumn, PrimaryKeyClause, TableLikeClause, WindowTvf, MatchRecognize, SetOption, UseStatement.
Symbols (api/api.mbt): kind_id arms "show_statement" | "describe_statement" | "explain_statement" | "analyze_statement" | "create_catalog" | "create_database" | "create_function" | "drop_catalog" | "drop_database" | "drop_table" | "drop_view" | "drop_function" | "alter_table" | "watermark_clause" | "computed_column" | "metadata_column" | "primary_key_clause" | "table_like_clause" | "window_tvf" | "match_recognize" | "set_option" | "use_statement".
Symbols (dialect/flink.mbt): flink_classification_rows entries for MATCH_RECOGNIZE + diffed Calcite-base reserved tokens (Reserved, per-release source line).
Symbols (scripts/extract_flink_grammar.py): CLI with no external args, problems/ok reporting, exit 0/1.
Symbols (parity/flink_grammar_test.mbt): FlinkGrammarFixture { fixture_id, profile, raw }, flink_grammar_parse_json, flink_snapshot_test; parity/__snapshot__/flink-grammar.{fixture}.{profile}.{strict,editor}.json.
