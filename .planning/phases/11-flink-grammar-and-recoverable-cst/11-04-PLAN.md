---
phase: 11-flink-grammar-and-recoverable-cst
plan: 04
type: execute
wave: 4
depends_on: [11-03]
files_modified:
  - parser/parser.mbt
  - parser/flink_grammar.mbt
  - parity/flink_grammar_test.mbt
  - parity/fixtures/flink-grammar/manifest.tsv
  - parity/__snapshot__/flink-grammar.*.json
  - .planning/phases/11-flink-grammar-and-recoverable-cst/approved-changes.md
autonomous: true
requirements: [FLINK-05, FLINK-06]
estimate:
  tokens: 54000
  raw_tokens: 54000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A consumer can parse Window TVF forms for TUMBLE, HOP, CUMULATE, and SESSION under flink-2.3.0: FROM TUMBLE(TABLE t, DESCRIPTOR(col), INTERVAL 'n' UNIT), the HOP four-argument form (slide + size), CUMULATE and SESSION (no keyword token / non-reserved), the explicit TABLE(...) wrapper, an optional offset interval argument, named arguments via `=>`, and the window output columns window_start/window_end/window_time as ordinary identifiers (FLINK-05, WindowAggregateITCase.scala:228-297)."
    - "The TVF table-function call parses through the Flink table-ref path (TableFunctionCall, Parser.jj:2443-2460): a WindowTvf node wrapping the TABLE-reference, DESCRIPTOR(...) descriptor, and INTERVAL literal argument children with spans preserved; TUMBLE/HOP/SESSION/DESCRIPTOR are non-reserved words (Parser.tdd:587/:384/:509/:351) so no keyword-table change is needed."
    - "A consumer can inspect syntax-level MATCH_RECOGNIZE CST and diagnostics covering PATTERN, DEFINE, MEASURES, skip policy, pattern variables, and quantifiers under flink-2.3.0: PARTITION BY, ORDER BY, MEASURES (with MATCH_ROWTIME()/MATCH_PROCTIME()/MATCH_NUMBER() as ordinary function calls), ONE ROW PER MATCH / ALL ROWS PER MATCH, AFTER MATCH SKIP TO NEXT ROW / TO FIRST|LAST var / PAST LAST ROW, PATTERN (with ^ $ anchors, | ALTER, concatenation, {n}/{n,}/{n,m}/+/*/? and reluctant ? quantifiers), WITHIN INTERVAL, and DEFINE var AS expr (FLINK-06, Parser.jj:3062-3346)."
    - "MATCH_RECOGNIZE is a Flink-only construct: under Doris mode FROM t MATCH_RECOGNIZE (...) rejects with FATHOM-PARSE-009 (construct-level gate at the table-ref suffix), and Window TVF under Doris mode rejects with FATHOM-PARSE-009 — while the same inputs under Flink parse valid (DIALECT-03, D-04)."
    - "The MATCH_RECOGNIZE sub-language has independent sync points (PARTITION/ORDER/MEASURES/ONE ROW/ALL ROWS/AFTER MATCH/PATTERN/DEFINE/`)`) with is_flink_match_recognize_boundary so an unclosed PATTERN or missing DEFINE recovers at the sub-language boundary or `;` under the shared recovery budget — no infinite push (Pitfall 4/8, CST-01)."
    - "The supported/known-limitation subset is frozen: syntax-level PATTERN/DEFINE/MEASURES/skip/variables/quantifiers fully implemented; SUBSET, PERMUTE, and {- ... -} (PatternExclude) parse structurally and are classified known-limitation in fixtures; pattern-variable column-scope/type validation is NOT performed (syntactically valid input is never rejected for undeclared variables); TVF planner semantics (event time/watermark/window-aggregate type inference) are explicitly not claimed (FLINK-05/06, Pitfall 6)."
    - "Every positive/recovery TVF/MATCH_RECOGNIZE fixture asserts print_lossless(parse(x)) == x in strict and editor mode (CST-01, D-03); Doris baseline zero-drift holds (D-05/D-08)."
    - "[probe FLINK-05 adjacency] flagged-unverified: TVF argument boundaries (TABLE t vs DESCRIPTOR(col) vs INTERVAL literal) must not merge — each argument is a distinct source-backed child node; the WindowTvf span-contiguity invariant pins the edges."
    - "[probe FLINK-05 empty] flagged-unverified: an empty/truncated TVF call (TUMBLE(TABLE t, DESCRIPTOR(ts), or TUMBLE() with no arguments) yields a bounded error/missing node — never an infinite loop; incomplete fixtures cover each arity."
    - "[probe FLINK-05 ordering] flagged-unverified: the TVF argument order (table, descriptor, size, optional offset) is positional and stable; named-argument `=>` forms are recognized in the argument layer without reordering positional args — frozen by the TVF fixtures."
    - "[probe FLINK-06 unclassified] flagged-unverified: manual review — MATCH_RECOGNIZE is a nested sub-language with no repository analog; the syntax-level subset boundary (SUBSET/PERMUTE/{- -} known-limitation; no pattern-variable scope validation) is the recommended freeze and requires executor verification against the pinned Parser.jj:3062-3346 before finalizing fixtures."
  artifacts:
    - path: "parser/parser.mbt"
      provides: "Flink table-ref TVF recognition branch + Doris-mode 009 gate at the table-ref point, MATCH_RECOGNIZE suffix LOOKAHEAD in the Flink table-ref, is_flink_match_recognize_boundary predicate"
    - path: "parser/flink_grammar.mbt"
      provides: "parse_flink_tvf_call (TUMBLE/HOP/CUMULATE/SESSION + TABLE(...) wrapper + DESCRIPTOR + INTERVAL + named args), parse_match_recognize sub-language (PATTERN/DEFINE/MEASURES/skip/variables/quantifiers), parse_pattern_expr"
    - path: "parity/flink_grammar_test.mbt + parity/__snapshot__/flink-grammar.*.json"
      provides: "TVF + MATCH_RECOGNIZE fixtures (positive/negative/incomplete/recovery) with strict/editor snapshots and the supported/known-limitation classification"
    - path: ".planning/phases/11-flink-grammar-and-recoverable-cst/approved-changes.md"
      provides: "final register rows: TVF/MATCH_RECOGNIZE snapshot additions, the known-limitation subset freeze, and the negative-gate fixtures"
  key_links:
    - from: "parse_table_ref Flink TVF branch"
      to: "parse_flink_tvf_call"
      via: "identifier TUMBLE/HOP/CUMULATE/SESSION followed by `(` or the TABLE(...) wrapper routes to the table-function call parser (WindowTvf kind)"
    - from: "parse_table_ref Flink MATCH_RECOGNIZE suffix"
      to: "parse_match_recognize"
      via: "LOOKAHEAD for the reserved MATCH_RECOGNIZE keyword after the base table reference, mirroring MatchRecognize(SqlNode tableRef) Parser.jj:3062-3183"
    - from: "parse_match_recognize sub-language"
      to: "is_flink_match_recognize_boundary"
      via: "recover_to_clause_boundary so an unclosed PATTERN/DEFINE stops at the sub-language sync point or `;` (Pitfall 4/8)"
    - from: "the Doris-mode table-ref gate"
      to: "add_dialect_gate_diagnostic (FATHOM-PARSE-009)"
      via: "MATCH_RECOGNIZE and TUMBLE/HOP/CUMULATE/SESSION(` constructs reject under Doris with the construct-level code"
  prohibitions:
    - "Window TVF MUST NOT claim planner/execution equivalence (event time, watermarks, window-aggregate type inference) — syntax-level CST only (FLINK-05); legacy TUMBLE_START()/TUMBLE_ROWTIME() functions are ordinary function calls, never TVF output columns (Pitfall 8)."
    - "MATCH_RECOGNIZE MUST NOT perform pattern-variable column-scope/type validation — a syntactically valid MATCH_RECOGNIZE is never rejected for undeclared variables (FLINK-06, Pitfall 6); the SDK makes no planner or execution-equivalence claim."
    - "MATCH_RECOGNIZE and Window TVF MUST NOT be accepted under Doris mode — both reject at the table-ref point with FATHOM-PARSE-009 (DIALECT-03, D-04)."
    - "Unclosed MATCH_RECOGNIZE/PATTERN MUST NOT drive unbounded recovery — the sub-language sync points and the shared consume_recovery_step/depth_allowed budget cap recovery (Pitfall 4/8, CST-01)."
    - "No fabricated production line refs / fixture sources — every PATTERN/DEFINE/MEASURES/skip/quantifier production and TVF example traces to the pinned release grammars and planner tests with a line reference (D-05, Pitfall 5)."
---

<objective>
**As a** consumer, **I want to** parse Window TVF forms (TUMBLE/HOP/CUMULATE/SESSION) and syntax-level MATCH_RECOGNIZE (PATTERN/DEFINE/MEASURES/skip policy/variables/quantifiers) into lossless, recoverable, source-backed CST under the frozen supported/known-limitation subset, **so that** FLINK-05 and FLINK-06 land without any planner or execution-equivalence claim and the Doris baseline stays byte-identical.

Purpose: Deliver the last grammar slice — the two distinctive Flink sub-languages. Window TVF rides the generic table-function-call path (non-reserved TUMBLE/HOP/SESSION/DESCRIPTOR, Parser.jj:2443-2460) as a Flink table-ref branch producing WindowTvf nodes; MATCH_RECOGNIZE is a nested sub-language (Parser.jj:3062-3346) with no repository analog, implemented as an independent production with its own sync points so recovery never goes unbounded. Both are Flink-only and reject in Doris mode with FATHOM-PARSE-009. The supported/known-limitation subset (RESEARCH §8) is frozen into fixtures, and the bidirectional negative gates complete the D-04 matrix. This closes the Phase 11 grammar scope (D-07 slices FLINK-05 and FLINK-06).
Output: parse_flink_tvf_call, parse_match_recognize + parse_pattern_expr, the Flink table-ref TVF branch and MATCH_RECOGNIZE suffix, is_flink_match_recognize_boundary, the TVF/MATCH_RECOGNIZE fixtures and snapshots (positive/negative/incomplete/recovery × strict/editor), and the final approved-changes register rows.
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-flink-grammar-and-recoverable-cst/11-CONTEXT.md
@.planning/phases/11-flink-grammar-and-recoverable-cst/11-RESEARCH.md
@.planning/phases/11-flink-grammar-and-recoverable-cst/11-PATTERNS.md
@/opt/source/Fathom/parser/parser.mbt
@/opt/source/Fathom/parser/flink_grammar.mbt
@/opt/source/Fathom/dialect/flink.mbt

This plan closes the phase grammar scope. The production facts are RESEARCH §5.5 (TVF syntax structure: TUMBLE/HOP/SESSION/DESCRIPTOR non-reserved Parser.tdd:587/:384/:509/:351, TableFunctionCall Parser.jj:2443-2460, IntervalLiteral Parser-calcite-1.36.0.jj:4943, NAMED_ARGUMENT_ASSIGNMENT :8794; planner-test SQL WindowAggregateITCase.scala:228-297), §5.6 (MatchRecognize Parser.jj:3062-3346 with PARTITION :3091, ORDER :3095, MEASURES :3101-3103, rows-per-match :3108-3115, AFTER MATCH SKIP :3123-3154, PATTERN :3157-3176 with ^$ anchors, WITHIN INTERVAL :3170, DEFINE :3177-3178, PatternQuantifier :3314-3317, PatternExclude :3294-3335, PatternPermute :3337-3346), §8 (supported/known-limitation subset freeze), and §9 (negative-gate rows for TVF/MATCH_RECOGNIZE). The MATCH_RECOGNIZE reserved row was added to dialect/flink.mbt in 11-01 (Pitfall 9). The D-02 WindowTvf/MatchRecognize kinds were locked in 11-01. This is the NO-ANALOG sub-language: use the RESEARCH §5.6 / Pattern 3 grammar skeleton directly.
</context>

<tasks>

<task type="auto">
  <name>Window TVF: Flink table-ref branch + parse_flink_tvf_call (TUMBLE/HOP/CUMULATE/SESSION + TABLE/DESCRIPTOR/INTERVAL + named args)</name>
  <files>parser/parser.mbt, parser/flink_grammar.mbt, parity/flink_grammar_test.mbt, parity/__snapshot__/flink-grammar.*.json</files>
  <read_first>
    @.planning/phases/11-flink-grammar-and-recoverable-cst/11-RESEARCH.md
    @.planning/phases/11-flink-grammar-and-recoverable-cst/11-PATTERNS.md
    @/opt/source/Fathom/parser/parser.mbt
    @/opt/source/Fathom/parser/flink_grammar.mbt
  </read_first>
  <action>In parser.mbt, extend parse_table_ref (:909-947) with a Flink branch under `cursor.stream.context.dialect is @dialect.Dialect::Flink`: when the current identifier is TUMBLE, HOP, CUMULATE, or SESSION (byte-compare, case-insensitive) and the next significant token is `(`, or when the current token is `TABLE` followed by `(`, route to parse_flink_tvf_call in parser/flink_grammar.mbt. Implement parse_flink_tvf_call per the generic table-function-call path (TableFunctionCall, Parser.jj:2443-2460): the TVF name (TUMBLE/HOP/CUMULATE/SESSION or the TABLE(...) wrapper), then a parenthesized argument list where the first argument is a TABLE table-reference (consume the TABLE keyword then parse a qualified name), the second is a DESCRIPTOR( col ) call (non-reserved function call), the third is an INTERVAL 'n' UNIT literal (parse via the shared expression layer's INTERVAL literal), an optional fourth offset INTERVAL, and optional named arguments `name => expr` (recognized in the argument layer via the 11-02 named-arg support). Produce a WindowTvf node (the D-02 kind) whose children are the source-backed TVF name token, the TABLE-reference node, the DESCRIPTOR call node, and each INTERVAL argument node with continuous spans; window_start/window_end/window_time are ordinary identifiers that parse as normal projections/aliases (no special handling). Add the Doris-mode negative gate: in parse_table_ref under Doris, when the identifier is TUMBLE/HOP/CUMULATE/SESSION and the next token is `(`, or the construct is `TABLE(` with a table-function shape, call add_dialect_gate_diagnostic (FATHOM-PARSE-009). Add fixtures to parity/flink_grammar_test.mbt: positive (FROM TUMBLE(TABLE T1, DESCRIPTOR(rowtime), INTERVAL '1' DAY), FROM HOP(TABLE T1, DESCRIPTOR(rowtime), INTERVAL '30' SECONDS, INTERVAL '15' SECONDS), FROM CUMULATE(TABLE T1, DESCRIPTOR(rowtime), INTERVAL '1' HOUR, INTERVAL '1' DAY), FROM SESSION(TABLE T1, DESCRIPTOR(rowtime), INTERVAL '1' MINUTE), FROM TUMBLE(TABLE T1, DESCRIPTOR(rowtime), INTERVAL '1' DAY, INTERVAL '8' HOUR), FROM TABLE(TUMBLE(TABLE T, DESCRIPTOR(matchRowtime), INTERVAL '3' second)), SELECT window_start, window_end FROM TUMBLE(...) GROUP BY window_start, window_end), negative (TUMBLE(TABLE T1) — missing descriptor, TUMBLE(DESCRIPTOR(rowtime), INTERVAL '1' DAY) — missing table), incomplete (TUMBLE(TABLE T1, DESCRIPTOR(rowtime), INTERVAL), TUMBLE(TABLE T1, DESCRIPTOR(ts),), FROM TUMBLE(TABLE T1), recovery (TUMBLE(TABLE T1, DESCRIPTOR(rowtime), INTERVAL '1' DAY); SELECT 2). These under Doris produce FATHOM-PARSE-009 (add the negative-gate fixtures). Register snapshot additions; re-run the Doris baseline gate.</action>
  <acceptance_criteria>
    - `SELECT window_start, window_end FROM TUMBLE(TABLE T1, DESCRIPTOR(rowtime), INTERVAL '1' DAY) GROUP BY window_start, window_end` parses valid=true under flink-2.3.0 with a WindowTvf node, byte-identical lossless replay, and window_start/window_end as ordinary projection identifiers.
    - The HOP four-argument form, CUMULATE (no keyword token, generic identifier path), SESSION, the TABLE(...) wrapper, and the offset-interval fourth argument all parse valid=true; named-argument TVF (`TUMBLE(data => TABLE T1, ...)`) parses via the 11-02 named-arg support.
    - Negative: `FROM TUMBLE(TABLE T1)` (missing descriptor/size) and `FROM TUMBLE(DESCRIPTOR(rowtime), INTERVAL '1' DAY)` (missing table) produce localized errors with valid=false and the error span at the offending position.
    - Negative gate: the same TVF input under doris-4.x produces FATHOM-PARSE-009 with valid=false (table-ref point); under flink-2.3.0 it is valid (no double-valid).
    - Incomplete: `FROM TUMBLE(TABLE T1, DESCRIPTOR(rowtime), INTERVAL)` and `FROM TUMBLE(TABLE T1)` in editor mode produce bounded Missing nodes with print_lossless(parse(x)) == x; the recovery fixture keeps the trailing `SELECT 2` as its own statement.
    - flink-grammar TVF snapshots exist in strict/editor mode; moon test --package parity passes without --update; no doris-named snapshot changed.
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native --package parser && moon test --package parity && test -z "$(git diff --name-only -- parity/__snapshot__ | grep -vE 'flink-(grammar|lexical)\.')" && (printf "SELECT window_start, window_end FROM TUMBLE(TABLE T1, DESCRIPTOR(rowtime), INTERVAL '1' DAY) GROUP BY window_start, window_end" | _build/native/release/build/fathom-sql/fathom-sql.exe parse --dialect flink --profile flink-2.3.0 2>&1 | grep -q '"valid":true') && (printf "SELECT * FROM TUMBLE(TABLE T1, DESCRIPTOR(rowtime), INTERVAL '1' DAY)" | _build/native/release/build/fathom-sql/fathom-sql.exe parse --dialect doris --profile 4.x 2>&1 | grep -q 'FATHOM-PARSE-009')</automated>
  </verify>
  <done>Window TVF (TUMBLE/HOP/CUMULATE/SESSION, TABLE(...) wrapper, DESCRIPTOR/INTERVAL args, offset and named args) parses into WindowTvf nodes with bounded recovery; TVF rejects under Doris with FATHOM-PARSE-009; snapshots committed; Doris baseline byte-identical.</done>
</task>

<task type="auto">
  <name>parse_match_recognize sub-language: PATTERN/DEFINE/MEASURES/skip/variables/quantifiers</name>
  <files>parser/parser.mbt, parser/flink_grammar.mbt, parity/flink_grammar_test.mbt, parity/__snapshot__/flink-grammar.*.json</files>
  <read_first>
    @.planning/phases/11-flink-grammar-and-recoverable-cst/11-RESEARCH.md
    @.planning/phases/11-flink-grammar-and-recoverable-cst/11-PATTERNS.md
    @/opt/source/Fathom/parser/parser.mbt
    @/opt/source/Fathom/parser/flink_grammar.mbt
  </read_first>
  <action>In parser.mbt, extend the Flink parse_table_ref with a MATCH_RECOGNIZE suffix LOOKAHEAD (mirroring MatchRecognize(SqlNode tableRef), Parser.jj:3062-3183): after the base table reference under Flink, if the next significant token is the reserved MATCH_RECOGNIZE word, route to parse_match_recognize. Implement parse_match_recognize in parser/flink_grammar.mbt as an independent sub-language producing a MatchRecognize node (the D-02 kind) whose children are source-backed token leaves and Expression sub-trees: consume MATCH_RECOGNIZE then `(`, then the optional clauses in order — PARTITION BY expr-list (:3091-3093), ORDER BY order-item-list (:3095), MEASURES measure-column-list (:3101-3103, with MATCH_ROWTIME()/MATCH_PROCTIME()/MATCH_NUMBER() as ordinary function calls), rows-per-match ONE ROW PER MATCH | ALL ROWS PER MATCH (:3108-3115), AFTER MATCH SKIP TO NEXT ROW | TO FIRST var | TO LAST var | PAST LAST ROW (:3123-3154), PATTERN `(` [^] pattern-expr [$] `)` (:3157-3176) where pattern-expr parses ALTERNATION via |, concatenation, parenthesized groups, pattern variables with quantifiers {n}/{n,}/{n,m}/+/*/? and the reluctant ? suffix (:3314-3317), WITHIN INTERVAL literal (:3170), and DEFINE var AS expr-list (:3177-3178), then the closing `)`. Implement parse_pattern_expr in parser/flink_grammar.mbt preserving the pattern structure as source-backed children (variable names, quantifier tokens, |, ^, $, parentheses) — structure only, no pattern-variable scope/type validation (Pitfall 6). Add is_flink_match_recognize_boundary(raw: Bytes) -> Bool in parser.mbt (true for MEASURES, ONE ROW, ALL ROWS, AFTER MATCH, PATTERN, DEFINE, and `)`) in the per-family predicate shape (:1561-1565) and call recover_to_clause_boundary (:1583-1608) with it whenever a clause is malformed or the sub-language is unclosed. Add the Doris-mode negative gate: in parse_table_ref under Doris, when the identifier is MATCH_RECOGNIZE and the next token is `(`, call add_dialect_gate_diagnostic (FATHOM-PARSE-009). Add fixtures to parity/flink_grammar_test.mbt: positive (FROM Ticker MATCH_RECOGNIZE (PARTITION BY symbol ORDER BY ts MEASURES MATCH_NUMBER() AS n, A.price AS p ONE ROW PER MATCH AFTER MATCH SKIP TO NEXT ROW PATTERN (A B+ C{2,3}?) DEFINE A AS A.price > 0, B AS B.price > A.price) AS T; FROM t MATCH_RECOGNIZE (PATTERN (^ A $) DEFINE A AS A.x = 1); WITHIN INTERVAL form), negative (PATTERN () — empty pattern, DEFINE A AS ^^ — missing expression, MATCH_RECOGNIZE (PATTERN (A) — missing DEFINE), SUBSET/PERMUTE / {- -} known-limitation forms parse structurally), incomplete (FROM t MATCH_RECOGNIZE (, FROM t MATCH_RECOGNIZE (PATTERN (A+, FROM t MATCH_RECOGNIZE (PATTERN (A) DEFINE), recovery (SELECT * FROM t MATCH_RECOGNIZE ( PATTERN (A+) + `; SELECT 2` — nested unclosed recovery at the sub-language boundary). These under Doris produce FATHOM-PARSE-009 (add the negative-gate fixtures). Register snapshot additions; re-run the Doris baseline gate.</action>
  <acceptance_criteria>
    - `FROM Ticker MATCH_RECOGNIZE (PARTITION BY symbol ORDER BY ts MEASURES MATCH_NUMBER() AS n, A.price AS p ONE ROW PER MATCH AFTER MATCH SKIP TO NEXT ROW PATTERN (A B+ C{2,3}?) DEFINE A AS A.price > 0) AS T` parses valid=true under flink-2.3.0 with a MatchRecognize node, byte-identical lossless replay, and the pattern structure (variables, B+ quantifier, C{2,3}? reluctant quantifier) preserved.
    - PATTERN anchors (^ A $), WITHIN INTERVAL, and the MEASURES function calls (MATCH_NUMBER()/MATCH_ROWTIME()/MATCH_PROCTIME()) parse; the ALL ROWS PER MATCH and TO FIRST|LAST var skip forms parse.
    - Negative: `PATTERN ()` (empty pattern), `DEFINE A AS` (missing expression), and `MATCH_RECOGNIZE (PATTERN (A)` (missing DEFINE) produce localized errors with valid=false — but a syntactically valid MATCH_RECOGNIZE referencing an undeclared pattern variable is NOT rejected (Pitfall 6).
    - SUBSET, PERMUTE, and {- ... -} (PatternExclude) forms parse structurally and are classified known-limitation in the fixtures (syntax accepted, no planner semantics).
    - Recovery: `SELECT * FROM t MATCH_RECOGNIZE ( PATTERN (A+)` + `; SELECT 2` recovers at the sub-language boundary/`;` so the trailing SELECT parses as its own statement_id; incomplete editor fixtures produce bounded Missing nodes with print_lossless(parse(x)) == x.
    - Negative gate: MATCH_RECOGNIZE under doris-4.x produces FATHOM-PARSE-009; flink-grammar MATCH_RECOGNIZE snapshots exist in strict/editor mode; moon test --package parity passes without --update; no doris-named snapshot changed.
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native --package parser && moon test --package parity && test -z "$(git diff --name-only -- parity/__snapshot__ | grep -vE 'flink-(grammar|lexical)\.')" && (printf "SELECT * FROM Ticker MATCH_RECOGNIZE (PARTITION BY symbol ORDER BY ts MEASURES MATCH_NUMBER() AS n ONE ROW PER MATCH AFTER MATCH SKIP TO NEXT ROW PATTERN (A B+) DEFINE A AS A.price > 0) AS T" | _build/native/release/build/fathom-sql/fathom-sql.exe parse --dialect flink --profile flink-2.3.0 2>&1 | grep -q '"valid":true') && (printf "SELECT * FROM t MATCH_RECOGNIZE (PATTERN (A) DEFINE A AS A.x = 1)" | _build/native/release/build/fathom-sql/fathom-sql.exe parse --dialect doris --profile 4.x 2>&1 | grep -q 'FATHOM-PARSE-009')</automated>
  </verify>
  <done>MATCH_RECOGNIZE parses as a syntax-level sub-language (PATTERN/DEFINE/MEASURES/skip/variables/quantifiers, anchors, WITHIN INTERVAL) with bounded recovery at its own sync points and no pattern-variable scope validation; MATCH_RECOGNIZE rejects under Doris with FATHOM-PARSE-009; snapshots committed; Doris baseline byte-identical.</done>
</task>

<task type="auto">
  <name>TVF + MATCH_RECOGNIZE supported/known-limitation subset freeze + snapshot group finalization</name>
  <files>parity/flink_grammar_test.mbt, parity/fixtures/flink-grammar/manifest.tsv, parity/__snapshot__/flink-grammar.*.json, .planning/phases/11-flink-grammar-and-recoverable-cst/approved-changes.md</files>
  <read_first>
    @.planning/phases/11-flink-grammar-and-recoverable-cst/11-RESEARCH.md
    @/opt/source/Fathom/parity/flink_grammar_test.mbt
    @/opt/source/Fathom/parity/flink_lexical_test.mbt
  </read_first>
  <action>Complete the flink-grammar snapshot group for the phase: add the remaining TVF and MATCH_RECOGNIZE positive/negative/incomplete/recovery fixtures to parity/flink_grammar_test.mbt covering the frozen supported/known-limitation subset (RESEARCH §8): TVF offset-interval negative form (INTERVAL '-8' HOUR — syntactically accepted, classified known-limitation), MATCH_RECOGNIZE SUBSET/PERMUTE/{- -} known-limitation rows, and the TVF-in-Doris / MATCH_RECOGNIZE-in-Doris negative-gate rows. Each positive/recovery fixture asserts print_lossless(parse(x)) == x in both strict and editor mode, and the bidirectional-negative harness asserts the same input is valid in exactly one dialect (no double-valid, Pitfall 2). Update parity/fixtures/flink-grammar/manifest.tsv with the provenance rows for the new TVF/MATCH_RECOGNIZE fixtures (grammar_path + line_range referencing Parser.jj:2443-2460 and :3062-3346, source_archive_url, sha512, git_tag, git_commit from RESEARCH §10). Finalize .planning/phases/11-flink-grammar-and-recoverable-cst/approved-changes.md with the complete Phase 11 register: 008 retirement (vacant), 009 minting, flink-grammar snapshot namespace, flink-lexical re-generation, the DDL + TVF + MATCH_RECOGNIZE snapshot additions, and the known-limitation subset freeze. Generate the full snapshot set with a single `moon test --update --package parity` after the register is committed, then run the frozen Doris baseline and confirm zero doris-named drift.</action>
  <acceptance_criteria>
    - parity/flink_grammar_test.mbt contains the full four-category fixture set (positive/negative/incomplete/recovery) for TVF and MATCH_RECOGNIZE, including the known-limitation rows (TVF negative offset, SUBSET/PERMUTE/{- -}) classified as known-limitation in the fixture manifest.
    - Every positive/recovery fixture asserts print_lossless(parse(x)) == x in strict and editor mode; the bidirectional-negative harness asserts the same raw input is valid under exactly one dialect (flink XOR doris).
    - parity/fixtures/flink-grammar/manifest.tsv rows for the TVF/MATCH_RECOGNIZE fixtures carry grammar_path + line_range (Parser.jj:2443-2460 / :3062-3346) and the sha512/tag/commit from RESEARCH §10; python3 scripts/extract_flink_grammar.py validates the line refs and exits 0.
    - The approved-changes register documents every Phase 11 change; after the single --update, `git diff --name-only -- parity/__snapshot__` shows no doris-named snapshot changed (only flink-grammar.* and the registered flink-lexical.* appear).
    - moon test --package parity passes without --update after generation.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/extract_flink_grammar.py && moon test --package parity && test -z "$(git diff --name-only -- parity/__snapshot__ | grep -vE 'flink-(grammar|lexical)\.')"</automated>
  </verify>
  <done>The TVF + MATCH_RECOGNIZE supported/known-limitation subset is frozen into fixtures and snapshots, the manifest rows carry pinned provenance, the Phase 11 approved-changes register is complete, and the Doris 213-snapshot baseline is byte-identical.</done>
</task>

</tasks>

## Source Audit Coverage

| SOURCE | ID | Feature or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Window TVF + MATCH_RECOGNIZE syntax-level CST without planner claims; Doris baseline intact | 11-04 | COVERED | Final grammar slice (FLINK-05 + FLINK-06). |
| REQ | FLINK-05 | Window TVF TUMBLE/HOP/CUMULATE/SESSION: TABLE/DESCRIPTOR/INTERVAL, named args, window output columns | 11-04 | COVERED | Task 1 + subset freeze (Task 3). |
| REQ | FLINK-06 | syntax-level MATCH_RECOGNIZE CST/diagnostics: PATTERN/DEFINE/MEASURES/skip/variables/quantifiers; no planner equivalence | 11-04 | COVERED | Task 2 + subset freeze (Task 3). |
| CONTEXT | D-02 | WindowTvf/MatchRecognize CST kinds | 11-04 | COVERED | Consumes 11-01 kinds. |
| CONTEXT | D-03 | bounded recovery; sub-language sync points | 11-04 | COVERED | is_flink_match_recognize_boundary + recovery fixtures (Task 2). |
| CONTEXT | D-04 | bidirectional negative gate (TVF/MATCH_RECOGNIZE in Doris) | 11-04 | COVERED | 009 gates at the table-ref point + negative fixtures (Tasks 1-2). |
| CONTEXT | D-05 | pinned-release fixtures + snapshots | 11-04 | COVERED | manifest rows + final register (Task 3). |
| CONTEXT | D-07 | slice order FLINK-05 → FLINK-06 | 11-04 | COVERED | Both final slices in one plan. |
| RESEARCH | §5.5 | TVF syntax structure + planner-test SQL | 11-04 | COVERED | Task 1. |
| RESEARCH | §5.6 | MatchRecognize production line refs | 11-04 | COVERED | Task 2. |
| RESEARCH | §8 | supported/known-limitation subset freeze | 11-04 | COVERED | Tasks 1-3 (fixtures classify known-limitation). |
| RESEARCH | §9 | negative-gate rows for TVF/MATCH_RECOGNIZE | 11-04 | COVERED | 009 both directions (Tasks 1-2). |
| RESEARCH | Pitfall 4/6/8 | nested recovery; no pattern scope validation; legacy window functions | 11-04 | COVERED | Task 2 sync points; Pitfall 6 prohibition; Pitfall 8 note. |
| PROBE | FLINK-05 adjacency | TVF argument boundaries must not merge | 11-04 | FLAGGED | truth + span-contiguity fixtures. |
| PROBE | FLINK-05 empty | empty/truncated TVF call bounded | 11-04 | FLAGGED | truth + incomplete fixtures. |
| PROBE | FLINK-05 ordering | TVF positional argument order stable | 11-04 | FLAGGED | truth + TVF fixtures. |
| PROBE | FLINK-06 unclassified | manual review of the no-analog sub-language subset | 11-04 | FLAGGED | truth + executor verification against Parser.jj:3062-3346. |

<verification>
Run moon test --target native --package parser after each production task and moon test --package parity (no --update) after each snapshot step. Inspect: the Flink table-ref TVF branch and MATCH_RECOGNIZE suffix, parse_flink_tvf_call (TABLE/DESCRIPTOR/INTERVAL/offset/named args), parse_match_recognize + parse_pattern_expr (anchors/ALTER/concatenation/quantifiers), is_flink_match_recognize_boundary, the Doris-mode 009 gates at the table-ref point, the supported/known-limitation fixture classification, the lossless-replay assertions, the manifest provenance rows, and the complete approved-changes register committed before the single --update. Do not install packages; do not touch lexer.mbt, syntax/syntax.mbt, api/api.mbt, or dialect/flink.mbt (all complete from 11-01); do not touch the Doris parser paths.
</verification>

<success_criteria>
- A consumer can parse Window TVF (TUMBLE/HOP/CUMULATE/SESSION with TABLE/DESCRIPTOR/INTERVAL/offset/named args and window_start/window_end/window_time output columns) and syntax-level MATCH_RECOGNIZE (PATTERN/DEFINE/MEASURES/skip policy/variables/quantifiers) into lossless recoverable CST.
- The supported/known-limitation subset (RESEARCH §8) is frozen into fixtures and snapshots; no planner/execution-equivalence claim is made for either construct.
- TVF and MATCH_RECOGNIZE reject under Doris mode with FATHOM-PARSE-009, completing the bidirectional negative-gate matrix; the Doris 213-snapshot baseline is byte-identical.
</success_criteria>

<output>Create `.planning/phases/11-flink-grammar-and-recoverable-cst/11-04-SUMMARY.md` when this plan executes.</output>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flink table-ref input -> TVF/MATCH_RECOGNIZE parsing | Untrusted table-suffix constructs cross here; TVF/MATCH_RECOGNIZE must parse or reject explicitly (DIALECT-03). |
| MATCH_RECOGNIZE sub-language | Nested sub-language with its own sync points; recovery must stay bounded (Pitfall 4/8). |
| TVF/MATCH_RECOGNIZE under Doris mode | Both are Flink-only; must reject with FATHOM-PARSE-009, never misparse as an alias (D-04). |
| parity snapshots -> CI gate | New TVF/MATCH_RECOGNIZE snapshots must not touch the Doris 213 baseline (D-08). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-11-22 | Tampering | MATCH_RECOGNIZE accepted as a table alias under Doris mode | high | mitigate | Doris-mode 009 gate at the table-ref point when MATCH_RECOGNIZE is followed by `(` + negative fixture (Task 2). |
| T-11-23 | Tampering | Window TVF accepted as a Doris table-ref (TUMBLE(...) misparsed) | high | mitigate | Doris-mode 009 gate at the table-ref point for TUMBLE/HOP/CUMULATE/SESSION(` + TABLE(...) + negative fixture (Task 1). |
| T-11-24 | Denial of Service | Unclosed MATCH_RECOGNIZE/PATTERN driving unbounded recovery | high | mitigate | is_flink_match_recognize_boundary + shared consume_recovery_step/depth_allowed; nested unclosed recovery fixture (Task 2, Pitfall 4/8). |
| T-11-25 | Tampering | Pattern-variable scope/type validation rejecting syntactically valid MATCH_RECOGNIZE | medium | mitigate | no scope/type validation in the parser (Pitfall 6); prohibited in must_haves; negative fixtures never assert variable-resolution rejection. |
| T-11-26 | Spoofing | TVF/MATCH_RECOGNIZE parsing implying planner/execution equivalence | high | mitigate | syntax-level CST only; prohibitions + fixture classification (FLINK-05/06, Pitfall 8). |
| T-11-27 | Tampering | TVF/MATCH_RECOGNIZE snapshots leaking into the Doris namespace | medium | mitigate | independent flink-grammar naming + final register; git diff per-group check (D-08). |
| T-11-28 | Tampering | Fabricated MATCH_RECOGNIZE production line refs (no-analog sub-language) | high | mitigate | extract_flink_grammar.py validates line refs against Parser.jj:3062-3346; manifest sha512/tag/commit (D-05, Pitfall 5). |
| T-11-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new runtime dependencies; no package installs in this plan. |
</threat_model>

## Artifacts this phase produces

Files: parser/parser.mbt (modified — Flink table-ref TVF branch + MATCH_RECOGNIZE suffix, Doris-mode 009 gates at the table-ref point, is_flink_match_recognize_boundary), parser/flink_grammar.mbt (modified — parse_flink_tvf_call, parse_match_recognize, parse_pattern_expr), parity/flink_grammar_test.mbt (modified — TVF + MATCH_RECOGNIZE four-category fixtures + known-limitation classification + bidirectional-negative harness), parity/__snapshot__/flink-grammar.*.json (new, generated), parity/fixtures/flink-grammar/manifest.tsv (modified — TVF/MATCH_RECOGNIZE provenance rows), .planning/phases/11-flink-grammar-and-recoverable-cst/approved-changes.md (modified — complete Phase 11 register).

Symbols (parser/parser.mbt): Flink parse_table_ref TVF branch + MATCH_RECOGNIZE suffix LOOKAHEAD, is_flink_match_recognize_boundary(raw: Bytes) -> Bool, Doris-mode add_dialect_gate_diagnostic calls at the table-ref point.
Symbols (parser/flink_grammar.mbt): parse_flink_tvf_call(cursor, state, source, statement_id), parse_match_recognize(cursor, state, source, statement_id), parse_pattern_expr(cursor, state, source, statement_id).
