---
phase: 12-cross-dialect-corpus-and-parity-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - parity/fixtures/flink/manifest.tsv
  - parity/fixtures/flink/flink-2.3.0/*.sql
  - parity/fixtures/flink/flink-2.1.3/*.sql
  - parity/fixtures/flink/flink-1.20.5/*.sql
  - parity/fixtures/flink/doris-4.x/*.sql
  - scripts/verify_corpus.py
  - scripts/extract_flink_grammar.py
  - scripts/extract_flink_lexical.py
  - corpus/flink-coverage.tsv
  - corpus/tools/generate_corpus_report.py
  - corpus/CORPUS-REPORT.md
autonomous: false
requirements: [CORPUS-01, PARITY-03]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A maintainer can inspect a release-pinned Flink corpus at parity/fixtures/flink/manifest.tsv where every fixture row records fixture_id, dialect, profile, exact_release, calcite_version, parser_config, source_archive_url, sha512, git_tag, git_commit, source_url, heading, retrieval_date, category, expected_status, fixture_sha256, grammar_path, line_range, and mode — and category is exactly one of the 6 values positive | negative | recovery | known-limitation | catalog-prerequisite | planner-prerequisite (CORPUS-01, D-01)."
    - "A maintainer can run `python3 scripts/verify_corpus.py --check` entirely offline (no network, Doris FE, Flink cluster, or database) and it exits 0 only when: the header matches, every row's calcite_version/parser_config match the in-repo pin table for dialect/flink.mbt FlinkProfileMetadata, category is in the 6-value enum, expected_status is consistent with category (positive→valid, negative→error, recovery→recovered, known-limitation/catalog-prerequisite/planner-prerequisite→valid), fixture_sha256 matches the committed .sql bytes, and both strict+editor snapshots exist per row with the dialect-correct snapshot segment (PARITY-03, D-06)."
    - "All existing 97 flink-grammar and 13 flink-lexical fixtures are present in the unified manifest with their 6-category mapping — the D-02 migration loses no fixture; each committed .sql file byte-matches the embedded b\"...\" raw literal in parity/flink_grammar_test.mbt and parity/flink_lexical_test.mbt, verified by the extended extract_flink_grammar.py (D-02, D-08 embedded-raw provenance)."
    - "The cross-dialect coverage report (corpus/CORPUS-REPORT.md regenerated by corpus/tools/generate_corpus_report.py from corpus/flink-coverage.tsv) reports parser acceptance and engine-semantic prerequisite as two distinct totals — catalog-prerequisite, planner-prerequisite, and known-limitation rows are never counted as engine-supported, and the report never contains a full-compatibility claim (PARITY-03, D-01/D-06)."
    - "A missing release archive under /tmp/flink-research/ is reported as an archive-not-present research-fixture status and does not fail the gate; the resident fixture_sha256 over the committed .sql files remains the CI-checkable hash — no hash or provenance value is ever fabricated (D-06, Pitfall 3, STATE.md unavailable-offline discipline)."
    - "[probe CORPUS-01 unclassified] flagged-unverified: the edge-probe left CORPUS-01 unclassified — the manifest/verifier contract above is authored to satisfy D-01 in full (all 6 categories, generic-acceptance-never-engine-support) and remains flagged for manual review."
    - "[probe PARITY-03 concurrency] flagged-unverified backstop: { statement: interrupting or running the offline gates in parallel must never corrupt or half-write the corpus/verifier state — verify_corpus.py is read-only over pinned artifacts, so an interrupted run leaves no partial state, and an interrupted or concurrent run must not fabricate a pass; verification: backstop }"
  artifacts:
    - path: "scripts/verify_corpus.py"
      provides: "Offline manifest/hash verifier (stdlib-only): --check mode, exact header match, PINS table vs dialect/flink.mbt, 6-category enum, expected_status consistency, fixture_sha256 sha256_file check, archive sha512 present-verify / absent-archive-not-present, snapshot completeness (strict+editor per row, dialect-correct segment), non-empty guard, problems+ok exit gate"
    - path: "parity/fixtures/flink/manifest.tsv"
      provides: "Unified 19-column release-pinned Flink corpus manifest, 110 fixture rows (97 flink-grammar + 13 flink-lexical), 6-category mapping, fixture_sha256 pins"
    - path: "parity/fixtures/flink/{flink-2.3.0,flink-2.1.3,flink-1.20.5,doris-4.x}/{fixture_id}.sql"
      provides: "Committed raw-SQL fixture files (110) extracted from the embedded b\"...\" literals — the hashable object for fixture_sha256 (D-08 embedded-raw provenance)"
    - path: "corpus/flink-coverage.tsv"
      provides: "Semantic-distinction coverage matrix (profile/category/fixture_count/parser_accepted/parser_rejected/recovery/prerequisite/coverage_note) with prerequisite explicitly engine-supported=0"
    - path: "corpus/tools/generate_corpus_report.py"
      provides: "Extension reading corpus/flink-coverage.tsv and rendering the bilingual report; prerequisite hard rule (catalog/planner/known-limitation never engine-supported); --check staleness byte-compare; full-compatibility claim scan"
    - path: "corpus/CORPUS-REPORT.md"
      provides: "Regenerated deterministic bilingual report including the Flink corpus section"
    - path: "scripts/extract_flink_grammar.py"
      provides: "Extension: parses b\"...\" literals in flink_grammar_test.mbt and byte-compares against parity/fixtures/flink/**/*.sql; 6-category enum validation on the manifest"
    - path: "scripts/extract_flink_lexical.py"
      provides: "Extension: 13 flink-lexical fixture rows 6-category + token-source column validation"
  key_links:
    - from: "parity/fixtures/flink/manifest.tsv category + expected_status columns"
      to: "scripts/verify_corpus.py 6-category enum + consistency check"
      via: "enum membership and expected_status↔category mapping — a mislabeled planner-prerequisite fixture as positive fails --check (Pitfall 2)"
    - from: "parity/fixtures/flink/{profile}/{fixture_id}.sql files"
      to: "parity/fixtures/flink/manifest.tsv fixture_sha256 column"
      via: "hashlib.sha256 over the committed bytes — the resident CI-checkable hash (D-06)"
    - from: "parity/flink_grammar_test.mbt + flink_lexical_test.mbt embedded b\"...\" literals"
      to: "parity/fixtures/flink/**/*.sql files"
      via: "extract_flink_grammar.py byte-compare — embedded-raw provenance, .sql is an additive artifact never replacing embedded bytes (Pitfall 6)"
    - from: "corpus/flink-coverage.tsv prerequisite column"
      to: "corpus/tools/generate_corpus_report.py report renderer"
      via: "catalog/planner/known-limitation rows counted as prerequisite, never engine-supported; report regenerated deterministically and --check-verified"
    - from: "parity/__snapshot__/ snapshot files"
      to: "scripts/verify_corpus.py snapshot completeness check"
      via: "flink rows {fixture_id}.{profile}.{mode}.json and doris rows {fixture_id}.doris-{profile}.{mode}.json — every manifest row must have both strict and editor snapshots"
  prohibitions:
    - "The Flink corpus manifest and coverage report MUST NOT claim Flink engine support for generic SQL that the parser merely accepts — catalog-prerequisite, planner-prerequisite, and known-limitation fixtures must never be reported as engine-supported (D-01 core; transparency)."
    - "The D-02 migration MUST NOT silently drop, rename, or alter any existing flink-grammar/flink-lexical fixture or its embedded raw bytes — the unified manifest is an additive reorganization that preserves every existing fixture_id and snapshot filename (Pitfall 6)."
    - "Release/hash/provenance values (sha512, git_commit, fixture_sha256, retrieval_date) MUST NOT be fabricated or approximated — unavailable values are recorded as unavailable-offline or known-gap, never invented (STATE.md discipline; repudiation)."
---

<objective>
统一 Flink corpus 结构（CORPUS-01）并把 Phase 10/11 已建的两份 flink fixture/manifest 迁入单一 release-pinned 契约，同时落地离线 manifest/hash 验证器与语义区分覆盖报告（PARITY-03 的数据与工具层）。本 plan 是 Phase 12 的 tracer 切片：一条端到端路径 = 「统一 manifest 行 → 提交的 .sql fixture 字节 → verify_corpus.py --check（header/pins/6 类枚举/expected_status 一致性/fixture_sha256/归档 sha512/快照完整性）→ flink-coverage.tsv + generate_corpus_report.py 渲染语义区分报告」——先以覆盖全部 6 类的最小 seed 集验证，再横向扩展到全部 110 个既有 fixture（D-02 不丢任何 fixture）。

Purpose: CORPUS-01 要求维护者可审计 release-pinned Flink corpus（release/tag/commit、Calcite 版本/config、URL/标题/检索日期/hash、期望状态、6 类分类）；PARITY-03 要求离线门禁 + parser-接受 vs 引擎-语义 区分。Research 现状盘点表明：flink-grammar manifest（97 行）与 flink-lexical manifest（4 行 release 级）缺 5 列（retrieval_date/source_url/heading/category/expected_status）+ fixture_sha256 + mode；4 类→6 类重标是核心语义裁决（generic SQL 接受 ≠ Flink 引擎支持）。本 plan 把缺列补齐、把全部 fixture 落盘为可 hash 的 .sql 文件、把 6 类语义固化为可校验枚举，并让覆盖报告把 prerequisite 显式报为引擎支持=0。
Output: parity/fixtures/flink/manifest.tsv（110 行统一 manifest）、parity/fixtures/flink/{profile}/*.sql（110 文件）、scripts/verify_corpus.py、corpus/flink-coverage.tsv、corpus/tools/generate_corpus_report.py 扩展、corpus/CORPUS-REPORT.md 增补、scripts/extract_flink_grammar.py 与 extract_flink_lexical.py 扩展。
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-cross-dialect-corpus-and-parity-gates/12-CONTEXT.md
@.planning/phases/12-cross-dialect-corpus-and-parity-gates/12-RESEARCH.md
@.planning/phases/12-cross-dialect-corpus-and-parity-gates/12-PATTERNS.md
@/opt/source/Fathom/parity/flink_grammar_test.mbt
@/opt/source/Fathom/parity/flink_lexical_test.mbt
@/opt/source/Fathom/parity/fixtures/flink-grammar/manifest.tsv
@/opt/source/Fathom/parity/fixtures/flink-lexical/manifest.tsv
@/opt/source/Fathom/corpus/tools/generate_corpus_report.py
@/opt/source/Fathom/corpus/coverage.tsv
@/opt/source/Fathom/dialect/flink.mbt

This is the phase wave-1 tracer and carries the phase's offline-gate contract. D-01 (manifest schema + 6-category semantics, costly) drives the manifest header and the verify_corpus.py enum; D-02 (fixture migration, reversible) drives the additive .sql + manifest reorganization; D-06 (offline gate shape, one-way — CI 门禁形态是发布契约) is gated by Task 1 checkpoint. The unified manifest lives at parity/fixtures/flink/manifest.tsv (RESEARCH §5.1 recommendation, A1: parity 统一结构 per CONTEXT code_context); the existing per-area manifests (flink-grammar/manifest.tsv, flink-lexical/manifest.tsv) stay in place as the extraction source and are never deleted (Pitfall 6 — only add columns, never change fixture_id/snapshot filenames). The 4→6 category mapping is the frozen recommendation of RESEARCH §5.4 (positive 62 → ~43 positive / 13 planner-prerequisite / 3 catalog-prerequisite / 3 known-limitation; negative 18 all preserved incl. 7 FATHOM-PARSE-009 gates; incomplete 12 + recovery 5 → 17 recovery) with the priority rule negative > recovery > known-limitation > planner-prerequisite > catalog-prerequisite > positive.

> **Contribution hooks:** (1) API Coverage checkpoint — NOT detected: Phase 12 integrates NO external service API; /tmp/flink-research/ archives are research-time fixtures (never a runtime dependency), so a reasoned no-external-API declaration replaces any COVERAGE.md matrix. (2) Assumption-Delta scan — `detected: false` (no pluralization/optional/chosen signals): no promote-vs-add-alongside decision is required; the unified manifest is the locked new data structure, not an assumption-driven promotion. (3) Schema Push gate — no ORM schema files in this MoonBit parser project; skipped silently.

> **Scope note (breadth):** Task 3 creates ~110 .sql fixture files and 110 manifest rows. This is one mechanical extraction from the embedded b"..." literals (flink_grammar_test.mbt / flink_lexical_test.mbt) following the corpus/doris-*/ + baseline-hashes.txt precedent (A6), not 110 hand-written artifacts — the file count is data generation, not cognitive breadth. The 52k-token estimate is ≈52% of the phase budget; a split was considered and rejected because the manifest, .sql files, and verifier must land as one consistent corpus (verify_corpus.py --check cannot go green on a partial manifest).
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Confirm the offline corpus verifier contract form — D-06 one-way door</name>
  <decision>The offline manifest/hash verifier form that becomes the CI-wired release contract: a single stdlib entry `python3 scripts/verify_corpus.py --check` that validates the unified manifest (header, pins, 6-category enum, expected_status consistency, fixture_sha256, archive sha512, snapshot completeness) with the archive-missing behavior as archive-not-present (research fixture, NOT a failure) and fixture_sha256 over the committed .sql files as the resident CI-checkable hash. extract_flink_grammar.py / extract_flink_lexical.py stay out of CI (they need /tmp/flink-research/), remaining local maintainer tools (RESEARCH Open Question 4).</decision>
  <context>D-06 is one-way because the CI gate shape becomes a release contract (CONTEXT D-06). Three sub-forms are decided here: (a) single-entry verify_corpus.py vs splitting into verify_manifest.py + verify_hashes.py — RESEARCH Alternatives recommends the single entry and --check mode; (b) archive-missing semantics — Pitfall 3 forbids both failing on a missing research fixture and fabricating a hash; the resident fixture_sha256 is what keeps CI green without /tmp/flink-research/; (c) whether extract_* scripts enter CI — Open Question 4 recommends they do NOT (they need the research cache). The snapshot naming is fixed by the existing committed snapshots (flink rows {fixture_id}.{profile}.{mode}.json, doris rows {fixture_id}.doris-{profile}.{mode}.json) and must not change.</decision>
  <options>
    <option id="option-a">
      <name>Single-entry verify_corpus.py --check with fixture_sha256 resident hash; extract_* stay local (recommended — RESEARCH recommendation)</name>
      <pros>One CI step, one exit gate; fixture_sha256 over committed .sql files is CI-verifiable without the research cache; archive sha512 still verified locally when present; extract_* keep their /tmp/flink-research/ provenance role without blocking CI.</pros>
      <cons>verify_corpus.py carries the full 8-role responsibility (header/pins/enum/expected_status/hash/archive/snapshot/guard) — one script to maintain.</cons>
    </option>
    <option id="option-b">
      <name>Split verify_manifest.py + verify_hashes.py, and wire extract_* into CI</name>
      <pros>Smaller scripts; extraction provenance runs on every push.</pros>
      <cons>Two CI steps to keep in sync; extract_* fail without /tmp/flink-research/ (CI has no cache) — wiring them reds CI; contradicts Open Question 4 and Pitfall 3.</cons>
    </option>
  </options>
  <resume-signal>Select option-a (single-entry verifier, extract_* stay local) or describe a modified offline-gate form; the tracer task is authored only after this confirmation.</resume-signal>
</task>

<task type="tracer">
  <name>End-to-end offline corpus verification on an all-6-category seed set</name>
  <files>parity/fixtures/flink/flink-2.3.0/flink-grammar.select-cte-join-agg.sql, parity/fixtures/flink/flink-2.3.0/flink-grammar.insert-distributed.sql, parity/fixtures/flink/flink-2.3.0/flink-grammar.match-recognize-subset.sql, parity/fixtures/flink/flink-2.3.0/flink-grammar.create-function.sql, parity/fixtures/flink/flink-2.3.0/flink-grammar.tvf-tumble-day.sql, parity/fixtures/flink/doris-4.x/flink-lexical.double-quote.sql, parity/fixtures/flink/manifest.tsv, scripts/verify_corpus.py, corpus/flink-coverage.tsv, corpus/tools/generate_corpus_report.py, corpus/CORPUS-REPORT.md</files>
  <read_first>
    @.planning/phases/12-cross-dialect-corpus-and-parity-gates/12-CONTEXT.md
    @.planning/phases/12-cross-dialect-corpus-and-parity-gates/12-RESEARCH.md
    @.planning/phases/12-cross-dialect-corpus-and-parity-gates/12-PATTERNS.md
    @/opt/source/Fathom/parity/flink_grammar_test.mbt
    @/opt/source/Fathom/parity/flink_lexical_test.mbt
    @/opt/source/Fathom/parity/fixtures/flink-grammar/manifest.tsv
    @/opt/source/Fathom/corpus/tools/generate_corpus_report.py
    @/opt/source/Fathom/corpus/coverage.tsv
  </read_first>
  <action>Build the thinnest end-to-end path that proves the whole corpus + verifier + report stack: (1) Create parity/fixtures/flink/ with the 4 profile subdirectories (flink-2.3.0, flink-2.1.3, flink-1.20.5, doris-4.x). Extract the raw SQL bytes of the seed fixtures from the embedded b"..." literals in parity/flink_grammar_test.mbt and parity/flink_lexical_test.mbt into .sql files — seed set = flink-grammar.select-cte-join-agg (positive), flink-grammar.insert-distributed (negative, the FATHOM-PARSE-009 dialect gate), one recovery fixture (an existing incomplete fixture, e.g. the truncated SELECT the test marks category incomplete), flink-grammar.match-recognize-subset (known-limitation), flink-grammar.create-function (catalog-prerequisite), flink-grammar.tvf-tumble-day (planner-prerequisite), and flink-lexical.double-quote under doris-4.x (proves the dialect column and the doris snapshot segment). (2) Create parity/fixtures/flink/manifest.tsv with the exact 19-column header fixture_id, dialect, profile, exact_release, calcite_version, parser_config, source_archive_url, sha512, git_tag, git_commit, source_url, heading, retrieval_date, category, expected_status, fixture_sha256, grammar_path, line_range, mode; fill the seed rows — calcite_version/parser_config from the flink-grammar manifest (1.36.0 + Lex.JAVA,identifierMaxLength=256,conformance=FlinkSqlConformance.DEFAULT for flink-2.3.0; the doris-4.x row uses N/A per the flink-lexical manifest doris row), category from the RESEARCH §5.4 mapping, expected_status valid/error/recovered per category, fixture_sha256 = sha256 of each .sql file, mode = strict,editor for every row, grammar_path/line_range copied from the existing flink-grammar manifest where present, source_url/heading/retrieval_date populated from the pinned archive URL and the grammar production name (e.g. QueryOrExpr/WithList). (3) Create scripts/verify_corpus.py following the RESEARCH Common Operation 1 skeleton and the 12-PATTERNS Pattern Assignments: exact header prefix check on the first 5 columns, non-empty guard (empty manifest exits 1), PINS table mirroring dialect/flink.mbt FlinkProfileMetadata (flink-2.3.0=1.36.0, flink-2.1.3=1.34.0, flink-1.20.5=1.32.0, all with the same parser_config; doris rows skip the calcite/config pin), category enum membership over the exact 6 values positive|negative|recovery|known-limitation|catalog-prerequisite|planner-prerequisite, expected_status↔category consistency (positive→valid, negative→error, recovery→recovered, known-limitation/catalog-prerequisite/planner-prerequisite→valid), sha256_file over each .sql comparing fixture_sha256, archive sha512 present-verify/absent-archive-not-present (skip-not-fail, mirroring extract_flink_lexical.py validate_manifest), snapshot completeness per row (both strict and editor; segment = profile for dialect flink, doris-<profile> for dialect doris), problems+ok exit gate (ok line carries the verified row count), no network calls. (4) Create corpus/flink-coverage.tsv with the schema profile, category, fixture_count, parser_accepted, parser_rejected, recovery, prerequisite, coverage_note — seed rows for the 6 seed categories following the RESEARCH §8.3 form. (5) Extend corpus/tools/generate_corpus_report.py to read corpus/flink-coverage.tsv, render a Flink corpus section into corpus/CORPUS-REPORT.md, add the prerequisite hard rule (catalog-prerequisite/planner-prerequisite/known-limitation rows never counted as engine-supported; the report must print the two totals parser-accepted vs engine-semantic-prerequisite separately), keep the existing full-compatibility claim scan and the --check staleness byte-compare, and regenerate the report. Then run python3 scripts/verify_corpus.py --check and python3 corpus/tools/generate_corpus_report.py --check until both are green on the seed set. This is production-quality gate code, not a prototype — verify_corpus.py is the resident offline verifier for the whole phase.</action>
  <acceptance_criteria>
    - CLI: `python3 scripts/verify_corpus.py --check` exits 0 with an ok line reporting the seed row count verified offline; `python3 corpus/tools/generate_corpus_report.py --check` exits 0 after regenerating corpus/CORPUS-REPORT.md (staleness byte-compare passes); `python3 -m py_compile scripts/verify_corpus.py corpus/tools/generate_corpus_report.py` succeeds.
    - The manifest seed rows cover all 6 categories (positive, negative, recovery, known-limitation, catalog-prerequisite, planner-prerequisite) and both dialects (flink via the grammar rows, doris via flink-lexical.double-quote), each with the 19 columns populated and fixture_sha256 matching the committed .sql bytes.
    - expected_status consistency: the recovery row carries recovered, negative carries error, and the rest carry valid — verify_corpus.py --check passes on this mapping and fails (exit 1, problem line) when a fixture is relabeled inconsistently (e.g. a planner-prerequisite row given expected_status recovered).
    - Snapshot completeness: verify_corpus.py --check passes when both strict and editor snapshots exist for each seed row (flink-grammar.*.flink-2.3.0.{strict,editor}.json and flink-lexical.double-quote.doris-4.x.{strict,editor}.json) and fails when one is missing.
    - The coverage report distinguishes parser acceptance from engine semantic prerequisite: the planner-prerequisite and catalog-prerequisite and known-limitation seed rows appear under the prerequisite total, never under engine-supported; `python3 corpus/tools/generate_corpus_report.py --check` enforces this and fails on a report that counts a prerequisite row as engine-supported.
    - Doris baseline untouched by this task: `moon test --package parity` (no --update) still passes — the seed adds new parity/fixtures/flink data files and never modifies the existing flink_grammar_test.mbt / flink_lexical_test.mbt embedded bytes or the Doris 213 snapshots.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/verify_corpus.py --check && python3 corpus/tools/generate_corpus_report.py --check && python3 -m py_compile scripts/verify_corpus.py corpus/tools/generate_corpus_report.py && moon test --package parity && test -s parity/fixtures/flink/manifest.tsv && test "$(wc -l < corpus/flink-coverage.tsv)" -ge 2</automated>
  </verify>
  <done>The end-to-end path works: the all-6-category seed manifest + committed .sql files pass the offline verifier (header/pins/enum/expected_status/hash/snapshot), the coverage report renders parser-acceptance vs engine-prerequisite as distinct totals with the prerequisite hard rule, and the Doris baseline stays byte-identical.</done>
</task>

<task type="auto">
  <name>Full D-02 migration — all 110 fixtures into the unified manifest + extractor provenance + full coverage</name>
  <files>parity/fixtures/flink/manifest.tsv, parity/fixtures/flink/flink-2.3.0/*.sql, parity/fixtures/flink/flink-2.1.3/*.sql, parity/fixtures/flink/flink-1.20.5/*.sql, parity/fixtures/flink/doris-4.x/*.sql, scripts/extract_flink_grammar.py, scripts/extract_flink_lexical.py, corpus/flink-coverage.tsv, corpus/CORPUS-REPORT.md</files>
  <read_first>
    @.planning/phases/12-cross-dialect-corpus-and-parity-gates/12-RESEARCH.md
    @.planning/phases/12-cross-dialect-corpus-and-parity-gates/12-PATTERNS.md
    @/opt/source/Fathom/parity/flink_grammar_test.mbt
    @/opt/source/Fathom/parity/flink_lexical_test.mbt
    @/opt/source/Fathom/parity/fixtures/flink-grammar/manifest.tsv
    @/opt/source/Fathom/parity/fixtures/flink-lexical/manifest.tsv
    @/opt/source/Fathom/corpus/tools/generate_corpus_report.py
  </read_first>
  <action>Complete the D-02 migration so the unified manifest covers every existing fixture with zero loss. (1) Extract the remaining flink-grammar raw SQL bytes from parity/flink_grammar_test.mbt b"..." literals (all 97 fixture rows) into parity/fixtures/flink/{flink-2.3.0,flink-2.1.3,flink-1.20.5}/{fixture_id}.sql and the flink-lexical raw bytes from parity/flink_lexical_test.mbt (all 13 fixture entries, including the 6 doris-4.x control rows into the doris-4.x profile dir) — the extraction is mechanical from the embedded literals, never hand-edited; snapshot filenames and fixture_id prefixes (flink-grammar. / flink-lexical.) are preserved unchanged (Pitfall 6). (2) Extend parity/fixtures/flink/manifest.tsv to all 110 rows: expand the flink-lexical release-level manifest into per-fixture rows (13 rows with dialect/profile/token-source line/6-category), carry the 97 flink-grammar provenance rows (sha512/tag/commit/grammar_path/line_range) forward, add the 6-category and expected_status and source_url/heading/retrieval_date and fixture_sha256 and mode columns per the RESEARCH §5.4 mapping table (positive ~43, negative 18 incl. the 7 FATHOM-PARSE-009 dialect-gate rows, recovery 17, known-limitation 3, catalog-prerequisite 3, planner-prerequisite 13, plus the 13 lexical rows with their per-dialect parse-status categories); every fixture_sha256 is computed from the actual committed .sql bytes. (3) Extend scripts/extract_flink_grammar.py: parse the b"..." literals out of flink_grammar_test.mbt and byte-compare each against the corresponding parity/fixtures/flink/**/*.sql file (embedded-raw provenance, D-08), and validate the 6-category enum on the unified manifest rows it knows; extend scripts/extract_flink_lexical.py to validate the 13 expanded flink-lexical fixture rows (6-category + token source column) against the flink-lexical manifest. (4) Regenerate corpus/flink-coverage.tsv to aggregate the full 110-row classification (per profile per category: fixture_count, parser_accepted, parser_rejected, recovery, prerequisite, coverage_note — prerequisite column explicitly engine-supported=0 for catalog/planner/known-limitation), then regenerate corpus/CORPUS-REPORT.md via the extended generator. (5) Run python3 scripts/verify_corpus.py --check, python3 scripts/extract_flink_grammar.py, python3 scripts/extract_flink_lexical.py, and python3 corpus/tools/generate_corpus_report.py --check until all green. Commit the .sql extraction, manifest rows, and extractor extensions together as one cohesive change set.</action>
  <acceptance_criteria>
    - `python3 scripts/verify_corpus.py --check` exits 0 on the full 110-row manifest: every row's calcite_version/parser_config matches the PINS table (doris rows exempt), category is in the 6-value enum, expected_status is consistent, fixture_sha256 matches the committed .sql bytes, and both strict+editor snapshots exist per row (flink-grammar 194 + flink-lexical 26 snapshot files accounted for by the 110 rows × the relevant mode/dialect segments).
    - `python3 scripts/extract_flink_grammar.py` and `python3 scripts/extract_flink_lexical.py` both exit 0 with ok lines — the committed .sql files byte-match the embedded b"..." literals (embedded-raw provenance) and the 6-category enum holds for every manifest row; a deliberately altered .sql byte makes extract_flink_grammar.py exit 1.
    - Zero fixture loss: every fixture_id present in parity/fixtures/flink-grammar/manifest.tsv (97) and every flink-lexical fixture entry (13) appears in the unified manifest; `comm`-style set comparison against the two source manifests shows no missing id.
    - Snapshot namespace untouched: `git diff --name-only -- parity/__snapshot__` shows no file changed by this task (the migration adds data files under parity/fixtures/flink/ and never touches parity/__snapshot__); `moon test --package parity` (no --update) passes.
    - corpus/flink-coverage.tsv aggregates the full classification with the prerequisite column; the regenerated corpus/CORPUS-REPORT.md renders the bilingual report and `python3 corpus/tools/generate_corpus_report.py --check` passes the staleness byte-compare and the full-compatibility claim scan.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/verify_corpus.py --check && python3 scripts/extract_flink_grammar.py && python3 scripts/extract_flink_lexical.py && python3 corpus/tools/generate_corpus_report.py --check && moon test --package parity && test -z "$(git diff --name-only -- parity/__snapshot__)" && test "$(python3 -c 'import csv;print(sum(1 for _ in csv.DictReader(open(\"parity/fixtures/flink/manifest.tsv\"),delimiter=\"\\t\")))')" -eq 110</automated>
  </verify>
  <done>The full 110-fixture unified corpus exists (manifest + .sql files + 6-category classification + fixture_sha256), the offline verifier and the extractor provenance checks are green, the semantic-distinction coverage report renders both dialects, the flink snapshot namespace is untouched, and the Doris baseline is byte-identical.</done>
</task>

</tasks>

## Source Audit Coverage

| SOURCE | ID | Feature or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Maintainers audit reproducible Doris/Flink coverage; release only within explicit parity contracts | 12-01 | COVERED | Unified corpus + verifier + semantic-distinction report (release-pinned, offline). |
| REQ | CORPUS-01 | Release-pinned Flink corpus manifest with release/tag/commit, Calcite version/config, URL, heading, retrieval date, hash, expected status, 6 categories | 12-01 | COVERED | 19-column unified manifest; seed (Task 2) + full 110-row (Task 3). |
| REQ | PARITY-03 | Offline CI/release checks from pinned artifacts; coverage distinguishes parser acceptance from engine semantic support | 12-01 | PARTIAL (data+tool layer) | verify_corpus.py + flink-coverage.tsv + report extension here; CI wiring in 12-03. |
| CONTEXT | D-01 | Unified release-pinned manifest + 6-category semantics; generic SQL 接受 ≠ Flink 引擎支持 (costly) | 12-01 | COVERED | Manifest header + category enum + expected_status + coverage prerequisite hard rule. |
| CONTEXT | D-02 | Migrate flink-lexical + flink-grammar fixtures/snapshots into unified corpus by 6 categories; no fixture loss (reversible) | 12-01 | COVERED | Task 3 full 110-fixture migration + zero-loss set check. |
| CONTEXT | D-06 | Offline manifest/hash verifier; release-pinned artifacts only; coverage distinguishes parser vs engine prereq (one-way) | 12-01 | COVERED (form + tool) | Task 1 checkpoint (one-way form) + Task 2/3 verify_corpus.py + coverage report; CI wiring in 12-03. |
| CONTEXT | D-07 | docs/source/Calcite conflicts explicit; no silent batch snapshot updates | 12-01 | NOT IN SCOPE (12-02) | D-07 register/approval flow lives in 12-02 diff harness. |
| RESEARCH | §5.1 | Unified manifest schema (add 5 columns + fixture_sha256 + mode + dialect) | 12-01 | COVERED | 19-column header authored in Task 2/3. |
| RESEARCH | §5.3 | 6-category semantics + priority rule + generic-acceptance≠engine-support | 12-01 | COVERED | verify_corpus.py enum + consistency check + report hard rule. |
| RESEARCH | §5.4 | Existing fixture → 6-category mapping (positive split, 009 gates, recovery) | 12-01 | COVERED | Task 3 full mapping (A2/A3/A4/A5 flagged assumptions carried in RESEARCH). |
| RESEARCH | §5.5 | Hash verification design (archive sha512 + fixture sql sha256) | 12-01 | COVERED | sha256_file + archive present-verify/absent-skip. |
| RESEARCH | §8.1 | verify_corpus.py 8-role offline verifier design | 12-01 | COVERED | Task 2 tracer implements all 8 roles. |
| RESEARCH | §8.3 | Semantic-distinction coverage report + prerequisite hard rule | 12-01 | COVERED | flink-coverage.tsv + generate_corpus_report.py extension. |
| RESEARCH | §9.1/9.2 | Implementation surface: verify_corpus.py, flink fixture .sql, flink manifest, flink-coverage.tsv, extract_* extensions, report extension | 12-01 | COVERED | All landed in this plan. |
| RESEARCH | Pitfall 1 | No silent bulk --update | 12-01 | NOT IN SCOPE (12-02) | CI/register enforcement in 12-02. |
| RESEARCH | Pitfall 2 | Category mislabel — parser acceptance reported as engine support | 12-01 | COVERED | enum + expected_status + report hard rule. |
| RESEARCH | Pitfall 3 | Hash drift / archive-missing handled or fabricated | 12-01 | COVERED | archive-not-present status + fixture_sha256 resident hash + never-fabricate. |
| RESEARCH | Pitfall 6 | Break existing passing gates during reorg | 12-01 | COVERED | additive .sql + unchanged snapshot filenames + Doris zero-drift. |
| RESEARCH | Pitfall 7 | 6-category inconsistency across fixtures | 12-01 | COVERED | verify_corpus.py enum + consistency check. |
| RESEARCH | Pitfall 8 | Gate blind spot (0 files) | 12-01 | COVERED | non-empty guard on manifest/coverage. |
| PROBE | CORPUS-01 unclassified | Edge-probe left CORPUS-01 unclassified | 12-01 | FLAGGED | must_haves flagged-unverified truth; contract authored to satisfy D-01 in full. |
| PROBE | PARITY-03 concurrency | Interrupted/parallel gate runs guarantee | 12-01 | FLAGGED | must_haves backstop marker; read-only verifier leaves no partial state. |
| PROBE | PARITY-01 unclassified | Edge-probe left PARITY-01 unclassified | 12-01 | NOT IN SCOPE (12-02) | flagged in 12-02. |
| PROBE | PARITY-02 unclassified | Edge-probe left PARITY-02 unclassified | 12-01 | NOT IN SCOPE (12-03) | flagged in 12-03. |

<verification>
Run python3 scripts/verify_corpus.py --check, python3 scripts/extract_flink_grammar.py, python3 scripts/extract_flink_lexical.py, and python3 corpus/tools/generate_corpus_report.py --check after each task; run moon test --package parity (no --update) after every data-affecting step and git diff --name-only -- parity/__snapshot__ to confirm the snapshot namespace is untouched. Inspect: the 19-column manifest header and a sample of the 110 rows (category/expected_status/fixture_sha256), the verify_corpus.py problems+ok exit path and its non-empty guard, the .sql byte-compare in extract_flink_grammar.py, the flink-coverage.tsv prerequisite column, and the regenerated CORPUS-REPORT.md bilingual section.
</verification>

<success_criteria>
- A maintainer can inspect the unified 110-row release-pinned Flink corpus manifest with all CORPUS-01 fields and exactly the 6-category classification, with every fixture's committed .sql bytes hash-pinned.
- python3 scripts/verify_corpus.py --check runs fully offline and exits 0 only when the manifest structure, release pins, category enum, expected-status consistency, fixture hashes, and snapshot completeness all hold.
- The coverage report distinguishes parser acceptance from engine semantic prerequisite for both dialects, with catalog/planner/known-limitation never counted as engine-supported.
- The Doris 213-snapshot baseline is byte-identical and the flink snapshot namespace is untouched by the migration.
</success_criteria>

<output>Create `.planning/phases/12-cross-dialect-corpus-and-parity-gates/12-01-SUMMARY.md` when this plan executes.</output>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest/coverage TSV -> verify_corpus.py | Maintainer-authored audit data crosses here; the verifier is the integrity gate over it. |
| .sql fixture files -> manifest fixture_sha256 | Committed bytes are the hashable truth; drift between them and the manifest or the embedded b\"...\" literals is a provenance break. |
| release archive evidence (/tmp/flink-research/) -> sha512 column | Research-time external evidence; present-verify / absent-skip, never fabricated. |
| flink-coverage.tsv -> coverage report | Category semantics cross here; prerequisite rows must never surface as engine-supported. |
| parity/__snapshot__ -> migrate | The flink snapshot namespace is frozen by this plan's data additions; any touch is a Pitfall 6 break. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-12-01-01 | Tampering | manifest fixture_sha256 vs committed .sql bytes | high | mitigate | verify_corpus.py sha256_file per row; a drift fails --check (D-06, Pitfall 3). |
| T-12-01-02 | Tampering | Category mislabel — planner/catalog-prerequisite reported as positive/engine-supported | high | mitigate | 6-category enum membership + expected_status consistency + coverage prerequisite hard rule (Pitfall 2, D-01). |
| T-12-01-03 | Tampering | Path traversal via fixture_id injecting a path | medium | mitigate | fixture_id prefix whitelist (flink-grammar. / flink-lexical.) + reject ids containing a path separator or dot-dot; the .sql path is built from fixed prefix + whitelisted id. |
| T-12-01-04 | Spoofing | Gate blind spot — empty manifest / 0 files still exits 0 | high | mitigate | non-empty guard: empty manifest exits 1; coverage rows >= 1 (Pitfall 8, check_naming.py scanned==0 precedent). |
| T-12-01-05 | Tampering | Embedded-raw provenance drift — .sql files diverge from flink_grammar_test.mbt b\"...\" literals | high | mitigate | extract_flink_grammar.py byte-compares embedded literals to .sql files (D-08). |
| T-12-01-06 | Repudiation | Fabricated hash/provenance values masking an unavailable release | medium | mitigate | never-fabricate discipline (unavailable-offline / known-gap); archive-missing recorded as archive-not-present status (STATE.md precedent). |
| T-12-01-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new runtime dependencies (RESEARCH Package Legitimacy Audit N/A); all scripts are Python stdlib. |
</threat_model>

## Artifacts this phase produces

Files: scripts/verify_corpus.py (new), parity/fixtures/flink/manifest.tsv (new, 110 rows), parity/fixtures/flink/{flink-2.3.0,flink-2.1.3,flink-1.20.5,doris-4.x}/{fixture_id}.sql (new, 110 files), corpus/flink-coverage.tsv (new), corpus/tools/generate_corpus_report.py (modified — reads flink-coverage.tsv, bilingual render, prerequisite hard rule, --check staleness), corpus/CORPUS-REPORT.md (modified — regenerated bilingual), scripts/extract_flink_grammar.py (modified — b"..." literal ↔ .sql byte-compare + 6-category enum), scripts/extract_flink_lexical.py (modified — 13 expanded fixture rows + token-source + 6-category).

Symbols (scripts/verify_corpus.py): main(argv) with --check; PINS table {flink-2.3.0: (1.36.0, "Lex.JAVA,identifierMaxLength=256,conformance=FlinkSqlConformance.DEFAULT"), flink-2.1.3: (1.34.0, …), flink-1.20.5: (1.32.0, …)}; CATEGORIES = {positive, negative, recovery, known-limitation, catalog-prerequisite, planner-prerequisite}; EXPECTED_STATUS map {positive: valid, negative: error, recovery: recovered, known-limitation: valid, catalog-prerequisite: valid, planner-prerequisite: valid}; sha256_file(path); snapshot segment rule (flink → {fixture_id}.{profile}.{mode}.json, doris → {fixture_id}.doris-{profile}.{mode}.json); problems+ok exit gate; non-empty guard; archive sha512 present-verify/absent-archive-not-present.
Symbols (corpus/flink-coverage.tsv): header profile, category, fixture_count, parser_accepted, parser_rejected, recovery, prerequisite, coverage_note.
Symbols (corpus/tools/generate_corpus_report.py): flink-coverage reader, prerequisite hard rule (catalog/planner/known-limitation rows never engine-supported), parser-accepted vs engine-semantic-prerequisite totals, full-compatibility claim scan, --check staleness byte-compare.
Symbols (scripts/extract_flink_grammar.py): b"..." literal parser, .sql byte-compare, 6-category enum validation.
Symbols (scripts/extract_flink_lexical.py): 13-row fixture expansion validation, token-source column, 6-category enum validation.
