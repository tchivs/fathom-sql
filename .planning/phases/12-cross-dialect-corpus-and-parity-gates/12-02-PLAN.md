---
phase: 12-cross-dialect-corpus-and-parity-gates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/diff_parity.py
  - scripts/baseline_diff.py
  - .planning/phases/12-cross-dialect-corpus-and-parity-gates/approved-changes.md
autonomous: false
requirements: [PARITY-01]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A maintainer can run `python3 scripts/diff_parity.py --frozen-only` and get a frozen-vs-current diff report that regenerates the current snapshot tree from the committed baseline (via a temp `moon test --update --package parity` that never touches the working tree) and FAILS (exit 1) on ANY difference — the approved-changes register is NOT consulted in this mode; the register is consulted only in local `--approve <register>` mode, where a difference with a registered entry is classified approved (exit 0) and any unregistered difference is unexpected (exit 1) — the frozen Doris baseline is proven by regeneration, not by a vacuous left==right self-comparison (PARITY-01, D-03)."
    - "Doris 2.1/3.x/4.x valid/invalid/recovery/CST/span/diagnostic/formatter/completion behavior stays equal to the frozen baseline: `moon test --package parity` (no --update) passes, `git diff --name-only -- parity/__snapshot__` shows no doris-named snapshot changed, and `diff_parity.py --frozen-only` exits 0 — unless an intentional difference has a pre-committed approved-changes.md entry (PARITY-01, D-04)."
    - "The approval flow is the single-use path: any intentional snapshot change must have its key:/prefix:/field: register entry committed BEFORE the one `moon test --update --package parity` run; a diff with no register entry is unexpected and must be fixed or adjudicated, never absorbed (D-07, Pitfall 1)."
    - "Docs-vs-parser and release-fact-vs-docs conflicts surface explicitly in the diff report as unexpected rows routed to the human-adjudication register (approved-changes.md), never silently resolved by bulk snapshot updates (D-07, Research §6.3/§9)."
    - "The 12-approved-changes.md register pre-declares the Phase 12 snapshot-surface changes (unified manifest migration is data-only under parity/fixtures/flink/, so no parity/__snapshot__ change is expected for it) and re-asserts the Doris 213-snapshot zero-drift hard gate (D-08)."
    - "[probe PARITY-01 unclassified] flagged-unverified: the edge-probe left PARITY-01 unclassified — the frozen-vs-current harness contract above is authored to enforce PARITY-01 in full (regenerated comparison, register-approved intentional changes only) and remains flagged for manual review."
  artifacts:
    - path: "scripts/diff_parity.py"
      provides: "Frozen-vs-current diff harness (stdlib): --frozen-only CI mode (temp copy of committed snapshot tree + temp moon test --update regeneration + byte/path dual-channel compare + FAILS exit 1 on ANY difference, register NOT consulted), --approve <register> local mode (readable frozen-vs-current report, diffs classified approved/unexpected against the register, unexpected>0 exits 1), directory-missing exit 2, working-tree restore guarantee"
    - path: "scripts/baseline_diff.py"
      provides: "Minimal extension: --frozen/--current alias so diff_parity.py can invoke the approved-vs-unexpected engine with explicit left/right names (existing parse_approve/classify/diff_file/exit-gate reused unchanged)"
    - path: ".planning/phases/12-cross-dialect-corpus-and-parity-gates/approved-changes.md"
      provides: "Phase 12 D-08 register: header rule text (never --update without a committed entry; Doris 213 zero-drift HARD gate), pre-declared Phase 12 snapshot changes, machine-readable key:/prefix:/field: rows consumed by baseline_diff.py parse_approve"
  key_links:
    - from: "scripts/diff_parity.py temp current tree (moon test --update regeneration)"
      to: "scripts/baseline_diff.py --left frozen --right current --approve <register>"
      via: "approved-vs-unexpected classification; unexpected diffs exit 1 (D-03, baseline_diff engine at :100-149/:214-270)"
    - from: "parity/__snapshot__ committed tree"
      to: "parity/__snapshot__ temp regenerated tree"
      via: "diff_parity.py --frozen-only move/restore workflow (cp committed copy + mv working snapshot aside + moon test --update into temp + mv back) — zero working-tree residue (Research §6.2)"
    - from: "parity/__snapshot__ doris-named files"
      to: ".planning/phases/12-cross-dialect-corpus-and-parity-gates/approved-changes.md"
      via: "any doris snapshot difference must have a pre-committed register entry or it is a regression (D-08, Pitfall 1)"
    - from: "diff report unexpected rows"
      to: "human-adjudication register entry"
      via: "docs-vs-parser / release-fact-vs-docs / intentional-implementation-change tri-channel adjudication (D-07, Research §9)"
  prohibitions:
    - "Snapshot drift MUST NOT be absorbed by a silent bulk `moon test --update --package parity` — every intentional change needs a pre-committed approved-changes.md entry (single-use approval path, D-07); the --update flag must NEVER be added to any CI job (Pitfall 1)."
    - "The frozen-vs-current diff harness MUST NOT reduce to a vacuous left==right self-comparison — diff_parity.py --frozen-only must regenerate the current tree and FAIL (exit 1) on ANY difference, consulting NO register, never as an unconditional pass; approved/unexpected classification against the register happens only in local --approve <register> mode (D-03)."
    - "Docs-vs-parser and release-fact-vs-docs conflicts MUST NOT be silently resolved by overwriting snapshots — unexpected diffs route to the human-adjudication register with a recorded reason (D-07, Research §9)."
---

<objective>
把 Phase 9 已建的 Doris baseline 门禁（D-07/D-08：213 快照 + approved-changes 注册表 + baseline_diff）形式化为显式 diff harness（PARITY-01 / D-03/D-04）：任何共享/方言改造后输出「冻结 vs 当前」差异报告，故意变更须经注册表批准流，docs-vs-parser 冲突显式可见，绝不静默批量更新快照。当前 CI 的 baseline_diff.py 是 left==right 自比对（稳态恒 0 diff），只验证注册表可解析 + 快照树自洽；本 plan 新增 diff_parity.py 把「冻结」从恒真自比对升级为可证重生成比对（temp `moon test --update` 再与 committed 树逐字节 + 逐 path 双通道比对）。

Purpose: PARITY-01 要求 Doris 2.1/3.x/4.x 全部既有快照面（valid/invalid/recovery/CST/span/diagnostic/formatter/completion）在 Phase 9-11 方言/命名重构后与冻结 baseline 相等，除非故意变更已显式记录。Research §6 现状：baseline_diff.py 已是 approved-vs-unexpected 引擎（parse_approve 解析 key:/prefix:/field: 三行、classify 做 path 级配对、diff_file 非 JSON 字节比对、exit 0/1/2）；「冻结 vs 当前」harness 只需把「当前」树生成出来（copy + temp `moon test --update`）+ 与注册表比对。本 plan 交付该 harness（--frozen-only CI 模式 + --approve 本地模式）+ Phase 12 注册表 + Doris 零漂移形式化验证。零 MoonBit 核心改动，全部为 Python stdlib 工具 + 注册表 + CI 接线（CI 接线在 12-03）。
Output: scripts/diff_parity.py、scripts/baseline_diff.py 最小别名扩展、.planning/phases/12-cross-dialect-corpus-and-parity-gates/approved-changes.md。
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-cross-dialect-corpus-and-parity-gates/12-CONTEXT.md
@.planning/phases/12-cross-dialect-corpus-and-parity-gates/12-RESEARCH.md
@.planning/phases/12-cross-dialect-corpus-and-parity-gates/12-PATTERNS.md
@/opt/source/Fathom/scripts/baseline_diff.py
@/opt/source/Fathom/.planning/phases/09-dialect-boundary-and-neutral-naming/approved-changes.md
@/opt/source/Fathom/.planning/phases/11-flink-grammar-and-recoverable-cst/approved-changes.md
@/opt/source/Fathom/.github/workflows/ci.yml

This plan formalizes the Phase 9 baseline gate; it reuses scripts/baseline_diff.py (270 lines, read in RESEARCH §6.3) as the classification engine and the three existing approved-changes.md registers as the approval stream. D-03 (harness CI form, one-way once wired) is gated by Task 1 checkpoint. D-04 (PARITY-01 covers all snapshot surfaces; the frozen baseline is a public contract, one-way) is carried as a rating on Task 2 — the door was already walked in Phase 9 (freeze), so no checkpoint re-asks it; this plan formally verifies the zero-drift that Phases 10/11 already maintained. The CI wiring of diff_parity.py --frozen-only happens in 12-03 (this plan only builds and locally validates the harness); file ownership keeps ci.yml out of this plan.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Confirm the frozen-vs-current diff harness contract — D-03 one-way door</name>
  <decision>The diff harness form that becomes a release contract once CI-wired: a new stdlib wrapper `python3 scripts/diff_parity.py` with two modes — `--frozen-only` for CI (regenerate the current tree in a temp directory via `moon test --update --package parity` with move/restore so the working tree has zero residue, then byte + path dual-channel compare against the committed tree, exit 1 on ANY difference) and `--approve <register>` for local development (same regeneration, classify via the baseline_diff engine as approved/unexpected against the register, readable frozen-vs-current report, exit 1 when unexpected > 0). The existing baseline_diff.py left==right self-check stays as a fallback but is no longer the proof of frozenness.</decision>
  <context>D-03 is one-way because once parity-gate runs `diff_parity.py --frozen-only`, the regenerated-comparison becomes the release contract for Doris baseline integrity (CONTEXT D-03 reversibility: harness 可增补，但一旦 CI 接线即成为契约). RESEARCH §6.2: the current baseline_diff.py is a left==right self-comparison (steady-state zero) that only proves the register parses and the tree is self-consistent — the real frozen gate today is moon test's byte failure. The harness adds the missing "frozen vs current" report by regenerating current output. The form decisions: (a) wrapper around the existing engine vs a rewrite — RESEARCH recommends the wrapper; (b) --frozen-only in parity-gate vs a standalone job — 12-03 wires it per the RESEARCH recommendation (add a step to parity-gate); (c) the temp regeneration must never touch the working snapshot tree (move/restore, Research §6.2 workflow). The working-tree safety assumption is flagged A9 — if MoonBit rejects the directory move, fall back to a git-clean-plus-restore workflow; the executor surfaces this as a checkpoint rather than silently diverging.</decision>
  <options>
    <option id="option-a">
      <name>Wrapper diff_parity.py reusing the baseline_diff engine (recommended — RESEARCH recommendation)</name>
      <pros>Reuses the proven parse_approve/classify/diff_file engine (approved-vs-unexpected semantics); --frozen-only upgrades the CI proof from self-comparison to regeneration; --approve gives maintainers a readable local report; zero behavior change to the frozen gate's approved/unexpected classification.</pros>
      <cons>Two scripts to maintain (wrapper + engine); the wrapper must reproduce the temp-regeneration lifecycle carefully to avoid working-tree residue.</cons>
    </option>
    <option id="option-b">
      <name>Extend baseline_diff.py in place with --frozen mode</name>
      <pros>One script; no wrapper indirection.</pros>
      <cons>Conflates the diff engine with the regeneration lifecycle; the CI parity-gate step becomes a heavier command; harder to test the temp workflow in isolation; contradicts the 12-PATTERNS analog mapping (diff_parity.py is the named new file).</cons>
    </option>
  </options>
  <resume-signal>Select option-a (wrapper diff_parity.py + baseline_diff alias) or describe a modified harness form; the harness implementation in Task 2 is authored only after this confirmation.</resume-signal>
</task>

<task type="auto">
  <reversibility rating="one-way">Implements D-04: the frozen Doris baseline is the Phase-9-established public contract; this harness makes the regeneration-comparison the enforceable proof of PARITY-01 across all 213 snapshots (undoing a wrong harness form after CI wiring requires changing a release contract).</reversibility>
  <name>Implement scripts/diff_parity.py — frozen-vs-current regeneration harness</name>
  <files>scripts/diff_parity.py, scripts/baseline_diff.py</files>
  <read_first>
    @.planning/phases/12-cross-dialect-corpus-and-parity-gates/12-RESEARCH.md
    @.planning/phases/12-cross-dialect-corpus-and-parity-gates/12-PATTERNS.md
    @/opt/source/Fathom/scripts/baseline_diff.py
  </read_first>
  <action>Create scripts/diff_parity.py (Python stdlib, mirroring the baseline_diff.py CLI/exit conventions and the 12-PATTERNS skeleton) implementing the D-03-confirmed harness. CLI: argparse with --frozen-only (no register argument — CI mode) and --approve <register> (local mode); a required --package parity default and --left/--right not required (derived from the temp lifecycle). Lifecycle for BOTH modes: (1) resolve the committed snapshot tree at parity/__snapshot__; (2) copy it to a temp frozen dir; (3) move parity/__snapshot__ aside to a temp base dir, run `moon test --update --package parity` so the regenerated tree lands at parity/__snapshot__, then move the regenerated tree to a temp current dir and move the base dir back to parity/__snapshot__ (zero working-tree residue, Research §6.2 step 2); if any move fails, restore the original snapshot tree and exit 2 with an error line (never leave the working tree modified). Classification: invoke the baseline_diff engine (reuse via import of scripts/baseline_diff.py functions, or subprocess with --left/--right/--approve as the fallback) to classify frozen-vs-current diffs. --frozen-only: any difference — approved or not — exits 1 (CI treats ALL drift as failure since the register is not consulted; a pre-committed register entry must have been applied before a deliberate change is committed, so a clean --frozen-only is the proof). --approve <register>: classify against the register with the engine; print the frozen-vs-current report (changed-file list + per-path value diffs + approved/unexpected classification), exit 1 when unexpected > 0, exit 0 when only approved diffs appear, exit 2 when either directory is missing. Non-empty guard: a missing or empty snapshot tree exits 2, never ok. Add the minimal --frozen/--current aliases to scripts/baseline_diff.py only if the engine import requires them; otherwise leave baseline_diff.py byte-identical (document the reuse decision in the script header). Commit the harness and any baseline_diff.py change as one atomic change.</action>
  <acceptance_criteria>
    - CLI: `python3 -m py_compile scripts/diff_parity.py` succeeds; `python3 scripts/diff_parity.py --frozen-only` on the current clean tree exits 0 after regenerating and comparing (the committed tree equals the regenerated tree); the working tree has zero residue after the run (`git status --short parity/__snapshot__` is empty).
    - Approved classification: a deliberate snapshot change registered in a register (key:/prefix:/field: row) makes `--approve <register>` exit 0 with the diff listed as approved, while the same unregistered change makes `--approve <register>` exit 1 with the diff listed as unexpected.
    - CI proof is non-vacuous: a temporary injected drift into one committed snapshot (e.g. copy a flink-grammar snapshot over a doris snapshot, or touch a single byte in a doris snapshot) makes `--frozen-only` exit 1 with that file named in the report — proving the harness regenerates and compares rather than self-comparing.
    - Restore guarantee: killing the harness mid-regeneration (or a deliberately failed move) leaves parity/__snapshot__ identical to the committed tree (the restore path runs on failure).
    - Doris zero-drift (D-04): after the harness run, `moon test --package parity` (no --update) passes and `git diff --name-only -- parity/__snapshot__` shows no doris-named snapshot changed.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m py_compile scripts/diff_parity.py && python3 scripts/diff_parity.py --frozen-only && test -z "$(git status --short parity/__snapshot__)" && moon test --package parity</automated>
  </verify>
  <done>diff_parity.py exists with both modes, the regeneration lifecycle leaves the working tree clean, approved-vs-unexpected classification reuses the baseline_diff engine, a deliberately injected drift fails --frozen-only with the file named, and the Doris 213-snapshot baseline stays byte-identical.</done>
</task>

<task type="auto">
  <name>Phase 12 approved-changes register + local frozen-vs-current validation</name>
  <files>.planning/phases/12-cross-dialect-corpus-and-parity-gates/approved-changes.md</files>
  <read_first>
    @.planning/phases/12-cross-dialect-corpus-and-parity-gates/12-CONTEXT.md
    @.planning/phases/12-cross-dialect-corpus-and-parity-gates/12-RESEARCH.md
    @.planning/phases/09-dialect-boundary-and-neutral-naming/approved-changes.md
    @.planning/phases/11-flink-grammar-and-recoverable-cst/approved-changes.md
  </read_first>
  <action>Create .planning/phases/12-cross-dialect-corpus-and-parity-gates/approved-changes.md as the Phase 12 D-08 register, mirroring the Phase 11 register header and structure. Header rule text (follow the 12-PATTERNS template): the --update flag is NEVER run without a matching committed entry in this register (single-use approval path); any diff NOT in this register is a regression and must be fixed, not absorbed; the Doris 213-snapshot zero-drift is a HARD gate. Pre-declare the Phase 12 snapshot-surface expectations: (1) the unified-manifest migration (12-01) is data-only under parity/fixtures/flink/ and must NOT touch parity/__snapshot__ — no register entry is expected for it, and any snapshot change from it would itself be a regression; (2) any future intentional Doris or flink snapshot change in Phase 12 must add a machine-readable row here first. Include the machine-readable section skeleton with the three parse_approve line forms (key:<key>: <old> -> <new>, prefix: <old> -> <new>, field: <name>) and a placeholder note that the register is empty until an intentional change is proposed. Then run `python3 scripts/diff_parity.py --approve .planning/phases/12-cross-dialect-corpus-and-parity-gates/approved-changes.md` on the current clean tree and confirm it exits 0 (no unexpected diffs; the register is empty and the tree is clean), demonstrating the local review path works.</action>
  <acceptance_criteria>
    - The register file exists with the header rule text (single-use approval path + Doris 213 zero-drift HARD gate), the pre-declared Phase 12 snapshot-surface expectations, and the machine-readable row-format skeleton.
    - `python3 scripts/diff_parity.py --approve .planning/phases/12-cross-dialect-corpus-and-parity-gates/approved-changes.md` exits 0 on the current clean tree with an ok line (0 unexpected diffs), proving the local review path.
    - The register is parsed by the baseline_diff engine: `python3 scripts/baseline_diff.py --left parity/__snapshot__ --right parity/__snapshot__ --approve .planning/phases/12-cross-dialect-corpus-and-parity-gates/approved-changes.md` exits 0 (register parses, self-check steady state).
    - `git status --short parity/__snapshot__` is empty after the local validation run (the harness left no residue).
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/12-cross-dialect-corpus-and-parity-gates/approved-changes.md && python3 scripts/diff_parity.py --approve .planning/phases/12-cross-dialect-corpus-and-parity-gates/approved-changes.md && test -z "$(git status --short parity/__snapshot__)"</automated>
  </verify>
  <done>The Phase 12 D-08 register exists with the single-use approval contract and the Doris 213 zero-drift hard gate, the local --approve review path exits 0 on the clean tree, and the register is consumable by the baseline_diff engine.</done>
</task>

</tasks>

## Source Audit Coverage

| SOURCE | ID | Feature or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Maintainers audit reproducible Doris/Flink coverage; release only within explicit parity contracts | 12-02 | COVERED | Frozen-vs-current diff harness makes the Doris baseline provable. |
| REQ | PARITY-01 | Doris 2.1/3.x/4.x valid/invalid/recovery/CST/span/diagnostic/formatter/completion equals frozen baseline unless recorded | 12-02 | COVERED | diff_parity.py regeneration harness + zero-drift verification + register approval flow. |
| CONTEXT | D-03 | Formalize Phase 9 baseline gate into explicit diff harness; deliberate changes via register; docs-vs-parser conflicts visible (reversible until CI-wired) | 12-02 | COVERED | Task 1 checkpoint (one-way once wired) + Task 2 diff_parity.py; CI wiring in 12-03. |
| CONTEXT | D-04 | PARITY-01 covers all Doris snapshot surfaces; Phase 10/11 flink changes introduce no unrecorded Doris change (one-way) | 12-02 | COVERED | Task 2 reversibility rating (one-way, door already walked in Phase 9) + zero-drift acceptance. |
| CONTEXT | D-07 | docs/source/Calcite conflicts explicit + human adjudication; no silent batch snapshot updates (reversible) | 12-02 | COVERED | Task 3 register + prohibition (unexpected diffs route to adjudication). |
| RESEARCH | §6.2 | diff_parity.py temp regeneration workflow (copy + move + restore), --frozen-only CI mode, --approve local mode | 12-02 | COVERED | Task 2 lifecycle. |
| RESEARCH | §6.3 | docs-vs-parser conflict tri-channel adjudication | 12-02 | COVERED | Task 3 register + prohibition. |
| RESEARCH | §9.1/9.2 | diff_parity.py new file; baseline_diff.py optional alias; 12-approved-changes.md register | 12-02 | COVERED | Tasks 2/3. |
| RESEARCH | Pitfall 1 | Silent bulk --update absorbing drift | 12-02 | COVERED | single-use approval path + NEVER --update in CI prohibition + Task 3 register. |
| RESEARCH | Pitfall 9 (A9) | temp --update workflow safety on CI runner | 12-02 | FLAGGED (assumption) | A9 carried: if MoonBit rejects directory move, fall back to git-clean-restore; executor surfaces as checkpoint. |
| RESEARCH | §4.7 | approved-changes register machine-readable rows (key:/prefix:/field:) | 12-02 | COVERED | Task 3 register skeleton + engine consumption. |
| PROBE | PARITY-01 unclassified | Edge-probe left PARITY-01 unclassified | 12-02 | FLAGGED | must_haves flagged-unverified truth; harness authored to enforce PARITY-01 in full. |
| PROBE | CORPUS-01 unclassified | Edge-probe left CORPUS-01 unclassified | 12-02 | NOT IN SCOPE (12-01) | flagged in 12-01. |
| PROBE | PARITY-02 unclassified | Edge-probe left PARITY-02 unclassified | 12-02 | NOT IN SCOPE (12-03) | flagged in 12-03. |
| PROBE | PARITY-03 concurrency | Interrupted/parallel gate runs guarantee | 12-02 | NOT IN SCOPE (12-01) | backstop flagged in 12-01. |

<verification>
Run python3 -m py_compile scripts/diff_parity.py, python3 scripts/diff_parity.py --frozen-only, and python3 scripts/diff_parity.py --approve .planning/phases/12-cross-dialect-corpus-and-parity-gates/approved-changes.md after Task 2/3; verify the working tree has zero residue (git status --short parity/__snapshot__ empty); run moon test --package parity (no --update) after every step and git diff --name-only -- parity/__snapshot__ to confirm no doris-named snapshot changed. Inspect: the diff_parity.py temp lifecycle (frozen copy + update regeneration + move/restore), the --frozen-only and --approve paths, the report output on a deliberately injected drift, and the register file.
</verification>

<success_criteria>
- A maintainer can prove Doris baseline integrity by regeneration: diff_parity.py --frozen-only regenerates the current tree from the committed baseline and fails on ANY drift, replacing the vacuous left==right self-check as the frozenness proof.
- Deliberate snapshot changes flow through the single-use register approval path; unexpected diffs are classified and exit 1; docs-vs-parser conflicts surface for human adjudication.
- The Doris 213-snapshot baseline is byte-identical after the harness work (PARITY-01 zero-drift verified).
</success_criteria>

<output>Create `.planning/phases/12-cross-dialect-corpus-and-parity-gates/12-02-SUMMARY.md` when this plan executes.</output>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parity/__snapshot__ committed tree -> diff_parity.py temp regeneration | The harness reads committed bytes and writes a temp regenerated tree; the working tree must never be left modified. |
| approved-changes.md register -> diff classification | Maintainer-authored approval data crosses here; the engine trusts the register to classify diffs as approved vs unexpected. |
| frozen baseline -> release | The Doris baseline is the public parity contract; any unregistered drift is a contract break. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-12-02-01 | Tampering | Unregistered Doris snapshot drift absorbed by a local or CI --update | high | mitigate | single-use approval path (register entry committed before any --update); --frozen-only regenerates and fails on ALL drift; NEVER --update in CI (Pitfall 1). |
| T-12-02-02 | Spoofing | Frozen-vs-current self-comparison reporting zero as frozen | high | mitigate | diff_parity.py --frozen-only regenerates current output and byte+path compares; an injected drift must fail with the file named (non-vacuous proof, D-03). |
| T-12-02-03 | Tampering | Docs-vs-parser / release-fact conflicts silently resolved by overwriting snapshots | high | mitigate | unexpected diffs exit 1 and route to the human-adjudication register with a recorded reason (D-07, Research §9). |
| T-12-02-04 | Availability | Temp regeneration leaves the working snapshot tree modified after a crash | high | mitigate | move/restore lifecycle with a restore-on-failure path that returns parity/__snapshot__ to the committed bytes; harness exits 2 on any failed move (Research §6.2, A9). |
| T-12-02-05 | Tampering | Register forged/empty masking an unapproved drift | medium | mitigate | the register parses via the baseline_diff engine (malformed rows fail); --frozen-only does not consult the register at all, so an empty register cannot mask drift in CI mode. |
| T-12-02-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new runtime dependencies; diff_parity.py is Python stdlib reusing the existing baseline_diff engine. |
</threat_model>

## Artifacts this phase produces

Files: scripts/diff_parity.py (new), scripts/baseline_diff.py (modified — minimal --frozen/--current alias, or byte-identical if the import reuse needs no change), .planning/phases/12-cross-dialect-corpus-and-parity-gates/approved-changes.md (new).

Symbols (scripts/diff_parity.py): main(argv) with --frozen-only / --approve <register>; regenerate_current() temp lifecycle (frozen copy + snapshot move-aside + moon test --update --package parity + move-back, zero residue, restore-on-failure exit 2); compare_trees() byte + path dual-channel; classify via baseline_diff engine (import or subprocess); report printer (changed-file list + per-path value diffs + approved/unexpected); exit 0/1/2 contract; non-empty guard.
Symbols (scripts/baseline_diff.py): --frozen/--current aliases (only if the engine import requires them).
Symbols (.planning/phases/12-cross-dialect-corpus-and-parity-gates/approved-changes.md): header rule text (single-use approval path; Doris 213 zero-drift HARD gate), Phase 12 pre-declared snapshot-surface expectations, machine-readable row skeleton (key:/prefix:/field: forms).
