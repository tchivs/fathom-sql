---
phase: 08-incremental-parsing-benchmark-gated
plan: 02
type: execute
wave: 1
depends_on:
  - 01
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - EDIT-01
estimate:
  tokens: 18000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "REQUIREMENTS.md 的 EDIT-01 行已标记 descoped with evidence（`[x]` + DESCOPED WITH EVIDENCE + 08-BENCHMARK.md 引用），Traceability 表 EDIT-01 状态 = Descoped with evidence（SC1 明文允许 'or the requirement is descoped with the benchmark evidence documented'）"
    - "ROADMAP.md Phase 8 状态行 = Complete — descoped with evidence (see 08-BENCHMARK.md)"
    - "STATE.md Deferred Items 表新增 descope_evidence 行（EDIT-01 incremental parsing — benchmark-gated descope | closed 2026-08-12 — 08-BENCHMARK.md (fixture/规模/median/p95/结论)），仿 05-01-PLAN.md 的 closed 状态措辞"
    - "零增量解析代码：incremental/ 包未创建；parser/ 未动（git status 确认）"
  artifacts:
    - .planning/REQUIREMENTS.md（EDIT-01 行 + Traceability 表）
    - .planning/ROADMAP.md（Phase 8 状态）
    - .planning/STATE.md（Deferred Items descope_evidence 行）
  key_links:
    - "traceability 条目 ↔ 08-BENCHMARK.md 实测数字：REQUIREMENTS/ROADMAP/STATE 的 descope 记录必须引用 08-BENCHMARK.md 的 fixture/规模/median/p95/结论，无数据不宣称（D-17）"
  prohibitions:
    - statement: "绝不无测量数据宣称 descope（descope 记录必须引用 08-BENCHMARK.md 实测数字，无数据 → 无 claim）——FLAGGED-UNVERIFIED（无 wired check，specless 描述符空）"
    - statement: "本 plan 零增量解析代码（incremental/ 不创建、parser/ 不动）——FLAGGED-UNVERIFIED（无 wired check，specless 描述符空）"
  flagged_assumption:
    - "EDIT-01 边界探针 UNRESOLVED（requirement_id: EDIT-01, category: unclassified, probe: unclassified — review manually）：descope 是 ROADMAP SC1 明文的合法结局（'or the requirement is descoped with the benchmark evidence documented'）；本 plan 仅当 08-01 记录分支 A 才执行（见 objective 门标记与 precondition）"
---

<objective>
**Branch A only — do NOT run if the 08-01 gate records branch B.** 记录 EDIT-01 的**证据驱动 descope**（分支 A，预期路径）：以 08-BENCHMARK.md 的实测数据为依据，更新 REQUIREMENTS.md/ROADMAP.md/STATE.md 将 EDIT-01 标记为 **descoped with evidence**（ROADMAP Phase 8 SC1 明文允许），**零增量解析代码**。

Purpose: D-03 分支 A + D-17 诚实 provenance——descope 必须有可复现 benchmark 证据落库，绝不静默放弃。本 plan 是分支 A 的 traceability 收尾，不创建任何代码。
Output: 三处 traceability 更新（REQUIREMENTS.md + ROADMAP.md + STATE.md）。
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-incremental-parsing-benchmark-gated/08-BENCHMARK.md
@.planning/phases/05-closeout-and-analysis-foundation/05-01-PLAN.md
</context>

<precondition>
08-01 SUMMARY 记录 fired branch = A（descope）。若 08-01 记录 branch B，本 plan 必须跳过（orchestrator 应路由至 08-03/08-04 增量实现）。
</precondition>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: REQUIREMENTS.md 的 EDIT-01 descope 标记（descoped with evidence）</name>
  <files>
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    .planning/REQUIREMENTS.md（EDIT-01 需求行 + Traceability 表）
    .planning/phases/08-incremental-parsing-benchmark-gated/08-BENCHMARK.md（实测数字：fixture/规模/median/p95/结论——本任务措辞的唯一数据来源）
    .planning/phases/05-closeout-and-analysis-foundation/05-01-PLAN.md（traceability 更新先例：需求行追加标记 + Traceability 表状态升级的措辞）
  </read_first>
  <action>
    在 `.planning/REQUIREMENTS.md` 做两处更新：
    1) EDIT-01 需求行（现为 `- [ ] **EDIT-01**: ...`）改为 `- [x]`，行尾追加 `— DESCOPED WITH EVIDENCE 2026-08-12` + `（见 .planning/phases/08-incremental-parsing-benchmark-gated/08-BENCHMARK.md：≥100KB median=Xms ≤ 50ms 阈值，无超线性迹象）`——X 取自 08-BENCHMARK.md 实测中位数，绝不虚构数字。
    2) Traceability 表 EDIT-01 行状态更新为 `Descoped with evidence`（引用 08-BENCHMARK.md）。
    措辞严格基于 08-BENCHMARK.md 实测数字；约束：不修改 EDIT-01 以外的需求行、不触碰任何源码。
  </action>
  <verify>
    <automated>grep -q "DESCOPED WITH EVIDENCE" .planning/REQUIREMENTS.md && grep -q "\[x\]" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md 的 EDIT-01 行为 `[x]` 且含 "DESCOPED WITH EVIDENCE" 与 08-BENCHMARK.md 引用
    - Traceability 表 EDIT-01 行状态为 "Descoped with evidence"
    - 引用的 median 数字与 08-BENCHMARK.md 实测一致（无虚构证据）
  </acceptance_criteria>
  <done>REQUIREMENTS.md 已把 EDIT-01 标记为 descoped with evidence（含实测数据引用）</done>
  <reversibility rating="one-way">descope 决策记录在 REQUIREMENTS 后，改回需新 benchmark 证据（D-03 one-way）——但决策由 08-01 分支 A 锁定，非本 task 选择，不插 re-ask checkpoint</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ROADMAP.md Phase 8 状态 + STATE.md descope_evidence 记录</name>
  <files>
    .planning/ROADMAP.md
    .planning/STATE.md
  </files>
  <read_first>
    .planning/ROADMAP.md（Phase 8 Status 行）
    .planning/STATE.md（Deferred Items 表，STATE.md:269-281）
    .planning/phases/05-closeout-and-analysis-foundation/05-01-PLAN.md（closed 状态措辞先例：`closed — Phase 5 CLOSE-01 formally verified`）
    .planning/phases/08-incremental-parsing-benchmark-gated/08-BENCHMARK.md（证据）
  </read_first>
  <action>
    在 `.planning/ROADMAP.md` 将 Phase 8 状态行更新为 `Complete — descoped with evidence (see 08-BENCHMARK.md)`。在 `.planning/STATE.md` 的 Deferred Items 表新增一行：`| descope_evidence | EDIT-01 incremental parsing — benchmark-gated descope | closed 2026-08-12 — 08-BENCHMARK.md (fixture/规模/median/p95/结论) |`（仿 05-01-PLAN.md 的 closed 状态措辞，引用 08-BENCHMARK.md 五要素）。约束：只做状态与记录更新，不创建 incremental/、不触碰 parser/、不写任何增量解析代码；`git status` 确认改动仅限 .planning/ 下文件。
  </action>
  <verify>
    <automated>grep -q "descoped with evidence" .planning/ROADMAP.md && grep -q "descope_evidence" .planning/STATE.md && git status --porcelain</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md Phase 8 Status = "Complete — descoped with evidence (see 08-BENCHMARK.md)"
    - STATE.md Deferred Items 含 descope_evidence 行且状态 closed，引用 08-BENCHMARK.md
    - git status 确认无 incremental/、parser/ 改动（零增量代码）
  </acceptance_criteria>
  <done>ROADMAP.md 与 STATE.md 已记录证据驱动 descope，零增量代码（incremental/ 未创建、parser/ 未动）</done>
  <reversibility rating="one-way">ROADMAP/STATE 的 descope 记录是 D-03 的 one-way 门；由 08-01 分支 A 锁定，不插 re-ask checkpoint</reversibility>
</task>

</tasks>

## Artifacts this phase produces

- **修改文件（无新增符号，纯 traceability）：**
  - `.planning/REQUIREMENTS.md` — EDIT-01 需求行 `[ ]` → `[x]` + `DESCOPED WITH EVIDENCE` 标记 + 08-BENCHMARK.md 引用；Traceability 表 EDIT-01 状态 → `Descoped with evidence`。
  - `.planning/ROADMAP.md` — Phase 8 状态行 → `Complete — descoped with evidence (see 08-BENCHMARK.md)`。
  - `.planning/STATE.md` — Deferred Items 表新增 `descope_evidence` 行（closed + 08-BENCHMARK.md 引用）。

- **明确不改动（零增量代码）：** 不创建 `incremental/`；`parser/`、`bench/`、`source/`、`syntax/`、`api/` 全部不动；`08-BENCHMARK.md` 只读引用（数据来源）。

## threat_model

本 plan 为纯文档/traceability 更新（分支 A descope 记录），无代码、无未信任输入面；唯一完整性风险是 descope 记录与 08-BENCHMARK.md 实测证据不符（虚构证据）。

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| traceability 记录 → 08-BENCHMARK.md 实测证据 | descope 条目必须引用仓库内真实存在的 08-BENCHMARK.md 五要素数字，不允许虚构 median/p95 或跳过证据 |

### STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-02-01 | Tampering | REQUIREMENTS/ROADMAP/STATE 的 descope 记录与实测不符 | high | mitigate | 措辞严格引用 08-BENCHMARK.md 数字；grep 断言 DESCOPED WITH EVIDENCE / descope_evidence 存在且引用路径真实 |
| T-08-02-02 | Spoofing | 误把分支 B 当分支 A 记录 descope（gate 未读或错读） | high | mitigate | precondition + objective 门标记；执行器先核对 08-01 SUMMARY 的 fired branch 再动笔 |
| T-08-02-SC | Tampering | 依赖安装 | low | accept | 零新增依赖（Package Legitimacy Audit N/A）；无安装面 |

<verification>
- 三项 grep 断言分别锁定 REQUIREMENTS.md 的 descope 标记、ROADMAP.md 的状态、STATE.md 的 descope_evidence 行。
- `git status` 收窄改动范围，确保无 incremental/、无 parser/、无任何源码改动。
- 全程不运行 benchmark/构建/测试（证据来自 08-01 已落库的 08-BENCHMARK.md）。
</verification>

<success_criteria>
- REQUIREMENTS.md / ROADMAP.md / STATE.md 三处 traceability 已把 EDIT-01 标记为 descoped with evidence，引用路径与 08-BENCHMARK.md 实测数字一致。
- 零增量解析代码产生（incremental/ 未创建、parser/ 未动）。
- 分支 A（预期路径）的诚实 descope 记录完成，SC1 允许的 "descoped with the benchmark evidence documented" 落地。
</success_criteria>

<output>
Create `.planning/phases/08-incremental-parsing-benchmark-gated/08-02-SUMMARY.md` when done.
</output>
