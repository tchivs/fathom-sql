---
phase: 08-incremental-parsing-benchmark-gated
plan: 04
type: execute
wave: 2
depends_on:
  - 03
files_modified:
  - incremental/edit.mbt
  - incremental/incremental.mbt
  - incremental/incremental_test.mbt
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - EDIT-01
estimate:
  tokens: 52000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "SC2 全矩阵成立：incremental_test.mbt 覆盖 insert/delete/replace 三类编辑，含字符串字面量内部与注释内部的编辑（Pitfall 5 高发场景），每个 fixture 断言 @printer.print_lossless(inc) == @printer.print_lossless(full)（Bytes 相等，绝不近似）"
    - "SC3 无 stale spans/trivia：编辑后增量结果无越界 span（每个节点 span ∈ [0, source.byte_length()] 且 start ≤ end），trivia 与全量一致——局部递归 span-in-bounds 自检（类比 @api.ParseResult::all_spans_in_bounds，api.mbt:919；白盒在 incremental_test.mbt 内实现，不 import api，D-04）"
    - "跨目标 parity（若适用）：moon test --target js / --target wasm --package incremental 若可编译运行，SC2 断言同样通过（Phase 12 D-03/D-08 纪律）；不可编译则 SUMMARY 记录限制（D-01 '若适用'）"
    - "分支 B traceability 已记录：REQUIREMENTS.md EDIT-01 → `[x]` + Complete（Traceability 表）；ROADMAP.md Phase 8 → Complete — incremental/ delivered (see 08-BENCHMARK.md)；STATE.md Accumulated Context 追加分支 B 决策记录（引用 08-BENCHMARK.md 实测数字）"
  artifacts:
    - incremental/edit.mbt（扩展：完整编辑语义/span 重映射）
    - incremental/incremental.mbt（扩展：完整 bounded reparse + invalidation）
    - incremental/incremental_test.mbt（完整 edit fixture 矩阵 + SC3 自检）
    - .planning/REQUIREMENTS.md / ROADMAP.md / STATE.md（分支 B traceability）
  key_links:
    - "incremental_test.mbt → print_lossless：每个 edit fixture 的 SC2 断言是增量正确性的唯一仲裁"
    - "span-overlap invalidation → SC3：失效范围覆盖编辑区间 + 词法上下文，保证无 stale spans/trivia"
    - "traceability ← 08-BENCHMARK.md：分支 B 记录引用实测数字，D-17 诚实 provenance"
  prohibitions:
    - statement: "增量输出必须与整文档重解析字节相等（print_lossless 等值硬不变量，绝不近似）——FLAGGED-UNVERIFIED（无 wired check，specless 描述符空）"
    - statement: "绝不触碰 parser/（冻结 baseline；bounded reparse 复用既有 parser）——FLAGGED-UNVERIFIED（无 wired check，specless 描述符空）"
  flagged_assumption:
    - "EDIT-01 边界探针 UNRESOLVED（requirement_id: EDIT-01, category: unclassified, probe: unclassified — review manually）：分支 B 全矩阵的 fixture 集合与 js/wasm parity 适用性由执行期实测决定；偏差在 SUMMARY 记录而非静默改契约"
---

<objective>
**Branch B only — do NOT run if the 08-01 gate records branch A.** 分支 B 的**横向扩展**：把 08-03 tracer 的单路径扩展为完整 edit fixture 矩阵（insert/delete/replace，**含字符串字面量与注释内部编辑**——Pitfall 5 最易触发 trivia/span 漂移），补 SC3（无 stale spans/trivia），跨目标 parity（若适用），并完成分支 B 的 traceability 记录（REQUIREMENTS/ROADMAP/STATE → Complete — incremental/ delivered）。

Purpose: 兑现 ROADMAP Phase 8 SC2/SC3 全量（每个 edit fixture 增量输出与全量字节相等；span-overlap invalidation 无 stale spans/trivia），并延续 D-17 诚实 provenance——分支 B 也必须有记录，绝不静默收尾。
Output: 完整 incremental/ 库 + edit fixture 测试矩阵 + SC3 自检 + 三处 traceability 更新。
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-incremental-parsing-benchmark-gated/08-CONTEXT.md
@.planning/phases/08-incremental-parsing-benchmark-gated/08-RESEARCH.md
@.planning/phases/08-incremental-parsing-benchmark-gated/08-PATTERNS.md
@.planning/phases/08-incremental-parsing-benchmark-gated/08-BENCHMARK.md
@incremental/incremental.mbt
@incremental/incremental_test.mbt
@printer/printer.mbt
@parity/moon.pkg
@parity/parity_test.mbt
</context>

<precondition>
08-01 SUMMARY 记录 fired branch = B 且 08-03 已完成（parse_incremental tracer 路径绿，`moon test --target native --package incremental` 通过）。若 08-01 记录 branch A，本 plan 必须跳过（orchestrator 应路由至 08-02 descope 记录）。
</precondition>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 完整 edit fixture 矩阵 + SC3 无 stale spans/trivia</name>
  <files>
    incremental/edit.mbt
    incremental/incremental.mbt
    incremental/incremental_test.mbt
  </files>
  <read_first>
    08-RESEARCH.md（Pitfall 5——insert/delete/replace 含字符串/注释内部；SC2/SC3）
    printer/printer.mbt（print_lossless:28-31——SC2 比对目标）
    api/api.mbt（all_spans_in_bounds:919——SC3 自检形状参考）
    incremental/incremental_test.mbt（08-03 已有 tracer fixture，扩展基础）
  </read_first>
  <behavior>
    - Test 1（insert）：语句间/表达式内插入 → print_lossless(inc) == print_lossless(full)
    - Test 2（delete）：删语句/删子句 → print_lossless(inc) == print_lossless(full)
    - Test 3（replace 在注释内部）：注释文本替换 → print_lossless(inc) == print_lossless(full)（Pitfall 5 trivia 漂移高发）
    - Test 4（replace 在字符串字面量内部）：字符串内容替换 → print_lossless(inc) == print_lossless(full)
    - Test 5（SC3）：每个 fixture 的增量结果递归 span-in-bounds（span ∈ [0, source.byte_length()]，start ≤ end）全通过
  </behavior>
  <action>
    扩展 `incremental/incremental_test.mbt` 为完整 edit fixture 矩阵：insert（语句间/表达式内）、delete（删语句/子句）、replace（标识符/字面量替换），**含字符串字面量内部与注释内部的编辑**（Pitfall 5 最易触发 trivia/span 漂移）；每个 fixture 构造 (original, edited) bytes 对 → `full = parse_full(edited)`（仿 08-03 full 路径）+ `inc = parse_incremental(parse_full(original), [Edit::checked(...)])` → 断言 `@printer.print_lossless(inc.root, inc.source) == @printer.print_lossless(full.root, full.source)`（SC2）。按需扩展 `incremental/edit.mbt`（span 重映射、多编辑批处理）与 `incremental/incremental.mbt`（词法上下文扩展、invalidation 完整性）使全矩阵通过。补 **SC3 自检**：对每个 fixture 的增量结果做局部递归 span-in-bounds 校验（每个节点 span ∈ [0, source.byte_length()] 且 start ≤ end，类比 api.mbt:919 all_spans_in_bounds 形状；白盒在 incremental_test.mbt 内实现，**不 import api**，D-04 纪律）。
  </action>
  <verify>
    <automated>moon test --target native --package incremental</automated>
  </verify>
  <acceptance_criteria>
    - incremental_test.mbt 覆盖 insert/delete/replace 三类，含字符串/注释内部编辑，全部 SC2 断言（Bytes 相等）通过
    - SC3 自检：每个 fixture 增量结果无越界 span（递归 span-in-bounds 通过）
    - `moon test --target native --package incremental` 全绿；parser/ 零改动（git status 确认）
  </acceptance_criteria>
  <done>SC2 全矩阵 + SC3 无 stale spans/trivia 成立——增量解析正确性由完整 edit fixture 矩阵锁定</done>
  <reversibility rating="costly">库 API/行为扩展影响调用方（D-05 costly）；由门禁分支 B 锁定，不插 re-ask checkpoint</reversibility>
</task>

<task type="auto">
  <name>Task 2: 跨目标 parity（若适用）+ 分支 B traceability 记录</name>
  <files>
    incremental/incremental.mbt
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/STATE.md
  </files>
  <read_first>
    parity/moon.pkg + parity/parity_test.mbt（跨目标 parity 先例：options(targets:) + fixture_source/run_fixture 模式）
    05-01-PLAN.md（traceability 更新先例：REQUIREMENTS.md 行 + Traceability 表措辞）
    .planning/REQUIREMENTS.md（EDIT-01 行 + Traceability 表）
    .planning/ROADMAP.md（Phase 8 状态）+ .planning/STATE.md（Accumulated Context Decisions 格式）
    .planning/phases/08-incremental-parsing-benchmark-gated/08-BENCHMARK.md（实测数字）
  </read_first>
  <action>
    若 incremental 包对 js/wasm 目标可编译（08-03 已确认 import 面；parity 用 `options(targets: ...)` 或直接 `moon test --target js --package incremental` / `--target wasm`），运行跨目标 SC2 断言并记录结果（Phase 12 D-03/D-08 纪律）；不可编译则 SUMMARY 记录限制（D-01 "若适用"/"若可行"）。完成分支 B traceability：`.planning/REQUIREMENTS.md` 的 EDIT-01 行 → `[x]` + 实现记录引用（incremental/ + 08-BENCHMARK.md）；Traceability 表 EDIT-01 → `Complete`。`.planning/ROADMAP.md` Phase 8 Status → `Complete — incremental/ delivered (see 08-BENCHMARK.md)`。`.planning/STATE.md` Accumulated Context Decisions 追加一行记录分支 B 决策（仿既有 D-编号格式，引用 08-BENCHMARK.md 实测数字，如 ≥100KB median=Xms > 50ms 判定瓶颈）。约束：绝不触碰 parser/（git status 验证）；不新增 wire/CLI/LSP 导出（D-05 交付面为库 API）。
  </action>
  <verify>
    <automated>moon test --target native --package incremental && grep -q "incremental/ delivered" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - 跨目标 parity：js/wasm 若可编译则 SC2 断言通过；不可编译则 SUMMARY 记录限制
    - REQUIREMENTS.md EDIT-01 行为 `[x]` + Traceability 表状态 `Complete`
    - ROADMAP.md Phase 8 = "Complete — incremental/ delivered (see 08-BENCHMARK.md)"
    - STATE.md 含分支 B 决策记录（引用 08-BENCHMARK.md 实测数字）；parser/ 零改动（git status）
  </acceptance_criteria>
  <done>分支 B 交付完成——incremental/ 库 + 全矩阵测试 + traceability 记录，无 parser/ 改动、无 wire/CLI/LSP 新面</done>
  <reversibility rating="one-way">分支 B 的 REQUIREMENTS/ROADMAP/STATE 记录是 D-03 的 one-way 门；由 08-01 分支 B + 08-03 完成锁定，不插 re-ask checkpoint</reversibility>
</task>

</tasks>

## Artifacts this phase produces

- **扩展符号（分支 B 触发时）：**
  - `incremental/edit.mbt` — 扩展：完整编辑语义/span 重映射/多编辑批处理（Edit::checked 仍复用 @source.Span::checked）。
  - `incremental/incremental.mbt` — 扩展：完整 bounded reparse + span-overlap invalidation 完整性 + 词法上下文扩展。
  - `incremental/incremental_test.mbt` — 完整 edit fixture 矩阵（insert/delete/replace，含字符串/注释内部编辑）+ SC3 递归 span-in-bounds 自检。
- **traceability 修改（无新增符号）：**
  - `.planning/REQUIREMENTS.md` — EDIT-01 行 `[x]` + 实现记录；Traceability 表 → `Complete`。
  - `.planning/ROADMAP.md` — Phase 8 → `Complete — incremental/ delivered (see 08-BENCHMARK.md)`。
  - `.planning/STATE.md` — Accumulated Context Decisions 追加分支 B 决策记录。

- **明确不改动：** `parser/`（Phase 12 冻结 baseline，git status 验证）、`api/`（incremental 不 import api，D-04；SC3 自检白盒实现）、`lexer/`/`syntax/`/`source/`/`printer/`（只读复用）；无 wire/CLI/LSP 新导出（D-05）。

## threat_model

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| incremental_test.mbt edit fixtures → parse_incremental | 编辑输入（含字符串/注释内）跨入增量路径；Span::checked 校验边界 |
| incremental/ → @parser.parse_with_limits_context / lex_with_limit | 单向读冻结 parser；parser 永不 import incremental |
| traceability 记录 → 08-BENCHMARK.md / 08-03 SUMMARY | 分支 B 记录必须与实测证据一致，不虚构 |

### STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04-01 | Tampering | 字符串/注释内编辑的 trivia 漂移（Pitfall 5 高发） | high | mitigate | fixture 矩阵含字符串/注释内编辑 + SC2 字节断言逐 fixture（print_lossless 等值） |
| T-08-04-02 | Tampering | 增量结果越界 span（编辑区间失效不完整） | high | mitigate | SC3 递归 span-in-bounds 自检（类比 api.mbt:919，白盒实现）+ Edit::checked 复用 Span::checked |
| T-08-04-03 | Tampering | 分支 B traceability 记录与实测不符 | medium | mitigate | 措辞引用 08-BENCHMARK.md/08-03 SUMMARY 实测数字；grep 断言记录存在 |
| T-08-04-04 | Tampering | parser/ 漂移（冻结 baseline 被意外修改） | high | mitigate | D-04 单向纪律 + git status 确认 parser/ 未动 |
| T-08-04-SC | Tampering | npm/pip/cargo 安装 | low | accept | 零新增外部依赖（Package Legitimacy Audit N/A）；无安装面 |

<verification>
- Task 1 后：`moon test --target native --package incremental` 全绿——完整 edit fixture 矩阵（含字符串/注释内编辑）SC2 断言 + SC3 自检。
- Task 2 后：回归全绿；js/wasm parity（若适用）；REQUIREMENTS/ROADMAP/STATE 分支 B traceability 三处 grep 断言。
- 全程 `git status` 确认 parser/ 未动。
</verification>

<success_criteria>
- SC2 全矩阵成立：每个 edit fixture（insert/delete/replace，含字符串/注释内部编辑）增量输出与全量输出 Bytes 相等（`print_lossless(parse_incremental(x)) == print_lossless(parse_full(x))`）。
- SC3 成立：span-overlap invalidation 保证编辑后无 stale spans/trivia（递归 span-in-bounds 自检通过）。
- 跨目标 parity（若适用）与分支 B traceability 记录完成，D-17 诚实 provenance 落地。
</success_criteria>

<output>
Create `.planning/phases/08-incremental-parsing-benchmark-gated/08-04-SUMMARY.md` when done.
</output>
