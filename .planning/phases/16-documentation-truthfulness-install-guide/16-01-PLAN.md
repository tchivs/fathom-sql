---
phase: 16-documentation-truthfulness-install-guide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - README.zh-CN.md
  - docs/GETTING-STARTED.md
  - docs/zh-CN/GETTING-STARTED.md
  - .planning/tmp/verify-README.md.json
  - .planning/tmp/verify-GETTING-STARTED.md.json
autonomous: true
requirements:
  - DOC-01
  - DOC-02
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "DOC-01 / D-01/D-02/D-03：README（en+zh）License 段声明 Apache-2.0 并链接 LICENSE；无 `moon 0.1.20260724` 失实引用（改为锁定 `moon 0.1.20260807` + `.github/moonbit-toolchain.json`）；GETTING-STARTED（en+zh）无 `<repository-url>` 占位符、使用真实 remote。"
    - "DOC-02 / D-04：README（en+zh）含「Install `fathom-lsp` from GitHub Release」章节：三平台资产表、`fathom-lsp-manifest.json` SHA-256 校验、安装位置 `~/.fathom/bin`、`fathom-lsp --version` → `fathom-lsp 1.0.0` 验证。"
    - "D-05：`verify-README.md.json` 与 `verify-GETTING-STARTED.md.json` 重生成，LICENSE/占位符/安装章节 claim 全部通过（claims_failed: 0）；核对以可执行 python 断言实现（含 zh 一致性）。"
  artifacts:
    - path: "README.md"
      provides: "修正后的 en README（工具链/License/安装章节）"
    - path: "README.zh-CN.md"
      provides: "同步的 zh README"
    - path: "docs/GETTING-STARTED.md"
      provides: "真实 remote、锁定工具链、安装章节指引"
    - path: "docs/zh-CN/GETTING-STARTED.md"
      provides: "同步的 zh 入门文档"
  key_links:
    - from: "README.md"
      to: "LICENSE"
      via: "Apache-2.0 链接"
      pattern: "LICENSE"
    - from: "README.md Install section"
      to: "https://github.com/tchivs/fathom-sql/releases + fathom-lsp-manifest.json"
      via: "资产下载与 SHA-256 校验"
      pattern: "fathom-lsp-manifest"
---

<objective>
修正 README 与 GETTING-STARTED（en+zh）的失实声明（无 LICENSE 假声明、旧工具链引用、`<repository-url>` 占位符），新增「从 GitHub Release 安装 `fathom-lsp`」章节（三平台资产 + SHA-256 校验 + 安装位置 + `--version` 验证），并重生成可执行的文档核对记录（DOC-01、DOC-02，D-01..D-06）。
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-documentation-truthfulness-install-guide/16-CONTEXT.md
@README.md
@README.zh-CN.md
@docs/GETTING-STARTED.md
@docs/zh-CN/GETTING-STARTED.md
@LICENSE
@.github/moonbit-toolchain.json
@.planning/tmp/verify-README.md.json
@.planning/tmp/verify-GETTING-STARTED.md.json
@.planning/tmp/docs-work-manifest.json
@.github/workflows/fathom-native-release.yml
</context>

<dependency_graph>
- Task 1 fixes truthfulness claims (toolchain refs, LICENSE, placeholders) in all four docs.
- Task 2 adds the install section and regenerates the verification records; it needs Task 1's clean docs.
</dependency_graph>

<flagged_assumptions>
- **[ASSUMED]** docs/zh-CN/GETTING-STARTED.md 存在并镜像 en 的失实声明（已侦察）。
- **[PROHIBITIONS RECALLED]** 不改 API/CONFIGURATION/ARCHITECTURE/DEVELOPMENT/TESTING 及其 verify JSON；不写 CHANGELOG（Phase 17）；不打 tag/Release（Phase 20）。
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Fix stale and false documentation claims in README and GETTING-STARTED (en+zh)</name>
  <reversibility rating="one-way">Per D-02/D-03, the false LICENSE claim and placeholders are release blockers; truthfulness is non-negotiable.</reversibility>
  <files>README.md, README.zh-CN.md, docs/GETTING-STARTED.md, docs/zh-CN/GETTING-STARTED.md</files>
  <read_first>
    - README.md + README.zh-CN.md License/Installation sections and badge
    - docs/GETTING-STARTED.md + docs/zh-CN/GETTING-STARTED.md Prerequisites/Installation sections
    - .github/moonbit-toolchain.json (locked version)
    - LICENSE (Apache-2.0)
  </read_first>
  <action>
Per D-01: replace every `moon 0.1.20260724` reference and the MoonBit badge text in README (en+zh) and GETTING-STARTED (en+zh) with the locked toolchain story: the repo pins `moon 0.1.20260807` via `.github/moonbit-toolchain.json` (official SHA-256 sidecars, content-locked); `moon.mod` records module identity (name `fathom/sql`, version `0.1.0`, preferred target `native`) and the toolchain policy comment. Keep wording truthful: the lock file is the source of the pinned version.

Per D-02: rewrite the README (en+zh) License section to state Apache-2.0 with a link to `LICENSE`; delete the false "does not include a LICENSE file / license type and link are to be confirmed" claim.

Per D-03: in GETTING-STARTED (en+zh), replace `git clone <repository-url>` with `git clone https://github.com/tchivs/fathom-sql.git` and delete the "current working copy does not have a verifiable Git remote configured" sentence.
  </action>
  <verify>
    <automated>python3 -c 'from pathlib import Path; assert "0.1.20260724" not in Path("README.md").read_text(); assert "0.1.20260724" not in Path("README.zh-CN.md").read_text(); assert "0.1.20260724" not in Path("docs/GETTING-STARTED.md").read_text(); assert "0.1.20260724" not in Path("docs/zh-CN/GETTING-STARTED.md").read_text(); for p in ("README.md","README.zh-CN.md"): t=Path(p).read_text(); assert "does not include" not in t and "to be confirmed" not in t and "LICENSE" in t; for p in ("docs/GETTING-STARTED.md","docs/zh-CN/GETTING-STARTED.md"): t=Path(p).read_text(); assert "<repository-url>" not in t and "github.com/tchivs/fathom-sql.git" in t and "0.1.20260807" in t'</automated>
  </verify>
  <acceptance_criteria>
    - 四文档零 `0.1.20260724` 引用；README 两语言 License 段声明 Apache-2.0 且链接 LICENSE、无假声明。
    - GETTING-STARTED 两语言无 `<repository-url>` 占位符、使用真实 remote、引用锁定工具链 0.1.20260807。
  </acceptance_criteria>
  <done>README 与 GETTING-STARTED（en+zh）全部失实声明修正完成。</done>
</task>

<task type="auto">
  <name>Task 2: Install-from-Release section and regenerated verification records</name>
  <reversibility rating="one-way">Per D-04, the install guide is the consumer acquisition path; per D-05 the verification records are release evidence.</reversibility>
  <files>README.md, README.zh-CN.md, .planning/tmp/verify-README.md.json, .planning/tmp/verify-GETTING-STARTED.md.json</files>
  <read_first>
    - .github/workflows/fathom-native-release.yml asset naming (fathom-lsp-linux-x86_64 / macos-aarch64 / windows-x86_64.exe) and manifest (fathom-lsp-manifest.json)
    - .planning/tmp/verify-README.md.json and verify-GETTING-STARTED.md.json current claim schema
  </read_first>
  <action>
Per D-04: add an "Install `fathom-lsp` from GitHub Release" section to README.md and the matching zh-CN section to README.zh-CN.md. Content must cover: (1) per-platform asset table — `fathom-lsp-linux-x86_64`, `fathom-lsp-macos-aarch64`, `fathom-lsp-windows-x86_64.exe` — downloaded from `https://github.com/tchivs/fathom-sql/releases` (choose the release tag matching the version); (2) SHA-256 verification — download `fathom-lsp-manifest.json` from the same release and verify each asset with `sha256sum -c` semantics (compare the digest in the manifest against the computed digest); (3) recommended install location `~/.fathom/bin` with `chmod +x` and PATH export; (4) verification command `fathom-lsp --version` must print `fathom-lsp 1.0.0` (exit 0). Add one line in GETTING-STARTED (en+zh) pointing to this README section.

Per D-05: regenerate `.planning/tmp/verify-README.md.json` and `.planning/tmp/verify-GETTING-STARTED.md.json` with the same schema ({doc_path, claims_checked, claims_passed, claims_failed, failures}) and claims now covering: LICENSE linked and Apache-2.0; no `<repository-url>` placeholder; no `0.1.20260724` reference; install section present with asset table, manifest SHA-256 verification, install location, and `--version` verification; claims_failed must be 0. Keep `docs-work-manifest.json` status verified (no change needed).
  </action>
  <verify>
    <automated>python3 - <<'PYEOF'
import json, re
from pathlib import Path
for p in ("README.md", "README.zh-CN.md"):
    t = Path(p).read_text()
    assert "Install `fathom-lsp` from GitHub Release" in t
    for a in ("fathom-lsp-linux-x86_64", "fathom-lsp-macos-aarch64", "fathom-lsp-windows-x86_64.exe"):
        assert a in t
    assert "fathom-lsp-manifest.json" in t and "sha256" in t.lower()
    assert "~/.fathom/bin" in t and "fathom-lsp --version" in t and "fathom-lsp 1.0.0" in t
for v in (".planning/tmp/verify-README.md.json", ".planning/tmp/verify-GETTING-STARTED.md.json"):
    d = json.loads(Path(v).read_text())
    assert d["claims_failed"] == 0 and d["claims_passed"] == d["claims_checked"] and d["claims_checked"] > 0
PYEOF
  </verify>
  <acceptance_criteria>
    - 安装章节四要素齐全（资产表/manifest SHA-256 校验/安装位置/`--version` 验证），en+zh 同步。
    - `verify-README.md.json` 与 `verify-GETTING-STARTED.md.json` claims_failed == 0 且 claims_passed == claims_checked。
  </acceptance_criteria>
  <done>安装指引完整落文档，核对记录可执行通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Doc claims -> readers | 失实声明会误导消费者；核对记录是发布证据。 |
| Install guide -> release assets | 资产命名/manifest 契约必须与 release 产物一致。 |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-16-01 | Spoofing | LICENSE 声明 | high | mitigate | 真实 LICENSE 链接 + claim 断言。 |
| T-16-02 | Tampering | 安装资产命名 | medium | mitigate | 与 release workflow 命名逐字一致断言。 |
| T-16-03 | Repudiation | 核对记录 | medium | mitigate | verify JSON claims_failed=0 + 可执行断言。 |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | 文档失实修正 + Release 安装指引 | 16-01 | COVERED | Tasks 1/2。
REQ | DOC-01 | README/GETTING-STARTED 失实声明修正 | 16-01 | COVERED | Task 1。
REQ | DOC-02 | Release 安装 fathom-lsp 章节 | 16-01 | COVERED | Task 2。
CONTEXT | D-01 | 工具链引用修正 | 16-01 | COVERED | Task 1。
CONTEXT | D-02 | LICENSE 声明 | 16-01 | COVERED | Task 1。
CONTEXT | D-03 | 占位符/remote | 16-01 | COVERED | Task 1。
CONTEXT | D-04 | 安装章节 | 16-01 | COVERED | Task 2。
CONTEXT | D-05 | verify JSON 核对 | 16-01 | COVERED | Task 2。
CONTEXT | D-06 | 范围边界 | 16-01 | COVERED | 四文档 + 核对记录。
</source_audit>

## Artifacts this phase produces
- README.md / README.zh-CN.md — 修正后的 en/zh README（工具链、License、安装章节）。
- docs/GETTING-STARTED.md / docs/zh-CN/GETTING-STARTED.md — 真实 remote、锁定工具链、安装指引指针。
- .planning/tmp/verify-README.md.json / verify-GETTING-STARTED.md.json — 重生成的核对记录。

<verification>
- 可执行 python 断言覆盖 DOC-01（无 0.1.20260724/无假 LICENSE/无占位符）与 DOC-02（资产表/manifest/安装位置/--version）；verify JSON claims_failed=0。
</verification>

<success_criteria>
1. 四文档零失实声明，README 两语言含完整 Release 安装章节。
2. 核对记录全部通过（claims_failed=0），docs-work-manifest 保持 verified。
</success_criteria>

<output>
Create `.planning/phases/16-documentation-truthfulness-install-guide/16-01-SUMMARY.md` when done.
</output>
