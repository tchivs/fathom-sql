---
phase: 07-column-lineage
plan: 05
type: execute
wave: 4
depends_on:
  - 04
files_modified:
  - parity/lineage_parity_test.mbt
  - parity/run_js.mbt
  - parity/run_wasm.mbt
  - parity/export_smoke_test.mbt
  - docs/API.md
  - docs/zh-CN/API.md
  - .planning/phases/07-column-lineage/COVERAGE.md
autonomous: true
requirements:
  - LINE-01
estimate:
  tokens: 58000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "parity/lineage_parity_test.mbt：同一 fixture 经 fathom_lineage_v1 在 native/js/wasm 三目标产出相同 edges/gaps 字节（仿 fingerprint_parity_test.mbt:10-32 硬编码期望值断言；compare_backends.py 自动纳入 parity/__snapshot__ digest）——LINE-01 跨目标一致证明（D-07/Phase 12 D-03 纪律）"
    - "parity/run_js.mbt + run_wasm.mbt 冒烟调用 fathom_lineage_v1（Bytes+String+Int 参数 ABI，无 println/env 主机 IO——linear-Wasm 可移植性，Pitfall 8）"
    - "parity/export_smoke_test.mbt schema_v2_bump_is_additive 断言追加 fathom.lineage.v1（第 8 命名空间出现 + 既有 7 命名空间仍可用，Pitfall V6 加法）"
    - "docs/API.md + docs/zh-CN/API.md：Lineage Entry Points 章节（lineage_text 签名 + LineageResult/LineageEdge/LineageGap 结构 + 可选 catalog 语义）+ Wire Exports 表新增 fathom_lineage_v1 行（第 8 导出）+ 端点一览表新增 lineage_text 行 + D-08 flink 门禁说明 + SC2 无 catalog gap 文档"
    - "COVERAGE.md api-coverage 声明（api_coverage_gate 需要）：No external API integration——Phase 7 新增内部 lineage 分析包/内部 wire 命名空间/CLI 子命令，无外部 API/SDK/service 集成（内容形状锁定）"
    - "FLAGGED ASSUMPTION (LINE-01 ordering, UNRESOLVED→RESOLVED): 三目标 parity 的 edges/gaps 字节一致依赖 07-02 顺序契约（文档序 → 模型序 → refs 序 + LinkedHashMap catalog 列序）——parity 测试用硬编码期望值锁定同一 fixture 的确定性输出"
    - "FLAGGED ASSUMPTION (LINE-01 cross-target, UNRESOLVED→RESOLVED): fathom_lineage_v1 全参数 primitive（Bytes/String），无对象句柄/宿主对象跨边界——三目标同 fixture 字节一致可由 compare_backends.py 证明"
  artifacts:
    - parity/lineage_parity_test.mbt（三目标 edges/gaps 字节一致 + 硬编码期望值）
    - parity/run_js.mbt + parity/run_wasm.mbt（冒烟 fathom_lineage_v1）
    - parity/export_smoke_test.mbt（schema 第 8 命名空间加法断言）
    - docs/API.md + docs/zh-CN/API.md（Lineage 章节 + Wire Exports 行 + 端点行 + D-08/SC2 说明）
    - .planning/phases/07-column-lineage/COVERAGE.md（api-coverage 声明）
  key_links:
    - "lineage_parity_test → fathom_lineage_v1：三目标同一 ABI 调用点证明字节一致（run_js/run_wasm 冒烟保证符号存在 + 参数 ABI，compare_backends.py 聚合 digest）"
    - "export_smoke 第 8 命名空间断言 → schema.mbt（07-04）：Pitfall V6 加法被锁定，既有 7 命名空间消费者零影响"
    - "docs Wire Exports 表 ↔ binding/moon.pkg exports 列表：文档与注册清单一致（漏注册静默缺符号，Pitfall 5）"
    - "COVERAGE.md ↔ 本 plan 内 plan body 的 api.lineage_text/fathom.lineage.v1 术语：api-coverage 检测器 seal 时以 COVERAGE.md 的 reasoned declaration 为准（无外部 API）"
  prohibitions:
    - statement: "parity 测试永不修改 compare_backends.py 的 PACKAGE/DEFAULT_TARGETS 之外的逻辑（自动纳入 lineage_parity_test，Phase 12 D-03 纪律）——FLAGGED-UNVERIFIED（无 wired check，specless 描述符空）"
    - statement: "文档/COVERAGE.md 永不宣称外部 API 集成（本阶段纯内部 lineage/wire/CLI，无外部 API/SDK/service）——FLAGGED-UNVERIFIED（无 wired check，specless 描述符空）"
    - statement: "docs 永不宣称 lineage 支持 flink（D-08 Doris-only；flink → FATHOM-SCHEMA-003）——FLAGGED-UNVERIFIED（无 wired check，specless 描述符空）"
---

<objective>
交付 **LINE-01** 的收尾：三目标跨端 parity（native/js/linear-wasm 字节一致，D-07/Phase 12 D-03）+ docs/API.md 双语 Lineage 章节 + `COVERAGE.md` api-coverage 声明（api_coverage_gate 需要）。

Purpose: ROADMAP SC1/SC2 的交付面完整对齐 Phase 6 模式（07-CONTEXT D-07 明文）——parity 证明血缘结果在 Native/JS/Wasm 三目标字节一致（跨端可移植承诺），docs 让用户可查阅 Lineage 公共 API，COVERAGE.md 满足确定性 api-coverage 检测器的 seal 要求。
Output: parity 跨目标证明 + docs 双语章节 + COVERAGE.md 声明。

Execution note（scope_sanity）：本 plan files_modified=7，3 tasks——Task 1（parity tracer）、Task 2（docs）、Task 3（COVERAGE.md）。**按 task 原子提交**；Task 2/3（docs/COVERAGE）只依赖 07-03/07-04 的最终签名（lineage_text/fathom_lineage_v1/lineage_result_json），与本 plan Task 1（parity）无依赖，可并行执行。
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-column-lineage/07-CONTEXT.md
@.planning/phases/07-column-lineage/07-RESEARCH.md
@.planning/phases/07-column-lineage/07-PATTERNS.md
@parity/fingerprint_parity_test.mbt
@parity/export_smoke_test.mbt
@parity/run_js.mbt
@parity/run_wasm.mbt
@parity/moon.pkg
@docs/API.md
@docs/zh-CN/API.md
@binding/exports.mbt
@api/api.mbt
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: parity lineage 跨目标字节一致 + wire 冒烟 + export smoke 第 8 命名空间断言</name>
  <files>
    parity/lineage_parity_test.mbt
    parity/run_js.mbt
    parity/run_wasm.mbt
    parity/export_smoke_test.mbt
  </files>
  <read_first>
    parity/fingerprint_parity_test.mbt（fingerprint_parity_test.mbt:10-32——硬编码期望值断言先例：同一 fixture 三目标同一输出）
    parity/export_smoke_test.mbt schema_v2_bump_is_additive（export_smoke_test.mbt:1-29——schema 加法断言模板）
    parity/run_js.mbt + parity/run_wasm.mbt（现有冒烟形状：无 println/env，linear-Wasm primitive-only 头注释纪律）
    scripts/compare_backends.py + parity/__snapshot__（Phase 12 三目标聚合机制；PACKAGE="parity" DEFAULT_TARGETS=("native","js","wasm") 自动纳入新测试）
    binding/exports.mbt fathom_lineage_v1（07-04 产物）+ fathom_lint_v1/fathom_fingerprint_v1（既有冒烟先例）
    07-RESEARCH.md Pattern 6（边/gap 顺序确定性 = parity 前置）+ Pitfall 6
    07-PATTERNS.md「6. parity 跨目标：三目标字节一致」
  </read_first>
  <behavior>
    - Test 1（三目标字节一致）：fathom_lineage_v1(b"SELECT a FROM t", "doris", "4.x", "strict", b"") 在 native/js/wasm 三目标产出相同 envelope 字节（compare_backends.py 聚合 parity/__snapshot__）
    - Test 2（硬编码期望值）：同 fixture 的 edges/gaps JSON 断言（含 "fathom.lineage.v1"、"\"dialect\":\"doris\""、边字段名、gaps 数组）在三目标一致
    - Test 3（export smoke 加法）：fathom_lineage_v1 输出含 "fathom.lineage.v1"；既有 7 命名空间（fathom_parse_v1/fathom_lint_v1/fathom_fingerprint_v1 等）仍可用（Pitfall V6）
    - Test 4（run_js/run_wasm 冒烟）：fathom_lineage_v1(b"SELECT a FROM t", "doris", "4.x", "strict", b"") 在 js/wasm 产物含符号且不崩溃（无主机 IO）
  </behavior>
  <action>
    新增 `parity/lineage_parity_test.mbt`（LINE-01 跨目标一致，D-07/Phase 12 D-03）：复用 fingerprint_parity_test.mbt 的硬编码期望值模式——对选定 fixture（`SELECT a + b AS x FROM t` 等，含边；`SELECT * FROM t` 无 catalog 含 requires-catalog gap）调 `@binding.fathom_lineage_v1(source, "doris", "4.x", "strict", b"")`，断言 envelope 含 `fathom.lineage.v1`、`"dialect":"doris"`、`"edges"`、`"gaps"`，并对 edges/gaps 数组逐字段断言确定性值；parity 测试本身在 native/js/wasm 三目标跑同一断言，`scripts/compare_backends.py` 自动纳入（PACKAGE="parity" 不动）聚合 parity/__snapshot__ digest——证明同一 fixture 三目标字节一致。扩展 `parity/run_js.mbt` + `parity/run_wasm.mbt`：各追加 `let lineaged = @binding.fathom_lineage_v1(b"SELECT a FROM t", "doris", "4.x", "strict", b"")` + `ignore(lineaged)`（冒烟 Bytes+String 参数 ABI；无 println/env，linear-Wasm 可移植性，run_wasm.mbt 头注释纪律）。扩展 `parity/export_smoke_test.mbt` 的 `schema_v2_bump_is_additive`：追加 `fathom_lineage_v1` 输出含 `fathom.lineage.v1` 的断言 + 保留既有 7 命名空间断言（Pitfall V6 纯增）。
  </action>
  <verify>
    <automated>moon test --target native --package parity && moon test --target js --package parity && moon test --target wasm --package parity</automated>
  </verify>
  <acceptance_criteria>
    - lineage_parity_test.mbt 存在，含硬编码期望值断言（edges/gaps 字段 + dialect/profile 元数据）；compare_backends.py 聚合 parity/__snapshot__ 通过（三目标 digest 一致）
    - 同一 fixture 在 native/js/wasm 三目标产出相同 lineage envelope 字节（无 catalog 与有 catalog 各至少一个 fixture）
    - run_js/run_wasm 冒烟含 fathom_lineage_v1 调用且通过（js/wasm 产物含符号，无主机 IO）
    - export_smoke_test 断言 fathom.lineage.v1 出现 + 既有 7 命名空间仍可用（git diff 确认是追加断言）
  </acceptance_criteria>
  <done>LINE-01 跨 Native/JS/linear-Wasm 三目标字节一致由 parity 证明；schema 第 8 命名空间加法被 smoke 断言锁定</done>
  <reversibility rating="reversible">parity 跨目标测试与 smoke 断言是测试面扩展；所锁定决策（D-07 wire/parity 纪律、Pitfall V6 schema 加法）已在 07-04 评级——本 task 仅补测试，可逆</reversibility>
</task>

<task type="auto">
  <name>Task 2: docs/API.md + docs/zh-CN/API.md — Lineage 章节</name>
  <files>
    docs/API.md
    docs/zh-CN/API.md
  </files>
  <read_first>
    docs/API.md 既有结构（Analysis Entry Points / Lint Entry Points / Wire Exports 表 / 端点一览表）
    docs/zh-CN/API.md（中文镜像结构）
    api/api.mbt lineage_text + LineageResult/LineageEdge/LineageGap/StaticCatalog（07-03 最终签名）
    binding/exports.mbt fathom_lineage_v1 + catalog_json（07-04 最终签名）
    binding/schema.mbt LINEAGE_SCHEMA_VERSION（07-04 产物）
    07-RESEARCH.md Pattern 5（envelope/CLI/D-08 门禁说明）+ SC2（无 catalog gap 文档）
  </read_first>
  <action>
    更新 `docs/API.md`（commit_docs: true）：新增 **"Lineage Entry Points"** 节（仿 Lint Entry Points）——`lineage_text(raw, parse_options, catalog?)` 签名、`LineageResult`/`LineageEdge`/`LineageGap` 结构（字段含 source_name/source_resolved_to/source_start_byte/source_end_byte/target_name/target_start_byte/target_end_byte 与 gap code），可选 catalog 语义（None → `*`/外部视图产 requires-catalog gap，SC2 明文），gap codes 表（requires-catalog/unresolved-reference/requires-complete-parse）；**Wire Exports 表**新增 `fathom_lineage_v1(raw, dialect, profile, mode, catalog_json)` 行（第 8 导出，注明 catalog_json 空 bytes = 无 catalog）；**端点一览表**新增 lineage_text 行；**D-08 flink 门禁**说明（flink 选择 → FATHOM-SCHEMA-003 "lineage is Doris-only"，绝不静默空结果）；schema 版本说明（fathom.lineage.v1 为第 8 命名空间，纯增）。同步更新 `docs/zh-CN/API.md` 同结构中文版。
  </action>
  <verify>
    <automated>
grep -c "lineage_text" docs/API.md
grep -c "fathom_lineage_v1" docs/API.md
grep -c "fathom.lineage.v1" docs/API.md
grep -c "requires-catalog" docs/API.md
grep -c "lineage_text" docs/zh-CN/API.md
grep -c "fathom_lineage_v1" docs/zh-CN/API.md
    </automated>
  </verify>
  <acceptance_criteria>
    - docs/API.md 含 lineage_text（≥1 处）、fathom_lineage_v1（≥1 处）、fathom.lineage.v1（≥1 处）、requires-catalog（≥1 处，SC2 gap 语义）
    - docs/API.md Wire Exports 表含 fathom_lineage_v1 行 + Lineage Entry Points 节 + D-08 flink 门禁说明
    - docs/zh-CN/API.md 同步含 lineage_text/fathom_lineage_v1（各 ≥1 处）与同结构章节
    - 端点一览表新增 lineage_text 行
  </acceptance_criteria>
  <done>Lineage 公共 API 文档双语交付；Wire Exports 表 + 端点表 + D-08/SC2 说明同步</done>
  <reversibility rating="reversible">文档同步（markdown）可逆；所文档化 ABI（D-07 wire/CLI、D-08 门禁、D-06 gap 码）已在 07-03/07-04 评级——本 task 仅补文档，不新增决策</reversibility>
</task>

<task type="auto">
  <name>Task 3: COVERAGE.md api-coverage 声明</name>
  <files>
    .planning/phases/07-column-lineage/COVERAGE.md
  </files>
  <read_first>
    api/api.mbt lineage_text（07-03 产物——plan body 引用的内部术语）
    binding/exports.mbt fathom_lineage_v1 + binding/schema.mbt LINEAGE_SCHEMA_VERSION（07-04 产物）
    .planning/config.json api_coverage_gate: true（seal 时检测器）
    07-CONTEXT.md D-07/D-08（交付面：内部 lineage 包 + 内部 wire 命名空间 + CLI 子命令，无外部 API）
  </read_first>
  <action>
    新建 `.planning/phases/07-column-lineage/COVERAGE.md`，写入**精确内容形状**（api_coverage_declaration 锁定，plan body 引用的 api.lineage_text/fathom.lineage.v1 术语由本声明在 seal 时消解）：

```markdown
# API Coverage
No external API integration: Phase 7 adds an internal lineage analysis package (lineage/), an internal wire namespace (fathom.lineage.v1), and a fathom-sql lineage CLI subcommand. No external API/SDK/service is integrated.
```
    除上述两行外不添加其他内容（确定性检测器按形状匹配；如执行器需附注，追加在 # API Coverage 标题之后、声明块之外，且不改变声明句）。文件写入后 `read` 复核字节与锁定形状一致。
  </action>
  <verify>
    <automated>
grep -c "No external API integration" .planning/phases/07-column-lineage/COVERAGE.md
grep -c "lineage/" .planning/phases/07-column-lineage/COVERAGE.md
grep -c "fathom.lineage.v1" .planning/phases/07-column-lineage/COVERAGE.md
    </automated>
  </verify>
  <acceptance_criteria>
    - COVERAGE.md 存在且含 "No external API integration"（≥1 处）、"lineage/"（≥1 处）、"fathom.lineage.v1"（≥1 处）、"fathom-sql lineage CLI subcommand"（声明句）
    - 声明句与 api_coverage_declaration 锁定形状字节一致（read 复核）
    - 不声称任何外部 API/SDK/service 集成（本阶段纯内部）
  </acceptance_criteria>
  <done>api-coverage 声明落盘，seal 时确定性检测器对 plan body 的 api.lineage_text/fathom.lineage.v1 术语以 reasoned declaration 消解</done>
  <reversibility rating="reversible">COVERAGE.md 是文档声明（markdown），可逆；声明内容由 api_coverage_gate 形状锁定</reversibility>
</task>

</tasks>

## Artifacts this phase produces

- **新符号（扩展既有文件）：**
  - `parity/lineage_parity_test.mbt`（新增）— 三目标 edges/gaps 字节一致（硬编码期望值 + compare_backends.py 聚合）。
  - `parity/run_js.mbt` + `parity/run_wasm.mbt` — 冒烟调 `fathom_lineage_v1`（Bytes+String 参数 ABI）。
  - `parity/export_smoke_test.mbt` — schema 第 8 命名空间加法断言（fathom.lineage.v1 出现 + 既有 7 命名空间仍可用）。
  - `docs/API.md` + `docs/zh-CN/API.md` — Lineage Entry Points 章节 + Wire Exports 表 fathom_lineage_v1 行 + 端点一览表 lineage_text 行 + D-08/SC2 说明。
  - `.planning/phases/07-column-lineage/COVERAGE.md`（新增）— api-coverage 声明（No external API integration 形状锁定）。

- **明确不改动：** `lineage/`、`api/`、`binding/`、`fathom-sql/`（07-02/07-03/07-04 产物，只消费）、`scripts/compare_backends.py`（PACKAGE/DEFAULT_TARGETS 不动）、`parser/`/`analyzer/`。

## threat_model

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parity fixture → fathom_lineage_v1（三目标） | 未信任 fixture SQL；全 primitive 参数（Bytes/String）；无对象句柄跨边界 |
| compare_backends.py → parity/__snapshot__ | 三目标 digest 回归门禁；lineage 命名空间独立 |
| docs/COVERAGE.md → api-coverage 检测器 | seal 时声明文档；内容形状锁定 |

### STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05-01 | Tampering | 三目标 lineage 字节漂移（顺序/序列化） | high | mitigate | lineage_parity_test 硬编码期望值 + compare_backends.py 聚合 digest（Phase 12 D-03）；边/gap 顺序契约（Pattern 6） |
| T-07-05-02 | Spoofing | 快照漂移掩盖回归 | medium | mitigate | parity/__snapshot__ 命名空间独立 + export_smoke 加法断言（Pitfall V6）；--update 唯一写路径 |
| T-07-05-03 | Tampering | docs 宣称外部 API 集成（api-coverage 误触发） | medium | mitigate | COVERAGE.md 精确形状声明（No external API integration）；docs 只描述内部 lineage/wire/CLI |
| T-07-05-04 | Spoofing | docs 宣称 flink lineage 支持 | medium | mitigate | docs D-08 门禁说明（flink → FATHOM-SCHEMA-003，Doris-only） |
| T-07-05-SC | Tampering | npm/pip/cargo 安装 | low | accept | 零新增外部依赖 |

<verification>
- Task 1 后：`moon test --target native --package parity` + js + wasm 全绿；compare_backends.py 聚合报告一致（lineage 命名空间纳入 digest）。
- Task 2 后：docs grep 断言通过（lineage_text/fathom_lineage_v1/fathom.lineage.v1/requires-catalog 各 ≥1 处，双语）。
- Task 3 后：COVERAGE.md grep 断言通过 + read 复核形状字节一致。
</verification>

<success_criteria>
- LINE-01 跨 Native/JS/linear-Wasm 三目标血缘结果字节一致由 parity 证明（D-07 交付面闭环）。
- Lineage 公共 API 文档双语交付；api-coverage 声明（COVERAGE.md）落盘满足 seal 检测器。
- Phase 7 全部交付面完整：lineage/ + api.lineage_text + fathom.lineage.v1 + CLI lineage + parity + docs——LINE-01 全链闭环。
</success_criteria>

<output>
Create `.planning/phases/07-column-lineage/07-05-SUMMARY.md` when done.
</output>
