---
phase: 07-column-lineage
plan: 03
type: execute
wave: 2
depends_on:
  - 02
files_modified:
  - api/api.mbt
  - api/moon.pkg
  - test/lineage_test.mbt
  - test/moon.pkg
autonomous: true
requirements:
  - LINE-01
estimate:
  tokens: 56000
  raw_tokens: 37000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "api.lineage_text(raw, parse_options, catalog : @analyzer.StaticCatalog?) -> Result[LineageResult, ParseError]（D-07）：复用 parse_document（validate_limits → SourceText::new_with_limit → parse_with_limits_context → is_valid 门禁，api.mbt:717-741）；flink 门禁（D-08）在 parse 后显式拒绝，绝不静默空结果"
    - "D-08 方言门禁：lineage_text 对 dialect_id()==\"flink\" 返回结构化 ParseError（FATHOM-SCHEMA 族，复用 UnsupportedProfile→FATHOM-SCHEMA-003 语义，RESEARCH A6）；wire/CLI 面（07-04）在边界拦截 flink 并给明确 message \"lineage is Doris-only\"；不新造 FATHOM-LINE-*、不建 fathom.lineage.flink"
    - "api 类型再导出（D-38 别名块）：pub type LineageResult = @lineage.LineageResult / LineageEdge / LineageGap + pub type StaticCatalog = @analyzer.StaticCatalog（catalog 面经 api 暴露，api.mbt:539-557 既有别名块模式）"
    - "可选 catalog 纪律（ANLY-01/T-02-42）：catalog 为 StaticCatalog?；None → 所有星号/外部视图按 gap 报告（SC2），结果字节与 wire 缺省一致；有 catalog 星号展开为真实边"
    - "test/lineage_test.mbt 集成测试 + 快照 golden（analyzer_anal01_test.mbt 模式）：lineage 不能 import parser（D-21），集成测试放 test/——parse → @api.lineage_text（含 catalog）→ 快照锁定 edges/gaps 结构与顺序（moon test --update 唯一写路径）"
    - "FLAGGED ASSUMPTION (LINE-01 empty, UNRESOLVED→RESOLVED): lineage_text 对空文档返回 Ok(LineageResult{edges:[],gaps:[]})（accepted 语义）；无 catalog 参数（None）与空 StaticCatalog 等价——都是零元数据"
    - "FLAGGED ASSUMPTION (LINE-01 idempotency/concurrency, UNRESOLVED→RESOLVED): lineage_text 是纯函数——同 (raw, parse_options, catalog) 重跑产出字节一致结果；并行调用无共享状态"
  artifacts:
    - api/api.mbt（pub fn lineage_text + LineageResult/LineageEdge/LineageGap/StaticCatalog 类型再导出）
    - api/moon.pkg（+ analyzer + lineage import）
    - test/lineage_test.mbt（parse → lineage_text 集成测试 + 快照 golden）
    - test/moon.pkg（+ lineage import）
  key_links:
    - "lineage_text → parse_document → @lineage.derive_lineage：库面唯一入口链路，catalog 从 api 可选参数经 derive_lineage 注入（T-02-42 调用方拥有元数据）"
    - "flink 门禁在 api 层（ParseError）与 wire/CLI 层（07-04 明确 message）双保险——绝不静默空结果（D-08）"
    - "type 再导出使 binding（07-04）经 @api.LineageResult 构造 envelope，不直接依赖 lineage/——api 是共享核心入口（D-38 纪律）"
  prohibitions:
    - statement: "lineage_text 永不对 flink 选择返回空结果（D-08：结构化 FATHOM-SCHEMA 错误，绝不静默）——FLAGGED-UNVERIFIED（无 wired check，specless 描述符空）"
    - statement: "api 层永不接受 catalog 元数据改变语法 valid 通道（ANLY-01：catalog 只进 analyzer，不进 parser/语法诊断）——FLAGGED-UNVERIFIED（无 wired check，specless 描述符空）"
    - statement: "catalog 参数永不默认为伪造的空 StaticCatalog 假装有元数据——None 就是无元数据，星号走 requires-catalog gap（SC2）——FLAGGED-UNVERIFIED（无 wired check，specless 描述符空）"
---

<objective>
交付 **LINE-01** 的库面消费入口：`api.lineage_text(raw, parse_options, catalog?)`——复用 `parse_document` 内部解析，调 `@lineage.derive_lineage`，flink 门禁（D-08），类型经 D-38 别名块再导出；并在 `test/` 包建 parse → lineage 集成测试 + 快照 golden。

Purpose: ROADMAP SC1/SC2 的用户承诺需要库面可调用入口（D-07「api.lineage_text 序列化入口」）。api 是共享核心入口（D-38），binding wire（07-04）与 CLI（07-04）都经它消费 lineage，故本 plan 先行冻结库面签名与类型再导出。
Output: `api.lineage_text` + 类型再导出 + 集成测试快照。

Execution note（scope_sanity）：本 plan files_modified=4，2 tasks——Task 1（api 入口 tracer + flink 门禁）、Task 2（test/ 集成 + 快照）。**按 task 原子提交**。Task 1 的 tracer 先跑通 parse → derive_lineage → LineageResult 的最小链路；Task 2 补快照 golden。api/moon.pkg 增加 analyzer + lineage import（07-PATTERNS 已给 verbatim import 面）。
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-column-lineage/07-CONTEXT.md
@.planning/phases/07-column-lineage/07-RESEARCH.md
@.planning/phases/07-column-lineage/07-PATTERNS.md
@api/api.mbt
@api/moon.pkg
@lineage/model.mbt
@lineage/edges.mbt
@test/analyzer_anal01_test.mbt
@test/moon.pkg
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: api.lineage_text + 类型再导出 + flink 门禁（D-08）</name>
  <files>
    api/api.mbt
    api/moon.pkg
  </files>
  <read_first>
    api/api.mbt parse_document（api.mbt:717-741——lineage_text 复用同款内部 parse）+ lint_text（api.mbt:757-782——parse_document 复用 + 注入点模板）+ fingerprint_text（api.mbt:677-694）+ D-38 类型别名块（api.mbt:539-557）
    api/moon.pkg（import 面，需增 analyzer + lineage）
    lineage/model.mbt + lineage/edges.mbt（07-02 产物：LineageResult/LineageEdge/LineageGap + derive_lineage 签名）
    binding/schema.mbt validate_dialect_profile + schema_error_code（schema.mbt——FATHOM-SCHEMA-003 语义，D-08 门禁的 wire 面依据）
    07-RESEARCH.md Pattern 5（lineage_text 推荐签名 + flink 门禁）+ Open Question 5
    07-PATTERNS.md「3. api 序列化入口：lineage_text」
  </read_first>
  <behavior>
    - Test 1（库面入口）：lineage_text(b"SELECT a FROM t", ParseOptions::new("doris","4.x","strict"), Some(catalog)) → Ok(LineageResult) 含 1 边 t.a→a
    - Test 2（flink 门禁）：lineage_text(b"SELECT a FROM t", ParseOptions::new("flink","flink-2.3.0","strict"), None) → Err（结构化 ParseError，FATHOM-SCHEMA 族），绝不 Ok(空)
    - Test 3（无 catalog）：lineage_text(b"SELECT * FROM t", ParseOptions::new("doris","4.x","strict"), None) → Ok(LineageResult{edges:[], gaps:[requires-catalog]})
    - Test 4（无效树）：lineage_text(超限输入, ..., None) → Err(InputTooLarge/InvalidSyntaxTree)（与 format_text 一致的 ParseError 前置）
  </behavior>
  <action>
    扩展 `api/moon.pkg`：增 `"fathom/sql/analyzer" @analyzer,` + `"fathom/sql/lineage" @lineage,`（07-PATTERNS verbatim import 面）。在 `api/api.mbt` 新增 `pub fn lineage_text(raw : Bytes, parse_options : ParseOptions, catalog : @analyzer.StaticCatalog?) -> Result[LineageResult, ParseError]`：(1) 复用 `parse_document`（api.mbt:717-741）得 (parsed, source)；(2) **flink 门禁（D-08）**：`if parse_options.dialect_id() == "flink" { return Err(UnknownProfile(profile_id=parse_options.dialect_context.profile_id)) }`——结构化 ParseError（FATHOM-SCHEMA-003 族，RESEARCH A6；明确 message 由 wire/CLI 层 07-04 在边界给出 "lineage is Doris-only"），**绝不静默空结果**；(3) 调 `@analyzer.analyze(parsed.root, source.bytes(), catalog)`（catalog 为 None 时传空 StaticCatalog 或经 derive_lineage 的可选语义——以 07-02 derive_lineage 签名为准）；(4) 调 `@lineage.derive_lineage(parsed.root, source.bytes(), catalog)` 得 LineageResult → `Ok(result)`。**类型再导出（D-38 别名块）**：在 lint 别名块（api.mbt:539-557）后追加 `pub type LineageResult = @lineage.LineageResult`、`pub type LineageEdge = @lineage.LineageEdge`、`pub type LineageGap = @lineage.LineageGap`、`pub type StaticCatalog = @analyzer.StaticCatalog`（catalog 面经 api 暴露）。**首探针**：`moon check --target native` 核实 api 层跨包调 @lineage.derive_lineage（@syntax.SyntaxNode 参数 + Catalog? 可选）编译；若 derive_lineage 是泛型 [T:Catalog]，api 用 StaticCatalog? 具现或经 ViewCatalog/空 catalog 适配，记录结论。在 api.mbt 内加 test（api 包白盒）：Behavior Test 1-4（flink 门禁断言 Err；无 catalog 星号断言 requires-catalog）。
  </action>
  <verify>
    <automated>moon check --target native && moon test --target native --package api</automated>
  </verify>
  <acceptance_criteria>
    - api.mbt 含 lineage_text + 4 个类型再导出（LineageResult/LineageEdge/LineageGap/StaticCatalog）；api/moon.pkg 含 analyzer + lineage import
    - api 包内 test 全绿：doris 有效输入 Ok 含边；flink → Err（FATHOM-SCHEMA 族，绝不 Ok 空）；无 catalog 星号 → requires-catalog gap；超限/无效树 → ParseError（与 format_text 一致）
    - parse_document 复用（不复制解析逻辑）；flink 门禁在 parse 后、derive 前显式拒绝
    - lineage/ 仍只 import analyzer+syntax（D-21 未被 api 层破坏）
  </acceptance_criteria>
  <done>api.lineage_text 库面入口可用（含 flink 门禁与可选 catalog），类型经 D-38 别名再导出，binding/CLI 可经 @api 消费</done>
  <reversibility rating="one-way">D-07 交付面与 D-08 方言门禁是公开 ABI；lineage_text 签名发布后改动需迁移宿主——决策已在 CONTEXT.md 锁定，不插 re-ask checkpoint</reversibility>
</task>

<task type="auto">
  <name>Task 2: test/lineage_test.mbt 集成测试 + 快照 golden</name>
  <files>
    test/lineage_test.mbt
    test/moon.pkg
  </files>
  <read_first>
    test/analyzer_anal01_test.mbt（analyze_sql 助手 + analyzer_snapshot_test 快照模式，analyzer_anal01_test.mbt:9-90——lineage 集成测试镜像）
    test/moon.pkg（import 面，需增 lineage）
    api/api.mbt lineage_text + LineageResult（Task 1 产物）
    lineage/model.mbt（LineageEdge/LineageGap/LineageResult 形状——lineage_result_to_json 渲染）
    07-RESEARCH.md Pattern 7（测试/快照：lineage_result_to_json + lineage_snapshot_test）+ Pitfalls 1-4（测试用例覆盖要求）
    07-PATTERNS.md「7. 测试/快照：边/gap 快照 golden + 集成测试」
  </read_first>
  <action>
    扩展 `test/moon.pkg`：增 `"fathom/sql/lineage" @lineage,`。新建 `test/lineage_test.mbt`（镜像 analyzer_anal01_test.mbt 结构）：(1) `fn lineage_sql(raw : Bytes, catalog : @analyzer.StaticCatalog?) -> @api.LineageResult` 助手——@parser.parse_with_limits（doris 4.x strict）→ @api.lineage_text(raw, ParseOptions::new("doris","4.x","strict"), catalog) → match Ok/panic；(2) `fn lineage_result_to_json(result : @api.LineageResult) -> String` 确定性渲染（edges/gaps 字段逐项 JSON 字符串，仿 analysis_result_to_json）——**边/gap 顺序即文档序，快照锁定（Pattern 6）**；(3) `lineage_snapshot_test(t, content, filename)` 快照助手（moon test --update 唯一写路径）；(4) 集成用例覆盖研究 Pitfalls 1-4：表达式直通（`SELECT a + b AS x FROM t` → 2 边快照）、`*` 无 catalog → requires-catalog gap（SC2 断言 edges==[]）、有 catalog `*` 展开为真实边、CTE/UNION/INSERT 位置映射（`WITH c AS (...) SELECT ... FROM c` / `SELECT a FROM t UNION SELECT b FROM t2` / `INSERT INTO t(c1,c2) SELECT a,b FROM u`）、`* EXCEPT (b)`（07-01 诚实展开，被排除列无绑定/边）、视图注册表（`CREATE VIEW v AS SELECT a FROM t; SELECT * FROM v`）。快照文件名仿 analyzer 命名（`lineage.select-basic.doris-4.x.json` 等）。断言顺序确定性：对含多语句/多分支文档断言 edges/gaps 数组顺序与文档序一致。
  </action>
  <verify>
    <automated>moon test --target native --package test</automated>
  </verify>
  <acceptance_criteria>
    - test/moon.pkg 含 lineage import；test/lineage_test.mbt 存在且镜像 analyzer_anal01_test.mbt 模式
    - 集成用例全绿：表达式直通 2 边 / 无 catalog 星号 requires-catalog 0 边 / 有 catalog 星号真实边 / CTE/UNION/INSERT 位置映射 / `* EXCEPT` 诚实展开 / 视图注册表展开
    - 快照 golden 锁定 edges/gaps 结构与顺序（moon test --update 为唯一写路径，git diff 确认新增快照文件）
    - 快照文件名含 doris-4.x 与用例名；顺序断言（多语句/多分支）通过
  </acceptance_criteria>
  <done>lineage 库面经真实 parse 的集成测试 + 快照 golden 锁定（跨 SELECT/INSERT/CTE/UNION/视图/EXCEPT/无 catalog 场景）</done>
  <reversibility rating="reversible">集成测试与快照是测试面扩展；所锁定行为决策（D-01 边语义/D-06 gap 码/D-03 视图）已在 07-02 评级——本 task 仅补测试，可逆</reversibility>
</task>

</tasks>

## Artifacts this phase produces

- **新符号（扩展既有文件）：**
  - `api/api.mbt` — `pub fn lineage_text(raw : Bytes, parse_options : ParseOptions, catalog : @analyzer.StaticCatalog?) -> Result[LineageResult, ParseError]`；D-38 别名追加 `pub type LineageResult = @lineage.LineageResult`、`pub type LineageEdge = @lineage.LineageEdge`、`pub type LineageGap = @lineage.LineageGap`、`pub type StaticCatalog = @analyzer.StaticCatalog`。
  - `api/moon.pkg` — import 面增 `"fathom/sql/analyzer" @analyzer`、`"fathom/sql/lineage" @lineage`。
  - `test/lineage_test.mbt`（新增）— `lineage_sql` 助手 + `lineage_result_to_json` + `lineage_snapshot_test` + 集成用例（表达式直通/star/CTE/UNION/INSERT/EXCEPT/视图/无 catalog）。
  - `test/moon.pkg` — import 面增 `"fathom/sql/lineage" @lineage`。

- **明确不改动：** `lineage/`（07-02 产物，本 plan 只消费）、`binding/`/`fathom-sql/`/`parity/`（07-04/07-05）、`parser/`/`analyzer/`。

## threat_model

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 调用方 raw bytes + ParseOptions + catalog → lineage_text | 未信任 SQL 与 catalog；ParseLimits 封顶；flink 门禁在此边界 |
| lineage_text → @lineage.derive_lineage | 库面链路；catalog 经 api 可选参数注入（T-02-42） |
| @api.LineageResult → 调用方 | 库面结果；binding wire（07-04）经 @api 构造 envelope |

### STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03-01 | Spoofing | flink 选择被静默当作 Doris 处理 | high | mitigate | lineage_text 显式 flink 门禁 → Err（FATHOM-SCHEMA-003 族，RESEARCH A6）；wire/CLI 边界（07-04）双保险给明确 message——绝不静默空结果（D-08） |
| T-07-03-02 | Spoofing | catalog 元数据影响语法 valid 通道（ANLY-01） | medium | mitigate | catalog 只进 @analyzer/@lineage，parse_document 不含 catalog（api.mbt:717-741 先例）；语法诊断通道不被 catalog 触碰 |
| T-07-03-03 | DoS | 超大/畸形输入经 lineage_text | medium | mitigate | parse_document 前置 ParseLimits（8 MiB）+ is_valid 门禁 → ParseError（与 format_text 一致） |
| T-07-03-04 | Tampering | 快照 golden 漂移掩盖边/gap 顺序回归 | medium | mitigate | test/lineage_test.mbt 快照 + 顺序断言；moon test --update 唯一写路径（git diff 审计） |
| T-07-03-SC | Tampering | npm/pip/cargo 安装 | low | accept | 零新增外部依赖 |

<verification>
- Task 1 后：`moon check --target native && moon test --target native --package api` 全绿（lineage_text 库面 + flink 门禁 + 类型再导出）。
- Task 2 后：`moon test --target native --package test` 全绿（集成用例 + 快照 golden 新增）。
</verification>

<success_criteria>
- `api.lineage_text` 库面入口可用：parse → derive_lineage → LineageResult，可选 catalog 注入，flink 门禁（D-08）落地。
- 类型经 D-38 别名再导出，binding/CLI 可经 @api 消费（07-04 前置依赖满足）。
- 集成测试 + 快照 golden 锁定边/gap 结构与顺序（跨 SELECT/INSERT/CTE/UNION/视图/EXCEPT/无 catalog 场景）。
</success_criteria>

<output>
Create `.planning/phases/07-column-lineage/07-03-SUMMARY.md` when done.
</output>
