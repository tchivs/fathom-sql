---
phase: 01-core-kernel
plan: 04
type: execute
wave: 4
depends_on: [01-03]
files_modified:
  - parser/parser.mbt
  - test/parser_test.mbt
  - corpus/manifest.tsv
  - corpus/coverage.tsv
  - corpus/differential.tsv
  - corpus/doris-2.1/select-industrial.sql
  - corpus/doris-3.x/select-industrial.sql
  - corpus/doris-4.x/select-industrial.sql
autonomous: true
requirements: [CORE-01, CORE-02, CORE-03, CORE-04, CORE-05, CORE-06, CORE-07]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The public grammar exposes exactly profiles 2.1, 3.x, and 4.x; each fixture records exact source release/minor and feature-introduction metadata, and unsupported/version-invalid syntax receives a version-aware diagnostic (CORE-01, D-07, Q2)."
    - "Released official SELECT fixtures cover joins, subqueries, CTEs, windows, predicates, functions, set operations, grouping sets/ROLLUP/CUBE, hints, and Doris-specific SELECT clauses without DML/DDL (CORE-04)."
    - "Every supported and negative fixture checks strict/editor status, CST/diagnostic shape, and byte-exact replay; FE/Nereids and SQLGlot rows remain advisory and cannot widen acceptance (CORE-02, CORE-03, CORE-05, CORE-06, D-08)."
    - "Diagnostic codes, byte spans, and zero-based statement identities remain stable in fixture output, including version-invalid and multi-statement cases (Q4)."
    - "Corpus loading and parsing remain offline, synchronous, and independent of FE, database, network, filesystem services, and target-specific parser code (CORE-07)."
  artifacts:
    - path: "parser/parser.mbt"
      provides: "Profile/version-gated industrial SELECT and Pratt expression productions"
    - path: "corpus/manifest.tsv"
      provides: "Pinned released-doc provenance, exact release/minor, feature introduction, status, and mode metadata"
    - path: "corpus/coverage.tsv"
      provides: "Version/category support and known-gap accounting"
    - path: "corpus/differential.tsv"
      provides: "Advisory FE/Nereids and SQLGlot disagreement/resolution records"
    - path: "test/parser_test.mbt"
      provides: "Manifest-driven SELECT, version, golden, diagnostic, recovery, and replay assertions"
  key_links:
    - from: "corpus/manifest.tsv"
      to: "test/parser_test.mbt"
      via: "Module-root loader validates provenance/profile/mode/status before each golden"
    - from: "corpus/manifest.tsv"
      to: "parser/parser.mbt"
      via: "Exact release/minor and feature-introduction metadata select profile/version gates"
    - from: "parser/parser.mbt"
      to: "test/parser_test.mbt"
      via: "Version-invalid productions produce version-aware DORIS-PARSE diagnostics rather than silent acceptance"
    - from: "test/parser_test.mbt"
      to: "printer/printer.mbt"
      via: "Every fixture asserts byte-exact replay, including malformed and unknown material"
  prohibitions:
    - "FLAGGED-UNVERIFIED [probe CORE-01 unclassified]: no fixture or production is accepted solely because FE, SQLGlot, MySQL, or current/dev Doris accepts it."
    - "FLAGGED-UNVERIFIED [probe CORE-04 adjacency/empty/ordering]: clause lists and set-operation chains retain documented order with deterministic empty/single-element behavior; touching spans are not merged."
    - "FLAGGED-UNVERIFIED [probe CORE-05 empty/encoding]: accepted empty results do not synthesize diagnostics; all non-empty diagnostic spans are byte intervals, including invalid UTF-8 fixtures."
    - "FLAGGED-UNVERIFIED [probe CORE-06 unclassified]: unresolved malformed/version cases are explicit expected-error or flagged rows, never passing compatibility claims."
    - "FLAGGED-UNVERIFIED [probe CORE-07 concurrency]: each corpus parse owns its immutable snapshot and limits; corpus tests have no shared mutable parser state."
---

<objective>
**As a** Doris SQL consumer, **I want to** parse released industrial SELECT examples under an explicit version profile, **so that** Phase 1 coverage is reproducible, version-aware, lossless, and honest about known gaps.

Purpose: Final expansion after the source/CST/recovery tracers. Implement the documented SELECT surface only, freeze Q2's exact public profiles and metadata, and make D-07/D-08 executable through manifest-driven fixtures.
Output: Eight files containing profile-gated industrial grammar, three released-family fixture sources, provenance/coverage/differential records, and manifest-driven golden tests.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-kernel/01-CONTEXT.md
@.planning/phases/01-core-kernel/01-RESEARCH.md
@.planning/phases/01-core-kernel/01-PATTERNS.md
@.planning/phases/01-core-kernel/01-03-PLAN.md
</execution_context>

<context>
01-03 provides stable source/CST/recovery/diagnostic behavior and the test package. This plan owns only industrial SELECT grammar and released fixture accounting. Public profiles are exactly `2.1`, `3.x`, and `4.x`; fixture rows retain exact release/minor and feature-introduction metadata (Q2). DML/DDL, current/dev acceptance, analyzer, formatter, FE/database execution, CLI, LSP, JS/Wasm, and UI remain excluded.
</context>

<tasks>
<task type="tracer">
  <name>Task 1: Expand profile-gated industrial SELECT and Pratt expressions</name>
  <files>parser/parser.mbt, test/parser_test.mbt</files>
  <read_first>
    @.planning/phases/01-core-kernel/01-03-PLAN.md
    @.planning/phases/01-core-kernel/01-RESEARCH.md
    @.planning/phases/01-core-kernel/01-PATTERNS.md
    @.planning/REQUIREMENTS.md
    @.planning/ROADMAP.md
  </read_first>
  <action>Extend the existing recursive-descent document/statement path using only released official 2.1/3.x/4.x SELECT documentation (D-07). Add explicit productions and synchronization sets for hints, projection modifiers, CTEs/subqueries, table references, JOIN variants, FROM partition/tablet/sample options, predicates, WHERE, window specifications, GROUP BY grouping sets/ROLLUP/CUBE, HAVING, ORDER BY, LIMIT, Doris-specific SELECT clauses, and UNION/set-operation chains. Keep one centralized Pratt precedence/associativity table, source order, spans, and trivia. Require exactly the three public profiles, consult exact release/minor and feature-introduction metadata, and reject unsupported/version-invalid syntax with a version-aware `DORIS-PARSE-###` diagnostic (Q2, Q4). Extend parser tests for each named feature, profile/version negative, contextual keyword, statement identity, strict/editor status, and exact replay. Do not implement DML/DDL or generic MySQL fallback.</action>
  <acceptance_criteria>
    - Industrial SELECT tests cover joins, subqueries, CTEs, windows, predicates, functions, set operations, grouping sets/ROLLUP/CUBE, hints, and Doris-specific SELECT clauses with deterministic CST order/spans (CORE-04, D-07).
    - Only `2.1`, `3.x`, and `4.x` profiles are accepted; exact release/minor and feature-introduction metadata drive version-aware rejection rather than silent acceptance (CORE-01, Q2).
    - Pratt precedence/associativity handles nested arithmetic, predicates, calls, windows, and parentheses while preserving byte-exact replay (CORE-02, CORE-03).
    - Diagnostics keep `DORIS-PARSE-###`, byte spans, and per-snapshot monotonic statement IDs for multi-statement/version-invalid cases (CORE-05, Q4).
    - No DML/DDL, FE, catalog, analyzer, formatter, CLI, host adapter, or current/dev grammar path is added.
  </acceptance_criteria>
  <verify><automated>moon test</automated></verify>
  <done>The parser and parser tests cover the documented industrial SELECT slice under explicit released profiles with version-aware negatives, stable diagnostics, recovery, and exact replay.</done>
</task>

<task type="auto">
  <name>Task 2: Establish released fixture manifest, goldens, and coverage links</name>
  <files>corpus/manifest.tsv, corpus/coverage.tsv, corpus/differential.tsv, corpus/doris-2.1/select-industrial.sql, corpus/doris-3.x/select-industrial.sql, corpus/doris-4.x/select-industrial.sql</files>
  <read_first>
    @.planning/phases/01-core-kernel/01-RESEARCH.md
    @.planning/phases/01-core-kernel/01-PATTERNS.md
    @.planning/phases/01-core-kernel/01-03-PLAN.md
    @.planning/REQUIREMENTS.md
  </read_first>
  <action>Create the three Git-tracked released-family SQL fixture files and a manifest-driven loader contract. Each manifest row records exactly one public profile, exact source release/minor, feature-introduction release metadata, official documentation URL, retrieval date, pinned source revision/commit, page/heading/code-fence provenance, category, expected support status, parse mode, and classification (`parse-only`, `requires-session`, `requires-catalog`, `executable`, `expected-error`, or `not-sql`). Include supported industrial examples plus malformed/recovery, invalid-encoding, unsupported-version, contextual-keyword, and CORE-04 boundary rows for empty clauses, single-element clauses, adjacent clauses, and one/two/many set-operation chains; every supported row points to strict parse, CST/diagnostic, editor/recovery where relevant, deterministic clause/set-chain order, and byte-exact replay expectations. Record per-version/category supported features and known gaps in `coverage.tsv`; record FE/Nereids and SQLGlot observations with explicit resolutions in `differential.tsv` as advisory evidence only (D-08, Q2, Q3, Q4). Never use current/dev material as public support and never require network or FE access at test time.</action>
  <acceptance_criteria>
    - All three released fixture files have manifest rows with exact release/minor and feature-introduction metadata, provenance, status, profile, category, and mode; no row relies on floating/current/dev docs (CORE-01, D-07, Q2).
    - Supported and expected-error rows exercise strict/editor status, stable diagnostics, invalid-byte preservation where applicable, byte-exact replay, and CORE-04 empty/single-element clause and set-operation chain boundary assertions through the existing test link (CORE-02, CORE-03, CORE-05, CORE-06, Q3, Q4, D-08).
    - `coverage.tsv` reports support and known gaps by version/category without an unqualified compatibility claim; `differential.tsv` cannot make a fixture pass without the released-doc contract.
    - Manifest loading remains module-root-relative and offline, with no runtime FE/database/network/filesystem service or target-specific parser.
  </acceptance_criteria>
  <verify><automated>moon test</automated></verify>
  <done>Released 2.1/3.x/4.x SELECT fixtures, provenance manifest, coverage report, and advisory differential records are linked to executable golden/recovery/replay tests, including CORE-04 empty/single-element clause and set-chain boundary assertions.</done>
</task>
</tasks>

## Source Audit Coverage

| SOURCE | ID | Feature or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Explicit-profile, lossless, recoverable offline SELECT/expression kernel | 01-01, 01-02, 01-03, 01-04 | COVERED | Four sequential vertical expansions cover source through industrial corpus. |
| REQ | CORE-01 | Explicit Doris 2.1/3.x/4.x profile with no generic fallback | 01-01, 01-04 | COVERED | Boundary profiles and version-gated fixture/parser checks. |
| REQ | CORE-02 | Traversable lossless CST retaining bytes/trivia/spans | 01-02, 01-03, 01-04 | COVERED | CST, recovery, and corpus replay assertions. |
| REQ | CORE-03 | Byte-exact unchanged replay | 01-02, 01-03, 01-04 | COVERED | Printer and valid/error/version goldens. |
| REQ | CORE-04 | Industrial Doris SELECT and Pratt expressions | 01-04 | COVERED | All named SELECT features are explicit in Task 1. |
| REQ | CORE-05 | Structured diagnostics with stable code and statement identity | 01-02, 01-03, 01-04 | COVERED | Schema, recovery, and fixture assertions. |
| REQ | CORE-06 | Bounded strict/editor recovery with explicit nodes | 01-03, 01-04 | COVERED | Recovery tests and malformed corpus rows. |
| REQ | CORE-07 | Pure offline parser core | 01-01, 01-02, 01-03, 01-04 | COVERED | Every plan forbids service/runtime dependencies. |
| RESEARCH | R-01 | Immutable source snapshot, byte spans, centralized LineIndex | 01-01 | COVERED | Raw-byte source contract. |
| RESEARCH | R-02 | Trivia lexer and contextual token metadata | 01-01, 01-04 | COVERED | Lexer boundary and versioned grammar usage. |
| RESEARCH | R-03 | Immutable CST with source-backed leaves | 01-02, 01-03 | COVERED | CST and recovery nodes. |
| RESEARCH | R-04 | Recursive descent plus Pratt SELECT parser | 01-02, 01-04 | COVERED | Tracer then full SELECT expansion. |
| RESEARCH | R-05 | Layered recovery, progress/error, resource caps | 01-03, 01-04 | COVERED | Recovery implementation and corpus negatives. |
| RESEARCH | R-06 | Primitive serialized result and host-side UTF-16 | 01-02, 01-03 | COVERED | Root source/span schema. |
| RESEARCH | R-07 | Released official corpus and reproducible golden metadata | 01-04 | COVERED | Manifest, fixtures, coverage, and differential records. |
| RESEARCH | R-08 | Pure synchronous core and no package installs | 01-01, 01-02, 01-03, 01-04 | COVERED | Threat registers and offline verifies. |
| RESEARCH | R-09 | Untrusted-input size/recovery/diagnostic/output caps | 01-03, 01-04 | COVERED | Limits and adversarial fixtures. |
| CONTEXT | D-01 | Canonical UTF-8 byte offsets over immutable snapshot and LineIndex | 01-01, 01-02, 01-03 | COVERED | Source, CST, and recovery spans. |
| CONTEXT | D-02 | Versioned primitive serialized results and byte spans | 01-02, 01-03 | COVERED | Root payload and stable fields. |
| CONTEXT | D-03 | One CST with explicit strict/editor modes | 01-02, 01-03, 01-04 | COVERED | Tracer, recovery, and corpus modes. |
| CONTEXT | D-04 | Progress/error, clause/statement sync, retained text, caps | 01-03, 01-04 | COVERED | Bounded recovery and negatives. |
| CONTEXT | D-05 | Immutable CST source of truth; analyzer separate | 01-02, 01-04 | COVERED | CST-first parser and no analyzer. |
| CONTEXT | D-06 | Immutable source-backed leaves and explicit error forms | 01-01, 01-02, 01-03, 01-04 | COVERED | Raw bytes, CST, recovery, and fixtures. |
| CONTEXT | D-07 | Released official docs authority; FE/SQLGlot advisory only | 01-01, 01-04 | COVERED | Profile metadata and differential separation. |
| CONTEXT | D-08 | Versioned goldens, replay, malformed/recovery, keyword gates | 01-02, 01-03, 01-04 | COVERED | Executable golden and negative corpus path. |

<verification>
Run `moon test` after each task and then `moon check --target native`. Inspect representative manifest rows for exact release/minor and feature-introduction metadata, each released profile fixture, coverage known gaps, differential resolutions, diagnostics, recovery, and byte-exact replay. Do not install packages, run formatters/linters, invoke FE/database/network, or run project-wide cross-target suites.
</verification>

<success_criteria>
- CORE-01 through CORE-07 are all executable across the four-plan set, with CORE-04's full named SELECT surface covered by profile-gated tests.
- Q2, plus Q3/Q4 replay and diagnostic constraints, are represented in fixture metadata, parser actions, and acceptance checks.
- Released official provenance, exact release/minor and feature-introduction metadata, version/category coverage, known gaps, and advisory differential evidence are committed as Git data.
- Strict/editor semantics, stable diagnostics, bounded recovery, source-backed CST, and exact replay remain true for industrial and malformed fixtures.
- The parser remains one offline MoonBit implementation with no later-phase frontend, CLI, DML/DDL, formatter, analyzer, LSP, or target-specific additions.
</success_criteria>

<output>Create `.planning/phases/01-core-kernel/01-04-SUMMARY.md` when this plan executes.</output>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Corpus fixture/profile metadata → parser | Git fixture text and version claims are untrusted and may include malformed/adversarial examples. |
| Differential evidence → public support report | FE/SQLGlot/current-doc observations could widen acceptance if not explicitly separated. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-14 | Tampering | Profile/version acceptance | high | mitigate | Require exactly three profiles, exact release/minor and feature-introduction metadata, and version-aware negative diagnostics (Q2, D-07). |
| T-01-15 | Denial of Service | Industrial expressions/nested SELECTs | high | mitigate | Reuse 01-03 byte/token/depth/recovery/diagnostic caps and include deep/malformed fixtures. |
| T-01-16 | Tampering | Manifest/golden provenance | medium | mitigate | Require URL, pinned revision, date, heading, category, mode, and support status; fail rows missing provenance. |
| T-01-17 | Tampering | Differential records | low | accept | FE/SQLGlot data is advisory text only; no network or execution occurs and released docs remain authoritative. |
| T-01-SC | Tampering | Package installation | low | accept | No package installation task exists; corpus and parser checks use the pinned MoonBit toolchain and Git files. |
</threat_model>
