---
phase: 10-flink-release-profiles-and-lexical-core
plan: 03
type: execute
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - dialect/flink.mbt
  - dialect/classification.mbt
  - scripts/extract_flink_lexical.py
  - parity/fixtures/flink-lexical/flink-2.3.0-reserved.txt
  - parity/fixtures/flink-lexical/flink-2.1.3-reserved.txt
  - parity/fixtures/flink-lexical/flink-1.20.5-reserved.txt
  - parity/fixtures/flink-lexical/flink-2.3.0-nonreserved.txt
  - parity/fixtures/flink-lexical/flink-2.1.3-nonreserved.txt
  - parity/fixtures/flink-lexical/flink-1.20.5-nonreserved.txt
autonomous: true
requirements: [FLINK-01]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Flink keyword classification is independent from Doris and grounded in the pinned releases: flink_classification_rows carries production/conflict words (reserved/non-reserved) with per-release introduced_profile and a release-grammar source (path + token line, e.g. `flink-sql-parser codegen/templates/Parser.jj:8640 (VARIANT)`); the Doris 116-row table is untouched and no global union exists (DIALECT-02, D-13)."
    - "Release-sensitive keywords classify by profile: VARIANT is Reserved under flink-2.3.0 and flink-2.1.3 and ABSENT (identifier) under flink-1.20.5; QUALIFY is Reserved under flink-2.3.0/2.1.3 and ABSENT under flink-1.20.5 (RESEARCH §10 version-sensitive words); Flink classification_of filters rows by introduced_profile <= selected profile in release order flink-1.20.5 < flink-2.1.3 < flink-2.3.0, so a 2.1.3-introduced row is ABSENT under flink-1.20.5; Doris row selection stays dialect-only (Pitfall 4)."
    - "The full per-release reserved/non-reserved lists (443/334, 430/324, 412/323) are committed as parity fixture attachments and regenerated/validated by scripts/extract_flink_lexical.py from the pinned release parser (Parser.tdd keywords + nonReservedKeywords + the VARIANT template token) — no moving docs (D-02, Pitfall 3)."
    - "No Flink word affects Doris acceptance: is_reserved_word(doris, b\"VARIANT\") stays false and classification_entries(doris).length() stays 116; the two row arrays are independent module-level data with no shared mutable state (DIALECT-02, Pitfall 4)."
    - "[probe FLINK-01 ordering] flagged-unverified: classification lookup is first-match-wins over the dialect's own rows (token_bytes_equal_ci case-insensitive compare, source order); row order in flink_classification_rows is stable and frozen by the row-count/content audit tests."
    - "[probe FLINK-01 adjacency] flagged-unverified: a word present in the reserved list AND the non-reserved list (release parser overlap, e.g. words that are reserved as keywords but non-reserved in nonReservedKeywords) resolves by the row table's single classification — the extract script's overlap report surfaces these so the row table author decides each explicitly rather than the lookup silently choosing."
    - "[probe FLINK-01 empty] flagged-unverified: an empty flink_classification_rows for a profile that has not shipped rows (e.g. before this plan) is not possible after this plan — each of the three profiles has a non-empty row set; the audit test asserts the per-profile row counts are non-zero."
  artifacts:
    - path: "dialect/flink.mbt"
      provides: "flink_classification_rows filled with production/conflict KeywordEntry rows (word Bytes, ClassificationKind Reserved|NonReserved|Contextual, introduced_profile per release, source release-grammar path+line)"
    - path: "dialect/classification.mbt"
      provides: "profile-aware Flink row selection (classification_rows_for filters Flink rows by introduced_profile <= selected profile; Doris stays dialect-only) + the classification independence test rewritten (flink rows non-empty, VARIANT/QUALIFY version sensitivity, Doris 116 preserved)"
    - path: "scripts/extract_flink_lexical.py"
      provides: "keyword extraction/validation extended: reads Parser-release-{v}.tdd keywords + nonReservedKeywords + the VARIANT template token, emits the reserved/nonreserved lists and counts, validates the inlined rows against them"
    - path: "parity/fixtures/flink-lexical/flink-{2.3.0,2.1.3,1.20.5}-{reserved,nonreserved}.txt"
      provides: "committed full per-release keyword lists (manifest attachments per RESEARCH Open Question 3)"
  key_links:
    - from: "dialect/flink.mbt flink_classification_rows"
      to: "dialect/classification.mbt classification_of"
      via: "classification_rows_for selects Doris rows by context.dialect and filters Flink rows by introduced_profile <= selected profile (flink-1.20.5 < flink-2.1.3 < flink-2.3.0) — the two arrays stay independent (Pitfall 4)"
    - from: "dialect/flink.mbt rows' source columns"
      to: "parity/fixtures/flink-lexical/flink-{v}-reserved.txt"
      via: "scripts/extract_flink_lexical.py validates that every inlined row's word exists in the release list and that the source references the release grammar, not docs folklore"
    - from: "flink_classification_rows"
      to: "the Phase 11 grammar"
      via: "classification_of/is_reserved_word/is_unquoted_identifier gate identifier acceptance under the selected Flink profile (D-13/D-14)"
  prohibitions:
    - "No keyword union: a Flink word must never affect Doris acceptance and vice versa — doris_classification_rows (116) and flink_classification_rows stay independent module-level arrays; Doris rows are selected only by context.dialect, Flink rows are additionally filtered by introduced_profile <= selected profile (DIALECT-02, Pitfall 4)."
    - "Classification source columns reference the pinned release grammar (path + token line), never moving docs URLs or Calcite folklore (D-02, D-03, Pitfall 3)."
    - "No word's classification is invented: every inlined row is validated by scripts/extract_flink_lexical.py against the committed release reserved/nonreserved lists; an overlap resolves by explicit authoring, never by the lookup silently choosing."
---

<objective>
**As a** consumer, **I want to** rely on release-accurate Flink keyword classification — which words are reserved, non-reserved, or absent per Flink profile — **so that** identifier acceptance under Flink is correct and fully independent from Doris before Phase 11 grammar consumes the classification gate.

Purpose: Populate `flink_classification_rows` (production/conflict words per the three releases) and make the classification independence gate assert Flink rows non-empty and version-sensitive (VARIANT/QUALIFY absent under flink-1.20.5) while the Doris 116-row table stays byte-identical. Commit the full per-release reserved/non-reserved lists as provenance attachments and extend the extract script to regenerate/validate them (D-02 provenance discipline, Pitfall 3/4).
Output: flink_classification_rows, the rewritten classification independence tests, the six reserved/nonreserved list attachments, and the extract-script keyword validation.
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-flink-release-profiles-and-lexical-core/10-CONTEXT.md
@.planning/phases/10-flink-release-profiles-and-lexical-core/10-RESEARCH.md
@.planning/phases/10-flink-release-profiles-and-lexical-core/10-PATTERNS.md

Builds on 10-01's FlinkProfile identity (the closed enum + metadata live in dialect/flink.mbt). The classification analog is `dialect/classification.mbt` (`classification_rows_for` `:55-61` selects rows by context.dialect; `classification_of` `:63-77`; `KeywordEntry` `:24-32` with word Bytes/classification/introduced_profile/source) and the Doris row table `doris_classification_rows` (`dialect/doris.mbt:275-462`, 116 rows, byte-identical per D-08). This plan makes the Flink side profile-aware: `classification_rows_for` (`:55-61`) filters Flink rows by `introduced_profile` <= the selected profile in release order (flink-1.20.5 < flink-2.1.3 < flink-2.3.0), so a 2.1.3-introduced word is ABSENT under flink-1.20.5; the Doris side stays dialect-only (zero behavior change, Pitfall 4). The current Phase 9 test `classification_is_dialect_independent_and_flink_rows_are_empty` (`dialect/classification.mbt:132-143`) asserts flink rows == 0 — Phase 10 rewrites it to assert non-empty per release. RESEARCH §10 gives the per-release reserved/nonreserved counts (443/334, 430/324, 412/323), the release-delta sets (2.3.0 adds BITMAP/CONFLICT/CONNECTIONS/CONTAINS_SUBSTR/DATETIME_TRUNC/DEDUPLICATE/DO/JSON_SCOPE/NOTHING/ORDINAL/SAFE_CAST/SAFE_OFFSET/SAFE_ORDINAL over 2.1.3; 2.1.3 adds DATETIME/DATE_TRUNC/FRIDAY/MONDAY/QUALIFY/QUARTERS/SATURDAY/STRUCTURED/SUNDAY/THURSDAY/TIMESTAMP_DIFF/TIMESTAMP_TRUNC/TIME_DIFF/TIME_TRUNC/TUESDAY/VARIANT/WEDNESDAY/WEEKS over 1.20.5), the VARIANT template-token fact (Parser.jj:8640/8374, absent in 1.20.5), and the D-06 intersection/conflict sets (Doris-reserved-but-Flink-identifier 19 words; Doris-only 23 words; QUALIFY/VARIANT version sensitivity). Per RESEARCH Open Question 3 RESOLVED, the inlined row table covers production/conflict words (~120-180 rows) while the FULL lists are manifest attachments. This plan is serialized AFTER 10-02 (`depends_on: [10-01, 10-02]`, wave 3) so the flink-lexical parity snapshots materialize before this plan's `moon test --package parity` drift checks run.

> **File-scope note:** this plan does NOT touch parity/fixtures/flink-lexical/manifest.tsv or parity/__snapshot__/ (10-01/10-02 own them); the keyword lists are separate attachment files (flink-{v}-{reserved,nonreserved}.txt), so the serialized 10-02 → 10-03 ordering keeps zero files_modified overlap while guaranteeing the flink-lexical parity snapshots exist when this plan runs `moon test --package parity`.
</context>

<tasks>

<task type="auto">
  <name>Populate flink_classification_rows per release + rewrite the classification independence gate</name>
  <files>dialect/flink.mbt, dialect/classification.mbt</files>
  <read_first>
    @.planning/phases/10-flink-release-profiles-and-lexical-core/10-CONTEXT.md
    @.planning/phases/10-flink-release-profiles-and-lexical-core/10-RESEARCH.md
    @.planning/phases/10-flink-release-profiles-and-lexical-core/10-PATTERNS.md
    @/opt/source/Fathom/dialect/flink.mbt
    @/opt/source/Fathom/dialect/classification.mbt
    @/opt/source/Fathom/dialect/doris.mbt
  </read_first>
  <action>Fill `flink_classification_rows` in `dialect/flink.mbt` (currently `= []`, `:17`) with `KeywordEntry` rows (same struct as Doris, `dialect/classification.mbt:24-32`). Scope per RESEARCH Open Question 3 RESOLVED: inline the words the parser/lexer consume and the D-06 conflict words (roughly 120-180 rows across the releases), NOT the full 443/430/412-word lists (those are the 10-03 Task 3 attachments). Every row: `word` Bytes (uppercase ASCII as the classification-table convention), `classification` from `ClassificationKind` (Reserved | NonReserved | Contextual), `introduced_profile` set to the first Flink release that classifies it (flink-2.3.0 for words introduced by 2.3.0, flink-2.1.3 for 2.1.3-added words, flink-1.20.5 for baseline words), and `source` = the pinned release grammar path + token line (e.g. `flink-sql-parser codegen/templates/Parser.jj:8640 (VARIANT)` for VARIANT under 2.3.0) — NEVER a docs URL. Include: the core reserved words (SELECT, FROM, WHERE, GROUP, HAVING, ORDER, LIMIT, JOIN, UNION, WITH, AS, AND, OR, ON, USING, INNER, LEFT, RIGHT, CROSS, FULL, NATURAL, OVER, PARTITION, BUCKETS, WATERMARK, ANALYZE, etc.); the D-06 conflict words with version sensitivity — QUALIFY (Reserved, introduced_profile flink-2.1.3; ABSENT under flink-1.20.5 so NO row), VARIANT (Reserved, introduced_profile flink-2.1.3 — §10's 2.1.3-delta list `[VERIFIED: comm -23 flink-2.1.3-reserved.txt flink-1.20.5-reserved.txt]` names VARIANT among the 18 words 2.1.3 adds over 1.20.5; ABSENT under flink-1.20.5 so NO row); the Doris-reserved-but-Flink-identifier 19 words (ASC, AUTO, DEFAULT, DESC, DISTINCTROW, DUPLICATE, FIRST, FOLLOWING, INDEX, KEY, LAST, LIST, NULLS, OUTFILE, PRECEDING, REGEXP, SAMPLE, SETS, UNBOUNDED) as NonReserved or omitted per their Flink nonreserved/absent status; the Doris-only 23 words (AGGREGATE, ASYNC, BUILD, COMPLETE, ENGINE, LESS, MANUAL, PROPERTIES, RANDOM, SCHEDULE, STARTS, TABLET, THAN, etc.) omitted or NonReserved per their Flink status. Rewrite the `dialect/classification.mbt` test `classification_is_dialect_independent_and_flink_rows_are_empty` (`:132-143`) into a release-aware independence test: assert `classification_entries(flink_2_3_0_context).length() > 0` and likewise for 2.1.3/1.20.5; assert `classification_of(flink_2_3_0_context, b"VARIANT")` is Some(Reserved) and `classification_of(flink_2_1_3_context, b"VARIANT")` is Some(Reserved); assert `classification_of(flink_2_1_3_context, b"QUALIFY")` is Some(Reserved); assert `is_reserved_word(flink_2_3_0_context, b"SELECT")` is true; assert the Doris table is unchanged — `classification_entries(doris_test_context()).length() == 116` and `classification_of(doris_test_context(), b"SELECT")` is Some(Reserved) and `is_reserved_word(doris_test_context(), b"VARIANT")` is false (a Flink word never affects Doris). Do NOT assert the flink-1.20.5 absence of VARIANT/QUALIFY here — those assertions land in Task 2 once the profile-aware routing filter exists (with dialect-only routing a VARIANT row returns Some under every flink profile). The `classification_rows_for`/`classification_of`/`is_reserved_word` routing code (`:55-81,98-105`) is unchanged in this task; Task 2 makes Flink row selection profile-aware while Doris row selection stays dialect-only (Pitfall 4).</action>
  <acceptance_criteria>
    - flink_classification_rows is non-empty; every row has a release-grammar `source` (no docs URL, no folklore); introduced_profile is one of flink-2.3.0/flink-2.1.3/flink-1.20.5.
    - The VARIANT row's introduced_profile equals `flink-2.1.3` (W-1: §10's 2.1.3-delta list) and the QUALIFY row's introduced_profile equals `flink-2.1.3`.
    - `classification_of(flink_2_3_0_context, b"VARIANT")` is Some(Reserved); `classification_of(flink_2_1_3_context, b"VARIANT")` is Some(Reserved); `classification_of(flink_2_1_3_context, b"QUALIFY")` is Some(Reserved).
    - Doris is untouched: `classification_entries(doris).length() == 116`, `is_reserved_word(doris, b"VARIANT")` is false, `is_reserved_word(doris, b"SELECT")` is true.
    - `moon test --target native --package dialect` passes; `moon test --package parity` (no --update) passes with zero Doris drift.
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native --package dialect && moon test --package parity</automated>
  </verify>
  <done>The Flink classification rows are populated per release with release-grammar provenance (VARIANT/QUALIFY introduced_profile = flink-2.1.3), and Doris classification is byte-identical; the profile-aware routing that makes VARIANT/QUALIFY ABSENT under flink-1.20.5 lands in Task 2.</done>
</task>

<task type="auto">
  <name>Make Flink classification row selection profile-aware (introduced_profile <= selected profile)</name>
  <files>dialect/classification.mbt</files>
  <read_first>
    @.planning/phases/10-flink-release-profiles-and-lexical-core/10-CONTEXT.md
    @.planning/phases/10-flink-release-profiles-and-lexical-core/10-RESEARCH.md
    @.planning/phases/10-flink-release-profiles-and-lexical-core/10-PATTERNS.md
    @/opt/source/Fathom/dialect/classification.mbt
    @/opt/source/Fathom/dialect/flink.mbt
    @/opt/source/Fathom/dialect/dialect.mbt
  </read_first>
  <action>Make Flink row selection PROFILE-AWARE in `dialect/classification.mbt` so release-sensitive words classify only under the profiles that introduced them. Keep `KeywordEntry.introduced_profile` as the per-release gate. `classification_rows_for` (`:55-61`) currently returns the whole dialect array selected only by `context.dialect`; extend it so that under `Dialect::Flink` only rows whose `introduced_profile` is <= the selected profile in release order `flink-1.20.5 < flink-2.1.3 < flink-2.3.0` are returned — a row introduced in flink-2.1.3 must be ABSENT under flink-1.20.5, and a flink-2.3.0-introduced row must be ABSENT under both flink-2.1.3 and flink-1.20.5. Implement the release-order comparison explicitly (a small helper mapping profile_id -> ordinal, e.g. flink-1.20.5=0, flink-2.1.3=1, flink-2.3.0=2; an unknown Flink profile id falls back to an empty selection — never a silent default to all rows). `Dialect::Doris` row selection stays dialect-only — zero behavior change to `classification_entries(doris).length() == 116` (Pitfall 4: the two arrays stay independent, no union, no shared state). `classification_of` (`:63-77`), `classification_entries` (`:79-81`), and `is_reserved_word` (`:98-105`) keep their lookup shape; they automatically become release-aware because they read the filtered `classification_rows_for` result. Extend the rewritten independence test from Task 1 with the release-sensitivity assertions that only hold once routing is profile-aware: `classification_of(flink_1_20_5_context, b"VARIANT")` is None and `classification_of(flink_1_20_5_context, b"QUALIFY")` is None (the 1.20.5 regression profile sees neither word); a flink-2.3.0-introduced word (e.g. SAFE_CAST or NOTHING) is None under flink-2.1.3 and flink-1.20.5; `classification_of(flink_2_1_3_context, b"VARIANT")` is Some(Reserved) still holds. Add the flink test contexts as needed (analog of `doris_test_context` `:109-120`) with profile_id set to flink-1.20.5/flink-2.1.3/flink-2.3.0.</action>
  <acceptance_criteria>
    - `classification_of(flink_1_20_5_context, b"VARIANT")` is None and `classification_of(flink_1_20_5_context, b"QUALIFY")` is None (BLOCKER: the flink-1.20.5 regression profile sees neither word).
    - `classification_of(flink_2_1_3_context, b"VARIANT")` and `classification_of(flink_2_1_3_context, b"QUALIFY")` are Some(Reserved); `classification_of(flink_2_3_0_context, b"VARIANT")` is Some(Reserved).
    - A flink-2.3.0-introduced word (e.g. SAFE_CAST or NOTHING) is None under flink-2.1.3 and flink-1.20.5.
    - Doris is unchanged: `classification_entries(doris).length() == 116` and `is_reserved_word(doris, b"VARIANT")` is false — profile-awareness is Flink-only, zero Doris behavior change.
    - `moon test --target native --package dialect` passes; `moon test --package parity` (no --update) passes with zero Doris drift.
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native --package dialect && moon test --package parity</automated>
  </verify>
  <done>Flink classification row selection is profile-aware in release order (flink-1.20.5 < flink-2.1.3 < flink-2.3.0): VARIANT/QUALIFY are Reserved under 2.1.3/2.3.0 and ABSENT under 1.20.5; Doris row selection stays dialect-only with the 116-row table byte-identical.</done>
</task>

<task type="auto">
  <name>Release keyword provenance: full lists committed + extract-script keyword validation + audit test</name>
  <files>scripts/extract_flink_lexical.py, parity/fixtures/flink-lexical/flink-2.3.0-reserved.txt, parity/fixtures/flink-lexical/flink-2.1.3-reserved.txt, parity/fixtures/flink-lexical/flink-1.20.5-reserved.txt, parity/fixtures/flink-lexical/flink-2.3.0-nonreserved.txt, parity/fixtures/flink-lexical/flink-2.1.3-nonreserved.txt, parity/fixtures/flink-lexical/flink-1.20.5-nonreserved.txt</files>
  <read_first>
    @.planning/phases/10-flink-release-profiles-and-lexical-core/10-RESEARCH.md
    @.planning/phases/10-flink-release-profiles-and-lexical-core/10-PATTERNS.md
    @/opt/source/Fathom/scripts/extract_flink_lexical.py
    @/opt/source/Fathom/corpus/tools/check_keywords.py
    @/opt/source/Fathom/dialect/flink.mbt
  </read_first>
  <action>Commit the full per-release keyword lists as provenance attachments under `parity/fixtures/flink-lexical/`: `flink-{2.3.0,2.1.3,1.20.5}-reserved.txt` and `flink-{2.3.0,2.1.3,1.20.5}-nonreserved.txt`, containing exactly the release-extracted words (RESEARCH §10 counts 443/334, 430/324, 412/323) with a provenance header (release tag, source = generated parser keyword tokens + Flink Parser.tdd keywords + nonReservedKeywords + VARIANT template token). Extend `scripts/extract_flink_lexical.py` (created in 10-01) to: (1) read the pinned release grammar inputs — `Parser-release-{v}.tdd` (keywords + nonReservedKeywords + binaryOperatorsTokens/includePosixOperators per §9 operator matrix) and the Flink `codegen/templates/Parser.jj` VARIANT token (`:8640` under 2.3.0, `:8374` under 2.1.3, absent in 1.20.5); (2) regenerate/validate the six committed lists (counts and membership) and report the release-delta sets from §10 (comm -23 results: 2.3.0 adds 13 reserved words, 2.1.3 adds 18 reserved words, 2.3.0 adds 10 nonreserved words); (3) validate the inlined `flink_classification_rows` — every inlined word exists in the matching release list, no word is classified Reserved that the release lists as non-reserved, and overlapping words (in both reserved and nonreserved lists) are reported as an explicit overlap list for the row-table author (adjacency probe) rather than silently resolved; (4) verify no Doris source column references a Flink grammar file and vice versa. The script keeps the check_keywords.py shape (problems list + non-zero exit + trailing "ok: N ..." line) and remains Python-stdlib-only.</action>
  <acceptance_criteria>
    - The six list files exist with the §10 counts (443/334, 430/324, 412/323) and a provenance header; they are committed.
    - python3 scripts/extract_flink_lexical.py exits 0 and prints an "ok:" line asserting the three pin versions (1.36.0/1.34.0/1.32.0), the keyword counts, and that every inlined flink_classification_rows word is present in the matching release list; a deliberately wrong word in the row table makes it exit 1.
    - The release-delta report matches §10 (2.3.0 +13 reserved over 2.1.3; 2.1.3 +18 reserved over 1.20.5; 2.3.0 +10 nonreserved over 2.1.3); overlap words (reserved ∩ nonreserved) are listed, not silently resolved.
    - `moon test --target native --package dialect` passes (the audit test asserts the inlined rows' source columns reference release grammar and the independence from Doris rows); `moon test --package parity` (no --update) passes.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/extract_flink_lexical.py && python3 -m py_compile scripts/extract_flink_lexical.py && test -s parity/fixtures/flink-lexical/flink-2.3.0-reserved.txt && moon test --target native --package dialect && moon test --package parity</automated>
  </verify>
  <done>The full per-release keyword lists are committed and the extract script regenerates/validates them against the pinned release parser; the inlined rows are provenance-checked; the classification gate and Doris baseline stay green.</done>
</task>

</tasks>

## Source Audit Coverage

| SOURCE | ID | Feature or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | release-accurate Flink keyword classification independent from Doris | 10-03 | COVERED | Rows + independence gate. |
| REQ | FLINK-01 | release-specific reserved/non-reserved classification, conflict explainable | 10-03 | COVERED | Tasks 1-3. |
| CONTEXT | D-03 | classification from release fixtures, not folklore | 10-03 | COVERED | Rows sourced to release grammar; script validates. |
| CONTEXT | D-02 | provenance from pinned releases | 10-03 | COVERED | Full lists committed as attachments + script regeneration. |
| RESEARCH | §10 keyword matrix | counts 443/334, 430/324, 412/323; release deltas; VARIANT/QUALIFY version sensitivity; Doris conflict sets | 10-03 | COVERED | Tasks 1-3. |
| RESEARCH | Pitfall 4 | keyword union leakage | 10-03 | COVERED | Independence gate + prohibitions. |
| PROBE | FLINK-01 ordering | classification first-match-wins; stable row order | 10-03 | FLAGGED | truth + row audit. |
| PROBE | FLINK-01 adjacency | reserved ∩ nonreserved overlap words | 10-03 | FLAGGED | truth + script overlap report. |
| PROBE | FLINK-01 empty | per-profile non-empty row sets | 10-03 | FLAGGED | truth + row-count assertions. |

<verification>
Run moon test --target native --package dialect after each task and moon test --package parity (no --update) after Tasks 1-2. Inspect: flink_classification_rows content (source = release grammar, introduced_profile per release), the profile-aware routing filter (introduced_profile <= selected profile; Doris dialect-only), the rewritten independence test (VARIANT/QUALIFY version sensitivity incl. 1.20.5 absence, Doris 116 preserved), the six committed list files with the §10 counts, and the extract script's ok-line + overlap report. Do not touch lexer/ (10-02 owns it), do not change Doris rows, and do not run moon test --update without a register entry.
</verification>

<success_criteria>
- Flink keyword classification is populated per release with release-grammar provenance; VARIANT/QUALIFY version sensitivity is asserted; Doris classification is byte-identical.
- The full per-release reserved/non-reserved lists are committed and regenerable/validatable by the extract script; no union leakage and no folklore sources.
</success_criteria>

<output>Create `.planning/phases/10-flink-release-profiles-and-lexical-core/10-03-SUMMARY.md` when this plan executes.</output>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| classification rows -> is_reserved_word/is_unquoted_identifier | Rows gate identifier acceptance under the selected Flink profile; a wrong classification changes acceptance. |
| rows source columns -> release grammar | Provenance crosses from pinned grammar into classification; a fabricated source defeats auditability. |
| flink rows <-> doris rows | The two arrays must never merge or share state (union leakage). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-12 | Tampering | Keyword union leakage (Flink word affects Doris acceptance or vice versa) | high | mitigate | Independent module-level arrays; Doris selected only by context.dialect, Flink additionally filtered by introduced_profile <= selected profile; independence test asserts Doris 116 preserved + is_reserved_word(doris, VARIANT) false (Tasks 1-2, Pitfall 4). |
| T-10-13 | Tampering | Version-sensitive word misclassified (VARIANT/QUALIFY under 1.20.5) | high | mitigate | Per-release rows with introduced_profile; profile-aware routing filter (introduced_profile <= selected profile); classification tests assert the 1.20.5 absence; extract script validates rows against the release lists (Tasks 1-3). |
| T-10-14 | Tampering | Fabricated classification source (docs folklore instead of release grammar) | medium | mitigate | Row source columns are release-grammar path+line; audit test + script validation (Task 3, Pitfall 3). |
| T-10-15 | Tampering | Doris classification drift during row-table work | high | mitigate | Doris rows untouched; classification_entries(doris) == 116 asserted; parity gate no --update (Task 1). |
| T-10-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies; extract script is Python stdlib. |
</threat_model>

## Artifacts this phase produces

Files: dialect/flink.mbt (modified — flink_classification_rows populated), dialect/classification.mbt (modified — independence test rewritten), scripts/extract_flink_lexical.py (modified — keyword extraction/validation + overlap report), parity/fixtures/flink-lexical/flink-{2.3.0,2.1.3,1.20.5}-{reserved,nonreserved}.txt (new, committed attachments).

Symbols (dialect/flink.mbt): flink_classification_rows : Array[KeywordEntry] — rows with word/classification/introduced_profile/source per release.
Symbols (dialect/classification.mbt): profile-aware Flink row selection (classification_rows_for filters Flink rows by introduced_profile <= selected profile in release order flink-1.20.5 < flink-2.1.3 < flink-2.3.0; Doris stays dialect-only), rewritten classification_is_dialect_independent_and_release_aware test (flink rows non-empty, VARIANT/QUALIFY version sensitivity incl. 1.20.5 absence, Doris 116 preserved).
Symbols (scripts/extract_flink_lexical.py): keyword list regeneration/validation, release-delta report, reserved∩nonreserved overlap report, inlined-row validation, ok-line + exit 0/1.
Symbols (parity/fixtures/flink-lexical/): flink-{2.3.0,2.1.3,1.20.5}-reserved.txt + flink-{2.3.0,2.1.3,1.20.5}-nonreserved.txt (full release keyword lists).
