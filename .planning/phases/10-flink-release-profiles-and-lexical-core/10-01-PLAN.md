---
phase: 10-flink-release-profiles-and-lexical-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dialect/flink.mbt
  - api/api.mbt
  - binding/schema.mbt
  - binding/exports.mbt
  - fathom-sql/args.mbt
  - fathom-sql/run.mbt
  - lexer/lexer.mbt
  - scripts/extract_flink_lexical.py
  - parity/fixtures/flink-lexical/manifest.tsv
  - parity/flink_lexical_test.mbt
  - parity/__snapshot__/flink-lexical.hash-comment.flink-2.3.0.strict.json
  - parity/__snapshot__/flink-lexical.hash-comment.flink-2.3.0.editor.json
  - parity/__snapshot__/flink-lexical.hash-comment.doris-4.x.strict.json
  - parity/__snapshot__/flink-lexical.hash-comment.doris-4.x.editor.json
  - .planning/phases/10-flink-release-profiles-and-lexical-core/approved-changes.md
autonomous: false
requirements: [FLINK-01]
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 4
  confidence: low
must_haves:
  truths:
    - "A consumer can select flink-2.3.0 / flink-2.1.3 / flink-1.20.5 through the public API, CLI, and JS/Wasm selection path; an unsupported profile (e.g. `flink-9.9` or the Doris-shaped `2.1`) is rejected with the structured FATHOM-SCHEMA-003 profile error — no silent default and no Doris profile borrowing (D-01, SC1)."
    - "Each accepted Flink profile reports its release-derived contract: FlinkProfileMetadata carries calcite_version (flink-2.3.0→1.36.0, flink-2.1.3→1.34.0, flink-1.20.5→1.32.0) and parser_config (Lex.JAVA,identifierMaxLength=256,conformance=FlinkSqlConformance.DEFAULT) extracted from the pinned release archives, exposed on the wire via fathom.dialect.v1 (D-02, SC2)."
    - "A flink-selected document is lexed under the Flink lexical policy and parses to the explicit FATHOM-PARSE-008 not-implemented statement node — never a Doris fallback; a Flink lexical error (e.g. `#`) surfaces as a FATHOM-PARSE-003 lexical diagnostic with correct spans (DIALECT-03, SC3)."
    - "Doris behavior is byte-identical: the frozen 213-snapshot parity baseline shows zero drift, and the flink-lexical snapshot group is an independent namespace under parity/ with its own manifest (D-04, Pitfall 7)."
    - "[probe FLINK-01 adjacency] flagged-unverified: profile-id adjacency is exact-match only — `flink-2.3.0` resolves while `flink-2.3`, `flink-2.3.1`, and `flink-2.3.0-rc1` are rejected (closed enum from_id, no prefix/suffix/version-compare); the from_id exact-match tests pin this boundary."
    - "[probe FLINK-01 empty] flagged-unverified: empty Flink input keeps the Phase 9 single-router behavior (WINDOWS.md #4 tracks the silent-empty-document nuance); Phase 10 confirms the non-empty Flink contract (FATHOM-PARSE-008) and records the empty-input decision explicitly rather than silently changing it."
    - "[probe FLINK-01 ordering] flagged-unverified: wire profile enumeration order is stable (flink-2.3.0 primary first, then flink-2.1.3, then flink-1.20.5) in fathom.dialect.v1/fathom.capabilities.v1; classification lookup remains first-match-wins over the dialect's own rows; export-smoke assertions freeze the ordering."
  artifacts:
    - path: "dialect/flink.mbt"
      provides: "FlinkProfile closed enum (V2_3_0|V2_1_3|V1_20_5), FlinkProfileMetadata (id/release_family/exact_release/calcite_version/parser_config/feature_introduction), FlinkProfile::metadata()/from_id, flink_classification_rows (still [] this plan; filled in 10-03)"
    - path: "api/api.mbt"
      provides: "ParseOptions::new flink arm (FlinkProfile::from_id -> metadata -> DialectContext), flink profile metadata accessor for the wire, api tests updated (flink-2.3.0 no longer Err)"
    - path: "binding/schema.mbt + binding/exports.mbt"
      provides: "validate_dialect_profile flink arm, dialect_json/capabilities_json flink profile entries with calcite_version/parser_config (T-09-18 provenance discipline)"
    - path: "fathom-sql/args.mbt + fathom-sql/run.mbt"
      provides: "is_valid_dialect_profile flink arm, updated unknown-profile message listing flink-2.3.0|flink-2.1.3|flink-1.20.5, CLI tests"
    - path: "lexer/lexer.mbt"
      provides: "first dialect branch: `#` is a Comment under Doris (byte-identical) and an Error token under Flink (FATHOM-PARSE-003 lexical diagnostic)"
    - path: "scripts/extract_flink_lexical.py"
      provides: "Python-stdlib extractor/validator (check_keywords.py pattern): reads the pinned release POMs/grammar, validates the three calcite pins + parser config, emits the metadata constants and manifest rows"
    - path: "parity/fixtures/flink-lexical/manifest.tsv"
      provides: "per-profile provenance rows (fixture_id, profile, exact_release, calcite_version, parser_config, source_archive_url, sha512, git_tag, git_commit) from RESEARCH §7"
    - path: "parity/flink_lexical_test.mbt + parity/__snapshot__/flink-lexical.*"
      provides: "hash-comment conflict snapshot (same input under flink-2.3.0 vs doris-4.x tokenizes differently), the D-04 gate mechanism"
  key_links:
    - from: "dialect/flink.mbt FlinkProfileMetadata.calcite_version"
      to: "parity/fixtures/flink-lexical/manifest.tsv"
      via: "scripts/extract_flink_lexical.py validates both against the pinned release POM (flink-table/pom.xml:81) — one source of truth, no hand-written pin"
    - from: "binding/schema.mbt dialect_json flink entries"
      to: "dialect/flink.mbt FlinkProfileMetadata"
      via: "fathom.dialect.v1 profiles read calcite_version/parser_config from metadata only (T-09-18), never fabricated"
    - from: "parity/__snapshot__/flink-lexical.*"
      to: "moon test --package parity"
      via: "the existing D-08 byte gate (no --update in CI); Doris 213 snapshots stay byte-identical (Pitfall 7 independent namespace)"
  prohibitions:
    - "Calcite pins and parser-config metadata are taken ONLY from the sha512-verified pinned release archives (D-02) — never hand-written constants, Calcite folklore, or moving dev/stable docs (Pitfall 3)."
    - "No FATHOM-FLINK-* diagnostic namespace is minted and no string-keyed soft profile validation is introduced — unknown/unsupported flink profiles reject through the closed FlinkProfile enum with the existing FATHOM-SCHEMA-003 family (D-05, D-01)."
    - "moon test --update --package parity runs only after a matching approved-changes.md register entry is committed; the Doris 213-snapshot baseline stays byte-identical (D-04/D-08, Pitfall 7)."
    - "No silent Doris fallback for a flink selection: a flink-validated document lexes under Flink policy and parses to FATHOM-PARSE-008, never to Doris grammar (DIALECT-03)."
---

<objective>
**As a** consumer, **I want to** select an auditable Flink release profile (`flink-2.3.0` primary, `flink-2.1.3`/`flink-1.20.5` regression) whose Calcite/parser contract is extracted from the pinned release itself and whose lexical route is independent from Doris, **so that** I can trust the profile's provenance and tokenization before Flink grammar lands in Phase 11.

Purpose: Deliver the Phase 10 tracer slice — the thinnest end-to-end path that proves the whole Flink stack works: `fathom-sql parse --dialect flink --profile flink-2.3.0` on a fixture → Flink tokenization (one lexical conflict branch: `#`) → FATHOM-* diagnostics (FATHOM-PARSE-003 lexical + FATHOM-PARSE-008 grammar-not-implemented) → `fathom.parse.v1` envelope with `dialect=flink` + profile metadata (`calcite_version` via `fathom.dialect.v1`). Establishes the D-02 provenance mechanism (extract → metadata → manifest) and the D-04 flink-lexical snapshot namespace with the Doris 213-snapshot baseline kept byte-identical.
Output: FlinkProfile/FlinkProfileMetadata, the selection unlock at api/binding/CLI, the `#` lexical branch, `scripts/extract_flink_lexical.py`, `parity/fixtures/flink-lexical/manifest.tsv`, `parity/flink_lexical_test.mbt` + the hash-comment snapshots, and the Phase 10 approved-changes register.
</objective>

<execution_context>
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/workflows/execute-plan.md
@/root/.nvm/versions/node/v25.2.0/lib/node_modules/gsd-omp/node_modules/@opengsd/gsd-core/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-flink-release-profiles-and-lexical-core/10-CONTEXT.md
@.planning/phases/10-flink-release-profiles-and-lexical-core/10-RESEARCH.md
@.planning/phases/10-flink-release-profiles-and-lexical-core/10-PATTERNS.md

This is the phase's wave-1 tracer. D-01 (FlinkProfile closed enum + metadata, costly) and D-02 (Calcite-pin provenance, one-way) drive the identity; D-05 (FATHOM-SCHEMA error family, one-way) drives the rejection surface. The Doris analog is `dialect/doris.mbt` (DorisProfile/ProfileMetadata/metadata()/from_id, :13-26/:113-127/:183-190) — FlinkProfile/FlinkProfileMetadata are D-01-isomorphic. The unlock points are `api/api.mbt:79-100` (ParseOptions::new flink arm), `binding/schema.mbt:40-51` (validate_dialect_profile), `binding/schema.mbt:169-246` (dialect_json/capabilities_json flink `[]`), `fathom-sql/args.mbt:150-156` (is_valid_dialect_profile), and `lexer/lexer.mbt:277-283` (the `#` comment branch). The two checkpoint:decision gates precede the tasks that open the one-way doors (reversibility gates for D-02/D-05). The research pin table (§7) gives the exact calcite pins (1.36.0/1.34.0/1.32.0 from `flink-table/pom.xml:81`), the parser-config matrix (§8) is identical across the three releases, and §9 confirms `#` has no Calcite token (lexical error) while Doris treats it as a line comment (`lexer.mbt:277` byte==35) — the tracer's single lexical branch.

> **Contribution hooks:** (1) API Coverage checkpoint — NOT detected: Phase 10 integrates NO external service API; the Flink release archives are research-time fixtures (never a runtime dependency), so no COVERAGE.md is produced. (2) Assumption-Delta scan — `detected: false` (no pluralization/optional/chosen signals): no promote-vs-add-alongside decision is required; Flink profile identity is the locked D-01 enum addition over Phase 9's dialect generalization, not an assumption-driven promotion. (3) Schema Push gate — no ORM schema files in this MoonBit parser project; skipped silently.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm D-02 Calcite-pin provenance contract (one-way door)</name>
  <decision>The Calcite-pin / parser-config provenance contract: FlinkProfileMetadata.calcite_version and parser_config come ONLY from the sha512-verified pinned release archives (flink-2.3.0→1.36.0, flink-2.1.3→1.34.0, flink-1.20.5→1.32.0), with tag/commit/URL/sha512 carried in the parity fixture manifest — never hand-written inference or Calcite folklore.</decision>
  <context>The RESEARCH §7 table verified every pin verbatim from the release POM (`src/flink-{v}/flink-table/pom.xml:81 — <calcite.version>…</calcite.version>`) and §8 verified the parser config (`PlannerContext.java:256-260 — SqlParser.config().withParserFactory(...).withConformance(conformance).withLex(Lex.JAVA).withIdentifierMaxLength(256)`). SC2 explicitly requires the exact 2.1.3 pin to be extracted from that release, not inferred. These metadata fields are part of the wire contract (fathom.capabilities.v1 / fathom.dialect.v1), so a wrong or folklore-derived pin is a schema-level error that would mislead every downstream consumer. This is a one-way door because publishing a pin that later proves wrong requires a schema-level change record.</decision>
  <options>
    <option id="option-a">
      <name>Release-archive extraction per D-02 (recommended — locked decision)</name>
      <pros>Every pin is auditable against the pinned archive + SHA-512; SC2 is literally satisfied; the extract script makes re-verification mechanical.</pros>
      <cons>The extract script must read the archives (available at /tmp/flink-research/, already checksum-verified) before metadata is finalized.</cons>
    </option>
    <option id="option-b">
      <name>Hand-written constants from the research table</name>
      <pros>Faster to land.</pros>
      <cons>Violates D-02 and SC2 auditability; a typo becomes a wire-contract error that the manifest cannot catch.</cons>
    </option>
  </options>
  <resume-signal>Select option-a (release-archive extraction) or describe a modified provenance contract; metadata constants are authored only after this confirmation.</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Confirm D-05 error-family contract (one-way door)</name>
  <decision>Unknown/unsupported Flink profile rejection reuses the existing FATHOM-SCHEMA-* profile error family (FATHOM-SCHEMA-003 for UnsupportedProfile, FATHOM-SCHEMA-007 for UnknownDialect); NO new FATHOM-FLINK-* diagnostic namespace is minted, and dialect is expressed only in metadata fields.</decision>
  <context>D-05 continues Phase 9's D-10 decision (dialect is never encoded into a code prefix). The code mapping already exists in `binding/schema.mbt:138-150` (UnsupportedProfile→FATHOM-SCHEMA-003, UnknownDialect→FATHOM-SCHEMA-007) and the Phase 9 register minted FATHOM-PARSE-008 and FATHOM-SCHEMA-007 but deliberately not a Flink-specific family. Publishing a new FATHOM-FLINK-* family would change the stable diagnostic contract (schema migration required after release). The Phase 10 change is only that flink profiles move from "all rejected" to "three accepted"; the rejection code path is unchanged.</decision>
  <options>
    <option id="option-a">
      <name>Reuse FATHOM-SCHEMA-003/007 unchanged (recommended — locked decision)</name>
      <pros>Stable public contract; zero new codes; dialect rides in result/error metadata fields (DIALECT-04).</pros>
      <cons>Consumers must read the dialect field to distinguish a flink unknown-profile from a doris unknown-profile.</cons>
    </option>
    <option id="option-b">
      <name>Mint FATHOM-FLINK-001..N</name>
      <pros>Dialect-specific code readability.</pros>
      <cons>Breaks the D-10 "no dialect in code prefix" rule; requires a schema migration; contradicts the locked D-05.</cons>
    </option>
  </options>
  <resume-signal>Select option-a (reuse FATHOM-SCHEMA family) or describe a modified error surface; the selection-unlock task proceeds only after this confirmation.</resume-signal>
</task>

<task type="tracer">
  <name>End-to-end flink-2.3.0 selection + lexical route through dialect/api/binding/CLI/lexer</name>
  <files>dialect/flink.mbt, api/api.mbt, binding/schema.mbt, binding/exports.mbt, fathom-sql/args.mbt, fathom-sql/run.mbt, lexer/lexer.mbt</files>
  <read_first>
    @.planning/phases/10-flink-release-profiles-and-lexical-core/10-CONTEXT.md
    @.planning/phases/10-flink-release-profiles-and-lexical-core/10-RESEARCH.md
    @.planning/phases/10-flink-release-profiles-and-lexical-core/10-PATTERNS.md
    @/opt/source/Fathom/dialect/doris.mbt
    @/opt/source/Fathom/dialect/flink.mbt
    @/opt/source/Fathom/api/api.mbt
    @/opt/source/Fathom/binding/schema.mbt
    @/opt/source/Fathom/binding/exports.mbt
    @/opt/source/Fathom/lexer/lexer.mbt
    @/opt/source/Fathom/fathom-sql/args.mbt
    @/opt/source/Fathom/fathom-sql/run.mbt
  </read_first>
  <action>Replace the Phase 9 placeholder in dialect/flink.mbt with the D-01-closed enum and metadata: `pub(all) enum FlinkProfile { V2_3_0; V2_1_3; V1_20_5 } derive(Eq, @debug.Debug)` and `pub struct FlinkProfileMetadata { pub id : String; pub release_family : String; pub exact_release : String; pub calcite_version : String; pub parser_config : String; pub feature_introduction : String }`. Implement `FlinkProfile::metadata()` per-variant with the RESEARCH §7/§8 values: V2_3_0 → id "flink-2.3.0", release_family "2.x", exact_release "flink-2.3.0", calcite_version "1.36.0", parser_config "Lex.JAVA,identifierMaxLength=256,conformance=FlinkSqlConformance.DEFAULT", feature_introduction "primary profile"; V2_1_3 → calcite_version "1.34.0" (extracted from the 2.1.3 release per D-02); V1_20_5 → calcite_version "1.32.0". Implement `FlinkProfile::from_id(id : String) -> FlinkProfile?` exact-matching "flink-2.3.0"/"flink-2.1.3"/"flink-1.20.5" and nothing else (adjacency probe: no prefix/version-compare; the Doris-shaped "2.1" must NOT resolve). Unlock `api/api.mbt` `ParseOptions::new` (`:79-100`): the `@dialect.Dialect::Flink` arm becomes `FlinkProfile::from_id(profile_id)` → `metadata()` → `{ dialect: @dialect.Dialect::Flink, profile_id, exact_release: metadata.exact_release, feature_introduction: metadata.feature_introduction }`; unknown profile → `Err(UnknownProfile(profile_id~))`. Add a `ParseOptions::flink_profile_metadata(self) -> @dialect.FlinkProfileMetadata` accessor (mirror of the Doris `profile_metadata()` panic-on-non-resolvable convention) so binding can read calcite_version/parser_config for the wire. Unlock `binding/schema.mbt` `validate_dialect_profile` (`:40-51`): the `"flink"` arm becomes `match profile { "flink-2.3.0" | "flink-2.1.3" | "flink-1.20.5" => Ok(()) _ => Err(UnsupportedProfile(profile~)) }` — code mapping (:138-150) untouched per D-05. Fill the `dialect_json` flink arm (`:196-211`) and `capabilities_json` flink entry (`:234-241`): build the three profile entries through `@api.ParseOptions::new("flink", profile, "strict")` + `flink_profile_metadata()` (T-09-18 — never fabricated), adding `calcite_version` and `parser_config` fields to the fathom.dialect.v1 profile objects and listing the three ids in fathom.capabilities.v1. Unlock `fathom-sql/args.mbt` `is_valid_dialect_profile` (`:150-156`): add `"flink" => id == "flink-2.3.0" || id == "flink-2.1.3" || id == "flink-1.20.5"`. Update `fathom-sql/run.mbt` `parse_error_outcome`/`usage_error_message` flink message from "flink has no released profiles yet (Phase 9)" to list the actual values (e.g. "expected flink-2.3.0, flink-2.1.3, or flink-1.20.5") — exit 2 matrix unchanged (D-39). Add the ONE lexical branch in `lexer/lexer.mbt` (`:277-283`): in the line-comment branch, `match context.dialect` — under `Dialect::Flink`, byte 35 (`#`) becomes an `@token.TokenKind::Error` token with a diagnostic code (e.g. LEX_UNSUPPORTED_HASH, which the parser's FATHOM-PARSE-003 path at parser/parser.mbt:3563-3570 surfaces as "invalid source encoding or unterminated lexical material"); the Doris arm (including `--` and `#`) stays byte-identical. Do NOT touch `parse_flink_segment` (parser/parser.mbt:3405) — Flink statement grammar remains FATHOM-PARSE-008 in Phase 10. Update the api test `api_requires_explicit_dialect_profile_and_mode` (`api.mbt:636-640`): `ParseOptions::new("flink", "flink-2.3.0", "strict")` is now `Ok(_)` while `("flink", "2.1", "strict")` and `("flink", "flink-9.9", "strict")` stay `Err(UnknownProfile(..))`. Add export-smoke assertions in `binding/exports.mbt` test scope: `fathom_dialect_v1("flink")` output contains flink-2.3.0 with calcite_version "1.36.0" and the parser_config string; `fathom_capabilities_v1()` flink profiles array is non-empty with exactly ["flink-2.3.0","flink-2.1.3","flink-1.20.5"] in that order (ordering probe). Extend `fathom-sql/cli_test.mbt` with the flink acceptance cases: `--dialect flink --profile flink-2.3.0` on "SELECT 1" exits 0 with a fathom.parse.v1 envelope carrying dialect flink, profile flink-2.3.0, exact_release flink-2.3.0, and the FATHOM-PARSE-008 diagnostic; `--dialect flink --profile 2.1` exits 2; `--dialect flink --profile flink-9.9` exits 2 with the updated message.</action>
  <acceptance_criteria>
    - `ParseOptions::new("flink", "flink-2.3.0", "strict")` returns Ok with dialect_context { dialect Flink, profile_id "flink-2.3.0", exact_release "flink-2.3.0" }; `("flink", "2.1", "strict")` and `("flink", "flink-2.3.0-rc1", "strict")` return Err(UnknownProfile(..)) — no Doris profile borrowing, exact-match only.
    - `validate_dialect_profile("flink", "flink-2.3.0")` is Ok; `validate_dialect_profile("flink", "2.1")` is Err(UnsupportedProfile); `validate_dialect_profile("mysql", "flink-2.3.0")` is Err(UnknownDialect) — dialect-first validation unchanged.
    - `fathom_dialect_v1("flink")` returns a fathom.dialect.v1 envelope with three profile entries carrying calcite_version "1.36.0"/"1.34.0"/"1.32.0" and parser_config "Lex.JAVA,identifierMaxLength=256,conformance=FlinkSqlConformance.DEFAULT" in order flink-2.3.0, flink-2.1.3, flink-1.20.5; `fathom_capabilities_v1()` lists exactly those three under flink.
    - CLI: `printf 'SELECT 1 -- hi' | _build/native/release/build/fathom-sql/fathom-sql.exe parse --dialect flink --profile flink-2.3.0` exits 0 with the fathom.parse.v1 envelope (dialect flink, profile flink-2.3.0, FATHOM-PARSE-008 diagnostic); `--dialect flink --profile 2.1` exits 2; the stderr message lists flink-2.3.0/flink-2.1.3/flink-1.20.5, not "no released profiles yet".
    - Lexer: `lex_with_limit` under a Flink context over "a # comment" produces an Error token for `#` (diagnostic code set → FATHOM-PARSE-003 surfaces through the parser); the same input under a Doris context produces a Comment token byte-identically.
    - Doris baseline zero-drift: `moon test --package parity` (no --update) passes and `python3 scripts/baseline_diff.py` reports zero unexpected diffs; no `--update` was run.
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native --package api --package dialect --package binding --package fathom-sql --package lexer && moon build --target native --release && printf 'SELECT 1 -- hi' | _build/native/release/build/fathom-sql/fathom-sql.exe parse --dialect flink --profile flink-2.3.0 >/dev/null 2>&1 && (printf 'SELECT 1' | _build/native/release/build/fathom-sql/fathom-sql.exe parse --dialect flink --profile 2.1 >/dev/null 2>&1; test $? -eq 2) && moon test --package parity</automated>
  </verify>
  <done>The flink-2.3.0/2.1.3/1.20.5 selection works end-to-end through API/binding/CLI with auditable calcite_version/parser_config metadata, the `#` lexical conflict is dialect-routed (Flink Error vs Doris Comment), Flink statement grammar still routes to FATHOM-PARSE-008, and the Doris 213-snapshot baseline is byte-identical.</done>
</task>

<task type="auto">
  <name>Provenance extractor + flink-lexical manifest + hash-comment snapshot gate</name>
  <files>scripts/extract_flink_lexical.py, parity/fixtures/flink-lexical/manifest.tsv, parity/flink_lexical_test.mbt, parity/__snapshot__/flink-lexical.hash-comment.*, .planning/phases/10-flink-release-profiles-and-lexical-core/approved-changes.md</files>
  <read_first>
    @.planning/phases/10-flink-release-profiles-and-lexical-core/10-RESEARCH.md
    @.planning/phases/10-flink-release-profiles-and-lexical-core/10-PATTERNS.md
    @/opt/source/Fathom/corpus/tools/check_keywords.py
    @/opt/source/Fathom/parity/baseline_test.mbt
    @/opt/source/Fathom/corpus/manifest.tsv
    @/opt/source/Fathom/scripts/baseline_diff.py
  </read_first>
  <action>Create `scripts/extract_flink_lexical.py` as a Python-stdlib-only validator/extractor following the `corpus/tools/check_keywords.py` shape (problems list + per-line report + non-zero exit + trailing "ok: N ..." line). It reads the pinned release archives under /tmp/flink-research/src/flink-{2.3.0,2.1.3,1.20.5}/ (research-time fixtures, never shipped): `flink-table/pom.xml` line 81 (`<calcite.version>`), `flink-table/flink-sql-parser/pom.xml` (vanilla calcite-core dep), `.../PlannerContext.java` lines 252-260 (parser config), and validates the three pins/parser-config against RESEARCH §7/§8, printing "error: ..." and exiting 1 on any mismatch. Create `parity/fixtures/flink-lexical/manifest.tsv` with one header row and three profile rows carrying exactly the §7 provenance values: fixture_id, profile, exact_release, calcite_version, parser_config, source_archive_url, sha512, git_tag, git_commit (flink-2.3.0 → calcite 1.36.0, archive https://archive.apache.org/dist/flink/flink-2.3.0/flink-2.3.0-src.tgz, sha512 b189214e5b8b45c3f76bb296550f5ae49f711541d70ce2054787571dcbd026bf539b3b90ac902917523a6e894619c0df93210315f64bd0d6244c8328eb5d1cbd, tag release-2.3.0, commit c0f8d1a1e09f209885a88f9c19ceb9d9e9870283; 2.1.3 → 1.34.0 / a1a35ee35d5d417b7445de1648510060d2c3281f7185eefe4d2fc74b9c77821b89bbc9de55f47dbf2053d20d87fbc4c5d90af9bba4fd9d9c75d0f3ce44d8b3fe / release-2.1.3 / 6cda56b084d5c337b36d2f8ed464bc92093b0a34; 1.20.5 → 1.32.0 / ce11ae5a81a2ba16d1c5f22e5025a743bccff4f54a3b9836e58956ac98720c73db5a1c4ab7c87436c6fecc2c5dd8dca2514acc29d5e1c36334c41265729e3add / release-1.20.5 / 0980485040c6dbec3a1b32da05c4b655cf01ad2b). Create `parity/flink_lexical_test.mbt` mirroring the `parity/baseline_test.mbt` snapshot pattern (@test.T::snapshot): a small fixture array starting with the D-06 hash-comment conflict — raw b"SELECT a # comment" snapshotted under flink-2.3.0 (Flink: `#` is an Error token → FATHOM-PARSE-003 lexical diagnostic + FATHOM-PARSE-008 statement) and under doris-4.x (Doris: `#` is a Comment token, unchanged) — filename form flink-lexical.{fixture}.{profile}.{strict|editor}.json (independent namespace, Pitfall 7; never overlaps the Doris group). Create the Phase 10 approved-changes register `.planning/phases/10-flink-release-profiles-and-lexical-core/approved-changes.md` documenting the flink-lexical group as an independent new-file namespace (Doris 213 untouched; any future Doris diff requires a register entry). Sequence per D-04/D-08: commit the register FIRST, then run `moon test --update --package parity` exactly once to materialize the flink-lexical snapshots, then confirm `moon test --package parity` (no --update) is the genuine gate.</action>
  <acceptance_criteria>
    - python3 scripts/extract_flink_lexical.py exits 0 and prints an "ok:" line asserting the three calcite pins (1.36.0/1.34.0/1.32.0) and the identical parser config against the pinned release POMs; a deliberately wrong pin makes it exit 1.
    - parity/fixtures/flink-lexical/manifest.tsv exists with 3 profile rows whose sha512/git_tag/git_commit match RESEARCH §7 exactly; the Doris corpus manifest is untouched.
    - parity/flink_lexical_test.mbt snapshot tests exist and parity/__snapshot__/flink-lexical.hash-comment.flink-2.3.0.{strict,editor}.json and flink-lexical.hash-comment.doris-4.x.{strict,editor}.json exist; the flink snapshot shows the FATHOM-PARSE-003/#-error path and the doris snapshot shows the unchanged comment path (different tokenization, explainable per D-06).
    - .planning/phases/10-flink-release-profiles-and-lexical-core/approved-changes.md is committed BEFORE the single --update run; python3 scripts/baseline_diff.py reports 0 unexpected diffs; the Doris 213 snapshots are byte-identical (git diff shows no doris-named snapshot changed).
    - moon test --package parity passes without --update after generation.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/extract_flink_lexical.py && python3 -m py_compile scripts/extract_flink_lexical.py && test -s parity/fixtures/flink-lexical/manifest.tsv && moon test --package parity && python3 scripts/baseline_diff.py --left parity/__snapshot__ --right parity/__snapshot__ --approve .planning/phases/10-flink-release-profiles-and-lexical-core/approved-changes.md</automated>
  </verify>
  <done>The D-02 provenance mechanism (extract → manifest → metadata) is executable and audit-gated; the flink-lexical snapshot namespace exists with the hash-comment conflict frozen; Doris baseline is byte-identical.</done>
</task>

</tasks>

## Source Audit Coverage

| SOURCE | ID | Feature or constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | select auditable Flink release profiles with lexical behavior independent from Doris | 10-01 | COVERED | Tracer: identity + selection + one lexical branch end-to-end. |
| REQ | FLINK-01 | flink-2.3.0 primary + 2.1.3/1.20.5 regression profiles, release-derived Calcite/config, unsupported rejected | 10-01 | COVERED | Tasks 1-3 (profile metadata + unlock + rejection); Task 4 (provenance manifest). |
| CONTEXT | D-01 | FlinkProfile closed enum + FlinkProfileMetadata (id/release_family/exact_release/calcite_version/parser_config/feature_introduction), validate_dialect_profile unified | 10-01 | COVERED | Task 3 enum/metadata + binding unlock. |
| CONTEXT | D-02 | Calcite/parser config from pinned release archives, 2.1.3 pin from its own release, executable script/test | 10-01 | COVERED | Task 1 checkpoint (one-way) + Task 3 metadata + Task 4 extract script/manifest. |
| CONTEXT | D-05 | unknown/unsupported flink profile via FATHOM-SCHEMA-* family, no FATHOM-FLINK-* | 10-01 | COVERED | Task 2 checkpoint (one-way) + Task 3 reuse of 003/007 mapping. |
| CONTEXT | D-03 | lexical behavior from executable release fixtures, `#` per release | 10-01 | PARTIAL (one branch) | Task 3 `#` branch; full quoting/literals/operators in 10-02. |
| CONTEXT | D-04 | flink-lexical snapshot group + conflict matrix, Doris zero-drift | 10-01 | PARTIAL (namespace + first fixture) | Task 4; full matrix in 10-02. |
| RESEARCH | §7 pin table | 1.36.0/1.34.0/1.32.0 + sha512/tag/commit | 10-01 | COVERED | Tasks 3-4. |
| RESEARCH | §8 parser config | Lex.JAVA + 256 + DEFAULT identical across releases | 10-01 | COVERED | Task 3 metadata + Task 4 manifest. |
| RESEARCH | §9 lexical matrix | `#` not a Calcite token; Doris `#` comment | 10-01 | COVERED (branch) | Task 3. |
| RESEARCH | §Pattern 2/5, Pitfall 3/5/6 | validate unlock; extract pipeline; moving docs; half-filled metadata; profile-id shape | 10-01 | COVERED | Tasks 1-4; prohibitions. |
| PROBE | FLINK-01 adjacency | profile-id exact match vs prefix/version-adjacent | 10-01 | FLAGGED | truth + from_id exact-match tests. |
| PROBE | FLINK-01 empty | empty Flink input contract | 10-01 | FLAGGED | truth (WINDOWS.md #4 continuation). |
| PROBE | FLINK-01 ordering | wire profile enumeration order stable | 10-01 | FLAGGED | truth + export-smoke ordering assertion. |

<verification>
Run moon test --target native --package api --package dialect --package binding --package fathom-sql --package lexer after Task 3 and moon test --package parity (no --update) after each snapshot-affecting step. Inspect: FlinkProfileMetadata values match RESEARCH §7 (1.36.0/1.34.0/1.32.0, identical parser_config), validate_dialect_profile flink arm, dialect_json/capabilities_json flink entries with calcite_version/parser_config, CLI flink acceptance + exit-2 matrix, the `#` lexer branch (Flink Error vs Doris Comment byte-identical), the api/export/cli test updates, the extract script ok-line, the manifest rows, the flink-lexical hash-comment snapshots, and the approved-changes register committed before the single --update. Do not install packages, do not touch parser/parse_flink_segment (grammar is Phase 11), do not change any Doris profile id or behavior, and do not run moon test --update without a committed register entry.
</verification>

<success_criteria>
- A consumer can select flink-2.3.0/2.1.3/1.20.5 end-to-end (API/CLI/JS-Wasm) with auditable calcite_version/parser_config from the pinned releases; unsupported profiles reject with FATHOM-SCHEMA-003.
- A flink document lexes under the Flink policy (first conflict branch `#` proven) and parses to FATHOM-PARSE-008 — no Doris fallback.
- The D-02 extractor + manifest and the D-04 flink-lexical snapshot namespace exist; Doris 213-snapshot baseline is byte-identical.
</success_criteria>

<output>Create `.planning/phases/10-flink-release-profiles-and-lexical-core/10-01-SUMMARY.md` when this plan executes.</output>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dialect/profile strings -> api ParseOptions | Untrusted selection strings cross here; must be closed-enum validated (D-01), never soft-matched or defaulted. |
| flink context -> lexer/parser | A flink-selected document must lex under Flink policy and parse to FATHOM-PARSE-008 — never Doris grammar (DIALECT-03). |
| release archives -> metadata/manifest | Provenance crosses from pinned archives into wire metadata; a fabricated pin misleads every consumer (D-02). |
| parity snapshots -> CI gate | The flink-lexical group and the Doris 213 baseline must stay byte-identical unless registered (D-04/D-08). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-01 | Tampering | Flink profile string injection (`flink-2.3.0; DROP...`, `flink-2.3`, `2.1`) | high | mitigate | Closed FlinkProfile enum + from_id exact-match ("flink-2.3.0"/"flink-2.1.3"/"flink-1.20.5" only); unknown → UnknownProfile → FATHOM-SCHEMA-003; CLI/API tests cover the boundary set (Task 3). |
| T-10-02 | Tampering | Lexical dialect confusion (`#` accepted as comment under Flink) | high | mitigate | lexer branches by context.dialect; Doris arm byte-identical; conflict snapshot freezes Flink Error vs Doris Comment (Task 3-4). |
| T-10-03 | Tampering | Calcite pin / parser-config fabrication (2.1.3 pin hand-inferred) | high | mitigate | D-02 extract script reads pinned release POMs; manifest records sha512/tag/commit; Task 1 one-way checkpoint (D-02). |
| T-10-04 | Tampering | Doris baseline drift during flink unlock | high | mitigate | parity gate without --update; approved-changes register committed before any update; baseline_diff.py 0 unexpected (Task 4, D-04). |
| T-10-05 | Spoofing | Neutral-message regression (flink rejection advertising doris values or "no released profiles yet") | medium | mitigate | run.mbt flink message updated to flink-2.3.0\|flink-2.1.3\|flink-1.20.5; CLI exit-2 tests assert the text. |
| T-10-06 | Tampering | flink-lexical snapshots merge into the Doris namespace | medium | mitigate | Independent flink-lexical naming + Phase 10 register documenting the namespace boundary (Pitfall 7); baseline_diff.py per-group diff. |
| T-10-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies (10-RESEARCH Package Legitimacy Audit N/A); extract script is Python stdlib. |
</threat_model>

## Artifacts this phase produces

Files: dialect/flink.mbt (modified — enum + metadata + from_id), api/api.mbt (modified — Flink arm + flink_profile_metadata + tests), binding/schema.mbt + binding/exports.mbt (modified — validate + dialect/capabilities entries + export smoke), fathom-sql/args.mbt + fathom-sql/run.mbt (modified — flink acceptance + message + cli_test), lexer/lexer.mbt (modified — `#` branch), scripts/extract_flink_lexical.py (new), parity/fixtures/flink-lexical/manifest.tsv (new), parity/flink_lexical_test.mbt (new), parity/__snapshot__/flink-lexical.hash-comment.*.json (new, generated), .planning/phases/10-flink-release-profiles-and-lexical-core/approved-changes.md (new register).

Symbols (dialect/flink.mbt): FlinkProfile { V2_3_0 | V2_1_3 | V1_20_5 }, FlinkProfileMetadata { id, release_family, exact_release, calcite_version, parser_config, feature_introduction }, FlinkProfile::metadata(), FlinkProfile::from_id(id) -> FlinkProfile?.
Symbols (api/api.mbt): ParseOptions::new flink arm, ParseOptions::flink_profile_metadata(), updated api_requires_explicit_dialect_profile_and_mode test.
Symbols (binding/schema.mbt): validate_dialect_profile flink arm, dialect_json flink profile entries with calcite_version/parser_config, capabilities_json flink ["flink-2.3.0","flink-2.1.3","flink-1.20.5"].
Symbols (fathom-sql/args.mbt + run.mbt): is_valid_dialect_profile flink arm, updated unknown-profile message.
Symbols (lexer/lexer.mbt): `#` dialect branch under context.dialect (Flink Error vs Doris Comment).
Symbols (scripts/extract_flink_lexical.py): CLI with no external args, problems/ok reporting, exit 0/1.
Symbols (parity/flink_lexical_test.mbt): flink-lexical fixture array + @test.T::snapshot tests; parity/__snapshot__/flink-lexical.hash-comment.{flink-2.3.0,doris-4.x}.{strict,editor}.json.
