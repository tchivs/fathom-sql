// Single source of product version identity (Phase 15, D-01). Imported by the
// native executables fathom-sql and fathom-lsp only; the FFI helpers are cfg'd
// to native so js/wasm builds never pull this package in.
pkgtype(kind: "library")

import {
  "moonbitlang/core/encoding/utf8",
}
