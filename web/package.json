{
  "name": "@fathom-sql/sql-web-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Offline Monaco host for the Fathom SQL parser facade",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test",
    "build": "node scripts/offline-smoke.mjs --offline"
  },
  "dependencies": {
    "monaco-editor": "0.56.0"
  }
}
